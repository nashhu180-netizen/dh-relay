# dogfood·decision — 假需求（一屏）

目标：在运行现场 `work/` 目录下产出三个文件：
- `A.txt`：一句问候语，**语言由用户当场二选一决定（中文 / 英文）**——这是必须挂起等人的决策点。
- `B.txt`：一行 `B-DONE-<时间戳>`，与 A 无关，可并行。
- `C.txt`：把 `A.txt` 的内容原样抄一遍再加一行 `C-DONE`，依赖 A。

验收：三个文件都在；C.txt 第一行 == A.txt 第一行；A.txt 的语言与用户回答一致。

说明：dh-relay DHR_03 dogfood 夹具，演练用；不涉及真实业务。
