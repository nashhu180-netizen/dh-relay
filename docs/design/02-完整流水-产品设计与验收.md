# DH Relay P2 · 完整流水 —— 产品设计与验收

<!-- dh:topic tier=标准 review=完整流水 -->
<!-- dh:planning-event:v1 id=DHR-A-03 stage=A-full artifact=design/02-完整流水-产品设计与验收.md review=evidence/02-交叉审核记录-完整流水.md#review-a3 understanding=evidence/02-交叉审核记录-完整流水.md#understanding-a3 -->

## 文档信息

| 项目 | 内容 |
|---|---|
| 所属模块 | `dh-relay` |
| 文档类型 | 正式产品设计与验收（P2，与 `01-产品设计与验收.md` 共同组成正式设计输入包；P1 契约仍以 01 为准，本文只扩不改） |
| 当前状态 | A 方案已确认（2026-08-16 用户整版确认）；不授权 B 拆计划或任何卡开工 |
| 编辑模型 | Claude（Fable 5） |
| 最后更新 | 2026-08-16 |

> 承接：[design/01 §1.4 目标形态](./01-产品设计与验收.md)、DHR_03 销户记录 H2 方向决策账、[真实业务试点 IHSR_05](../workspace/DHR_03/evidence/H2-真实业务试点-IHSR_05.md)（RB-1～RB-5）、DHR_03 findings F-002/F-019/F-023；审核与裁决见 [evidence/02](./evidence/02-交叉审核记录-完整流水.md)。
## 1. 人话版目标

### 1.1 我们在做什么

我理解你要的是：P1 已经证明"没有常驻 AI 主控，靠文件状态也能接棒"，IHSR_05 也证明一张真实业务卡能在 relay 上跑完六棒。但那一趟还是靠主控会话手写计划、拼包装、盯窗口、亲跑 oracle、亲做收口。**P2 要把主控还在干的活搬进 relay 本身**：

- **relay 只管"已经立项、已经拆好计划、你在对话里授权开工的任务卡"**——立项（A）与拆计划（B）不在 relay 里，仍由你和主会话按 dev-harness 走。
- 你在**任意仓库**执行一次 `relay start`，指定这次授权的 **2～3 张卡**（一份接力计划带一批卡，先试验多卡）；之后从建工作区、施工、机器闸、两轮代码复核、集成与端到端、需求/一致性复核、教训复核，到收口销户，全程由确定性 Runner 按接力计划接棒，每张卡各自 verify 销户。
- 你只在**agent 弹出的窗口里说话**：落户确认、偶尔的决策、真机端到端协作、收口确认、异常时的诊断问答；不敲命令行、不依赖飞书。
- 终端全关、隔天再来：磁盘上的接力计划和进度都在，`relay` 打开控制台选 run 续跑，断掉的棒由诊断 agent 带你接上。

一句话：**P2 交付"一批已授权任务卡从落户到销户的无常驻 AI 主控接力"，可从任意仓发起、可跨天接续，并用真实卡自举跑通。**

> 措辞约定（审核 U2）："无常驻 AI 主控"不等于没有控制面——Runner/宿主、接力计划生成 agent、汇合 agent、诊断 agent 与 finalizer 脚本仍构成控制面，只是不再由一个常驻 AI 会话承担。

### 1.2 与 P1 / IHSR_05 试点的差异

| 项 | P1 PoC | IHSR_05 试点 | P2 目标 |
|---|---|---|---|
| 从哪发起 | dh-crew 仓内 `run-dogfood.ps1` | 业务仓自写 `run-relay.ps1`，现场却落在 dh-crew | `relay` 命令在 PATH，任意仓执行；运行现场落**该仓** `.dh-runtime/relay/<run_id>/`；注册表与 run 索引在用户级 `~/.dh-relay/` |
| 一次跑多少 | 假需求 A/B/C | 一张卡 | 一份接力计划带 **2～3 张已授权卡**（试验多卡），按 DevPlan 依赖串/并行，每卡独立走完流水并 verify |
| 计划从哪来 | headless 编排 agent 写 v1 | 主控手写 plan-v1 | 一次性**接力计划生成 agent**按标准流水模板实例化（只填槽位：卡、brief 引用、profile_id、script_spec_id、开关、返工上限）；也允许手动 `propose`（RB-2）。通用自由规划留 P3 |
| 节点角色 | worker / reviewer / replanner | build / review×N / fix | 沿用三个 + 新增 `planner / workspace / build / fix / review_lead / integration / requirement_review / lesson_review / closer / diagnoser / gate(脚本) / finalizer(脚本)` |
| 复核 | 无 | 两轮 fresh 代码复核 | **三路复核**显式成棒：代码复核 R1 → fresh R2 → 汇合；需求与一致性复核；教训复核 |
| 机器闸 | 无 | 主控亲跑 oracle | **脚本节点**：宿主按注册表 script_spec 执行、不起 agent，结果是机器事实（`gate_outcome`） |
| 真机端到端 | 无 | 无 | 可选棒 **集成与端到端 agent**：合到 test 分支、跑 e2e；需要真机时在窗口给你命令、你回贴输出（P1 §4.2 多轮配合语义） |
| 每棒用谁跑 | 默认 claude | 包装写死 cwd/账号 | 计划只引用 `profile_id`；用户级注册表定义 cli/配置目录/工作目录根/模型/fallback；支持 codex（含 ninth 账号） |
| 额度耗尽 | 一律 `interrupted_unknown` | 靠人换 | 高置信识别才换预登记 fallback；否则 paused → 诊断 agent 问你 |
| 异常处理 | 用户手动触发诊断 | 主控人肉 | Runner **自动拉起只读诊断 agent**，它在窗口告诉你现场并问"续跑/换账号/放弃"，替你发控制事件 |
| 用户怎么知道该出手 | 看窗口 / 主控汇报 | 主控对话约 12 条汇报 | 只看弹出的窗口 + `relay`/`relay status` 控制台；飞书通知留 P3；主控对话消息=0 |
| 收口 | 无 | 主控亲做 E4～E13 | 收口 agent 备料 + 在窗口问你"认可本地收口？" → 你答"认可" → 它调 **finalizer 脚本**（暂存分支 squash/复验/回填/verify → 一次快进 master → 清树） |
| 接续 | 同 RunId 续（实证） | 同左 | `relay` 控制台列全部未完成 run；续跑时断棒自动 `interrupted_unknown` → 诊断 agent（登录自启留 P3） |

### 1.3 关键决策（动这些要重新讨论）

1. **relay 的边界 = 已授权任务卡**：输入是 DevPlan 里状态可开工、用户对话授权的卡；不做立项/拆计划；不改 DevPlan 任务的目标/验收（只回填状态与 SHA）。
2. **流水就是 dev-harness 节点表的接力版**：S0～S3（落户）→ 施工 → E2 代码复核（两轮换人）→ 集成/需求境证据 → E4 需求复核 → E6 教训复核 → E5～E10 备料 → E11 用户确认 → E12～E13 verify/销户。relay 不发明第二套流程；`review.md`/`progress.md`/证据账本仍是 dev-harness 工件，由对应棒写。
3. **Runner 仍不判质量**：汇合 agent 写 `verdict`（`approve / rework / escalate`），Runner 只按枚举分支，并强制"缺任一复核棒结果不放行、返工轮次有界"。
4. **脚本节点是机器事实的唯一来源**：dh check、测试、e2e 脚本、finalizer 以脚本节点跑；宿主脚本执行器扮演该 attempt 的 worker（receipt backend=script），只写宿主私有 candidate，Runner 照常校验晋级；结果带 `gate_outcome=pass|fail|error`；**不新增 result_status**（审核 S1）。
5. **返工默认 fresh fix worker**（用户拍板 D1'）；原会话恢复留目标形态。
6. **确认闸 = agent 窗口作答**（用户 2026-08-16 对话拍板，推翻审核推荐的一次性回执 CLI）：落户确认、决策、收口确认都在对应 agent 的窗口里明文回答，agent 把回答原文写进 result/review.md 确认记录，Runner 不读回答。**登记为已接受的弱信任**（输入者身份不可机器验证；IHSR_05 F-006 有误点/代答先例），配三道**由宿主持有**的护栏（审核 R4-04）：① agent 必须先写 `decision_required` checkpoint 挂起；Runner 摄入该 checkpoint 时记 `decision_checkpoint_event_id`，收口 result 引用的 event_id 须存在于事件账且早于 result 摄入，否则拒；② 收口确认要求用户输入固定短语「认可执行本地收口」原文（agent 抄录，弱信任）；③ **截图由宿主拍**——摄入 decision checkpoint 时与摄入确认 result 时各自动拍一张该 launch 绑定的 RELAY 窗口截图，宿主写哈希与 `evidence_refs` 进事件账，agent 不能自填。不再有 `relay approve` 命令、不再要求飞书。
7. **收口执行 = 收口 agent 前台 + finalizer 脚本后台，且 finalizer 只由宿主启动**（用户拍板 D2 折中；xhigh 审 S3）：收口 agent 备料、写交付汇报、在窗口拿到确认后只提交 `succeeded + next_action=finalize` 的 final result（含确认记录字段）；Runner 校验三护栏与 manifest 后**自己签发 finalizer 的 script receipt 并由宿主执行**，agent 不得直接调用 finalizer 脚本、不持任何 Git 收口权限。finalizer 按 closure journal 逐步幂等执行——**全部在从 master HEAD 新建的暂存分支上完成，最后一次 CAS 快进**（用户拍板 D6）：审计（预期 master HEAD、index 干净、路径所有权无重叠）→ squash → 合入态复验（机器闸复跑）→ 准备并校验销户回填 → 原子 verify 提交（含回填）→ 回读一致性 → master 仍为预期 HEAD 才快进 → 清树与暂存分支。崩溃从最后完成步续跑，master 永无"已合入未 verify"中间态；失败进 `closeout_blocked` 由诊断 agent 带你处置。永不 deploy；finalizer 永不 push（test 受控 push 只属 7a 集成棒，见决策 13）。
8. **三路复核显式成棒、代码复核顺序两轮**（用户拍板 D3 + 对话补充）：代码复核 R1 → fresh R2（读已落账 R1）→ fresh 汇合；需求与一致性复核 agent（fresh）；教训复核 agent；三者账号可同、会话必不同、启动命令不得含 resume/continue。返工复审 = 同节点新 attempt；复核工件按 attempt 不可变 `review-<node>-<attempt>.md`，canonical `review.md` 只由汇合/收口 agent 以 expected-hash CAS 写。
9. **计划只引用注册表条目**（审核 S2）：RelayPlan 只写 `profile_id / script_spec_id / 开关`；命令模板（含冻结 hash）、允许参数白名单、工作目录根、账号配置目录路径、fallback 关系、push 目标白名单放**用户级**注册表 `~/.dh-relay/registry/*.psd1`（只含路径/别名、零凭据）；**P2 不支持仓级覆盖**（用户拍板 D9，防仓内文件被改后接管账号映射；留 P3）；计划内不得出现任意 CLI 参数。
10. **运行现场归目标仓**：经 canonical runtime resolver 解析该仓 dh-relay 模块命名空间（默认 `<repo>/.dh-runtime/relay/<run_id>/`，不进 git，不写死路径）；用户级 `~/.dh-relay/runs.json` 只做索引（repo 路径 + run_id + 摘要 + 更新时间，不含 config_dir 等路径），写入用临时文件 + 原子改名 + 文件锁；run_id 含仓 slug 前缀避免跨仓冲突。
11. **一 run 一批卡（2～3 张试验）与基线刷新**（用户拍板 D8、D11、D12）：卡间按 DevPlan 依赖串行/并行；接力计划生成时按 DevPlan 变更范围做**静态路径重叠检查，重叠卡强制串行**；每卡在棒 1 开工时刻从当时 master HEAD 建树；**同一仓同一时刻只允许一个 finalizer 在跑**。**基线落后**（前卡已快进）：finalizer 在暂存分支先 rebase 候选到新 master → 生成新 candidate generation（新 `base_sha + candidate_tree_sha + diff_hash`）→ 若 **patch 哈希不变**且机器闸复跑通过 → 旧复核工件仍有效，但旧 manifest/确认失效，**重新弹一次收口窗口重新确认**（新 manifest）；若 **patch 变了**（冲突解决或内容漂移）→ 自动重跑受影响证据（机器闸 → R1′→R2′→lead′，开关的 e2e）后再重新确认；只有 master 未漂移才是"一次确认"。rebase 冲突 → `closeout_blocked` → 诊断 agent 可拉修复 agent 解冲突后重走。**多卡下的重编排**（xhigh 审 S2，用户 2026-08-16 定）：保持 run 全局 generation（P1 身份链不改）；某卡 `dependency_blocked` 触发的重编排只允许在**该卡内**新增前置棒（不改其它卡、不加卡）；重编排 agent 计算期间其它卡照常；新计划生效那一刻 Runner 判影响面——**不受影响的卡**（节点未变、不依赖该卡）手上在跑的棒先交完再切换届次（不重做、不丢），切换后下一棒 fresh 继续；**受影响的卡**（依赖该卡）干完手上一棒后按 P1 依赖冻结等待。卡级 generation 留 P3。多卡 DAG 通用调度器、跨仓 run 留 P3。
12. **诊断 agent 自动拉起、代发控制事件属 P2 对 P1 §2.2/§4.4 的明确放宽**：任一棒 `interrupted_unknown`/paused/`closeout_blocked`/quota 无 fallback/续跑发现断棒 → Runner 拉一次性只读诊断 agent；它读现场、写诊断事件，在窗口问你"续跑 / 换账号重试 / 重来 / 放弃 / 丢弃暂存分支"，按你的回答代发**预定义子类型**控制事件（`actor=diagnoser:<node_id>`、`on_behalf_of=user`、`user_reply_verbatim`、绑定 launch/session）；Runner 消费控制事件改状态，诊断 agent 本身仍不直接写 active state、不施工。此代发同属弱信任，与确认闸一并登记。
13. **test 分支受控 push**（用户拍板 D10，P1 "永不 push" 的唯一受控例外；xhigh 审 S8 收紧）：仅在 `relay start -AllowTestPush`（run 级授权）且计划开关 `integration=true` 时，7a 集成脚本节点可把候选合入并推到远端 **`test` 一个分支**——注册表 push 白名单冻结 `remote URL 指纹 + refs/heads/test`，脚本硬拒其它 remote/ref（master/main 永不）；**权限边界在远端**：目标 Git 服务侧须把 relay 使用的凭据 ACL 限制到该仓 `refs/heads/test`（不可由 relay 自证，B13 施工者提供脱敏权限回读、用户在测试仓确认）；推前 journal 冻结 remote URL 指纹、远端 test 原 HEAD、source SHA、完整 refspec；e2e 失败或用户在窗口说"回滚"时以 `--force-with-lease=test:<原HEAD>` 精确回退；lease 冲突（外部人员改过 test）或补偿失败 → `closeout_blocked`，不得宣称已回滚/run done；`cancel_card` 发生在 push 之后必须先走同一补偿路径；同仓 test 推送持宿主锁串行；部署到测试服务器仍由用户在窗口协作中亲手执行（agent 只给命令）。
14. **不改 dh-crew**：新代码落 `tools/relay/`；dh-crew 的 dispatch/loop/notify 不动、不被 relay 读状态；飞书通知 adapter、登录自启降 P3（用户拍板 D9）。

### 1.4 范围与分期

**P2（本设计；用户拍板 D4 分卡逐层冻结，预计 8～10 张标准卡，具体由 B 拆）**：

1. v2 契约与兼容：新增角色、`script` 节点、`gate_outcome`、`verdict`、`rework`（re-ready 新 attempt）、profile/script 引用、按 attempt 不可变复核工件、release manifest、控制事件子类型（resume/retry/reset/cancel/abort）、run 生命周期、多卡 run 结构；P1 业务状态机与合法边不变，旧 run 可读。
2. 宿主基础债：RB-1 截图/通知非致命、F-019 决策态快照、F-023 三条（consumed 轮转 / spawner 异步 / Runner 提交临时名）。
3. 任意仓发起 + 用户级注册表 + run 索引 + `relay` 控制台（start/resume/status/propose/cancel）+ 现场经 resolver 归目标仓；codex-ninth 实棒（非秘密身份回执 + expected_identity 比对）。
4. 脚本节点（机器闸、e2e/集成脚本、finalizer 承载）。
5. 三路复核 + 汇合 + 有界返工/补证回路 + 集成/端到端可选棒（多轮协作 + test 受控 push/回滚）。
6. 收口 agent + finalizer 脚本（暂存分支一次快进 + 基线落后 rebase 重过闸）+ 宿主持有的确认闸护栏 + release manifest。
7. 诊断 agent 自动拉起 + 接续（先摄入未消费结果、再判断棒、控制台续跑）+ quota 高置信识别/预登记 fallback。
8. 多卡调度（依赖/静态重叠检查/finalizer 与 test 推送串行锁）。
9. 真实自举（一批 2～3 张与 relay 实现无关的真实卡）+ H3/H4。

（用户 D4 拍板"分 7 卡"时范围尚不含多卡/集成/诊断；v5 范围扩大后预计 8～10 张，待用户在整版确认时一并认可；B 拆时可合并/拆分，每张卡只承载一个安全边界。）

**P2 内降级实现**：quota 只做"高置信识别则换预登记 fallback，否则 paused→诊断"；接力计划生成 agent 只做模板实例化。

**不做（留 P3+）**：完整 quota 识别/恢复通道、计划内任意 CLI 参数、通用动态 planner、通用多卡 DAG 调度器与跨仓 run、仓级注册表覆盖、登录自启、飞书通知 adapter、原施工会话恢复返工、非 psmux 后端、跨机器、UI、自动决策、dh-crew 侧改窗口拥有会话（F-021）。

### 1.5 核心决策清单

| 决策点 | 选了什么 | 备选与取舍 | 不可逆程度 |
|---|---|---|---|
| relay 边界 | 只接已授权任务卡 | 把 A/B 也接进去 → 与 dev-harness 人闸冲突 | 高 |
| 流程来源 | 直接映射 dev-harness 节点表 | 自定义精简流程 → 与收口硬规则漂移 | 高 |
| 汇合判定归属 | 汇合 agent 写 verdict，Runner 只按枚举分支 | Runner 解析 review 判 P0/P1 → 违反"Runner 不判质量" | 高 |
| 机器闸形态 | 脚本节点 + `gate_outcome`，不加新 result_status | 新增 `failed` → 改共享状态机 | 高 |
| 返工会话 | fresh fix worker + handoff | 原会话恢复省上下文，但 psmux 无可靠 suspend/resume | 中 |
| 确认闸载体 | 全部在 agent 窗口作答（弱信任 + 三护栏） | 一次性回执 CLI（审核推荐）→ 更强身份证明，但用户不要命令行/飞书 | 高（用户拍板） |
| 收口执行者 | 收口 agent 前台 + finalizer 脚本后台 | agent 自己做 git → 幂等/防篡改难证；纯脚本无前台 → 用户没有对话口 | 高 |
| 合入方式 | 暂存分支做完后一次 CAS 快进 master | 直接上 master → 崩溃后中间态 | 高 |
| 复核形态 | 代码 R1→fresh R2→汇合 + 需求复核 + 教训复核，各自成棒 | 并行两 reviewer → 不满足交叉评估；把需求/教训塞进收口棒 → 不可见、不可单独验收 | 高 |
| 执行 profile/脚本 | 计划只引用用户级注册表 id | 计划内写命令/账号目录 → 注入、越权、暴露面 | 中 |
| 运行现场归属 | 目标仓 `.dh-runtime/relay/` + 用户级索引 | 全落 dh-crew → 跨仓混、IHSR_05 已踩 | 中 |
| run 粒度 | 一 run 一批 2～3 卡（试验），同仓 finalizer 串行 | 一 run 一卡 → 少一层调度但无法验证多卡；通用 DAG → 超 P2 | 中 |
| 异常处置 | 诊断 agent 自动拉起、窗口问答代发控制事件 | 用户手动触发 + CLI → 用户不要命令行 | 中 |
| 额度耗尽 | 高置信识别才换预登记 fallback | 宽松识别自动换号 → 误判 | 中 |
| 多卡基线 | 重叠卡强制串行；落后候选在暂存分支自动 rebase 复跑机器闸再快进；冲突转 closeout_blocked | 任何落后都停等人 → 串行多卡几乎每卡停一次 | 中 |
| test 分支 push | 允许 relay 推远端 `test` 一个分支（run 级 `-AllowTestPush` + 白名单 + 记原 HEAD + force-with-lease 回滚）；master 永不 | 只动本地 test、push 由用户亲手 → 更保守但多一次人工 | 高（P1 "永不 push" 的唯一受控例外） |
| 通知/自启/仓级覆盖 | 全部降 P3 | 保留在 P2 → 卡数 10～12 | 低 |
| 契约版本 | `relay/v2` 双解析器；只承诺 v1 枚举/合法边/只读解析不变；v1 存量 run 只读不续跑 | 笼统"只扩不改" → 不可证且 v1 解析器会被新字段打破 | 中 |
| 多卡重编排 | run 全局 generation 不变；生效时不受影响卡交完在跑棒再切、受影响卡依赖冻结 | 卡级 generation → 改 P1 身份链（P3） | 中 |
| rebase 后处置 | patch 不变→只重确认；patch 变→重跑机器闸与两轮复核后重确认 | 任何 rebase 全套重跑 → 成本高；不重确认 → 确认绑旧候选 | 高 |
| finalizer 触发权 | Runner 校验护栏后签发 finalizer receipt；agent 不可调用 | closer 直接调脚本 → agent 持 Git 收口权限 | 高 |
| 确认闸时序 | 宿主 decision_ack（精确单窗口截图）后 agent 才可展示确认；result 摄入再拍一次 | 只核文件顺序 → 截图可能在回答后同刻拍成 | 高 |
| tracked 写入 | 全部落任务树、主工作树只读、一仓同卡一个 active run | 棒直接写主树 → 弄脏 master/并发追加 | 高 |

## 2. 全局地图与角色边界

### 2.1 每张卡的接力（棒表，含文件路径）

约定：在业务仓 `<repo>` 执行 `relay start`；`<run>` = `<repo>/.dh-runtime/relay/<run_id>/`（不进 git）；`<ws>` = `<repo>/docs/modules/<模块>/workspace/<卡>/`（进 git）；任务树 `D:\wt\<卡>`；node_id = `<卡>-<角色>`。

| 棒 | 谁跑 | 读什么 | 写什么 | 交给谁 / 你要做的 |
|---|---|---|---|---|
| 0 | 接力计划生成 agent（一次性，一 run 一次） | DevPlan `<repo>/docs/modules/<模块>/dev_plan/P*.md`（授权的 2～3 张卡）+ `design/README.md` 白名单输入；模板 `tools/relay/templates/standard-pipeline.json`；注册表 `~/.dh-relay/registry/` | `<run>/inbox/proposal-v1.json` → Runner 晋级 `<run>/plans/relay-plan.v1.proposal.json` + `active-plan.json` + `authority.json` | Runner 按依赖拉第一批卡的棒 1 / 你：无 |
| 1 | workspace agent | 自己的派工单 `<run>/launches/<launch_id>.json`、DevPlan 该卡、design | `D:\wt\<卡>`（dh wt new）；`<ws>/` 8 件套（含 worker 粒度 task_plan）；`<run>/attempts/<卡>-workspace/1/checkpoint.json`（decision_required 落户确认）→ `result.json` + `handoff.md` | 施工 / 你：窗口答"确认" |
| 2 | 施工 agent（cwd=`D:\wt\<卡>`） | `<ws>/brief.md`、`task_plan.md`、上一棒 handoff | 代码改动；`<ws>/progress.md`；`<run>/attempts/<卡>-build/<n>/result.json` + `handoff.md` | 机器闸 / 无 |
| 3 | 机器闸（脚本节点，宿主执行） | 注册表 script_spec（`dh <模块>` + 该卡测试命令）；cwd=`D:\wt\<卡>` | `<run>/attempts/<卡>-gate/<n>/result.json`（gate_outcome、exit、脱敏尾部）；证据行追加 `<ws>/progress.md` | pass→R1；fail→修复 agent（fresh，`<卡>-fix/<n>`）→回 3（连续 ≥3 次 fail 转 decision）/ 无 |
| 4 | 代码复核 R1（fresh，只读） | `D:\wt\<卡>` diff、`<ws>/*`、handoff | `<ws>/review-R1-<attempt>.md`（不可变）；`<run>/attempts/<卡>-R1/<n>/result.json` | R2 / 无 |
| 5 | 代码复核 R2（fresh，只读，可读 R1） | 同上 + `<ws>/review-R1-*.md` | `<ws>/review-R2-<attempt>.md`；`…/<卡>-R2/<n>/result.json` | 汇合 / 无 |
| 6 | 汇合 agent（fresh） | 两份复核工件 | `<ws>/review.md` 代码复核段（CAS）+ `findings.md` 归档；`…/<卡>-lead/<n>/result.json`（verdict） | approve→7a/7b；rework→修复 agent 回 3（≤3 轮，R1'/R2'/lead' 为新 attempt）；escalate→窗口问你 / 偶尔答 |
| 7a | 集成与端到端 agent（**可选棒**，模板开关 `integration=true`；push 需 run 级 `-AllowTestPush`） | `<ws>/brief.md` 人验/需求项、仓库 test 分支约定、集成/e2e script_spec | 调集成脚本节点：记录本地与远端 `test` 原 HEAD → 把候选合到本地 `test` → 若授权则 push（白名单仅 `test`，宿主锁串行）→ 跑 e2e 脚本节点；需要真机时先写 `decision_required` checkpoint，再在窗口逐条给你命令（含部署到测试服务器的命令）、你亲手执行回贴输出，多轮直到过；失败或你说"回滚"→ 脚本以 `--force-with-lease=test:<原HEAD>` 回退远端、本地 reset 回原 HEAD；过程与结果写 `<ws>/progress.md` 证据账本（需求境证据 E-ID）；`…/<卡>-integration/<n>/result.json` | 需求复核 / 你：窗口协作输入 |
| 7b | 需求与一致性复核 agent（fresh） | `<ws>/brief.md` 验收口径、`progress.md` 证据账本、`review.md`、DevPlan 该卡、gate/e2e 结果 | `<ws>/review.md` 需求复核段（E4：每条需求/人验项 → 场景路径 → 证据 ID → 结论）+ 一致性表（brief ↔ DevPlan 验收口径 ↔ 证据；verify 文案一致性由棒 8 收口 agent 自查、finalizer 按 manifest 校验）；`…/<卡>-requirement_review/<n>/result.json`（verdict：pass / needs_evidence / escalate） | pass→7c；needs_evidence→回指定目标棒（施工或集成）新 attempt，只重跑该棒及其下游机器闸；若目标棒改了代码则按 rework 全套（R1'→R2'→lead）；≤2 轮，超轮 escalate 窗口问你 / 无 |
| 7c | 教训复核 agent（不阻断） | `<ws>/findings.md`、`lesson_candidates.md`、`progress.md`；`dh mine` | `<repo>/docs/modules/<模块>/knowledge/教训库-候选.md` 追加候选（待人裁决，不入正册）；`<ws>/review.md` 教训复核段；`…/<卡>-lesson_review/<n>/result.json`（无 verdict，成功即 succeeded） | 收口 / 无 |
| 8 | 收口 agent | `<ws>/*`、gate/e2e 结果、三路复核段 | E5～E10 备料：`dh gate`、as-built、交付汇报与证据展示区写 `<ws>/review.md`；`<run>/attempts/<卡>-closer/<n>/release-manifest.json` + `verify-message.txt`；写 `decision_required`「已查看证据，认可执行本地收口？」→ **等宿主 ACK**（Runner 已接受该 checkpoint 且完成同 launch 精确截图）后才向你展示确认问题 → 你答固定短语 → 回答原文写 review.md 确认记录 → 提交 `succeeded + next_action=finalize` 的 `result.json`（不调用任何脚本） | Runner 校验护栏与 manifest 后签发 finalizer receipt / 你：窗口答「认可执行本地收口」 |
| 8' | finalizer（脚本节点，**Runner 签发 receipt、宿主执行**，agent 不可触发） | manifest、`D:\wt\<卡>`、预期 master HEAD | 暂存分支 `relay/closeout/<卡>`：审计→squash→（基线落后则 rebase→新 candidate generation→按决策 11 决定重确认或重跑）→复验→回填 DevPlan 行/`<ws>`→verify 提交→回读→快进 `master`→删 `D:\wt\<卡>` 与暂存分支；日志 `<run>/closeout/<卡>/journal.json`；`…/<卡>-finalizer/<n>/result.json` | Runner：有下一张卡→拉其棒 1；全部完成→run done / 无 |
| 全程 | Runner/宿主（确定性程序） | — | `<run>/events.jsonl`、`relay-state.json`、`launches/`、`psmux-handles/`、`shots/`；索引 `~/.dh-relay/runs.json` | — |

### 2.2 非业务角色

| 角色 | 是什么 | 出现时机 | 你要做的 |
|---|---|---|---|
| Runner / 宿主 | 常驻确定性程序（非 AI）：身份链、CAS、依赖、返工/补证轮次上限、脚本节点执行、多卡调度、finalizer 串行锁、quota 识别、run 索引；每 tick 一行状态 | `relay start`/`resume` 到 run 结束 | 无 |
| 重编排 agent | 一次性 AI：判断 `dependency_blocked` 是否成立、写 v2 proposal（P1 已有） | 某棒缺前置时 | 涉及方案/删改时转 decision 在窗口问你 |
| 诊断 agent | 一次性只读 AI：读现场、写诊断事件、在窗口问你并代发控制事件（resume / retry-with-profile / reset-attempt / cancel / abort-closeout） | `interrupted_unknown`/paused/`closeout_blocked`/quota 无 fallback/续跑发现断棒 | 窗口答一句 |
| 机器闸 / e2e / finalizer 脚本 | 宿主调用的确定性脚本（script 节点） | 计划指定处 | 无 |
| 用户 | 授权开工（对话）、`relay start`/`relay`（控制台）、窗口作答（落户/决策/协作/收口/诊断） | — | 不巡检、不搬运交接、不敲收口命令 |

### 2.3 多卡调度

- 接力计划把每张卡实例化为一组节点（1～8'），卡间边来自 DevPlan 依赖列；无依赖卡可并行（受注册表 `max_parallel_cards`，默认 2）。
- 同一仓同一时刻只允许一个 finalizer 节点 running（宿主级锁）；后一张卡的 finalizer 以前一张快进后的 master HEAD 为预期 HEAD 重新审计。
- 任一卡 escalate/paused 只冻结依赖它的卡；其它卡照常。
- 某卡重编排（仅限本卡内加前置棒）：计算期间其它卡照常；生效时不受影响卡的在跑棒交完再切届次、下一棒 fresh 继续，受影响卡按依赖冻结（决策 11）。
- run 终态区分 `done-complete`（全部卡 finalizer succeeded）与 `done-partial-cancelled`（含被 cancel 的卡）；后者不得表述为"全部销户"；部分完成也留完整事件账。

### 2.4 任意仓发起与接续

- `relay start -Task A,B,C`（在目标仓执行）：校验该仓 AGENTS/DevPlan/模块 slug、卡状态可开工、树干净；写 `<run>` 与用户级索引；拉棒 0。
- `relay`（无参数）= 控制台：读 `~/.dh-relay/runs.json` 列出所有仓未完成 run（卡到第几棒、断了哪些），选一个即 `resume`；`relay status [-RunId]` 只读。
- **续跑判定**（P2 对 P1 A6 的恢复边补充；xhigh 审 S7 补竞态屏障）：宿主启动时先取 **run 恢复锁**：冻结新 launch → 原子 claim 并摄入所有未消费的 tmp/result/ack/inbox → probe 各在册 session → 对"进程/会话已消失"的 attempt **再做一次 claim/双空扫描**（防 result 在首扫后、probe 前原子落盘）→ 仍无 final result 者才 CAS 记 `interrupted_unknown`（A6 fail-closed 起点，不自动推进业务）→ 释放锁 → 拉诊断 agent。日常 tick 同样对"非 launched 节点"的迟到合法 result 做摄入并按身份链判 stale/接受，不得因扫描器只看 launched 而永久拒收。"已交棒 attempt 不重跑"仅指不因重启机械重跑，其结果是否仍有效由后续机器闸/finalizer 审计兜底；工作树与工作区现状保留。
- 诊断 agent 的选项：续跑（fresh 新 attempt，带 handoff/工作树现状）、重来（丢弃工作树未提交改动后新 attempt）、换账号重试（预登记 fallback profile）、放弃这张卡（cancel 该卡节点组）、丢弃暂存分支（closeout_blocked 时）。
- 登录自启（计划任务 `relay resume --all`）留 P3，P2 不做。

## 3. 契约增量（`relay/v2`，v1 只读兼容）

- **节点角色**：沿用 `worker/reviewer/replanner`，新增 `planner / workspace / build / fix / review_lead / integration / requirement_review / lesson_review / closer / diagnoser / gate / finalizer`。
- **节点类型**：`agent`（默认）| `script`。script 节点只引用 `script_spec_id`；宿主脚本执行器扮演该 attempt 的 worker（receipt backend=`script`），只写宿主私有 candidate，Runner 按完整身份链 + script_spec hash + 矩阵校验后晋级 final；result = `succeeded` + `gate_outcome=pass|fail|error` + `exit_code` + 脱敏限长 `output_tail` + `duration_ms`；计划 `on_gate_fail: <fix_node_id>`；`error` 按 `interrupted_unknown` 兜底。
- **多卡结构**：plan 顶层 `cards[]`（card_id、DevPlan 引用、节点组、卡间 depends_on、开关 `integration/e2e`），节点 node_id 前缀 = card_id；Runner 调度以节点为单位、锁以卡/仓为单位。
- **profile 引用**：节点 `profile_id`；用户级注册表条目 `cli / config_dir / work_dir_root / model / fallback_profile_id / account_alias / expected_identity`（只是路径与别名）；receipt 记 `profile_id + account_alias + cli + config_fingerprint`；拉起后尽力取 CLI 返回的脱敏账号主体与 `expected_identity` 比对，不符即 `launch_failed`，取不到登记"账号身份不可机器验证"（B4 据此判）；fresh 由新 launch/session + 命令模板无 resume/continue/旧 session 参数证明。
- **verdict**：`review_lead` result 允许 `verdict ∈ {approve, rework, escalate}`；`requirement_review` result 允许 `verdict ∈ {pass, needs_evidence, escalate}`；其它角色携带即拒。
- **返工/补证回路**：计划 `rework: { fix_node_id, max_rounds: 3, re_review_node_ids[] }`、`evidence_rework: { target_node_ids[], max_rounds: 2 }`；触发时目标节点重新 ready 为新 attempt（round→node/attempt 映射记事件）；超上限由对应 agent 写 `decision_required`。
- **复核工件**：reviewer/lead/需求复核每个 attempt 写不可变 `review-<node>-<attempt>.md`；canonical `review.md` 各段只由 lead / 需求复核 / 教训复核 / 收口 agent 以 expected-hash 顺序 CAS 写。
- **人机多轮协作**（P1 §4.2 语义）：integration 等棒进入需人配合阶段写一次 `decision_required` checkpoint；其后多轮往返 Runner 不感知；写出后续 `working` checkpoint 或 final result 才恢复调度；依赖节点期间冻结。
- **确认记录**（xhigh 审 S5 补时序屏障）：agent 写 `decision_required` checkpoint → Runner 接受后**宿主立即对该 launch 绑定的唯一 RELAY 窗口精确截图**（按 handle/window_title 单窗口，零窗口或占位图=失败）并写 `decision_ack`（含 event_id、截图哈希）→ agent 只有读到 ACK 才可向用户展示确认问题；用户回答后 agent 提交 result（`user_reply_verbatim`（抄录）+ `decision_checkpoint_event_id`）→ Runner 摄入时再拍一次同窗口精确截图并核对：ACK 存在、event 顺序、launch/session/attempt 一致、收口时 `user_reply_verbatim == "认可执行本地收口"`；缺任一项 → 不签发 finalizer receipt。截图与哈希只由宿主写入事件账，agent 自填一律忽略。
- **release manifest**：收口 agent 备料终点生成规范化 `release-manifest.json`：候选 squash 目标 SHA、预期 master HEAD、精确路径集、各复核 attempt 工件哈希、gate/e2e 结果哈希、release_mode、Risk-Count/Refs、verify 文案哈希、动作范围；finalizer 与宿主外 oracle（B10）都以它为准。
- **quota**：`quota_exhausted` 枚举转正；识别规则表按 CLI/版本冻结正反样本（脱敏）于注册表；仅高置信匹配才写；fallback 只用注册表预登记条目；无 fallback → paused → 诊断。
- **控制事件子类型**（扩 P1 §3.4，字段仍为 actor/source/nonce + 身份链绑定；xhigh 审 S6 补目标绑定）：`action ∈ {resume, retry_with_profile, reset_attempt, cancel_card, cancel_run, abort_closeout, rollback_test}`；每条控制 candidate 必须带 `target = {run_id, card_id, node_id, attempt_id, launch_id, generation}` + `expected_state_hash`（目标当前状态快照哈希）+ 参数（如 profile_id）+ `option_id`（必须来自诊断 agent 该次 checkpoint 声明的选项列表）；由诊断 agent（`actor=diagnoser:<node_id>`、`on_behalf_of=user`、`user_reply_verbatim`、绑定其 launch/session）通过宿主提供的**单次、目标绑定、选项绑定的窄工具**发出，或由 `relay` 控制台（`actor=console`）发出；Runner 唯一消费者，重验目标与 expected_state 后执行；旧 generation/错 run/重复 nonce/非预定义 action/选项不在声明列表/状态哈希不符一律拒绝并留 `control_rejected`。弱信任仅限"输入者是谁"，不扩成"agent 可任选控制动作"。
- **run 生命周期**（控制面维度，与 P1 task overall state 分离）：`active → done`；`active → cancelled`；卡级 `finalizing → closeout_blocked → finalizing | aborted`；finalizing 中不接受 cancel。
- **运行现场、tracked 写入与所有权**（xhigh 审 S9）：运行现场经 resolver 落 `<repo>/.dh-runtime/relay/<run_id>/`（含 `closeout/<卡>/journal.json`）；`~/.dh-relay/runs.json` 只存 repo 路径 / run_id / 摘要 / 更新时间。**所有 tracked 写入（工作区 8 件套、review 工件、knowledge 候选、as-built、DevPlan 回填）统一落在任务树 `D:\wt\<卡>` 内、随该卡 squash 合入；主工作树对所有棒只读**（finalizer 除外，且只经暂存分支快进）；宿主持 `(canonical_repo, task_id)` 跨进程 claim lease，**一仓同卡同时只允许一个 active run**。
- **通知**：P2 不做通知 adapter（留 P3）；用户靠弹出窗口与 `relay`/`relay status` 控制台知情。
- **版本与兼容**（xhigh 审 S1，用户拍板 D13）：P2 工件 `schema_version=relay/v2`，Runner/校验器双解析器；承诺不变的是 **v1 业务状态枚举、合法转换边集合、v1 目录的只读解析**；v2 新增字段/角色/控制事件与"Runner 核对确认记录字段与事件顺序、接受诊断代发控制事件"登记为 **v2 版本化例外**（P1 §3.4 "Runner 不读取回答"的语义收窄为：Runner 不解析对话内容，只校验结构化确认字段的等值与时序），不再笼统称"只扩不改"。v1 存量 run（DHR_03/IHSR_05 等）在 P2 下**只读状态/归档，不续跑**；迁移另立项。转换矩阵：**不新增业务出口**。

## 4. 固定流转补充

- **落户确认**：workspace 棒完成 8 件套后写 `decision_required`（档位/目标/范围/验收/落点摘要）；回答"确认"→ `succeeded`；回答"取消"→ 诊断 agent 路径 `cancel_card`。
- **机器闸失败**：`gate_outcome=fail` → 修复 agent（fresh）→ 回闸；连续 ≥3 次由修复 agent写 decision。
- **代码复核**：R1/R2 只读、fresh；汇合规则写在 brief（P0/P1=0 且 findings 归档 → approve；有 P0/P1 → rework；争议/超轮 → escalate）。
- **集成与端到端**：仓约定有 test 分支/需真机验证时开启；集成脚本节点管本地/远端 test 的合入、push（仅 `-AllowTestPush` 且白名单 `test`）、回滚；agent 只给命令、你执行并回贴（含部署测试服务器）；结果作需求境证据进账本。
- **需求与一致性复核**：按 brief 验收口径逐条要证据；缺则 `needs_evidence` 回目标棒（只重跑该棒及下游机器闸；改代码则走 rework 全套）；一致性表覆盖 brief/DevPlan/证据。
- **收口**：收口 agent 走 E5～E10 备料清单、产出 manifest 与 verify 文案，窗口问确认；拿到固定短语后调 finalizer；finalizer 在暂存分支执行授权包（squash / 复验 / 回填 / verify）后一次快进 master 并清树；不 push、不 deploy。
- **恢复**：`relay`/`relay resume` 同 RunId 续跑；断棒 → 诊断 agent；finalizer 崩溃从 journal 最后完成步续跑，master 未动；`closeout_blocked` 由诊断 agent 带你重试或丢弃暂存分支。

## 5. 操作剧本（自举）

1. 你在对话里授权一批 2～3 张**与 relay 实现无关**的真实标准档卡（B 阶段与你定）；在该仓执行 `relay start -Task …`，之后主控不再介入。
2. 棒 0 出计划；每张卡按依赖依次/并行走 1～8'；你只在窗口里：落户确认、（如有）决策、真机协作、收口确认。
3. 途中关掉全部终端一次，隔一段时间 `relay` 控制台续跑，诊断 agent 带你接上断棒。
4. 全部卡 verify 销户后 run done；宿主外 oracle 核对主干 commit/verify trailer/DevPlan/树。

## 6. 验收清单

### 6.1 AI 自动验收栏

| ID | 验收项 | 怎么证明 |
|---|---|---|
| B1 | `relay/v2` 契约与 v1 兼容：新增角色/script 节点/gate_outcome/verdict/rework/evidence_rework/profile 与 script 引用/复核工件/多卡结构/控制事件子类型（含 target/expected_state/option 绑定）/确认记录与 decision_ack/candidate generation 字段有 schema + 矩阵测试；v1 业务枚举与合法边集合不变；v1 目录只读可解析、resume 被拒 | 契约套件正反例（双解析器）；矩阵集合 == 独立硬编码 oracle；P1 15 套件全绿；DHR_03 现役证据目录 v1 只读回放 + `relay resume` 拒绝 v1 run |
| B2 | 脚本节点由宿主执行、结果为机器事实 | fake+真实：exit 0→pass、非 0→fail 并按 on_gate_fail 分支、超时→error→interrupted_unknown；agent 结果伪造 gate_outcome 被拒；script_spec hash 不符拒执行；output_tail 先脱敏后限长 |
| B3 | 三路复核顺序 + 汇合 + 有界返工/补证 | fake 回放：R2 只在 R1 succeeded 后 ready；lead 只在 R1、R2 后；rework 走 fix→gate→R1'→R2'→lead（新 attempt）；第 4 轮转 decision；requirement_review needs_evidence 回目标棒新 attempt，第 3 轮 escalate；非授权角色携带 verdict 被拒；各棒 launch/session 各不相同且命令模板无 resume/continue |
| B4 | 用户级注册表生效、任意仓发起、真实拉起 codex-ninth 一棒 | 真实：在非 dh-crew 仓 `relay start`，现场落该仓 `.dh-runtime/relay/`，索引写 `~/.dh-relay/runs.json`；同一 run 内 claude 与 codex（ninth）各一棒交棒；receipt 记 profile_id/account_alias/config_fingerprint 且脱敏账号主体与 expected_identity 匹配（取不到/不匹配则本项不通过，不得宣称 ninth）；plan/receipt/事件零凭据；计划内出现 command/extra_args 被 schema 拒 |
| B5 | quota 高置信识别与预登记 fallback | fake：冻结样本→`quota_exhausted`→fallback fresh attempt；非额度错误样本→不判 quota；无 fallback→paused→诊断节点被拉起 |
| B6 | 宿主健壮性 | 截图/通知抛错不致宿主崩；`.consumed-*` 有轮转；spawner 不阻塞 tick；Runner 提交临时名与 worker tmp 不同名；决策态有 `state-at-decision-*.json` |
| B7 | 控制台与接续 | `relay`/`relay start/resume/status/propose/cancel` 可用；手动 propose v2 走同一 CAS；模拟"全部终端被杀"后 resume：恢复锁下先摄入未消费 tmp/result/inbox、再 probe、再二次 claim/双空扫描（**注入"首扫后、probe 前原子落 result"的并发反例：该 result 必须被接受而非判断棒**）；有派工单无 final result 且无会话的 attempt 才判 `interrupted_unknown`；工作树/工作区保留；诊断节点被拉起；诊断 agent 代发的控制事件按预定义子类型、目标绑定、expected_state 生效，旧 generation/错 run/非声明选项/状态哈希不符拒绝；索引写入原子且并发两宿主不互相覆盖；一仓同卡第二个 `relay start` 被 claim lease 拒绝 |
| B8 | 窗口确认闸护栏与收口幂等 | fake 回放 + **真实 psmux 临时仓**：无 decision_ack 时 agent 展示确认/提交 result → 拒；收口 result 缺 `decision_checkpoint_event_id`/回答原文非固定短语/ACK 截图或 result 截图缺失或为占位图/多窗口时截错 handle → 不签发 finalizer receipt、无 verify、无 git 写；先 result 后 checkpoint 的时序被拒；**真实 psmux 快速作答反例**（用户在 ACK 前作答→不接受）；agent 直接调用 finalizer 脚本 → 无 receipt 被拒；有效确认 → Runner 签发 finalizer receipt → journal 在暂存分支逐步完成后一次快进；每一步之后模拟崩溃再续跑不重复 squash/verify、master 在快进前未动；master 被外部提交后快进拒绝→`closeout_blocked`；abort 丢弃暂存分支不碰 master |
| B9 | 隔离与禁改 | 静态守卫：生产代码变更仅 `tools/relay/`；治理工件仅 dh-relay 对应任务精确路径；dh-crew 代码与 active state 零改动；dh-crew run-all 不受影响 |
| B10 | 自举 oracle（宿主外） | 独立脚本（不 import relay 代码）重算并核对：每张卡 master 新 commit 含 `verify(<slug>)` trailer 与 Risk-Count、变更路径集 == manifest 路径集、squash 内容 == 候选 SHA 树；DevPlan 任务行状态=已完成且 SHA 一致；各复核 attempt 工件哈希 == manifest；任务 worktree 与暂存分支仅在 journal 终态后不存在 |
| B11 | 复核身份链与工件不可变 | fake 回放：旧 attempt 再写 `review-<node>-<attempt>.md` 不影响 canonical review.md；lead/需求复核写 review.md 时 expected-hash 不符即拒 |
| B12 | 多卡调度、重编排影响面与基线刷新 | fake 回放 + **真实临时 Git 仓三卡剧本（一对并行、一对重叠、一次 master 漂移）**：三卡按 DevPlan 依赖串/并行；静态路径重叠的两卡被强制串行；某卡 dependency_blocked 重编排时不受影响卡的在跑棒交完被正常接受、切届后下一棒 fresh 继续，受影响卡按依赖冻结；同仓 finalizer 串行锁生效；第二卡 finalizer 发现基线落后 → 暂存分支 rebase → 新 candidate generation → patch 哈希不变→只重确认（旧确认失效、必须重弹窗口）；patch 变→重跑机器闸与 R1′→R2′→lead′ 后重确认；rebase 冲突 → `closeout_blocked`；一卡 escalate 只冻结依赖卡；cancel_card 不影响其它卡；宿主外 oracle 从 start 时的 authority 快照独立推导授权卡/路径集，不信任 closer manifest |
| B13 | 人机多轮协作与 test 受控 push | fake+真实临时仓（含远端裸仓）：integration 棒写 decision_required 后多轮往返期间 Runner 不感知不计数；后续 working checkpoint/result 恢复调度；依赖节点期间冻结；无 `-AllowTestPush` 时集成脚本拒 push；有授权时只允许白名单 remote 指纹 + `refs/heads/test`（推 master/其它 remote 被硬拒）；journal 记 remote 指纹/远端原 HEAD/source SHA/refspec；e2e 失败或 `rollback_test` → `--force-with-lease` 回退到原 HEAD、本地 reset；**外部并发改 test 后 lease 冲突 → `closeout_blocked` 不宣称已回滚**；**cancel-after-push 先走补偿**；同仓 test 推送串行；远端 ACL 限 test 属不可验证项，由施工者提供脱敏权限回读 |

### 6.2 人类验收栏

| ID | AI/你做什么验证动作 | 对话里展示什么证据 | 你判断什么 |
|---|---|---|---|
| H3 | 用 relay 自举跑一批 2～3 张与 relay 实现无关的真实标准档卡，从 `relay start` 到全部 verify 销户，中途故意关掉全部终端一次再续；你只在窗口里作答/协作 | 时间线 + 事件摘要 + 每次窗口作答的 checkpoint/result/前后截图 + 诊断 agent 接续记录 + 每卡 verify 提交 + 用户显式动作清单（口径：每卡基础 2 = 落户 + 收口，加实际 decision/协作轮次，逐次列原因） | 无常驻主控接力是否真的"只在该出手时出手"，窗口作答/协作/接续体验是否可接受 |
| H4 | 与常驻主控模式、IHSR_05 试点并排对照（同 P1 H2 口径；预定上限：主控对话消息=0、每卡人工动作 = 2 + decision/协作数，**另列 run 级动作（start/控制台选择/resume）与每轮服务器操作**、无飞书） | 对照表（区分 `done-complete` 与 `done-partial-cancelled`，后者不给"全部销户"结论）+ 未覆盖范围 + 控制面故障恢复演示（宿主中途停→续；finalizer 中途停→续；全部终端关→控制台续） | 是否达到"取消常驻 AI 主控"的终态；还差什么进 P3 |

## 7. 边界

- **不做**：立项/拆计划、通用多卡 DAG 调度器、跨仓 run、仓级注册表覆盖、登录自启、通知 adapter、原会话恢复返工、非 psmux 后端、跨机器、UI、自动决策、任何 deploy、除 `test` 外的任何 push（master/main 永不）、完整 quota 恢复通道、通用动态 planner。
- **禁改**：dh-crew controller/loop/dispatch/notify 代码与 active state；DevPlan/workspace 只由对应棒或 finalizer 按 dev-harness 规矩写；DevPlan 任务的目标/验收不由 relay 改。
- **凭据**：注册表只存路径/别名；事件、计划、manifest、tail、通知零凭据。
- **信任登记**：窗口作答（落户/决策/协作/收口/诊断）的输入者身份不可机器验证，登记为用户接受的弱信任；收口另有宿主持有的三护栏（先挂起且事件可核、固定短语、宿主拍前后截图）；诊断 agent 代发控制事件同属弱信任；test 分支 push 是 P1 "永不 push" 的唯一受控例外（run 级授权 + 白名单 + 可回滚）。
- **表述**：P2 通过只能称"一批任务卡完整接力自举通过"，不宣称通用多卡生产编排。

## 8. 待决策（B 阶段处理）

- 自举用哪 2～3 张真实卡：留 B 阶段与用户定（原则：与 relay 实现无关、预期 diff/测试已知、标准档；至少一张开 integration 开关以验证多轮协作）。

## 9. A 方案审核与理解确认

- **事件类型**：A-完整（`DHR-A-02` 首版落盘 `cf5b973`；`DHR-A-03` 同日修订=用户指定 codex gpt-5.6-sol xhigh 第六轮交叉审核后的结构性收紧 + 拍板 D11～D13）——正式输入本文，`design/README.md` 白名单已含；P1 正式输入 01 不改。
- **审核记录**：[五轮 fresh 审核与裁决](./evidence/02-交叉审核记录-完整流水.md#review-a)（R1 全面 + R2/R3/R4 定向 + R5 收敛）+ [第六轮 sol xhigh 交叉审核与修订裁决](./evidence/02-交叉审核记录-完整流水.md#review-a3)（均为 headless codex read-only 独立实例，侦测型降级、非机器只读）。
- **主会话裁决**：采纳 S1（不加新 result_status，用 gate_outcome）、S2（计划只引用注册表）、S5（独立自举 + 宿主外 oracle）、S7、U2～U4、R2 全部、R3 全部、R4 全部；审核推荐的一次性回执 CLI（U1/D1）与本地 test 方案（R4 D10）被用户推翻，按用户拍板落地并诚实登记为弱信任 / 受控例外。
- **讲解记录**：[整版讲解与理解确认](./evidence/02-交叉审核记录-完整流水.md#understanding-a)；[修订讲解与拍板 D11～D13](./evidence/02-交叉审核记录-完整流水.md#understanding-a3)。
- **理解问题**：复核发现 P1 需要返工时，默认拉全新 fix worker 还是保留原施工窗口？
- **用户回答 / 拍板**（2026-08-16）：拉全新 fix worker；另九项拍板 D2～D10 见审核记录 §2.3/§3.3/§3.7。
- **调整与复审**：草案 v1→v5，五轮审核；无 open P0/P1；剩余为 B 阶段契约卡字段级冻结事项。
- **用户确认**：2026-08-16 对话点选「确认整版，落盘（含 8～10 卡）」；本次确认不授权 B 或任何卡开工。

## 文档更新日志

| 日期 | 编辑模型 | 更新内容 |
|---|---|---|
| 2026-08-16 | Claude（Fable 5） | 首版：P2 完整流水正式设计（一批任务卡的无常驻 AI 主控接力、任意仓发起、三路复核成棒、窗口确认闸、finalizer 暂存分支一次快进、test 受控 push、诊断 agent 与接续）；由 drafts/02 v5 晋级。 |
| 2026-08-16 | Claude（Fable 5） | A 修订（同事件 DHR-A-02，用户指定 codex gpt-5.6-sol xhigh 第六轮交叉审核 9 P1 全采纳 + 用户拍板 D11 多卡重编排影响面/D12 rebase 后处置/D13 v1 只读）：`relay/v2` 版本化与例外登记；finalizer 只由 Runner 签发 receipt；确认闸加宿主 decision_ack 与精确单窗口截图；控制事件目标/expected_state/选项绑定；恢复锁与二次 claim；test push 白名单含 remote 指纹、lease 冲突与 cancel-after-push 补偿；tracked 写入全落任务树 + claim lease；B1/B7/B8/B12/B13/H4 补真实反例；决策清单增 7 行。 |
