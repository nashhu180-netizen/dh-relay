# DH Relay P2 完整流水 · A 交叉审核记录

> 性质：A 过程证据，不属于 `designInputs[]`，不得作为 B 的补充输入。正式结论只认 `design/02-完整流水-产品设计与验收.md`（晋级后）；晋级前草案在 `design/drafts/02-完整流水-共创草案.md`。

<a id="review-a"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-02 artifact=design/02-完整流水-产品设计与验收.md kind=review -->

## 1. 起因与需求理解对齐

- 2026-08-16 用户点选 DHR_03 H2=「值得，进完整流水」，随后指令「P2 完整流水立项」。
- 用户原话与既有拍板：「对齐下理解，我们的终态是取消你这样的统一主控对吗？」（终态=取消常驻 AI 主控）；「下次你拉 codexninth 试下」「codexninth 不是 codex 用到也是 codexcli 但不是一个账号」。
- 需求理解对齐（一次一问）：主会话问"复核发现 P1 需要返工时默认怎么处理"，用户 2026-08-16 点选**「拉全新 fix worker」**（原会话恢复留目标形态）。据此放行 fresh review。

## 2. 第一轮 fresh 审核（R1）

### 2.1 审核方式与独立性

- 审核实例：headless `codex exec --sandbox read-only`（codex CLI 默认账号），fresh-context，未参与草案起草；派出指令禁止写文件、提交和再派 agent。
- 调度说明：仓库 `AGENTS.md` 无机器强制只读的 A/B reviewer 配置，按规矩登记为**侦测型降级、非机器只读**（read-only 沙箱由 codex CLI 提供，非本仓机制）。
- Git 基线：派出前 `master@568348f`，工作区 7 项既有 WIP（dh-crew 设计文档/design 57～59/P10/.claude）；审核后基线与 WIP 列表一致，reviewer 未产生文件变化。
- reviewer 引用用户原话（终态取消统一主控、codexninth 澄清、H2 点选、返工点选）并自行核查仓库事实：`tools/relay/contracts/relay-schema.ps1:90`（role 枚举 worker/reviewer/replanner；final result 四枚举）、`transition-matrix.json`（无 failed）、`runner/relay-runner.ps1:95 Get-RelayReadyNodes`（全部前置 succeeded 才启动；decision 递归冻结）、`host/run-dogfood.ps1:63`（spawner WaitForExit 同步）、`runner/relay-store.ps1:22`（`$Path.tmp` 提交名）、`relay-params.psd1`（仅 8 个超时/容量参数、无 quota/profile）。
- 原始输出留存：主控 scratchpad `review-a02-codex.out`（本文件 §2.2 为逐条摘录，结论未删改）。

### 2.2 R1 findings 与主会话裁决

VERDICT: changes-requested；P0=0，P1=8（独立根因）。

| ID | 级别 | 问题（摘） | 独立事实 | 主会话裁决 | 落点 |
|---|---|---|---|---|---|
| S1 | P1 | 新增 `result_status=failed` 改共享状态机，"只扩不改"不成立 | schema 四终态、矩阵无 failed；P1 设计"不得新增出口" | **采纳**：不加新状态；脚本节点 `succeeded + gate_outcome=pass/fail/error`，计划 `on_gate_fail` 分支 | 草案 §1.3-3、§3 |
| S2 | P1 | 计划内任意 command/cwd/extra_args/config_dir 给 planner 过大执行权与暴露面 | 宪章密钥红线；用户澄清 ninth=另一账号 | **采纳**：计划只引用 `profile_id/script_spec_id`；命令模板+hash、参数白名单、目录根、账号目录、fallback 入宿主私有注册表；禁计划内任意 CLI 参数 | §1.3-8、§3 |
| S3 | P1 | 并行两 reviewer 不满足"两轮换人交叉评估"，并写 review.md 竞态 | 宪章复核闸；Get-RelayReadyNodes 可原生表达顺序依赖 | **采纳**（用户拍板 D3）：R1 → fresh R2（读已落账 R1）→ fresh review_lead；并行专项 reviewer 只作附加 | §1.3-7、§2.1、B3 |
| S4 | P1 | closer 兼做 squash/verify/清树缺权限隔离与崩溃恢复 | 宪章 worktree 纪律；用户要求审"半途崩溃/索引脏" | **采纳**（用户拍板 D2）：closer 只备料+起草 verify 文案；宿主 finalizer 持回执按 closure journal 幂等收口；脏索引/HEAD 漂移 fail-closed | §1.3-6、§4、B8 |
| S5 | P1 | 验收自证循环与证据缺口（自举用 P2 自身卡、B8 只 fake、B7 真发≠收到、B4 profile_id≠ninth、H2 无阈值） | IHSR_05 价值来自独立 oracle 与真实用户动作 | **采纳**：H1（现 H3）改用与 relay 实现无关的真实标准卡；新增 B10 宿主外 oracle；B7 拆 provider 回执（机器）+ 用户收到截图（人验）；B4 记非秘密 account_alias/config_fingerprint；H2（现 H4）预定上限 | §5、B4/B7/B8/B10、H3/H4 |
| S6 | P1 | 范围过大，不宜一卡 | F-023 基础债未清（spawner 同步、tmp 同名） | **采纳**（用户拍板 D4）：分 7 卡逐层冻结；自动 quota 换号完整通道、任意 CLI 参数、通用动态 planner 降 P3；P2 quota 只做"高置信识别则换预登记 fallback，否则 paused" | §1.4 |
| S7 | P2 | 取消写成 dependency_blocked 语义错；B9 "tools/relay 外零改动"与治理留痕冲突 | P1 dependency_blocked 专用于新增前置 | **采纳**：run 级 `cancelled` 可信控制事件（`relay cancel`）；B9 改为"生产代码仅 tools/relay/，治理工件仅 dh-relay 精确路径，dh-crew 零改动" | §3、B9 |
| U1 | P1 | "窗口作答=确认闸"不可机器证（IHSR_05 F-006 误点+send-keys 代答实例） | 用户终态原话只取消常驻主控、未取消确认闸身份要求 | **采纳**（用户拍板 D1）：收口授权唯一凭宿主生成一次性 approval receipt（nonce/证据包哈希/范围/过期）+ 用户 `relay approve`；落户/decision 仍窗口作答，登记为弱信任、不触发发布动作 | §1.3-5、§3、§7、B8 |
| U2 | P2 | "无主控"表述误导：控制面仍在 | P1 目标=一次性编排+确定性 Runner | **采纳**：统一表述"无常驻 AI 主控"；H2 加控制面故障恢复演示 | §1.1、H4 |
| U3 | P1 | codexninth/fresh/quota fallback 概念混同；params 无 quota 表 | 用户澄清；relay-params 现状 | **采纳**：profile 回执记非秘密身份；fresh 只由新 launch/session 与上下文隔离证明；quota 按 CLI/版本冻结正反样本，识别不了不换号 | §1.2、§3、B4/B5 |
| U4 | P2 | 通知发出≠用户获知；无去重/重试/失败恢复 | 草案承诺用户只在通知时出手 | **采纳**：机器项 provider 回执 + 人验项收到截图；去重键、有界重试、`notify_failed` 事件、`relay status` 兜底 | §3、B7、H1 |
| — | — | 不可验证项：窗口输入者身份、ninth 真实账号身份、真实 quota 特征、飞书实际送达 | 仓内无外部系统回执 | 登记：D1 已由用户选回执模型；其余由对应施工卡补 identity receipt / 脱敏样本 / provider 回执 + 用户截图 | B4/B5/B7、H1 |

### 2.3 需要用户决定的问题与回答（2026-08-16，AskUserQuestion 点选）

| ID | 问题 | 用户选择 |
|---|---|---|
| D1 | 收口确认闸载体 | **收口用一次性 approval receipt**（落户/decision 窗口作答·弱信任） |
| D2 | 收口执行者 | **宿主确定性 finalizer** |
| D3 | 复核形态 | **R1 → fresh R2 → fresh review_lead** |
| D4 | P2 范围 | **分 7 卡·三项降 P3** |

### 2.4 调整

草案 v1 → v2（`design/drafts/02-完整流水-共创草案.md`）：按上表全部落地；新增 B10；H1/H2 改口径；核心决策表增 4 行。

## 3. 定向复审（R2）

<a id="review-a2"></a>

### 3.1 方式

- 审核实例：另一 fresh headless `codex exec --sandbox read-only`（默认账号；新进程、未参与 R1 与起草，仅可读仓内已落账 R1 记录）；Git 基线 `master@568348f` 前后一致，无文件变化。
- 任务：逐条判 S1～S7/U1～U4 在草案 v2 中 resolved/partially/not addressed，并查修订是否引入新问题（approve 控制事件、finalizer 顺序、script result 所有权、复审身份链、验收自证）。原始输出留 scratchpad `review-a02-r2-codex.out`。

### 3.2 R2 结论与裁决

VERDICT: changes-requested；P0=0，P1=7。核对表：S2/S6/U2 resolved；S1/S3/S4/S5/S7/U1/U3/U4 partially。

| # | 级别 | 问题（摘） | 主会话裁决 | 落点（草案 v3） |
|---|---|---|---|---|
| 1 | P1 | approval receipt 不能直接复用 P1 control event（缺 action/scope/digest/expires/消费态；`source=user`≠身份认证） | **采纳**：控制事件子类型 `action∈{approve,cancel,abort,resume}` + scope/manifest_digest/expires_at/consumed_by，一次性 CAS 消费；身份靠 nonce 一次性 + OS 用户名核对（用户拍板 D5） | §1.3-5、§3 控制事件子类型 |
| 2 | P1 | "证据包哈希"未定义，B10 自证循环 | **采纳**：规范化 release manifest（候选 SHA/预期 HEAD/路径集/复核工件哈希/gate 哈希/release_mode/Risk/verify 文案哈希/动作范围）；B10 由不 import relay 的独立脚本重算可观测项 | §3 release manifest、B10 |
| 3 | P1 | finalizer 顺序错：verify 提交应含 DevPlan/workspace 回填（仓内 DHR_02 verify 提交即含回填） | **采纳**：改为 审计→squash→复验→准备回填→原子 verify（含回填）→回读→快进→清树 | §1.3-6、§2.1 |
| 4 | P1 | 崩溃不回滚合理但恢复不变量不足（master 会停"已合入未 verify"） | **采纳并升级**（用户拍板 D6）：全部在暂存分支完成、最后一次 CAS 快进 master；master 永无中间态；`closeout_blocked` + `relay abort` | §1.3-6、§3 run 生命周期、§4、B8 |
| 5 | P1 | script result 所有权与 P1 原子晋级表冲突 | **采纳**：脚本执行器扮演 attempt worker（receipt backend=script）、只写宿主私有 candidate，Runner 照常校验晋级 | §3 节点类型 |
| 6 | P1 | 返工复审身份链与 review 工件未闭合（R1'/R2' 是新 attempt 还是新节点；旧 reviewer 可改共享 review.md） | **采纳**：同节点新 attempt（re-ready）；按 attempt 不可变 `review-<node>-<attempt>.md`；lead expected-hash 顺序 CAS 写 canonical review.md；新增 B11 | §1.3-7、§3、B11 |
| 7 | P1 | B4 仍不能证明 codex-ninth；fresh 只靠 session 不同不够 | **采纳**：拉起后取 CLI 返回脱敏账号主体与注册表 `expected_identity` 比对（取不到登记不可验证）；fresh 加"命令模板无 resume/continue/旧 session 参数" | §3 profile 身份、B4 |
| 8 | P2 | cancelled 与通知缺完整机器契约 | **采纳**：run 生命周期矩阵（active/cancelled/finalizing/done/closeout_blocked）、cancel/abort 允许阶段、通知重试/去重字段持久化 | §3 |
| 9 | P2 | H2 "≤3" 计数条件不明 | **采纳**：基础 2（落户+approve）+ 实际 decision 数 | H3/H4 |

### 3.3 需要用户决定（2026-08-16 点选）

| ID | 问题 | 用户选择 |
|---|---|---|
| D5 | 回执信任强度 | **短时 nonce + 记录/核对 OS 用户**（nonce 不落 agent 可读文件） |
| D6 | finalizer 合入方式 | **暂存分支完成后一次 CAS 快进 master** |
| D7 | 自举用哪张真实卡 | 留 B 阶段与用户定（原则：与 relay 实现无关、预期 diff/测试已知、标准档） |

### 3.4 调整

草案 v2 → v3：按上表落地；新增 B11；决策表增"回执信任强度""合入方式"两行。R2 的 7 条 P1 均属"改法对、细节未闭合"，v3 已逐条闭合到设计层粒度；字段级冻结归 B 拆出的契约卡。

## 3.5 第三轮定向复审（R3）

- 实例：第三个 fresh headless `codex exec --sandbox read-only`（默认账号，新进程，未参与起草与 R1/R2）；Git 基线 `master@568348f` 前后一致。原始输出留 scratchpad `review-a02-r3-codex.out`。
- 结论：R2 #2/#3/#4/#5/#9 resolved；#1/#6/#7/#8 partially；VERDICT changes-requested，P0=0，P1=3（均为一致性/措辞层）。reviewer 另确认：暂存分支+一次 CAS 快进与 dev-harness"squash 合入本地主干"语义不冲突；script-worker 与 P1 所有权表不冲突；run 生命周期属控制面维度，与 P1 task overall state 分离即可。
- 主会话裁决（全部采纳，草案 v3 就地闭合）：
  - P1 nonce 暴露：nonce 明文只走外部通知通道；运行现场只存不可逆 verifier；`relay status` 不显 nonce；`relay approve` 拒绝在 relay worker 会话环境内调用；同 OS 用户下 agent 与用户不可区分登记为已接受的单机单人边界（§1.3-5、§3）。
  - P1 R1/R2 写 review.md 的内部矛盾：流程图/角色表统一为 reviewer 只写按 attempt 不可变工件、lead CAS 写 canonical review.md（§2.1、§2.2）。
  - P1 B4：账号主体取不到或不匹配 → B4 不通过，不得宣称 ninth（B4）。
  - P2 生命周期：补 `awaiting_approval`、`aborted`、resume/abort 合法边，finalizing 中不接受 cancel/abort（§3）。
- 三轮 fresh 审核后无 open P0/P1；剩余全部为 B 阶段契约卡的字段级冻结事项。

## 3.6 用户对话澄清与草案 v4（2026-08-16 下午）

主会话给出整版讲解（四层/五问压缩版）后，用户未直接确认，提出澄清，逐条落地：

| 用户原话（摘） | 处置 | 落点（草案 v4） |
|---|---|---|
| 「计划、立项不在这个流程里面。relay 接力只针对已经立项、有任务的计划」 | 明确边界：relay 只接已授权卡；"planner"改名接力计划生成 agent（模板实例化） | §1.1、§1.3-1 |
| 「如何全局任意仓库都能发起」 | `relay` 入 PATH；现场落目标仓 `.dh-runtime/relay/`；用户级注册表与 runs 索引 | §1.3-9/10、§2.4 |
| 「计划放哪个文件夹，接力计划做到哪一步为止？…跨 session 或者跨天也能把这个接力计划接续下去」 | 计划在 run 目录 plans/ + active-plan；用户选**一卡可以放 2～3 个任务试验**（一 run 一批卡）；接续机制专节 | §1.3-11、§2.3、§2.4 |
| 「没看懂这个流转。这是接力吗？」 | 改为一棒一行的棒表 + 文件路径列 | §2.1 |
| 「这里只有 2 个代码复核，没有一致性、需求、教训复核」 | 三路复核显式成棒：7b 需求与一致性复核、7c 教训复核 | §1.3-8、§2.1 |
| 「收口不需要飞书。我理解就是开个 agent 窗口对话即可」「relay approve <nonce> …如果是的话，不要这一步」 | **用户推翻审核推荐 A（一次性回执 CLI）**，选窗口作答；登记弱信任 + 三护栏（先挂起 / 固定短语 / 前后截图）；飞书降为可选默认关；`relay approve` 删除 | §1.3-6、§3 确认记录、§7 |
| 「finalizer 为啥是脚本不是 agent？」 | 解释确定性/幂等/防自签；折中=收口 agent 前台 + finalizer 脚本后台 | §1.3-7 |
| 「合并到 test…需要我登录服务器输入命令测试。agent 给命令，我来输入…收口前的步骤」 | 新增可选棒 7a 集成与端到端 agent（P1 §4.2 多轮配合语义） | §2.1、§4 |
| 「应该还有 runner 和中间诊断用的 agent 吧？」 | 非业务角色表；诊断 agent 改为 Runner 自动拉起、窗口问答代发控制事件 | §1.3-12、§2.2 |
| 「终端全关了，如何接续这个接力计划呢？」 | 接续专节：控制台 `relay`、断棒判定、诊断 agent 选项、可选自启 | §2.4、B7 |

用户对上述棒表/角色/接续机制口头「好的，请写吧」→ 主会话重写草案 v4；D1（确认闸）改判记录：审核推荐 A → 用户拍板窗口作答（弱信任登记）。v4 属实质调整，另做定向复审 R4。

## 3.7 第四轮定向复审（R4，审草案 v4）与用户拍板 D8～D10

- 实例：第四个 fresh headless `codex exec --sandbox read-only`（默认账号，新进程，未参与起草与 R1～R3；已被告知用户拍板窗口作答、不得再推回执 CLI）；Git 基线 `master@568348f` 前后一致。原始输出留 scratchpad `review-a02-r4-codex.out`。
- 结论：VERDICT changes-requested，P0=0，P1=6（R4-01 命名空间/索引隔离、R4-02 多卡基线刷新、R4-03 7b/7c 语义、R4-04 确认闸证据由 agent 自产、R4-05 诊断 agent 只读与控制权冲突、R4-06 test 分支写入契约）+ P2×3（R4-07 接续先摄入结果、R4-08 反例验收、R4-09 卡数）。
- 需用户决定（2026-08-16 点选）：

| ID | 问题 | 用户选择 |
|---|---|---|
| D8 | 并行卡同路径 / 后卡基线落后 | **重叠卡不并行 + 落后自动 rebase 重过闸** |
| D9 | 仓级注册表覆盖 / 登录自启 / 飞书 adapter | **三项都降 P3** |
| D10 | 7a test 分支 | **允许 relay push 远端 `test`**（审核建议本地为主；用户选更彻底自动化）→ 主会话收紧为：run 级 `-AllowTestPush` 授权 + 注册表 push 白名单仅 `test`（master 硬拒）+ 记远端原 HEAD + `--force-with-lease` 回滚 + 同仓串行锁；登记为 P1 "永不 push" 的唯一受控例外 |

- 主会话裁决（全部采纳，草案 v5）：R4-01 经 canonical runtime resolver 解析命名空间、索引原子写+锁、run_id 带仓前缀、索引不含路径（§1.3-10）；R4-02 静态重叠检查+落后 rebase 复跑闸（§1.3-11、B12）；R4-03 7b 一致性范围改 brief↔DevPlan↔证据、needs_evidence 只重跑目标棒及下游闸、改代码走 rework 全套，7c 明确不阻断（§2.1）；R4-04 三护栏由宿主持有：event_id 时序核对、宿主拍截图写哈希（§1.3-6、§3 确认记录）；R4-05 明确为 P2 对 P1 的放宽，actor/on_behalf_of/user_reply 字段与预定义 action，Runner 唯一消费者（§1.3-12、§3）；R4-06 见 D10（§1.3-13、B13）；R4-07 续跑先摄入未消费结果再判断棒（§2.4、B7）；R4-08 反例并入 B7/B12/B13；R4-09 卡数改 8～10、每卡一个安全边界（§1.4）。

## 3.8 第五轮收敛复审（R5，审草案 v5）

- 实例：第五个 fresh headless `codex exec --sandbox read-only`（默认账号，新进程）；Git 基线 `master@568348f` 前后一致。原始输出留 scratchpad `review-a02-r5-codex.out`。
- 结论：R4-01～R4-09 全部 resolved；仅报 2 条一致性矛盾（P1）：①§2.4 登录自启 / §3 通知契约与 D9 降 P3 冲突 → 已改为 P3、P2 不做；②卡数 8～10 与 D4 "分 7 卡" 不一致 → 说明范围已扩（多卡/集成/诊断），在整版确认时请用户一并认可。
- 五轮 fresh 审核后无 open P0/P1；剩余全部为 B 阶段契约卡的字段级冻结事项。

<a id="understanding-a"></a>

## 4. 讲解与理解确认

<!-- dh:planning-evidence:v1 event=DHR-A-02 artifact=design/02-完整流水-产品设计与验收.md kind=understanding -->

### 4.1 讲解（四层五问，主会话 2026-08-16 对话中给出的压缩版）

- **全局地图**：入口=用户对话授权卡 + 任意仓 `relay start`；流转=每卡 0～8' 棒（接力计划生成→workspace→施工→机器闸→R1→R2→汇合→[集成/端到端]→需求复核→教训复核→收口 agent→finalizer 脚本）；状态与结果存该仓 `.dh-runtime/relay/<run_id>/`（events/plans/launches/attempts/closeout）+ 工作区 `<ws>/*` + 任务树 `D:\wt\<卡>`；更新方式=文件两阶段写 + Runner CAS；权限与影响面=新代码只在 `tools/relay/`，dh-crew 零改动，master 只经 finalizer 一次快进，push 仅 test。
- **契约/定义**：Runner 不判质量（verdict/gate_outcome 枚举分支）；P1 状态机不动；三路复核成棒、返工/补证有界；确认闸=窗口作答（弱信任 + 宿主三护栏）；test push 受控例外；诊断 agent 代发预定义控制事件。
- **运作机制**：为何影响正确性/可靠性——机器闸与 finalizer 是脚本以保证幂等与防自签；暂存分支保证 master 无中间态；重叠卡串行与落后 rebase 保证多卡不互踩；接续先摄入结果再判断棒保证不丢已交棒。
- **代码层**：按需，本轮未讲。
- **五问**：进入=授权+`relay start`；存放=run 目录/工作区/任务树；承诺=上列契约；验证=B1～B13 + B10 宿主外 oracle + H3 自举/H4 对照；失败=paused/closeout_blocked → 诊断 agent 窗口问答 → resume/retry/reset/cancel/abort/rollback_test。

### 4.2 理解问题与回答

- 理解问题（一次一问）：复核发现 P1 需要返工时，默认拉全新 fix worker 还是保留原施工窗口？→ 用户点选「拉全新 fix worker」（2026-08-16）。
- 用户主动澄清 6 条（§3.6）与拍板 D2～D10（§2.3/§3.3/§3.7），均已落草案。

### 4.3 设计整版确认

- 2026-08-16 主会话给出整版讲解后，用户 AskUserQuestion 点选「**确认整版，落盘（含 8～10 卡）**」。
- 落盘动作：drafts/02 v5 原子晋级为 `design/02-完整流水-产品设计与验收.md`（planning-event `DHR-A-02`）；`design/README.md` 白名单增补 02；drafts/02 改为存档指针；design/01 更新日志加一行指向 02。
- 本次确认**不授权** B 拆计划或任何卡开工；自举卡选择留 B 阶段与用户定。
