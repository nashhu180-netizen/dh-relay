# DH Relay P2 完整流水 · A 交叉审核记录

> 性质：A 过程证据，不属于 `designInputs[]`，不得作为 B 的补充输入。正式结论只认 `design/02-完整流水-产品设计与验收.md`（晋级后）；晋级前草案在 `design/drafts/02-完整流水-共创草案.md`。

<a id="review-a"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-02 artifact=design/02-完整流水-产品设计与验收.md kind=review -->

## 1. 起因与需求理解对齐

- 2026-08-16 用户点选 DHR_03 H2=「值得，进完整流水」，随后指令「P2 完整流水立项」。
- 用户原话与既有拍板：「对齐下理解，我们的终态是取消你这样的统一主控对吗？」（终态=取消常驻 AI 主控）；「下次你拉 codexninth 试下」「codexninth 不是 codex 用到也是 codexcli 但不是一个账号」。
- 需求理解对齐（一次一问）：主会话问"复核发现 P1 需要返工时默认怎么处理"，用户 2026-08-16 点选**「拉全新 fix worker」**（原会话恢复留目标形态）。据此放行 fresh review。

## 2. 第一轮 fresh 审核（R1）

### 2.1 审核方式与独立性

- 审核实例：headless `codex exec --sandbox read-only`（codex CLI 默认账号），fresh-context，未参与草案起草；派出指令禁止写文件、提交和再派 agent。
- 调度说明：仓库 `AGENTS.md` 无机器强制只读的 A/B reviewer 配置，按规矩登记为**侦测型降级、非机器只读**（read-only 沙箱由 codex CLI 提供，非本仓机制）。
- Git 基线：派出前 `master@568348f`，工作区 7 项既有 WIP（dh-crew 设计文档/design 57～59/P10/.claude）；审核后基线与 WIP 列表一致，reviewer 未产生文件变化。
- reviewer 引用用户原话（终态取消统一主控、codexninth 澄清、H2 点选、返工点选）并自行核查仓库事实：`tools/relay/contracts/relay-schema.ps1:90`（role 枚举 worker/reviewer/replanner；final result 四枚举）、`transition-matrix.json`（无 failed）、`runner/relay-runner.ps1:95 Get-RelayReadyNodes`（全部前置 succeeded 才启动；decision 递归冻结）、`host/run-dogfood.ps1:63`（spawner WaitForExit 同步）、`runner/relay-store.ps1:22`（`$Path.tmp` 提交名）、`relay-params.psd1`（仅 8 个超时/容量参数、无 quota/profile）。
- 原始输出留存：主控 scratchpad `review-a02-codex.out`（本文件 §2.2 为逐条摘录，结论未删改）。

### 2.2 R1 findings 与主会话裁决

VERDICT: changes-requested；P0=0，P1=8（独立根因）。

| ID | 级别 | 问题（摘） | 独立事实 | 主会话裁决 | 落点 |
|---|---|---|---|---|---|
| S1 | P1 | 新增 `result_status=failed` 改共享状态机，"只扩不改"不成立 | schema 四终态、矩阵无 failed；P1 设计"不得新增出口" | **采纳**：不加新状态；脚本节点 `succeeded + gate_outcome=pass/fail/error`，计划 `on_gate_fail` 分支 | 草案 §1.3-3、§3 |
| S2 | P1 | 计划内任意 command/cwd/extra_args/config_dir 给 planner 过大执行权与暴露面 | 宪章密钥红线；用户澄清 ninth=另一账号 | **采纳**：计划只引用 `profile_id/script_spec_id`；命令模板+hash、参数白名单、目录根、账号目录、fallback 入宿主私有注册表；禁计划内任意 CLI 参数 | §1.3-8、§3 |
| S3 | P1 | 并行两 reviewer 不满足"两轮换人交叉评估"，并写 review.md 竞态 | 宪章复核闸；Get-RelayReadyNodes 可原生表达顺序依赖 | **采纳**（用户拍板 D3）：R1 → fresh R2（读已落账 R1）→ fresh review_lead；并行专项 reviewer 只作附加 | §1.3-7、§2.1、B3 |
| S4 | P1 | closer 兼做 squash/verify/清树缺权限隔离与崩溃恢复 | 宪章 worktree 纪律；用户要求审"半途崩溃/索引脏" | **采纳**（用户拍板 D2）：closer 只备料+起草 verify 文案；宿主 finalizer 持回执按 closure journal 幂等收口；脏索引/HEAD 漂移 fail-closed | §1.3-6、§4、B8 |
| S5 | P1 | 验收自证循环与证据缺口（自举用 P2 自身卡、B8 只 fake、B7 真发≠收到、B4 profile_id≠ninth、H2 无阈值） | IHSR_05 价值来自独立 oracle 与真实用户动作 | **采纳**：H1（现 H3）改用与 relay 实现无关的真实标准卡；新增 B10 宿主外 oracle；B7 拆 provider 回执（机器）+ 用户收到截图（人验）；B4 记非秘密 account_alias/config_fingerprint；H2（现 H4）预定上限 | §5、B4/B7/B8/B10、H3/H4 |
| S6 | P1 | 范围过大，不宜一卡 | F-023 基础债未清（spawner 同步、tmp 同名） | **采纳**（用户拍板 D4）：分 7 卡逐层冻结；自动 quota 换号完整通道、任意 CLI 参数、通用动态 planner 降 P3；P2 quota 只做"高置信识别则换预登记 fallback，否则 paused" | §1.4 |
| S7 | P2 | 取消写成 dependency_blocked 语义错；B9 "tools/relay 外零改动"与治理留痕冲突 | P1 dependency_blocked 专用于新增前置 | **采纳**：run 级 `cancelled` 可信控制事件（`relay cancel`）；B9 改为"生产代码仅 tools/relay/，治理工件仅 dh-relay 精确路径，dh-crew 零改动" | §3、B9 |
| U1 | P1 | "窗口作答=确认闸"不可机器证（IHSR_05 F-006 误点+send-keys 代答实例） | 用户终态原话只取消常驻主控、未取消确认闸身份要求 | **采纳**（用户拍板 D1）：收口授权唯一凭宿主生成一次性 approval receipt（nonce/证据包哈希/范围/过期）+ 用户 `relay approve`；落户/decision 仍窗口作答，登记为弱信任、不触发发布动作 | §1.3-5、§3、§7、B8 |
| U2 | P2 | "无主控"表述误导：控制面仍在 | P1 目标=一次性编排+确定性 Runner | **采纳**：统一表述"无常驻 AI 主控"；H2 加控制面故障恢复演示 | §1.1、H4 |
| U3 | P1 | codexninth/fresh/quota fallback 概念混同；params 无 quota 表 | 用户澄清；relay-params 现状 | **采纳**：profile 回执记非秘密身份；fresh 只由新 launch/session 与上下文隔离证明；quota 按 CLI/版本冻结正反样本，识别不了不换号 | §1.2、§3、B4/B5 |
| U4 | P2 | 通知发出≠用户获知；无去重/重试/失败恢复 | 草案承诺用户只在通知时出手 | **采纳**：机器项 provider 回执 + 人验项收到截图；去重键、有界重试、`notify_failed` 事件、`relay status` 兜底 | §3、B7、H1 |
| — | — | 不可验证项：窗口输入者身份、ninth 真实账号身份、真实 quota 特征、飞书实际送达 | 仓内无外部系统回执 | 登记：D1 已由用户选回执模型；其余由对应施工卡补 identity receipt / 脱敏样本 / provider 回执 + 用户截图 | B4/B5/B7、H1 |

### 2.3 需要用户决定的问题与回答（2026-08-16，AskUserQuestion 点选）

| ID | 问题 | 用户选择 |
|---|---|---|
| D1 | 收口确认闸载体 | **收口用一次性 approval receipt**（落户/decision 窗口作答·弱信任） |
| D2 | 收口执行者 | **宿主确定性 finalizer** |
| D3 | 复核形态 | **R1 → fresh R2 → fresh review_lead** |
| D4 | P2 范围 | **分 7 卡·三项降 P3** |

### 2.4 调整

草案 v1 → v2（`design/drafts/02-完整流水-共创草案.md`）：按上表全部落地；新增 B10；H1/H2 改口径；核心决策表增 4 行。

## 3. 定向复审（R2）

<a id="review-a2"></a>

### 3.1 方式

- 审核实例：另一 fresh headless `codex exec --sandbox read-only`（默认账号；新进程、未参与 R1 与起草，仅可读仓内已落账 R1 记录）；Git 基线 `master@568348f` 前后一致，无文件变化。
- 任务：逐条判 S1～S7/U1～U4 在草案 v2 中 resolved/partially/not addressed，并查修订是否引入新问题（approve 控制事件、finalizer 顺序、script result 所有权、复审身份链、验收自证）。原始输出留 scratchpad `review-a02-r2-codex.out`。

### 3.2 R2 结论与裁决

VERDICT: changes-requested；P0=0，P1=7。核对表：S2/S6/U2 resolved；S1/S3/S4/S5/S7/U1/U3/U4 partially。

| # | 级别 | 问题（摘） | 主会话裁决 | 落点（草案 v3） |
|---|---|---|---|---|
| 1 | P1 | approval receipt 不能直接复用 P1 control event（缺 action/scope/digest/expires/消费态；`source=user`≠身份认证） | **采纳**：控制事件子类型 `action∈{approve,cancel,abort,resume}` + scope/manifest_digest/expires_at/consumed_by，一次性 CAS 消费；身份靠 nonce 一次性 + OS 用户名核对（用户拍板 D5） | §1.3-5、§3 控制事件子类型 |
| 2 | P1 | "证据包哈希"未定义，B10 自证循环 | **采纳**：规范化 release manifest（候选 SHA/预期 HEAD/路径集/复核工件哈希/gate 哈希/release_mode/Risk/verify 文案哈希/动作范围）；B10 由不 import relay 的独立脚本重算可观测项 | §3 release manifest、B10 |
| 3 | P1 | finalizer 顺序错：verify 提交应含 DevPlan/workspace 回填（仓内 DHR_02 verify 提交即含回填） | **采纳**：改为 审计→squash→复验→准备回填→原子 verify（含回填）→回读→快进→清树 | §1.3-6、§2.1 |
| 4 | P1 | 崩溃不回滚合理但恢复不变量不足（master 会停"已合入未 verify"） | **采纳并升级**（用户拍板 D6）：全部在暂存分支完成、最后一次 CAS 快进 master；master 永无中间态；`closeout_blocked` + `relay abort` | §1.3-6、§3 run 生命周期、§4、B8 |
| 5 | P1 | script result 所有权与 P1 原子晋级表冲突 | **采纳**：脚本执行器扮演 attempt worker（receipt backend=script）、只写宿主私有 candidate，Runner 照常校验晋级 | §3 节点类型 |
| 6 | P1 | 返工复审身份链与 review 工件未闭合（R1'/R2' 是新 attempt 还是新节点；旧 reviewer 可改共享 review.md） | **采纳**：同节点新 attempt（re-ready）；按 attempt 不可变 `review-<node>-<attempt>.md`；lead expected-hash 顺序 CAS 写 canonical review.md；新增 B11 | §1.3-7、§3、B11 |
| 7 | P1 | B4 仍不能证明 codex-ninth；fresh 只靠 session 不同不够 | **采纳**：拉起后取 CLI 返回脱敏账号主体与注册表 `expected_identity` 比对（取不到登记不可验证）；fresh 加"命令模板无 resume/continue/旧 session 参数" | §3 profile 身份、B4 |
| 8 | P2 | cancelled 与通知缺完整机器契约 | **采纳**：run 生命周期矩阵（active/cancelled/finalizing/done/closeout_blocked）、cancel/abort 允许阶段、通知重试/去重字段持久化 | §3 |
| 9 | P2 | H2 "≤3" 计数条件不明 | **采纳**：基础 2（落户+approve）+ 实际 decision 数 | H3/H4 |

### 3.3 需要用户决定（2026-08-16 点选）

| ID | 问题 | 用户选择 |
|---|---|---|
| D5 | 回执信任强度 | **短时 nonce + 记录/核对 OS 用户**（nonce 不落 agent 可读文件） |
| D6 | finalizer 合入方式 | **暂存分支完成后一次 CAS 快进 master** |
| D7 | 自举用哪张真实卡 | 留 B 阶段与用户定（原则：与 relay 实现无关、预期 diff/测试已知、标准档） |

### 3.4 调整

草案 v2 → v3：按上表落地；新增 B11；决策表增"回执信任强度""合入方式"两行。R2 的 7 条 P1 均属"改法对、细节未闭合"，v3 已逐条闭合到设计层粒度；字段级冻结归 B 拆出的契约卡。

## 3.5 第三轮定向复审（R3）

- 实例：第三个 fresh headless `codex exec --sandbox read-only`（默认账号，新进程，未参与起草与 R1/R2）；Git 基线 `master@568348f` 前后一致。原始输出留 scratchpad `review-a02-r3-codex.out`。
- 结论：R2 #2/#3/#4/#5/#9 resolved；#1/#6/#7/#8 partially；VERDICT changes-requested，P0=0，P1=3（均为一致性/措辞层）。reviewer 另确认：暂存分支+一次 CAS 快进与 dev-harness"squash 合入本地主干"语义不冲突；script-worker 与 P1 所有权表不冲突；run 生命周期属控制面维度，与 P1 task overall state 分离即可。
- 主会话裁决（全部采纳，草案 v3 就地闭合）：
  - P1 nonce 暴露：nonce 明文只走外部通知通道；运行现场只存不可逆 verifier；`relay status` 不显 nonce；`relay approve` 拒绝在 relay worker 会话环境内调用；同 OS 用户下 agent 与用户不可区分登记为已接受的单机单人边界（§1.3-5、§3）。
  - P1 R1/R2 写 review.md 的内部矛盾：流程图/角色表统一为 reviewer 只写按 attempt 不可变工件、lead CAS 写 canonical review.md（§2.1、§2.2）。
  - P1 B4：账号主体取不到或不匹配 → B4 不通过，不得宣称 ninth（B4）。
  - P2 生命周期：补 `awaiting_approval`、`aborted`、resume/abort 合法边，finalizing 中不接受 cancel/abort（§3）。
- 三轮 fresh 审核后无 open P0/P1；剩余全部为 B 阶段契约卡的字段级冻结事项。

## 3.6 用户对话澄清与草案 v4（2026-08-16 下午）

主会话给出整版讲解（四层/五问压缩版）后，用户未直接确认，提出澄清，逐条落地：

| 用户原话（摘） | 处置 | 落点（草案 v4） |
|---|---|---|
| 「计划、立项不在这个流程里面。relay 接力只针对已经立项、有任务的计划」 | 明确边界：relay 只接已授权卡；"planner"改名接力计划生成 agent（模板实例化） | §1.1、§1.3-1 |
| 「如何全局任意仓库都能发起」 | `relay` 入 PATH；现场落目标仓 `.dh-runtime/relay/`；用户级注册表与 runs 索引 | §1.3-9/10、§2.4 |
| 「计划放哪个文件夹，接力计划做到哪一步为止？…跨 session 或者跨天也能把这个接力计划接续下去」 | 计划在 run 目录 plans/ + active-plan；用户选**一卡可以放 2～3 个任务试验**（一 run 一批卡）；接续机制专节 | §1.3-11、§2.3、§2.4 |
| 「没看懂这个流转。这是接力吗？」 | 改为一棒一行的棒表 + 文件路径列 | §2.1 |
| 「这里只有 2 个代码复核，没有一致性、需求、教训复核」 | 三路复核显式成棒：7b 需求与一致性复核、7c 教训复核 | §1.3-8、§2.1 |
| 「收口不需要飞书。我理解就是开个 agent 窗口对话即可」「relay approve <nonce> …如果是的话，不要这一步」 | **用户推翻审核推荐 A（一次性回执 CLI）**，选窗口作答；登记弱信任 + 三护栏（先挂起 / 固定短语 / 前后截图）；飞书降为可选默认关；`relay approve` 删除 | §1.3-6、§3 确认记录、§7 |
| 「finalizer 为啥是脚本不是 agent？」 | 解释确定性/幂等/防自签；折中=收口 agent 前台 + finalizer 脚本后台 | §1.3-7 |
| 「合并到 test…需要我登录服务器输入命令测试。agent 给命令，我来输入…收口前的步骤」 | 新增可选棒 7a 集成与端到端 agent（P1 §4.2 多轮配合语义） | §2.1、§4 |
| 「应该还有 runner 和中间诊断用的 agent 吧？」 | 非业务角色表；诊断 agent 改为 Runner 自动拉起、窗口问答代发控制事件 | §1.3-12、§2.2 |
| 「终端全关了，如何接续这个接力计划呢？」 | 接续专节：控制台 `relay`、断棒判定、诊断 agent 选项、可选自启 | §2.4、B7 |

用户对上述棒表/角色/接续机制口头「好的，请写吧」→ 主会话重写草案 v4；D1（确认闸）改判记录：审核推荐 A → 用户拍板窗口作答（弱信任登记）。v4 属实质调整，另做定向复审 R4。

## 3.7 第四轮定向复审（R4，审草案 v4）与用户拍板 D8～D10

- 实例：第四个 fresh headless `codex exec --sandbox read-only`（默认账号，新进程，未参与起草与 R1～R3；已被告知用户拍板窗口作答、不得再推回执 CLI）；Git 基线 `master@568348f` 前后一致。原始输出留 scratchpad `review-a02-r4-codex.out`。
- 结论：VERDICT changes-requested，P0=0，P1=6（R4-01 命名空间/索引隔离、R4-02 多卡基线刷新、R4-03 7b/7c 语义、R4-04 确认闸证据由 agent 自产、R4-05 诊断 agent 只读与控制权冲突、R4-06 test 分支写入契约）+ P2×3（R4-07 接续先摄入结果、R4-08 反例验收、R4-09 卡数）。
- 需用户决定（2026-08-16 点选）：

| ID | 问题 | 用户选择 |
|---|---|---|
| D8 | 并行卡同路径 / 后卡基线落后 | **重叠卡不并行 + 落后自动 rebase 重过闸** |
| D9 | 仓级注册表覆盖 / 登录自启 / 飞书 adapter | **三项都降 P3** |
| D10 | 7a test 分支 | **允许 relay push 远端 `test`**（审核建议本地为主；用户选更彻底自动化）→ 主会话收紧为：run 级 `-AllowTestPush` 授权 + 注册表 push 白名单仅 `test`（master 硬拒）+ 记远端原 HEAD + `--force-with-lease` 回滚 + 同仓串行锁；登记为 P1 "永不 push" 的唯一受控例外 |

- 主会话裁决（全部采纳，草案 v5）：R4-01 经 canonical runtime resolver 解析命名空间、索引原子写+锁、run_id 带仓前缀、索引不含路径（§1.3-10）；R4-02 静态重叠检查+落后 rebase 复跑闸（§1.3-11、B12）；R4-03 7b 一致性范围改 brief↔DevPlan↔证据、needs_evidence 只重跑目标棒及下游闸、改代码走 rework 全套，7c 明确不阻断（§2.1）；R4-04 三护栏由宿主持有：event_id 时序核对、宿主拍截图写哈希（§1.3-6、§3 确认记录）；R4-05 明确为 P2 对 P1 的放宽，actor/on_behalf_of/user_reply 字段与预定义 action，Runner 唯一消费者（§1.3-12、§3）；R4-06 见 D10（§1.3-13、B13）；R4-07 续跑先摄入未消费结果再判断棒（§2.4、B7）；R4-08 反例并入 B7/B12/B13；R4-09 卡数改 8～10、每卡一个安全边界（§1.4）。

## 3.8 第五轮收敛复审（R5，审草案 v5）

- 实例：第五个 fresh headless `codex exec --sandbox read-only`（默认账号，新进程）；Git 基线 `master@568348f` 前后一致。原始输出留 scratchpad `review-a02-r5-codex.out`。
- 结论：R4-01～R4-09 全部 resolved；仅报 2 条一致性矛盾（P1）：①§2.4 登录自启 / §3 通知契约与 D9 降 P3 冲突 → 已改为 P3、P2 不做；②卡数 8～10 与 D4 "分 7 卡" 不一致 → 说明范围已扩（多卡/集成/诊断），在整版确认时请用户一并认可。
- 五轮 fresh 审核后无 open P0/P1；剩余全部为 B 阶段契约卡的字段级冻结事项。

<a id="review-a3"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-03 artifact=design/02-完整流水-产品设计与验收.md kind=review -->

## 3.9 第六轮交叉审核（R6 · codex gpt-5.6-sol · xhigh，审已落盘的正式输入 02 @ `cf5b973`）——事件 DHR-A-03

- 触发：用户 2026-08-16 指令「让 codex 5.6-sol xhigh 交叉审核下」。实例：`codex exec -m gpt-5.6-sol -c model_reasoning_effort=xhigh --sandbox read-only`（独立进程，未参与起草与 R1～R5；前五轮为同模型 medium 档）；Git 基线 `cf5b973` 前后一致。原始输出留 scratchpad `review-a02-r6-codex.fixed.md`（控制台编码乱码已转码，内容未改）。
- reviewer 自核仓库事实：`relay-schema.ps1:83-97,200-283`（v1 严格枚举、未知字段 fail-closed）、`relay-runner.ps1:53-91`（generation 为 run 全局）、`relay-host.ps1:58-95`（摄入顺序、只扫 launched）、`Save-RelayScreenshot:140-157`（拼接全部 RELAY 窗口）、`psmux-adapter.ps1:112-126`（已持唯一 window_title）、H2 试点证据。
- 结论：**changes-requested，P0=0，P1=9**（medium 档五轮未逮到的结构性问题）。逐条裁决（全部采纳，写入 02 同事件修订）：

| # | 级别 | 问题（摘） | 裁决 → 落点（design/02） |
|---|---|---|---|
| S1 | P1 | "只扩不改"不成立：Runner 校验确认字段/接受诊断控制事件与 P1 §3.4 冲突，v1 严格 schema 会拒新字段 | `relay/v2` 双解析器；只承诺 v1 枚举/合法边/只读解析不变；确认字段校验与诊断控制登记为 v2 版本化例外；v1 存量 run 只读不续跑（用户拍板 D13）→ §3 版本与兼容、B1 |
| S2 | P1 | run 全局 generation 与"其它卡照常"冲突：某卡重编排换届会把并行卡的合法交棒判 stale | 用户拍板 D11：保持 run 全局 generation；重编排只限本卡内加前置棒；生效时**不受影响卡在跑棒交完再切届、下一棒 fresh 续，受影响卡依赖冻结**；卡级 generation 留 P3 → 决策 11、§2.3、B12 |
| S3 | P1 | closer→finalizer 触发闭环矛盾（agent 调脚本=持 Git 权限；先等脚本又无法 ready） | closer 只提交 `succeeded + next_action=finalize`；Runner 校验护栏后签发 finalizer script receipt、宿主执行；agent 不可调用 → 决策 7、棒 8/8′、B8 |
| S4 | P1 | 自动 rebase 后复核/e2e/manifest/用户确认全绑旧候选 | 用户拍板 D12：rebase → 新 candidate generation；patch 哈希不变→只重新弹窗确认（新 manifest）；patch 变→重跑机器闸与 R1′→R2′→lead′（及 e2e）后重确认；复核工件绑定 base_sha+candidate_tree_sha+diff_hash → 决策 11、B12 |
| S5 | P1 | 三护栏无真实时序屏障（同 tick 摄入 checkpoint 与 result、截图可能回答后同刻拍成、拼图非精确窗口） | 宿主 `decision_ack`（Runner 接受 checkpoint + 同 launch 精确单窗口截图后写）→ agent 收 ACK 才展示确认；result 摄入再拍精确窗口；零窗口/占位图=失败；B8 加真实 psmux 快速作答反例 → §3 确认记录、棒 8、B8 |
| S6 | P1 | 诊断代发控制事件缺目标身份与能力约束 | 控制 candidate 必带 target 身份链 + expected_state_hash + option_id（须来自该次 checkpoint 声明选项）；宿主提供单次/目标绑定/选项绑定的窄工具；Runner 重验 → §3 控制事件子类型、B7 |
| S7 | P1 | 接续"先摄入再判断棒"仍有 TOCTOU（首扫后、probe 前落 result 被永久拒收） | run 恢复锁：冻结新 launch → 原子 claim 摄入 → probe → 二次 claim/双空扫描 → 才 CAS interrupted；日常 tick 对非 launched 节点的迟到合法 result 也摄入按身份链判 → §2.4 续跑判定、B7 |
| S8 | P1 | test push 例外权限边界不在 relay 侧；白名单未冻结 remote；lease 冲突/cancel-after-push 未定 | 白名单冻结 remote URL 指纹 + refs/heads/test；远端 ACL 限 test（不可验证项，施工者脱敏回读+用户确认）；journal 冻结指纹/原 HEAD/source SHA/refspec；lease 冲突或补偿失败→closeout_blocked 不宣称回滚；cancel-after-push 先补偿 → 决策 13、B13 |
| S9 | P1 | tracked 写入落主工作树会弄脏 master/并发追加；未禁两 run 认领同卡 | 所有 tracked 写入落任务树、主树只读；`(canonical_repo, task_id)` claim lease，一仓同卡一个 active run → §3 运行现场与所有权、B7 |
| P2 | — | 验收自证/样本空洞（B12 只 fake、H3 未覆盖并行/重叠/落后、B8 无真实 psmux、B10 信任 manifest） | B12 加真实临时 Git 仓三卡剧本；B8 真实 psmux；宿主外 oracle 从 start 时 authority 快照独立推导授权卡/路径 → B8/B12 |
| U1～U4 | P2 | H4 计数应含 run 级动作与服务器操作；cancelled≠收口；"旧 run 可读"≠可续跑；8～10 卡只是估算 | H4 补计数与 `done-complete/done-partial-cancelled`；§3 版本与兼容明确 v1 只读；范围超 10 卡回 A → H4、§2.3、§3 |
| 不可验证 | — | 窗口输入者身份、ninth 账号主体/quota 样本、远端 test ACL、`relay` 安装/PATH 契约 | 已登记：前两项同前；ACL 由 B13 施工者回读；安装契约归控制台/注册表卡 |

- 需用户决定（2026-08-16 点选）：D11 多卡重编排=甲（用户先追问"届次号"含义，主会话解释后用户表述"重编排对并行任务无影响就继续、有影响干完手上暂停"，主会话确认与甲一致并按此写入）；D12 rebase 后=patch 不变只重确认、变了全套重跑；D13 v1 存量 run=只读不续跑。
- 修订落盘：design/02 以事件 `DHR-A-03` 修订（更新日志第 2 行）；决策清单增 7 行；B1/B7/B8/B12/B13/H4 补真实反例。

<a id="understanding-a3"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-03 artifact=design/02-完整流水-产品设计与验收.md kind=understanding -->

### 3.10 修订讲解与理解确认（DHR-A-03）

- 讲解（对话中）：用户追问"届次号是什么 / 影响啥"，主会话用"当前有效计划的版本号、派工单盖章、旧版结果拒收"解释，说明它只影响 Runner 认不认交回的结果、对用户/代码/DevPlan 无感；两方案对用户可见差别仅"其它卡是否短暂等待"。
- 理解确认：用户主动复述「如果重编排对并行任务没有影响，那其他任务就继续。如果有影响，干完手上的任务暂停」→ 主会话确认与方案甲一致并精确化为"计算期间照常；生效时不受影响卡交完在跑棒再切届、受影响卡依赖冻结"，写入 02 决策 11 与 §2.3。
- 拍板：D11 甲；D12 patch 不变只重确认/变了全套重跑；D13 v1 只读不续跑。本修订不改变已确认的目标/范围/人验项，属同日 A 修订；不授权 B 或任何卡开工。

<a id="review-a4"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-04 artifact=design/02-完整流水-产品设计与验收.md kind=review -->

## 3.11 第七轮：B 拆计划交叉审核回流（R7）——事件 DHR-A-04

- 触发：用户 2026-08-16 指令「你让 codex 做 B 拆 P2 DevPlan」。**起草人**=`codex exec -m gpt-5.6-sol -c model_reasoning_effort=xhigh --sandbox workspace-write`（只准写 `dev_plan/drafts/草案-P2-完整流水-开发方案.md` 一个文件、禁 git 写；实测只新建该文件，主树其余状态不变），产出 10 张卡草案 `DHR_04～DHR_13`。
- **B reviewer**（另派 fresh 实例，未参与起草、未参与 R1～R6）：`codex exec -m gpt-5.6-sol -c model_reasoning_effort=xhigh --sandbox read-only`；Git 基线 `e85a5eb` 前后一致；原始输出留 scratchpad `b-p2-review-codex.out`。结论 **changes-requested，P0=0，P1=5**。
- reviewer 自核仓库事实：P1 只发号至 `DHR_03`；`relay-schema.ps1` 仍固定 `relay/v1`、角色仅三个；`tools/relay/{cli,templates,finalizer,integration,oracle}` 均不存在；`relay-store.ps1` 仍用固定 `$Path.tmp`；宿主仍同步调用 replanner spawner；`.consumed-*` 仍持续累加。主会话独立复核了后四条（`tools/relay` 只有 `contracts/runner/adapters/host/dogfood/tests` 六个子目录），并独立重算了 B1～B13/H3/H4 的覆盖表，与 reviewer 一致。

| # | 级别 | 问题（摘） | 主会话裁决 |
|---|---|---|---|
| R7-01 | P1 | 验收 ID 形式全覆盖，但四条流水行为**没有任何验收 ID 能证明**：未授权卡必须被拒、计划生成器不得改卡的目标/验收、落户棒真产出 8 件套、教训复核棒真跑 → B1～B13 全绿也证明不了流水走完 | **采纳（主会话独立复核成立：§6.1 十三条逐条比对，确无覆盖）**。B 不得自造验收终点 → 回 A 补 **B14**（授权边界与计划生成约束）与 **B17**（棒完整性） |
| R7-02 | P1 | 为守 10 卡上限，多个独立安全边界被压进同卡（DHR_09=恢复锁+quota+控制台+诊断权限；DHR_10=确认闸+finalizer/master CAS；DHR_12=多轮协作+远端 push） | **部分采纳**。根因不在 B 而在 A：**B7 与 B13 各自打包了两个安全边界** → 原子化拆出 **B15**（诊断代发控制事件）与 **B16**（远端 test push 与补偿）。**不采纳拆 DHR_10**：确认闸的存在意义就是决定 finalizer receipt 签不签发，两者是同一条收口授权链、由同一条 B8 覆盖，拆开后两边都无法单独验收。另澄清：`design/02 §1.4` 原文即写"B 拆时可合并/拆分"，10 张**不是硬上限**，故纯卡数问题无需回 A；卡数估算随验收项增加上调为 12～13 |
| R7-03 | P1 | 草案在 `drafts/` 下用 `../../design/…`，晋级到 `dev_plan/` 后全部回链断裂 | **采纳**，属 B 落盘前的机械修正（改 `../design/…` 并跑链接检查），不影响本次 A 修订 |
| R7-04 | P2 | 过度串行：DHR_05（宿主基础债）不必等 v2 契约；DHR_09 不必等 DHR_07 脚本执行器 | **采纳**，归 B 重拆时调整（`DHR_04 ∥ DHR_05`；DHR_09 只依赖控制台/注册表卡） |
| R7-05 | P2 | DHR_13 颗粒度失控：新建 oracle + 隔离守卫 + 驱动 2～3 张真实卡 + B9/B10/H3/H4 + P3 缺口判断，且自举卡未选 | **采纳**，归 B 重拆。主会话补一条 reviewer 未提的同源问题：**B9 隔离守卫（生产代码只改 `tools/relay/`、dh-crew 零改动）是全程红线**，排在最后一张卡等于前九张卡施工期无机器守卫 → B 重拆时前移到契约地基卡 |
| R7-U1 | P1 | "第 2 批已有第一个端到端 demo"是未被验收约束的宣传：两卡互不依赖，无人负责验"CLI 启动→真实 profile→宿主 script gate"组合链路 | **采纳**，归 B 重拆：要么给该批指定组合演示的责任卡，要么把首个产品级端到端垂直切片诚实标到能证明它的那一批；不得制造无验收支撑的批次承诺 |
| R7-U2 | P1 | 远端 ACL 被错放"人判"栏：`design/02` B13 在机器验收栏且写明 ACL 是权限红线，草案写成"用户确认凭据只允许 test"→ 口头判断顶替权限硬证据 | **采纳**，按验收二分（G11 复合观察点强制原子化）拆开：机器项归 **B16**（检查器验脱敏回读工件的存在/格式/覆盖范围/remote 指纹一致），人判项归新增 **H5**（判该回读的来源与时效）；共享同一 E-ID，工件缺失即未验证，红线不得风险放行 |
| R7-U3 | P2 | 现状事实支持"大量为新建"，不支持"已接近完整流水" | **采纳**，写入讲解口径：批次地基阶段仍属能力建设，H3/H4 前不得宣称"完整流水可用" |
| R7-U4 | P2·不可验证 | expected identity 稳定脱敏取值方式、真实测试仓 ACL、真实 psmux/裸远端条件均未选定或执行 | **登记为不可验证项**：identity 取值由 B4 施工者补证；测试仓 ACL 由 B16 施工者 + 用户（H5）补证；真实终端/断电恢复/自举证据由 H3 执行者补证 |
| R7-M1 | — | 机器契约核验通过项：P1 只用 `DHR_01～03`、草案连续 `DHR_04～13` 且唯一；`<!-- dh:tasks -->`、planning-event、状态枚举、工作区占位、dh:status 六字段齐备、依赖图无环 | 无需修改 |

- **需用户决定（2026-08-16 AskUserQuestion 点选）**：面对 R7-01/R7-02 两条"回 A"诉求，用户点选 **「回 A 补齐再拆 B（推荐）」**（备选：只拆卡不改设计 / 维持当前 10 卡草案，均未选）。
- **本次 A 修订落点**（design/02 事件 `DHR-A-04`）：§文档信息、§1.4 卡数与新增范围项 10、§1.5 决策清单增 3 行、§4 补授权校验与教训复核两条流转、§6.1 收窄 B7/B13 并新增 B14～B17、§6.2 新增 H5、§7 信任登记逐项挂独占验收 ID、§9 与更新日志。
- **仍待 B 阶段处理（不进本次 A）**：R7-03/04/05/U1 的落点全在 DevPlan 重拆；自举卡与测试仓/凭据仍是 `{待用户定}`。
- **草案本体不入仓**：R7 的 10 卡草案（`DHR_04～DHR_13`）在 A 修订后已作废重拆，且 `dev_plan/` 下任何 `.md` 都被 `dh` 当 DevPlan 校验（R29 要求恰有一条 planning-event），草案带活标记会误判、去掉标记又缺标记。故草案文件移出仓库（留 scratchpad），**其全部结构性结论与必改项已逐条记入本节表格**，重拆时以本节 + 新验收清单为输入，不依赖那份文件存活。重拆后的 10 卡形态供参考：`04` v2 契约 / `05` 宿主基础债 / `06` 任意仓入口+注册表+profile 身份 / `07` 脚本节点机器闸 / `08` 三路复核与返工 / `09` 控制台+恢复+诊断+quota / `10` 确认护栏+finalizer / `11` 多卡调度与基线刷新 / `12` 人机协作+test push / `13` 隔离守卫+oracle+自举。

<a id="understanding-a4"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-04 artifact=design/02-完整流水-产品设计与验收.md kind=understanding -->

### 3.12 DHR-A-04 定向复审（R8）、讲解与理解确认

#### R8 定向复审

- 实例：第八个 fresh headless `codex exec -m gpt-5.6-sol -c model_reasoning_effort=xhigh --sandbox read-only`（未参与起草，未参与 R1～R7）；审对象=工作树里刚修订、尚未提交的 design/02；原始输出留 scratchpad `a04-recheck-codex.out`。结论 **changes-requested，P0=0，P1=6**。
- reviewer 自核仓库事实（主会话复核属实）：`tools/relay/host/relay-worker-entry.ps1:31-34` 现役以 `--dangerously-skip-permissions`/`--yolo` 拉起 claude/codex；`dh-check.mjs:753-780,884-887` 的 R15 只查八件套文件**存在**、不查占位；`tools/relay/contracts/relay-schema.ps1:160-168` 的 `authority.json` 只有 run/generation/plan 信息，没有授权卡集合。

| # | 级别 | 问题（摘） | 主会话裁决 |
|---|---|---|---|
| R8-01 | P1 | 修订被提前写成"已确认"（design/02 文档信息与 §9），但 evidence §3.12 仍待填 | **采纳**：文档信息与 §9 改为"`DHR-A-03` 及之前已确认；`DHR-A-04` 待用户确认，确认前不得作为 B 的依据" |
| R8-02 | P1 | B14 无可独立验证的"授权清单"来源：设计要求对话授权，机制却只有 `relay start -Task`，"传入但不在授权清单"不可构造；`authority.json` 现役无卡集合，也挡不住 TOCTOU | **采纳 + 用户拍板 D14**：授权真源=主会话落的不可变 **authority snapshot**（授权时刻/卡集合/各卡目标·验收·变更范围/DevPlan 与 design 的 **Git blob 哈希**/授权证据指针），plan 与 receipt 绑定其哈希；新增 §3 契约段与 B14 的四类拒绝反例（不在 snapshot / DevPlan 无此卡 / 状态非可开工 / blob 哈希不符=授权后 DevPlan 被改） |
| R8-03 | P1 | B15 的 option 只绑 ID 未绑动作与参数（合法 option 可被换成任意 action/profile）；`actor=console` 与"option 必须来自 diagnoser checkpoint"自相矛盾 | **采纳**：checkpoint 冻结**规范化 option 载荷** `{option_id, action, params, target}` + 哈希，代发需逐字段全等（换参反例）；console 另立"直接用户命令"schema（仍须 target + expected_state_hash）；两条路径各补正向用例 |
| R8-04 | P1 | B15 的"诊断 agent 只读、写入被拒"无可信执行边界——现役 worker entry 以 `--yolo`/`skip-permissions` 拉起，profile 无 capability/sandbox 字段，只能靠 prompt 劝阻 | **采纳**：注册表 profile 增 `capabilities.readonly`（含各 CLI 的具体实现，如 codex `--sandbox read-only`）；diagnoser 只能绑定该能力位为真的 profile，宿主按其拉起并在 receipt 记配置哈希；**不支持强制只读的 profile 拒绝用于该角色**；不得以 prompt 替代 |
| R8-05 | P1 | B16/H5 未真正原子化（无共享字段），且 design/02 写"H5 判否则 B16 不通过"把人判混回机器结论；棒 7a 仅凭 `-AllowTestPush` 即可 push、不要求 ACL 证据先成立 → 可能先产生远端副作用再发现红线缺证 | **采纳**：新增共享 `acl_evidence_id`，B16 与 H5 **各留独立结论**；同一 (run, remote 指纹) 的**首次 push 之前**两者必须都成立才放行，缺失/过期在 push 前 fail-closed；H5 有效期=每 run × 每 remote 指纹首次 push 前判一次，ACL 或指纹变化即失效重判（主会话按推荐值定，用户未反对） |
| R8-06 | P1 | B17 不自洽：把"八件套"与"教训复核棒"两个不同写者/阶段捆一起；且"非占位等价 R15"不成立（R15 只查存在） | **采纳**：拆成 **B17**（落户八件套完整性，自带精确文件集 + 逐文件非占位谓词，每文件各一条缺失反例与占位反例，**明确不挂靠 R15**）与 **B18**（lesson_review 三终态：成功/无候选/失败；缺 launch 或未终结才阻断 closer） |
| R8-07 | P2 | 认领重叠：B3 标题仍称"三路复核"却把 lesson_review 让给 B17；B4 与 B14 都认领 `command/extra_args` 拒绝 | **采纳**：B1 只管 schema 形状（含任意 CLI 参数的形状层拒绝）、B4 只管 registry/profile 的执行侧、B14 只管授权语义等价、B3 标题与范围改为"代码复核两轮 + 汇合 + 需求复核 + 有界返工/补证的调度顺序与轮次上限，**不含 lesson_review**" |
| R8-08 | P2 | 12～13 卡估算无可复算依据，按本轮再拆下限已到 14～15 | **采纳**：改为 **14～16**，并写明"**这是估算不是闸**，以 B 拆的覆盖/颗粒度审计为准；硬约束只有一卡一个安全边界" |
| R8-不可验证 | — | 真实 Git 服务是否支持"凭据只允许该仓 `refs/heads/test`"、ACL 导出能否完整表达继承与 default-deny | **登记**：补证人=B16 施工者提供 provider 专属完整脱敏回读与规范化规则，用户在 H5 判来源/时效/忠实性。**另：主会话实测 `dh-crew` 与 `infohub-vps-services` 当前均无 Git 远端**（`git remote -v` 空），故该项在现有两仓上不可触发 |

#### 用户新拍板（2026-08-16，AskUserQuestion 与对话原话）

- **D14 开跑方式**：用户先答「我不想敲命令行」，主会话说明"命令行参数不再是你亲手敲的，授权就必须另外落成可审计工件"后，用户点选 **「对话里让我代起」** → 授权真源=主会话落的 authority snapshot；`relay start -Task` 降为脚本/调试通道且同样必须消费 snapshot。
- **D15 远端 push 的定位**：主会话报告"两个仓 `git remote -v` 均为空"并给出降 P3 / 条件保留 / 换仓三选项后，用户原话 **「push 远端这个动作是跟着项目的，项目有就做没有就不做」** → 不降 P3、不砍，改写为**项目属性驱动的条件分支**，谓词不成立时登记"前提不成立·本次不适用"（既不是未验证，也不是风险放行）。
- **D16 集成端到端棒的定位**：用户原话 **「至于进服务器验证，本质也是人类验收或者帮 AI 验收的动作，只是这里会有大量人和 AI 协调，需要在交互式窗口进行的」** → 棒 7a 改写为"人验/帮 AI 取证的动作"，agent 只组织命令与用户输出成证据账本条目、不判通过。

#### 讲解与理解确认

- **讲解**（主会话 2026-08-16 对话中给出）：按五问讲修订后的差别——① 任务从哪进入：由"你敲 `relay start -Task`"改为"你在对话里授权 → 主会话落 `authority-snapshot.json`（含各卡 DevPlan/design 的 Git blob 哈希）→ 代起 relay"，命令行降为调试通道且同样消费该工件；② 状态存哪：不变，新增 snapshot 工件，plan 与每张派工单绑其哈希；③ 承诺什么：新增三条硬承诺——授权后 DevPlan 被改由 blob 哈希逮住、诊断 agent 只读由注册表能力位+宿主拉起方式保证（不靠 prompt）、落户棒必须真产出 8 件套且逐文件非占位；④ 怎么独立验证：B17 明确**不挂靠 `dh` R15**（实测 R15 只查文件存在、不查占位），须自带每文件的非占位判据与缺失/占位两类反例；⑤ 失败怎么发现：远端 push 改为**首次 push 前置闸**（ACL 机器证据 ∧ H5 都成立才推），不留到收口才发现红线缺证。并说明三个新账目状态的区别：**未验证 / 前提不成立·本次不适用 / 风险放行**互不相同，条件分支不成立时不得记成带风险放行。
- **理解问题（一次一问）**：场景题——"你在对话里授权了 DHR_04 与 DHR_05，snapshot 已落、relay 已跑；半小时后你在另一个窗口正当地改了 DevPlan 里 DHR_05 的验收口径，而 DHR_05 的施工棒还在跑。你希望 relay 怎么反应？"（三选：停住弹窗告知 / 悄悄按旧口径跑完 / 自动换新口径续跑）
- **用户回答（D17）**：点选 **「停住，弹窗告诉我卡变了」**。→ 暴露原稿缺口：B14 原写"哈希不符 → 全部推进 fail-closed"，与用户要的"只冻那张卡、其它卡照常"不符 → **同轮调整**：把两类失配分开——snapshot 工件自身被篡改=攻击面、全 run 停；run 中途 DevPlan/design 正当变更致某卡 blob 失配=只冻该卡并拉诊断 agent 给出"重新授权 / 回滚 / 取消该卡"三选项，其余卡照常；B14 反例相应增加"只冻该卡"与"其它卡不受影响"两条。决策 1 与 §6.1 B14 已按此改写。
#### R9 定向复审（只审 D14～D17 的落地，前八轮均未审过这四条）

- 实例：第九个 fresh headless `codex exec -m gpt-5.6-sol -c model_reasoning_effort=xhigh --sandbox read-only`；原始输出留 scratchpad `a04-r9-codex.out`。结论 **changes-requested，P0=0，P1=6，全部采纳**。
- reviewer 自核仓库事实（主会话复核属实）：`tools/relay/host/relay-host.ps1:106` 的 `Invoke-RelayHost` 是**调用方进程内的同步有界 `MaxTicks` 循环**，`as-built/relay-psmux-host.md` 也只定义退出码、无 detached/service owner；`tools/relay/contracts/relay-schema.ps1:160` 的 `authority.json` 无卡集合；`dev_plan/P1-最小接力PoC-开发方案.md` 一份文件承载 DHR_01～03（故整文件 blob 判不出"只影响哪张卡"）；独立复核 `dh-crew` 的 `git remote -v` 为空。

| # | 级别 | 问题（摘） | 主会话裁决 |
|---|---|---|---|
| R9-S1 | P1 | 授权工件创建边界不闭合：snapshot 位于尚未创建的 `<run>/`，§2.4/§3/§4 三处说法互斥；且无法证明主会话没多写卡，`-Task` 可能重新生成授权成旁路 | **采纳**：拆成 **agent 只写 `authorization-request`**（落 `<repo>/.dh-runtime/relay/requests/`）+ **宿主校验后原子建 run 并生成、锚定 `authority-snapshot.json`**；CLI 只能消费既有 request/snapshot，禁止凭 `-Task` 自造（旁路反例）。**并拍板 D19** 补弱信任缺口 |
| R9-S2 | P1 | relay 进程归属与存活语义未定义：现役是调用方进程内有界循环，主会话一结束就停，与"终端全关也能续"的承诺不符 | **采纳 + 用户拍板 D18=独立 detached 宿主**：新增 detached 形态 + PID/lease + 同 run 单宿主 + `relay stop` + lease 过期接管 + `relay status` 三态，验收挂 B7；H3 演示两种形态（关终端宿主仍活 / 连宿主一起停走恢复） |
| R9-S3 | P1 | D17 两类失配不能稳定分类：§3 仍笼统写"哈希不符→全 run"；且 Git blob 是整文件级，一份 DevPlan 承载多卡时判不出影响面 | **采纳**：先校验宿主锚定的 `snapshot_hash`（信任根）→ 再比**两级哈希**（整文件 blob 证来源 + **每卡规范化片段哈希**证影响面）；失配改**三分**：工件被篡改=全 run 停 / 卡片段变=冻该卡 / 整文件变但片段未变=只记事件 |
| R9-S4 | P1 | D17 与 P1 A4 依赖冻结、决策 11 换届、`finalizing` 不接受 cancel 冲突；push/CAS 途中无安全停点 | **采纳**：改为"触发卡 **+ 其传递依赖闭包**冻结，无关卡继续"；冻结判定在**每次 launch 前、远端 push 前、finalizer 最终 CAS 前**各重验；已跑 worker 不强杀、其结果按失效隔离；已 push 先走 B16 补偿；`finalizing` 中途命中 → `closeout_blocked` 而非中断 CAS |
| R9-S5 | P1 | D15 把"项目适用性"与"安全前置条件"混成一个谓词 → 漏配授权/ACL 会被记成 N/A，等于给权限红线开新口子（与 G12/G14 不自洽） | **采纳（这正是主会话事先自陈不放心的那处）**：分两账——**只有"授权冻结时刻该仓确无可用远端"可记 N/A**；有远端但缺白名单/开关/run 授权/ACL/H5 一律记**未授权·未验证并禁 push**，不得记 N/A、不得风险放行 |
| R9-S6 | P1 | D16 后缺卡级人判结论的合法写者：棒 7a 说 agent 不判通过却写"多轮直到过"，B13 只证留账，7b 是 AI 复核，H3/H4 只判整体体验 → "有人给证据、无人有权判结果"的夹缝 | **采纳**：每个需人判 E-ID **必须留用户原文 verdict**（agent 原文抄录、不得改写代判；用户说"再看看"记未定不得当通过），7b 只审覆盖与来源，最终业务结论进 E10/E11 放行证据包由用户在收口确认时看到；B13 增该反例 |
| R9-S7 | P2 | 连带文字未同步（§1.4 仍 12～13 卡、决策表旧 push 条件、用户职责仍列 `relay start`、§4 教训复核误指 B17、更新日志称"三项拍板"漏 D17） | **采纳**，已做 D14～D20 定向全文反扫 |
| R9-U1～U4 | P2 | ①"终端全关也能续"≠后台持续跑 ②"不可变 snapshot"不能证明忠实转录 ③ H5=N/A ≠ 远端能力已通过、B13 通过 ≠ 人验证据已被接受 ④ 真实 Git 服务 ACL/继承/default-deny 不可验证 | **全部采纳**：①由 D18 解决并在 §2.4/H3 写明两种形态 ②由 D19 解决（弱信任 + 窗口核对） ③交付汇报须分别展示"能力已证 / 本 run 未触发 / 业务人判结果"三栏 ④登记不可验证项，补证人=将来接入带远端项目时的施工者 + 用户 H5 |

#### 用户第三批拍板（2026-08-16 AskUserQuestion 点选）

- **D18 relay 存活**：点选「独立在后台跑」→ 新增 detached 宿主形态与 lease 契约，验收挂 B7。
- **D19 授权清单可信度**：点选「弱信任 + 宿主再摆一次让你核」→ 宿主建 run 前窗口展示精确卡清单请用户确认，验收挂 B14。
- **D20 B16 的 P2 收口口径**：点选「本地+裸仓验机械链，真实服务器能力标未验证」→ P2 不专门搭真实测试 Git 服务；B16 交付表述只能写"机械链已证、真实服务器能力未验证"，H5 无真实远端时记 N/A；将来接入带远端项目时按该未验证项补证，不另立 A。

- **用户确认**：2026-08-16 对话 AskUserQuestion 点选 **「确认，提交并继续重拆 B」**——`DHR-A-04` 整版确认，落盘（提交本地 master，不 push），并授权按新验收清单（B1～B18 / H3～H5）重拆 DevPlan。**本次确认仅授权 B 重拆本身；重拆结果仍须经 B 交叉审核、讲解、理解问答与用户确认才落盘，落盘也不等于授权任何卡开工。**

<a id="review-a5"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-05 artifact=design/02-完整流水-产品设计与验收.md kind=review -->

## 3.13 落点收束修订（事件 DHR-A-05）

- **起因（用户 2026-08-16 对话原话）**：「relay 作用到业务项目后不是会在业务模块下面建文件夹对吧。比如聚合分析这个仓库，有 20 多个模块，就会在每个模块下建 relay 文件夹记录接力情况。我希望是 relay 统一放在一个文件夹下，或者还是可以放模块下，但有个总的文档像是目录一样收束下。最好是前者。因为有些接力计划，可能涉及多个模块。」
- **主会话先做的区分（讲给用户并获认可）**：把「卡本身的 dev-harness 工件」与「relay 自己的接力记录」分开——前者（8 件套 / review / progress / findings / knowledge 候选 / DevPlan 回填）是那张卡本来就该有的，relay 只是代笔，留在各自模块 `workspace/<卡>/` 才对，挪走等于 relay 自造第二套流程（与决策 2 冲突）且 `dh` 体检 R15 会红；后者才是"每个模块下建 relay 文件夹"要消灭的东西。
- **主会话独立发现（前九轮审核均未提）**：原运行现场路径 `<repo>/.dh-runtime/relay/` 挂在 **dh-crew 的运行现场根** `.dh-runtime/` 下面，与决策 14「不改 dh-crew、不与其状态纠缠」矛盾 → 一并换根。
- **用户拍板 D21（AskUserQuestion 点选「`docs/relay/`，普通文件夹」）**：
  - 业务仓内 relay 只有两个落点：`<repo>/.dh-relay/<run_id>/`（现场，不进 git）+ `<repo>/docs/relay/`（留档，进 git；`README.md` 全仓总目录，每 run 一行含涉及模块与卡、终态、指针；`runs/<run_id>/` 装该 run 接力记录）。
  - **各模块目录下零 relay 自建文件夹**；卡级工件位置不变，只在该卡 `progress.md` 留一行指针指向 `docs/relay/runs/<run_id>/`。
  - 留档形态选"普通文件夹"而非 module：它不是开发模块，套 design/dev_plan/workspace 骨架会留一堆空壳且体检报缺件；代价（`dh` 模块体检与看板扫不到它）已向用户说明并接受。
- **主会话连带解决的冲突（未经用户提出，属实现必然）**：run 级留档跨卡，而既有规矩是"tracked 写入落各卡任务树、随该卡 squash 合入"——一个 run 三张卡三棵工作树，无法共写同一份跨卡文件 → **新增 run 收尾脚本节点**（宿主执行、一 run 一次），run 达终态时在**最后一次暂存分支**上单写 `runs/<run_id>/` 并向 `README.md` 追加一行、随该次 CAS 快进合入；单一写者无并发。run 中途取消/中断则现场留在 `.dh-relay/`，由控制台补导出。
- **落点落到验收**：B9 扩为"隔离、禁改与落点纪律"（写入路径集合白名单；模块目录下出现 relay 文件夹即失败，逐类反例；不得写 `.dh-runtime/`；跨模块 run 的留档只在一处）；B10 增"README 行与 runs 内容 == 事件账独立重算，且只在 run 终态那次快进出现一次、无重复追加"。
#### R10 定向复审（只审 D21 的落地）

- 实例：第十个 fresh headless `codex exec -m gpt-5.6-sol -c model_reasoning_effort=xhigh --sandbox read-only`（未参与起草，未参与 R1～R9）；原始输出留 scratchpad `a05-r10-codex.out`。结论 **changes-requested，P0=0，P1=9，全部采纳**。
- reviewer 自核仓库事实（**主会话逐条亲跑复核，全部属实**）：`.gitignore:10` 只有 `.dh-runtime/`，`git check-ignore .dh-relay/x` 退出 1（NOT_IGNORED）；legacy 根 `.dh-runtime/relay/` 现存 5 个目录（`archive-DHR_03-review-logs`、`archive-DHR_03-runtime`、`RELAY-IHSR05-20260816100852`、`RELAY-IHSR05-20260816100959`、`RELAY-IHSR05-RW-20260816113004`），其中 3 个为可读 `relay/v1` run；现役代码对 `.dh-runtime/relay` 的硬编码仅 `tools/relay/host/run-dogfood.ps1:36`，Runner/store 本身按 Root 参数化（`relay-store.ps1:4-5`、`relay-host.ps1:7-12`）。

| # | 级别 | 问题（摘） | 主会话裁决 |
|---|---|---|---|
| R10-S1 | P1 | run 尾时序自相矛盾：finalizer 是"快进后即删暂存分支"，run 尾却要在 run 终态后往该分支写；`cancel_run`/`closeout_blocked` 时也判不出终态 | **采纳**：改为**独立 run 级 archive script 节点**——Runner 签发 receipt、宿主执行、**从当时 master HEAD 新建自己的暂存分支**，持同仓 finalizer 锁 + 仓级 archive lease；**归档 CAS 与 B10 oracle 均过才进 `done`/`cancelled`，失败进 `archive_blocked`、不得报 done** |
| R10-S2 | P1 | 取消与 B18 边界未闭合：取消的 run 可能永不自动入总目录；或错误要求被提前取消的卡补跑教训棒 | **采纳**：**所有 settled 终态自动归档**（不靠用户手动补导出）；进入 closeout 的卡须满足 B18，**closeout 前被取消的卡记 `cancelled-exempt`**、不要求补跑。与 B18 无固有次序冲突 |
| R10-S3 | P1 | `README.md` 是跨 run 共享写点、不属于任何卡范围 → 撞决策 7 路径所有权审计与 B12 重叠检查，且并发无定义 | **采纳**：新增 `archive-manifest`（owner=`run:<run_id>`，路径集与各卡 release manifest **不相交**）+ **仓级 archive lease**；README 按 `run_id` **幂等 upsert**（禁盲目 append），CAS 漂移后重取基线重算 |
| R10-S4 | P1 | **B9 白名单漏项且无法静态证明**：漏了"卡授权的业务代码路径"（施工棒本就要改代码）→ 合法真实卡会误判失败；也未区分 Git refs/worktrees、用户级索引与 CLI 配置副作用；静态终态扫描抓不到"写后删除" | **采纳（本轮最要命的一条）**：B9 重写为**两类策略**——业务仓内容策略（允许集 = `.dh-relay/**` ∪ `docs/relay/**` ∪ ⋃各卡(snapshot 冻结的该卡变更范围 ∪ 由其 `module_slug` 派生的 dev-harness 路径)）+ 宿主外副作用策略（Git/用户级/CLI/psmux 各自登记）；取证改为**受限写环境 + 前后快照 + `git diff` + 独立 oracle 四者联证** |
| R10-S5 | P1 | "模块下 relay 文件夹即失败"缺机器语义：按 basename 扫会误伤合法 `docs/modules/relay/` 或预存目录；也证不了跨模块留档唯一 | **采纳（主会话事先自陈担心的那条）**：收窄为**只禁本次 run 新建、且带 relay archive/runtime schema 或 marker 的非规范落点**；补"仓内本就有合法 `relay` 模块"与"run 前已存在同名目录"两条**不得误杀**的反例；唯一性按 `archive-manifest`/`run_id` 搜索证明，卡 `progress.md` 的指针行不算第二份留档 |
| R10-S6 | P1 | `.dh-relay/` 会被 Git 看见（实测 `.gitignore` 只忽略 `.dh-runtime/`）→ 易被误暂存，直接破坏"不进 git" | **采纳**：start 前置检查要求仓根 `.gitignore` 含**锚定**的 `/.dh-relay/`，缺失 → `start_rejected`；**relay 不得自改业务仓 `.gitignore`**（属仓接入变更、须另行授权）；B9 用 `git check-ignore`、`git ls-files` 与误暂存反例验收 |
| R10-S7 | P1 | v1 发现链未定义：只认新根的控制台看不见 legacy 根里的 5 个存量目录，与"v1 可读/可归档"承诺不闭合 | **采纳**：resolver **双根**——v2 只写 `.dh-relay/`；legacy 根仅供显式 discovery/status/归档读取，`resume` 硬拒，**绝不新写**；B1/B7 直接用现存 DHR_03/IHSR_05 目录验 |
| R10-S8 | P1 | 跨模块卡缺逐卡 module slug 契约：`dh` 体检、DevPlan 回填、workspace 推导、`verify(<slug>)` 的 scope 无唯一来源，run 归档还会被错归到最后一张卡 | **采纳（主会话出题第 4 条，答案成立）**：authority snapshot **逐卡冻结 `module_slug`/`devplan_path`/`workspace_path`/`verify_scope`**；逐卡跑 `dh <module_slug>`、提交 `verify(<verify_scope>)`；`docs/relay/` 只归 `run:<run_id>`，由 B10 独立验 |
| R10-S9 | P1 | planning-event 状态账矛盾：evidence 已登记 A-04 确认并提交，正式输入却仍写"A-04 待确认"；§9 又称 A-05 已完成复审而 evidence 仍 `{待填}` | **采纳**：回填 A-04 已确认并提交 `49615e2`；A-05 在本节落账前保持"复审待登记" |
| R10-U1 | P2 | "业务仓只有两个落点"不等于 relay 只写两条路径（还有任务树、Git refs/worktrees、`~/.dh-relay/`、CLI 配置与会话、psmux 进程等） | **采纳**：正文改称"两个 relay **自有内容根**"，并在 §3 补**完整副作用表**（七类，逐项标"进不进 git / 归谁"） |
| R10-U2 | P2 | 换根理由的历史表述不准确：P1 `design/01:208-223` 与 `AGENTS.md:73` 明确把 `.dh-runtime/<dh-relay 命名空间>` 定义为**独立命名空间**，不等于读 dh-crew active state | **采纳并公开更正**：主会话此前在对话中称"挂 `.dh-runtime/` 与决策 14 矛盾、前九轮没看出来"属**讲过头**；改口径为"P1 legacy 共享 runtime 根 → P2 迁至独占根以取得更强物理隔离"，**不宣称 P1 违规**，旧证据不改判 |
| R10-U3 | P2·不可验证 | 未来业务仓是否存在同名 `relay` 模块、CLI 配置目录的实际写面、真实 Git provider ACL 均不在当前仓可证 | **登记**：补证人=B4/B9 施工者用同名模块与外部目录 fixture 补证、B10 独立复核；真实 provider 继续按 D20/H5 标未验证 |

- **需用户决定**：R10 明确"无新增用户拍板项，D21 的取舍无需重开"——以上均为落实 D21 所必需的状态机、所有权与验收收束。

<a id="understanding-a5"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-05 artifact=design/02-完整流水-产品设计与验收.md kind=understanding -->

### 3.14 DHR-A-05 讲解与理解确认

- **讲解**（主会话 2026-08-16 对话中给出）：先讲清 D21 达成的形态（业务仓两个 relay 自有内容根：`.dh-relay/` 现场不进 git、`docs/relay/` 留档进 git 且 README 为总目录；各模块下零 relay 自建文件夹；卡级 dev-harness 工件位置不变、只留一行指针）；再逐条讲 R10 补掉的三处会真炸的地方——① run 尾原写法在 finalizer 删掉暂存分支之后无分支可写 → 改独立 archive 节点自建分支、归档过了才报 done；② B9 白名单**漏了卡授权的业务代码路径**，施工棒本就要改代码，原写法会让一张合法真实卡直接判失败；③ "模块下出现 relay 文件夹即失败"会误杀仓内本就合法的 `relay` 模块 → 收窄为只禁本次 run 新建、带 relay marker 的非规范落点。再讲换根带出的三条：`.gitignore` 须锚定 `/.dh-relay/`（缺则拒启动、relay 不自改业务仓 gitignore）、resolver 双根（legacy 根 5 个存量目录仍可见但只读不续跑）、跨模块 run 逐卡冻结 `module_slug` 决定 `dh` 体检与 `verify` scope。
- **主会话主动更正**：此前在对话中称"relay 挂 `.dh-runtime/` 与决策 14 矛盾、前九轮没人看出来"属讲过头，已按 R10-U2 更正为"P1 legacy 共享根 → P2 独占根以取得更强物理隔离"，并明确 P1 当时未违规。
- **理解问题（一次一问）**：场景题——"一个 run 带三张卡，前两张已 verify 销户合进 master，第三张你在窗口里说'不做了，取消'。run 收尾写 `docs/relay/` 留档时，总目录里这一行怎么写？"（三选：诚实记 `done-partial-cancelled` 并列出取消卡 / 只记已完成的两张 / 整个 run 不归档）
- **用户回答（D22）**：点选 **「诚实记 `done-partial-cancelled`」**——总目录该行写"部分完成"、**逐张列出被取消的卡**并标 `cancelled-exempt`（未跑教训复核），**绝不表述为"全部销户"**，且被取消的卡不得从总目录隐去。已写入 §1.3 决策 10 的 README 契约（与 §2.3 既有的"`done-partial-cancelled` 不得表述为全部销户"一致，此处进一步要求逐张列出）。
#### 补充讲解与 D23（run_id 命名与时间语义）

- **起因**：主会话在向用户举例 `docs/relay/README.md` 时随手写了 `RELAY-agg-20260818-02` 这种"日期-当日序号"的 run_id，用户追问「relay 是以日期-数字为文件夹吗？为什么？」。主会话核查后**自陈该例是编的**：设计原文只有一句"run_id 含仓 slug 前缀避免跨仓冲突"，**格式实为空白**；而现役实际用的是时间戳且前缀是卡名（实测 legacy 根下 `RELAY-IHSR05-20260816100852` 等 3 个 v1 run）。
- **讲解**：① 为何不用当日序号——须先数当日已有 run，多宿主/多仓并发会撞号、须另加发号锁；时间戳到秒天然唯一有序、离线可生成。② 为何要仓 slug 前缀——`~/.dh-relay/runs.json` 是**跨仓**索引，两仓同秒起 run 会冲突，且控制台列表需一眼分辨归属。③ 为何不沿用 P1 的卡名前缀——P1 一 run 一卡故可用卡名，P2 一 run 带 2～3 张卡，取任一卡名都不当。
- **用户追问二**：「我要确认下这是创建时间还是最后更新时间，怎么和 `.run` 里的文件关联」。
- **主会话回答**：**是创建时刻且永不改写**——`run_id` 是贯穿现场目录名 / `events.jsonl` 每条事件 / authority snapshot / launch receipt / attempt result / closeout journal / `runs.json` 索引 / `docs/relay/runs/<run_id>/` 留档 / psmux 窗口标题的**主键**；若跟随"最后更新"变化，则须改目录名、已写入的全部事件 run_id 失配、运行中的窗口标题失效、宿主 lease 找不到自身，**整条身份链断裂**，与 P1「身份签发后不可改」冲突。"最后动过"另由三处承载：`events.jsonl` 末条时间戳（权威）、`runs.json` 最后更新时间（索引）、`docs/relay/README.md` 结束时间列（归档定稿）。**关联方式**=三处同名同键按字符串全等对应，不靠时间推断。
- **用户新想法（推翻主会话的纯时间戳推荐）**：「有个新想法，能不能加个类似计划 Id 一样的 id 再加个接力计划主题概要再加上日期代替时间戳」。
- **主会话评估与自我更正**：该方案可行且更可读，但需补两块——① **序号要发号**：主会话此前称"要另加一把发号锁"属**说重了**，建 run 本就要拿仓级锁（claim lease / archive lease 同级），发号可搭在同一把锁上、不新增机制；② **主题概要要冻结字符集**：它进文件夹名、psmux 窗口标题与事件账主键，而本仓有中文路径踩坑先例（中文路径下 `git status` 会返回错误结果），故限 ASCII 小写字母/数字/短横线、≤30、首尾非短横线，中文标题只进 README 与 `概览.md`。日期无需时分秒，因唯一性已由序号承担。
- **用户拍板 D23（run_id 格式）**：**`R<nnn>-<主题slug>-<yyyyMMdd>`**，例 `R007-kpi-alignment-20260818`。`R<nnn>` 为**仓级全局递增序号**（每仓各自从 R001 起、锁内 +1 发号）是唯一性来源；日期只为人读；**取消仓 slug 前缀**——`runs.json` 改以 **(repo 路径, run_id) 复合键**索引，跨仓天然不撞。
- **用户拍板 D24（主题来源）**：点选 **「我拟，你在核对时顺带看一眼」**——主题由主会话据卡的目标拟定（中文标题 + ASCII slug），宿主在 D19 的窗口核对步骤中**连同精确卡清单一并展示**，用户可当场纠正；用户不必每趟自己起名。
- **落到验收**：B7 增 run_id 规范化条（并发两 start 各得不同序号、无跳号无重号；非 ASCII/大写/空格/超长/首尾短横线各一反例，**不得静默截断或转写**；复合键唯一）；B14 的窗口核对反例增"须一并展示主题"与"用户当场改主题后按新 slug 生成"正向用例；README 总目录增「中文主题」列，时间列改为开始/结束两列。

- **用户确认**：2026-08-16 主会话问「现在这版你还有要改的地方吗？没有的话我就提交，然后按最终验收清单（B1～B18 / H3～H5）派 codex 重拆 B」，用户答 **「没了」** → `DHR-A-05` 整版确认，落盘（提交本地 master，不 push），并授权按最终验收清单重拆 DevPlan。**本次确认仅授权 B 重拆本身；重拆结果仍须经 B 交叉审核、讲解、理解问答与用户确认才落盘，落盘也不等于授权任何卡开工。**

<a id="review-a6"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-06 artifact=design/02-完整流水-产品设计与验收.md kind=review -->

## 3.15 DHR-A-06：纯状态回填与措辞订正（**无实质设计变更**）

- **性质声明（重要）**：本事件**不是**一轮新的 A 修订，**没有任何目标/范围/验收/契约变更，也没有新增审核轮次**。它只做一件事：把 `design/02` 里残留的"`DHR-A-05` 待用户确认"字样改成事实（用户已于 2026-08-16 在对话中答「没了」完成整版确认，A-05 已提交 `25b2ec8`）。
- **为什么仍要发号**：dev-harness 规矩本身说"状态回填等机械进度事实不形成新事件"，但 `dh` 的 R29 是**按文件判**的——只要正式输入的内容变了，其 `planning-event` 就必须在同一变化集中新增或修改，否则报 `EVENT_STALE` + 4 条 `EVIDENCE_STALE`。为**保持权威文档内容为真**又**不绕过体检**，此处显式发号并如实登记性质，**不伪装成新一轮审核**。
- **发现者**：B 重拆的起草人（codex，第 11 次调用）在读 master 版 `design/02` 时核出该状态账矛盾并主动上报，而非主会话自查。
- **改动清单（全部为措辞/状态）**：文档信息「当前状态」行；§9 的 `DHR-A-05` 状态行与「用户确认」行；更新日志中 `DHR-A-04`/`DHR-A-05` 两行的"待用户确认"→"已确认并提交 `<SHA>`"。**正文的决策、契约、验收清单、棒表一字未动。**
- **不涉及**：无新增/删除验收 ID，B1～B18 与 H3～H5 不变；无新增用户拍板。

<a id="understanding-a6"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-06 artifact=design/02-完整流水-产品设计与验收.md kind=understanding -->

### 3.16 DHR-A-06 讲解与确认

- **讲解**：主会话在对话中说明——R29 拦下了这次回填，因为工具不区分"机械回填"与"实质修订"；留着错字会让 B 阶段误判"A-05 不能用作依据"，故发一个明确标注为纯回填的事件，evidence 如实写清无设计变更、无新增审核。
- **理解问题**：本事件不改变任何设计内容，按 dev-harness「仅机械记录不形成新事件」的原则**不另设理解问题**；用户对 A-05 的整版确认（2026-08-16「没了」）即为其实质确认，本事件不重复索取。
- **用户确认**：不另行索取（性质为回填）。若用户事后认为该回填不当，可直接回退本次提交，`design/02` 的实质内容不受影响。

<a id="understanding-a"></a>

## 4. 讲解与理解确认

<!-- dh:planning-evidence:v1 event=DHR-A-02 artifact=design/02-完整流水-产品设计与验收.md kind=understanding -->

### 4.1 讲解（四层五问，主会话 2026-08-16 对话中给出的压缩版）

- **全局地图**：入口=用户对话授权卡 + 任意仓 `relay start`；流转=每卡 0～8' 棒（接力计划生成→workspace→施工→机器闸→R1→R2→汇合→[集成/端到端]→需求复核→教训复核→收口 agent→finalizer 脚本）；状态与结果存该仓 `.dh-runtime/relay/<run_id>/`（events/plans/launches/attempts/closeout）+ 工作区 `<ws>/*` + 任务树 `D:\wt\<卡>`；更新方式=文件两阶段写 + Runner CAS；权限与影响面=新代码只在 `tools/relay/`，dh-crew 零改动，master 只经 finalizer 一次快进，push 仅 test。
- **契约/定义**：Runner 不判质量（verdict/gate_outcome 枚举分支）；P1 状态机不动；三路复核成棒、返工/补证有界；确认闸=窗口作答（弱信任 + 宿主三护栏）；test push 受控例外；诊断 agent 代发预定义控制事件。
- **运作机制**：为何影响正确性/可靠性——机器闸与 finalizer 是脚本以保证幂等与防自签；暂存分支保证 master 无中间态；重叠卡串行与落后 rebase 保证多卡不互踩；接续先摄入结果再判断棒保证不丢已交棒。
- **代码层**：按需，本轮未讲。
- **五问**：进入=授权+`relay start`；存放=run 目录/工作区/任务树；承诺=上列契约；验证=B1～B13 + B10 宿主外 oracle + H3 自举/H4 对照；失败=paused/closeout_blocked → 诊断 agent 窗口问答 → resume/retry/reset/cancel/abort/rollback_test。

### 4.2 理解问题与回答

- 理解问题（一次一问）：复核发现 P1 需要返工时，默认拉全新 fix worker 还是保留原施工窗口？→ 用户点选「拉全新 fix worker」（2026-08-16）。
- 用户主动澄清 6 条（§3.6）与拍板 D2～D10（§2.3/§3.3/§3.7），均已落草案。

### 4.3 设计整版确认

- 2026-08-16 主会话给出整版讲解后，用户 AskUserQuestion 点选「**确认整版，落盘（含 8～10 卡）**」。
- 落盘动作：drafts/02 v5 原子晋级为 `design/02-完整流水-产品设计与验收.md`（planning-event `DHR-A-02`）；`design/README.md` 白名单增补 02；drafts/02 改为存档指针；design/01 更新日志加一行指向 02。
- 本次确认**不授权** B 拆计划或任何卡开工；自举卡选择留 B 阶段与用户定。
