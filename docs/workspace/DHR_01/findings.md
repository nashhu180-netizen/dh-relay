<!-- findings.md — 问题清单。🟢 边做边记。 -->
# findings — DHR_01 冻结接力权威、双维状态与异常契约

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|----|------|------|------|------|------|
| F-001 | P2 | 枚举比对大小写不敏感（`Test-RelayEnumValue` 用 `-notin`，PS 默认不区分大小写）→ `"role":"Worker"` 等变体过校验，冻结枚举 fail-closed 失效；批B result_status/status/kind 同用此助手 | 批A 小审(account9/deepseek)探针：`Test-RelayEnumValue @{e='Worker'} 'e' @('worker',...)`→ok:true | 返工：改精确区分大小写比对（`-cnotin`/`[string]::Equals(...,Ordinal)`）+ 补大小写变体反例断言 | resolved（codex b87e770 反例锁定·主控亲跑 RELAY ALL PASS）|
| F-002 | P2 | node 级校验（`Test-RelayNode`）零断言钉住：role/next_action 枚举拒绝、node 未知字段、resume_from 结构、depends_on 元素类型、duplicate-node、unknown-dep 均无正反例；变异探针 role 枚举加 'alien' 套件仍全绿 | 批A 小审探针④ | 返工：schema 套件补 node 级正反例 ≥6 条 | resolved（codex b87e770 反例锁定·主控亲跑 RELAY ALL PASS）|
| F-003 | P3 | `Test-RelayIsoValue` 接受纯日期 `2026-08-15`/纯时间 `09:00`（DateTimeOffset.TryParse 放行） | 批A 小审值域探针 | 返工顺带：收紧为须含日期+时间（`yyyy-MM-ddTHH:mm:ss` 前缀）或用 ParseExact 白名单 + 反例断言 | resolved（codex b87e770 反例锁定·主控亲跑 RELAY ALL PASS）|
| F-004 | P3 | badhash fixture 用整串 `f` 而非"改一位"；cycle/unknown-field fixture 全零 hash 依赖检查顺序（环/字段先于 hash）才不被误拒，顺序无测试锁定；A3 hash 回填步骤 progress 未记 | 批A 小审 | 返工顺带：补一条断言锁"unknown-field 先于 hash 比对"；progress 补记 | resolved（codex b87e770 反例锁定·主控亲跑 RELAY ALL PASS）|
| F-005 | P2 | 身份链判定 `stale-plan`（错版本/错 hash）与 `wrong-node` 两个分支**零夹具零断言**（删掉 relay-identity.ps1 对应行套件仍全绿）——与 F-002 同类"分支无断言钉住"在批B 复发 | E4 需求复核（subagent） | 返工：补 `result-wrong-plan.json`（plan_version=2 或 hash 变异）+ `result-wrong-node.json` + 断言 reason=`stale-plan`/`wrong-node` | resolved（返工轮1 a834052） |
| F-006 | P2 | A8 "handoff/result/event/session-tail 四类工件残留即失败"在 redaction 套件里是**同一字符串换四个标签循环断言**，不是四类工件形态的夹具；真正走脱敏的只有 session tail；另 `Test-RelayArtifactClean` 判 kind 用 `hunter` 关键字是贴夹具的启发式 | E4 需求复核 | 返工：补三类工件形态最小夹具（含凭据的 handoff.md 头文件 / result.json summary 字段 / event 行 reason 字段），各一条"脱敏后零残留"断言；`Test-RelayArtifactClean` 改为按传入 SecretValues 反查、kind 由调用方给或去掉 kind | resolved（返工轮1 a834052） |
| F-007 | P3 | "Runner 不读取/转发人工回答、不调用 resume"仅靠缺席证明，无显式断言锁：event kind 无 `user_answered` 类、checkpoint 带 `answer` 字段拒、矩阵 idle→running guard=`host-observed-new-turn` 非 runner-resume | E4 需求复核 | 返工顺带：补 3 条断言（kind='user_answered'→unknown-enum；checkpoint 带 `answer`→unknown-field；矩阵 idle→running 边 guard 值断言）；review 写明行为面归 DHR_02 A3/A4 与 DHR_03 真跑 | resolved（返工轮1 a834052） |
| F-008 | P3 | `Get-RelayStallVerdict` 用 `-in @('running','idle')`、`Get-RelayExitWithoutResultVerdict` 用 `-ne 'exited'` 仍大小写不敏感（F-001 修复未覆盖 relay-transitions.ps1 的快照判定） | E4 需求复核 | 返工顺带：改 `-cin`/`-cne` | resolved（返工轮1 a834052） |
| F-009 | P3 | DONE/E-010 自报 transitions "72+13=85"，主控/E4 亲跑均 84（定向 12 条）；SHA 列表核对无误 | E4 亲跑 | 记录即可，收口以亲跑计数为准（L-010 类自报笔误） | resolved（记录） |
| F-010 | P3 | DevPlan DHR_01 变更范围写 `tools/relay/tests/fixtures/`，brief 写 `tools/relay/tests/`（含套件+runner），产出落在后者；方向正确但字面宽一层 | E4 | 收口时把 DevPlan 变更范围同步为 `tools/relay/tests/`（progress 留痕） | open |
| F-011 | P2 | 穷举转换测试以矩阵 json 自身为期望来源（自证循环）：删掉 `running→idle` 合法边套件仍 84 全绿；错加 design 未列边亦放行；只有 exited/自环/unknown guard/quota 4 类有独立锚 | 轮1 批B~D 小审(account4/glm-5.2)探针 3a | 返工：测试里**独立硬编码** design §3.1 的合法边集合（terminal 18 边 + result 边）作为 oracle，断言矩阵边集合 == oracle（多一条/少一条都红），穷举再以 oracle 为期望 | resolved（返工轮1 a834052） |
| F-012 | P3 | `decision_required→quota_exhausted` 无 p1=false 留位（design §3.1 decision_required 可经 final result 转其终态、四种结束/暂停结果含 quota），走 edge-not-listed 与 quota 留位口径不对称 | 轮1 批B~D 小审 | 返工顺带：矩阵加该边 p1=false，穷举期望同步（reserved-target-form） | resolved（返工轮1 a834052） |
| F-013 | P3 | `Get-RelayProbeVerdict` 硬编码 from=running/working，不覆盖 design §4.4 decision_required 挂起期间连续 probe 失败→unknown/interrupted_unknown；矩阵 `decision_required→interrupted_unknown(probe-lost)` 边无生产函数/测试能产生 | 轮1 批B~D 小审 | 返工顺带：`Get-RelayProbeVerdict` 增 `-CurrentTerminal/-CurrentResult` 入参（默认 running/working），补 decision_required 挂起 probe-lost 断言且过 TransitionPair | resolved（返工轮1 a834052） |
| F-014 | P3 | 身份链只拒 `attempt_id < Current`，`attempt_id > Current`（同 launch/session）被 accept——attempt 与 launch/session 交叉矛盾不拒 | 轮1 批B~D 小审 | 返工顺带：`attempt_id -ne Current.attempt_id` 且 launch/session 与 Current 相同→rejected `identity-mismatch:attempt_id`；小于仍 stale；补断言 | resolved（返工轮1 a834052） |
| F-015 | P3 | 断言名 `checkpoint stale attempt rejected` 实际断 verdict=stale；task_plan B4⑩ 写"拒"、实现给 stale（语义更合理）但计划-实现-命名不一致且 progress 未记 | 轮1 批B~D 小审 | 返工顺带：断言名改 `...→stale`；progress 记一笔"ckpt 旧 attempt 判 stale 非 rejected（与 result 对齐）" | resolved（返工轮1 a834052） |
| F-016 | P2 | A2 launch 绑定 `identity-mismatch:launch_id` 零夹具零断言：去掉 launch_id 检查 identity 20 + failures 16 全绿 | 轮2 grok 探针 4 | 返工轮2：补 `result-wrong-launch.json`（launch_id 变异·session 同）+ 断言 reason=`identity-mismatch:launch_id`；同时对 checkpoint 补 wrong-launch 断言 | resolved（返工轮2 36c9593） |
| F-017 | P2 | FreezeSet 传递闭包只被 1 跳夹具钉住（plan-v1-good 仅 C→A/B 一层）；改成只冻直接依赖后 `B freezes transitive dependent C` 仍绿 | 轮2 grok 探针 4b | 返工轮2：新增 `plan-v1-chain.json`（A←B←C←D 三跳链 + 独立 E）+ 断言 FreezeSet(A)=[B,C,D]、FreezeSet(C)=[D]、E 不在任何集合 | resolved（返工轮2 36c9593） |
| F-018 | P3 | `stale-plan` 的 hash 半边无独立夹具（wrong-plan 只改 plan_version） | 轮2 grok | 返工轮2 顺带：补 `result-wrong-hash.json`（version 同、hash 变异）+ 断言 | resolved（返工轮2 36c9593） |
| F-019 | P3 | `bad-handoff-header` 原因码无断言（只锁了缺 session_id）；`illegal-final-status:working` 无独立夹具（只锁了 decision_required） | 轮2 grok | 返工轮2 顺带：各补一条断言 | resolved（返工轮2 36c9593） |
| F-020 | P2·系统性 | "判定分支/reason 码无断言钉住"在批A(node)/批B(stale-plan,wrong-node)/轮2(launch_id,FreezeSet 2 跳) 三次复发——靠人肉小审逮不干净 | F-002/F-005/F-016/F-017 | 返工轮2：新增静态守卫套件 `relay-contract-reason-coverage.ps1`：正则扫 `tools/relay/contracts/*.ps1` 中所有 `New-RelayValidationError "…"`/`reason = '…'` 字面原因码（去掉 `:$变量` 尾巴取前缀），再扫 `tools/relay/tests/*.ps1` 出现的原因码字面，差集非空→列出并 FAIL；加入 run-relay-tests 清单 | resolved（返工轮2 36c9593） |
| F-021 | P3（复验者报 P2·主控裁定 P3：生产代码当前无全变量构造的 reason，属预防性精度问题） | reason 覆盖守卫两处精度：①全变量插值 `"$prefix:$x"` 形式的 reason 不进 productionReasons（静默漏抓）；②测试侧任意引用 kebab 字面（含注释/无关字符串）即算覆盖（假覆盖） | 轮2 返工复验（codex fresh 会话） | 返工轮3（窄）：①生产扫描——`New-RelayValidationError`/`reason =` 后若不是引号开头的字面 → FAIL `non-literal-reason:<file>:<line>`，仅允许显式白名单（relay-schema.ps1 `Test-RelayPositiveInteger` 的 `$reason` 变量，两条候选字面在上一行、已被抓）；②测试侧只统计**含 `Assert` 的行**上的 kebab 字面 | resolved（返工轮3 a1846d3） |
| F-000 | P1 | （codex 首派登记·主控改号去重）`task_plan.md` A2 声称字段表见“下方【schema 冻结表】”，但文件中不存在该表；design §3～§4 只给出身份链与语义，未定义 RelayPlan proposal、active-plan、authority、launch receipt 的完整必填/可选字段及嵌套 node 白名单。v1 要求未知字段 fail-closed，因此施工者不能安全猜测 schema。 | E-002 | 主控已在 4e8f07f 补齐冻结表；续派后批A按表实现并由 E-003 证明 | resolved |

> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞
> 遗留（P0/P1 唯一合法路径，DC_46）：状态列写 `遗留→<DC_xx / backlog / 下计划名>（已确认）`——"已确认"三字代表用户已在对话里明确点头；没有这三个字，`dh-check` R13 仍按 `open` 处理。
