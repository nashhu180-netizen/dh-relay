# H2 对照表（DHR_03）

> 口径按 `task_plan.md` K-14：人工动作只计用户显式确认/回答/重启；通知拆为宿主 tick 状态行与主控转述；blocked→fresh A hop 含起止事件；对照组只引用已有材料，不重跑。

## 表 1：dh-relay dogfood 实测

| 场景 | 人工动作数 | 通知数：宿主状态行 | 通知数：主控转述 | blocked→fresh A hop 数 | 可见步骤数 | 耗时 | 来源 |
|---|---:|---:|---|---:|---:|---|---|
| blocked（run5） | 0 | 46 | 2（run5 起跑一条 + 结果一条；主控未逐 tick 转述） | 19 | 3 | 7 分 53.389408 秒 | 人工动作：`progress.md` E-012；状态行：`review-logs/dogfood-blocked.log` 中 `[relay-host] tick=` 计数；hop：`evidence/blocked/events.jsonl` 从 `EV-...-000019 result_accepted|A|1` 到 `EV-...-000037 launch_receipt|A|2` 含两端；可见步骤：A/1、B/1、A/2；耗时：`timeline.md` 首末事件 `2026-08-15T13:53:18.9243015+00:00`→`2026-08-15T14:01:12.3137095+00:00` |
| blocked（run6·复核返工后重取） | 0 | 65 | 0（主控后台跑完才转述一次结果） | 15 | 3 | 8 分 40 秒 | `progress.md` E-018；原件移至 `review-logs/evidence-blocked-run6/`（PNG 因 F-024 已删） |
| blocked（run7·F-024 截图修后重取，**现役 A3 证据**） | 0 | 67 | 0（同上） | 27（本次 A/1 交棒到 fresh A 之间 observation 更多：replanner 74s） | 3 | 8 分 42 秒（含 C3 迟到结果事件） | 人工动作/签名：`progress.md` E-020；状态行：`review-logs/dogfood-blocked-run7.log` `[relay-host] tick=` 计数；hop：`evidence/blocked/events.jsonl` `EV-...-000023 result_accepted|A|1` → `EV-...-000049 launch_receipt|A|2` 含两端；可见步骤：A/1、B/1、A/2；耗时：events.jsonl 首末事件 `2026-08-16T00:40:27`→`00:49:10` |
| decision（run2） | 1（用户在 A 窗口回车选 zh） | 62 | 10（主控按 monitor 事件逐条转述，其中 1 条为提醒回答） | 不适用（无 blocked） | 3（A/1、B/1、C/1 窗口；A 窗口含提问→回答两态） | 9 分 20 秒（首事件 08/15/2026 22:38:21 → 末事件 08/15/2026 22:47:41）；其中挂起等人 2 分 54 秒 | 人工动作/签名：`progress.md` E-014；状态行：`review-logs/dogfood-decision.log` `[relay-host] tick=` 计数；耗时：`evidence/decision/events.jsonl` 首末事件；挂起时长：第 2 与第 3 条 `checkpoint_accepted|A|1` 之差 |

说明：blocked 的 19 个 hop 事件包含两端与其间 observation、plan proposal/activation、B/1 launch/result；耗时按指定首末口径包含 run5 首次 plan 事件到 C3 迟到结果事件。

## 表 2：对照组 dh-crew 常驻主控巡检

| 对照材料 | 已有记录 | 人工动作数 | 通知数 | hop / 可见步骤 | 耗时 | 可比性结论 |
|---|---|---|---|---|---|---|
| `docs/modules/dh-crew/design/10-手动派活范式.md` §三 watcher | 2026-07-03 已验证“后台挂起→DONE→进程退出→主控自动唤醒”全链路；watcher 轮询间隔 30 秒，不需人转达；另建议 ≥10 分钟无产出预警 | 无可引用记录 | 仅记录“自动通知/唤醒”，无条数 | 无可引用记录 | 仅有 30 秒轮询与 H3“≤1 分钟醒来”设计口径，无单次实测总耗时 | 可作机制对照，不能与 run5 做数值差值 |
| `docs/modules/dh-crew/workspace/DCR_47/` | 目录不存在，无可引用记录 | 无可引用记录 | 无可引用记录 | 无可引用记录 | 无可引用记录 | 无 dogfood 手记可用于量化对照 |

## 表 3：未覆盖范围

| 范围 | 当前证据状态 | 结论 |
|---|---|---|
| 多线并发 | blocked run5～run7 只有 A/B 依赖链；decision run2 有 A/B 并行 + C 依赖 A（三节点·单 run），未做多条独立链/多 run 同时调度 | 部分（仅三节点并行） |
| 非 psmux 后端 | 未实现或运行其他真实 adapter | 未覆盖 |
| orca 后端 | 仅原语级 preflight 4/5；P5 `terminal close` 返回 `tab_not_found`，且 P2 无 OS 级标题全等证 | 只可作原语差异参考，不是 adapter/dogfood 证据 |
| 长时稳定性 | blocked run5～run7、decision run2 均为单次最小场景，未做小时级/连续多 run 压测 | 未覆盖 |
| 真实业务任务 | blocked/decision 均为 dogfood 假需求；产出仅 work/{A,B,C}.txt | 未覆盖 |

数值均由主控复核后生效（2026-08-15 主控已复核：blocked run5 46/19/3/7分53秒 与 decision 62/1/3 均按来源文件重数一致；2026-08-16 主控按 run6/run7 来源文件重数：65/67 tick、hop 15/27、8分40秒/8分42秒；主控转述条数按对话逐条计）。
