<!-- findings.md — 问题清单。🟢 边做边记。 -->
# findings — DHR_03 接真实可见 psmux 并完成阻塞接力 dogfood

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|----|------|------|------|------|------|
| F-001 | P2 | psmux `kill-session -t =<name>`（tmux 精确匹配语法）exit 0 但**不杀会话**——若 adapter 沿用 tmux 习惯写 `=name`，stop 永远"成功"而 session 长存，Runner 收不到 exited；已定 K-3/K-6（裸名 + list-sessions 全等复核）并进离线套件断言 | E-002 | task_plan K-3/K-6/A3⑧ 锁住 | mitigated（待套件落地转 resolved） |
| F-002 | P3 | psmux 无宿主推送通道，`emit_observation` 只能返回空；running/idle 判据退化为 `window_activity` 时间窗启发式（`IdleAfterSeconds`），agent 在"思考中不输出"时可能被误判 idle（不影响业务投影，只影响终端维展示） | Code Scout | 记录·P1 接受；orca `terminal wait --for tui-idle` 是对比项（批0 记结论） | open |
| F-003 | P3 | 本仓 dh-crew 尚未注册进 orca `repo list`；orca preflight 需先 `orca repo add`（只加注册元数据·不建 worktree·不改本仓文件） | Code Scout S6 | 批0 已 `orca repo add --path D:/MyFiles/ai-workflow/dh-crew`（repo id f2ec28fb…·仅 orca 元数据） | resolved |
| F-004 | P3（orca） | `orca terminal create … --focus` 稳定超时（"Timed out waiting for terminal handle after creation"，11～18s），去掉 `--focus` 即可建；`--focus` 不可用于自动拉起 | E-004 | 记录·preflight 脚本已去 `--focus`；orca 非选定后端 | open（记录） |
| F-005 | P2（orca·A7 P5） | `orca terminal close --terminal <h>` 返回 `ok=false error=tab_not_found`，但 pty 实际在 ~2s 内 `read.status=exited` 且 `terminal list` 不再含——回收命令返回值与真实效果不一致，adapter 无法把 close 返回当"已回收"，须以 read/wait 复核为准；`terminal show` 对已退出 handle 仍返回旧元数据 | E-004 | 记录为 orca 不满足 A7 P5 的原因之一；K-1 裁决不换后端 | open（记录） |
| F-006 | P3（orca·A7 P2） | orca 终端可见性只能由 app 内字段（`surface=visible`、`terminal list` 含 handle、`status.app.desktopWindowStatus`）间接证明，无 OS 级窗口标题可供 EnumWindows 全等核对；orca 也没有 psmux `set-titles` 那种把 session 名投到 OS 窗口标题的机制 | E-004 | 记录；对比项写进 H2 未覆盖范围 | open（记录） |
| F-007 | P3 | preflight 脚本首版 helper 参数命名 `$Args` 撞 PowerShell 自动变量 `$args`，导致所有 psmux 调用无参执行、刷出 35 个默认会话（已 `kill-server` 清理，当时无他人会话）；教训：PS 函数参数禁用自动变量名（`$Args`/`$Input`/`$PID`…）——DHR_02 已有 `$pid` 同类教训 | E-003 首跑 | 已改 `$Arguments`；lesson L-002 | resolved |
| F-008 | P2 | DHR_02 `relay-runner-authority.ps1` 硬断言 params 恰好五键，与本卡 A1 追加三键直接冲突；codex 按 K-15 正确 blocked 升级而非自改旧套件 | E-005 | 主控裁决 K-16：仅迁移该条断言为八键+逐键点名；其余旧套件正文不动 | resolved |
| F-009 | P1（设计假设错） | psmux `#{window_activity}` **只在建会话时写一次、不随 pane 输出更新**（实测：持续 `Write-Host tick` 的 pane，8s 内该值纹丝不动，`cursor_y` 却在涨）→ K-5 原设计"activity 时间窗判 running/idle"在真实 psmux 上恒判 idle（真实套件 `initial probe running` 首跑红）；批0 preflight P4 只要求 running/idle 之一所以没逮到 | E-006 | K-5 修订为屏幕内容指纹（capture-pane + cursor 哈希·registry 存 `activity_fingerprint/activity_seen_at`）；离线套件 45 断言 + 真实套件 14 断言 + adapter 级 preflight P4 `running,idle,idle` 实证 | resolved |
| F-010 | P3 | 本机负载 71%（用户其它进程 herdr 两个常驻高 CPU）下 psmux 单次 CLI 调用 0.5～3.7s（new-session 2.9s、set-option×2 3.7s、list-sessions 0.6s、list-panes 1.2s），adapter.launch 实测 3.5～8.3s、单次 probe 4～9s；原定 `AttachDeadlineSeconds=10` 逼近上限 | E-006 | 定值 Attach 30 / Stop 15（K-16）；probe 已合并 cursor 到同一 `list-panes` 调用省一次往返；宿主 tick 间隔设计须容忍 probe 秒级 | resolved（记录） |
| F-011 | P3 | 批0 preflight 脚本把 adapter 点源写在函数内，adapter 动词闭包（GetNewClosure）在自身模块作用域解析不到 `Get-RelayPsmux*` → `-Level adapter` 首跑 "term not recognized" | E-006 | 改为脚本作用域点源（Test-Path 守卫） | resolved |
| F-012 | P2 | 主控会话派出的 psmux pane 继承 `CLAUDECODE`/`CLAUDE_CODE_CHILD_SESSION` 标记与一个**空值** `CLAUDE_CONFIG_DIR`，交互 `claude` 因此显示 "Not logged in / API Usage Billing" + 子会话告警（实测 cl-test4/5）；显式清掉这些继承值后同一命令正常（Claude Max·Fable 5·答 RELAY-OK）。另：`--dangerously-skip-permissions` 首次会弹 Bypass 确认（人工一次），主控在测试会话里代按一次后主账号不再弹；trust 提示只要 WorkDir 在已信任仓库路径下即免 | E-007（cl-test 4～7 pane 抓屏，未入库） | worker-entry 未给 -ConfigDir 时 `Remove-Item Env:CLAUDE_CONFIG_DIR` 并清 5 个继承标记；run-dogfood 进程启动即清；WorkDir 定在 `<run>/work`（仓内） | resolved |
| F-013 | **P1（真 bug·dogfood run1 B worker 逮到）** | `relay-agent-tool result` 不带 `-ChangedPaths/-TestsRun` 时（worker 没改文件也没跑测试），`[string[]]` 参数收到 ``、`@()` 变成含 null 的单元素数组 → `Test-RelayResult` 报 `bad-type:tests_run` exit 4，**没跑测试的节点永远交不了棒**；B worker 两次 exit 4 后拒绝编造测试名、改写 working checkpoint 如实记录并停下（正确行为，见 review-logs/dogfood-blocked-run1-B-pane.txt）；codex 套件只测了带列表的用例所以漏掉 | E-008 | 工具改 `@($Paths|Where-Object{$null-ne$_})`；套件加"不带列表也能交棒且为空数组"断言（原版红→修后绿）；run1 作废重跑 run2 | resolved |
| F-014 | P2（环境） | dogfood run2 前 8 步全对（编排→A 阻塞交棒→重编排→B 成功→A/2 发 receipt），A/2 拉起时 `launch-not-attached`：本机 88% 负载下 psmux `new-session` 7s、attach 客户端 30s 内没确认（手工复现）；宿主判"无事可做"后 run-dogfood 返回 0 掩盖了 paused 节点 | E-009（review-logs/dogfood-blocked-run2*） | `AttachDeadlineSeconds` 30→90、`StopDeadlineSeconds` 15→30（K-16 更新）；run-dogfood 结束时若有非 succeeded 节点返回 3 并打印 `<node>=<scheduling>/<pause_reason>` | resolved |
| F-015 | **P1（真缺口·run3 逮到）** | B 拉起后**首个** probe 因启动慢、屏幕指纹未变而判 `idle`，Runner 契约 `launching→idle` 不是合法边 → `transition-rejected:edge-not-listed` → B 被 pause `illegal-terminal-transition`；run2 没撞是时序运气（首 probe 恰逢 claude 在输出） | E-010（review-logs/dogfood-blocked-run3*） | adapter：handle 记 `observed_running`，会话活着且从未报过 running 时首 probe 一律 `running`（launching→running），之后再走指纹判据；离线套件 idle 用例改为"首 probe running、二次 idle"（46 断言）；run4 重跑 | resolved |
| F-016 | **P1（psmux 缺陷·run2/run4 根因）** | psmux `attach -t <name>`（含 `attach-session`/`-t \`/`-t name:`）**无视目标**，一律接到服务器"当前会话"（有客户端时=最近活动的会话，否则=最后创建的）；单会话时碰巧正确（批0/批A 全过），多会话共存时（A/2 拉起时 B 仍在）新窗口接到旧会话 → 新会话 attached=0 → `launch-not-attached`；`list-clients` 记账也不稳（switch-client 后客户端条目消失）；主控 3 会话复现脚本 100% 重现 | E-011 | K-4 修订=**窗口拥有会话**：客户端进程直接 `psmux new-session -s <name> -n <node> -- <cmd>`（非 -d），adapter 轮询会话出现→设标题→等 attached∧visible，全程不调 attach（离线套件加"源码无 attach"断言·48）；真实套件 14/14、3 会话并存全 attached、adapter 级 preflight 5/5 重取 | resolved |
| F-017 | **P1（真缺口·decision run1 逮到）** | A 会话明明活着（pane 进程在、attached=1、用户尚未被问到），某次 probe 报 `exited` → Runner 判 `exit-without-result` 暂停 A（K-13 之外的 fail-open）；根因推断=负载下 psmux 客户端偶发连不上服务器（列表为空/"no server running"），adapter 把"列不到"当"不存在" | E-013（review-logs/dogfood-decision-run1*） | adapter 增 `ProcessProbe` 缝：列表缺席且 pane 进程仍活 → `probe_error psmux-command-failed`（有界·ProbeMaxConsecutiveFailures 兜底），进程也没了才 `exited`；stop 确认同样要求进程消失；离线 49 断言含"列缺席但进程活=probe_error" | resolved |
| F-018 | P2 | `relay-agent-tool checkpoint -Status decision_required` 不带 `-Note` 被 schema 拒（`bad-type:progress_note`），A worker 卡住排查 | E-013 | 工具默认 progress_note=`checkpoint <status>`；套件 32 断言；decision brief 步骤 2 也补了 -Note | resolved |
| F-019 | P3 | decision run2 通过，但"挂起期间 C.frozen_by=[A]"缺 state 快照（当时只截图）；本轮由事件顺序（B 在决策期间 result_accepted、C 在 A result 后才 launch_receipt）+ DHR_02 决策回放套件的 frozen_by 断言间接证 | E-014 | run-dogfood 已加"决策态时拷 relay-state.json 为 state-at-decision-<event_id>.json"，下次跑即有直接快照 | open（记录·下轮补） |
| F-020 | P3（orca 派活体验·用户指示试用） | 用 orca 拉起 codex 做文档活（as-built + H2 表）：`terminal create` 成功但返回 warning "could not make it discoverable / reveal timed out"、surface=background；`terminal focus` 11.6s 后才亮出；`terminal read` 可取 tail（够用）；codex 在其中正常跑完（DOC-DONE·两份稿质量可用、数字全按来源文件重数）；**`terminal close` 44s 后返回 `tab_not_found`，随后 `read` 仍报 status=running 而 `list` 已不含**（与 F-005 同类：回收返回值/状态不可信） | E-016 | 记录：orca 当"派活终端宿主"能用但可见性/回收语义不稳，作 relay 后端仍不达 A7 P5；主控清理时以进程为准 | open（记录） |
| F-021 | P3（范围外·backlog 提示） | dh-crew `dispatch-launch.ps1` 的 psmux 拉起走 `new-session -d` + 另开 attach 客户端；按 F-016 实测，本机 psmux `attach -t` 无视目标，dh-crew 多 worker 会话共存时也会接错窗口（DHR_03 只做记录，不改 dh-crew） | E-011 | 记 backlog：dh-crew 侧评估是否同样改窗口拥有会话 | open（记录·范围外） |
| F-022 | P2（两轮换人复核合集·失败分支健壮性 + 证据卫生） | 轮1 account4（`review-logs/review-r1-account4.md`）与轮2 account9（`review-r2-account9.md`）0 P0/P1、各 approved-with-P2。P2 汇总：①launch 阶段1 失败/超时不 kill-session、不停窗口进程（R2-01/R1 附注A）②stop 遇 list 瞬态为空判 unknown-session、与 probe F-017 不对称（R2-02）③launchCommand/clientLauncher 抛异常穿透宿主 tick（R2-03；真机另撞一次 registry Move "Access denied" 同类）④dogfood 回收 stop 失败永不重试（R2-04）⑤worker-entry 无条件删非空 CLAUDE_CONFIG_DIR（R1-02/R2-05）⑥`Copy-Item -LiteralPath '*'` 不展开通配→dogfood 夹具从未进证据（R1-03）⑦宿主 done 含 paused 节点仍返 0（R1-04/R2-08）⑧**windows.txt 记录全部可见窗口标题（含个人应用）进仓（R1-05）**。P3：静态扫描漏 run-dogfood/send-keys（R1-06）、E-012 措辞不实+plans/ 不进证据（R1-07/R2-06）、截图 event_id 前缀复用（R2-07） | E-017 | **全部已修**（adapter：`Invoke-RelayPsmuxLaunchCleanup`+可注入 `ClientStopper`、stop 判据、launch 三处 try/catch、registry Move 有界重试、默认窗口启动器改 ProcessStartInfo.ArgumentList；host：`Get-RelayHostOutcome`、windows.txt 只记 RELAY:*、evidence 含 plans/；run-dogfood：stop 重试 3 次+收尾清单、逐目录拷证据、截图名加 tick、出口走 Get-RelayHostOutcome；worker-entry 只删空值；preflight windows.txt 同改；已入仓 22 份 windows.txt 就地清洗为 RELAY:* 白名单）；离线 adapter 49→53、host-loop 32→35 断言（每条修复配对应用例）；重跑离线/真实/preflight/dogfood blocked run6（E-018） | resolved |
| F-024 | P2（证据隐私面·主控自查） | 主控 E10 备料查看截图时发现：`Save-RelayScreenshot`/preflight `Save-Screenshot` 抓的是**整个虚拟桌面**，RELAY 窗口之外还把用户其他终端/应用的可见内容一并存进了进仓证据（run5/run6/decision run2/preflight 共 24 份已入仓 PNG + 14 份未入仓归档 PNG）；两轮复核只逮到 windows.txt 标题（R1-05），没看图内容 | 主控查看 `evidence/decision/shots/*decision-waiting.png` 与 `evidence/blocked/shots/*S-0002.png`（已删） | 截图改为只拍 `RELAY:*` 窗口自身：EnumWindows 找标题前缀 → `GetWindowRect` + `PrintWindow(PW_RENDERFULLCONTENT)`（被遮挡也拍得到窗口内容）→ 多窗口纵向拼图、无窗口写占位图；preflight 复用同一函数；**全部 38 份旧 PNG 已删除**（tracked 24 份随本轮提交删除、branch squash 合入后历史不带入 master），preflight 5/5 重取（新图仅含 RELAY 窗口）、blocked run7 重取（E-020）；decision run2 截图删除后无图——建议用户空闲时再答一次 decision run3 补图（可选，H1 判断不依赖此图：事件/checkpoint/timeline 证据完整） | resolved（截图代码）/ decision 补图待用户定 |
| F-023 | P3（backlog·本卡不处理） | 复核遗留三条：R1-08 宿主 `.consumed-<ticks>` 文件无轮转（长跑 inbox/attempts 目录无界增长）；R1-09 dogfood spawner 同步 `WaitForExit` 阻塞 tick（有意设计：headless spawner 秒~分钟级、README 已述；异步化留完整流水阶段）；R2-09 Runner `Write-RelayJsonAtomic` 提交临时名 `checkpoint.json.tmp/result.json.tmp` 与 worker 两阶段写同名（宿主 `.host-read-<guid>` 只解摄入竞态，提交窗口内 worker 新写会被覆盖；涉 Runner core=本卡"只扩不改"） | R1/R2 报告 | 记 DevPlan backlog（DHR_04 或完整流水卡）：consumed 归档/轮转、spawner 异步、Runner 提交 tmp 改 `.runner-tmp-<guid>` | open（记录·backlog） |
