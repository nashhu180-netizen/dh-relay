<!-- progress.md — 施工日志 + 证据账本。🟢 边做边记。这是"实际发生了啥"，容忍跑偏——路径偏了记这里，不回写任何计划文档。 -->
# progress — DHR_04 隔离/禁改/落点守卫

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|------|----|---------|------|--------|
| 2026-08-16 | 主控(Claude) | 用户对话授权「用 relay 的方法做 P2 开发，写代码让 grok 进行」；本卡定为 stage0 自举第一张试水卡（只新建独立目录、不碰驱动器本体）。`dh wt new DHR_04` 建树 `.dh-worktrees/DHR_04`（branch `wt/DHR_04`）。 | E-001 | 建 stage0 冻结驱动器 |
| 2026-08-16 | 主控(Claude) | 把 `tools/relay/` @ `34df46a` 整份冻结到 `D:\relay-stage0\`（附 `STAGE0.md` 说明"永不手改"），从冻结副本跑 P1 全量套件验活。 | E-002 | 写接力计划与三份 brief，起 relay 宿主 |
| 2026-08-16 | worker(grok) | 批 A：路径规范化 + 开发期隔离。新建 `tools/relay/policy/relay-policy.ps1`（`Get-RelayPolicyPath` / `Test-RelayPolicyUnderRoot` / `Get-RelayDevIsolationVerdict`）与 `tools/relay/tests/relay-policy.ps1`。TDD 五拍：A1 红（函数不存在）→ A2 路径 4 绿 → A3 隔离 5 绿。 | E-003 | 批 B 内容白名单 |
| 2026-08-16 | worker(grok) | 批 B：业务仓内容白名单。夹具 `fixtures/policy/authority-2cards.json`；`Get-RelayContentAllowSet`（固定两根 + change_scopes + 四条 harness 派生，缺字段 throw）+ `Get-RelayContentPolicyVerdict`（精确文件 vs 前缀、null snapshot fail-closed）。 | E-004 | 批 C 落点守卫 |
| 2026-08-16 | worker(grok) | 批 C：落点守卫 + 两条不得误杀反例 + 副作用分账。`Test-RelayPolicyModuleRelayFolder` / `Test-RelayPolicyWorkspaceRelayFolder` / `Get-RelayLandingVerdict` / `Get-RelaySideEffectLedger`。反例① 预存同名目录放行；反例② `docs/modules/relay/` 合法模块不误杀。 | E-005 | 批 D CLI 与接线 |
| 2026-08-16 | worker(grok) | 批 D：`Invoke-RelayPolicyCheck.ps1` 黑盒 CLI；真实临时 Git 仓四者交叉联证；套件登记进 `run-relay-tests.ps1`；reason 扫描根加 `../policy`（69 码）；全量 `RELAY ALL PASS (SKIPPED: 1)`；本卡 diff 自举 dev-isolation 退出码 0。 | E-006 E-007 E-008 | 交棒 review |
| 2026-08-16 | 返工(grok) | ① R1-01：`Get-RelayPolicyPath` 规范化后含 `..` 段直接 throw（`policy-path-dotdot`），不消解。TDD：三模式红测先红后绿。 | E-010 | ② R2-02 消解内部 `./` |
| 2026-08-16 | 返工(grok) | ② R2-02：`Get-RelayPolicyPath` 消解全部内部 `./` 段。断言 `src/./alpha/a.ts` → `src/alpha/a.ts`。 | E-011 | ③ R1-02 补 workspace 不得误杀红测 |
| 2026-08-16 | 返工(grok) | ③ R1-02：补 workspace 谓词 + landing 层两条不得误杀断言。变异 `$parts[5] -eq 'relay'`→`-contains` 后恰这两条红，改回后 49 绿。实现未改。 | E-012 | ④ R1-04 受限写环境 |
| 2026-08-16 | 返工(grok) | ④ R1-04：临时仓 `src/gamma/locked` 上 `icacls /deny USERNAME:(W)`，越权写失败，策略同样拒 `src/gamma/locked/x.ts`；`finally` 里 `/remove:d` 后删目录。 | E-013 | ⑤ R2-01 Mode all reason |
| 2026-08-16 | 返工(grok) | ⑤ R2-01：`-Mode all` 多 verdict 失败时顶层 reason 改为 `policy-violation:multi`（单失败仍用原码）。字面量断言已加。覆盖闸 70。 | E-014 | ⑥ R1-08/R1-07 退出码 |
| 2026-08-16 | 返工(grok) | ⑥ R1-08+R1-07：空 AfterPath / 零条有效路径 → exit 2；畸形 snapshot 与守卫 throw → exit 3 并打印 `relay-policy: guard error:`。头部注释写清退出码契约。 | E-015 | 抄 R1-03 等价覆盖 + 全量回归 |
| 2026-08-17 | 返工(grok) | 六件完成；清 `RELAY_*` 后全量回归绿。抄入 R1-03 等价覆盖 E-016/E-017。 | E-016 E-017 E-018 | 写 handoff 交棒 |
| 2026-08-17 | 二次返工(grok) | ① R4-01：`Get-RelayPolicyPath` 反斜杠归一后、逐段处理前，对前导 `/`、前导 `~`、盘符 `^[A-Za-z]:`、`IsPathRooted` 一律 throw `policy-path-not-relative`。非相对优先于 `..`（检查更早）。TDD 6 条先红后绿。review4 8 种形态 + 两条模块口径均 throw。 | E-019 | ② R4-02 验证绝对路径 reason |
| 2026-08-17 | 二次返工(grok) | ② R4-02：① 之后两条绝对路径不再走违规分支。`D:/repo/.dh-runtime/n.json` 与 `D:/repo/docs/modules/dh-crew/x.md` 纯函数均 throw `policy-path-not-relative`，三模式 CLI 均 exit 3。未再落到 `unclassified-path`，无需改 reason 归属。 | E-020 | ③ R3-02 钉死 .. 红测 exit 3 |
| 2026-08-17 | 二次返工(grok) | ③ R3-02：`..` 三条红测 `-ne 0` 改为 `-eq 3`。现役套件仍 62 绿。scratchpad 副本做 M-H（catch 把 `policy-path-dotdot` 路由到 exit 2）→ `SUITE FAIL (3)` 恰红这三条；仓库 CLI 未改。 | E-021 | ④ R3-03 守卫异常带出问题路径 |
| 2026-08-17 | 二次返工(grok) | ④ R3-03：throw 改为 `policy-path-dotdot: $Path` / `policy-path-not-relative: $Path`。既有 3 条全等断言改 `StartsWith`；补一条钉死消息含路径原文。CLI stderr 现为 `relay-policy: guard error: policy-path-dotdot: tools/relay/../protocol/evil.ps1`。 | E-022 | ⑤ R4-03 git 引号形态输入契约 |
| 2026-08-17 | 二次返工(grok) | ⑤ R4-03：CLI 头部紧挨退出码契约写输入契约（只收仓库相对路径；喂入前须 `-z` / `core.quotePath=false`；给可照抄命令）。**不做**读清单时剥 git 引号：半剥（只 Trim 引号）盖不住 `\xxx` 八进制，完整反转义越界且易半吊子。正确做法在调用方。R3-04 记 F-004，不改测试。 | E-023 | 全量回归 + E-008 复跑 |
| 2026-08-17 | 二次返工(grok) | 五项完成。清 `RELAY_*` 子进程全量回归绿。E-008 三点比较 + 剥引号自举 EXIT=0。先踩坑：带 git 引号的中文路径 → EXIT=1。 | E-024 E-025 | 写 handoff 交棒 |
| 2026-08-17 | 三次返工(主控) | ① R5-01+R6-01：`Read-RelayPolicyPathFile` 拆分由 `\r?\n` 改为 `[\0\r\n]+`（`-z` 输出与孤立 CR 都能拆），并在 `Get-RelayPolicyPath` 对控制字符 throw `policy-path-control-char` 做纵深防御。TDD 先红。**实现踩坑**：控制字符判据初版写成 `ToCharArray() \| Where-Object`，因 `[bool][char]0 -eq $false`，恰好对 NUL 失效、对 CR 生效——改用正则 `[\x00-\x1F\x7F]`。 | E-026 | ② R5-02 引号形态 |
| 2026-08-17 | 三次返工(主控) | ② R5-02：`Get-RelayPolicyPath` 对前导/尾随双引号 throw `policy-path-quoted`。**拒绝而非剥离**——半剥盖不住 `\xxx` 八进制，完整反转义越界；拒绝后调用方按推荐命令重生成清单即可。landing 由静默 exit 0 变为 exit 3。 | E-027 | ③ R6-02 JSON 条目类型守卫 |
| 2026-08-17 | 三次返工(主控) | ③ R6-02：`Read-RelayPolicyPathFile` 的 JSON 分支加条目类型守卫，非 string 条目 throw `policy-list-entry-not-string`（exit 3），不再降级成 `unclassified-path`。null / int / 嵌套数组三形态均覆盖。 | E-028 | ④ R5-03 断言半边 |
| 2026-08-17 | 三次返工(主控) | ④ R5-03：补一条断言钉死 `policy-path-not-relative` 消息含路径原文（此前只有 dotdot 半边有 Contains 断言）。行为本就正确，缺的是断言。 | E-029 | ⑤ R5-04 ~ 判据收窄 |
| 2026-08-17 | 三次返工(主控) | ⑤ R5-04：`~` 判据由 `StartsWith('~')` 收窄为 `-match '^~($\|/)'`，只拦 `~` 与 `~/…`；`~$tmp.docx` 这类 Office 锁文件回归为普通仓库相对路径。该断言用 try/catch 包住——裸调用一旦回归会抛异常中断整套件、遮住后面全部断言。 | E-030 | ⑥ 头部输入契约重写 |
| 2026-08-17 | 三次返工(主控) | ⑥ 重写 CLI 头部输入契约（拆分规则 + 四类拒绝码 + 为何拒绝不剥离）。**顺带逮到二次返工⑤ 留下、两轮复核均未发现的文档错误**：原推荐命令 `git status --porcelain -z` 输出是 `XY <path>` 不是裸路径，照抄直喂会报 3 条假违规（方向 fail-closed，非放行）。改推荐 `diff --name-only -z ×2 + ls-files -z --others`，并补一条断言钉住 porcelain 前缀不得被静默接受。 | E-031 E-032 | 全量回归 + 五条变异探针 |
| 2026-08-17 | 三次返工(主控) | 六项完成。套件 63→78 断言；清 `RELAY_*` 子进程全量回归绿；五条变异探针逐条见红；三条 P1 用返工前同一批探针文件复跑，全部由 EXIT=0 转为 1/3。R6-03 记 F-005。 | E-033 E-034 E-035 | 交两轮换人复核 |
| 2026-08-17 | 收口前小修(主控) | **范围裁决**：review7/review8 的八条 P3 按「是不是本卡的面」二分——R7-01/R8-02（U+2028 家族）/R8-03（TAB 认定不对称）属**清单解析面**，该面本不在本卡目标与三条机器证内（实施提示逐字写「只消费正式设计字段的**纯守卫**」），**整面收窄另立 `DHR_22`**；其余四条属谓词/账本面，本卡修完。 | E-036 | ① R7-02 回归 |
| 2026-08-17 | 收口前小修(主控) | ① R7-02：修我在三次返工里引入的回归——`~` 判据 `^~($\|/)` 把 POSIX `~user/` 放开了（review6 时 `~root/x.ts` 为 THROW）。改为 `^~($\|[^/]*/)`：判别子是**斜杠**不是 `$`，`~`/`~/…`/`~user/…` 全拦，`~$tmp.docx`（无斜杠）仍放行。补两条断言。 | E-037 | ② R7-03 引号两侧 |
| 2026-08-17 | 收口前小修(主控) | ② R7-03：引号断言原只有「两端包裹」夹具，只留 `StartsWith` 或只留 `EndsWith` 都不见红。补前导-only / 尾随-only 各一条。 | E-038 | ③ R7-04+R8-01 优先级 |
| 2026-08-17 | 收口前小修(主控) | ③ R7-04+R8-01：注释写的拒绝优先级（控制字符 > 引号 > 非相对 > dotdot）此前**零断言**——review8 把前两层整块对调，78 条断言一条不红。补相邻三层各一条混合夹具。 | E-039 | ④ R8-04 账本口径 |
| 2026-08-17 | 收口前小修(主控) | ④ R8-04：E-030 记法补上两层口径——纯函数层 `~$tmp.docx` 返回普通相对路径，**CLI 层同输入仍 EXIT=1 `unclassified-path`（默认拒绝兜底），不是放行**。原措辞易被读成"会放行"。 | E-030（修订） | 一轮换人复核 |

## 证据账本 (Evidence Ledger)

| ID | 类型 | 命令 / 路径 | 结果 (pass/fail/observed/waived) | 支撑什么结论 |
|----|------|-----------|------|------|
| E-001 | command | `dh wt new DHR_04` | pass | 任务树 `.dh-worktrees/DHR_04` + 分支 `wt/DHR_04` 起于 master `34df46a` |
| E-002 | command | `pwsh -NoProfile -File D:\relay-stage0\tests\run-relay-tests.ps1` | pass | 冻结驱动器可独立运行：`RELAY ALL PASS (SKIPPED: 1)`；跑的代码与被改的代码物理分离 |
| E-003 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 批 A 单套件绿：`ASSERTIONS 9` / `SUITE PASS`；覆盖规范化、前缀不误吞、大小写、空输入 fail-closed、forbidden-root / production-outside-relay / unclassified-path |
| E-004 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 批 B 单套件绿：`ASSERTIONS 21` / `SUITE PASS`；`src/alpha/` 可写、`src/beta/util.ts` 精确放行、`src/beta/other.ts`/`src/gamma/a.ts` 拒、null snapshot → `content-policy-violation:missing-authority` |
| E-005 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 批 C 单套件绿：`ASSERTIONS 37` / `SUITE PASS`；预存 `docs/modules/alpha/relay/x.json` 放行、合法模块 `docs/modules/relay/` 不误杀、legacy 根新写拒、marker-unprobed fail-closed、git 副作用不进内容白名单 |
| E-006 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 批 D 单套件绿：`ASSERTIONS 43` / `SUITE PASS`；CLI 缺参 exit 2、干净输入 exit 0；临时仓 `git status --untracked-files=all` 与 After−Before 一致；独立 CLI 与纯函数都拒 `src/gamma/x.ts` |
| E-007 | command | 清掉 `RELAY_RECEIPT`/`RELAY_RUN_ROOT` 后 `pwsh -NoProfile -File tools/relay/tests/run-relay-tests.ps1` | pass | 末行原文：`RELAY ALL PASS (SKIPPED: 1)`；出现 `=== relay-policy.ps1 ===`；reason coverage `PASS  production reason codes covered: 69` |
| E-008 | command | `git diff --name-only master` + 未跟踪文件 → `Invoke-RelayPolicyCheck.ps1 -Mode dev-isolation` | pass | 退出码 0；变更集均在 `tools/relay/` 或 `docs/modules/dh-relay/` |
| E-010 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 返工①：`ASSERTIONS 46` / `SUITE PASS`；dev-isolation / content / landing 对含 `..` 的输入均非 0 退出（先红后绿） |
| E-011 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 返工②：`ASSERTIONS 47` / `SUITE PASS`；`Get-RelayPolicyPath 'src/./alpha/a.ts'` → `'src/alpha/a.ts'` |
| E-012 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1`（含变异探针：`$parts[5] -eq 'relay'`→`-contains` 后改回） | pass | 返工③：现役 `ASSERTIONS 49` / `SUITE PASS`；变异后恰 `legal module workspace predicate is not a hit` 与 `legal module workspace is not killed by landing` 两条红（SUITE FAIL 2），改回后全绿 |
| E-013 | command | 套件内：`icacls <tmp>/src/gamma/locked /deny "%USERNAME%":(W)` → 写 `x.ts` 失败；`Get-RelayContentPolicyVerdict @('src/gamma/locked/x.ts')` → `ok=false` / `content-policy-violation:outside-allow-set`；`icacls /remove:d` + `Remove-Item` | pass | 返工④：受限写环境与策略结论一致（均拒未授权路径）；`ASSERTIONS 52` / `SUITE PASS`；finally 复原 ACL，无残留 |
| E-014 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1`；`pwsh -NoProfile -File tools/relay/tests/relay-contract-reason-coverage.ps1` | pass | 返工⑤：`ASSERTIONS 53` / `SUITE PASS`；覆盖闸 `PASS  production reason codes covered: 70`；顶层 reason 字面量 `policy-violation:multi` |
| E-015 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 返工⑥：`ASSERTIONS 56` / `SUITE PASS`；空 AfterPath exit 2；`[]` / `{"cards":null}` 均 exit 3 且输出含 `relay-policy: guard error` |
| E-016 | command | `pwsh -NoProfile -File tools/tests/run-all.ps1 -ListOnly` | pass | `SUITE COUNT: 105`，名字含 `relay` 的套件数 = **0**。等价判据 = 用 dh-crew 官方受影响面机制证明零受影响面，替代耗时 2h43m 且有已知 OOM 风险的全量 run-all。dh-crew run-all 的内建套件清单与 relay 无交集 |
| E-017 | command | `pwsh -NoProfile -File tools/tests/run-all.ps1 -AffectedBy 'relay-policy.ps1,Invoke-RelayPolicyCheck.ps1,run-relay-tests.ps1,relay-contract-reason-coverage.ps1,authority-2cards.json' -ListOnly` | pass | `SUITE COUNT: 0`。等价判据 = 用 dh-crew 官方受影响面机制证明零受影响面，替代耗时 2h43m 且有已知 OOM 风险的全量 run-all。`-AffectedBy` 是**过包含**启发式（引用即入选，宁多勿少），返回 0 = 105 个套件中无一引用本卡改动的文件 |
| E-018 | command | 清 `RELAY_RECEIPT`/`RELAY_RUN_ROOT`/`RELAY_ATTEMPT_DIR`/`RELAY_TOOL` 后 `pwsh -NoProfile -File tools/relay/tests/run-relay-tests.ps1` | pass | 末行原文：`RELAY ALL PASS (SKIPPED: 1)`；`ASSERTIONS 56`；`PASS  production reason codes covered: 70` |
| E-019 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1`；另探针 8 种非相对形态 + `/docs/modules/alpha/relay/x.json` + `D:/r/docs/modules/alpha/relay/x.json` 均 throw `policy-path-not-relative` | pass | 二次返工①：先红 `SUITE FAIL (6)` / `ASSERTIONS 62`，改实现后 `ASSERTIONS 62` / `SUITE PASS`。CLI 三模式 exit 3；纯函数盘符相对 / 前导斜杠模块 / 盘符模块 / `D:/repo/../.dh-runtime/n.json`（非相对赢 `..`）均钉死 |
| E-020 | command | 纯函数 + CLI 三模式探针 `D:/repo/.dh-runtime/n.json` 与 `D:/repo/docs/modules/dh-crew/x.md` | pass | 二次返工②：两条均 `policy-path-not-relative` / CLI iso+content+landing **exit=3**。R4-02 随 ① 自然消解，未改 reason 归属代码 |
| E-021 | command | 现役 `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1`；scratchpad M-H：副本 `Invoke-RelayPolicyCheck.ps1` catch 把 `policy-path-dotdot` 改路由 `Exit-RelayPolicyInputError`（exit 2）后跑副本套件 | pass | 二次返工③：现役 `ASSERTIONS 62` / `SUITE PASS`；M-H 副本 `SUITE FAIL (3)` 恰红 `dev-isolation/content/landing rejects path with ..`。仓库 CLI 未提交变异 |
| E-022 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 二次返工④：`ASSERTIONS 63` / `SUITE PASS`；新增 `guard error message includes the offending path`；CLI stderr 含路径原文 |
| E-023 | observed | `Invoke-RelayPolicyCheck.ps1` 头部注释（退出码契约正下方）；`findings.md` F-004 | observed | 二次返工⑤：输入契约与推荐命令已写；可选剥引号未做（理由见日志）；R3-04 放行记 F-004 |
| E-024 | command | `pwsh -NoProfile -Command "Remove-Item Env:RELAY_RECEIPT,Env:RELAY_RUN_ROOT,Env:RELAY_ATTEMPT_DIR,Env:RELAY_TOOL -EA SilentlyContinue; & pwsh -NoProfile -File tools/relay/tests/run-relay-tests.ps1"` | pass | 二次返工全量回归末行原文：`RELAY ALL PASS (SKIPPED: 1)`；`ASSERTIONS 63`；`PASS  production reason codes covered: 70`。本壳 `RELAY_*` 仍在 |
| E-025 | command | `git diff --name-only master...HEAD` + `git status --porcelain --untracked-files=all` 剥引号合并去重 12 条 → `Invoke-RelayPolicyCheck.ps1 -Mode dev-isolation` | pass | E-008 复跑 EXIT=0。对照坑：喂入 `"docs/modules/dh-relay/design/01-\344\272\247..."` → EXIT=1（unclassified-path） |
| E-026 | command | 返工前后同一批探针文件：`docs/…/findings.md\0.dh-runtime/relay/new.json\0` 与同内容纯 `\r` 版，各喂 `-Mode dev-isolation` 与 `-Mode landing` | pass | 三次返工①：返工前四项全 **EXIT=0**（静默盖章）；返工后全 **EXIT=1**（禁改根/legacy 根被逮）。纯函数层 NUL/CR 入单条目 → `policy-path-control-char` |
| E-027 | command | `-Mode landing` / `-Mode dev-isolation` 喂 `".dh-runtime/relay/\346\226\260.json"`（git quotePath 形态） | pass | 三次返工②：返工前 landing **EXIT=0**、返工后 **EXIT=3**（`policy-path-quoted`）；dev-isolation 亦 3 |
| E-028 | command | `-Mode dev-isolation` 喂 `[null,"src/alpha/a.ts"]` 与 `["src/alpha/a.ts",["nested"]]` | pass | 三次返工③：均 **EXIT=3**，stderr 含 `policy-list-entry-not-string`；此前两者均降级为 exit 1 `unclassified-path` |
| E-029 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 三次返工④：新增断言 `not-relative guard error also includes the offending path` 通过；行为本就正确，补的是断言 |
| E-030 | command | 纯函数 `Get-RelayPolicyPath '~$tmp.docx'` 与 `'~/.dh-runtime/n.json'`；CLI 同输入 `-Mode dev-isolation` | pass | 三次返工⑤：**两层口径分开读**——纯函数层前者返回 `~$tmp.docx`（普通相对路径）、后者 throw `policy-path-not-relative`；**CLI 层 `~$tmp.docx` 仍 EXIT=1 `dev-isolation-violation:unclassified-path`（默认拒绝兜底），不是放行**。R5-04 的意图（不被一个 Office 锁文件把整份清单打成 exit 3）达成，但"回归为普通相对路径"只描述纯函数层。返工前前者亦 throw（R8-04 订正措辞） |
| E-031 | command | `git -c core.quotePath=false status --porcelain -z --untracked-files=all` 原始输出直喂 `-Mode dev-isolation` | fail(预期) | 三次返工⑥：**EXIT=1**，3 条 violations 的 path 均带 ` M ` 前缀 → 证明 porcelain 输出不是裸路径清单，二次返工⑤ 的推荐命令有误。方向 fail-closed，非放行 |
| E-032 | command | 改后推荐命令三条（`diff --name-only -z master...HEAD` + `diff --name-only -z HEAD` + `ls-files -z --others --exclude-standard`）原始输出拼接直喂 `-Mode dev-isolation` | pass | 三次返工⑥：17 条目、**零预处理**（不剥引号、不手工拆 NUL）→ **EXIT=0**。E-008 自举现在可直接吃 `-z` 原始输出 |
| E-033 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1` | pass | 三次返工收尾：`ASSERTIONS 78` / `SUITE PASS`（返工前 63） |
| E-034 | command | `pwsh -NoProfile -Command "Remove-Item Env:RELAY_* …; & pwsh -NoProfile -File tools/relay/tests/run-relay-tests.ps1"` | pass | 三次返工收尾：末行原文 `RELAY ALL PASS (SKIPPED: 1)`；`PASS  production reason codes covered: 70` |
| E-036 | observed | 对 review7/review8 八条 P3 逐条判「面归属」，比对 DevPlan `#### DHR_04` 目标/非目标/三条机器证与实施提示原文 | observed | 收口前裁决：三条（R7-01/R8-02/R8-03）属清单解析面 → 整面另立 `DHR_22`；四条属谓词/账本面 → 本卡修。判据：实施提示逐字要求「只消费正式设计字段的**纯守卫**」，三条机器证只说「以变更前后**路径集**证明」，未规定守卫如何取得路径集 |
| E-037 | command | 纯函数 `~root/.dh-runtime/n.json` / `~` / `~/…` / `~$tmp.docx`；scratchpad 变异 M5-D（判据退回 `^~($\|/)`）与 M5-E（退回一律拦） | pass | 收口前①：前三者 throw `policy-path-not-relative`、`~$tmp.docx` 仍放行；M5-D → 恰红 `posix ~user/ throws not-relative`，M5-E → 恰红 `tilde-dollar lock file is a normal relative path`。两侧边界各自有牙 |
| E-038 | command | 纯函数 `".dh-runtime/relay/a.json`（前导-only）与 `.dh-runtime/relay/a.json"`（尾随-only）；变异 M5-B（只留 `StartsWith`）/ M5-C（只留 `EndsWith`） | pass | 收口前②：两者均 throw `policy-path-quoted`；M5-B → 恰红 `trailing-only quote throws quoted`，M5-C → 恰红 `leading-only quote throws quoted` |
| E-039 | command | 三条混合夹具（引号+NUL / **尾随引号+盘符** / 盘符+`..`）；变异 M5-A（控制字符块↔引号块整块对调）与 M-QR（引号块移到非相对块之后），**均回读确认块序真的变了才采信** | pass | 收口前③：三条分别报 `control-char` / `quoted` / `not-relative`，与注释优先级一致。M5-A → `SUITE FAIL (1)` 恰红 `control-char outranks quoted`（review8 做同一变异时 78 条一条不红）。M-QR → `SUITE FAIL (1)` 恰红 `quoted outranks not-relative`。**订正**：初版此格写「任意两层对调都见红」是**过头的**——review9 R9-01 实证，原夹具 `"D:/repo/…"` 两端包裹时前导 `"` 让 `^[A-Za-z]:` 与 `IsPathRooted` 双双落空，只有引号层命中，对调后仍报 `quoted`、85 条不红。已按 review9 建议改为尾随-only 引号 `D:/repo/.dh-runtime/n.json"`（两层谓词都命中），M-QR 才真正见红 |
| E-040 | command | `pwsh -NoProfile -File tools/relay/tests/relay-policy.ps1`；清 `RELAY_*` 子进程全量；`-z` 三条推荐命令原始输出直喂 `-Mode dev-isolation` | pass | 收口前收尾：`ASSERTIONS 85` / `SUITE PASS`；全量末行 `RELAY ALL PASS (SKIPPED: 1)`、闸门 70；E-008 自举 EXIT=0 |
| E-035 | command | scratchpad 副本五条变异（A 去控制字符 throw / B 去引号 throw / C `~` 退回一律拦 / D 拆分退回 `\r?\n` / E 去 JSON 类型守卫），每条写入后**回读确认变异已生效**再跑 | pass | 三次返工收尾：A→`FAIL(2)`、B→`FAIL(3)`、C→`FAIL(1)`、D→`FAIL(4)`、E→`FAIL(2)`，全部有牙。C 首跑因副本文件被上一进程占用、`Set-Content` 失败而得到假 `SUITE PASS`，回读校验后重跑才有效——**变异探针必须回读确认已生效** |
