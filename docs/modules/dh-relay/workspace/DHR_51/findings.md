<!-- dh:v1 -->
# findings — DHR_51

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|----|------|------|------|------|------|
| F-101 | P3 | 范围注记：F-011 封堵与 fencing 的最小实现触碰了卡面变更范围外的 `relay-core/store/store.mjs`（公开 `appendEvent` 拒绝终态 kind；新增可选通用 `writeGuard` 回调） | brief「授权范围内的 store 触碰」预登记段；`store.mjs` diff 两处共约 20 行 | 判定为 finding 处置授权内的必要延伸：只挡 runtime 层会给一切后续调用方（含 DHR_52 RPC）留库级旁路，违背 DHR_29 findings F-011 处置本意；guard 为域中立回调，不含 lease 语义。回归钉在 store.test.mjs 两测。package.json 仅改 `scripts.test` 纳入新测试文件（DHR_29 同例），未注册任何 bin | resolved（轮2 复核核销） |
| F-102 | P3 | 撕裂窗口评估（as-built relay-core §3.5 边界①移交）：工件 create-new 与事件追加是两个文件两次写盘，强杀可留下「有工件无事件」现场 | 本卡评估；`openStore` 以事件账为真值重建、工件按幂等键回装 | **评估结论：有意接受，本卡不实施 WAL/单文件事务**。依据：①单宿主 lease 独占下无并发写者，撕裂只源于自身强杀；②openStore 重建以事件账为准，「有工件无事件」表现为该次写入未发生，调用方按幂等键原样重投即可收敛，状态签名不受污染；③DHR_52 引入 RPC 后写者仍唯一（lease+fencing），撕裂面不随客户端数扩大。**重启评估的触发条件（登记）**：出现「工件存在与否本身参与状态判定」的机制（如以 receipts 目录计数推导 attempt），或 Run 现场迁移到非本地 FS | resolved（轮2 复核核销） |
| F-103 | P3 | PID 复用残余风险：宿主死亡后其 pid 被无关进程复用，inspect 会把已死宿主误报 alive 直到 TTL 过期 | 设计取舍：不做跨平台进程启动时间比对（Node 无统一 API） | 接受并双保险：①TTL 是兜底（最坏假 alive 至过期，期间第二宿主被 E_LEASE_HELD 正确拒绝，独占性无损）；②「持有人进程确认已死即视同过期」路径覆盖反向场景。登记为已知边界 | resolved（轮2 复核核销） |
| F-104 | P2 | 「持有人进程已死视同 lease 过期可接管」比 design 字面的「过期接管」更早放行接管 | `lease.mjs` acquire 分支；runtime.test.mjs 对应用例 | 有意增强而非缺陷：独占性由 wx 独占创建仲裁保证（并发接管者输家重读即知），可用性对齐 P1 恢复锁的陈旧锁回收语义；这是移交②等价性结论里明示的「强于 v1」差异点，非静默偏离 | resolved（轮2 复核核销） |
| F-105 | P2 | 移交② 等价性结论落账（compat-matrix §6 第 1 条「authority_generation → lease」） | `runtime.test.mjs`「移交②：P1 恢复锁/CAS Oracle 复验」用例（以 `result-A1-wrong-generation.json` 为 Oracle，authority_generation=2 锚点）；`store.mjs` writeGuard；`lease.mjs` verify/renew | **结论：v2 唯一写者保证不弱于 v1，且严格更强**。映射：v1 权威代次（写时 CAS 比代次）→ v2 fencing（写时重验 lease 持有人，覆盖一切变更入口而非仅 result）；v1 恢复锁陈旧锁回收 → v2「持有人进程已死即视同过期可接管」+ wx 独占仲裁；v1 代次随进程持有、崩溃即失 → v2 lease 有 TTL 过期兜底。实测：陈旧写者的事件写与结果写均在队列内被 `E_LEASE_HELD:lease-lost` 拒绝、状态零变化；当前权威幂等重投收敛。差异面登记：v1 无「接管」概念（崩溃后 authority 自然废弃、新进程从头来），v2 显式接管并补记 lease 事件——语义等价且可追溯性更强 | resolved（轮2 复核核销） |
| F-106 | P3 | 发号失败路径原子性边角（E4 复核 G 观察）：run 根先建、runs.json 后写，索引回写失败会留下孤儿 run 根且该 seq 未记账（下次同 seq 重发将撞 run.json EEXIST 而 start 失败） | `startrun.mjs` createRunWithNumbering 顺序 | 登记观察、本卡不修：失败现场明确报错、无静默重号（重发同 run_id 时 createStore 的 wx 会 EEXIST 拒掉而非覆盖），恢复动作是人工清理孤儿目录；彻底消除需「索引预留/回滚」事务化，归后续卡的运行治理 | resolved（轮2 复核核销） |
| F-107 | P3 | 宿主指纹要素登记（E14 一致性复核遗漏项）：design/02 D18 原文的 lease 语义含「宿主可执行指纹」要素，本卡 lease payload 未含该字段 | `lease.mjs` payload 形态；E14 复核结论 | 登记为有意不承接：卡面 B7 三态半条与 P5-M3 均未要求指纹；指纹的用途是防「同 PID 被不同可执行文件冒充」，本卡的 PID 复用残余风险已由 F-103 + TTL 兜底覆盖；lease 文件是运行现场内部形态（非契约），将来需要时加入 payload 不触碰能力指纹。若 DHR_52 RPC 阶段需要「宿主身份防冒充」更强保证，在此登记为重启评估触发点 | resolved（轮2 复核核销） |
| F-108 | P3 | 宿主层接线测试空洞（E5 教训复核 MUT-C/MUT-D）：删掉 host.mjs tick 循环的 `lease.renew()`、撤掉 openStore 的 `writeGuard` 接线，41/41 全绿 | E5 变异探针实证 | 已修一半：新增「会话在宿主层真实续租」（咬 MUT-C：删 renew 则 expires_at 不再推迟、renewals 计数红——变异探针实测红）与「租约被换手后旧会话 lost_lease 停机且不释放别人的租约」两测。MUT-D 残余空洞处置：当前宿主无业务写路径（无 executor），会话内无可触发的写点，writeGuard 接线闭环由 store 单测（writeGuard 失守拦截）+ 移交② Oracle 测（guard 挂载于 createStore）承担；不为测试给产品加 debug 钩子（属测试基建污染产品，违背 §6 教训的形态纪律）。DHR_31 引入 executor 写路径时，宿主层 fencing 端到端用例在该卡补 | resolved（轮2 复核核销） |
| F-109 | P1 | acquireLease 无限自旋：空/损坏 host-lease.json（open-wx 与 writeFile 之间被强杀即触发）→ readLease 当无锁 → wx 永远 EEXIST → 死循环无期限，M3 恢复路径挂死 | 轮1 复核 @ReviewRound1 实测 HUNG + 本卡修复后变异探针（损坏当无锁→E_LEASE_ACQUIRE_TIMEOUT 红） | 已修（66d24e3）：readLease 区分 ENOENT（无锁）与 LEASE_CORRUPT（在途/损坏）；损坏=可接管（unlink+wx，fencing 兜底旧持有人自愈，无双持有人）；acquire 加有界超时 E_LEASE_ACQUIRE_TIMEOUT + 退避；inspect 对损坏报 lease_expired 与 acquire 语义一致。同批修 repolock 反向洞：空文件=在途锁**不回收**（无 fencing 的临界区偷锁=双持有人，等超时而非偷）——回归测试两只。轮2 核销：真修+锁住（MUT-F109 红） | resolved（轮2 核销） |
| F-110 | P3 | 轮2 增量复核新发现汇总（R2-N1~N5） | 轮2 复核 @ReviewRound2 | R2-N1（readLease 不校验 expires → 异形租约打破三态不变量）已修：缺 pid/epoch/expires 一律 LEASE_CORRUPT；R2-N3（as-built 测试数 49→50）已校；R2-N4（E_REPO_LOCK_TIMEOUT 未入 reason-codes §四）已补。R2-N2（catch-all 吞程序错误为 CORRUPT）登记不修：接管的 wx 仲裁兜底安全，误吞至多触发一次回收；R2-N5（目录占位损坏 → inspect=lease_expired 而 acquire=有界超时）登记为 fail-closed 有意形态（R2 判定可接受）。R1-07 barrier 并发、R1-11 Windows 路径大小写归一：P3 观察登记不修（归后续卡运行治理） | resolved（处置落账） |

> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞
