<!-- progress.md — 施工日志 + 证据账本。🟢 边做边记。 -->
# progress — DHR_03 接真实可见 psmux 并完成阻塞接力 dogfood

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|------|----|---------|------|--------|
| 2026-08-15 | 主控(Claude) | 用户对话"继续DHR3"= 开工授权；主控 Code Scout（读 design/01、as-built、runner/adapter/schema 源码、dispatch-launch psmux helper）+ **本机 psmux/orca 原语实测**（S1～S6：精确匹配、`kill-session -t =name` 静默无效坑、set-titles+attach 可见窗口 EnumWindows 可证、orca 运行时在线）；建工作区 8 件套，task_plan 写到 worker 可照做粒度（批0～批E + K-1～K-15）；DevPlan 回填进行中 | 本工作区文件 + 落户 commit 6361b8d | 建 worktree wt/DHR_03 → 批0 preflight 主控亲跑 |
| 2026-08-15 | 主控(Claude) | 落户后主控亲跑 relay 11 套件基线全绿（E-001）；`dh wt new DHR_03` 建树 `.dh-worktrees/DHR_03`（branch wt/DHR_03）；写 `adapters/preflight/Invoke-RelayBackendPreflight.ps1`（P1～P6 判据表·psmux/orca 双后端·截图+windows.txt 同刻互证）。**首跑踩坑**：helper 参数名 `$Args` 撞 PowerShell 自动变量→每次 psmux 调用变成裸 `psmux`（=新建默认会话）刷出 35 个垃圾会话，`kill-server` 清理（当时会话表原本为空·无他人会话）→ 改名 `$Arguments` 重跑。**psmux 原语级 5/5 ALL PASS**（E-003）；**orca 原语级 4/5**（E-004：`--focus` 令 create 超时 18s→去掉后可建；P5 `terminal close` 返回 `tab_not_found` 错误但 `read.status=exited` 1.9~2.6s 内成立、list 不再含；P2 可见只有 app 内 `surface=visible` 字段·无 OS 级窗口标题可证）。**K-1 裁决：psmux 定案为后端；orca 不满足"全部判据 pass"，不提议修订设计决策 7**（结论与差异记 findings F-003～F-006）| E-001/E-003/E-004 + `evidence/preflight/` | 提交批0 → 派 codex 施工批A（psmux adapter）+批B（宿主循环/agent 工具）→ 主控跑真实套件 + adapter 级 preflight |

| 2026-08-15 | 施工 worker(Codex) | 批A 按 TDD 先新增 psmux 离线/真实套件，缺 adapter 红灯后实现三参数键、九键 adapter、清单/守卫/README；离线 42 断言全绿，真实套件按约定 SKIP。全量门禁发现不可自行化解的基线冲突：A1 要求 params 恰好 8 键，但禁改的 DHR_02 `relay-runner-authority.ps1` 仍断言恰好 5 键，导致全量失败；另 reason 守卫报告 adapter 一处非字面 reason。按 K-15 停工，未提交、未进入批B | E-005 | 主控裁决旧套件五键断言如何迁移后再续批A |
| 2026-08-15 | 主控(Claude) | 收 codex blocked → 裁决 K-16（旧五键守卫迁八键·仅此一条）；改 adapter L86 非字面 reason 为两分支、set-titles argv 挪出 reason 行（守卫误扫）；跑真实套件首红 `initial probe running` → 实测 **psmux `window_activity` 不随输出更新（F-009）** → K-5 改屏幕指纹判据（capture-pane+cursor 哈希·registry 存 `activity_fingerprint/activity_seen_at`），离线套件补 3 断言（45）；负载下 psmux CLI 时延实测（F-010）→ Attach 30/Stop 15；preflight 脚本点源作用域修复（F-011）；全量 `RELAY ALL PASS (SKIPPED: 1)`、真实套件 14/14、adapter 级 preflight 5/5（A7 正式证据）；提交批A | E-006 | 派 codex 施工批B（宿主循环/agent 工具·仅批B） |
| 2026-08-15 | 施工 worker(Codex) | 只施工批B：按 TDD 先落 host/agent-tool 两套件与全 FAKE 静态夹具，分别因缺生产文件红灯；随后实现 receipt 唯一身份链、checkpoint/result/propose 两阶段写、handoff+脱敏 tail+Git snapshot、worker-entry DryRun、可注入 spawner/adapter/clock 的宿主 tick 与 timeline/screenshot/evidence helper。Runner 的 `checkpoint.json` 原子临时名会占用 worker 的 `checkpoint.json.tmp`，宿主以私有读名规避冲突后仍在摄入后改名 `.consumed-*`，未改 Runner core。host 32 断言、agent-tool 28 断言及 15 套全量门禁全绿 | E-007 | 提交批B并写 DONE；批C/批D真实 dogfood 交回主控 |
| 2026-08-15 | 主控(Claude) | 收 codex 批B DONE（e7c8584·host-loop 32/agent-tool 28·15 套件绿）→ 主控亲验并补两处：propose 无 receipt 时按 `RELAY_RUN_ROOT` 定位 inbox（orchestrator/replanner 没有回执，原版 exit 3）+ 套件 2 断言；写 `host/run-dogfood.ps1`（渲染夹具→真实 psmux adapter→headless spawner→逐 tick 宿主+succeeded 回收+decision 截图→证据）与 `dogfood/{blocked,decision}` 夹具；交互 claude 进 psmux 实测三坑（F-012：继承 env 致未登录/子会话告警、Bypass 首次确认、trust）逐一排掉；**dogfood blocked run1** 跑到 B 交棒时 B worker 逮到 F-013（空列表 result 被拒）→ run1 作废留证、修工具+红绿断言 → run2 重跑 | E-007/E-008 | run2 结果 → C3 迟到结果 → 批D |
| 2026-08-15 | 主控(Claude) | dogfood blocked 连跑 run2～run5：run2 逮 F-014（负载下 attach 30s 超时·Attach 90/Stop 30·包装脚本 paused 返 3）、run3 逮 F-015（首 probe idle 撞 launching→idle 非法边·adapter 首 probe 一律 running）、run4 复现 attach 失败→深挖出 **F-016 psmux `attach -t` 无视目标**（3 会话脚本 100% 复现）→ K-4 改"窗口拥有会话"（新窗口直接 `psmux new-session -s` 非 -d·不调 attach）→ 离线 48/真实 14/3 会话并存/adapter preflight 5/5 全过 → **run5 全链 exit 0（0 人工动作）**；C3 迟到结果 A3 五条 PASS（`Invoke-LateResultCheck.ps1`）；提交批C | E-009～E-012 | 派 orca+codex 起草 as-built/H2 表（用户指示试 orca 派活）→ 换人复核轮1 → 批D decision（需用户在窗口答一次）|
| 2026-08-15 | 主控(Claude)+用户 | dogfood decision run1：A 撞 F-018（无 -Note 被拒）与 **F-017 活会话被误判 exited**（psmux 失联=fail-open）→ 修 adapter（进程存活交叉核对·49 断言）+ 工具默认备注（32 断言）→ **run2 exit 0**：A/B 并行拉起 → A 报到 → A 登记 decision（C 冻结）→ B 期间完成并回收 → **用户在窗口回答一次（zh）** → A 恢复 → A 成功 → C 拉起 → C 成功；A.txt「你好，接力世界」，C.txt 首行同 A；提交批D | E-013/E-014 | orca+codex 文档稿 + 复核轮1 收 → 轮2 → E9/E10 |
| 2026-08-15 晚～08-16 凌晨 | 主控(Claude)+复核 account4/account9 | orca 派 codex 写 as-built/H2 表（F-020/E-016·用户指示试用）；两轮换人 headless 复核并行：轮1 account4/glm-5.2（1h26m·`review-r1-account4.md`）approved-with-P2（HEAD 口径 0 P0/P1；钉定版 P1=F-017 已修）、轮2 account9/deepseek（`review-r2-account9.md`）approved-with-P2（对抗·三支变异复现）→ 主控合并两轮清单一次返工（F-022 ①～⑧ + P3 三条；见 review.md 返工收敛）+ 转 backlog F-023；重跑离线全绿（adapter 53/host 35）、真实 14/14（首跑撞 registry Move "Access denied" 一次→加有界重试）、preflight 5/5、**dogfood blocked run6 exit 0**（0 人工·新证据含 plans/dogfood/·windows.txt 只记 RELAY:*）+ late-check L1~L5；主控三支变异 M4/M5/M6 各恰被新断言逮红；已入仓 22 份 windows.txt 就地清洗；用户新指示「下次 codex 派活改用 codex plugin（cc）」已记忆 | E-017/E-018/E-019 | E9 七段汇报 + E10 证据展示 → 用户 H1/H2 人判 → verify → 合入 master → 销户 |
| 2026-08-16 凌晨 | 主控(Claude)+用户 | E10 备料自查逮 F-024（整屏截图含用户其他窗口内容）→ 截图改 PrintWindow 只拍 RELAY 窗口、删 38 份旧 PNG、preflight 5/5 与 blocked run7 重取（E-020）；E9 七段汇报 + E10 证据展示 → 用户对话点选：**H1 通过、H2 有条件值得（先跑真实业务任务再定）、授权本地收口包**；review 人类签名区回填 | E-020 + review.md 确认记录 | squash 合入本地 master → 集成复验 → verify → DevPlan 销户 → 清 worktree |

## 证据账本 (Evidence Ledger)

| ID | 类型 | 命令 / 路径 | 结果 (pass/fail/observed/waived) | 支撑什么结论 |
|----|------|-----------|------|------|
| E-001 | test | `pwsh tools/relay/tests/run-relay-tests.ps1`（master 6361b8d·落户后主控亲跑） | pass：11 套件 SUITE PASS + `RELAY ALL PASS` exit 0（reason 覆盖 55 码） | DHR_01/02 11 套件基线全绿，本卡不得回归 |
| E-003 | observed/test | `pwsh tools/relay/adapters/preflight/Invoke-RelayBackendPreflight.ps1 -Backend psmux -Level primitives -Out docs/modules/dh-relay/workspace/DHR_03/evidence/preflight` → `evidence/preflight/preflight-psmux-primitives.json` + `shot-psmux-visible.png`（+`.windows.txt`） | pass：P1 1:1（list-sessions 全等恰 1 行·psmux_id `$2700`）/ P2 visible（EnumWindows 标题全等 `RELAY:relay-PF-…` 1645ms 出现）/ P3 interactive（attached=1·list-clients 有行·send-keys 回显）/ P4 三次 probe 全等 / P5 裸名 kill 后 553ms 会话消失+客户端进程消失；INFO P6 `kill-session -t =name` exit 0 但会话仍在（陷阱复现）；`RESULT psmux 5/5: ALL PASS` exit 0 | **批0 psmux 过闸·定为后端**（K-1）；A7 正式证据待批A adapter 级 preflight |
| E-004 | observed/test | 同上 `-Backend orca -Level primitives -OrcaWorktree D:/MyFiles/ai-workflow/dev-harness`（→ `preflight-orca-primitives-devharness.json`）与 `-OrcaWorktree D:/MyFiles/ai-workflow/dh-crew`（→ `preflight-orca-primitives-dhcrew.json`·本仓已 `orca repo add` 注册）+ `shot-orca-visible*.png` | 两仓均 **4/5 HAS FAIL** exit 3：P1/P3/P4 pass；P2 pass 但仅凭 app 内 `surface=visible`+list 可见（无 OS 级窗口标题可证）；**P5 fail**：`terminal close` 返回 `tab_not_found` 错误、但 `read.status=exited` 1887/2609ms 内成立且 list 不再含（回收命令返回值不可信，须靠 read/wait 复核）；另：`--focus` 令 create 超时 18s（去掉后可建） | orca 作纯终端宿主候选**不满足 K-1 全过**；不提议修订设计决策 7；差异记 findings F-004～F-006 |
| E-002 | observed | 主控 psmux 原语实测（临时会话 RELAY-PF-PROBE-1/2，已清理）：`list-sessions -F` 三段/`list-panes -F` 八段可解析；`has-session -t <前缀>` exit 1；**`kill-session -t =<name>` exit 0 但会话仍在**；裸名 kill 后 has-session exit 1、pane 进程消失、attach 客户端进程退出；`set-titles-string RELAY:#S` + `Start-Process psmux attach` → EnumWindows 找到标题全等可见窗口（owner=WindowsTerminal）、attached=1、list-clients 有行 | observed | task_plan「主控 Code Scout 实测事实」S1～S5 |
| E-005 | test | `pwsh tools/relay/tests/relay-psmux-adapter.ps1`; `pwsh tools/relay/tests/relay-psmux-real.ps1`; `pwsh tools/relay/tests/run-relay-tests.ps1` | partial pass / blocked：离线 `ASSERTIONS 42` + `SUITE PASS`；真实 `SUITE SKIP`；全量中既有 authority 套件 59 条里 1 条失败（仍要求 params 恰好 5 键），reason coverage 另报 `non-literal-reason:psmux-adapter.ps1:86`，最终 `RELAY TESTS FAIL (2)` exit 1 | 批A 实现自身已转绿，但 A1 八键要求与禁止修改旧套件正文直接冲突，不能形成可提交批次 |
| E-006 | test/observed | 工作树 wt/DHR_03 主控亲跑：`pwsh tools/relay/tests/run-relay-tests.ps1`；`RELAY_REAL_TERMINAL=1 pwsh tools/relay/tests/relay-psmux-real.ps1`；`pwsh tools/relay/adapters/preflight/Invoke-RelayBackendPreflight.ps1 -Backend psmux -Level adapter -Out docs/modules/dh-relay/workspace/DHR_03/evidence/preflight` → `evidence/preflight/preflight-psmux-adapter.json` + `shot-psmux-adapter-visible.png`（+`.windows.txt`） | pass：全量 12 套件 + real SKIP 明示 `RELAY ALL PASS (SKIPPED: 1)` exit 0（reason 覆盖 63 码）；真实套件 `ASSERTIONS 14 / SUITE PASS`（真起可见窗口·initial running→silence idle→stop 249~2751ms exited→client 退出→二次 stop unknown-session）；adapter 级 preflight `RESULT psmux 5/5: ALL PASS`（P1 receipt session_id 全等+registry launch_id 绑定+list-sessions 恰 1 行·P2 EnumWindows 标题全等 3467ms·P3 attached/list-clients/echo·P4 probes `running,idle,idle`·P5 stop exited_after_ms=249 client gone）；中间过程观察：`window_activity` 静态（F-009）、psmux CLI 时延 0.5～3.7s（F-010） | **A7 正式证据（adapter 级 P1～P5 全过）**；A3 离线 45 断言、A4 真实 14 断言；批A 可提交 |
| E-007 | test | `pwsh tools/relay/tests/relay-host-loop.ps1`; `pwsh tools/relay/tests/relay-agent-tool.ps1`; `pwsh tools/relay/tests/run-relay-tests.ps1` | pass：host loop `ASSERTIONS 32 / SUITE PASS`；agent tool/worker-entry `ASSERTIONS 28 / SUITE PASS`（含 exit 3/4、两阶段写、handoff 头、脱敏限长、proposal hash、DryRun）；全量 15 套件 `RELAY ALL PASS (SKIPPED: 1)` exit 0，reason 覆盖 64 码 | 批B K-9～K-12 在 fake/注入时钟下完成且 DHR_01/02/批A 无回归；未起真实窗口、未跑 preflight |
| E-007 | observed | 主控 psmux 手工会话 cl-test～cl-test7（已清）：`claude --dangerously-skip-permissions` 在 pane 内依次撞 trust 提示（非仓内目录）、Bypass 首次确认、"Not logged in"（继承空 CLAUDE_CONFIG_DIR）；清继承 env 后答 RELAY-OK；headless `claude -p` 干净 env 34s 答 RELAY-HEADLESS-OK | observed | F-012 根因与修法；dogfood 前置条件 |
| E-008 | observed/test | dogfood blocked **run1**（run_id RELAY-DF-BLOCKED-20260815204413·作废）：orchestrator 87s `PROPOSED v1` → A 窗口起、A 按 brief `dependency_blocked` 交棒（0 人工）→ replanner 56s `PROPOSED v2` → B 窗口起 → **B result exit 4 ×2**（`bad-type:tests_run`）→ B 写 working checkpoint 说明并停下；日志 `review-logs/dogfood-blocked-run1.log` + B pane 抓屏 `review-logs/dogfood-blocked-run1-B-pane.txt`；修后 `relay-agent-tool.ps1` 套件 31 断言（原版对新断言红 3 条：F-013 1 条 + propose 无 receipt 2 条） | 前半链路（编排→拉起→阻塞交棒→重编排→拉起 B）真实跑通；F-013 真 bug 实证 |
| E-009 | observed | dogfood blocked run2（review-logs/dogfood-blocked-run2*）：前 8 步签名正确到 `launch_receipt|A|2`，A/2 `launch_failed adapter-launch-failed:launch-not-attached`（Attach 30s）；手工复现 88% 负载下 psmux new-session 7s | F-014 定值依据 |
| E-010 | observed | dogfood blocked run3（review-logs/dogfood-blocked-run3*）：B/1 首 probe `observation transition-rejected:edge-not-listed` → paused `illegal-terminal-transition` | F-015 实证 |
| E-011 | observed/test | run4 再撞 launch-not-attached（Attach 已 90s）→ 主控 3 会话复现脚本：psmux `attach -t m1` 恒接到 m2（当前会话）；`attach-session`/`-t \`/`-t name:`/`-t 0` 同样；改窗口拥有会话后 3 会话并存 `relay-RUN-3C-S-000{1,2,3}|1` 全 attached、逐个 stop 干净；离线 48 断言含"源码无 attach"、真实 14/14、adapter 级 preflight `RESULT psmux 5/5`（`evidence/preflight/preflight-psmux-adapter.json` 重取·P4 running,running,running） | F-016 实证 + K-4 修订有效 |
| E-012 | test（A3 首份正式证据·**08-16 起被 E-018 run6 取代**，run5 全套移至 `review-logs/evidence-blocked-run5/`） | dogfood blocked **run5** `pwsh tools/relay/host/run-dogfood.ps1 -Scenario blocked -ScreenshotOnLaunch -ScreenshotOnStop` → 当时 `evidence/blocked/`：`signature.txt` 精确 = `plan_proposed→plan_activated→launch_receipt|A|1→result_accepted|A|1→plan_proposed→plan_activated→launch_receipt|B|1→result_accepted|B|1→launch_receipt|A|2→result_accepted|A|2`（+C3 后 `result_stale|A|1|stale-plan`）；exit 0；三 receipt session S-0001/2/3 互异；A/2 的 `resume_from={A,1}` 在 plan v2 提案体（`.dh-runtime/relay/<run>/plans/relay-plan.v2.proposal.json` / `inbox/proposal-v2.json.consumed-*`；receipt L-0003.json 本身无该字段——复核 R1-07/R2-06 更正措辞，run6 起 evidence 含 plans/）；work/A.txt=`ref: B-DONE-20260815215722` 引用 work/B.txt 首行；shots/ 3 张拉起 + 3 张回收前（A1/B1/A2 各一·复核更正计数；文件名含 event_id + windows.txt 含 RELAY:<session>）；orchestrator 54s/replanner 71s（spawns/*.log）；**人工动作 0**（主控与用户均未碰窗口）；C3 `Invoke-LateResultCheck.ps1` L1～L5 PASS（旧 receipt 合法 succeeded result 经真实 `Submit-RelayResultFile` → `result_stale|A|1|stale-plan`、仅新增 1 事件、relay-state/active-plan/authority sha256 不变、A/1 已提交 result 仍 dependency_blocked）→ `late-result-check.json` | **A3 机器证：blocked→重编排→fresh A 续跑 0 人工 + 迟到结果拒收** |
| E-013 | observed | dogfood decision run1（review-logs/dogfood-decision-run1*）：orchestrator 299s（后台 codex+复核抢 CPU）；A checkpoint decision 因无 -Note exit 4；22:23:14 A 被判 exited/interrupted_unknown 而 pane 进程 34256 仍活、attached=1（A pane 抓屏 dogfood-decision-run1-A-pane.txt） | F-017/F-018 实证 |
| E-014 | test（**A4 正式证据**·H1 材料·**08-16 截图已删**：F-024 整屏截图含用户其他窗口内容，PNG 全部删除，只保留 `.windows.txt`/events/state/timeline；补图需用户再答一次 decision run3，可选） | dogfood decision **run2** `run-dogfood.ps1 -Scenario decision -ScreenshotOnLaunch -ScreenshotOnStop` → `evidence/decision/`：签名 `plan_proposed→plan_activated→launch_receipt|A|1→launch_receipt|B|1→checkpoint_accepted|A|1(working)→checkpoint_accepted|A|1(decision_required q=zh/en)→result_accepted|B|1→checkpoint_accepted|A|1(working·user chose zh)→result_accepted|A|1→launch_receipt|C|1→result_accepted|C|1`；exit 0；B result 落在决策事件(idx19)与恢复事件(idx37)之间(idx32)=挂起期间 B 继续；C 的 launch_receipt 在 A result 之后=依赖冻结/解冻；三 checkpoint consumed 文件内容与签名一致；shots：A/B 拉起、A decision-waiting、B before-stop、A decision-resumed、A before-stop、C 拉起、C before-stop（文件名含 event_id + windows.txt 含 RELAY:<session>）；work/A.txt=你好，接力世界、C.txt=同首行+C-DONE；宿主/worker-entry 源码 forbidden 引用 0、host 日志 suspend/resume 0；**人工动作 1**（用户在 A 窗口回车选 zh）；宿主 status 行 62 条（blocked run5 为 46 条） | **A4 机器证 + H1 人判材料**；frozen_by 直接快照缺（F-019） |
| E-015 | test（变异探针） | 主控对 `psmux-adapter.ps1` 三支变异（M1 去掉首 probe running / M2 去掉进程存活交叉核对 / M3 偷偷改回 attach）各跑离线套件 → 分别恰被 `first probe after launch reports running (F-015)` / `session missing from list while pane process alive is a probe error (F-017)` / `adapter never calls attach (F-016)` 单条断言逮红（SUITE FAIL (1)），恢复后 SUITE PASS、工作树干净 | 套件对本卡三处关键修复真有牙 |
| E-016 | observed | orca 派活 codex（handle term_8035ba5c…·title RELAY:DOC-CODEX·worktree path:wt/DHR_03·command pwsh -File orca-codex-run.ps1）：create ok+reveal 超时告警 → focus 11.6s → read tail 可用 → codex 完成 DOC-DONE（`as-built/relay-psmux-host.md` 91 行、`evidence/H2-对照表.md` 31 行·主控复核后补 F-017/F-018/断言数/decision 行）→ close 44s 返 tab_not_found、read 仍 running、list 已空 | F-020；用户"试 orca 派活"的实测结论 |
| E-017 | review（两轮换人） | 轮1 `review-logs/review-r1-account4.md`（account4/glm-5.2 headless·钉定 6361b8d..12e1353+漂移 ..8a0fcbc·A~H 全 PASS·离线 15 套件亲跑·签名亲自重算）；轮2 `review-logs/review-r2-account9.md`（account9/deepseek headless·对抗·`6361b8d..HEAD`·M1/M2/M3 变异复现各恰红·禁改路径 0 命中·151 文件全在 tools/relay+docs/modules/dh-relay） | 轮1 approved-with-P2（R1-01 P1 已修·P2×4·P3×5·附注 2）；轮2 approved-with-P2（P2×4·P3×5）；0 open P0/P1；两份报告均"证据链可信、未发现可伪造点" | G6 两轮独立复核记录；返工输入（F-022/F-023） |
| E-018 | test（A3 证据·run6·**08-16 被 E-020 run7 取代**：run6 PNG 属 F-024 整屏截图已删、其余原件移至 `review-logs/evidence-blocked-run6/`） | 返工后 dogfood blocked **run6** `pwsh tools/relay/host/run-dogfood.ps1 -Scenario blocked -ScreenshotOnLaunch -ScreenshotOnStop`（run_id RELAY-DF-BLOCKED-20260816001924·日志 `review-logs/dogfood-blocked-run6.log`）→ `evidence/blocked/`：签名 12 段 `plan_proposed→plan_activated→launch_receipt|A|1→checkpoint_accepted|A|1→result_accepted|A|1→plan_proposed→plan_activated→launch_receipt|B|1→result_accepted|B|1→launch_receipt|A|2→checkpoint_accepted|A|2→result_accepted|A|2`（比 run5 多两条 worker 自愿 working checkpoint，A3 要求的 blocked→v2+B→B done→fresh A 子序列精确成立）；exit 0；**人工动作 0**；65 tick / 8m40s（事件 00:21:05→00:29:46）；orchestrator 95s / replanner 71s；work/A.txt=`ref: B-DONE-20260816002642` = B.txt 首行；`plans/relay-plan.v2.proposal.json` A.resume_from={A,1}（直接证）；shots 6 张（3 拉起 + 3 before-stop·before-stop 名含 tick 序号）+ windows.txt 只含 `RELAY:*`（首行 total=16 relay=1）；`dogfood/` 六份渲染夹具进证据；stop 两会话各 try=1 ok（491/281ms）、收尾无未回收会话、psmux list-sessions 空；C3 `Invoke-LateResultCheck` L1~L5 PASS（events 72→73 仅一条 `result_stale|A|1|stale-plan`·三文件 sha256 不变）→ `late-result-check.json`；顺带观察：headless orchestrator 结束时打印新装 codex 插件 SessionEnd hook "Hook cancelled"（不影响 exit 0·下次派活试插件时留意） | **A3 机器证（返工后代码路径全量重取）** |
| E-019 | test（返工验证） | 离线 `run-relay-tests.ps1` → `RELAY ALL PASS (SKIPPED: 1)`（adapter 53 / host-loop 35 / agent-tool 32·reason 覆盖 64 码）；`RELAY_REAL_TERMINAL=1 relay-psmux-real.ps1` 14/14（首跑 registry Move Access denied 一次→加 5 次有界重试后两次全过·孤儿会话已清）；adapter 级 preflight `RESULT psmux 5/5`（tag PF-20260816001554）；变异探针 M4（阶段1 超时不清理）/M5（stop 空列表判 unknown）/M6（宿主 done 恒 0）各恰被 `stage-1 timeout kills session and stops the client process (R2-01)` / `stop with transiently empty list but live pane still kills and confirms (R2-02)` / `host loop returns 3 when done with a paused node (R1-04)` 单条逮红、恢复字节一致 | 返工代码有测试咬合、无回归 |
| E-020 | test（**A3 正式证据·现役**） | F-024 修后 dogfood blocked **run7** `pwsh tools/relay/host/run-dogfood.ps1 -Scenario blocked -ScreenshotOnLaunch -ScreenshotOnStop`（run_id RELAY-DF-BLOCKED-20260816003930·日志 `review-logs/dogfood-blocked-run7.log`）→ `evidence/blocked/`：签名 13 段（A3 子序列 blocked→v2+B→B done→fresh A 精确成立；本次 A/1、B/1、A/2 各多一条 worker 自愿 working checkpoint）；exit 0；**人工动作 0**；67 tick / 8m42s（00:40:27→00:49:10）；orchestrator 52s / replanner 74s；work/A.txt=`ref: B-DONE-20260816004440`=B.txt 首行；plans/ v2 A.resume_from={A,1}；**shots 6 张只含 RELAY 窗口**（PrintWindow·before-stop 图为 A/2+B/1 两窗纵向拼图，主控打开逐张目视确认无桌面其他内容）+ windows.txt 只含 RELAY:*；stop 两会话 try=1 ok（1035/297ms）、收尾无未回收、psmux list-sessions 空；C3 late-check L1~L5 PASS（75→76 仅 `result_stale|A|1|stale-plan`·三文件 sha256 不变）| **A3 机器证（现役）+ H1 blocked 现场图** |
