<!-- progress.md — 施工日志 + 证据账本。🟢 边做边记。这是"实际发生了啥"，容忍跑偏——路径偏了记这里，不回写任何计划文档。项目有 journal 时本文件代替 journal（不双写）。 -->
# progress — DHR_31 basic-agent-task 端到端闭环

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|------|----|---------|------|--------|
| 2026-08-28 | 主会话 (Fable 5) | D 开工分流经用户对话确认（开 worktree / 施工全派 codex / 委托按默认）；落户建工作区骨架 | 本次对话 AskUserQuestion 三题点选 | S1 brief 委托起草 + Code Scout 侦察 + S2 施工步骤 |
| 2026-08-28 | 主会话 (Fable 5) | S1 brief + review 预填由委托 subagent 起草、主会话逐字审过；主树落户提交 `403b367`；开树 worktree=`.dh-worktrees/DHR_31` branch=`wt/DHR_31` client=claude-code。`dh wt new` 默认基点 origin/master 落后本地（DHR_30 合入未 push），已 `git reset --hard master` 对齐 403b367 | `git worktree list`；`git log --oneline -2` | 等 Code Scout 报告 → S2 写施工步骤（codex worker 粒度） |
| 2026-08-28 | 主会话 (Fable 5) | Code Scout 侦察回收（8 分节：workflows/ 不存在、validate 是 stub@service.mjs:550、节点/attempt 状态机就位无人驱动、H6 四例现成素材、DSH 树外配方与三坑）；据此冻结 S2 施工步骤（批 0~5，codex worker 粒度）；发现 H5 已 superseded 登记 F-002（方向不受影响，P5-M5+H1/H2 承载） | E-002（见证据账本）；task_plan.md 施工步骤节；findings F-002 | 派 codex 施工批 0+1 |
| 2026-08-28 | codex-worker | 批 1 TDD：新增 H6 四例与缺失依赖/环/自环测试，完成零依赖 `assessRunReachability`，纯函数 7/7；服务级 RPC 测试见红后发现冻结 `contract_id` 被冻结 identifier pattern 拒于 handler 之前，按硬边界停止，登记 F-003；未改 contracts | E-010~E-014；F-003 | 等主会话裁决 contract id / 契约批次；批 1 未提交、未进入复核 |
| 2026-08-28 | 主会话 (Fable 5) | 基线四道门（与 codex 同树并发跑）：audit 0 违规、selftest 48/48、指纹 a990fdda 零漂移；npm test 有一条续租时序断言红（runtime.test.mjs:514，expires_at 落后 0.4s）——判并发高负载毛刺，树安静后重跑定论 | E-003 | 批 1 收尾后无沙箱重跑全量 |
| 2026-08-28 | 主会话 (Fable 5) | **F-003 用户裁决（对话 AskUserQuestion 点选）：运行时层证明、疤入尾巴**——task_plan 步骤 1.3 跑偏记录：四例不经 RPC 信封，改为直接调 runtime 层 validate 接口（handler 实现照旧）+ 保留一条 RPC 信封拒 `/` 的钉子测试作 F-003 证据；1.4 start fail-closed 不变。F-003 P1→P2 | findings F-003 处理栏 | 派 codex 续做批 1 剩余（1.3 调整版 + 1.4 + 1.5） |
| 2026-08-28 | codex-worker | 按 F-003 用户裁决续完批 1：`validateRuntimeDocument` 先走共享冻结校验器、再叠加 H6 可达性；四例与三类坏图改经 Runtime 直连接口证明，保留一条 RPC `E_BAD_VALUE` 疤痕钉；`runStart` 在发号/createStore 前 fail-closed，拒绝深度 1 且不留 Run 目录，golden 照常启动 | E-015~E-019；`relay-core/runtime/`、`relay-core/test/` diff | 精确暂存并提交；交回主会话派 fresh 小审 |
| 2026-08-28 | codex-worker | 以 6 个精确允许路径执行 `git add` + 既定 message 提交，Git 管理目录 `index.lock` 被 sandbox 拒绝；未暂存、未产生 commit，登记 F-004，完整工作树现场交回 controller | E-020；F-004 | controller 在可写 Git 元数据的会话精确暂存并提交；本 worker 停止 |
| 2026-08-28 | 主会话 (Fable 5) | 代 codex 精确提交批 1（`6823196`，F-004 resolved）；派批 1 小审（E-021，fresh codex 只读）——结论 changes-requested：E021-01 P2 缺全量绿证、E021-02 P3 reachability 未进默认测试清单，四例保真/算法/接线/边界四类 clean；整改：E-022 161/161 + package.json 追加 reachability 末位 + E-023 168/168；F-005/F-006 登记并 resolved，批 1 闭合 | E-021~E-023；review.md 第一轮表；findings F-005/F-006 | 按用户新指示经 Herdr 拉 claude(Opus) 交互终端施工批 2 |
| 2026-08-28 | 主会话 (Fable 5) | 用户改派活方式：批 2 起施工经 Herdr 拉交互式终端（本会话在 w1:p1，HERDR_ENV=1 已验证；claude kind 受支持）；worker=claude --model opus，agent prompt/wait/read 派活催收 | 本次对话用户明文指示 | pane split → agent start → 派批 2 |
| 2026-08-28 | opus-worker | 批 2 TDD 红：先写 `test/workflow.test.mjs`（5 组命题：依赖序闭环+重放一致 / 非 0 退出 / resume fresh attempt / stop 中断 / ref 逃逸），跑出 `ERR_MODULE_NOT_FOUND: runtime/process-executor.mjs` | E-024 | 落 2.1 Workflow 定义 + 2.3 driver |
| 2026-08-28 | opus-worker | 批 2 步骤 2.1：新建 `workflows/basic-agent-task/`（`run.template.json` = 一份合法 `relay.run/v2`；`steps/prepare|process-task|verify.mjs` 零依赖、stdin JSON→stdout JSON；README 写清 ref 解析口径与步骤合同）。三节点全 `required:true` 且只有 `process` profile ⇒ 顺带是 H6 阴性对照 | E-026 | 2.3 driver 接线 |
| 2026-08-28 | opus-worker | 批 2 步骤 2.3：新建 `runtime/process-executor.mjs`（`resolveStepRef` 防目录逃逸 + `startProcessStep` + 纯函数 `classifyStepOutcome`）与 `runtime/workflow-driver.mjs`（ready 节点→registerReceipt→spawn→appendResult→run_finished）；`service.mjs` 只**起** driver 不持写句柄，start / resume 接线、stop 先停 driver 再停 actor、close 一并收口。终态全部经 `appendResult`，driver 一切读写排进 actor 串行队列（唯一写者不破） | E-025；`relay-core/runtime/`、`relay-core/workflows/`、`relay-core/test/` diff | 全量四门 + 提交 |
| 2026-08-28 | opus-worker | 批 2 收口四门：全量 `npm test` 173/173、audit 0 违规、selftest 48/48、指纹 `a990fdda` 零漂移且 `contracts/` diff 为空。过程中另见两条 Windows 并发毛刺（`EBUSY rmdir` 温度目录清理 / `EPERM pending-operations.lock` / 续租时序），单跑均绿，与 E-003/E-023 同族、非本批回归 | E-027~E-030 | 精确 `git add` 提交；登记 F-007/F-008；不进入复核、不开批 3 |
| 2026-08-28 | opus-worker | 批 2 小审整改（F-009 P0 / F-010 P2；F-007 已裁保持 fail-soft）：`resolveStepEntry` 新增**真实落点**守卫——对仓根与目标各 realpath 后再判仓内，符号链接逃逸与词法逃逸同途记 `E_BAD_VALUE`；原 `stepIsFile`（跟随链接的 `stat`）删除，driver 改用三态返回（越界 / 不可驱动 / 可驱动）。`resolveStepRef` 保留为纯词法守卫并在注释里写明它**挡不住链接**，安全边界在第二道 | E-033~E-034；`relay-core/runtime/`、`relay-core/test/` diff | 全量四门 + 精确提交 |
| 2026-08-28 | opus-worker | 整改回归两组：F-009 端到端链接逃逸（仓外脚本一次都不许执行——以 marker 从未生成断言）、F-010 缺文件保持 pending（零 node_started / 零 attempt_started / attempt_count=null）。**跑偏记录**：本机 Windows 无 symlink 特权（EPERM），按任务要求本应 skip；改为先试文件符号链接、失败退到**目录 junction**（Windows 免特权、对本命题等价），于是 F-009 在本机**实际执行**而非 skip，skip 分支仍保留并会打印原因 | E-033（变异核验）；E-034（7/7、0 skipped） | — |
| 2026-08-28 | opus-worker | 整改收口四门：`npm test` 175/175、audit 0 违规、selftest 48/48、指纹 `a990fdda` 零漂移且 contracts/fixtures diff 为空。F-009/F-010 置 resolved，F-007 随 F-010 闭 | E-035~E-039 | 交回主控；不进入复核、不开批 3 |
| 2026-08-28 | opus-worker | 批 3 步骤 3.1：新建 `test/e2e-basic-agent-task.test.mjs`——全程经 `relay` CLI 子进程走 start → events → status/inspect 到 succeeded。text/json 同源按 read-model-mirror 口径**升级为逐字比对**：把 `--json` 模型喂给 `cli/render.mjs` 的同一渲染函数，与 text 输出 deepEqual（弱断言「text 里含某字样」放得过「text 自己算字段」，正是该纪律要禁的）；另钉 status 与 inspect 的 `state_signature` 逐字相同 | E-041 | 3.2 备料（延时旋钮） |
| 2026-08-28 | opus-worker | 批 3 · 3.2 备料：`process-task` 支持可配置延时，经 run 文档 label `basic-agent-task.delay_ms` 注入（driver 把 `labels` **原样转交**进步骤上下文，不读不筛——labels 是 relay.run/v2 唯一的不透明搭车位）。选 label 不选环境变量的理由写进 README 与代码注释：service 是常驻进程，env 在它起来那刻就定死，后来的 `relay start` 改不动，会让「我设了 30 秒」与「1 秒就跑完」同时为真 | E-042 | 生成器 + 四门 |
| 2026-08-28 | opus-worker | **跑偏记录**：task 只要求「README 写清怎么生成带延时的 run 文档」，我先按字面写了 `node -e '…'` 一行；实测该写法在 python 落盘时 `
` 被吃、且跨 bash/PowerShell 引号转义各挂各的——3.2 是真实终端实录，当场调命令是最不该发生的事。改为新增 30 行生成器 `workflows/basic-agent-task/make-run.mjs`（自动推导 ref 前缀、`--delay-ms`/`--out`/`--prefix`），README 改为文档它。已实跑两种形态验证（带延时/不带延时）均 validate PASS | E-043 | 四门 + 提交 |
| 2026-08-28 | opus-worker | 批 3 · 3.1 收口四门：`npm test` 178/178、audit 0 违规、selftest 48/48、指纹 `a990fdda` 零漂移且 contracts/fixtures diff 为空。期间另见一次 `EPERM: spawn git`（service.test.mjs makeRepo）并发毛刺，复跑全绿，与 E-003/E-023/E-027 同族 | E-044~E-047 | 交回主控执行 3.2 实录；不开 3.2、不开批 4 |
| 2026-08-28 | opus-worker | 批 4（Agent 节点·窄路径）：新建 `test/agent-node.test.mjs` 四组。**本批零生产代码改动**——窄路径要的正是「Runtime 什么都不用做」，agent 节点因 driver 只认 `kind==="process"` 而天然不被托管，`capability-baseline.json` 的 `executor_kinds:["process"]` 因此仍是真话。四组：边界钉（driver 不碰 agent 节点、零事件）／① Adapter 消失 → 仅该 attempt `failed`+`E_EXECUTOR_ADAPTER_LOST`，他节点状态与结果工件逐字节不动／② 恢复后探活失败 → `orphaned`+`E_EXECUTOR_ORPHANED`，retry 开 fresh attempt、旧 attempt 迟到结论进隔离区／③ Agent 失败不推翻 Process 结论 | E-053 | 全量四门 |
| 2026-08-28 | opus-worker | **口径更正**：主控交代「两个孤儿码冻结零使用」——实际 `E_EXECUTOR_ORPHANED` 已在 `test/store.test.mjs:318` 作为 raw-append 拒绝用例的载荷出现过一次；真正首次使用的只有 `E_EXECUTOR_ADAPTER_LOST`。不影响决策（都没新增码），但记准 | `grep -rn E_EXECUTOR_ORPHANED` | — |
| 2026-08-28 | opus-worker | **自查出的测试竞态（我在批 2 引入的）**：批 4 首次全量红一条 `步骤非 0 退出`（期望 failed 实得 running）。根因不是产品——Store 写序是「先 append `events.jsonl`、后 writeAtomic `state.json`」，我的测试轮询事件账判完成、紧接着读快照，读到的是上一条事件时的旧快照；机器闲时窗口几毫秒，全量并发才现形。修法：新增 `test/helpers/settled-state.mjs`，等的是**不变量**（快照签名 === 事件账重放签名）而非「等出我要断言的值」，故后续断言仍是真断言。8 处直读 state.json 全部改走它 | E-054、E-055 | 复跑 |
| 2026-08-28 | opus-worker | 另两处观察登记 findings、本卡不修：F-013/F-014（`state.mjs` 的 `aggregate()` 不看 `required`——可选节点失败即把整条 Run 拉成 failed；必经全绿但有 pending 可选节点时又报 pending，于是「已完成」与「pending」同真）——已按**当前真实行为**钉进批4③ 的断言；F-015（并发下观察到一次 `relay events --json` 首行不是快照，根因假设是 CLI 写序竞态，未稳定复现，改为按内容定位快照行 + 显式断言它在首行，乱序时会红并打印原样输出） | E-056；findings F-013~F-015 | — |
| 2026-08-28 | opus-worker | 批 4 收口四门：全量 `npm test` **182/182**（108s，空载）、audit 0 违规、selftest 48/48、指纹 `a990fdda` 零漂移且 contracts/fixtures/capability-baseline 三者 diff 全空。**但全量绿现在看机器**：套件加到 17 个真实 service 文件后，`service.test.mjs:73` 那条 8s 的 start 预算会偶发超时（一轮 1 红、机器另有负载时 6 红；单跑该文件 13/13 绿）——登记 F-016 交主控裁决，本卡不改别人的预算 | E-057~E-060；findings F-016 | 精确提交；不开批 5 |
| 2026-08-28 | opus-worker | F-016 按用户裁决走方案②整改：`package.json` 的 test 脚本加 `--test-concurrency=4`，**只动这一行**——没碰 `service.test.mjs` 的 8s 预算、没改任何测试。当前机器（非空载）全量 182/182 绿、92.2s，比此前不限并发的 108.1s 还快：先前那些偶发红是 17 个文件各起真实 service 自相争抢，不是并行度不够。F-016 置 resolved | E-062 | 停下等小审派发 |
| 2026-08-28 | opus-worker | 批 4 小审 P1-1 整改：窄路径回归补齐 dsh-agent。`agentNode` 参数化出 kind（ref 语义按 ADR-002：pi=adapter_ref、dsh=bridge_ref），边界钉改为**同一份 run 文档里同时挂 pi + dsh 两个 agent 节点**、断言两者都不被 driver 碰；另加 ①bis 同形用例（宿主消失 → 仅该 attempt `orphaned`+`E_EXECUTOR_ORPHANED`，他节点状态与工件逐字节不动，retry 开 fresh attempt）。**零生产代码改动**。码位选择留痕见 F-017：没照抄 `E_EXECUTOR_ADAPTER_LOST`（:52 专指 pi 的 Adapter，套 dsh 是错的） | E-064 | P1-2 |
| 2026-08-28 | opus-worker | 批 4 小审 P1-2 整改：`--test-concurrency` 需 Node >=18.19.0，与 `engines: >=18.0.0` 冲突——`package.json` 与 `package-lock.json` 的 `packages[""].engines` 各改一行提到 `>=18.19.0`，未动任何依赖（`git diff` 恰为 2 行）。注：`npm ls` 报 ELSPROBLEMS 是本工作树无 `node_modules`、ajv 从父仓解析所致，与本次改动无关 | E-065 | 全量取绿证 |
| 2026-08-28 | opus-worker | **跑偏记录**：取绿证时 shell cwd 漂到 worktree 根，`npm test` 在无 package.json 的父目录跑挂（EXIT=127），且 `mkdir -p ../docs/...` 指向了主检出——已核主树 `git status` 干净、未生成游离目录。此后一律用绝对路径起后台任务 | E-066 | — |
| 2026-08-28 | opus-worker | 批 4 小审整改收口：全量 `npm test` **183/183**（101.1s，EXIT=0）、audit 0 违规、selftest 48/48、指纹 `a990fdda` 零漂移且 contracts/fixtures/capability-baseline 三者 diff 全空。按小审 P2 保留原始 TAP 输出落仓。**并发上限收窄了但没全灭紧预算这一类**：整改后仍见一次 `runtime.test.mjs:449`（detached 宿主 10s 落 2 事件）满载超时，单跑 26/26 绿、复跑全量 183/183 绿——已补记进 F-016 | E-067~E-070 | 停下等复裁 |
| 2026-08-28 | opus-worker | 批 4 复裁三件打包整改（**零生产代码改动**）：①P1-1 尾巴——`holderReports()` 的 `executor_kind` 由硬编码 `pi-agent` 改为必传参数（缺省即 assert 失败），6 处调用点补齐；dsh 的孤儿结论与重试成功结论**两条都断言 `executor_kind==="dsh-agent"`**。②F-016 退回 open。③F-017 按用户授权换用 `E_EXECUTOR_HOST_LOST`。变异核验：把 helper 改回硬编码 `pi-agent` → ①bis 以「dsh 的孤儿结论必须记成 dsh-agent，不得沿用 pi-agent」见红 | E-072、E-073 | 全量取绿证 |
| 2026-08-28 | opus-worker | **对复裁指令的一处偏离（技术前提不成立）**：复裁要求「在两条**事件**上断言 `executor_kind` 为 dsh-agent」——但 `store.appendResult` 只把 kind/at/node_id/attempt_id/reason/detail 传给 `emitEvent`（`store/store.mjs:279`），`executor_kind` **不进事件账**，事件里恒为 null。故断言落在**结果工件** `results/<receipt_id>.json`；同时把「事件里恒为 null」也钉住，免得日后有人把断言挪到事件上拿 `null===null` 白拿一个绿。要让事件带上它属改冻结 Store 的写入路径，不在本卡范围 | E-072；`store/store.mjs:279` | — |
| 2026-08-28 | opus-worker | F-016 记账方式的选择：**退回 open、不拆新 flake finding**。理由——本条命题是「全量绿不看运气」，该命题没成立、只是症状换了地方（`service.test.mjs:73` → `runtime.test.mjs:449`）；拆条会让 F-016 以「那个具体症状已修」的名义闭合，而真正要守的那句话反而没有归属人。一条命题一条账 | findings F-016 | — |
| 2026-08-28 | opus-worker | 批 4 复裁整改收口：全量 `npm test` **183/183**（104.8s，EXIT=0）、audit 0 违规、selftest 48/48、指纹 `a990fdda` 零漂移且三冻结物 diff 全空；原始 TAP 落仓。F-017 闭合、F-016 退回 open | E-074~E-078 | 停下等复裁 |
| 2026-08-28 | opus-worker | **批 4 第二轮复裁 Accept，批 4 闭合**（dhr31-r3，gpt-5.6-terra high）。复核方注明两点边界：仅放行批 4、`F-016` 保持 open·P2，**整卡收口另行处理**。据此落账：`task_plan.md` 批 4/5 检查点下加闭合块（复核链 + 落点 + 绿证指针）；`findings.md` F-016 补记「复裁明示保持 open，批 4 的 Accept 不代表本条命题已成立」 | E-079；task_plan「批 4 已闭合」块；findings F-016 | 停下等主控指令；不开批 5、不做收口动作 |
| 2026-08-28 | opus-worker | 批 5 开工第一动作按 5.1 规定跑镜像基线核对：`check-drift.ps1` → **IN SYNC**、exit 0、27 文件 / 2 包，24 个 EOL-only NOTE（`core.autocrlf` 已知项，不失败）。基线干净，可以动 | E-083 | 读 5.1 目标与数据形状 |
| 2026-08-28 | opus-worker | 读树外 `dsh-host` 与 `dsh-client` 后**停工**：5.1 的窄路径表达不了——面板消费 P4 期 pilot 模型，Bridge 只能给冻结的 `relay.client-read-model/v1`，两者字段集不是子集关系；缺的 13 个渲染字段里，一半在 `relay.run/v2` 运行文档里而冻结 RPC 不暴露它，另一半（attentions 族）在 P5 压根不存在。四条出路分别撞「禁编造 title（契约原文）／改冻结协议／破控制面只发 RPC 边界／改面板（brief out of scope）」。按硬边界登记 F-018 摆回主控，**未动树外任何文件** | E-084；findings F-018 | 停下等主控裁决 |
| 2026-08-29 | opus-worker | F-018 用户裁决走 (a)，恢复批 5·5.1。改动前重跑 `check-drift.ps1` = IN SYNC/exit 0（E-085b）。新增树外 `dsh-host/live-store.mjs`：把 v2 Read Model 投影成 Pilot 模型——P5 没有的 Attention 给 `attentions: []`（真值），运行文档里但 RPC 不暴露的（`workflow_name`/`title`/`role`/`depends_on`…）**整键省掉**，绝不拿 node_id 冒充 title。fixture 那条路一个字没动（DHR_26/49 转录与 `fixture_hash` 基线靠它） | E-086~E-088；`evidence/dsh-host-diff-20260829.txt` | 验收投影 |
| 2026-08-29 | opus-worker | **裁决授权的「面板校验放宽」没有动用**：实测两个校验器要的字段活数据全给得出（list 的 `run_id/group/run_status`、detail 的 `node_id/node_status` + `attentions` 是数组），其余未知字段面板全有 `"k" in run` / `Array.isArray` / `kv()` 守卫，**省键即优雅降级**。于是 `dsh-client` 一行未改，「面板零改动」实际保住了。顺带更正我在 F-018 里的一处推断错误：「attentions 是硬校验」为真，但据此推出的「所以必须改面板」为假 | findings F-018 处理栏 | — |
| 2026-08-29 | opus-worker | 5.1 验收：本仓起真实 detached service + 经 CLI 起一条真实 basic-agent-task Run（`R001-relay-basic-agent-task-1-20260829`），把活数据仓接上去 → 五方法全返回活数据，且与 Bridge 的 Read Model 逐字对拍 **ALL PASS**（含「未编造 title/role/depends_on/workflow_name」四条反向断言） | E-086 | 5.2 卡住 |
| 2026-08-29 | opus-worker | **5.2/5.3 卡住，登记 F-019 停下**：打包后的插件拿不到 Bridge——`dsh-host` 自己的安装拓扑契约（`test/package-contract.test.mjs:124`）禁止计算动态 `import()`，裸依赖更是当年炸过的那种形态，相对路径又会随 worktree 删除而断。已把 `LiveRelayPilotRepository` 改成**注入式 connect**（投影完整交付、契约不破、空位显式），但 DSH profile 只能传数据不能传函数，所以 5.2 repack / 5.3 截图在这一环定下来之前跑不了。**没有偷偷放宽别人的安全契约** | E-087；findings F-019 | 停下等主控裁决 |
| 2026-08-29 | opus-worker | F-019 用户裁决走 (iii)：新增 `relay-core/adapters/dsh-bridge/snapshot-main.mjs`——**本卡首次也是唯一一次动本仓生产代码目录**。只调 `listRuns`/`inspect`、原样吐 `relay.client-read-model/v1`，不投影不改名不补字段（投影留在插件侧，协议层客户端中立不破）；归 `adapters/` 不入 `runtime/`；只读。插件侧改为 `spawn` 它（`node:child_process` 是字面 `node:` 说明符，本包安装拓扑契约不破） | E-091；`control-plane-imports` 两钉仍绿 | 5.2 repack |
| 2026-08-29 | opus-worker | **自查出并修掉一个真缺陷**：首次 DSH boot 内 probe 拿到的是 `live-connecting` 空快照——`apply()` 同步返回，而我的首拉是异步的，一次性 probe 当场读完就退出，面板首屏也会空一拍。改为 spawn 模式下**构造期有界同步预热**（`execFileSync` timeout 20s，失败只降级成 diagnostics）。修后 probe 直接拿到活数据 | E-092、E-093 | — |
| 2026-08-29 | opus-worker | 5.2 完成：`npm pack` → **remove 再 add**（F-005）→ 7 个 shipped 成员 SHA256 与源码逐一相符、`mismatches=0`。**版本号全程未变**（0.0.0-pilot.2），而 `cordis.patch.yml` 的哈希在两轮 remove/add 间从 `1162357F…` 变成 `69756BB0…`——这就是 remove-再-add 真生效、且哈希核对不可省的直接证据。装法从 `link:` 换成 tgz 真拷贝 | E-093 | 5.3 |
| 2026-08-29 | opus-worker | 5.3 跑到**只差截图**：真实 DSH boot 内 headless probe 拿到活数据（`source_kind=relay-v2`、`diagnostics: []`、真实 Run `R001-relay-basic-agent-task-1-20260829` 及其三节点真实状态、`attentions: []`）；与 CLI `relay status` 的 ledger 逐字相同（H3 同一状态成立）；`dsh --profile web --port 3080` 已起、HTTP 200。**剩下的一步做不了**：Relay 页签只在有会话时可见，建会话要在界面上人手发消息（调模型、走用户额度），第三方无法用代码切 tab（DHR_49 已登记） | E-093；`evidence/dsh-live-render-ready-20260829.txt` | 停下交主控补截图 |
| 2026-08-29 | 主会话 | 5.3 截图取证完成（人手建会话激活 Relay 页签后截图）：列表视图 + 详情视图 + CLI 同态对照三份材料落 `evidence/` | E-096~E-098 | worker 登记证据账并收批 5 |
| 2026-08-29 | opus-worker | 登记 5.3 证据前**逐份看过**（不照抄描述）：列表截图确认 Relay 页签激活、头部 `relay.pilot-run-list/v1 · fixture a5cb14c4`、已完成(1) 的 `R001-relay-basic-agent-task-1-20260829` `3/3 步 0 秒`；详情截图确认三步 prepare/process-task/verify 全「已完成」、尝试次数各 1、待处理事项 (0)、更新于 `2026-08-28T16:15:06.747Z`，且 `内容`/`角色` 两列空、`前置步骤 —`——正是裁决 (a) 省键的预期留白；CLI 对照的 `updated_at` 与详情页**逐字节相同**、`source: runtime-v2` 证实非 fixture | E-096~E-098 | 留痕两条 |
| 2026-08-29 | opus-worker | 两处渲染痕迹按要求**如实留痕不美化**，并各自登记：F-020（头部 "fixture" 是 P4 客户端硬编码标签，真实来源 `runtime-v2`——读截图的人需要知道「写着 fixture，看的是活数据」）；F-021（详情 `用时 undefined`——**这条是我的投影不对称**，`elapsed_seconds` 在 run-state 里是真字段、我在列表落了详情漏了，不是「不可得」；一行可修，但改完截图就与在装版本对不上，留给小审后整改并重取详情截图） | findings F-020/F-021 | 收批 5，等小审 |
| 2026-08-29 | opus-worker | 批 5 小审 P2 三处整改（**只动 evidence/progress 记账，代码零改动**）：P2-1 E-087 由「恰 3 个树外文件」更正为 **4 个 `dsh-host` 文件**（漏记了 `cordis.patch.yml`——该行写于那次改动之前且没跟着更新），并补 `dsh-client` 未改的可复核依据（源码 vs DHR_49 期安装副本，6 文件 `mismatches=0`）；P2-2 `live-store.mjs` 哈希由手抄的旧值 `8E28EAFA…` 更正为现场复算的 `60FDF641…`，并加做**三方**（源码/tgz 内/已安装）对比，`mismatches=0`；P2-3 probe 段落重排为「命令-输出-退出码」一一对应，`--port`/`--patch` 冲突那次失败单列 3b、事实与原因都留。**根因是同一个**：转录里有手抄成分。新版转录整份由脚本现场采集生成，没有一行是抄的 | E-100；`evidence/dsh-live-render-ready-20260829.txt`（重新生成） | 停下等复裁 |
| 2026-08-29 | opus-worker | **批 5 复裁 Accept，批 5 闭合**（复核方现场独立复算三方 SHA 7/7；三条 P2 全闭）。按批 4 同样式落账：`task_plan.md` 加「批 5 已闭合」块（三步产出 + 复核链 + 承接项边界未越）；`findings.md` **F-001 → resolved**（销 `RISK-DHR30-DSH-RENDER`）、**F-008 → resolved**（as-built 增量已写）。五个施工批次至此全闭 | E-104；task_plan「批 5 已闭合」块 | 收口准备 |
| 2026-08-29 | opus-worker | 收口准备（**只填 AI 提交区与 as-built，人类签名区一个字未碰**，已用 `git diff` 核过签名区与独立复核区零改动）：review.md 填 Confidence Challenge（7 条 gap，含 `elapsed_seconds` 恒 0、聚合与 required 脱钩、events 首行竞态、全量绿看负载、H3 只到「同一状态」层、Agent 是代持记账非真跑、版本号复用靠 SHA 兜底）+ **尾巴清单 8 条待用户分流**（F-002/003/013/014/015/016/020/021）+ 契约无变化声明 + 需求对齐 4 行 + 完成条件 6 条挂证据 + 验收项元数据 11 行 + 业务化五段展示区（P5-H 四问 + 承接项，逐问写清读数与**证据局限**）+ 风险表 0 风险 | E-105；`review.md` AI 提交区 | as-built |
| 2026-08-29 | opus-worker | `as-built/relay-core.md` 增量按 F-008 承诺写明现役边界：§3.11 `workflows/basic-agent-task/`（Workflow 定义即一份合法 `relay.run/v2`，顺带是 H6 阴性对照）；§3.12 `workflow-driver`/`process-executor`（**开头即就地更正 design/07 §6 那句「不推进业务节点」的适用范围**——那是 DHR_30 的边界，接法不动唯一写者；含 realpath 落点守卫、可驱动判据、Agent 不被托管三条）；§3.10 增量 sidecar 三条边界（只做取数转发 / 归 adapters 不进 runtime / 只读） | E-105 | docs commit 后停下 |
| 2026-08-29 | opus-worker | 补收口最后两个结构登记位：①**第 4 路·一致性复核表**（禁空表）逐行写扫了什么，6 行——H6 判定两调用点共享同一 `assessRunReachability`（一致）／`E_EXECUTOR_*` 五码与场景配对（一致）／同批扫出的 outcome 映射不对称（**不一致·遗漏待修**）／活数据投影列表 vs 详情的 `elapsed_seconds`（**不一致·遗漏待修**→F-021）／`engines` package.json vs lockfile（一致）／冻结契约与指纹全卡横扫（一致）。②**返工收敛表** 8 行如实填各轮 P0/P1 出现与闭合，末行 `open P0/P1 = 0 · 已收敛` | E-108；review.md 两表 | docs commit 后彻底停下 |
| 2026-08-29 | opus-worker | **一致性复核扫出一条新的（F-022，P3）**：pi-agent「Adapter 消失」记 `outcome:failed`、dsh-agent「宿主消失」记 `outcome:orphaned`——同类事件（executor 消失、从未拿到结论）在两条路径落了不同 outcome，按 `relay.result/v2` 的分界两处都该是 `orphaned`。**reason code 配对本身没错**，错的是 outcome 映射不对称，且 `reason-codes.md` 对这两码都没规定 outcome ⇒ 是我这侧取舍不统一、非契约歧义。本轮不改（批 4 已两轮复裁闭合，且不影响 H7/H12 任一已成立命题），登记交用户分流并同步进提交区尾巴清单 | E-108；findings F-022 | — |
| 2026-08-29 | 主会话 (Fable 5) | **E11 人验确认 + 尾巴分流落账**：用户对话明文「认可，收口」（本地收口授权包生效）；AskUserQuestion 两题裁决——9 条尾巴整批按建议去处（F-003/015/016→验收池，F-013/014/020/021/022→backlog）、F-002 不回改留注记；人类签名区勾选回填、确认记录表追加一行、findings 逐条回写去处并登记 F-023 | review.md 人类签名区；findings F-023 | squash 合入 master → 合入复验四门 → verify → DevPlan/backlog/验收池回填 → 清树 |
| 2026-08-29 | 主会话 (Fable 5) | **E7 miner 备料**：`dh mine` 按派单模板委托 fresh subagent 通读 review/findings/progress，本次 miner 产出 6 条候选 → 候选区（候选-30~35：symlink 双边 realpath / 全量绿带负载条件 / 冻结契约先查消费者降级 / 对称变体逐一参数化 / 转录禁手抄 / 测试助手等不变量）；逐条标疑似重复，仅 append 未改既有。阶段汇报@收口（七段收口汇报已于上一 session E10 给出，本 session E11 确认后补压缩版） | knowledge/教训库-候选.md 候选-30~35 | worktree 提交 → squash 合入 |
| 2026-08-29 | 主会话 (Fable 5) | **E12 squash 合入 + 合入复验**：`git merge --squash wt/DHR_31` → master `52f6427`；四门复验——audit 0 违规、selftest 48/48、指纹 `a990fdda` 零漂移且 contracts/fixtures/baseline diff 全空；全量首跑 1 红（`workflow.test.mjs:498` delay 族等 attempt_failed 超时，机器 CPU 1% 空载，非负载）→ 整文件单跑 9/9 绿（64s）→ 判 **F-016 一族时序偶发的新实例位置**（workflow.test.mjs delay 族，第 3 个已知位置，命题归属验收池 ACC-2026-08-29-01）→ 复跑全量定论（见下一行） | E-110（本行）；ACC-2026-08-29-01 | 全量复跑绿证 → verify |
| 2026-08-29 | 主会话 (Fable 5) | **合入复验绿证定论**：第 2 次全量又 1 红（`workflow.test.mjs:174` settledState 30s 未追上事件账）后，停掉演示期残存的 worktree detached service（PID 32796，root=`.dh-worktrees/DHR_31`，清树前置动作）再跑第 3 次——**183/183 全绿**（133.1s），且此前超时的两条 delay 测试降到 8~9s。**F-016 新数据点**：偶发红与「存在其他存活 relay service」强相关（疑用户级 `~/.dh-relay` run 索引锁或探活链路），不只是 CPU 负载——已补进验收池 ACC-2026-08-29-01 备注。四门终值：183/183 / audit 0 违规 / selftest 48/48 / 指纹 `a990fdda` 零漂移 | E-111（本行）；task bzvxetpls 输出 | verify → 回填 SHA → 清树 |
| 2026-08-28 | opus-worker | **账本缺口交主控**：批 4 这一轮的复核**派出侧**记账不在本 worker 手上——`review.md` 尚无批 4 行，证据账本也没有对应的 `review-dispatch` 条目（批 1~3 分别是 E-021 / E-032+E-040 / E-051）。我没有替主控补写 review.md，也没有为一次不是我发起的派出编 E-0xx；本轮 Accept 的结论由主控在对话中转达，我只落自己可复核的那半边 | 本条日志 + E-079 | 主控补 review.md 批 4 两行与派出证据 |

## 证据账本 (Evidence Ledger)

<每条"完成"结论挂一条可复跑的命令 / grep / runtime 输出。不能空口说"做完了"。>
<类型枚举含 `review-dispatch`（派 agent 复核）/ `session-run`（主控本会话直跑复核）——大小写精确，只认这两个小写值。review 派出证据用 `dh dispatch` 一键落账；命令只向本标题下唯一、表头规范的账本写入，缺失/重复/畸形时 fail-closed，不追加孤儿行。>

| ID | 类型 | 命令 / 路径 | 结果 (pass/fail/observed/waived) | 支撑什么结论 |
|----|------|-----------|------|------|
| E-001 | | | | |
| E-002 | session-run | Code Scout（Explore subagent，只读 very thorough）侦察报告，全文由 scout 会话回传主会话（要点已沉淀 task_plan Context Packet 与施工步骤基点事实） | observed | S2 施工步骤的现状依据（validate stub 位置、契约冻结形状、DSH 树外配方、H6 素材） |
| E-003 | test | 主会话无沙箱：`cd relay-core; npm test`（与 codex 同树并发）+ `npm run audit` + `node tools/validate.mjs --selftest` + `node tools/capability-baseline.mjs` | partial：audit 0 违规 / selftest 48/48 / 指纹 a990fdda 零漂移 pass；npm test 一条续租时序断言红（runtime.test.mjs:514，判并发毛刺待复跑） | 批 0 基线核对（全量绿结论待树安静后 E-0xx 复跑补证） |
| E-010 | test | `cd relay-core; node --test test/reachability.test.mjs`（生产模块创建前） | fail：`ERR_MODULE_NOT_FOUND runtime/reachability.mjs`，0 pass / 1 fail | 批 1 TDD 红灯确已发生，失败原因仅为待建模块 |
| E-011 | test | `cd relay-core; node --test test/reachability.test.mjs` | pass：7/7，0 fail | H6 四例、缺失依赖、环、自环的纯函数判定转绿 |
| E-012 | test | `cd relay-core; node --test --test-name-pattern='Runtime service validate' test/service.test.mjs` | fail：RPC schema 先报 `E_BAD_VALUE`，`contract_id` 不匹配 identifier pattern；0 pass / 1 fail | F-003：任务指定的 `relay.run/v2` 无法经冻结 RPC 请求 schema 到达 validate handler |
| E-013 | baseline | `cd relay-core; node tools/capability-baseline.mjs` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…` | 本批未改冻结 contracts，能力基线零漂移 |
| E-014 | inspect | `git status --short; git diff -- relay-core/contracts relay-core/runtime relay-core/test docs/modules/dh-relay/workspace/DHR_31` | observed：contracts 无改动；仅 runtime/test 与 DHR_31 progress/findings | 硬边界未越界，阻塞现场可交接 |
| E-015 | test | `cd relay-core; node --test test/reachability.test.mjs test/service.test.mjs` | partial：本批 10 项均 pass；总计 19 pass / 1 fail，唯一失败为既有 service 首例访问 `C:\Users\nash\.dh-relay\runs.json.lock` 的沙箱 `EPERM` | 用户指定目标命令已复跑；失败符合任务说明预告的 home/system temp 沙箱假红，未追改范围外锁路径 |
| E-016 | test | `cd relay-core; node --test --test-name-pattern='H6|图校验|Runtime validate 直连接口|F-003 证据钉|Runtime service start' test/reachability.test.mjs test/service.test.mjs` | pass：10/10，0 fail | H6 四例、三类坏图、Runtime validate 两字段形状、F-003 RPC 钉、start 拒绝无目录及 golden 成功全部转绿 |
| E-017 | audit | `cd relay-core; npm run audit` | pass：未登记开口 0、厂商 token 违规 0、`$ref` 失败 0、命名/条件/meta/JSON-only 违规均 0 | Runtime/test 改动未破坏协议静态约束 |
| E-018 | test | `cd relay-core; node tools/validate.mjs --selftest` | pass：golden 17 + negative 31，合计 48/48，0 fail | 共享冻结校验器与全部既有 fixture 语义保持不变 |
| E-019 | baseline | `cd relay-core; node tools/capability-baseline.mjs`；`git diff --name-only -- relay-core/contracts` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…`；contracts diff 为空 | 冻结契约零漂移且本批未触碰 contracts |
| E-022 | test | 主会话无沙箱、树安静：`cd relay-core; npm test` | pass：161/161，0 fail（duration 134.8s） | 批 1 合入后全量回归绿；E-003 的续租时序红确证为并发毛刺（同断言本轮通过），批 0 基线核对补完 |
| E-023 | test | `cd relay-core; npm test`（reachability.test.mjs 追加进默认清单末位后） | pass：168/168，0 fail（duration 110.0s）；期间 rpc-service 一条订阅重挂断言两次偶红，经主树/工作树对照单跑均 11/11 绿，判时序毛刺非批 1 回归 | E021-01/E021-02 整改证据：默认 npm test 现含 H6 纯函数 7 条；批 1 全量绿证补齐 |
| E-020 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{findings.md,progress.md} relay-core/runtime/{reachability.mjs,service.mjs} relay-core/test/{reachability.test.mjs,service.test.mjs}`；`git commit -m "feat(dh-relay): DHR_31 批1 H6 可达性推导（运行时层）"` | fail：两步均无法创建 `.git/worktrees/DHR_31/index.lock`（Permission denied）；无路径被暂存、无 commit | F-004：提交阻塞仅为 sandbox Git 元数据权限，非实现或测试失败 |
| E-021 | review-dispatch | codex task read-only: review commit 6823196 diff (批1 H6 可达性) | observed | 复核派出：codex-fresh-readonly(批1小审)｜批1批次检查点小审，fresh 实例只看本批 diff |
| E-024 | test | `cd relay-core; node --test test/workflow.test.mjs`（生产模块创建前） | fail：`ERR_MODULE_NOT_FOUND runtime/process-executor.mjs`，0 pass / 1 fail | 批 2 TDD 红灯确已发生，失败原因仅为待建模块 |
| E-025 | test | `cd relay-core; node --test test/workflow.test.mjs` | pass：5/5，0 fail（duration 30.4s） | 闭环 5 组命题转绿：依赖序推进到 run_finished + 重放与 state.json 逐字一致；步骤非 0 退出→`E_EXECUTOR_EXIT_NONZERO` 且下游不启动、run 不 succeeded；resume 开 fresh attempt（attempt_count=2、id 不复用）；stop 中断在跑子进程→`E_EXECUTOR_KILLED`；ref 目录逃逸纯函数 9 档 + 端到端 `E_BAD_VALUE` |
| E-026 | validate | `cd relay-core; node tools/validate.mjs workflows/basic-agent-task/run.template.json --schema relay.run/v2`；`assessRunReachability(template)` | pass：`PASS` / exit 0；可达性 `{"valid":true,"reason":null}` | Workflow 定义是一份合法冻结 `relay.run/v2`，且过 H6 可达性推导（本卡的行使场景 = 阴性对照） |
| E-027 | test | `cd relay-core; npm test`（workflow.test.mjs 追加进默认清单末位后） | pass：173/173，0 fail | 批 2 全量回归绿；新增 5 条已进日常回归清单 |
| E-028 | audit | `cd relay-core; npm run audit` | pass：未登记开口 0、厂商 token 违规 0、`$ref` 失败 0、命名/条件/meta/JSON-only 违规均 0 | workflows/runtime/test 改动未破坏协议静态约束 |
| E-029 | test | `cd relay-core; node tools/validate.mjs --selftest` | pass：golden 17 + negative 31，合计 48/48，0 fail | 共享冻结校验器与全部既有 fixture 语义保持不变 |
| E-030 | baseline | `cd relay-core; node tools/capability-baseline.mjs`；`git diff --name-only -- relay-core/contracts relay-core/fixtures` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…`；contracts/fixtures diff 为空 | 硬边界未越界：冻结契约与 fixture 零漂移，本批一份基线都不必重生成 |
| E-031 | commit | `git add -- <12 个精确路径：relay-core/{package.json,runtime/{service,process-executor,workflow-driver}.mjs,test/workflow.test.mjs,workflows/basic-agent-task/**} + workspace/DHR_31/{progress,findings}.md>`；`git commit`（message 首行 `feat(dh-relay): DHR_31 批2 basic-agent-task 闭环`） | pass：commit `43be22c`，无 `add -A`；本行 SHA 由紧随其后的回填 commit 落定 | 批 2 按路径精确落账到 wt/DHR_31 |
| E-032 | review-dispatch | herdr agent start dhr31-r2 --kind codex; review diff 43be22c+a1e8cbf | observed | 复核派出：codex-r2-readonly(批2小审·herdr交互终端)｜批2批次检查点小审，fresh 只读；F-007 fail-soft 判据专项 |
| E-033 | test | 变异核验：把 `resolveStepEntry` 的 `escapesRoot(realRoot, realTarget)` 短路成 `false` 后 `node --test --test-name-pattern='F-009' test/workflow.test.mjs`；随即改回 | fail（如期）：`AssertionError: 真实落点守卫必须拒绝经链接的逃逸`，0 pass / 1 fail | F-009 新增回归**非空转**：守卫一没就红。注：断言在单元档先红，端到端的「仓外脚本 marker 从未生成」那半条被短路未走到，两半走的是同一段守卫代码 |
| E-034 | test | `cd relay-core; node --test test/workflow.test.mjs` | pass：7/7，0 fail，0 skipped（duration 44.8s）；打印 `[F-009] 链接逃逸回归以 目录 junction 形态执行` | F-009/F-010 两组新回归转绿，且 F-009 在本机**实际执行**而非 skip |
| E-035 | test | `cd relay-core; npm test` | pass：175/175，0 fail（duration 108.8s） | 整改后全量回归绿；新增 2 条已进日常回归清单（workflow.test.mjs 已在默认清单内） |
| E-036 | audit | `cd relay-core; npm run audit` | pass：未登记开口 0、厂商 token 违规 0、`$ref` 失败 0、结构位置 token 未登记 0、白名单↔OPEN-POINTS 不同步 0 | 整改未破坏协议静态约束 |
| E-037 | test | `cd relay-core; node tools/validate.mjs --selftest` | pass：合计 48/48，0 fail | 共享冻结校验器与全部既有 fixture 语义保持不变 |
| E-038 | baseline | `cd relay-core; node tools/capability-baseline.mjs`；`git diff --name-only -- relay-core/contracts relay-core/fixtures` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…`；contracts/fixtures diff 为空 | 整改仍未越硬边界：冻结契约与 fixture 零漂移 |
| E-039 | commit | `git add -- relay-core/runtime/{process-executor,workflow-driver}.mjs relay-core/test/workflow.test.mjs docs/modules/dh-relay/workspace/DHR_31/{progress,findings}.md`；`git commit`（message 首行 `fix(dh-relay): DHR_31 批2 整改`） | pass：commit `b085e7f`，无 `add -A`；未碰 `review.md`（属主控）。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 2 小审整改按路径精确落账到 wt/DHR_31 |
| E-040 | review-dispatch | herdr agent prompt dhr31-r2 <批2整改定向复审> | observed | 复核派出：codex gpt-5.6-terra high (herdr w1:p3 dhr31-r2)｜批2整改定向复审：确认 F-009/F-010 收敛，复审 commit b085e7f+0aeeba4 |
| E-041 | test | `cd relay-core; node --test test/e2e-basic-agent-task.test.mjs` | pass：1/1，0 fail（duration 11.1s） | 3.1 达成：CLI 进程级 `start`→`events`→`status`/`inspect` 走到 succeeded；events 补发 seq 无缺口、三节点依赖序、run_finished 唯一；三个视图的 text 逐字等于 `--json` 模型经 `cli/render.mjs` 的渲染；status/inspect 的 `state_signature` 同源 |
| E-042 | test | `cd relay-core; node --test test/workflow.test.mjs` | pass：9/9，0 fail（duration 62.1s） | 延时旋钮两组新回归：`delay_ms=1500` 时**事件账里** attempt_started→attempt_succeeded 时间差 ≥1500ms 且结果载荷带 `delay_ms`、被校验字段不受影响、run 仍 succeeded；label 值非法（`soon`）→ `attempt_failed`/`E_EXECUTOR_EXIT_NONZERO`，不得当 0 静默跑完 |
| E-043 | validate | cwd=worktree 根：`node relay-core/workflows/basic-agent-task/make-run.mjs --out <tmp> --delay-ms 35000`（及不带 `--delay-ms` 的形态）；各自 `node relay-core/tools/validate.mjs <tmp> --schema relay.run/v2` + `assessRunReachability` | pass：两种形态均 `PASS`；ref 前缀自动推为 `relay-core/workflows/basic-agent-task/steps/*.mjs`；带延时时 `labels=[{basic-agent-task.delay_ms,"35000"}]`、不带时无 labels；可达性均 `{"valid":true,"reason":null}` | 交给主控做 3.2 的命令是**实跑验证过**的，不是照着写的 |
| E-044 | test | `cd relay-core; npm test`（两个新测试文件已入默认清单） | pass：178/178，0 fail | 批 3 · 3.1 全量回归绿 |
| E-045 | audit | `cd relay-core; npm run audit` | pass：未登记开口 0、厂商 token 违规 0、`$ref` 失败 0、结构位置 token 未登记 0、白名单↔OPEN-POINTS 不同步 0 | 本批改动未破坏协议静态约束 |
| E-046 | test | `cd relay-core; node tools/validate.mjs --selftest` | pass：合计 48/48，0 fail | 共享冻结校验器与全部既有 fixture 语义保持不变 |
| E-047 | baseline | `cd relay-core; node tools/capability-baseline.mjs`；`git diff --name-only -- relay-core/contracts relay-core/fixtures` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…`；contracts/fixtures diff 为空 | 硬边界未越界：冻结契约与 fixture 零漂移 |
| E-048 | commit | `git add -- relay-core/{package.json,runtime/workflow-driver.mjs,test/{workflow,e2e-basic-agent-task}.test.mjs,workflows/basic-agent-task/{README.md,make-run.mjs,steps/process-task.mjs}} docs/modules/dh-relay/workspace/DHR_31/progress.md`；`git commit`（message 首行 `feat(dh-relay): DHR_31 批3 3.1 CLI 端到端 + 3.2 延时旋钮备料`） | pass：commit `a6ac36c`，无 `add -A`。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 3 · 3.1 按路径精确落账到 wt/DHR_31 |
| E-049 | evidence | `evidence/e2e-disconnect-20260828.txt`（主控经 herdr 编排两个独立真实 PowerShell 终端：w1:p4 起 180s 延时 run → `herdr pane close` 整终端杀掉 → w1:p5 全新终端重连；7 命令全 exit=0，自检行齐） | observed | 3.2 达成：终端 A 死后 service 存活（host alive 且 **epoch:1 全程未换代**）、run 在无客户端期间自行推进到 succeeded、事件账 seq 1~12 连续无缺口、断连前后 state_signature 一致 → 「关终端 Runtime 继续、重连一致」的需求境证据（非测试内存态：service 由 CLI detached 拉起，业务仓为全新 scratch git 仓） |
| E-050 | observed | 耗时读数（全部以事件账/launch-receipt 时间戳为准，主控终端侧时间戳仅定命令发出时刻）：R001 冷启动=CLI 发出 09:33:09.0 → lease_acquired 09:33:16.742 ≈ **7.7s**（含 service 冷拉起）；R002 warm start=09:35:11.4 → receipt 09:35:14.286 ≈ **2.9s**；终端 B 重连查询（独立 CLI 进程）≈ **2s** 内返回；延时保真：R001 process-task 35.762s ≥ 35000ms、R002 180.337s ≥ 180000ms（attempt_started→attempt_succeeded）；断连时点 09:36:05.648（process-task 延时剩 ~129s） | observed | P5-H 人判材料：启动/恢复耗时读数 + 延时旋钮账面保真；R001（35s 形态）同时证明延时旋钮生效（第一次实录因编排延迟错过断连窗口，run 在杀终端前跑完——改用 180s 形态重跑，两条 run 均留在 `relay list` 对照） |
| E-051 | review-dispatch | herdr agent prompt dhr31-r3 <批3小审> | observed | 复核派出：codex gpt-5.6-terra high (herdr w1:p3 dhr31-r3, fresh 实例)｜批3小审：commit a6ac36c(3.1)+3c3c7be(3.2 实录)，重点=实录真实性（service 真 detached 非测试内存态） |
| E-052 | evidence | `evidence/e2e-disconnect-20260828-events.jsonl.txt`（R001/R002 `relay events --json` 原样输出 + run store events.jsonl 首行/行数；采集于实录后同一 scratch 仓，service 已死、CLI 直读 store） | observed | 批 3 小审 P2 整改（耗时读数可复算：帧带 `at`，R001 35.762s / R002 180.337s）+ P1 澄清（seq:0 `run_created` 计入 status events 行数但不进 CLI 列表 → 断连前 events:8=seq0~7 自洽，F-011） |
| E-053 | test | `cd relay-core; node --test test/agent-node.test.mjs` | pass：4/4，0 fail（duration 17.1s） | 批 4 三条命题（H7 最小失败单元 / H12 fresh attempt / Agent 不推翻 Process）+ 「driver 不托管 agent 节点」边界钉全部转绿 |
| E-054 | test | 全量首跑（含批 4）：`cd relay-core; npm test` | fail：182 中 3 红——`CLI pending 50 路并发`、`host 真实续租`（两条既有负载毛刺）+ `步骤非 0 退出`（**我引入的测试竞态**：期望 run_status=failed 实得 running） | 暴露 batch2 起测试轮询事件账后直读 state.json 的读序错误；产品无缺陷 |
| E-055 | test | 修复后定向复跑：`node --test test/workflow.test.mjs test/agent-node.test.mjs test/e2e-basic-agent-task.test.mjs` × 3 轮 | pass：每轮 14/14，0 fail | `settledState` 修复有效；三文件并发不再复现该竞态 |
| E-056 | observed | `node -e` 直接喂造好的事件序列给 `store/state.mjs` 的 `replayRun` | observed：必经全绿+可选节点 pending → `run_status=pending`；可选节点 failed → `run_status=failed`、`progress={done:1,total:2}` | F-013/F-014 的原始观测（`aggregate()` 不看 required），已按现状钉进批4③ 断言 |
| E-057 | test | 空载机器：`cd relay-core; npm test` | pass：**182/182**，0 fail（duration 108.1s） | 批 4 全量回归绿（另见 F-016：满载时该结论不稳定） |
| E-058 | audit | `cd relay-core; npm run audit` | pass：未登记开口 0、`$ref` 失败 0、结构位置 token 未登记 0、白名单↔OPEN-POINTS 不同步 0 | 本批未破坏协议静态约束 |
| E-059 | test | `cd relay-core; node tools/validate.mjs --selftest` | pass：合计 48/48，0 fail | 冻结校验器与既有 fixture 语义不变 |
| E-060 | baseline | `cd relay-core; node tools/capability-baseline.mjs`；`git diff --name-only -- relay-core/contracts relay-core/fixtures relay-core/capability-baseline.json` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…`；三者 diff 全空 | 窄路径成立的硬证据：没托管 agent executor，所以 `executor_kinds:["process"]` 不必改 |
| E-061 | commit | `git add -- relay-core/{package.json,test/agent-node.test.mjs,test/helpers/settled-state.mjs,test/workflow.test.mjs,test/e2e-basic-agent-task.test.mjs} docs/modules/dh-relay/workspace/DHR_31/{progress,findings}.md`；`git commit`（message 首行 `feat(dh-relay): DHR_31 批4 Agent 节点窄路径`） | pass：commit `2e9faf4`，无 `add -A`。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 4 按路径精确落账到 wt/DHR_31 |
| E-062 | test | `cd relay-core; npm test`（脚本已加 `--test-concurrency=4`） | pass：**182/182**，0 fail（duration 92.2s，当前机器非空载） | F-016 整改证据：并发设上限后全量稳定绿且更快；`service.test.mjs:73` 的 8s 预算原样未动 |
| E-063 | commit | `git add -- relay-core/package.json docs/modules/dh-relay/workspace/DHR_31/{progress,findings}.md`；`git commit`（message 首行 `fix(dh-relay): DHR_31 F-016 整改`） | pass：commit `959d9ad`，无 `add -A`。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | F-016 整改按路径精确落账 |
| E-064 | test | `cd relay-core; node --test test/agent-node.test.mjs` | pass：5/5，0 fail | P1-1 整改：边界钉同时覆盖 pi-agent 与 dsh-agent；新增 ①bis dsh 同形（orphaned + `E_EXECUTOR_ORPHANED` + fresh retry + 他节点工件逐字节不变） |
| E-065 | inspect | `git diff -- relay-core/package.json relay-core/package-lock.json`；`node -e` 比对两处 engines | pass：diff 恰 2 行（各文件 1 行），`package.json` 与 lockfile `packages[""].engines` 均为 `>=18.19.0`、逐字一致；依赖零改动 | P1-2 整改：`--test-concurrency` 的最低 Node 版与 engines 声明对齐 |
| E-066 | inspect | `git -C <主检出> status --short -- docs`；`ls docs/modules/dh-relay/workspace/DHR_31/evidence`（主检出） | observed：主树 docs 无改动、该 evidence 目录在主检出**不存在** | 跑偏自查：cwd 漂移期间的 `mkdir -p ../docs/...` 未在主检出留下游离目录 |
| E-067 | test | `cd relay-core; npm test`（`--test-concurrency=4`） | pass：**183/183**，0 fail，`EXIT=0`（duration 101.1s，当前机器非空载）；原始 TAP 转录落仓 `evidence/npm-test-20260828-batch4-fix.tap.txt`（203 行，未删改） | 批 4 小审整改后的全量绿证（小审 P2：保留原始输出） |
| E-068 | audit | `cd relay-core; npm run audit` | pass：未登记开口 0、`$ref` 失败 0、结构位置 token 未登记 0、白名单↔OPEN-POINTS 不同步 0 | 整改未破坏协议静态约束 |
| E-069 | test | `cd relay-core; node tools/validate.mjs --selftest` | pass：合计 48/48，0 fail | 冻结校验器与既有 fixture 语义不变 |
| E-070 | baseline | `cd relay-core; node tools/capability-baseline.mjs`；`git diff --name-only -- relay-core/contracts relay-core/fixtures relay-core/capability-baseline.json` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…`；三者 diff 全空 | 窄路径未被整改破坏：仍未托管任何 agent executor |
| E-071 | commit | `git add -- relay-core/{package.json,package-lock.json,test/agent-node.test.mjs} docs/modules/dh-relay/workspace/DHR_31/{progress.md,findings.md,evidence/npm-test-20260828-batch4-fix.tap.txt}`；`git commit`（message 首行 `fix(dh-relay): DHR_31 批4 小审整改`） | pass：commit `95477dd`，无 `add -A`。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 4 小审整改按路径精确落账 |
| E-072 | test | `cd relay-core; node --test test/agent-node.test.mjs` | pass：5/5，0 fail | P1-1 尾巴闭合：`holderReports` 必传 `executorKind`；dsh 的 orphan 与 fresh-success 两条**结果工件**均断言 `executor_kind="dsh-agent"`，并钉住事件里 `executor_kind` 恒 null（冻结行为）；①bis 改用 `E_EXECUTOR_HOST_LOST` |
| E-073 | test | 变异核验：把 `holderReports` 的 `executor_kind` 改回硬编码 `'pi-agent'` → `node --test --test-name-pattern='①bis'`；随即改回 | fail（如期）：`AssertionError: dsh 的孤儿结论必须记成 dsh-agent，不得沿用 pi-agent`，0 pass / 1 fail | 新增断言非空转：正是复裁点出的那个缺陷，去掉修复即红 |
| E-074 | test | `cd relay-core; npm test` | pass：**183/183**，0 fail，`EXIT=0`（duration 104.8s，当前机器非空载）；原始 TAP 落仓 `evidence/npm-test-20260828-batch4-rejudge.tap.txt`（204 行，未删改） | 复裁整改后的全量绿证 |
| E-075 | audit | `cd relay-core; npm run audit` | pass：未登记开口 0、`$ref` 失败 0、结构位置 token 未登记 0、白名单↔OPEN-POINTS 不同步 0 | 整改未破坏协议静态约束 |
| E-076 | test | `cd relay-core; node tools/validate.mjs --selftest` | pass：合计 48/48，0 fail | 冻结校验器与既有 fixture 语义不变 |
| E-077 | baseline | `cd relay-core; node tools/capability-baseline.mjs`；`git diff --name-only -- relay-core/contracts relay-core/fixtures relay-core/capability-baseline.json` | pass：10 份 digest 相符，`capability_hash=a990fddad486f669…`；三者 diff 全空 | **`E_EXECUTOR_HOST_LOST` 首用授权的硬证据**：它是冻结集里既有的码，首用不新增、不改 contracts、指纹零漂移 |
| E-078 | commit | `git add -- relay-core/test/agent-node.test.mjs docs/modules/dh-relay/workspace/DHR_31/{progress.md,findings.md,evidence/npm-test-20260828-batch4-rejudge.tap.txt}`；`git commit`（message 首行 `fix(dh-relay): DHR_31 批4 复裁整改`） | pass：commit `e6f8a3d`，无 `add -A`。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 4 复裁整改按路径精确落账 |
| E-079 | observed | 批 4 第二轮复裁结论（dhr31-r3，主控对话转达）：**Accept，批 4 可闭合**；附注「仅放行批 4，F-016 保持 open·P2，整卡收口另行处理」。可复核的支撑面：整改链 `2e9faf4`（批 4 主体）→ `95477dd`（小审 P1-1/P1-2）→ `e6f8a3d`（复裁三件）；绿证 E-074~E-077；原始 TAP `evidence/npm-test-20260828-batch4-rejudge.tap.txt` | observed | 批 4 闭合的结论性记录。**注意**：本行只记「结论已转达」这一事实——复核派出侧的一手证据（review.md 批 4 行 + `review-dispatch` 条目）由主控落，不在本 worker 手上，未代写、未代编号 |
| E-080 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{task_plan.md,findings.md,progress.md}`；`git commit`（message 首行 `docs(dh-relay): DHR_31 批4 复裁 Accept，批 4 闭合`） | pass：commit `042158c`，无 `add -A`；纯 docs 提交，零代码改动。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 4 闭合落账 |
| E-081 | review-dispatch | herdr agent prompt dhr31-r3 <批4小审：2e9faf4+959d9ad+回填> | observed | 复核派出：dhr31-r3(codex gpt-5.6-terra high, herdr w1:p3)｜批4小审派出（主控 dh-relay-19）；结论 Request changes：P1x2（dsh-agent 覆盖缺失/engines 冲突）+P2x2 |
| E-082 | review-dispatch | herdr agent prompt dhr31-r3 <批4复裁轮1: 95477dd+97da880 / 轮2: e6f8a3d+35ab3cb> | observed | 复核派出：dhr31-r3(codex gpt-5.6-terra high, herdr w1:p3, 同实例复裁)｜批4复裁两轮派出（主控 dh-relay-19）；轮1 request-changes（P1-1尾巴 executor_kind 硬编码），轮2 Accept 批4可闭合（变异验证/指纹零漂移） |
| E-083 | inspect | `powershell docs/modules/dh-relay/workspace/DHR_26/check-drift.ps1`（5.1 规定的改动前基线核对） | pass：`IN SYNC (content): 27 files across 2 package(s)`，`EXITCODE=0`；24 个 `line endings only` NOTE（`core.autocrlf`，脚本设计上不失败） | 树外镜像与落地副本一致——批 5 开工前基线干净，后续任何 diff 都可归因到本轮 |
| E-084 | inspect | 字段差集实测：`contracts/relay.rpc.v1.schema.json` 方法枚举；`relay.run-state.v1.schema.json` `$defs.node_state`；`runtime/service.mjs:601`；`grep -c` 统计 `dsh-client/lib` 对 pilot 字段的使用点 | observed：RPC 方法全集 `["contracts","listRuns","inspectRun","validate","start","control","subscribe"]`——**无运行文档读取**；`node_state` 仅 `node_id/status/current_attempt_id/attempt_count`；面板使用 `attentions`×15、`depends_on`×8、`node_status`×6、`workflow_name`×5、`trigger_by`×2、`elapsed_seconds`×2、`current_node_title`×2、`attention_count`×2、`max_attention_severity`×2、`top_attention_summary`×2、`log_locator`×1、`.title`×1、`.role`×1；`dsh-client/lib/client.js:118` 对 `attentions` 是**校验非容错** | F-018 的事实底座：不是「难做」，是活数据源里没有这些字段、且四条取得方式各自越界 |
| E-085 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{progress.md,findings.md}`；`git commit`（message 首行 `docs(dh-relay): DHR_31 批5 5.1 停工登记 F-018`） | pass：commit `675a37a`，无 `add -A`；**纯 docs，零代码、零树外改动**。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 5 停工落账 |
| E-085b | inspect | 5.1 改动前重跑 `check-drift.ps1` | pass：`IN SYNC (content): 27 files across 2 package(s)`，exit 0 | 本轮改动前基线仍干净，后续 drift 可全部归因到本轮 |
| E-086 | test | `node <scratchpad>/live-probe.mjs`（注入 `connectDshBridge`，接本仓真实 service 与真实 Run） | pass：**ALL PASS**，exit 0；转录落 `evidence/live-projection-20260829.txt` | 5.1 验收：五方法返回活数据；run_id/run_status/updated_at/节点数/逐节点状态与 Bridge Read Model 逐字一致；`attentions: []`；**未编造 title/role/depends_on/workflow_name** 四条反向断言全绿 |
| E-087 | inspect | `check-drift.ps1`（改动后）；`find <树外> -newermt`；**（批 5 小审 P2-1 更正后补）**`Get-FileHash` 比对 `src/dsh-client/*` 与 DHR_49 期就装好的面板副本 | observed：**树外改动为 4 个 `dsh-host` 文件**——`index.mjs`、`live-store.mjs`、`package.json`、`cordis.patch.yml`；`dsh-client` 6 个文件与安装副本逐字节相同（`mismatches=0`）⇒ 面板本卡未改；check-drift 报 DRIFT/exit 1 属**预期**（DHR_26 镜像是那张卡的基线、本卡不回写） | 树外改动面 = 4 个文件且全在 `dsh-host`，未越出承接项边界。**本行原记「恰 3 项」是错的**：它写于 `cordis.patch.yml` 改动之前、此后没跟着更新，被批 5 小审 P2-1 抓出。`dsh-client` 未改的可复核依据见 `evidence/dsh-live-render-ready-20260829.txt` 第 5 段 |
| E-088 | test | `cd <树外>/src/dsh-host; npm test` | pass：41/41，0 fail | 树外插件自测全绿——含它自己的**安装拓扑契约**用例（正是这条挡下了计算动态 import，见 F-019）与 `files/exports` 声明完整性用例（它抓出我漏declare `live-store.mjs`，否则 tarball 会缺文件） |
| E-089 | test | 本仓四门（批 5 未动 relay-core，取核对证据）：`git status -- relay-core`；`npm test`；`npm run audit`；`node tools/validate.mjs --selftest`；`node tools/capability-baseline.mjs` | pass：relay-core 工作树零改动；183/183，0 fail（130.5s）；audit 0 违规；selftest 48/48；`capability_hash=a990fddad486f669…` 零漂移；contracts/fixtures/baseline diff 全空 | 批 5 迄今**没有触碰本仓一行代码**，硬边界完好 |
| E-090 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{progress.md,findings.md,evidence/**}`；`git commit`（message 首行 `docs(dh-relay): DHR_31 批5 5.1 活数据投影落地 + F-019 停工`） | pass：commit `2a72b84`，无 `add -A`；本仓仅 docs/evidence，代码零改动。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 5·5.1 落账 |
| E-091 | test | `cd relay-core; node --test test/control-plane-imports.test.mjs`；`node adapters/dsh-bridge/snapshot-main.mjs --repo <worktree>` | pass：两条钉子 2/2 绿；sidecar 对真实 service 吐出 `relay.client-read-model/v1` + `relay.run-state/v1`，exit 0 | 新增 sidecar 未破控制面 import 纪律，且取数即通 |
| E-092 | test | `cd <树外>/src/dsh-host; npm test`（含该包**安装拓扑契约**用例） | pass：41/41，0 fail | 插件改 spawn 后契约仍满足：无裸说明符、无 `require`、无计算动态 `import()`；`files/exports` 完整（`live-store.mjs` 已声明） |
| E-093 | inspect | 真实 DSH：`npm pack` → `dsh plugin --profile web remove/add` → **三方 SHA256（源码 / tgz 内 / 已安装）** → `dsh --profile web --patch <installed>/probe.patch.yml` → `relay status --json` → web HTTP | pass：`three-way mismatches=0`（版本号未变而 `cordis.patch.yml` 哈希在两轮间改变，证明 remove/add 生效）；probe exit=0 且 `source_kind=relay-v2`/`diagnostics:[]`/真实 Run 与三节点真实状态/`attentions:[]`；与 CLI ledger 逐字相同；web HTTP 200 | 5.2 全部 + 5.3「只差截图」。**转录已按批 5 小审 P2-2/P2-3 重新生成**：①原记 `live-store.mjs = 8E28EAFA…` 是**手抄旧表**（加同步预热之前那版）的错值，真值 `60FDF641…`，新版由脚本现场三方复算写入、无一行手抄；②原版 probe 段命令与退出码错位（一条 `[exit=0]` 后紧跟 `[exit=1]`、成功 JSON 落在别处），新版按「命令-输出-退出码」一一对应重排，并把 `--port`/`--patch` 冲突那次失败单列为 3b、保留事实与原因 |
| E-094 | test | 本仓四门（含新增 sidecar）：`npm test`；`npm run audit`；`node tools/validate.mjs --selftest`；`node tools/capability-baseline.mjs` | pass：183/183，0 fail（96.3s，EXIT=0）；audit 0 违规；selftest 48/48；`capability_hash=a990fddad486f669…` 零漂移；contracts/fixtures/baseline diff 全空；本仓改动仅新增 `adapters/dsh-bridge/snapshot-main.mjs` 一个文件 | 硬边界完好。（前一轮曾 182/183，唯一红是 `dsh-bridge.test.mjs` 的 `EBUSY rmdir` 清理毛刺——我当时并发跑着 DSH，属 F-016 那一类，复跑全绿） |
| E-095 | commit | `git add -- relay-core/adapters/dsh-bridge/snapshot-main.mjs docs/modules/dh-relay/workspace/DHR_31/{progress.md,findings.md,evidence/**}`；`git commit`（message 首行 `feat(dh-relay): DHR_31 批5 DSH 附加客户端活数据`） | pass：commit `06f38af`，无 `add -A`。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 5·5.1/5.2/5.3(只差截图) 落账 |
| E-096 | inspect | `evidence/dsh-render-20260829-run-list.png`（主控取证，worker 逐份查看核对） | observed：Relay 页签激活；头部 `Relay 1 条 · relay.pilot-run-list/v1 · fixture a5cb14c4`；分组「已完成 (1)」含 `R001-relay-basic-agent-task-1-20260829`，`3/3 步  0 秒` | H3 前半：DSH 作为**附加客户端**在真实浏览器里显示出本仓真实 Run。`0 秒` = 列表投影落了真字段 `elapsed_seconds`（当前实现恒 0）；头部 "fixture" 字样见 F-020 |
| E-097 | inspect | `evidence/dsh-render-20260829-run-detail.png`（同上） | observed：`已完成 R001-relay-basic-agent-task-1-20260829`；`更新于 2026-08-28T16:15:06.747Z`；接力计划 (3 步) = prepare / process-task / verify，状态均「已完成」、尝试次数各 1、前置步骤 `—`；`内容`/`角色` 两列空；待处理事项 (0)「(无)」；`用时 undefined` | 详情侧同一状态。空列与 `—` 是裁决 (a) 省键的**预期留白**（title/role/depends_on 确实取不到）；`用时 undefined` 是我的投影不对称，见 F-021 |
| E-098 | inspect | `evidence/cli-status-20260829-parity.json.txt`（CLI `status --json` 同态对照） | pass：`run_id` 同、`run_status: succeeded`、`group: done`、`progress {done:3,total:3}`、`updated_at: 2026-08-28T16:15:06.747Z` 与详情截图**逐字节相同**；`source: runtime-v2` / `host: alive` | **H3 成立的关键一条**：附加客户端与 CLI 看到的是同一状态，且 `source=runtime-v2` 证实屏上是活数据而非 fixture |
| E-099 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{progress.md,findings.md,evidence/{dsh-render-20260829-run-list.png,dsh-render-20260829-run-detail.png,cli-status-20260829-parity.json.txt}}`；`git commit`（message 首行 `docs(dh-relay): DHR_31 批5 5.3 截图取证落账`） | pass：commit `9f533e3`，无 `add -A`；纯 docs/evidence，代码零改动。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 5 三步齐，落账收口 |
| E-100 | inspect | 批 5 小审 P2 整改复核：`tar -xzf <tgz>` 后三方 `Get-FileHash`（源码/tgz 内/已安装，7 成员）；`Get-FileHash` 比对 `dsh-client` 6 文件与 DHR_49 期安装副本；重跑 probe 与 CLI status 并由脚本直接写入转录 | pass：`three-way mismatches=0`（`live-store.mjs=60FDF641…`）；`dsh-client source-vs-installed mismatches=0`；probe exit=0、CLI exit=0、web HTTP 200 | P2-1/P2-2/P2-3 三处证据缺陷已闭合；新版转录零手抄，`8E28EAFA…` 仅作为「原版错值」出现在更正说明里 |
| E-101 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{progress.md,evidence/dsh-live-render-ready-20260829.txt}`；`git commit`（message 首行 `docs(dh-relay): DHR_31 批5 小审 P2 证据链整改`） | pass：commit `4dcc6bc`，无 `add -A`；纯 docs/evidence，代码零改动。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 5 小审 P2 整改落账 |
| E-104 | observed | 批 5 复裁结论（dhr31-r3，主控转达）：**Accept，批 5 闭合**；三条 P2 全闭、复核方现场独立复算三方 SHA 7/7；F-020/F-021 P3 open 不阻塞、F-016 P2 独立状态不变。派出侧记账由主控补于 `773cae5`（review.md 批 5 两行 + E-102/E-103） | observed | 批 5 闭合的结论性记录；本行只记「结论已转达」，一手派出证据在主控侧 |
| E-105 | inspect | 收口准备产出：`review.md` AI 提交区全填（Confidence Challenge 7 条 + 尾巴 8 条 + 需求对齐 4 行 + 完成条件 6 条 + 元数据 11 行 + 五段展示区 + 0 风险表 + 两个自检项）；`as-built/relay-core.md` 新增 §3.11/§3.12/§3.10 增量；`git diff` 核验**人类签名区与独立复核区零改动** | pass | 收口材料就位，待用户人验；第二轮增量复核由主控另派 fresh 实例 |
| E-106 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{task_plan.md,findings.md,progress.md,review.md} docs/modules/dh-relay/as-built/relay-core.md`；`git commit`（message 首行 `docs(dh-relay): DHR_31 批5 闭合 + 收口准备`） | pass：commit `f3b1e70`，无 `add -A`；纯 docs，代码零改动。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 批 5 闭合与收口准备落账 |
| E-108 | inspect | 收口结构登记位补齐：`review.md` 第 4 路一致性复核表 6 行（逐行列扫描对象/同类路径/是否一致/裁决/派出证据，枚举值按表头锁定）+ 返工收敛表 8 行（批 1~批 5 各轮 + 轮 2 增量 + 收口时点）；据此新登记 findings F-022 | pass：两表均非空、枚举值合法；两条 `不一致` 均给 `遗漏待修` 且在 findings 有对应条目（F-021、F-022）；末行 `open P0/P1 = 0 · 已收敛` | 收口审的结构缺口补齐（dh-check 拦空表）；一致性复核**真扫出一条新问题**而非走过场 |
| E-109 | commit | `git add -- docs/modules/dh-relay/workspace/DHR_31/{review.md,findings.md,progress.md}`；`git commit`（message 首行 `docs(dh-relay): DHR_31 补一致性复核表与返工收敛表`） | pass：commit `63772ef`，无 `add -A`；纯 docs，代码零改动。SHA 由紧随其后的回填 commit 落定——本行写就时 commit 尚未产生，不预填串 | 两个结构登记位落账 |
| E-102 | review-dispatch | herdr agent prompt dhr31-r3 <批5小审: 2a72b84+06f38af+9f533e3+回填> | observed | 复核派出：dhr31-r3(codex gpt-5.6-terra high, herdr w1:p3)｜批5小审派出（主控 dh-relay-19）；结论 request-changes 仅P2x3（证据链：E-087文件数/E-093 SHA/probe转录分段），代码与边界全放行 |
| E-103 | review-dispatch | herdr agent prompt dhr31-r3 <批5复裁: 4dcc6bc+c6e4c08> | observed | 复核派出：dhr31-r3(codex gpt-5.6-terra high, herdr w1:p3, 同实例复裁)｜批5复裁派出（主控 dh-relay-19）；Accept 批5可闭合，三方SHA现场独立复算7/7一致，F-020/F-021 P3 open不阻塞 |
| E-107 | review-dispatch | herdr agent start dhr31-r4 --kind codex -- --sandbox read-only; herdr agent prompt dhr31-r4 <轮2增量复核> | observed | 复核派出：dhr31-r4(fresh codex 只读实例, gpt-5.6-terra high, 新pane新会话, 未参与实施且≠第一轮r1/r2/r3会话)｜第二轮增量复核派出（主控 dh-relay-19）：核全程+各批小审记录+收口增量 diff+需求路/教训路 |
