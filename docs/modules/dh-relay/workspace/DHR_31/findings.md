<!-- findings.md — 问题清单。🟢 边做边记。 -->
# findings — DHR_31 basic-agent-task 端到端闭环

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|----|------|------|------|------|------|
| F-001 | P2 | 承接 DHR_30 移交风险项 RISK-DHR30-DSH-RENDER：真实 DSH 渲染 Bridge 活数据未证（含截图取证义务），随本卡 H3/P5-X 附加客户端项执行 | DHR_30 verify `93df648` Risk-Refs；P5 DevPlan §3.2 DHR_31 验收口径（登记位原文在 DHR_30 review.md:85/92/95，findings.md 无该串） | 本卡 DSH 附加客户端项完成真实渲染 + 截图后关闭。**已闭**：批 5 完成——数据源改经 `connectDshBridge`（经 sidecar）取活数据、repack + remove/add + 三方 SHA256 7/7、真实浏览器截图两张（列表 + 详情）+ CLI 同态对照（`updated_at` 逐字节相同、`source=runtime-v2` 证实非 fixture）。证据 E-086/E-093/E-096~E-098/E-100；批 5 复裁 Accept | resolved |
| F-002 | P3 | DevPlan DHR_31 卡面与 brief 条件 1 引用 design/06 H5，但 `HC-CTRL-H5` 已被 design/records/01:34 标 superseded（由 HC-3AT-A10/H4 取代，「不得再引用『无人在线自动跨节点』」）；design/06 整体是冻结历史材料（dev_plan/README:7）。**方向不受影响**：条件 1 的实质由 P5-M5（DevPlan §4.1，用户确认的机器闸）+ HC-CTRL-H1/H2（design/10 §10 表 retained·当前）承载，dev_plan/README「控制面硬约束」#3 仍保留自动节点继续语义。 | design/records/01:34；design/10:684-694；dev_plan/README:7、:80 | 收口时在 review 验收项元数据的等价判据里写明 DHR31-M1 以 P5-M5+H1/H2 为准、H5 引用作历史注记；DevPlan 卡面文字是否回改留给用户裁决（尾巴分流）。**已裁决（F-023①②）**：不回改，DevPlan 状态行留 H5 已 superseded 注记 | resolved |
| F-003 | P2（原 P1，用户裁决后降级） | 冻结 RPC 接口与冻结 schema 冲突：`validate_params.contract_id` 引用 `identifier`，其 pattern `^[A-Za-z0-9][A-Za-z0-9._:-]{0,127}$` 禁止 `/`，故 RPC 层永远无法表达 `relay.run/v2` 这类协议 ID——请求进 handler 前即被信封拒为 `E_BAD_VALUE`。属 DHR_52 冻结期遗留疤痕，非本卡引入。 | `node --test --test-name-pattern='Runtime service validate' test/service.test.mjs`：`data/params/contract_id must match pattern ...`；`contracts/relay.rpc-methods.v1.schema.json:62`；`contracts/_shared/relay.common.v1.schema.json:9`；主会话独立核实（grep 契约原文） | **用户 2026-08-28 对话 AskUserQuestion 点选裁决：运行时层证明、疤入尾巴**——条件 3 四例改经 runtime 层 validate 接口 + start fail-closed 证明（卡面落点本就是 runtime 的 `validate(request)`，RPC 只是传输）；不动冻结契约、不扩范围；本疤痕收口时随尾巴分流入验收池，留后续契约批次修。**已分流（F-023①）**：入项目验收池 | resolved（已入验收池） |
| F-004 | P1 | 批 1 实现与验证已完成，但本 codex sandbox 对主仓 Git worktree 管理目录只有读权限；精确 `git add` 无法创建 `.git/worktrees/DHR_31/index.lock`，因此未暂存、未生成要求的 commit。代码工作树文件均已正常落盘。 | `git add -- <6 个精确允许路径>` 与随后 `git commit` 均报 `fatal: Unable to create 'D:/MyFiles/ai-workflow/dh-relay/.git/worktrees/DHR_31/index.lock': Permission denied`；`git status --short` 显示 6 个预期路径仍未暂存 | 主会话已代打提交：同 6 路径精确暂存，commit `6823196`（无 add -A）；后续批次改派 Opus subagent 施工（用户 2026-08-28 指示），不再经 codex 沙箱提交 | resolved |

| F-005 | P2 | 批 1 小审 E021-01：commit `6823196` 时点的 progress 只有失败的全量记录（E-003 并发毛刺）与过滤后 10/10，缺完整回归绿证——硬边界要求每批完成即全量绿 | codex-r1 小审报告（e:E-021）；task_plan 硬边界「每批完成 = 全量 npm test 绿」 | 提交后主会话已补跑树安静全量 161/161（E-022），并随 E021-02 整改再复跑含 reachability 的全量（E-023）；证据随小审整改 commit 落账。教训：批完成的全量绿要在提交前跑、不与 worker 并发 | resolved |
| F-006 | P3 | 批 1 小审 E021-02：`test/reachability.test.mjs` 未列入 package.json 默认 `npm test`（13 文件清单是显式枚举），纯函数 7 条日常回归跑不到 | codex-r1 小审报告（e:E-021）；package.json:12 | 主会话把 reachability.test.mjs 加进默认清单（排 runtime 之后），复跑全量为 E-023 | resolved |
| F-007 | P3 | 批 2 设计取舍待复核确认：driver 的「可驱动」判定是 **ref 在本仓解析到真实文件**——解析不到（如既有测试用的 `bin/probe` / golden 的 `bin/fix.sh`）时该节点保持 `pending`、**不开 Attempt**，Runtime 不为一个自己启动不了的入口凭空造一次尝试。这是有意的 fail-soft，与「ref 目录逃逸 → 开 Attempt 并立刻记 `E_BAD_VALUE` 终态」的 fail-closed 并列。副作用是它同时保住了 DHR_30 既有回归（那些 Run 的事件账逐字不变）；请复核判断这条判据是否需要收紧成「process profile 解析不到即判 Attempt 失败」 | `runtime/workflow-driver.mjs` `driveNode()` 注释；`test/workflow.test.mjs` 第 5 组；E-025/E-027 | **批 2 小审（e:E-032，terra high）已裁：保持 fail-soft 不收紧**——收紧会把 DHR_30 兼容问题误记为执行失败；但缺文件保持 pending 须补回归钉住（→F-010 必修③）。F-010 闭合后本条随之闭。**F-010 已闭**（E-034 回归钉住该语义），本条随之闭 | resolved |
| F-008 | P3 | 文档漂移待收口回填：`design/07 §6` 写的「不 import workflow / Process / executor，不推进业务节点」是 DHR_30 的边界；本卡按 DevPlan §3.2 DHR_31 把节点推进接进 Runtime，该句已被**有意收窄**（接法不动唯一写者：service 只起 driver，driver 一切读写仍排进宿主 actor 队列）。design/ 是冻结历史材料、本卡不改；已在 `runtime/service.mjs` 头注就地写明收窄理由 | `runtime/service.mjs` 头注（DHR_31 段）；`design/07 §6`；DevPlan §3.2 DHR_31 变更范围 | 收口时随 `as-built/relay-core.md` 增量一并写明「workflows / workflow-driver / process-executor」的现役边界；design/07 原文按冻结材料处理、不回改。**已闭**：`as-built/relay-core.md` 新增 §3.11（`workflows/basic-agent-task/`）、§3.12（`workflow-driver` / `process-executor`，开头即就地更正 design/07 §6 那句的适用范围）、§3.10 增量（sidecar 三条边界）；design/ 未回改 | resolved |
| F-009 | P0 | 批 2 小审必修①②：`resolveStepRef`（`runtime/process-executor.mjs:35`）只做词法 resolve，`:45` 的 `stat()` 会跟随符号链接——仓内 `steps/link.mjs → 仓外/evil.mjs` 能通过检查并被 `workflow-driver.mjs:127` 执行，「解析后按最终落点防逃逸」的安全边界未满足；现有测试只覆盖 `../`/绝对路径，无 symlink 反例 | 批 2 小审报告（e:E-032，terra high）P0 条 | 整改：对 repo root 与实际目标做 realpath 后仓内判定，逃逸仍开 Attempt 记 `E_BAD_VALUE` 终态；补真实 symlink 逃逸端到端回归（Windows 无 symlink 权限时测试须显式 skip 并留因，不得静默绿）。派回 dhr31-b2 整改。**已整改**：新增 `resolveStepEntry()`（两边 realpath → `escapesRoot` 判定 → 三态返回），driver 改调它并删掉跟随链接的 `stepIsFile`；`resolveStepRef` 降为纯词法守卫并注明「挡不住链接」。回归以「仓外脚本 marker 从未生成」为最硬断言；变异核验（短路守卫→红，E-033）证明非空转。**skip 分支保留但本机未走**：Windows 文件符号链接 EPERM 时退到免特权的目录 junction（对本命题等价），故 F-009 在本机实际执行、0 skipped（E-034） | resolved |
| F-010 | P2 | 批 2 小审必修③：F-007 的「仓内合法但文件不存在 → 保持 pending、零 node_started/零 attempt_started」语义（`workflow-driver.mjs:92`）没有回归断言（`test/workflow.test.mjs:270` 附近缺该组），兼容 DHR_30 的保护语义只靠注释固化 | 批 2 小审报告（e:E-032）P2 条 | 整改：补该回归断言；完成后 F-007/F-010 同时闭合。派回 dhr31-b2 整改。**已整改**：`test/workflow.test.mjs` 新增「仓内合法但文件不存在 → 节点保持 pending」一组，钉住零 `node_started` / 零 `attempt_started` / `attempt_count=null` / 下游同样 pending / 不得凭空造失败终态（E-034、E-035） | resolved |
| F-011 | P1 | 批 3 小审 P1：3.2 实录被判「时间线自相矛盾」（断连前 `events: 8`，而事件表 seq 8 = 180s 后的 attempt_succeeded，复核方据此判实录不可作需求境证据、要求重录） | 批 3 小审报告（e:E-051）；status.mjs:41 | 主控核查为**复核误读，实录自洽**：status 的 `events` = events.jsonl 行数（status.mjs:41-47），而账本首行是 **seq:0 `run_created`**，CLI 文本列表与 `--json` 帧均从 seq 1 起渲、不含此行，故行数恒 = seq_max+1——断连前 events:8 = seq0~7（彼时 seq7=attempt_started 已落账，正在延时中）、终态 13 = seq0~12；DHR_30 已收口先例 `cli-smoke-20260828.txt` 同一形态（events:7 对列表 [1]~[6]）。seq:0 原始行 + 全帧已落 E-052，提交复核方复裁。**复裁 accept，P1 撤销**：复核方确认漏算 seq:0（写点 service.mjs:367；CLI events 从 after_seq=0 补发只渲 seq≥1，cli/main.mjs:247），实录自洽 | resolved |
| F-012 | P2 | 批 3 小审 P2：E-050 耗时结论（R002 180.337s 等）在已提交证据里不可复算——转录只有事件名摘要，无带 `at` 时间戳的原始帧 | 批 3 小审报告（e:E-051） | 整改：新增 `evidence/e2e-disconnect-20260828-events.jsonl.txt`——R001/R002 `relay events --json` 原样输出（含 snapshot 头与逐帧 `at`）+ run store events.jsonl 首行与行数；attempt_started→attempt_succeeded 差值可直接复算（R001 35.762s ≥35s、R002 180.337s ≥180s），断连窗口亦可对时间轴复核。**复裁 accept，闭合**：复核方按帧复算 R001 35,762ms / R002 180,337ms / 断连时剩 129,295ms。附措辞指正（非阻塞）：E-052 头注「CLI 直读 run store」不准——原 service 已死后由 launcher 重建服务读取持久账本，此更正记录于此、evidence 原件不回改 | resolved |
| F-013 | P2 | 批 4 观察（**非本卡引入、非本卡修**）：`store/state.mjs` 的 `aggregate()` 不看节点的 `required`——一个 `required:false` 节点失败就把 `run_status` 拉成 `failed`，尽管全部必经节点已成功、`run_finished` 已落定。本卡是第一张撞上它的卡（第一次有可选的非 process 节点）。影响验收叙事：批 4 的 Agent 节点按窄路径是 `required:false` 的代持节点，它失败后整条 Run 在投影上显示 failed，与卡面「Agent 失败不推翻 Process 结论」的**字面**读法冲突；但 Process 侧的节点终态、结果工件与 `run_finished` 逐字节未动（已由 `test/agent-node.test.mjs` 批4③ 钉住），所以「结论」本身没被推翻，被改变的只是聚合投影 | `store/state.mjs:35-44` `aggregate()`；`test/agent-node.test.mjs` 批4③ 的 `stateAfter.run_status==="failed"` 断言；E-053 | 已按**当前真实行为**钉进回归（将来若按 required 收敛聚合，那条断言会红，正是希望它红的时刻）。是否收敛 `aggregate` 语义交主控/用户裁决：属 DHR_29/DHR_30 的 state 投影范围，改它会波及 read-model 与 CLI 渲染的既有回归，本卡不动。**已分流（F-023①）**：记 backlog，随「终态/投影语义收敛」批一并处理 | 已分流→backlog |
| F-014 | P3 | 批 4 观察（同上，非本卡引入）：必经节点全绿、`run_finished` 已写进事件账，但只要还有 `pending` 的可选节点，`aggregate()` 就返回 `pending`——于是「Run 已完成」与「run_status=pending」同时为真。批 4 边界钉那条用例里可直接看到（agent 节点保持 pending 时 `run_status=pending`） | `store/state.mjs:35-44`；`test/agent-node.test.mjs` 批4③ 的 `stateBefore.run_status==="pending"` 断言；E-053 | 同 F-013：按现状钉住，语义是否收敛交主控裁决。两条一并处理更划算（都在 `aggregate()` 一个函数里）。**已分流（F-023①）**：记 backlog，与 F-013 同批 | 已分流→backlog |
| F-015 | P2 | 批 4 全量并发下**观察到一次**：`relay events <id> --json` 的第一行不是快照（`view` 为 undefined），而 design/08 §5 要求客户端拿到的顺序永远是「先快照、再按 seq 的增量」。**根因假设（未证实）**：`cli/main.mjs` 的 `runEvents` 在 `await conn.call('subscribe')` **之前**就注册了通知处理器；若响应帧与随后的事件帧在同一次读取里批量到达并被同步派发，通知处理器会**同步** `out.write(event)`，而写快照的那行在 promise 续体里、要等一个微任务——于是增量可能抢在快照前落盘。服务端一侧是对的（`afterSend` 在响应发出之后才排空缓冲），可疑的是客户端写序。属 DHR_30 的 `cli/main.mjs`，非本卡引入、不在本卡范围 | 并发跑 `node --test test/workflow.test.mjs test/agent-node.test.mjs test/e2e-basic-agent-task.test.mjs` 时一次红：`Expected values to be strictly equal: + undefined - 'event_stream_snapshot'`（e2e 第 176 行）；随后连跑 3 轮三文件并发 + 单文件多次均未复现 | 本卡不修：`test/e2e-basic-agent-task.test.mjs` 改为**按内容定位快照行**并**显式断言它就在首行**（乱序时该断言会红并打印 CLI 原样 stdout/stderr），把「是否真有写序竞态」变成下次可自证的红。是否修 CLI 写序交主控/DHR_30 范围裁决。**已分流（F-023①）**：入项目验收池（DHR_30 范围确定性复现留后续卡） | resolved（已入验收池） |
| F-016 | P2 | 批 4 把套件从 15 个测试文件加到 17 个（`agent-node.test.mjs` + 批 3 的 `e2e-basic-agent-task.test.mjs`），每个都起真实 service / 真实子进程。`node --test` 默认按 CPU 数并行跑文件，于是**既有测试里最紧的那个超时预算**开始偶发超时：`test/service.test.mjs:73` 给 `start` 往返只留 8s（`until(() => received.length === 4, 8_000)`），而 `start` 要 spawn `git check-ignore`、建 Store、取 lease。实测：满载并发时该条与 `rpc-service`/`cli` 的若干条一起红（一轮 6 红、一轮 1 红），**单跑 `service.test.mjs` 13/13 绿、耗时 24s**；空载重跑全量 182/182 绿。非功能缺陷，是套件重量与预算的比例问题——但它会让「全量绿」这道闸变成看运气 | 空载三轮全量：182/182 绿（108s）／181 绿 1 红（149s，红在 service.test.mjs:73 `Error: timeout`）／176 绿 6 红（366s，机器另有负载）；`node --test test/service.test.mjs` 单跑 13/13 | 本卡不改 `service.test.mjs`（属 DHR_30，改别人的超时预算来迁就我加的文件是悄悄扩范围）。建议主控二选一：①把该预算从 8s 提到 20s；②`npm test` 固定 `--test-concurrency`（如 4）。**用户裁决走方案②**：`package.json` 的 test 脚本加 `--test-concurrency=4`（**只动这一行**，不碰 `service.test.mjs` 的 8s 预算、不改任何测试）。整改后当前机器全量 **182/182 绿、92.2s**——比不限并发时的 108.1s 还快，说明先前是自相争抢而非并行不足；「全量绿看机器负载」这一条随之消解，收口不再要求空载。**批 4 小审 P1-2 补记**：`--test-concurrency` 需 Node **>=18.19.0**，与 `package.json` 原 `engines: >=18.0.0` 冲突——已把 `engines` 与 `package-lock.json` 的 `packages[""].engines` 一并提到 `>=18.19.0`（两处各一行，未动任何依赖）。**另需知**：并发设上限**收窄了但没有全灭**这一类——整改后仍见一次 `runtime.test.mjs:449`（detached 宿主 10s 内落 2 条事件）在满载下超时，单跑 `runtime.test.mjs` 26/26 绿、复跑全量 183/183 绿。同属「既有紧预算 × 更重套件」，不是新缺陷；如日后再现，按同样办法（提该预算或再降并发）处置，本卡不动别人的预算。**批 4 复裁改判：退回 open**——本条的命题是「全量绿不看运气」，那个命题**没有**成立，只是症状换了地方；把它标 resolved 等于让 headline 命题借一次局部修复悄悄关掉。**不拆新 flake finding** 的理由：拆了之后 F-016 会以「service.test.mjs:73 已修」的名义闭合，而真正要守的那句话反而没有归属人——一条命题一条账更干净。**已知实例 2 个**：`service.test.mjs:73`（start 往返 8s，`--test-concurrency=4` 后未再现）、`runtime.test.mjs:449`（detached 宿主 10s 内落 2 事件，整改后仍见一次）。**已做的部分缓解**：`npm test` 固定 `--test-concurrency=4`（`package.json` 一行）+ `engines` 提到 `>=18.19.0`。**未做的**：没有任何机制保证下一个紧预算不再被撞到。**批 4 第二轮复裁（dhr31-r3）明示**：本条**保持 open / P2**，复核方仅放行批 4，整卡收口另行处理——即批 4 的 Accept **不**代表本条命题已成立。**已分流（F-023①）**：入项目验收池（「全量绿不看运气」命题在池内继续有归属人） | resolved（已入验收池，命题存活于池） |
| F-017 | P3 | 批 4 小审 P1-1 整改时的**码位选择留痕**：dsh-agent 宿主消失这一档，`reason-codes.md:53` 有更专指的 `E_EXECUTOR_HOST_LOST`（「承载 Executor 的宿主消失（DSH Native Agent 场景）」，第③问），但它是**第三个**首用码，不在本批主控授权的两码（`E_EXECUTOR_ADAPTER_LOST`/`E_EXECUTOR_ORPHANED`）之内。本卡改用 `:54` 的 `E_EXECUTOR_ORPHANED`——原文标注「第①②③问共用」、语义是「按 ref 探活/重连失败、确认该 Attempt 已无宿主」，对 DSH 宿主消失成立，不是凑合。**没有用 `E_EXECUTOR_ADAPTER_LOST`**：那条按 :52 专指 pi-agent 的 Adapter，套到 dsh 头上是错的 | `contracts/reason-codes.md:52-54`；`test/agent-node.test.mjs` 批4①bis 用例与其注释 | 收口时请主控/复核确认：dsh-agent 场景是否应改用更专指的 `E_EXECUTOR_HOST_LOST`。若是，那是一次独立的首用授权 + 一条断言改动，不涉生产代码。**已裁决**：用户 2026-08-28 对话 AskUserQuestion 点选**授权首用 `E_EXECUTOR_HOST_LOST`**（施工方与复核方的语义论证一致）。`test/agent-node.test.mjs` 批4①bis 已换用该专指码；`outcome` 仍为 `orphaned`——relay.result/v2 把两者分界写死为「`orphaned`=确认观察不到了 / `failed`=拿到了失败结论」，宿主没了属前者。该码是冻结集里既有的码，首用不算新增，`contracts/` 一个字未动（指纹 a990fdda 零漂移） | resolved |
| F-018 | P1 | **批 5·5.1 窄路径表达不了，已按硬边界停工摆回主控**（未动树外任何文件）。任务要求「fixture 数据源改经 `connectDshBridge` 取活数据 + 面板 `src/dsh-client` 零改动」。但面板消费的是 P4 期的 `relay.pilot-run-list/v1` / `relay.pilot-read-model/v1`，而 Bridge 能拿到的只有冻结的 `relay.client-read-model/v1`，两者字段集**不是子集关系**：`run-state/v1` 只有 `run_id/run_status/group/progress/elapsed_seconds/updated_at/state_signature` + `node_states[node_id,status,current_attempt_id,attempt_count]`；**面板渲染但活数据拿不到的字段（实测 13 个使用点）**：`attentions`(15 处)、`depends_on`(8)、`node.title`/`node.role`、`workflow_name`(5)、`trigger_by`(2)、`current_node_id`/`current_node_title`、`attention_count`/`max_attention_severity`/`top_attention_summary`、`log_locator`。其中 `workflow_name`/`title`/`role`/`depends_on`/`trigger_by` **确实存在于 `relay.run/v2` 运行文档**，但冻结 RPC 方法全集 `["contracts","listRuns","inspectRun","validate","start","control","subscribe"]` **不暴露运行文档**（`inspectRun` 的 detail 只读 `state.json`）；`attentions` 在 P5 根本不存在（P7/P8，brief 明列 out of scope）。**四条出路各自撞墙**：①编造 title —— `relay.run/v2` 的 `title` 描述逐字禁止「实现不得用 node_id 自动兜底填充，那等于把 P4 的有损降级写进协议」；②加一个读运行文档的 RPC —— 改冻结协议、破指纹；③宿主直接读 `<repo>/.dh-relay/<run_id>/run.json` —— 破 design/07 §4「客户端只发 RPC」，仓内 `control-plane-imports.test.mjs` 正是守这条；④改面板收 v2 形状 —— brief out of scope 明写「承接项只改 DHR_49 面板数据源，不新建 UI」。注：面板对 `attentions` 是**校验而非容错**（`dsh-client/lib/client.js:118` 不是数组即拒模型），所以缺字段不会优雅降级 | `check-drift.ps1` 基线 IN SYNC exit 0（E-083）；字段差集实测见 E-084；`contracts/relay.rpc.v1.schema.json` 方法枚举；`relay.run-state.v1.schema.json` `$defs.node_state`；`runtime/service.mjs:601`（detail 读 state.json）；`dsh-client/lib/client.js:118/369` | **交主控裁决**，我不自行绕。可选：(a) 缩到 H3 真正要的那句「附加客户端看到**同一状态**」——只投影 v2 真有的字段，缺的显式留空/`attentions: []`，接受面板留白甚至需要小改校验（要放宽「面板零改动」）；(b) 走契约批次给运行文档开一个只读 RPC（改冻结集，重生成三份基线，不是本卡范围）；(c) 判本项不适用、F-001 转下一张卡。**未做任何树外改动，工作树与树外目录均未被本轮触碰**。**已裁决（用户 2026-08-29 对话确认）：走 (a)**，缩到 H3 本意「附加客户端看到同一状态」；只投影 `relay.client-read-model/v1` 真有的字段，缺的显式留空、不编造不兜底；并放宽「面板零改动」为允许 `dsh-client` 最小校验放宽。**落地结果比裁决预期更省**：实测两个校验器要的字段（list 的 `run_id/group/run_status`、detail 的 `node_id/node_status` 与 `attentions` 是数组）**活数据全给得出**，`attentions: []` 又是 P5 的真值，所以 `dsh-client` **一行都没改**——授权的放宽额度没有动用。**另需更正我在本条里的一处判断**：原文说「面板对 attentions 是校验而非容错，缺字段不会优雅降级」——这话本身对，但我据此推出的「所以必须改面板」是错的：只要给 `attentions: []` 就同时满足校验与事实，其余未知字段面板全有 `"k" in run` / `Array.isArray` / `kv()` 守卫，**省键即优雅降级**。投影已在真实 service + 真实 Run 上验收 ALL PASS（E-086）| resolved |
| F-019 | P1 | **批 5·5.1 的剩余空位：打包后的插件如何取得 Bridge，撞上本包自己的安装拓扑契约**。`dsh-host/test/package-contract.test.mjs:124` 禁止裸说明符、`createRequire`、`require()`，**以及计算出来的动态 `import()`**（用例原话：「计算出的动态说明符一律拒绝，因为光读源码无法核实它指向哪」）。这条契约来自一次 `ERR_MODULE_NOT_FOUND` 启动失败 + 二轮复核，不是随手加的。于是「插件内 `import(relayCoreRoot + "/adapters/dsh-bridge")`」被本包契约挡死；裸依赖 `@dh-relay/relay-core` 同样被挡（且正是当年炸过的那种形态）；相对路径 import 跨仓且指向 `.dh-worktrees/DHR_31/`——收口删树即断。**已落地的规避**：`LiveRelayPilotRepository` 改为**注入式** `connect`，投影与活数据仓完整交付并已验收，只把「怎么拿到 Bridge」留成显式空位，**没有偷偷放宽别人的安全契约**。但 DSH profile 只能传数据、不能传函数，所以 5.2/5.3 在 DSH 内真跑之前必须先定这一环 | `dsh-host/test/package-contract.test.mjs:101-128`；`dsh-host/index.mjs` 顶部安装拓扑注释；E-086（注入式已验收 ALL PASS）；E-088（插件自测 41/41） | **交主控裁决三选一**：(i) 宿主 `spawn` 本仓 CLI（`cli/main.mjs … --json`）取 Read Model——不破任何契约、relay-core 零改动，但**取数机制不再是 `connectDshBridge`**，与任务书措辞有出入；(ii) 授权放宽本包契约，允许**一处**受控的计算动态 import（需明确它与当年 boot 失败的差别：绝对 file URL 不走裸说明符解析）；(iii) relay-core 内加一个极小 sidecar（`adapters/dsh-bridge/` 下），由它用 `connectDshBridge` 吐 v2 Read Model JSON，宿主 `spawn` 它并在插件侧投影——真正经过 Bridge、插件契约不破、relay-core 仍保持客户端中立（投影留在插件里），代价是本仓多一个文件。**已裁决（用户 2026-08-29 对话确认）：走 (iii)**。落地：新增 `relay-core/adapters/dsh-bridge/snapshot-main.mjs`（本卡首次、也是唯一一次动本仓生产代码目录）——只调 `listRuns`/`inspect`、原样吐 `relay.client-read-model/v1`，不投影、不改名、不补字段（投影留在插件侧的 `live-store.mjs`，协议层对客户端中立不破）；归 `adapters/` 不入 `runtime/`；只读，不碰 `start`/`control`。`control-plane-imports.test.mjs` 两条钉子仍绿，contracts 零改动、指纹 `a990fdda` 零漂移。插件侧改 `spawn` 它（`node:child_process` 是字面 `node:` 说明符，本包安装拓扑契约不破，41/41 仍绿）。已在真实 DSH boot 内验到活数据（E-093） | resolved |
| F-020 | P3 | 截图留痕①：面板头部那句 `relay.pilot-run-list/v1 · fixture a5cb14c4` 里的 **"fixture" 是 P4 期客户端写死的标签**，不是数据来源判断——它渲染的是 `relayPanel/hash` 拿到的 `fixture_hash`，而该字段在活数据下已改由投影内容算出（哈希随内容变：`b05712d2` → `a5cb14c4`）。**真实来源是 `runtime-v2`**，快照里 `source_kind` 与 CLI 的 `source` 都写着它（E-098）。不修、如实留痕：改这个标签要动 `dsh-client`，而本卡「面板零改动」已经保住，为一句措辞破例不划算 | `evidence/dsh-render-20260829-run-list.png` 头部；`evidence/cli-status-20260829-parity.json.txt` 的 `source: runtime-v2`；`live-store.mjs` 的 `fixture_hash` 注释 | 留给后续「面板改用 v2 形状」那张卡一并改措辞（如 `source: runtime-v2 · content <hash>`）。**读这张截图的人需要知道：写着 fixture，看的是活数据**。**已分流（F-023①）**：记 backlog，随「面板改用 v2 形状」卡改措辞 | 已分流→backlog |
| F-021 | P3 | 截图留痕②：详情页 `用时 undefined`。**这条不是「字段不可得」，是我的投影不对称**——`elapsed_seconds` 在 `relay.run-state/v1` 里是真字段（当前实现恒为 0），我在**列表**投影里落了它（所以列表渲染成 `0 秒`），**详情**投影里漏了；面板的 `duration(undefined)` 走 `String(undefined)` 就印出了字面量 `undefined`。与 `内容`/`角色` 两列空、`前置步骤 —` 不是一回事：那两者是 `title`/`role`/`depends_on` 确实取不到、按裁决 (a) 省键的**预期留白**，渲染也体面 | `evidence/dsh-render-20260829-run-detail.png`（`用时 undefined` vs 列表的 `0 秒`）；`live-store.mjs` `projectListRun` 有 `elapsed_seconds`、`projectDetail` 无 | **本轮不改**：一行就能修（详情投影补 `elapsed_seconds`），但改完当前截图就与在装版本对不上了，而截图刚由主控取完。留给批 5 小审后的整改一并做，届时需重取详情截图。**已分流（F-023①）**：记 backlog（一行可修 + 重取详情截图，随后续面板卡） | 已分流→backlog |
| F-023 | — | **收口尾巴分流裁决（用户 2026-08-29 对话 AskUserQuestion 点选，两题）**：①9 条 open 尾巴按 review 建议去处整批落账——F-003 / F-015 / F-016 入项目验收池（`dh accept add`）；F-013 / F-014 / F-022（终态与投影语义不对称一族）、F-020（fixture 标签措辞）、F-021（详情页用时 undefined）记 `backlog.md` 待后续卡承接；②F-002 卡面**不回改**，收口回填时在 DevPlan 状态行留一句 H5 已 superseded 注记。同拍用户确认「认可，收口」= E11 本地收口授权包生效 | 本次对话 AskUserQuestion 两题点选 + 用户明文「认可，收口」 | 分流执行结果逐条回写各条状态列；验收池与 backlog 落账在合入后主干执行 | resolved |
| F-022 | P3 | **一致性复核（第 4 路）扫出**：同一类事件在 pi / dsh 两条代持路径上落了**不同的 `outcome`**。`test/agent-node.test.mjs:222` 的 pi-agent「Adapter 消失」记 `outcome:"failed"` + `E_EXECUTOR_ADAPTER_LOST`；`:279` 的 dsh-agent「宿主消失」记 `outcome:"orphaned"` + `E_EXECUTOR_HOST_LOST`。两者都是「executor 消失、我方从未拿到这次执行的结论」，而 `relay.result/v2` 的 `outcome` 描述把分界写得很死——「`orphaned` = 确认观察不到了 / `failed` = 拿到了失败结论」，按这条**两处都该是 `orphaned`**。**reason code 本身配对没错**（`:52` ADAPTER_LOST 专指 pi 的 Adapter、`:53` HOST_LOST 专指 DSH 宿主），错的是 outcome 映射不对称；`reason-codes.md` 对这两个码都没规定 outcome，所以是我这一侧的取舍不统一，不是契约歧义 | `test/agent-node.test.mjs:222` vs `:279`；`contracts/relay.result.v2.schema.json` 的 `outcome` 描述；一致性复核表第 3 行（裁决=遗漏待修）；e:E-072 | **本轮不改**：批 4 已经两轮复裁 Accept 闭合，此刻改 ① 的 outcome 会动已闭批次的断言与账本语义，且它不影响任何已成立的命题（H7「只中断对应 attempt」两条路径都成立、H12 fresh attempt 两条都成立）。建议随 F-013/F-014 那批「投影与终态语义收敛」一并处理：把 ADAPTER_LOST 也归到 `orphaned`，或在 `reason-codes.md` 明确两码各自的 outcome。交用户分流。**已分流（F-023①）**：记 backlog，随 F-013/F-014「终态/投影语义收敛」批一并处理 | 已分流→backlog |
> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞
> 遗留（P0/P1 唯一合法路径，DC_46）：状态列写 `遗留→<DC_xx / backlog / 下计划名>（已确认）`——"已确认"三字代表你已在对话里明确点头同意把这条留到别处；没有这三个字，`dh-check` R13 仍按 `open` 处理、拦下"带病收口"。AI 不得自行把 open 的 P0/P1 划成遗留。
