<!-- dh:v1 · lesson_candidates.md — 教训候选。🟡 边做边攒，收口时由教训复核筛。 -->
# lesson_candidates — DHR_49

| # | 现象 | 根因 | 可推广的教训 | 证据 |
|---|------|------|------------|------|
| L-01 | 面板显示占位符 `…`，控制台零报错、探针也记成"成功"，实际拿到的是 `undefined` | `ctx.remote.<ns>.<m>()` 返回信封 `{ok, value}`，Host 侧失败**不 reject**；直接读 `result.<field>` 静默得 `undefined` | **边界返回值是信封时，拆信封必须显式**。"成功但值是空的"比抛异常难查得多——字段名对不上不会自己冒出来。写跨进程/跨语言边界的消费代码，先确认返回的是裸值还是 Result 信封，再动手 | findings §9；evidence/dhr49/batch1/probe-transcript.md |
| L-02 | 顶层 `inject` 里声明自己 `$mount` 出来的 namespace → 死锁 | 依赖声明表达的是"我需要它先存在"，而这个服务恰恰由本插件创建 | **自产自销的依赖不能写进依赖声明**，要用运行时作用域（`ctx.inject(names, cb)`）在创建之后再消费 | findings §8 |
| L-03 | 侦察阶段担心"tgz 装法开发回路慢"，实测 pack+装只要 ~1.2 秒 | 把 DHR_26 记录的"软链农场首建 300s+"错误外推到了增量安装 | **性能担忧要量了再信**。前人记录的耗时往往绑着特定前提（首次建farm），换个场景直接引用就是误判 | findings §5 |
| L-04 | 变异脚本放在 `test/` 下被 `node --test` 当测试收走，全量回归假红一条 | 采集脚本与单测混在同一目录 | **采集脚本归 `scripts/`，测试归 `test/`**——目录即契约，测试运行器按目录收文件 | progress E-006/E-007 |

> 收口时由教训复核（E5）筛选，命中的沉淀进 `knowledge/教训库-候选.md`。

| L-05 | 本卡的 DevPlan 开工回填被另一会话的销户提交 `e08a130` 顺走，历史里挂错了归属 | 两个会话改同一文件的不同行，行级不冲突；但对方 `git add <该文件>` 时整文件入库，把我未提交的改动一并带走 | **行级不重叠 ≠ 提交级不重叠**。同一文件的并行 WIP，光靠"改不同的行"只防合并冲突、不防归属错位。要真隔离得靠 worktree 或错开时间；做不到就在 progress 里显式留痕说明改动实际落在谁的提交里 | findings F-002 |

| L-06 | 批 1 源码在复核者读到一半时被批 2 覆写，复核对象当场丢失；只能靠 profile 安装副本重建才把复核做完 | ①没等复核结论就进下一批（闸口被自己架空）；②树外无 git，没有任何回滚点 | **无版本控制的工作区，「派审」和「冻结被审对象」必须是同一个动作**。有 git 时 diff 天然锁住了被审版本，没 git 就得手动冻结——否则你派出去的是一个会动的靶子。更一般地：闸口的意义就是拦住"还没人知道有没有问题就往前推"，绕过它省下的时间会以证据丢失的形式还回来 | findings F-004 / CP1 P1-1、P2 |
| L-07 | 「客户端 bundle 零 bare import」写进了结论，实际 bundle 有 `require("react")`，而断言只禁了 zod 的四种写法 | 黑名单式断言：禁掉想到的，没想到的一律放行。结论的强度超过了断言的强度 | **凡是「零 X」「只用 Y」这类全称结论，断言必须是白名单**。黑名单只能证明"没有我想到的那几种"，证不出"没有别的"。而且这条结论当时正撑着一个止损条件的判定——结论越关键，越要检查它下面的断言是不是同样强 | CP1 P2-4 |
| L-08 | 扩容变异脚本时，正控当场逮到我自己新写的一条断言有 realm bug（vm 里造的数组 prototype 不同，`deepEqual` 误报） | 正控（未变异副本必须全绿）是变异测试里最容易被当成走过场的一环 | **变异测试的正控不是形式**：它验的是"见红"归因正确。没有正控，一个自身写坏的断言会表现成"所有变异都见红"，看起来完美 | progress E-021 |

| L-09 | 我从类型注释读出「三根柱子全被占 ⇒ 第三方只能做浮层」，据此做了两版落点、都被用户否掉；实际存在一个正经的主区域加法位（`conversation.view`），而且本机就躺着一份 42 项的机器可读槽位目录 | 只读了**一个包**的类型注释就外推成全局机制结论，没去看「官方自己是怎么做的」这个最直接的反证源 | **要判断「平台允许我做什么」，读契约不如读先例**。契约注释只讲它自己那一块；把「官方产品里最像我要做的那个东西」拆开看，才知道路子存不存在。另：先找有没有机器可读的能力目录（本例是 `CLIENT_SLOT_API`），比逐个翻 `.d.ts` 又快又全 | findings「机制定论」 |
| L-10 | 用户连否两版落点，第三版才对；前两版都是「能装上、能渲染、测试全绿」的 | 我把「机制上跑通」当成了「设计上可用」。前两版在机器证层面全部成立，问题出在它们**是什么东西**——设置页里的一项、盖在聊天上的一层弹窗 | **UI 任务里「跑通」和「像那么回事」是两个验收项**，前者机器能证、后者只能人判。带界面的卡应当尽早把落点形态交人看，别等机器证攒齐了才问——机器证攒得越多，改落点的沉没成本越高 | findings 落点三版实录 U-01~U-03 |

| L-11 | 我给「手工调用装饰器」写的回归测试，自己把 initializer 应用到裸实例上——于是把构造器那行删掉，测试照样全绿 | 测试重建了被测对象的一部分行为。它验的是「零件好使」，不是「机器会用这个零件」 | **给"某个初始化会发生"写测试时，必须走真实构造/启动路径**，不能自己代替被测代码执行那一步。判断标准很简单：把生产代码里那行删掉，测试红不红？红才算数——而且这个判断本身要固化成变异，手工验一次不留痕等于没验 | review「CP1 返工收敛复检」P2 |
| L-12 | 变异脚本把包拷到临时目录改，测试却用写死的相对路径导入客户端面——变异改副本、测试读真包，那条变异当场没咬 | 测试里混用了「可重定向的根」和「写死的路径」两种寻址方式 | **变异测试要求被测对象的每一条读取路径都可重定向**。只要有一处绕过根变量直连真包，那部分就永远测不到——而且症状是"变异没咬"，很容易被误读成"断言写坏了" | progress E-025 |

| L-13 | 任务卡为中途闸预设了 (i)/(ii)/(iii) 三分桶，要求用户逐条点选；实跑时用户四道题全部弃桶、以自己的话作答，分类法当场作废 | 三个桶按**成因**切分（机制问题／字段问题／偏好），而用户给的是**结论**（该不该、行不行、我加需求了）。分类法要求用户先替主会话完成一次成因分析——那本来就是主会话该干的活 | **给用户的选项要按用户的表达方式切，不按内部处理流程切**。用户看界面产出的是判词，不是归因；需要归因就自己做完再拿核对结论去确认。更一般地：设计里凡是「请用户先分类，再据分类走不同分支」的闸，都要先问一句"用户真会这么想问题吗"——否则闸口会在实跑时空转，还得临时改口径 | DevPlan §3.2 B-13 / findings「中途闸点选结果」 |
| L-14 | 在 Bash 里用双引号包 `node -e` 脚本写含反引号的中文文档，反引号被 shell 命令替换吃掉一段路径——**同一个错误本卡内犯了第二次**，第一次已经写下"改用 Edit 工具"的结论 | 第一次只把结论记在 findings 里，没变成动作约束；下次遇到"要往 md 里插一整块中文"时，仍旧顺手选了最方便的 `node -e` | **写下教训 ≠ 行为改变**。会重复的操作类错误，得绑到触发条件上——本例是「内容含反引号或 CJK ⇒ 一律走 Edit/Write 或 quoted heredoc，不用双引号 `-e`」。另：这类错误的可怕之处是**静默**，脚本照常报成功，损坏要回读才发现；凡是用 shell 拼文档内容的，写完必须回读校验 | progress E-031 |
| L-15 | 改完代码 `npm pack` + `dsh plugin add`，命令打印 `Already up to date`、退出码 0，实际装的还是上一版；是核哈希才发现的，不是从症状查出来的 | 包管理器按**版本号**判定要不要重装，内容变了但版本没变，它认为无事可做 | **「命令成功」不等于「产物是新的」**。凡是"打包→安装→在别处运行"的链路，中间必须有一道**内容级**校验（哈希对比），不能拿退出码当验收。这类失败最坏的地方是症状出现在下游——你会去查代码、查注册、查缓存，就是不会想到"装的根本不是这一版" | findings F-005 / progress E-041 |
| L-16 | 「原生文件夹选择框驱动不了」被我当硬阻塞记了三批，实际那个选择器是个 auto 插件按环境解析出来的，置一个环境变量就换成页内版本 | 读到「这里挂的是 native picker」就停了，没往下看**它凭什么挂 native** | **遇到"平台就是这么定的"的阻塞，先找那个做决定的函数**。本例决策全写在一个纯函数里，读五行就知道有哪些开关。同一类错误本卡犯了第二次（L-09 是只读类型注释就断言第三方只能做浮层）——**读到一层封装就停下，是我的稳定失效模式**，值得当成检查项：凡是要下"平台不支持"的结论，先问一句"这个限制是谁在哪一行做的决定" | findings F-006 |
| L-17 | 给「卸载会清干净」写证据时，第一反应是在 disposer 里记一个 `disposed` 标记 | 标记本身就是留在页面上的东西——用"留下一个东西"去证明"什么都没留下" | **证明"不留痕"的正确判据是缺席，不是标记**。本例改用探针对象的**身份**：工厂用 `?? ` 复用已有全局，所以没删就是同一个对象、删了就是新对象。另外要主动排除替代解释——先在 window 上放一个与插件无关的标记，用它证明"页面没刷新"，否则身份变化也可以由整页刷新解释 | evidence `batch4/hmr-disposer-transcript.md` / progress E-037 |
| L-18 | 查「轨迹页签下有没有输入框」，用 `[class*="InputBar"]` 得到 false，差点据此答「按视图可以隐藏输入框」；实际那是 CSS Modules 哈希类名（`uV2eYG_*`），选择器永远匹配不上 | 拿**构建产物里会被改名的东西**当探针。返回 false 的真正含义是"我的选择器没写对"，不是"这东西不存在" | **否定结论要比肯定结论多验一步**。"存在"被找到即成立；"不存在"必须先证明探针本身有效——换个稳定锚点（`data-*`、`role`、官方自己留的标记）复查一次，或在一个已知存在的场景下跑同一个选择器当正控。DOM 里凡是哈希过的类名一律不能当查询依据 | findings F-008 / progress E-044 |
| L-19 | 用户问"这个输入框能去掉吗"，答案不是能或不能，而是"整个去不掉、但官方自己有一套让它不碍事的约定" | 我一开始只想到两种答案（删得掉 / 删不掉），没去看**官方视图遇到同一问题时怎么办的** | **平台限制面前，先看官方组件在同一处境下的做法**。本例 ui-conversation 特意留了 `:has([data-conversation-composer-overlay])` 这条规则把决定权下放给视图，还发布了 `--dsh-composer-height` 让视图自己算留白——都是明摆着的公开约定，只是不写在类型里。这与 L-09、L-16 是同一条：**读契约不如读先例** | findings F-008 |
| L-20 | DM5b 校验器第一版用 `Object.getPrototypeOf(v) === Object.prototype` 判「普通对象」，把 `vm` 里跑出来的纯数据全判脏，理由还是自相矛盾的「不是普通对象：Object」 | 拿**跨边界不成立的 identity** 当判据。原型是**每个 realm 一份**的，`vm`、iframe、worker 各有各的 `Object.prototype` | 判据要选**跨边界仍然成立的性质**，而不是身份相等。本例改判「原型链根不根」（`Object.getPrototypeOf(proto) === null`）——每个 realm 的 `Object.prototype` 都是自己链的根，而 `Date.prototype` 等都不是。同族的还有 `instanceof`、`=== Array.prototype`、构造器引用比较，凡是被测代码可能跑在另一个 realm 里（`vm` / iframe / worker）就都不能用 | findings F-011 / progress E-047 |
| L-21 | 验收口径写的是「`JSON.parse(JSON.stringify(x))` 深等自身」，我照写了，但真正干活的是另写的那个逐字段 walker；写完才发现往返那条被 walker 完全包住，根本不是独立证据 | 默认「口径点名的判据 = 最强的判据」，两条都摆上去就当成两条证据 | 断言写完补一问：**这两条里有没有一条是另一条的推论？** 是的话，说清谁是主力、谁只是口径的字面形式，别让读的人以为有两道独立的网。口径不是上限也不是下限——达不到要说，超出了也要说清超在哪 | findings F-011 |
| L-22 | 12:00 探到 `ssh thinkpad` 超时 + Tailscale `NoState`，登记「目标机不可达、判据②未能开始」；12:31 只是去补留痕，重跑同一组命令发现已经全通，判据②当场就能跑 | 把**环境类的否定结论**（连不上 / 没装 / 不存在）当成了和代码事实一样稳定的东西，探一次就写进结论 | **环境类否定结论有时效，收口前必须重测**。判断依据是"这个事实由谁控制"：代码事实由仓库控制、探一次即可；网络可达性、进程在不在、外部服务状态由**别人**控制，随时会变。凡是要写进验收结论的这类事实，都在收口那一刻再测一遍，并把两次的时间戳都留下 | progress E-059 / E-063 / findings F-012 |
| L-23 | 任务卡判据①写的是「删除产物**与依赖缓存**」，我写施工步骤时改写成「删掉 `*.tgz`、`node_modules`、profile 条目」——而本 Pilot 根本没有 `node_modules`，那一项等于自动满足，于是 pnpm store 一个字节没清，整轮是 warm install | **把验收条款改写成自己的清单时丢了一项**。改写时我按"这个项目有什么"去列，而不是按"卡里要求清什么"去列，于是一个我这儿不存在的形态（node_modules）替换掉了一个真实存在的形态（pnpm store） | **验收条款转成施工清单时逐词对照，不许同义替换**。卡里的名词要么原样保留，要么写「卡里的 X，在本项目对应的是 Y」并保留 X。特别是"缓存/产物/状态"这类词——它们在不同技术栈里的具体形态不同，一改写就容易漏掉本栈真正的那个。检验法：清单里每一项都问一句「这项在本项目**非空**吗」，全是空的那项就是漏点 | findings F-012 / progress E-062 / CP3 P1 |
| L-24 | 换机重跑第一条命令就失败：`npm pack --pack-destination ../../dist` 报 `ENOENT`，因为目标机上 `dist/` 不存在。本机跑了无数遍都没事——因为 `dist/` 早就在了 | 配方在作者机器上**从来没在初始状态下被走过**。每一步都成立，但成立的前提里混着前一轮遗留的状态 | **配方类文档的验证必须在"从没跑过"的状态下走一遍**，不是在"跑过很多遍"的机器上重跑。换机重跑之所以值钱，不在于换了 CPU 架构，而在于换到了一台**没有任何遗留状态**的机器——它会把作者看不见的隐含前提逐条逼出来。同理：清净重跑要真清到底（见 L-23），否则它只是"重跑"不是"清净" | progress E-065 / cross-machine.txt |
| L-25 | 同一张卡里，判据②我花整节去分「不是跑失败、是没跑起来」，判据①却把「本轮判为不划算所以没跑」写成了「被 pnpm 拒绝」——复核者一眼看出双标 | **对自己没做的事和对外部环境，用了两把尺子**。外部失败我分得很细，自己的取舍就顺手包装成客观阻塞 | **「没做」要标清是哪一种**：技术上做不到（给出被拒的原文）／代价太大主动不做（给出代价数字与判断）／忘了。三种对读者的意义完全不同：第一种是结论，第二种是可以被推翻的取舍，第三种是缺陷。写「被 X 拒绝」之前先问：**被拒的是不是我真正该走的那条路？** 本例 pnpm 拒的是"给已存在 profile 换 store"，而端到端那条路是"全新 DSH_HOME"，压根没被拒过 | CP3 二轮③ / progress E-069 / clean-rerun.txt §6 |
| L-26 | 冷装验证只比了 `lib/` 四个代码文件，漏了 `cordis.patch.yml` 与 `package.json`——而那两个才决定"装上之后接不接得上" | 把「产物」默认理解成「代码」。但交付物里**接线文件**与代码同等重要，而且它们更容易在打包/安装环节被漏掉或改写 | **比对产物时，比对面要覆盖交付边界内的全部成员，不是只覆盖"主要的那几个"**。判据里凡是出现「A 相同 ⇒ B 也相同」这类拼接推理，**接缝处的相同必须是全量相同**，漏一个成员整条推理就断。实操：拿打包清单（`tar -tzf` / `files`）当对比清单，逐项打勾，不同的那几项要能一句话解释 | CP3 二轮① / progress E-070 |
| L-27 | DM5b 的校验器逐字段走原型链，看着很严，但**数组分支只遍历索引元素**——一个真 Cordis Context 挂在 `runs.ctx` 上，两张网都看不见，12/12 全绿 | 写遍历器时把「数组」当成「元素的容器」，忘了**数组本身也是对象、也有 own key**。而 `JSON.stringify` 恰好也丢掉这些键，于是"往返深等"那条兜底网跟着一起瞎——**两条网共享同一个盲点，等于只有一条** | **两条互为兜底的判据，要先检查它们的盲点是不是同一个**。共享盲点的两条网不构成冗余。具体到遍历器：凡是分支处理（对象／数组／原始值），每加一个分支就问「这个分支跳过了什么」；数组分支尤其要显式处理 own key 与原型，别默认 `forEach` 覆盖了全部 | findings F-012 / progress E-073 / CP4 P1 |
| L-28 | 「客户端不加料」那条断言，期望值用的是 stub 交给客户端的**同一个对象**——于是只拦得住"换对象"，拦不住"就地改"，而后者才是随手会写出的那种 | 拿**被测路径自己传下去的引用**当期望值。变异脚本恰好写成了 `{ ...x }` 换对象的形式，于是"见红"了，掩盖了这个洞 | **期望值必须独立于被测路径重新取得**（重新读文件 / 重新构造 / `structuredClone`）。检验法：把被测代码改成"就地修改而不换对象"，断言还红不红？不红就是自比。**变异见红不等于断言够严**——变异体也可能恰好走了断言唯一能拦的那条形态 | progress E-075 / CP4 P2 |
| L-29 | `roots === 7` 那条源码级断言：合规新增一屏 → 红（逼人改魔数）；违规新增一屏（忘打浮层标记）→ **绿**。它存在的唯一理由恰好是它唯一沉默的场景 | 用**计数**代替**不变量**。计数会随合规改动漂，于是它对合规改动敏感、对违规改动迟钝——**方向正好反了** | **断言要钉不变量，不要钉数字**。写完追问一句：「**我想拦的那件事发生时，它会红吗？我不想拦的事发生时，它会不会误红？**」两个方向各造一个反例跑一次——本例改成「全文件只许出现一次 `className: "rp-page"`」后，违规必红、合规不误红。凡是断言里出现"恰好 N 个"且 N 会随正常开发变化，基本都是这个毛病 | progress E-077 / CP4 P2 |
| L-30 | 目标机报 `Cannot find package '@personal/dsh-relay-host'`，我先怀疑 `link:` 装法、再怀疑符号链农场没建好，都不是；真正断的是一个**平台相关的原生插件**没装上，中间隔着两层 | 报错文本指向的是**症状发生的地方**，不是原因所在的地方。而我按报错文本的字面意思去排查 | **排查"找不到 X"时，先确认"负责去找 X 的那套机制"本身是好的**。本例的分水岭是一个廉价的对照观察：**两个包（`link:` 装的和 tgz 装的）报同样的错**——装法不同而症状相同，说明问题不在装法，在解析机制。这类"同时失败"的对照能一步排掉一大片猜测，比顺着报错逐层试便宜得多 | findings F-013 / progress E-082 |
| L-31 | `task_plan.md` 的 CP2 检查点写的是两个动作——「派 fresh 小审本批 diff；**然后停下**，请用户亲跑并表态」。实跑只做了后半，前半**从来没派过**，`review.md` 的 CP2 行一直空着，直到轮 2 复核翻出来 | 检查点被当成「一个事件」在心里记着，实际它是**一串动作**。人判那半有对话痕迹、有用户回复，做没做一目了然；AI 派活那半**没做也没有任何痕迹**，于是它静默地消失了。而空着的表格行读起来像「待填」，不像「漏了」 | **多动作的闸，每个动作各占一行台账，缺席要留下缺席记录**。表格里的空单元格是歧义的——它可能是还没到、跳过了、忘了。收口前扫一遍所有空单元格，逐个判定属于哪一种，别让忘了混在还没到里过关。另：**最吃重的那一批最值得复核**，而本例恰恰是最吃重的批 2 漏掉了 | review.md CP2 行 / progress E-100 |
| L-32 | 「面板与 CLI 共用一套词表」这条主张，钉它的断言把**期望值手抄进了测试**。于是 CLI 那侧单方面改译法（「待你处理」→「该你了」），全套测试照样全绿——轮 2 复核实测三条全绿 | 这是 L-28 的**复发**，而且是在我已经因为 duration 那条踩过一次、并且写下 L-28 之后复发的。当时只把 duration 那一处从手抄升级成真对照，**没把同一条教训横向推广到 role / status / group** | **修完一处手抄，立刻横扫同类**：问还有哪些断言的期望值是我抄进来的？而不是只修被指出的那一处。另外，**子串兜底（`includes`）不能当方向控**——本例里 `cliText.includes("复核")` 恰好被 fixture 某个节点标题里的「复核」二字中和掉了，兜底形同虚设。要证两侧一致，就得让**期望值真的来自另一侧** | progress E-090/E-091 / CP4 收尾提示① |
| L-33 | 新写的那条方向性断言，第一次跑就报「演算本身没生效」——我用全文 `replace` 改 `running: '运行中'`，而这个词在 `STATUS_LABELS` 里**先出现**，替错了表 | 同一个词在多张表里重复出现是常态，全文替换默认命中第一个 | **变异/替换必须锚定到作用域，不能全文替**。更值得记的是：这个错是被**我刚写的那条断言自己抓住的**——它检查改完之后解析结果真的变了没有，也就是把变异是否生效当成前置条件去验（同 E-021 那道 sha 校验的思路）。**变异脚本要先证明自己改动了东西，再去看测试红不红** | progress E-093 |
| L-34 | 钉住「分组不许读 `run_status`」的是一条**扫源码的词法断言**（切函数体 grep `run_status`）。CP2 复核把那行读取**挪进同一段里的一个 helper**，词法网整个失效——它造的「按 `run_status` 静默丢 run」实现跑全量 **60/60 全绿** | 词法断言拦的是**写法**，不是**行为**。同一个行为有无数种写法，挪一层函数是最便宜的那种。我当时还特意把切片范围**收窄**（理由是"展示层用 `run_status` 是正当的，所以只切分组函数"）——**收窄本身就是在给绕开留门** | **词法断言只能当辅助，主力必须是行为断言。** 判断法：问「这条断言能不能被一次纯粹的重构绕过？」能，它就不是网。本例的正解是守恒断言——不管你怎么写，**进去几条 run 就得出来几条、一条不丢一条不重**，这是行为，绕不过去。附带一条：词法网要扫**代码**不扫散文，切宽之后我第一次跑就咬到了自己解释这条约束的 JSDoc | progress E-105/E-106 |
| L-35 | 守恒断言补上之后仍需要第三步：**列表 fixture 只覆盖 7 个 `run_status` 里的 5 个**，缺 `pending` 与 `unknown`。而违约实现恰恰挑了 `unknown`——一个合法但没被覆盖的值 | fixture 是「真实样本」，真实样本天然不会把枚举值取遍。于是**没被覆盖的枚举值成了违约实现的天然藏身处**，而且不是刻意躲，是碰巧 | **枚举型输入要单独做全覆盖扫，不能只靠真实样本。** 不必改冻结 fixture（那是别人的产物）：在副本上把枚举值逐个刷一遍，再加一轮逐条混合（防"整份同值"恰好被某种实现放过）。顺带：**镜像断言要逐条对照另一侧已有的约束清单**——本例的守恒断言 CLI 侧一直有，DSH 这边只是**漏搬**，不是没想到 | progress E-106/E-107 |
| L-36 | 为修 L-32 新写的「从 CLI 源码解析词表」helper，docstring 写着「解析不到就抛——响亮地失败，不许静默回退」。CP2 一测：**混引号写法会让某个键无声消失**、注释里的花括号会让字面量被截早，两种情况**都不抛** | 「全空才抛」和「不全就抛」差着一整类失败。少一个键 = 少一条被比对的译法 = 网上多一个洞，而洞的大小恰好等于我没检查的那部分 | **凡是"解析/提取"型的辅助代码，都要问：部分成功算不算成功？** 默认答案应该是"不算"。可操作的判据：**能不能数出应该有多少个，再和实际拿到的比？** 能就一定要比——这比枚举各种畸形写法可靠得多，畸形写法列不全，计数对不上却一定会露 | progress E-108/E-109 |
| L-37 | 派出去的 subagent 连发三次**空的 idle 信号**、两次催促无效，全程零文本产出。我一度在干等 | 「等它回话」把整条链路的产出**押在了它愿不愿意说**上。而它做的事**在磁盘上全都留了痕**——profile 的 `package.json`、安装副本哈希、监听端口、进程命令行、文件 mtime | **别把 agent 的自述当唯一取证渠道。** 派活时就想清楚：**它做的事会在哪里留下我能自己去看的痕迹？** 本次靠磁盘痕迹独立还原出了完整链路（打包→装 Host→装 Panel→加工作区→起服务），结论**比它自述更硬，因为不依赖叙述**。附带：催促要给**具体的观察**（"我看到你 3087 起来了"）而不是泛泛的"到哪了"——前者能让对方知道你已经掌握了什么，也能让你自己确认催的方向对不对 | progress E-131/E-133 |
| L-38 | 用户拒绝亲跑「照文档从零敲一遍」那道闸，要我代跑。**我代跑顶不了这一格**——配方是我写的，我敲的时候会不自觉用脑子里的知识补上文档没写的步骤 | 「文档自不自足」这个属性，**测试者必须不知情**。写文档的人测自己的文档，测的是"我记不记得住"，不是"文档说没说" | **文档自足性只能由不知情的人来测。** 可操作的替代方案：派一个**零上下文执行者**，硬性限制「只许读这份文档、不许读源码、不许问人」，卡住和猜错都算数据、偷看不算。本次一轮就抓到 `--port` 未写——**任何人在本机已跑着一个实例时照文档敲都会撞端口**，而我永远试不出来。同类前科：`--pack-destination` 不自建目录（L-24）。⚠️ 但要记清口径：**机器代跑是旁证，不顶人判**——人判问的是"用户能不能"，那只有用户能答 | progress E-130~E-132 |
