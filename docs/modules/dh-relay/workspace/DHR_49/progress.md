<!-- progress.md — 施工日志 + 证据账本。🟢 边做边记。这是"实际发生了啥"，容忍跑偏——路径偏了记这里，不回写任何计划文档。项目有 journal 时本文件代替 journal（不双写）。 -->
# progress — DHR_49

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|------|----|---------|------|--------|
| 2026-08-20 | 主会话(Opus) | 开工闸：用户对话确认三项——确认开工 / 不开 worktree / 委托**全留主会话**（两轮换人复核仍派 fresh agent）；工作区 8 件套建立 | AskUserQuestion 三项点选 | S2 现场侦察 |
| 2026-08-20 | 主会话(Opus) | S2 现场侦察（只读，未上机）：摸清 client bundle 产物形态、UI 注册面、Client→Host 数据通路、两侧 codec 严格度差异、装法约束、包形状、命名避让，落 `findings.md` F-A~F-G + §4.4 止损**预判**表 | findings.md「现场侦察结论」 | 写 task_plan 施工步骤 |
| 2026-08-20 | 主会话(Opus) | 施工步骤落 `task_plan.md`：分 4 批（探针 → 列表屏 → 配方 → 详情屏），CP2 = 列表屏中途闸强制暂停 | task_plan.md | 回填 DevPlan，进批 1 |
| 2026-08-20 | 主会话(Opus) | 回填 DevPlan §3.1：DHR_49 行状态→进行中 + 工作区链接；**顺手补 F-001 账目缺口**——DHR_26 行由「未开始」改「已完成」并回填 verify SHA `c90cf88`（DHR_26 已收口但 §3.1 未回填，成因见 workspace/DHR_26/devplan-handoff.md）。**未碰头部 `dh:status` 块**（DHR_27 会话并行占用，F-002） | E-001 | 批 1 施工 |
| 2026-08-20 | 主会话(Opus) | 批 1 TDD：先写 4 条包契约失败测试（exports 四口 / client.js 零 zod / platform=web / files 覆盖运行时清单）→ 跑红（包不存在） | E-002 | 写实现 |
| 2026-08-20 | 主会话(Opus) | 批 1 实现：新包 `@personal/dsh-relay-panel`（`src/dsh-client/`）——宿主半边 `lib/index.mjs`（`RelayPanelGateway extends TypertRemoteService`，namespace `relayPanel`，`inject:['relayPilot']`，只转发不算数）+ 手写描述符 `lib/typert.host.js`（真 zod）/ `lib/typert.remote-client.js`（结构化 `{parse}`，零 zod）+ **手写** bundle `lib/client.js`（`window.__ModuleLoader__.load`，`React.createElement`，无打包器）。**`src/dsh-host/` 一行未改** | E-003 | 跑绿 + 变异 |
| 2026-08-20 | 主会话(Opus) | 批 1 验证：包契约 4/4 绿；新增防漂移测试 3/3 绿（bundle 内联描述符 vs `./remote` 逐项相同、两侧 schema 判决一致、注册 id/导出面）；**7 条变异逐条见红 + 正控全绿**（承接 DHR_26 §17 教训：断言写完必须跑变异）；全量回归 167/167 绿 | E-004 / E-005 / E-006 | 真机装载 |
| 2026-08-20 | 主会话(Opus) | 批 1 真机：`npm pack`（5.4 kB/6 文件）→ `dsh plugin --profile web add <tgz>`（694ms，EXIT 0）→ `dsh --profile web` → 浏览器 `http://127.0.0.1:3080`。**装载产物与源文件 SHA256 逐字节相同**（零构建步骤坐实）。踩两个坑并修：①`remote.relayPanel` 须走 `ctx.inject(names, cb)` 开作用域（顶层 inject 会死锁）；②Remote 返回信封 `{ok,value}` 不是裸值、失败不 reject（第一版静默拿 undefined、控制台无报错） | E-007 / E-008 | 判据对拍 |
| 2026-08-20 | 主会话(Opus) | 批 1 判据命中：面板在设置页渲染出 `fixture_hash: 67fb18b3…612c`，与 DHR_26 四轮转录**逐字符相同**；浏览器刷新、DSH 重启（本轮 3 次）后重建一致；控制台零 error/warn | E-009 | CP1 阶段汇报 + 小审 |
| 2026-08-20 | 主会话(Opus) | CP1 阶段汇报交付；用户点选「每批派 fresh 小审」。派出 CP1 只读复核 agent（brief=review-brief-cp1.md） | E-010 | 批 2 施工 |
| 2026-08-20 | 主会话(Opus) | 批 2 TDD：先写 7 条分组测试（分节顺序 / 镜像断言①②③ / 词表与 CLI 一致 / 空列表 / 源码级不读 run_status）→ 跑红 7/7 | E-011 | 实现 groupRuns |
| 2026-08-20 | 主会话(Opus) | 批 2 实现：`groupRuns()` 只读 `group`（分节 = 首现顺序，成员 = `runs[]` 顺序，词表外原样成节）+ Host 加 `list()` 只读转发 + 两侧描述符加 `relayPanel/list`（宿主真 zod 且**刻意宽松**——权威 schema 在 `src/read-model/`，此处重述会造第二真相源）+ 列表屏 UI（`React.createElement`，官方同款 `<style data-plugin-css>` 注入） | E-011 / E-013 | 跑绿 + 变异 + 真机 |
| 2026-08-20 | 主会话(Opus) | 批 2 验证：分组测试 7/7 绿（其中一条**是我的期望写反了、实现是对的**，已改断言）；6 条分组变异逐条见红 + 正控全绿；全量 174/174 绿 | E-011 / E-012 / E-013 | 真机镜像断言 |
| 2026-08-20 | 主会话(Opus) | 批 2 **真机 DM6 双向对照**（不是单测）：造两份变异 fixture，只换 `RELAY_PILOT_FIXTURE_ROOT` 重启 DSH、代码不动——①只改 `group`：run 换节、状态芯片仍显旧状态、`running` 整节消失；②只改 `run_status`：分节骨架与基线逐项相同、仅芯片改字 | E-014 / E-015 | 交中途闸 |
| 2026-08-20 | 主会话(Opus) | **落点被用户连否两版**（U-01 设置页 / U-03 全屏浮层，见 findings「批 2：面板落点的三版与用户原话」）。第二版改为 `sidebar.footer.action` 入口 + `shell.overlay` 浮层并实测通过（刷新后重建一致），仍被判「等于在对话框上盖一层」。按用户指示派出只读调研 agent 摸官方 39 个一方 client 包的界面做法，**中途闸暂缓交付**——落点未定时交出去等于让用户跑一个自己已经否掉的东西 | E-016 | 等调研结论定 v3 落点 ⚠️ **2026-08-21 按 CP2 复核补量程**：本条引用的 `list-screen-overlay.png` 是**白底极浅灰字，正文基本读不出来**——节标题、run_id、fixture 指纹全不可辨，只有红/橙的 attention 行可读。也就是说这张图**撑不起本条列举的任何一项**，当时的实际依据是 a11y 树转录。该落点（v2 `shell.overlay`）已废弃，不必重拍；本条改记为「以 a11y 树转录为准，截图不可读」。最终落点上的同类证据见 E-102/E-103。 |
| 2026-08-21 | 主会话(Opus) | v3 落点定为 `conversation.view`（会话主区页签，与「对话」「轨迹」并排），用户实机确认渲染成立。**两条固有代价随位子而来、非本实现毛病**：①会话级——hero 空屏整个会话头隐藏，页签够不着；②第三方切不到自己的页签（`setView` 是 ui-conversation 私有 store 动作） | E-029 / E-043 / F-007 | 中途闸材料 |
| 2026-08-21 | 主会话(Opus) | **中途闸：用户以原话作答，未采用三分桶**。四问全部自由文本回答，随后点选「作废，改记原话」+「放行，开干」。DevPlan 插入 B-13 决定块：分类规则 2 作废，人判验收改为「记原话 → 主会话核工程影响 → 用户确认」，规则 1/3 保留 | E-031 / E-032 / findings 中途闸点选结果 | 批 4 开工 |
| 2026-08-21 | 主会话(Opus) | 批 4 详情屏：新增 `relayPanel/get(runId)`（两侧描述符各加一条带参 invocation，宿主真 zod、客户端结构化 `{parse}`），列表卡片可点/可键盘进详情，主体为**接力计划棒序表**（按 `nodes[]` 源顺序，不按 `depends_on` 重排）。**任务卡 4.2 写的是 `detail(runId)`，实现叫 `get(runId)`**——名字改了、契约没改，两侧描述符与测试都按 `get` 对齐，此处登记偏差 | E-033 / E-039 | 卸载清场 |
| 2026-08-21 | 主会话(Opus) | 用户问「输入框和面板什么关系、能不能去掉」。查清：输入框归**会话**不归视图，按页签隐藏做不到；照官方 Trajectory 的约定改成浮层（屏根打 `data-conversation-composer-overlay`）+ 用官方发布的 `--dsh-composer-height` 算底部留白。**中途我用哈希类名当探针查错过一次并自纠** | E-044 / E-045 / F-008 / L-18 | 补批 4 缺口 |
| 2026-08-21 | 主会话(Opus) | 用户问「以后会不会点一下拉起 herdr」。查清 `relay focus` 在 `design/` 全目录**只出现一次、无正文定义**，登记为设计缺口带往 P5；并核清四条边界（GUI 只作展示增强 / 抬窗口必须走宿主侧即写路径 / 目标须由句柄给 / 跨机不在范围）与两道前置闸（H6 底座选型、P5 写权） | F-009 | 按计划推 |
| 2026-08-21 | 主会话(Opus) | **补齐批 4 的四个缺口**：4.5 DM5b 纯 JSON 断言（12 条 + 12 变异，此前完全没做）、4.4 `--dump-config` 三态转录、4.3 详情屏重启与刷新复验、以及顺手修掉探针里的 `detail: undefined`。全量 213/213 | E-047~E-055 / F-010 / F-011 | 批 3 |
| 2026-08-21 | 主会话(Opus) | **批 3**：判据①本机清净重跑**成立**（bundle 逐字节相同、`RELOADABLE`）；判据②换机重跑**阻塞**（`ssh thinkpad` 超时、本机 Tailscale `NoState`），按任务卡记「本机可复现、跨机未成立」，**DM2 不得记 pass**。顺带查清 tgz 哈希变化的归因、补了配方里两条会绊住重跑者的坑 | E-056~E-060 / F-012 | CP3/CP4 小审（需用户放行派 agent） |

## 证据账本 (Evidence Ledger)

<每条"完成"结论挂一条可复跑的命令 / grep / runtime 输出。不能空口说"做完了"。>

| ID | 类型 | 命令 / 路径 | 结果 (pass/fail/observed/waived) | 支撑什么结论 |
|----|------|-----------|------|------|
| E-001 | cmd | `git diff -- "docs/modules/dh-relay/dev_plan/P4-DSH工作台最小Pilot-开发方案.md"` | observed | DevPlan §3.1 两行已回填，`dh:status` 块未被本卡触碰（与 DHR_27 会话并行 WIP 零重叠） |
| E-002 | test | `node --test test/dsh-client-package-contract.test.mjs`（实现前） | observed（4/4 红，ENOENT: 包不存在） | TDD 跑红：包契约先于实现钉住 |
| E-003 | cmd | `<pilot>/evidence/dhr49/recon/`（`dsh-version.txt` / `slot-names.txt` 28 项 / `client-plugin-inventory.txt` 39 包） | observed | 侦察机器事实落盘；`settings.section` = 「一整页」槽位（官方 slots 契约 d.ts 原文），选它做面板落点 |
| E-004 | test | `node --test test/dsh-client-package-contract.test.mjs`（实现后） | pass（4/4） | 包形状满足 exports 四口 / 零 zod / platform=web / files 覆盖 |
| E-005 | test | `node --test test/dsh-client-typert-drift.test.mjs` | pass（3/3） | bundle 内联描述符与 `./remote` 无漂移；两侧 schema 判决一致 |
| E-006 | cmd | `node scripts/mutate-dsh-client-contract.mjs` | pass（`RESULT: ALL 7 MUTATIONS CAUGHT` + 正控全绿） | 断言确实在咬（DHR_26 §17 教训的强制动作） |
| E-007 | test | `node --test`（relay-control-pilot 全量） | pass（167/167，fail 0） | 批 1 无回归 |
| E-008 | cmd | `Get-FileHash` 对比 profile 安装产物 vs 源文件 `lib/client.js` | pass（`MATCH: True`，`67B8510F…62BC`） | **零构建步骤**：安装的就是手写源文件本身，§4.4「只能在 monorepo checkout 内构建」不命中 |
| E-009 | cmd | `<pilot>/evidence/dhr49/batch1/probe-transcript.md` + `panel-settings-relay.png` | pass（`host-value` = `67fb18b3…612c`，与 DHR_26 四轮转录逐字符相同；刷新/重启后一致；控制台零 error） | 树外 client bundle 可加载 ∧ `ctx.remote.$mount` 自挂手写描述符成立 ∧ 数据真从 Host 穿到浏览器 |
| E-010 | review-dispatch | Agent tool | observed | 复核派出：fresh-subagent-cp1（general-purpose, 零上下文）｜CP1 批1小审：树外Client Bundle可行性探针（src/dsh-client + 3份测试 + 变异脚本 + evidence/dhr49）只读复核；brief=workspace/DHR_49/review-brief-cp1.md |
| E-011 | test | `node --test test/dsh-client-grouping.test.mjs`（实现前 / 后） | observed 7/7 红 → pass 7/7 绿 | TDD：P4-DM6 的三条镜像断言先于实现钉住 |
| E-012 | cmd | `node scripts/mutate-dsh-client-grouping.mjs` | pass（`RESULT: ALL 6 MUTATIONS CAUGHT` + 正控全绿） | 镜像断言确实在咬：6 种「偷看 run_status / 自定优先级 / 兜底节 / 改词表」变体逐条见红 |
| E-013 | test | `node --test`（relay-control-pilot 全量） | pass（174/174，fail 0） | 批 2 无回归 |
| E-014 | cmd | `<pilot>/evidence/dhr49/batch2/dm6-mirror-assertions.md` + `mutant-fixtures/` | pass（双向成立） | **P4-DM6 真机双向对照**：①只改 `group` → run 换节且状态芯片仍显旧状态、`running` 节整节消失；②只改 `run_status` → 分节骨架与基线逐项相同、仅芯片改字 |
| E-015 | cmd | `<pilot>/evidence/dhr49/batch2/list-screen-baseline.png` | pass | 列表屏首次渲染：5 条 run 分 4 节，节序 = 源头 `runs[]` 首现顺序，标题词表与 CLI 一致 |
| E-016 | cmd | `<pilot>/evidence/dhr49/batch2/list-screen-overlay.png` + a11y 树转录 | pass | **落点改版后**：侧栏底部 `Relay` 入口按钮 → `shell.overlay` 全屏浮层；刷新后重建一致（4 节 / 5 条 / fixture 67fb18b3 全同） |
| E-017 | review | CP1 复核结论（`cp1-review` fresh subagent，只读，零上下文） | observed（changes-requested：2 P1 / 7 P2 / 6 P3；批 1 六条声称逐条复跑对拍**全部属实**） | 轮 1 批次小审已做；发现与处置逐条见 `review.md`「CP1 发现与处置」 |
| E-018 | cmd | `<pilot>/evidence/dhr49/batch1/src-snapshot/`（10 文件 + `SHA256SUMS.txt`）、`batch1/dsh-host-baseline.txt`（23 文件） | pass（`client.js` = `67b8510f…62bc`，与 E-008 记录的安装哈希逐字符相同） | **P1-1 已修**：批 1 源码快照抢救并永久落档；`src/dsh-host/` 内容基线补上（此前只有 mtime 佐证） |
| E-019 | test | `node --test test/dsh-client-host.test.mjs` | pass（6/6，非跳过） | **P1-2 已修**：宿主半边从零覆盖到 6 条断言，**用真库不用 stub**——`markRemote()` 的手工装饰器施加、两侧描述符集合、`_zod` 品牌硬查、两侧 schema 判决一致、`list()`/`hash()` 原样转发、注入声明 |
| E-020 | test | `node --test test/dsh-client-typert-drift.test.mjs test/dsh-client-package-contract.test.mjs` | pass | P2-1/2-3/2-4 与 P3-2/3-3 已修：描述符按 id 全量配对（新加不加用例就红）、`dsh.bundle.patch` 与 `files` 双向核对、require 改白名单、注释路径与 README 补齐 |
| E-021 | cmd | `node scripts/mutate-dsh-client-contract.mjs` | pass（`RESULT: ALL 15 MUTATIONS CAUGHT` + 两份正控全绿） | P2-2/P3-1 已修：变异由 7 条扩到 15 条并覆盖防漂移测试；每条变异 apply 后核对 sha256 确实变了，锚点漂了报「变异未生效」而非「没咬」。**扩容期间正控当场逮到我自己写的一个 realm bug**（vm 数组 prototype 导致 deepEqual 误报），已修 |
| E-022 | test | `npm test`（`scripts.test` 已由四份旧测试改为 `node --test`） | pass（185/185，fail 0，skipped 0） | P2-7 已修：复跑者按 `npm test` 拿到的不再是 partial 绿；全量含本卡 19 条新断言 |
| E-023 | review | CP1 返工收敛复检（同一 fresh 实例，只读） | observed（两条 P1 收敛；新提 2×P2 + 3×P3，其中一条是**真洞**） | 复核者独立复跑三条数字全对；逐条核了 SHA256SUMS 10 条哈希与 dsh-host 基线 23 条 |
| E-024 | test | `node --test test/dsh-client-host.test.mjs` | pass（6/6，skipped 0） | **补洞**：第一版测试自己把 initializer 应用到裸实例，删掉构造器那行仍全绿。改为真 Cordis Context + `ctx.plugin()` 走**真实构造路径**，并顺带验 `typertRemote` 绑定 |
| E-025 | cmd | `node scripts/mutate-dsh-client-host.mjs` | pass（`RESULT: ALL 10 MUTATIONS CAUGHT` + 正控全绿） | 把复检者手工试过的四个变体固化成常驻变异（构造器不跑 initializer / addInitializer 空实现 / context 字段不合契约 / 漏 markRemote），另加 6 条。**跑的时候又逮到我测试里的一个 bug**：客户端面用写死相对路径导入，变异改副本而测试读真包，那条变异当场没咬——已改为全部经 `PKG_ROOT` |
| E-026 | cmd | `node scripts/mutate-dsh-client-contract.mjs` | pass（`ALL 16 MUTATIONS CAUGHT`，新增计算式 require 一条） | P3 补：白名单只认字面量，`require(NAME)` 能溜过去；加 `require(` 出现次数必须等于字面量匹配数 |
| E-027 | cmd | `<pilot>/evidence/dhr49/batch1/src-snapshot/SHA256SUMS.txt`（附注） | observed（如实标注一条不实） | 复检者指出 `load-client-bundle.mjs` 那条是批-2 版本（抢救晚了 36 秒），批-1 原件已不存在。**按其建议如实标注而不补造假文件**；差异仅为新增 `export const PKG_ROOT`，round-1 结论不受影响 |
| E-028 | test | `npm test` | pass（185/185，fail 0，skipped 0） | 全部处置后无回归 |
| E-029 | 人判 | 用户亲跑 DSH（`http://127.0.0.1:3080`，本机安装版 `client.js`/`index.mjs` 哈希与源同）并在会话头部 tab 条查看 | observed（用户 2026-08-21 原话「对，是你说的」） | **v3 落点渲染的人判证据**：Relay 页签出现在 Chat / Trajectory 旁，点进去是列表屏。AI 侧仍只到 `slot-registered`（原生文件夹选择框驱动不了），不得改写成 AI 已验 |
| E-030 | cmd | `node bin/relay-pilot.mjs list testdata/fake/runs-active.json`（split 与 grouped 两版）+ `node bin/relay-pilot.mjs show testdata/fake/run-basic.json` | observed（输出已原样回贴给用户） | 回应 U-04：确认 CLI 分两层——`list` 一行一个 run，「接力计划（4 步）」棒序表**只在 `show` 详情**里。据此对齐词表（findings「接力计划／run／workflow 词表对齐」），并核出 `workflow_name` 是模板名、不唯一 |
| E-031 | 决策 | 用户 2026-08-21 点选：三分桶分类法「作废，改记原话」 | decided | 中途闸实跑时用户四道题全部弃桶。已改 DevPlan §3.2 口径（B-13 块）：人判项不再要求用户逐条点选 (i)/(ii)/(iii)，改为原样登记判词 + 主会话核对工程影响面 + 交用户确认；规则 2 作废，规则 1/3 继续有效。**插入该块时反引号被 shell 吃掉一段路径，已回读发现并用 Edit 修复**——见 L-14 |
| E-032 | 决策 | 用户 2026-08-21 点选：批 4「放行，开干」 | decided | 中途闸解除。形态按候选甲：列表行补「流程」列（`workflow_name`）+ 详情屏承载接力计划棒序表；不做按 workflow 分堆。§4.4 止损条件无一命中 |
| E-033 | test | `node --test test/dsh-client-detail.test.mjs` | pass（9/9，skipped 0） | 批 4 新断言：棒序=nodes[] 源顺序、镜像①只改 depends_on 行序不变、镜像②只改 nodes[] 行序跟着变、词表外 role/status 原样打印、缺 attempt/空 depends_on 打破折号、空 nodes[] 走「（无）」、与 CLI `show` 逐节点对照、待处理事项五列、卡片「流程」行与点击/键盘交出 run_id。**写的时候两条断言是我自己猜错的**（节点 id 猜成 s2-implement/s5-closeout，实际 s2-build/s5-verify；跨客户端对照拿整行拼串去比，测的其实是 CLI 的折行算法），跑出来才改对  （**已被 E-046 取代**：详情屏断言此后扩到 11 条） |
| E-034 | test | `node --test test/dsh-client-unload.test.mjs` | pass（4/4，skipped 0） | 卸载清场客户端半边：disposer 撤 Remote + 摘 `<style>` + 删探针全局（要求删键而非置 undefined）；slot inject 面交 loadList/loadRun 两把钥匙；`ok:false` 必须抛错。**测试头显式写明不证「Cordis 会调这个 disposer」**——那由真机 HMR 证据给（E-037） |
| E-035 | cmd | `node scripts/mutate-dsh-client-detail.mjs` | pass（`ALL 19 MUTATIONS CAUGHT` + 两份正控全绿） | 19 条含两条要害：「按 depends_on 拓扑排序」「把在跑那棒提到最前」——都是看着像改进、实则让客户端替源头做主的写法，镜像①把两条都逮住  （**已被 E-080 取代**：变异总数此后到 80 条） |
| E-036 | cmd | `node scripts/mutate-dsh-client-host.mjs` | pass（`ALL 13 MUTATIONS CAUGHT`） | 加 `get` 后原「list 漏标」锚点因行尾多了逗号而失效，脚本报的是**「变异未生效」而不是「没咬」**——E-021 那道 sha 校验在起作用。修锚点并新增 3 条（get 漏标 / get 偷排棒序 / get 把 null 翻成异常） |
| E-037 | 真机 | HMR 热重载对照（两轮），记录见 `<pilot>/evidence/dhr49/batch4/hmr-disposer-transcript.md` | pass（`pageDidNotReload:true` ∧ `probeObjectReplaced:true` ∧ `styleTagIsNewNode:true`） | **CP1 P2-5 收敛**：不再是「代码里有 disposer」。判据用探针对象的**身份**（工厂用 `?? `复用，没删就同一对象），并用一个与插件无关的 window 标记排除「整页刷新」这个替代解释。P3-5（探针全局没清）一并收敛 |
| E-038 | 真机 | 卸载冷启动：`dsh plugin remove` → 重启 → 刷新页面 | pass | Relay 页签消失（只剩 对话/轨迹）、`style[data-plugin-css]` 中无 relay 项、`window.__RELAY_PANEL_PROBE__` 为 undefined、`.rp-page/.rp-run/.rp-table` 计 0；**官方两个页签完好**，启动日志无 error。截图 `after-uninstall-no-relay-tab.png` |
| E-039 | 真机 | 浏览器内驱动详情屏：列表→点卡片→详情→返回→键盘 Enter 进详情 | pass | 详情屏渲染出「接力计划（5 步）」表，六列与 CLI `show` 逐格相同；返回列表后五张卡片各带「流程」行；点 `fake-run-0007`（宿主无详情 fixture）显示「该运行…没有详情，宿主未为它配置详情 fixture。」而非报错。截图三张存 `evidence/dhr49/batch4/` |
| E-040 | cmd | `npm test` | pass（199/199，fail 0，skipped 0） | 批 4 净增 13 条断言，无回归  （**已被 E-049 / E-080 取代**：全量此后到 216/216） |
| E-041 | cmd | 安装副本 vs 源码 SHA256（4 个文件） | pass（逐字节相同） | 零构建步骤的结论在批 4 仍成立。**但发现一个配方缺口**：版本号不变时 `dsh plugin add` 会报 "Already up to date" 并**静默保留旧内容**（pnpm 按版本去重），必须先 `remove` 再 `add`——见 F-005 |
| E-042 | 真机 | 为 dh-relay 建 DSH 工作区 | pass（重启后仍在） | `storages/workspace.json` 新增 `D:\MyFiles\ai-workflow\dh-relay`，侧栏出现 dh-relay。**原生文件夹框的绕法**：`dsh-host-directory-picker-auto` 按环境选后端，置 `SSH_TTY` 即解析为 `browse`（页内选择器，可自动化驱动），不必改绑定地址、不对外暴露；加完即以常规环境重启 |
| E-043 | 真机 | 新建工作区里的空会话看页签 | observed（`tabs: []`） | **固有代价①再次坐实**：hero 态整个会话头隐藏，Relay 页签不可达。工作区建好≠面板可见，还需该会话里有一次交互 |
| E-044 | 真机 | 输入框与面板的关系查证（用户提问触发） | observed | 输入框是 `ConversationRoot` **无条件渲染**的 composer seat，三个页签共用，`conversation.view` 条目删不掉它。**中途我自己查错过一次**：用 `[class*="InputBar"]` 查，得出「轨迹页签下输入框不存在」——CSS Modules 把类名哈希成了 `uV2eYG_*`，那个选择器永远匹配不上。改用 `[data-composer-seat]` 复查，三个页签**都在**，真正的差别是 `position`：轨迹 `absolute`、Relay `sticky` |
| E-045 | 真机 | 按官方 Trajectory 的做法改浮层与底部留白 | pass | 屏根加 `data-conversation-composer-overlay`（ConversationRoot 用 `:has()` 读它，把 seat 翻成 `absolute` 并把滚动交给视图），底部留白用官方发布的 `calc(var(--dsh-composer-height,152px) + 16px)`。实测：`seatPosition: absolute`、`paddingBottom: 142px`（=126 实测高 + 16）、滚到底 `lastCardCovered: false`。截图 `list-screen-composer-overlay-fixed.png` |
| E-046 | test/cmd | `node --test test/dsh-client-detail.test.mjs` + `node scripts/mutate-dsh-client-detail.mjs` | pass（11/11；`ALL 22 MUTATIONS CAUGHT`） | 新增两条断言盯这个布局：①共享 `PAGE_PROPS` 必须带浮层标记 ②源码里不许再出现 `{ className: "rp-page" }` 字面量、且 7 个屏根都走 `PAGE_PROPS` ③底部留白必须来自变量、不许写死像素。三条各配一条变异，全见红。**写断言时 `PKG_ROOT` 忘了 import，两条当场红**——顺带按 L-12 的教训在 import 处写清为什么不能用写死相对路径 |
| E-047 | test | `node --test test/dsh-client-read-model-purity.test.mjs` | pass（12/12） | **批 4 的 4.5 / P4-DM5b**，此前完全没做。四段：§A 校验器自证（真 Cordis `Context`／`Fiber`、函数、getter、循环、undefined、symbol、NaN、Date、Map、class 实例逐个必须被逮住；真 fixture 必须零违规）→ §B 与 DHR_26 `isPlainJson` 交叉判决对照 → §C 真 Cordis + 真 repository + 真 fixture 下网关的 `hash`/`list`/`get`（含 null 分支）逐个验纯 → §D 真 `apply()` 跑出来的 `loadList()`/`loadRun()` 与探针全局验纯，并与宿主给的模型 `deepStrictEqual` |
| E-048 | test | `node scripts/mutate-dsh-client-purity.mjs` | pass（`ALL 12 MUTATIONS CAUGHT`） | 变异刻意分两类：**①宿主网关往 Read Model 掺活对象**（挂 `this.ctx`、挂 `this`、贴 `refresh()`、把 `run_id` 做成 getter、缓存 `list()` 让两次调用共享同一对象）；**②客户端消费路径加料**（整个信封交下去、加自算字段、塞 `reload` 回调、给详情贴 `scope`、探针记 `scope`、探针记 `Date`、探针写回 `detail: undefined`）。12 条全见红，未变异正控全绿。**⚠️ 2026-08-21 按 CP4 复核补归因拆分**：这 12 条里只有 **9 条**是被 DM5b 那张纯 JSON 网逮住的；另 3 条各有别的原因——①「信封整个交给视图」实际死在 `client.js` 抛 `TypeError`（**代码崩了，不是断言咬了**）②「加自算字段」被 `deepStrictEqual` 那条逮住 ③「网关缓存 list()」被 §C「不共享内部快照」逮住。「12 条全见红」本身没错，但把它整个当成"纯 JSON 断言在咬"的归因偏强。变异总数此后扩到 17 条（E-080） |
| E-049 | test | `npm test` | pass（213/213，fail 0，skipped 0） | 净增 12 条。**增量是实测出来的不是推的**：把新测试文件临时移走再跑，得 201/201，移回得 213/213（E-040 记的 199 是更早的快照，中间还长过 2 条） |
| E-050 | 真机/代码 | 探针 `record()` 的 `detail: undefined` | fixed | §D 第一次跑就红在这：`probe.stages` 里躺着 4 个 `{ stage, detail: undefined }`。**这不是断言太严**——探针要被 DevTools 读出来抄进证据转录，而 `JSON.stringify` 会把 undefined 值的键**静默丢掉**，转录和控制台里看到的就不是一回事。改成 `detail === undefined ? { stage } : { stage, detail }`，并配一条变异盯住它不许改回去 |
| E-051 | 真机 | **4.4 装卸清理三态**：`--dump-config` 装着／卸掉／装回各取一次 → `evidence/dhr49/batch4/lifecycle.txt` | pass | 组合树 **505 → 502 → 505**；**A 与 C 逐字节相同**（`cmp -s` → IDENTICAL）；A 与 B 的 `diff` **恰好只有面板那三行**（`# == @personal/dsh-relay-panel` / `- id: relay-panel-host` / `name:`），无连带改动。profile 的 `dsh.profile.bundles` 与 `dependencies` 同步进退，`node_modules` 里的包目录 present→absent→present。**DHR_26 的 `relay-pilot-host` 三态恒为 1 行**，变更范围与任务卡「不改 `src/dsh-host/`」一致。装回后四个文件 SHA256 与源码逐字节相同 |
| E-052 | — | E-051 的量程（照实登记，不粉饰） | 限制 | `--dump-config` 读的是**组合出来的配置树**，只回答「配置里还在不在」，**回答不了「运行时清干净了没有」**——DHR_26 轮 2 复核当年就点过这个限度。运行时那半由 E-037（HMR 触发 disposer，`<style>` 被摘、探针键被 `delete`、探针对象身份变了从而排除"页面整个重载了"）与 E-038（冷卸载重启后 Relay 页签消失）承担。另：包外 absence 探针查的是 `ctx.relayPilot`（DHR_26 的服务），**不查 `relayPanel`**，所以本卡宿主半边没有 removed 态服务探针，这一格记限制不记 pass |
| E-053 | 真机 | **4.3 详情屏 DSH 重启复验**：杀掉 3080 进程 → 重启 `dsh --profile web` → 进会话 → Relay 页签 → 点 `fake-run-0001` | pass | 探针 stages 完整（`bundle-loaded`→`apply-called`→`remote-mounted`→`slot-registered`→`host-value`→`host-detail`），棒序表 4 行 + 待处理事项表 1 行，内容与重启前一致。截图 `detail-after-dsh-restart.png` |
| E-054 | 真机 | **4.3 详情屏刷新复验**：F5 后重走同一条路径，比对屏内全文 SHA256 | pass | 刷新前后 **`d70cac6e5211…` 逐字符相同**，长度同为 680。刷新前往 `window` 上写了 `__RELAY_43__`，刷新后读到空——**与"页面重载了"一致**。⚠️ **2026-08-21 按 CP4 复核降档措辞**：这只是**否定控**，排得掉"同一个 window 还在"，排不掉"读的是另一个 eval 上下文 / 另一个 frame"（那种情况同样读到空）。E-037 那次做法更强：标记**存活**证明没重载 + 探针身份变证明 disposer 跑了，一正一负。本条改记为「与刷新一致，未排除 eval 上下文切换」 |
| E-055 | 真机 | 改完 `record()` 后回归浮层布局 | pass | `data-conversation-composer-overlay` 仍在、seat `position: absolute`、`.rp-page` 的 `padding-bottom` = **142px**（=126 实测高 + 16）、滚到底最后一张卡未被压（747 < 763）。**中途读到一次 `paddingBottom: ""`**——那是在 React 已经把节点换掉之后读的旧节点，脱离文档的元素 `getComputedStyle` 返回空串；在活节点上重读才是 142px。没把这个空串当成回归报上去 |
| E-056 | 真机 | **批 3 判据①清净重跑** → `evidence/dhr49/batch3/clean-rerun.txt` | pass | 停服务 → `plugin remove` → 删光 `dist/*.tgz` → 核清场（tgz 0 个、profile `bundles`/`deps` 都没本包、安装目录没了、pilot 本来就没有 `node_modules`）→ 照 README 重跑。**装出来的四个 bundle 文件与清场前逐字节相同**；页面上 `RELOADABLE`，探针六段齐全，`fixture_hash` 仍为 `67fb18b3d7d8…`（与 DHR_26 四轮一致），详情屏全文 SHA256 `d70cac6e…` 与 E-054 同 |
| E-057 | cmd | tgz 哈希变了（`62d66053…`19781B → `a557dbe3…`21117B）的归因 | 查清 | **不是不确定性**：`npm pack` 对同一份源码连打两次得**同一个哈希**（实测）。变的原因是中途改过 `README.md`，而 **npm 无条件把 README 打进包**（`tar -tzf` 里能看到 `package/README.md`，即使它不在 `files` 白名单）。所以「装的是不是这一版」只能看**安装副本 vs 源码**逐文件哈希，不能看 tarball——已写进 README |
| E-058 | cmd | 配方补丁：干净状态下 `plugin remove` 会 exit 1 | 记录 | 从**全新**状态起步时 README 第 2 步的 `remove` 报错退 1（pnpm 抱怨要删的依赖本来就不在），后面 `add` 不受影响。这一步不能因此删掉——它是防 F-005 的。README 已改成「干净状态下 exit 1，忽略即可」，否则照着重跑的人会卡在第一条命令 |
| E-059 | cmd | **批 3 判据②换机重跑**·首探 12:00 | **已被 E-063/E-064 取代（结论过期）** | 当时：`ssh thinkpad` → `Connection timed out`（`<tailnet-ip>`）；ping 全丢；`tailscale status --json` 显示本机后端 `NoState`。据此登记「目标机不可达、判据②未能开始」。**半小时后 tailnet 连上，此结论作废**——保留本行是为了留下判断链，不是因为它还成立。教训见 L-22 |
| E-060 | cmd | 提前量出「装树不在的机器上跳几条」 | pass（skipped 11） | 把 `RELAY_PANEL_DSH_PACKAGE_JSON` 指向不存在的路径模拟「目标机没装 DSH」：`tests 213 / pass 202 / fail 0 / **skipped 11**`。跳的恰好是必须用真装树的那 11 条——`dsh-client-host` 7 条 + purity 的 §A Cordis 正控 1 条与 §C 3 条。**换机真跑起来时跳了几条必须如实记**，目标机没装 DSH 的话「202/202 全绿」不能说成全套通过 |
| E-061 | review | **CP3 批 3 小审**（fresh agent，只读，brief=`review-brief-cp3.md`） | changes-requested | 12 条发现。**其中 1 条我驳回**：它说 `mutate-dsh-client-detail.mjs` 只有 20 条变异，实跑输出 22 行 `✔ 见红` + 2 行正控、末行 `ALL 22 MUTATIONS CAUGHT`——它是按 `    name: '` grep 计数，漏掉了单行字面量写法的变异体。其余 11 条全部采纳并已落实 |
| E-062 | cmd | CP3 P1 处置：warm install 坐实 + 冷 store 补跑 | 认了并补跑 | 独立复验：`fsutil hardlink list` 证明 profile 里的 `client.js` 与 `.pnpm-store/v11/files/8b/c5a1f6…` 是同一文件（links=2，mtime 早 11 分钟）；store 文件名 = 内容 sha512 十六进制（本包六文件逐条对上）。补跑「全新空 store + 全新空项目」冷装：`reused 0, downloaded 1`，四个 bundle 文件与源码**逐字节相同**。转录 `_raw/08`、`_raw/09`。**根因在我**：`task_plan.md` 3.2 把卡里的「依赖缓存」写成了「node_modules」，已就地更正 |
| E-063 | cmd | 目标机可达性**复测** | observed（与首探相反） | 12:31 补留痕时重跑同一组命令：Tailscale 后端 **Running**、对端 `<目标机>`（ssh 别名 `thinkpad`） **online**、ping **0% 丢包**、`ssh thinkpad` 直接 `REACHABLE` 退 0。**环境类否定结论有时效**，不能一次探测吃到收口（L-22） |
| E-064 | 真机(跨机) | **批 3 判据②真跑** → `evidence/dhr49/batch3/cross-machine.txt` + `_raw/13` | **部分成立** | 目标机 Ubuntu 24.04 / Node **v18.19.1** / npm 9.2.0 / **未装 DSH**。源码经 tar+scp 同步（150KB，无 `npm install` 这一步——本 Pilot 无依赖，被跨机实证）。`npm test` → **213 / pass 202 / fail 0 / skipped 11**，与 E-060 的预测逐数字吻合；整套断言在 Node v18 上一条不改照跑。**缺最后一格**：目标机没装 DSH，「产出可加载 bundle」未证；装 DSH 属于在用户另一台机器上装软件，未擅自执行 |
| E-065 | 真机(跨机) | 换机重跑抓出配方缺陷 | fixed | 照 README 敲 `npm pack --pack-destination ../../dist` 在目标机**直接失败**：`ENOENT … dist/…tgz`。根因：`--pack-destination` **不会自建目标目录**（npm 9.2.0）；本机一直没事只因为 `dist/` 早就在了——配方从没在"目录不存在"的状态下被走过。`mkdir -p` 后成功。README 已补这一步。**这正是换机重跑存在的意义**：把「只在作者机器上成立」的隐含前提逼出来 |
| E-066 | cmd | 跨平台产物比对 | observed | 同一份源码：tarball sha256 **win32 `f72c6cf5…` ≠ linux `968e668f…`**；但目标机解开自己打的包，四个 `lib/` 文件的 sha256 与本机**逐字符相同**。即 **交付物（bundle）跨机可复现、外包装（tarball）不可**。加上此前已知的「改 README 就变」，tarball 哈希在两个维度上都不能当「装的是不是这一版」的判据 |
| E-067 | 真机 | 清净重跑后页面零 error/warn | pass | DevTools 控制台按 `error`/`warn`/`assert` 过滤 → 空。**配了正控**（L-18）：同一查询按 `log` 过滤能取到 6 条 `[relay-panel]` 日志，所以「没有报错」是真的没有，不是查询查空了 |
| E-068 | cmd | CP3 P2 处置：补核心判据的原始留痕 | fixed | 此前判据①的核心结论（`RELOADABLE`／探针六段／`fixture_hash`／详情屏 SHA256）只有叙述没有转录。已补 `_raw/08`（冷 store 重跑）、`_raw/09`（warm 硬链证据）、`_raw/10`（探针原始 dump）、`_raw/11`（`npm pack` 连打两次同哈希 + tarball 成员 mtime 旁证）、`_raw/12`（可达性探测两次）、`_raw/13`（换机实跑全程） |
| E-069 | review | **CP3 二轮**：我把处置发回去后它的复审 | 3 条异议，全部采纳 | ①**撤回**它一轮那条「detail 只有 20 条变异」——它自己重数得 22，确认是 grep 锚了行首漏掉单行写法；②冷 store 的对比面太窄：只比 `lib/` 四个文件，而决定"接不接得上"的是 `cordis.patch.yml` 与 `package.json`，接缝盖不住；③「被 pnpm 拒绝」是**成本判断不是技术阻塞**——pnpm 只挡"给已存在 profile 换 store"，端到端那条路是全新 `DSH_HOME`，没人挡，只是要 5.5 分钟。它指出本卡在判据②上花整节分"不是跑失败是没跑起来"，这里写"pnpm 不让"就是双标。**认** |
| E-070 | cmd | 按 E-069① 扩冷装对比面 → `_raw/08` 重采 | pass（同 6 / 不同 1） | tarball 七个成员逐一比：`lib/` 四个 + `cordis.patch.yml` + `package.json` **逐字节相同**；`README.md` 不同是预期的（dist 里的 tgz 打于最后一次改 README 之前）。接缝闭合 |
| E-071 | cmd | 按 E-069② 补登第二个缺口 | 记录 | warm 那半的 profile 里 `@personal/dsh-relay-host` 与 `dsh-relay-absence-probe` 是 **`link:` 绝对路径**装的，只有 panel 走 `file:` tgz。全冷环境下这两个必须重装，而冷/warm 两半都没覆盖到它们的重装结果。面板数据全来自 `ctx.relayPilot`，这条不点名会让人以为只差一个符号链农场 |
| E-072 | review | **CP4 批 4 小审**（fresh agent，只读，brief=`review-brief-cp4.md`） | changes-requested（1 P1 / 5 P2 / 9 P3） | 它自己跑了三份测试 + 两份变异 + **另造 6 组反例变异做探洞**，并逐条给了可复现的反例。质量很高，**P1 是真洞**，我逐条复现后全部采纳 |
| E-073 | test | **CP4 P1 复现**：活对象挂在**数组自身**属性上 | 复现成立 | 把宿主 `list()` 改成 `model.runs.ctx = this.ctx`——一个**真 Cordis Context** 就这么进了 Read Model，而 DM5b 的 12 条断言 **12/12 全绿放行**。原因：`walk()` 的数组分支只 `forEach` 索引元素、从不看数组自身的 own key；`intoThisRealm` 用 `for...of` 重建、同样丢掉这些键，于是往返那条网**跟着一起失明**。而 Read Model 的主结构恰恰是 `runs[]` / `nodes[]` / `attentions[]` |
| E-074 | test | E-073 的修复 | pass（14→17 条断言） | 数组分支补两件事：①先过 `isPlainArrayRooted()`（挡 `class RunList extends Array`）②把非索引、非 `length` 的 own key 记违规；`intoThisRealm` 同步**保留**这些键。**中途踩了一下**：第一版直接复用 `isPlainRooted()`，而普通数组的原型是 `Array.prototype`（其原型是 `Object.prototype`，不是 null），于是真 fixture 全被误判——8 条当场红。数组要单独一个判据，比对象深一层 |
| E-075 | test | **CP4 P2 复现**：「客户端不加料」那条是拿同一个引用跟自己比 | 复现成立，已修 | stub 把 `answers.list` 原样交回客户端，测试再拿它当期望值——**同一个对象**。实测：客户端就地写 `both[0].client_run_count = …`（不换对象）→ 12/12 全绿。变异脚本那条之所以见红，只因为它写成了 `{ ...both[0], … }` **换了对象**。改成跟**新读一份**比 |
| E-076 | test | **CP4 P2 复现**：面板与 CLI 的「用时」不是一个词 | 复现成立，已修 | 同一 fixture（`elapsed_seconds: 1500`）：CLI 打 **`用时 25 分钟`**，面板打 **`25 分`**；CLI 有「天」这一档面板没有（48 小时：CLI「2 天」/ 面板「48 小时 0 分」）；非法值 CLI 原样打印、面板打「—」。而本卡第 3 条主张正是"两个客户端共用一套词表"，此前只逐节点对了 role / node_status。面板的 `duration()` 已从 `src/render/text.mjs` **逐行照抄**，并补了一条 14 个取值的跨客户端对照断言 + 3 条变异 |
| E-077 | test | **CP4 P2 复现**：`roots === 7` 那条魔数断言是**反的** | 复现成立，已修 | 实测：合规新增一屏（走 `PAGE_PROPS`）→ 计数 8 → **红**（逼人改魔数）；违规新增一屏（手写 `{ className: "rp-page", role: "main" }` 忘打浮层标记）→ 计数仍 7 → **绿**。而后者正是这条断言存在的唯一理由。换成不含魔数的不变量「全文件只许出现一次 `className: "rp-page"`」，**双向各验一次**：违规 → 红，合规 → 绿 |
| E-078 | test | CP4 P2：`get(runId)` 参数 codec 两侧从没被对照过 | 已补 | 原来的循环只比 `result.schema`，一个字不碰 `parameters`；drift 测试里的 `PARAM_CASES` 比的是"客户端内联 vs 客户端导出"，两边都是手写那份，跟真 zod 无关。今天两侧判决恰好等价（不是现存 bug，是缺网）。已把 host 测试的循环扩到 `parameters` + 覆盖强制断言，并加 3 条变异（宿主单方面收紧 / 放宽 / wire 名漂开） |
| E-079 | test | CP4 P2：§C 镜像覆盖面偏窄 | 已补 | 原镜像只掺在 `listRuns` 一条路径、四种掺法全是"对象字段"型，`get()` 那条纯 JSON 断言**没有对应镜像**。已加：`get()` 专属镜像 3 格 + 数组型掺法 2 格 |
| E-080 | test | 上述修复的变异验证 | pass | `mutate-dsh-client-purity.mjs` **12 → 17 条**（新增：ctx 挂 runs 数组、runs 换 Array 子类、ctx 挂 nodes 数组、客户端就地改、scope 挂 runs 数组）；`mutate-dsh-client-detail.mjs` **22 → 25 条**（用时三条）；`mutate-dsh-client-host.mjs` **13 → 16 条**（参数 codec 三条）。**五份合计 80 条全见红 + 各自正控全绿**；全量 `npm test` **216/216** |
| E-081 | 真机(跨机) | **判据②：装 DSH 前先撞两堵没写进文档的墙** | 记录 | ①**DSH 要 Node ≥ 20.12 但没有 `engines` 字段**——系统 Node v18.19.1 上装完直接 `SyntaxError: … does not provide an export named 'parseEnv'`；②**DSH 管插件要 `pnpm`**，目标机没有。两条都**没动目标机系统**：便携 Node v24.12.0 + corepack pnpm@11.1.2 都放在临时目录里 |
| E-082 | 真机(跨机) | **F-013：Linux 上第三方插件一个都加载不了**，根因定位 | 查清并修复 | 全新 `DSH_HOME` 首启报 `Cannot find package '@personal/dsh-relay-host'`，**两个 `@personal` 包一视同仁**（`link:` 与 tgz 都报），所以与装法无关。读 `cordis-plugin-loader` 源码定位：`loader.internal` 拿不到 → 退化成从加载器自己位置做 `import()` → 永远走不到 profile 的 `node_modules`。而 `loader.internal` 依赖平台原生插件 `node-addon-require-builtin`，目标机报 `No usable native binding found for …-linux-x64-gnu`。**不是没发布**（`npm view` 有 `0.1.5`），是系统 npm 9.2.0 装 DSH 时跳过了 optional 平台包；`--include=optional` 重装即修好。**报错说"找不到我的插件"、真正断的是原生插件，中间隔两层** |
| E-083 | 真机(跨机) | **全冷环境跑通**：全新 `DSH_HOME` + 装 Host + 装 panel + 首启 | pass | 安装副本与源码**六个成员逐字节相同**；headless probe 答 `relay-pilot-host-ready`、`plain_json: true`、`fixture_hash` = `67fb18b3d7d8…`——与本机基线和 DHR_26 四轮转录**逐字符相同**。顺带修正一条本地经验的适用范围：符号链农场在 Linux 上建了 **1 秒**，而本机 Windows 是约 5.5 分钟，那条「不可中断、预算 5.5 分钟」的警告是 **Windows 特有的** |
| E-084 | 真机(跨机) | 客户端 bundle 在目标机浏览器里加载 | pass（四步齐全） | 经 `ssh -L 3081:127.0.0.1:3080` 隧道，用本机浏览器打开**目标机的** DSH：探针 `bundle-loaded` → `apply-called` → `remote-mounted` → `slot-registered`，且 `<style data-plugin-css>` 已注入。**差最后一格**：Relay 页签未出现，因为该实例没有开着的会话（F-007 固有代价①）；开会话要在目标机上发消息、需要该机 LLM 凭据，**我没有去配**。这一格记未证，且不是 bundle 的问题 |
| E-085 | 真机(跨机) | 目标机全套测试（装 DSH 后） | pass（216/216，skipped 0） | 不设 `RELAY_PANEL_DSH_PACKAGE_JSON` → `skipped 12`；设成目标机自己的装树 → **skipped 0**。**实证了 CP3 提前指出的陷阱**：不设变量会让"装了 DSH"看起来像"没装"，而跨机最该证的那 11 条恰好在被跳的那批里 ⚠️ **2026-08-21 按轮 2 复核补口径**：换机那轮跑的是 **216 条的树**，不是当前这棵（此后随三轮返工到 228 条）。新增的那些条都是纯 JS（稀疏数组 / `-0`、词表解析器、守恒断言），与平台无关，风险极低；但收口引用时口径必须是「**换机时点 216/216**」，不能拿当前总数往回套。 |
| E-086 | 真机 | 本机重装最新 bundle 并复验「用时」 | pass | 重打包重装，**七个成员全部逐字节相同**（README 也对上）。真机详情屏现在打 **`用时 25 分钟`**，与 CLI 一致 |
| E-087 | test | **把「往返那条网不是冗余」钉成断言**（CP4 收尾提示②） | pass（217/217，skipped 0） | 原先只有文件头一段注释在说明"往返深等不是冗余"，注释拦不住后人当冗余删掉。新增 §A 一条测试，用 CP4 给的两个反例钉死：**稀疏数组**（`purityViolations` 全绿，因为 `forEach` 跳空洞；而 JSON 往返把洞变 `null`）与 **`-0`**（walker 只看类型；JSON 往返抹成 `0`，只有 `deepStrictEqual` 的 `Object.is` 语义分得开）。两条都断言 **walker 全绿 ∧ `assertPureJson` 抛**——删掉往返那行即见红。变异脚本不覆盖这条（变异改的是产物代码，删测试断言不在其射程内），保护机制就是这条测试本身 |
| E-088 | cmd | 修完之后复跑 | pass | `npm test` **217/217，fail 0，skipped 0**；`node scripts/mutate-dsh-client-purity.mjs` → `ALL 17 MUTATIONS CAUGHT` + 正控全绿。五份变异脚本合计仍是 **80 条**（本条没加变异，理由见 E-087） |
| E-089 | 复核 | **轮 2 换人复核派出**（硬闸 G6：fresh context、未参与实现、不继承轮 1 会话） | 结论 changes-requested（0 P0 / 2 P1 / 4 P2 / 3 P3） | brief：`review-brief-round2.md`。复核方**独立复现了全部机器数字**：`npm test` 亲跑两遍 217/217 skipped 0；五份变异脚本全跑（contract 16 / grouping 6 / host 16 / detail 25 / purity 17 = 80），与声称一字不差，各自正控全绿。要求返工的不是数字，是**闸口漏了一道 + 证据表停在半路 + 一类断言的方向性空洞** |
| E-090 | 复核 | **轮 2 P2：词表断言是单向的**——只咬面板漂，不咬 CLI 漂 | **复现并接受** | 复核方在 scratch 副本上实测（真包未动）：CLI 侧 `GROUP_LABELS.needs_you`「待你处理」→「该你了」**全绿**；`GROUP_LABELS.running`「运行中」→「跑着呢」**全绿**；`ROLE_LABELS.reviewer`「复核」→「审核」**全绿**。根因：期望值是**手抄进测试的第三份常量**，grouping 那条全程不碰 CLI 输出；detail 那条的 `cliText.includes(row[2])` 兜底被 fixture 里含「复核」二字的节点标题中和。本卡第 3 条主张是「两个客户端共用一套词表」，这张网**只有一半** |
| E-091 | fix | 词表期望值改为从 CLI 定义原文取（新 `test/helpers/cli-labels.mjs`） | pass | 与 duration 那条同形（L-28：期望值必须独立于被测路径获得）。词表住在 `src/render/text.mjs`——本卡**范围外，不许改**，所以不去 export，改成读定义原文正则解析；**解析不到就抛**，不许静默回退硬编码。新增 `test/dsh-client-cli-vocabulary.test.mjs`（5 条）专证解析器不空转：§1 换个词解析结果必须跟着换 + 读真文件逐字对照、§2 词表不见了／写法变了必须抛、§3 拿真源码演算 CLI 漂移必然见红 |
| E-092 | test | **第六份变异脚本** `scripts/mutate-cli-vocabulary.mjs`（6 条） | pass（`ALL 6 MUTATIONS CAUGHT` + 正控全绿） | 与另外五份不同：它**就地改仓内真文件 `src/render/text.mjs` 再恢复**（测试按真实相对路径 import 它，拷副本绕不过）。三道保险：开跑前存原文与 sha256、每条变异 try/finally 恢复、全部跑完再校验 sha256（对不上则报错并保留备份路径）。**轮 2 实测漏网的那三条现在全部见红**，另补三条压住 ROLE/STATUS 两表。跑前跑后 `text.mjs` sha256 均为 `661f70677f5716ac…`，逐字节未变 |
| E-093 | fix | 自己踩了一次全文替换的坑，被新断言当场抓住 | 记录 | §3 演算第一版用 `source.replace(from, to)` 全文替换，而 `running: '运行中'` 在 `STATUS_LABELS` 里**先出现**——替错了表，`GROUP_LABELS` 一个字没动。新断言直接报「演算本身没生效」。改成只在目标词表那段字面量里替。**同一个词在多张表里重复出现，全文替换就是不安全的** |
| E-094 | fix | `mutate-dsh-client-purity.mjs` 补 skipped 判定（轮 2 P3） | pass（`ALL 17 MUTATIONS CAUGHT`） | 五份脚本里此前只有 host 那份查 skipped。这份的 §A Cordis 段与 §C 宿主段要 DSH 装树，装树不可达时是**跳过**而非失败、退出码仍是 0——于是正控会打「✔ 正控全绿」，**伪绿**，跨机重跑最容易撞上。另外三份不用装树，不受影响 |
| E-095 | doc | 按轮 2 P1②把 findings §4.4 止损表推到实测终态 | 记录 | 7 行里 4 行原本停在过程态。其中「清净重跑」那行的理由写的是「pnpm 不让给已存在 profile 换 store」——**那是本卡自己已撤回的说法**（`clean-rerun.txt` §6 与 L-25：判为不划算、主动未跑，写成被挡住是双标）。撤回记在证据文件里，唯一那张给 DHR_50 看的止损表却没跟着改，等于**把成本取舍留成了技术阻塞**。另三行（生命周期 / 重启重建 / 分组推导）一并推到终态 |
| E-096 | doc | 按轮 2 P2 把「v3 渲染 AI 未验」改为分段登记 | 记录 | 原登记写死「AI 侧仍只到 `slot-registered`，不得改写成 AI 已验」，写于 E-042 卸掉原生文件夹框卡点**之前**，此后一直没跟上——是**低报**。低报比高报安全但代价真实：DM3 的机器证会被读成「最终落点上只有人判、没有机器证」，而 DHR_50 人判读的就是这个。改为三段：①E-029 时点卡点未解 ②E-042 卡点解除 ③E-039/E-045/E-053/E-054/E-055/E-067 已取得渲染事实。`review.md` 需求对齐表里同一句低报、以及「自动化驱动不了原生文件夹选择框」这条过期卡点，一并更正 |
| E-097 | doc | README 复现配方的过期数字（轮 2 P2） | 记录 | 停在「全量 213 条（本包 …=51）/ host 13 / detail 22 / purity 12」，实际早已是 217/55/16/25/17。E-080、E-088 明确记了三次扩容，README 没跟。**一张以「数字必须可复跑」立身的卡，唯一的复现配方里数字对不上格外扎眼**。已刷新到当前值并补上第六份脚本 |
| E-098 | doc | `clean-rerun.txt` 量程节理由过期（轮 2 P3） | 记录 | 该文件 12:40 定稿，`cross-machine.txt` 14:10 整份重写，两份没对齐：量程节仍写「『可加载』未证（目标机未装 DSH）」，而目标机后来装上并跑通全冷环境，未证的那格已收窄成「面板渲染出数据（该实例没有开着的会话）」。**结论「DM2 不得记 pass」不变，错的是理由那一档** |
| E-099 | doc | `review.md` P3-6 那句占位声明范围限定（轮 2 P3） | 记录 | 原写「tailnet IP 与对端主机名已在 2026-08-21 就地占位」读起来像**全域声明**，而 `evidence/dhr49/batch3/_raw/` 里保留着原始转录（真 tailnet IP、SSH 用户、主机名、家目录路径）。已限定成「仅指入仓文档」，并写明 `_raw/` 若将来入仓或被 P4 主报告引用要先过占位。**入仓那侧复核方逐文件扫过：零 IP、零主机名、零凭据** |
| E-100 | 复核 | **CP2 批 2 小审补派**（轮 2 P1①） | 已派出 | 轮 2 查出：`task_plan.md` 的 CP2 原文是两个动作——「派 fresh 小审本批 diff；**然后停下**，请用户亲跑并表态」，实跑**只做了后半**（E-031/E-032 中途闸人判），前半的 fresh 小审**从来没派过**，`review.md` CP2 行至今是空的。而批 2 装的恰恰是 `groupRuns` + 两条镜像断言，即本卡自称「最不可替代的产出」——**最吃重的那一批，恰好是唯一没过独立复核的那一批**。brief：`review-brief-cp2.md`。⚠️ **这是补派，不是当时那道闸**：它看到的树已含批 3/批 4 与三轮返工，口径按「批 2 产出的当前状态」，不考古当时快照 |
| E-101 | cmd | 上述返工后复跑 | pass | `npm test` **223/223，fail 0，skipped 0**（净增 6：词表自证 5 + grouping 那条重写不增条数、purity 稀疏/-0 那条 1）。六份变异脚本：contract 16 / grouping 6 / host 16 / detail 25 / purity 17 / **cli-vocabulary 6** = **86 条全见红**，各自正控全绿 |
| E-102 | 真机 | **列表屏在最终落点上的首载/刷新/重启三次对照**（轮 2 P2，补 DM3 列表屏那半） | pass（三次逐字相同） | 屏内全文 SHA-256 三次均为 `076c9d4a1036b073…`，663 字符，run 顺序三次均为 `0005/0007/0002/0001/0006`（= fixture 源顺序），屏上 fixture 指纹三次均为 `67fb18b3`。**证据强度补齐**：此前详情屏有 E-053（重启）+ E-054（刷新 + 全文 SHA256），列表屏只有 E-015/E-016——那两条取自 v1 与 v2（`shell.overlay`）两个**已废弃**落点，顶不了最终落点的格。转录：`evidence/dhr49/batch2/list-screen-rebuild.md` |
| E-103 | 真机 | 上条的「其实没刷新/没重启」排除，**比 E-054 强一档** | pass（一正一负两道控） | E-054 只有否定控（写标记→读到空），排不掉「读的是另一个 eval 上下文里的旧 window」。本次加了**身份控**：给探针打一个不可枚举的 `__identity__`，刷新后与重启后读到的探针**都是新对象**。两道同时成立才说明面板真的重新装载并重新取数。重启那次另有硬证据：`stages` 从 `bundle-loaded` 重新排起，不是接在旧序列后面 |
| E-104 | 复核 | **CP2 补派复核回结论** | changes-requested（0 P0 / 1 P1 / 5 P2 / 4 P3） | 它独立复跑了 `npm test` 223/223 与 grouping 变异 6/6，确认 E-012 属实；并**去 fixture 里数了**：`fake-run-0007` 是 `run_status: failed` 而 `group: needs_you`，两键**真的分叉**，所以「分节顺序」那条断言不是恒真。也确认源码级切片边界当时是准的（切歪会响亮见红，方向安全） |
| E-105 | 复核 | **CP2 P1：词法网只切函数体，挪一行就绕开** | **复现并接受** | 它造了两个实现：①`__bucket(run)` 把 `run_status` 推导挪进 helper → grouping **7/7 全绿**；②`__visible(run){return run.run_status!=="unknown"}` 前置 filter，**按 run_status 静默丢 run** → 全量 **60/60 全绿**。②是**可达**行为（`unknown` 是 `STATUS_LABELS` 里的合法值，而列表 fixture 恰好没覆盖它），且正是 DM6 要禁的「客户端拿 run_status 替源头做主」。**唯一的网是那条词法 grep，而 grep 挪一行就能绕**——这落在本卡自称「最不可替代的产出」上 |
| E-106 | fix | 三处一起补 | pass | **(a) 切片范围**从「`groupRuns` 函数体」扩到**整个 `//#region grouping`**，并先剥注释再扫（切宽后第一次跑就咬到自己的 JSDoc——那段本来就在解释「只改 run_status 输出必须不变」；词法网该扫代码不该扫散文），另加两条切片自证（region 里必须含 `groupRuns`、剥完注释代码不能被剥没）。**(b) 补守恒断言**：每条 run 恰好出现一次、`run_id` 集合相等——**CLI 侧 `test/list.test.mjs` 早有这条，DSH 镜像漏了**。**(c) 补 run_status 全覆盖**：不动冻结 fixture，在副本上把 7 个 `run_status` 逐个刷一遍（含列表 fixture 没覆盖的 `pending`/`unknown`）再加一轮逐条混合，出现的 run 及顺序必须一字不变 |
| E-107 | test | CP2 那两个 mutant 原样固化 + **归因单验** | pass（`ALL 8 MUTATIONS CAUGHT`） | `mutate-dsh-client-grouping.mjs` **6 → 8 条**。并按 CP2 的归因要求单独验了一次：把 `__visible` mutant 装上，**只跑守恒那两条**（排掉词法网）→ 第二条守恒**见红**。所以它是被**行为断言**咬住的，不是被词法网。⚠️ 同时按 CP2 指出的登记一条**反向归因**：原有的「group 缺失时回退 run_status 推导」那条 mutant **行为上与正确实现等价**（fixture 里 `group` 恒非空，面板边界校验也强制 `group` 非空字符串，右半支在生产不可达），它见红**靠的是词法网**——已在脚本注释里写明，别当行为级证据引用 |
| E-108 | 复核 | **CP2 P2：我新写的词表解析器自己在空转边缘** | **复现并接受** | 它的 docstring 写着「解析不到就抛——响亮地失败，不许静默回退」，实现却只在**全空**时抛。CP2 实测两例：①混引号 `{worker:'执行', reviewer:"复核", system:'系统'}` → 返回 `{worker,system}`，**reviewer 无声消失**；②注释里出现花括号 → `indexOf('}')` 截早，只返回 `{worker}`。**少一个键就少一条被比对的译法**，而那正是这张网存在的理由 |
| E-109 | fix | 解析器改成严格模式 | pass | **(a)** `indexOf('}')` 换成**配对扫描**（跳过字符串与注释）；**(b)** 剥注释后数字面量里有几个 `键:`，与解析出的条数**必须相等**，不等就抛。三条新断言把 CP2 的两个反例连同嵌套花括号一起钉住（嵌套那条还顺带证明配对扫描走到了**最外层**：若停在内层，报的会是 2/3 而不是 2/4） |
| E-110 | fix | `mutate-cli-vocabulary.mjs` 补两道保险（CP2 P2） | pass（`ALL 6 MUTATIONS CAUGHT`） | CP2 指出三道保险挡不住两条路径：①**SIGINT 不展开 `try/finally`**，而每条变异下真文件被改写的窗口是一整轮三文件测试，窗口内 Ctrl-C 会把 `src/render/text.mjs` 留在被改状态——而 `dh-relay-p4-pilot/` **整个不在任何 git 仓库里**，没有 VCS 兜底；②`ORIGINAL` 是运行时从磁盘读的，**脏基线会被当成新基线**，收尾 sha 校验必然通过，等于给损坏盖章。已补：四个信号的恢复钩子 + **已知好基线 sha 常量**（不符就拒绝开跑，并分别提示"去备份拷回"还是"你正当改了词表，把新值写进常量"） |
| E-111 | fix | §3 演算把 role 那一侧也接到面板上（CP2 P3） | pass | 原来只有 `GROUP_LABELS` 那两条做了「面板值 ≠ 漂后 CLI 值」的断言，`ROLE_LABELS.reviewer` 只验到解析器层——**而 reviewer 恰恰是当年靠 `cliText.includes()` 子串兜底漏网的那一条**。现在从**真实渲染路径**取面板译法（走 `__NodeTable__` 展平取第 3 列），不是去读 bundle 里的常量表——读常量表就又变成两份常量互相担保了 |
| E-112 | 真机 | **DM6 两条镜像断言的可独立复核产物**（CP2 P2） | pass | CP2 指出原 `dm6-mirror-assertions.md` 只有**手抄表格**，零上下文复核者无法独立复核。不改 bundle、只改 fixture 重跑：**镜像①**（`fake-run-0001` 的 `group` running→done）→ 它搬进「已完成（2）」节、「运行中」节整个消失，而**卡片上打的状态词仍是「运行中」**——分节归 `group`、状态词归 `run_status`，两者在屏上当场分家（截图 `mirror1-group-moved.png`）。**镜像②**（只改 `run_status`，`group` 不动）→ 两份屏内全文各 44 行，逐行 diff **恰好 2 行不同**：fixture 指纹行（换了输入，必然变）与那一个状态词，**其余 42 行逐字相同**。原始 dump 两份都落盘可自行 diff。转录：`evidence/dhr49/batch2/dm6-mirror-real-machine.md` |
| E-113 | 真机 | 上条的基线自证 | pass | `_mirror/baseline/`（整份拷贝、一字未改）跑出的屏内全文 SHA-256 = `076c9d4a1036b073…`，**与 E-102 用原 fixture 根取的那次完全相同**——拷贝没引入任何差异，后面两条比对才站得住。收尾已把 DSH 的 `RELAY_PILOT_FIXTURE_ROOT` 指回原目录 |
| E-114 | cmd | CP2 返工后全量复跑 | pass | `npm test` **228/228，fail 0，skipped 0**。六份变异脚本：contract 16 / **grouping 8** / host 16 / detail 25 / purity 17 / cli-vocabulary 6 = **88 条全见红**，各自正控全绿。`src/render/text.mjs` 跑前跑后 sha256 均为 `661f70677f5716ac…` |
| E-115 | 复核 | **CP2 追加观察：词法网扩到 region 之后仍可绕，绕法只多一行** | **复现并接受（不返工，改量程）** | 它把 `__visible` 的定义挪到 `//#region grouping` **上面一行**，实测：源码级断言**不咬**、守恒①**不咬**（基准 fixture 里没有 `unknown`）、只有守恒②「`run_status` 取任何值都不影响谁出现」**咬住**。**region 边界是一行文本注释标记，绕开它零成本**——扩宽只是把门槛从"挪一层函数"抬到"挪出 region"，没关上这条路。处置：①在那条断言下方写明**量程**（它是廉价的第一道网，只拦本 region 内的读取；跨 region 由守恒断言承担）②把这个绕法**固化成第 9 条变异**，让归因自己说话——承重的是守恒断言，不是词法网 |
| E-116 | test | `mutate-dsh-client-grouping.mjs` **8 → 9 条** | pass（`ALL 9 MUTATIONS CAUGHT` + 正控全绿） | 新增条用 `extra` 钩子把 helper 定义插到 region 标记之前，`groupRuns` 体内只留调用 |
| E-117 | doc | `visual_map.md` 整表刷新 | 记录 | 此前冻在批 1 时点（四行 0% / missing），轮 2 复核点了这条。同时补了一张「当前口径」小表供 DHR_50 人判三态时直接读：DM3 / DM6 / DM4b / DM5b 四条成立，**DM2 不得记 pass**（判据②差「面板渲染出数据」一格），三态不自裁 |
| E-118 | doc | `review.md` 四张复核表填完 | 记录 | 第一轮 CP1/CP2/CP3/CP4 四行 + 第二轮一行，此前只有 CP1 那行有内容，CP3/CP4 的结论只活在 progress 里。**CP2 那行注明是补派、不是当时那道闸** |
| E-119 | cmd | CP2 追加返工后全量复跑 | pass | `npm test` **228/228，fail 0，skipped 0**。六份变异脚本：contract 16 / **grouping 9** / host 16 / detail 25 / purity 17 / cli-vocabulary 6 = **89 条全见红**，各自正控全绿 |
| E-120 | doc | 入仓侧安全复扫 | pass（一处待收口处理） | `docs/modules/dh-relay/workspace/DHR_49/` 全目录扫 IP / 凭据关键字 / `C:\Users\nash` → **零命中**；tailnet IP 与对端主机名已占位为 `<tailnet-ip>` / `<目标机>`。**唯一残留**：SSH 别名 `thinkpad` 在 brief / task_plan / review / lesson / progress / visual_map 里共 7 处，以及 `D:\MyFiles\ai-workflow\…` 若干处——两者都在 **P3-6「收口时统一占位」** 的范围内（CP4 已扩写该条范围），留给收口一次处理。`evidence/dhr49/batch3/_raw/` 保留原始转录不占位，那批不入仓（E-099） |
| E-121 | doc | 收口账目：`review.md` 完成条件 8 行逐条挂证据 ID 与结论 | 记录 | 8 行此前**全空**（轮 2 复核点出）。逐条填了证据 ID + 达成结论 + 量程。其中 #6/#7 是人判项，填的是**事实部分**（用户亲跑记录、操作路径），结论栏写「**待用户签，AI 不代签**」 |
| E-122 | doc | 收口账目：验收项元数据表 8 行填满 | 记录 | 覆盖态逐条判定：DM3-list / DM3-detail / DM6 / DM5b = **等价覆盖**；DM2 / DM4b = **部分**；两条人判项 = **待人判**。每行都写了「未覆盖边界」与 `arbiterAuthorization`——后者一律注明是 AI 自证、以及复核方各复跑到了什么程度 |
| E-123 | doc | 收口账目：**第 4 路横向一致性复核**逐行真扫 | 记录 | 5 行此前全是 `<一致 / 不一致>` 占位符，而 grouping 测试的注释还在写「一致性复核会横向再扫一次」——**一张空表在被当兜底引用**（CP2 点出）。逐行核过源码后：4 行**一致·无需处置**；1 行**不一致·有意差异**——「零 bare import」这条本包做不到（宿主半边必须 import typert-protocol 与真 zod，因为 `dsh-typert-loader` 硬查 `_zod` 品牌），后果是**本包必须 tgz 安装、不能目录 link**，已在 README 立节并有指针。那条空头支票注释已随词表返工一并删掉。另记一条差异：CLI 的 split 布局有一层 `SECTION_LABELS`/`SECTION_OF_GROUP`（四 group 并三节），**面板没做这层合并**——不影响 DM6，但「共用一套词表」在 split 那层不成立，收口措辞要避开 |
| E-124 | doc | 收口账目：Confidence Challenge 逐条列 gap | 记录 | 按「签下去最可能后悔哪条」排序写了 8 条。头三条：①DM2 差的那格是**真的差**，且补起来很便宜（用户在目标机开个会话即可）；②**这张卡的断言被三轮复核各捅穿一次，每次都是整类方向搞反**（数组盲点 / 手抄期望值 / 词法网可绕）——修完当然更严，但**没有理由相信第四类不存在，只是没人再去找**；③228 与 89 是实现方自证，两个复核方都在我返工**之前**结束 |
| E-125 | cmd | 收口前全量复跑 | pass | `npm test` **228/228，fail 0，skipped 0** |
| E-126 | 决策 | 用户 2026-08-21 点选：**主机标识占位「先不管」** | decided | P3-6 的占位处理（SSH 别名 `thinkpad` 7 处、`D:\MyFiles\ai-workflow\…` 若干处）**由用户判为暂不处理**。⚠️ 按 L-25 的口径明确登记：这是**用户的成本/取舍判断，不是被什么挡住、也不是忘了**。相关事实：入仓侧的凭据、tailnet IP、对端主机名扫描**零命中**（E-120），残留的只是 SSH 别名与本机绝对路径。**P3-6 条目保持 open**，去处仍是收口——真要处理时按 CP4 扩写后的范围（所有本机绝对路径与主机标识）一次过完 |
| E-127 | 决策 | 用户 2026-08-21 点选：**目标机「暂时不动」** | decided | P4-DM2 判据②的最后一格——**面板在目标机上渲染出数据**——**就此定格为未证**，不再等补。⚠️ 按 L-25 口径登记：这是**用户的取舍判断，不是被挡住、也不是遗漏**。补法一直存在且很便宜（在目标机上开一个会话发一条消息，AI 可隔 SSH 隧道核 `window.__RELAY_PANEL_PROBE__` 里的 `host-value`），是**决定不补**。**因此 P4-DM2 定格为「不得记 pass」**，带着这个状态进 DHR_50 人判三态。目标机上留下的东西（`~/dhr49-crossmachine/` 253 MB + 用户级 `npm i -g @deepseek-ai/dsh`）清理方式见 `cross-machine.txt` §7，本卡不代清 |
| E-128 | 决策 | 用户 2026-08-21 点选：**认可两轮复核结论与全部返工**，但**签名先不动** | decided | 明确范围：认可的是**复核结论与返工内容**，**不是** verify 签字。**人类签名区保持未解锁**，`verify` 提交**不打**。⚠️ 记这条是因为「认可」二字本身不足以支撑代签——按确认闸（G5），签名与三态都要用户在对话里就**具体那件事**表态，AI 不得把一句泛泛的认可放大成签字授权 |
| E-129 | 人判 | **签名区目的二（P4-DHR49-H-e2e）：用户答「没从零敲过」** | **未做（如实记）** | 该闸的原文要求是「从零照文档敲一遍，**中途不需要 AI 补任何未写在文档里的步骤**」。用户答：之前是 AI 在旁边带着跑的，**不算**。故此格记 **未做**，签名区那个框**不勾**。⚠️ 这条含金量不低——同类的「文档自足性」缺陷，配方在一台**无遗留状态**的机器上就暴露过一个 `mkdir` 缺步（E-081/L-24），**本机永远试不出这类问题**。目的一（midgate，E-029/E-031/E-032）不受影响，那条是真跑过的 |
| E-130 | 决策 | 用户 2026-08-21：**「我不敲，你帮我敲吧」** | decided（**但不解锁 H-e2e**） | 用户要求 AI 代跑签名区目的二。⚠️ **AI 代跑顶不了这一格**，原因不是规矩是它真证不到东西：**配方是我写的**，我敲的时候会不自觉用脑子里的知识补上文档没写的步骤——那正是这道闸要抓的毛病。故处置为：H-e2e **仍记未做**（E-129 不变），另派一次**零上下文机器代跑**作为**旁证**，测同一个属性（配方对只有文档的执行者自不自足），只是裁决者从人换成机器 |
| E-131 | 真机 | **零上下文照文档从零跑通**（H-e2e 的机器旁证） | pass（配方自足，除端口一条） | 派 `DHR49-e2e`：**只许读两份 README**，不许读源码/测试/workspace 文档、不许 `git log`、不许问主控。用**全新空 `DSH_HOME`**（临时目录），不碰在用实例。⚠️ **它始终没交出报告**（连发三次空的 idle 信号，催两次无效），所以下面全部是我**从磁盘痕迹独立还原**的客观事实，**不含它的任何自述**：①`dist/…tgz` 18:44 重打 → `npm pack` 成功（`mkdir -Force` 那步文档里有，没绊住）②profile 里 `@personal/dsh-relay-host` 走 `link:`、`@personal/dsh-relay-panel` 走 `file:…tgz` → **两份 README 的装法都照做了**，没踩「panel 不能目录装」那个坑 ③客户端 4 个文件安装副本与源码**逐字节相同** ④`storages/workspace.json` 已生成 → 加工作区那步走到了 ⑤服务 `http://127.0.0.1:3087` **HTTP 200** |
| E-132 | fix | **抓到一条真文档缺陷：`--port` 未写** | 已补 README | 它起服务用的是 `dsh --profile web --port 3087`，而当时 README 第 3 步只写 `dsh --profile web` + 一句 `→ http://127.0.0.1:3080`，**`--port` 一个字都没有**。后果：**任何人在本机已跑着一个 DSH 时照这份文档敲都会撞端口，而文档不告诉他怎么办**（换 `DSH_HOME` 并不会换端口）。⚠️ 这类缺陷**写文档的人自己永远试不出来**——我知道有 `--port`，敲的时候会下意识加上，压根意识不到文档里没写。与 `npm pack --pack-destination` 不自建目录那条 `mkdir` 缺步**同一性质**（L-24：只有无遗留状态的机器才撞得到）。已补进 README，并把「这条是怎么被发现的」一并写在那里 |
| E-133 | 观察 | 派出去的 subagent **不交报告**这件事本身 | 记录 | `DHR49-e2e` 连发三次空 idle 信号、两次催促无效，全程零文本产出。轮 2 那个 agent 也有过同样开头（先发两次空信号，追问后才给出完整结论）。**处置办法是不要干等**：本次靠磁盘痕迹（profile `package.json`、安装副本哈希、监听端口、进程命令行、文件 mtime）独立还原出了完整链路，结论质量不低于它自述——**而且更硬，因为不依赖它的叙述**。教训见 L-37 |

> **⚠️ 归因声明（2026-08-21，按 CP4 收尾提示③）**：本表里的 `217/217`、`80 条变异全见红`、`17 条 purity 变异` 这些数字**全部是我（主 session）自己跑出来的**，不是复核方验过的。CP3 与 CP4 都是**只读复核**，且都在我按其发现返工**之前**结束——CP4 没有复跑过修完之后的树。收口时如果要引用这些数字，口径是「实现方自证 + 复核方指出的洞已补」，**不能写成「经复核验证」**。真正的独立复验落在轮 2 换人复核上。

## 硬依赖核验（开工前置）

| 前置 | 出处 | 核验结果 |
|---|---|---|
| DHR_26「侦察落档」已交付（缺则 DHR_49 不得开工） | DevPlan DHR_26 验收口径「机器证 · 侦察落档」 | **满足**——`workspace/DHR_26/findings.md` §5（`dsh.client` 声明形态）、§6（`exports['./client']` 产物形态）、§13（本机类型定义位置，4 份 `.d.ts` 逐项列出）、§14（官方 client 插件声明实例，本机 39 个包）、§2/§3（profile 扫描锚点与 `--patch` 边界）、「交给 DHR_49 的硬输入」节均已落盘 |
| DHR_26 已收口 | `git log --grep="^verify"` | **满足**——`c90cf88 verify(dh-relay): DHR_26 树外 DSH Host Plugin 与 rc.7 现场侦察 user-signed` |
| DSH 版本未漂 | `dsh --version` | **满足**——`0.1.0-rc.7`，与 DHR_26 版本基线一致 |
