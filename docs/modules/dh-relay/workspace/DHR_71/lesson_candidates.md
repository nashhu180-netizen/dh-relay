<!-- lesson_candidates.md — 教训候选。收尾顺手记。AI 起草、人裁决。 -->
# lesson_candidates — DHR_71

## 教训候选

| ID | 一句话教训 | 状态 |
|---|---|---|
| L-7101 | **`t.after` 救不了「等待抛错后 driver 还活着」——它按登记顺序排在夹具的 `rm(repoRoot)` 之后**；要让被测组件先收口，只能用用例体内的 `try/finally`。实测：`t.after` 兜底 4 轮 2 挂，`finally` 5 轮 0 挂（F-7103） | 待裁决 |
| L-7102 | **进程「跑完却不退」先看活跃句柄再猜**：`process.getActiveResourcesInfo()` 一行就把 652s 挂死从「以为是泄漏的轮询定时器」翻成「是一次不返回的 `FSReqPromise`（Windows 递归删目录撞上仍在写盘的进程）」。探针要用 `unref()` 的 interval——它只在进程本该退出却没退时才会说话，正常路径上完全不留痕 | 待裁决 |
| L-7103 | **「超时」是表象不是归类**：F-3520 把 `agent-node:198` 与 `herdr-adapter:354` 记成「1s–10s 预算的时序脆弱」，本卡把等待有界化并让超时输出打印事件序列后，两条当场现形为 DHR_64 之后的**语义红**（等的事件现役 driver 根本不会写）。判「时序还是语义」的成本，几乎全在「超时那一刻有没有把现场打出来」 | 待裁决 |
| L-7104 | **脱敏要做在打印那一步，不能等证据落盘后再扫**：本卡的 `dumpDriverScene` 初版把 `fake.sent` 原样打进 evidence，当场复刻了 DHR_35 `F-3521` 的 Receipt ID 泄漏。凡是「会被写进 evidence 的诊断输出」，工具自己就得带 `redactIds()`（AGENTS.md 宪章 #6 的「不能事后靠扫描兜底」是有实例的） | 待裁决 |
| L-7105 | **等待上限必须由生产侧上限推出来，并把推导写在等待旁边**：`herdrReadyTimeoutMs=10_000` / `agentGet` per-call `timeoutMs=10_000` / `doneTimeoutMs=60_000` / `HERDR_START_TIMEOUT_MS=60_000` 各自对应哪条等待，是可以逐条查的；原来的 1s / 10s 预算连一次生产调用的上限都装不下，红了只能靠复跑猜 | 待裁决 |
| L-7106 | **等待上限的方向别搞反：它必须 ≥ 生产合法上限**，不是「在生产上限之内还留了余量」。首轮给 `herdr-adapter:510` 保留 45s 的理由正是后者——而该用例调用链的合法上限是 80s，等于一次合法的慢启动就会被判失败；`dhr69:179` 时红时绿是同一个错（45s < 链路 60s）。B-36 把规则冻结成「按真实调用链逐段相加 × 1.5」，可逐段查、不可拍脑袋 | 待裁决 |
| L-7107 | **skip 不是豁免，是记账**：隔离 4 条语义红之后绿闸才可信，但结论只能写「隔离 4 条 skip、其余全 pass」并写明每条去向（DHR_72），不能写「全绿」。措辞一松，下一个 reader 就会把「转出去的债」读成「已经没有债」 | 待裁决 |
