<!-- dh:v1 · execution_strategy.md — 分工。开工时定一次。 -->
# execution_strategy — DHR_71

## 操作模型

派活：S3 施工由 **Opus** 在 Herdr 交互式 pane 里按 `task_plan.md` 执行（主控左、worker 右上；右下留给收口复核者）。主控只监工、催收、核 DONE，不改代码。不分批（一个验收单元：绿闸）。**先取基线再改**——隔离单跑时长是机器证 A 的分母，必须在改代码前拿到；652s 挂死要先用负例证实/证伪再定修法。

## 子 agent 授权（若派单）

| 子 agent | 范围（只读 / 可写哪些文件） | 谁批准 |
|---|---|---|
| 施工 worker（Opus · `wt/DHR_71`） | 可写：DevPlan `dh:allowed-paths:v1 task=DHR_71` 逐条路径（含用例行级限定），`workspace/DHR_71/**`；可在 `wt/DHR_71` 提交；**不可** push / 合并 / 改 DevPlan 状态 / 动共享夹具与 `fake-herdr.mjs` / 改生产代码 | 用户 2026-09-02 D-start 点选 |
| 收口两路复核（E5 教训 / E14 一致性） | 只读全仓；不得改任何文件 | 同上（委托节点沿默认） |

## 收尾铁律

- **只许 skip 恰 2 条**（`:242`、`:279`），措辞永远是「隔离 2 条 skip、其余全 pass」，不写「全绿」。
- **等待改法只认「目标事件 + 上限 + 超限 dump」三件套**；把等待改成「等出我要断言的值」等于把断言废掉，不算修。
- 红因落到生产代码 / 共享夹具 / EPERM（BL-17）→ 只登记不修；夹具问题一律移交 DHR_72。
- 不改 `package.json`（不在允许路径），`--test-timeout` 只进命令行与 evidence。
- 证据不全 / 三轮未全部收口前，不许标"待验收"。
