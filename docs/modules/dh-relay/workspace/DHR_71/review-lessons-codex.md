<!-- dh:v1 · review-lessons-codex.md — DHR_71 复核原样输出（主控从 Herdr pane 收回落盘，未删改）。派出=e:E-7111；brief=review-brief-lessons.md；形态=codex gpt-5.6-terra · high · --sandbox read-only · Herdr 交互 pane · fresh · cwd=wt/DHR_71@28af9af -->
# dhr71les · 原样输出

## 形态自述（模型 / 沙盒 / HEAD / 是否能跑测试）

                   模型：gpt-5.6-terra；沙盒：read-only。静态复核，未重跑测试。
  实际 HEAD=28af9af，基线=master@25d697d；brief 写的 ecdc941 已落后
  一笔仅新增 review brief 的提交，期间测试代码未变。观察到
  progress.md 有并发未提交修改，未触碰。

  ## 正册状态结论（N/A 可核查 的依据）

                               docs/modules/dh-relay/knowledge/教训库.md 不存在；候选库存在且为
  68 条。因此本路正册结论为 N/A（可核查），不涉及升册裁决。

  ## 候选库命中清单（扫了多少条、命中哪几条、逐条重蹈判定）

  已扫候选-1～68；深读的直接命中为：候选-23、25、26、31、34、35、
  48、51、55、58、62、65、67、68。

  - 候选-23 / 31 / 48：三轮证据明确了 --test-concurrency=1、exit、
    墙钟与结果；未把偶发问题归咎环境，652s 也有 A–F 对照。未重蹈，
    但结论不得外推到其它并发度或负载。

  - 候选-25 / 26：task_plan 的 t.after(stop) 初始嫌疑被负例推翻，
    worker 如实保留计划、改以 try/finally 收口。未重蹈。

  - 候选-34：原始 gate / hang 输出已保留，而非手抄摘要。未重蹈。
  - 候选-35 / 55：部分等待仍“等到随后要断言的状态出现”，与冻结的“等
    不变量、不等期望值”相冲突。重蹈，见 F-71-LES-01。

                - 候选-51 / 58：负例有 exit code、墙钟与终态；try/finally 还补足
    了 t.after 的登记顺序盲区。未重蹈，并形成对候选-51 的有效细化。

  - 候选-62：先取基线失败集合、再改动；B-36 也按完整 skip 名单核
    销。未重蹈。

  - 候选-65 / 67：review brief 把 HEAD 写死为 ecdc941，实际已到
    28af9af。虽代码未变化，本次静态审仍可成立，但派单文本已重蹈版本
    锚点问题。见 F-71-LES-03。

  - 候选-68：绿闸证据有明确 code_head，之后仅文档变化；就测试代码而
    言未见证据失配。未重蹈。

  ## 发现

  ### F-71-LES-01 (P1) 有界等待仍与后续断言自证循环

  agent-node 两条用例先等待 checkpoint_recorded，随后直接断言同一事
  件存在；:510 用例先等待 waiting_human，随后又断言同一状态。把固定
  sleep 换成有界轮询、增加 dump 是进步，但没有满足候选-35 / 候选-55
  所要求的“独立不变量或终态屏障”。

  这会让“等待成功”本身成为该断言的主要证明，未证明相关快照、终态或
  后续语义已收敛。应以独立屏障收口后再保留事件/状态断言；在此之前，
  不能把这些用例称为已按候选-35 消除的等待脆弱性。

  ### F-71-LES-02 (P2) 三轮绿闸缺少负载条件的终态表述

  E-7109 可证明同一台机器、--test-concurrency=1 下连续三轮为 55 /
  51 / 4 / 0，且 157 / 110 / 170 秒均收口。它不能证明其它并发度、其
  它机器负载或更大套件下同样稳定。

  当前原始 evidence 有并发参数，但 DONE 的简表未随结论带出该条件。
  应固定表述为“在该命令及该并发条件下”，不外推成一般性的“可重复绿
  闸”。

  ### F-71-LES-03 (P2) review brief 的 HEAD 已过期

  brief 固定 HEAD=ecdc941，实际审阅时为 28af9af。经静态核对，二者间
  没有 relay-core/test/** 变化，故本次代码审对象未漂移；但这正是候
  选-65 所禁止的派单写死基线形态。后续派单应要求复核者自报 live
  HEAD，并只把候选代码锚另行冻结。

  ## 本卡 L-7101~L-7107 质量核（逐条）

  - L-7101：真教训。 与候选-51 同族但非简单重复：补充了 after 钩子
    按登记顺序执行、夹具先登记 rm 时 t.after(stop) 仍无效这一条件。
    建议作为候选-51 的修订补充，而非独立并列规则。

  - L-7102：真教训，无重复。 “进程不退先取活跃句柄，再形成根因假
    设”可复用；应避免把一次句柄快照表述为根因证明本身。

  - L-7103：真教训，无直接重复。 它与候选-35、48 相邻，但重点是超时
    后的分类与现场可观测性。应明确：dump 加源码调用链核对共同完成分
    类，非 dump 单独证明。

  - L-7104：真教训，无候选库重复。 与 DHR_35 F-3521 同类，且实现是
    在输出前脱敏、非事后扫描；这一点符合 design/12 §2 的边界。

  - L-7105：真教训，但应与 L-7106 合并。 其核心是按实际生产调用链推
    导等待上限并留下推导。

  - L-7106：不宜单列。 它是 L-7105 的关键约束：上限必须不小于生产合
    法上限，并保留余量；建议并入 L-7105。

  - L-7107：真教训，无直接重复。 与候选-12 的“结论范围”相邻，
    但“skip 是显式债务、须逐条核销并写去向”有独立触发场景。

  ## 该记而未记的

  B-36 的两次主控失守不应另起两条：

  - 将 :198 / :354 的超时表象误判为时序问题，已由 L-7103 覆盖。
  - 将不同真实调用链套用同一等待公式，候选-12 / 14 / 33 只能提供类
    比，不能精确覆盖；应在合并后的 L-7105 中明确补入：不得按名称或
    抽象类别共用公式；每条等待都要逐段追其实际调用链。

  这会使 L-7105/7106 的合并条目同时覆盖“如何推导”与“不得错把不同链
  路当同一路”。

