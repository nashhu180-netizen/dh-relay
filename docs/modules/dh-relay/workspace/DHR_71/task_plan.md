<!-- dh:v1 · task_plan.md — 施工图。开工那一刻写，一次性消耗品：跑偏了去 progress.md 记实际，不回头改这里。 -->
# task_plan — DHR_71

> **执行者：headless worker**（Opus · Herdr 交互式 pane · cwd = `.dh-worktrees/DHR_71`）。本文件写到 worker 可照做粒度。
> **worker 铁律**（仓根 `AGENTS.md`「编排协议段」）：你是 worker 不是主控——不拉终端、不派活、不问用户、不加载 dev-harness skill；卡住就写 blocked（见第 12 步）而不是干等；范围外想法记 `findings.md` 不顺手做；**不改 DevPlan 状态列**；**不进复核**。

## 要读的上下文 (Context Packet) ★前置

| ID | 来源 (path / url) | 为什么 |
|---|---|---|
| C-001 | `docs/modules/dh-relay/dev_plan/P6-Herdr多账号执行底座-开发方案.md` §3.2 DHR_71 | 唯一权威验收口径 + 逐条允许路径 + 用例行级冻结 |
| C-002 | `docs/modules/dh-relay/workspace/DHR_71/brief.md` | 完成条件逐字副本、边界与红线 |
| C-003 | `relay-core/test/herdr-adapter.test.mjs` `:203-230`（共享夹具，**只读**）、`:242`、`:279`、`:335-352`（**只读**）、`:354-364`、`:510-538` | 本卡在该文件的全部可动行 + 不可动行 |
| C-004 | `relay-core/test/agent-node.test.mjs` `:39-47`（`untilAsync`/`sleep`）、`:198-229`、`:231-263`、`:265-294` | 本卡在该文件的全部可动行 |
| C-005 | `relay-core/test/helpers/settled-state.mjs` | 「等不变量、不等断言值」的正面样板；新等待工具照这个口径写 |
| C-006 | `relay-core/test/helpers/fake-herdr.mjs`（**只读**） | 看 `makeFakeHerdr` 暴露了哪些计数器/状态（`agentGets`、`sent`、`paneKills`、`statuses` 粘性规则），超限 dump 要打印它们 |
| C-007 | `relay-core/runtime/workflow-driver.mjs`（**只读**）`:33-34` 默认值、轮询段 `:330-422` | 读懂 driver 什么时候写 `checkpoint_recorded` / `attempt_orphaned` / `waiting_human`，才能把等待钉到正确的目标事件；`herdrPollMs`、`herdrReadyTimeoutMs`、`doneTimeoutMs`、`observationLostMs` 决定上限该取多大 |
| C-008 | `.dh-worktrees/DHR_35/docs/modules/dh-relay/workspace/DHR_35/findings.md` F-3520 行 + 同目录 `evidence/targeted-tests-*.txt`（三份） | 5 红 + 652s 挂死的原始现场（注意：在 DHR_35 的 worktree 里，不在 master） |
| C-009 | `docs/modules/dh-relay/backlog.md` `DHR-BL-17` | `%TEMP%` 原子重命名 EPERM 签名——判红前先比对，命中即登记不修 |

## 施工步骤 (Steps)　★详细级

> 全部命令在 **`relay-core/` 目录下**跑（`cd relay-core`）。五文件绿闸命令固定为：
> `node --test --test-concurrency=1 --test-timeout=180000 test/herdr-adapter.test.mjs test/agent-node.test.mjs test/dhr64-driver-observation.test.mjs test/dhr69-false-ready.test.mjs test/dhr70-submission-gate.test.mjs`
> （`--test-timeout` 是进程级上限的一半实现：单条用例超 180s 判 fail；另一半是用例体内自己的有界等待。`package.json` 不在允许路径，**不改 npm script**。）
> 证据文件一律落 `docs/modules/dh-relay/workspace/DHR_71/evidence/`，文件名带 UTC 时间戳，如 `baseline-20260902T0800Z.txt`。用 PowerShell 时：`node ... 2>&1 | Tee-Object -FilePath <路径>`；记得同时记 `$LASTEXITCODE`。

| # | 改动文件（Create/Modify/Test + 路径） | 怎么改（代码片 / 签名） | 怎么验（命令 → 预期输出） |
|---|---|---|---|
| 0 | 进场 | `pwd` = `.dh-worktrees/DHR_71`；`git branch --show-current` = `wt/DHR_71`；`git rebase master`（应 up to date）；读 C-001~C-009。在 `progress.md` 日志表追加一行「worker 进场，基线 `<git rev-parse --short HEAD>`」。 | `git status` clean；`git log -1 --oneline` 与 master 一致。 |
| 1 | Test · 基线取证（**不改代码**） | 跑一次五文件绿闸命令，输出存 `evidence/baseline-<ts>.txt`。再**分别**单跑五个文件各一次（同样的 flag、每次只给一个文件），存 `evidence/isolated-<文件短名>-<ts>.txt`，把五个耗时记进 `progress.md`——这是机器证 A「隔离单跑之和」的分母，**必须在改代码前取**。对每条红逐条归类：①语义红（`:242` `herdrJudge` 通路已删；`:279` 期待 `attempt_failed(E_EXECUTOR_KILLED)` 现役写 `human_input_requested`）②时序红（`timeout:` / `timeout waiting for`）③EPERM（报错含 `EPERM: operation not permitted, rename` 且路径在 `%TEMP%`）④其他。 | `progress.md` 有一张「基线红名单」表：用例行号 / 名称 / 归类 / 报错首行。红名单与 F-3520 对不上的差异要写明（机器不同、负载不同都算解释，但要写）。 |
| 2 | Modify · `relay-core/test/herdr-adapter.test.mjs` `:242`、`:279` | 把 `test('DHR_33 driver #3/#5：…', async (t) => {` 改为 `test('DHR_33 driver #3/#5：…', { skip: 'F-3520 → DHR_72：herdrJudge 直写 Result 通路已被 DHR_64 删除，用例待按 Receipt-bound 语义重写' }, async (t) => {`；`:279` 同法，skip 理由写 `F-3520 → DHR_72：现役 driver 在 stop 撞 launch 窗口时写 human_input_requested 而非 attempt_failed(E_EXECUTOR_KILLED)，用例待重写`。**用例体一个字不动**。 | `node --test test/herdr-adapter.test.mjs 2>&1 \| Select-String "skipped"` → 恰 2 条 skipped；`git diff --stat` 只有这两处（各一行）。 |
| 3 | Create · `relay-core/test/helpers/bounded-wait.mjs` | 新增两个工具，签名固定：`export async function untilEvent(check, { label, timeoutMs, pollMs = 25, dump })` —— 轮询 `await check()` 为真即返回；超 `timeoutMs` 抛 `new Error('bounded-wait: ' + label + ' 在 ' + timeoutMs + 'ms 内未发生\n' + (dump ? await dump() : ''))`；`dump` 是可选的异步函数，返回当时现场字符串（约定打印：`store.events.map(e => e.kind + (e.reason ? ':' + e.reason : ''))`、`node_states[*].status`、fake 的 `agentGets/sent.length/paneKills`）。`export async function withDeadline(promise, { label, timeoutMs, dump })` —— `Promise.race` 包一个会拒绝的定时器，**定时器必须 `unref()` 并在 promise 先完成时 `clearTimeout`**（否则你自己就在造泄漏）。文件头写一段注释说明「等目标事件不等断言值」的口径，回链 `settled-state.mjs`。 | `node -e "import('./test/helpers/bounded-wait.mjs').then(m=>console.log(Object.keys(m)))"` → `[ 'untilEvent', 'withDeadline' ]`。 |
| 4 | Modify · `relay-core/test/agent-node.test.mjs` `:198` 用例体 | ① 紧跟 `const driver = startWorkflowDriver({...})` 之后加 `t.after(() => driver.stop());`（**这是 652s 挂死的第一嫌疑：`untilAsync` 在 `:223` 抛错后 `driver.stop()` 永远不会执行，driver 以 5ms 轮询把事件循环撑活，`node --test` 收不了口**——第 9 步要用负例证实或证伪，现在先补上）。② 把 `await untilAsync(async () => store.events.some(event => event.kind === 'checkpoint_recorded'), 1_000, 'herdr heartbeat');` 改为 `await untilEvent(() => store.events.some(event => event.kind === 'checkpoint_recorded'), { label: 'DHR_33 窄路径 首条 checkpoint_recorded', timeoutMs: 30_000, dump: () => dumpDriverScene(store, fake) });`，其中 `dumpDriverScene` 是本文件顶部新加的小函数（或放进 `bounded-wait.mjs` 导出），打印事件 kind 序列 + `fake.agentGets`。上限取 30s 的依据要写进 `progress.md`（默认 `herdrReadyTimeoutMs=10_000` + 启动/写 Receipt 的 IO；**不许**拍脑袋）。③ 若基线归类里这条是**语义红**（例如首采样 `idle` 就让 driver 退出观测、`working` 根本轮不到——对照 C-007 轮询段确认），那它不是本卡能修的：保留①②改动，在 `findings.md` 登记 `F-71xx`「归 DHR_72（F-3517 同根）」，并在 `progress.md` 写明本条**预期仍红**、进入第 12 步 blocked 分支判断。 | `node --test --test-timeout=180000 test/agent-node.test.mjs` 三次 → 这条三次同结果（三绿，或三红且红因为语义）。 |
| 5 | Modify · `relay-core/test/agent-node.test.mjs` `:231` 用例体 | 同第 4 步①②：加 `t.after(() => driver.stop())`；`:255` 的 1s 等待改 `untilEvent(..., { label: 'DHR_61 D1 首条 checkpoint_recorded', timeoutMs: 30_000, dump })`。这条 `statuses: ['working','working']` 没有 idle 前缀，若仍红且非 EPERM，才是真时序问题，要在 `progress.md` 写出你观察到的实际耗时（在 dump 里打 `Date.now() - t0`）。 | 同上三次同结果。 |
| 6 | Modify · `relay-core/test/agent-node.test.mjs` `:265` 用例体 | `assert.deepEqual(await driver.done, { ok: true });` 改为 `assert.deepEqual(await withDeadline(driver.done, { label: 'DHR_61 projection-missing driver.done', timeoutMs: 60_000, dump }), { ok: true });`，并在 `startWorkflowDriver` 后加 `t.after(() => driver.stop())`。上限依据：这条只跑一个 process 节点 + 一个 pending herdr 节点，`doneTimeoutMs=60_000` 是 driver 自己的上限。**文件级超时**：在文件顶部 `untilAsync` 旁加 `const FILE_TEST_TIMEOUT_MS = 180_000;` 并只给 `:198`/`:231`/`:265` 三条 `test(name, { timeout: FILE_TEST_TIMEOUT_MS }, fn)`——其余用例不在允许行，不加。 | 同上。 |
| 7 | Modify · `relay-core/test/herdr-adapter.test.mjs` `:354` 用例体 | 在文件顶部 import 区加 `import { untilEvent, withDeadline } from './helpers/bounded-wait.mjs';`（import 行不属于夹具区，允许）。用例体：`runtimeUntil(() => gone.store.events.some(e => e.kind === 'attempt_orphaned'), 'orphaned recovery')` 改为 `untilEvent(..., { label: '#10 恢复届 attempt_orphaned', timeoutMs: <上限>, dump })`；`await gone.driver.done;` 改 `await withDeadline(gone.driver.done, { label: '#10 gone.driver.done', timeoutMs: <上限>, dump })`；`live` 那段的 `runtimeUntil(..., 'take over recovery')` 同法。**上限怎么定**：先读 C-007 recovery 路径——恢复届探活用的是 `herdr-cli` 的 `timeoutMs` 还是 `HERDR_START_TIMEOUT_MS=60_000`？基线里这条 10.3s 超时，若探活上限本来就 > 10s，那 10s 预算是**测试自己给少了**，上限取「生产上限 + 50%」并写依据；若探活应在几百 ms 内完成而实际 > 10s，那是生产侧或 EPERM 问题 → `findings.md` 登记，等待只做有界化。**不动 `recoveryFixture`（`:335-352`）**。 | `node --test --test-timeout=180000 test/herdr-adapter.test.mjs` 三次 → 这条三次同结果。 |
| 8 | Modify · `relay-core/test/herdr-adapter.test.mjs` `:510` 用例体 | 两处 `runtimeUntil(..., 45_000)` 改 `untilEvent`（label 分别 `DHR_68/C waiting_human` / `DHR_68/C ≥5 blocked polls`），`dump` 打印 `(await store.readState()).node_states[0]?.status`、事件 kind 序列、`fake.agentGets`。**归因**：基线里这条只在隔离跑 45s 超时——先在 dump 里看清 45s 时 node 状态到底停在哪（`running`？`pending`？）、`agentGets` 是多少。三种出口：①时序（事件确实来了只是慢）→ 调上限并写依据；②要改 `runtimeFixture` 才能修（例如夹具的 `herdrReadyTimeoutMs`/`statuses` 粘性行为）→ **不改**，`findings.md` 登记「移交 DHR_72」，`progress.md` 写明本条预期状态；③EPERM → 登记 BL-17 签名。 | 同上三次同结果。 |
| 9 | Test · 机器证 C 定位 652s（**负例先行**） | 复现步骤（写进 `progress.md` 并存 `evidence/hang-repro-<ts>.txt`）：把第 4 步①的 `t.after(() => driver.stop())` **临时注释掉**，把 `:198` 的 `untilEvent` 目标临时改成永远不满足（例如 `kind === 'never_such_event'`、上限 3s），跑 `node --test test/agent-node.test.mjs`，用 `Measure-Command` 或时间戳记录进程是否在用例 fail 后仍不退出（等 ≥ 60s 判挂）。然后恢复 `t.after`、保留假目标再跑一次 → 预期：用例在 3s 内 fail **且进程随即退出**。两次原始输出都进 evidence。最后把假目标改回真目标。若负例证明挂死**不是**这个原因（进程照样退出），转向查其他泄漏：`node --test --test-timeout=180000 test/agent-node.test.mjs` 连跑 3 轮各存 evidence，任一轮超 300s 即用 `process._getActiveHandles()`（在 `t.after` 里临时打印）定位是 timer 还是 child process；结论指向生产代码（如 driver 在失败路径不清 interval）→ `findings.md` 登记并注明「去向 DHR_72 / backlog」，本卡不修。 | `progress.md` 有「652s 归因」小节：复现步骤 / 观察 / 结论 / 修法或去向；evidence 里有挂与不挂两份对照输出。 |
| 10 | Test · 机器证 B 清单 + 机器证 A 三轮 | ① 在 `progress.md` 列一张表：每个被改的等待 → 目标事件 / 上限 ms / 上限依据 / 超限输出样例（从第 9 步或任一 dump 里贴一段）。② 五文件绿闸命令**连续 3 轮**，每轮存 `evidence/gate-round<N>-<ts>.txt`，用 `Measure-Command` 记每轮总时长；每文件时长从 reporter 输出里按文件汇总（或每轮外层脚本按文件计时——写个 `evidence/scripts/run-gate.ps1` 也行，脚本进 workspace 不越界）。三轮的 pass/skip/fail 表进 `progress.md`。判据：**skip 恰 2、fail 0、三轮都收口**、每轮总时长 ≤ 第 1 步五个隔离单跑之和 × 2。任一轮红：先比对 BL-17 EPERM 签名——命中则**如实记录该轮作废并注明原因、再补一轮**（作废轮的输出也留着，不许删）；不命中则回到对应步骤归因。 | 三份 `gate-round*.txt` + `progress.md` 三轮表。措辞必须是「隔离 2 条 skip、其余全 pass」。 |
| 11 | Test · 机器证 D 越界自查 + 提交 | `git diff --name-only master...HEAD` 逐条对 DevPlan `dh:allowed-paths:v1 task=DHR_71`（含行级限定：`herdr-adapter.test.mjs` 的 diff hunk 只许落在 import 区、`:242`/`:279` 头行、`:354-364`、`:510-538`；用 `git diff master...HEAD -- relay-core/test/herdr-adapter.test.mjs` 逐 hunk 核对行号）。`git diff --check` 无输出。在 `wt/DHR_71` 分支上提交，信息 `test(dh-relay): DHR_71 bounded waits for Herdr targeted suite`，正文列改动点与三轮结果；**不 push、不合并**。 | `git diff --name-only master...HEAD` 输出 ⊆ 允许路径；`git log master..HEAD --oneline` 至少一笔。 |
| 12 | Record · 收口信号 | `progress.md` 证据账本逐条 `E-71xx`（命令 / 退出码 / 落点）；`findings.md` 登记所有「只登记不修」项（含去向）；`lesson_candidates.md` 至少一条（例如「等待要 `t.after` 兜底 stop，否则超时用例会把进程撑活」——若第 9 步证实）。最后写 **`docs/modules/dh-relay/workspace/DHR_71/construction.DONE`**（结构化：`status: done` 或 `status: blocked`；`head: <sha>`；`gate: round1=<p/s/f> round2=… round3=…`；`open_findings: F-…`；`blocked_reason:`（仅 blocked 时））。**写完即停**，不进复核、不改 DevPlan。 | 文件存在且 `status` 字段可机读。 |

**blocked 判据（第 4/7/8 步的语义出口撞上时怎么办）**：若第 10 步三轮之后 fail 仍 > 0 且每条红都已归类为「语义 → DHR_72」或「EPERM → BL-17」或「生产 → finding」，**不要**继续折腾夹具或断言；照实跑完第 11 步提交，`construction.DONE` 写 `status: blocked`，`blocked_reason` 列出红名单与各自去向。绿闸「skip 恰 2、fail 0」在这种情况下达不成，由主控裁决是否扩路径——这不是 worker 的决定。

## 关键决策（一句话各一行）

- Worktree：**是**，分支 `wt/DHR_71`，目录 `.dh-worktrees/DHR_71`（从 `master@5408f1e` 切出）
- 派子 agent：**是**——S3 施工派 Opus 交互式 Herdr pane（用户 2026-09-02 指定），主控只监工不改代码
- Review：light Recipe = E5 教训 + E14 一致性两路，收口时另派 fresh 只读实例；无代码轮、无有效单测
- 分批：**不分批**——一个验收单元（绿闸），过程在 Herdr pane 里可见
- `--test-timeout` 只进命令行与 evidence，不进 `package.json`（不在允许路径）
