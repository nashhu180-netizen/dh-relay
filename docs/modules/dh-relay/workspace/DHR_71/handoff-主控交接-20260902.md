<!-- dh:v1 · handoff-主控交接-20260902.md — DHR_71 主控中途换人（Claude Fable 主 session → codex 主控）的现场交接。自足：光读本文件 + 本工作区就能续做，不依赖上一位主控的对话。 -->
# DHR_71 · 主控交接（2026-09-02，Claude → codex）

> **为什么换**：用户 2026-09-02 明文「现在换，5 小时额度不足了」。换的是**主控**（计划层 / 收口裁决 / verify 代签），不是 worker。施工 worker 已于同日早些时候按用户「用 codex 进行施工吧」换成 codex（`dhr71-codex`，Herdr pane `w2:p12`，`gpt-5.6-terra` · high）。
> **新主控第一动作**：读仓根 `AGENTS.md`（含「任务类型阅读矩阵」→ 收口读 dev-harness `references/动作-E-收口.md` + `references/节点表.md` + `references/verify-代签与汇报.md`；dev-harness 装在 `~/.codex/skills/dev-harness/`），再读本文件，再读本工作区 `brief.md` / `findings.md` / `progress.md`（末尾）/ `construction.DONE` / `review.md`。**主控必须在 Herdr pane 内**（`HERDR_ENV=1`），派活规程见 `docs/modules/dh-relay/knowledge/herdr-派活操作.md` + `knowledge/主控派活备忘-20260902.md`。

## 1. 现场快照（交接时刻）

| 项 | 值 |
|---|---|
| master | `25d697d`（B-36 已落盘 `1b4b1ce`；DHR_71 目标段 2→4 机械同步 `25d697d`；`dh dh-relay` 0 失败） |
| `wt/DHR_71` | 目录 `.dh-worktrees/DHR_71`，HEAD `e1074d8`（上一届 Opus worker 提交：CON-01/02 整改 + 未达标各轮取证），已 rebase 到 master `25d697d` |
| 工作树未提交 | codex worker 正在产出的 `evidence/gate-round*-2026...Z.*`（含 `.VOID-*` 作废轮）；`review-lessons-narrow-codex.md`（本交接一并提交） |
| 卡合同 | DevPlan §3.2 DHR_71（B-36 后）：隔离清单 **4 条** S1~S4（按完整用例名核销）、允许路径含 `dhr69-false-ready.test.mjs`（仅 `:179` 用例体）、等待上限规则「按真实调用链逐段相加 × 1.5」、S3/S4 反向禁改；机器证 A 措辞只能是「隔离 4 条 skip、其余全 pass」 |
| 授权边界 | 用户 D-start 授权至 **E10 人验备料**。**E11 用户确认 / E12 verify 代签 / E13 销户 / 合并 / 删树 / push** 均须用户在对话里明确确认（G5）；已确认的：B-36 落盘、`%TEMP%` 清理、施工换 codex、主控换 codex |
| 在飞 agent | `dhr71-codex`（施工 worker，右上 pane `w2:p12`）正在跑清理 `%TEMP%` 后的三轮绿闸（新时间戳系列）；它会更新 progress / findings（CON-01/02 → resolved）/ DONE 并在 `wt/DHR_71` 提交后停下。**收活方式**：`herdr agent wait dhr71-codex --timeout <ms>` → `herdr agent read dhr71-codex --source recent-unwrapped --lines 200` → 核 `construction.DONE` + `evidence/gate-round*.txt` 原始输出（不要只信 DONE 表） |

## 2. E 阶段进度（light 配方：教训 + 一致性两路，无代码轮、无有效单测）

| 节点 | 状态 | 说明 |
|---|---|---|
| E0 机器体检 | ✅ | 工作树 `dh dh-relay` 0 失败（`ecdc941` 时） |
| E5 教训复核 | ✅ 已回收 | `dhr71les`（codex 只读 · fresh），派出 `E-7111`，原样输出 `review-lessons-codex.md`。1×P1（F-71-LES-01）+ 2×P2 |
| E14 一致性复核 | ✅ 已回收 | `dhr71con`（同形态），派出 `E-7112`，原样输出 `review-consistency-codex.md`。2×P1（F-71-CON-01/02，**采纳，worker 整改中**）+ 1×P2 范围外（C-71-03）+ import 裁决站得住 |
| 候选-26 窄审 | ✅ 已回收 | `dhr71les2` 攻击主控对 LES-01 的驳回，派出 `E-7113`，原样输出 `review-lessons-narrow-codex.md`：**部分成立** |
| CON-01/02 整改 | 🔄 | worker `dhr71-codex` 在跑；上一届已改 `agent-node` 两处 30_000 → 120_000（`e1074d8`），绿闸命令 `--test-timeout` 180000 → **300000** |
| **F-71-LES-01 终裁** | ⏳ **待落账（见 §3）** | 主控原驳回 → 窄审部分成立 → 终裁「部分采纳」，**尚未写进 findings.md**（避免与 worker 并发改同一文件，留给新主控在 worker 停下后落） |
| E6 miner | ⏳ 未做 | `dh mine dh-relay DHR_71` 只读备料已跑过一次（输出未落盘），需派 fresh 只读实例按简报抽候选 → `miner-codex.md` |
| E7 as-built | ⏳ 未做 | 本卡只改测试；`as-built/relay-core.md` 预计只补一句绿闸命令（或判无需改，写进 review） |
| E8/E9/E10 | ⏳ 未做 | review.md AI 提交区 + 七段交付汇报 + 证据展示区（四条机器证；H=0） |
| E11~E13 | ⏳ | 等用户 |

## 3. 主控未落账的裁决（新主控照此落进 findings.md / review.md / lesson_candidates.md）

1. **F-71-LES-01 → 部分采纳**（窄审 `E-7113` 结论「部分成立」）：
   - 站得住：DHR_61 / DHR_68 用例等待之后的断言是独立性质；施工者不得擅改 task_plan 冻结的等待目标。
   - 站不住：主控把候选-35 限缩为「只管快照滞后」过头——候选-35 反思明写「等待条件与断言目标同一件事时轮询会自证」；S4「DHR_33 窄路径」等 `checkpoint_recorded` 后又断言同一事件存在，是重复证明，「冗余非缺陷」不成立。
   - 落法：findings 状态改「**部分采纳**」；S4 的重复断言登记 **P3 移交 DHR_72**（重写断言时去掉 `some(checkpoint_recorded)` 重复项）；「冻结等待目标 vs `settled-state.mjs` 口径」的张力记入 lesson_candidates（新条）并作为 DHR_72 重写 S1~S4 时的输入；本卡不改等待目标。review.md 措辞：本卡只声称「等待有界 + 现场可见」，不声称「按候选-35 消除等待脆弱性」。
2. **F-71-CON-03（P2，范围外）**：`dhr64-driver-observation` 夹具与 `dhr70-submission-gate:350` 先登记 `rm` 后登记 `driver.stop()` → 入 backlog（`DHR-BL-17` 补一条「测试侧巡检清单」或新条目），不在本卡修。
3. **F-71-LES-02 / LES-03（P2）采纳**：三轮结论固定措辞「在该五文件命令、`--test-concurrency=1`、本机负载下：隔离 4 条 skip、其余全 pass」；派单不写死 HEAD、让复核者自报（候选-65）。
4. **教训候选整理**（按 `dhr71les` 质量核）：L-7105 + L-7106 合并（并补一句「不得按名称/抽象类别共用上限公式，每条等待逐段追其实际调用链」——即 B-36 R2-01 的教训）；L-7101 作为候选-51 的修订补充而非独立条；L-7102/7103/7104/7107 保留；B-36 两次主控失守不另起条（已被 L-7103 与合并后的 L-7105 覆盖）。
5. **`dhr69` import 行**：主控裁「机械前提、等价放行」，两路复核独立确认——已落 progress，review 一致性表第 6 行照此填。
6. **F-7108（BL-17 停顿形态）**：worker 已登记，定位 `store.mjs:484-498` `registerReceipt` → `persistState()` 原子 rename 在 `%TEMP%` 停顿；签名 = 「events 止于 `attempt_started` + fake 计数全 0 + 等待超时」。**保持 open、只登记**；收口时把这个签名补进 backlog BL-17（与 B-36 已补的「rm 永不返回」并列）。
7. **`%TEMP%` 清理事实**（用户授权后主控执行）：删除超过 1 天的 `dh-*` / `dh<数字>*` / `dhr*` / `.tmp*` 目录，24,594 → 约 1,900（3 个被占用未删：`dh53-ro-review-*` ×2、`DHR35-fixture-herdr-claude-main`）；**node 进程一个没杀**（44 个"陈旧"进程实为各 Claude session 的 MCP server 与另一项目的 vite，非测试残留）。这条写进 progress 作为环境变更留痕，三轮绿闸的「本机负载」条件以清理后为准。

## 4. 收口路线（新主控）

1. 等 `dhr71-codex` 停下 → 核三轮原始输出（55/51/4/0 ×3、S1~S4 同名、每轮 ≤ 370s）→ 核 `git diff master...HEAD` 逐 hunk（允许路径 + 行级冻结 + 反向禁改）→ 若红：先比 BL-17 / F-7108 签名，再决定作废补轮还是归因。
2. 落 §3 各项 → `git add` 精确路径提交。
3. E6：`dh mine dh-relay DHR_71` → 派 fresh 只读 codex（`--sandbox read-only`）抽候选 → `miner-codex.md`；`dh dispatch dh-relay DHR_71 --who <名> --type review-dispatch ...` 落账。
4. E7：判 as-built 是否需改（只改测试 → 多半只补绿闸命令一句或写「无实现变化」）。
5. E8~E10：填 review.md AI 提交区（Confidence Challenge 要写清「三轮绿闸带条件」「不跑真实 Agent」「复核者均静态审」）、七段交付汇报、证据展示区（四条机器证各一段：要证明啥 / 期望 / 实际 / 差没差 / 证据局限）。
6. E11：AskUser 等价的对话确认「已查看证据，认可执行本地收口」→ E12 精确 squash 合入 master + 复验 + `verify(dh-relay):` → E13 DevPlan 回填（DHR_71 已完成 + SHA、dh:status）、`dh dh-relay` 0 失败、删树删分支。**push 不在授权内。**
7. 之后：DHR_72 须另行 D-start（B-36 后其机器证 E = 4 条、允许路径含 `agent-node` S4）。

## 5. 上一位主控的自我更正（别再犯）

- D-start 给 worker 的 652s 第一嫌疑（缺 `t.after(stop)`）**错了**，被负例证伪；真因 `rm` 撞 tmp 写入永不返回。
- B-36 v1 把两条语义红当时序红（沿用 F-3520 旧归类没核）；v2 把 Claude `paneRun` 链和 Codex `agentStart` 链当同一条并共用公式——各被一轮审核抓出。写「X 会兜住 / 同一路径」之前先回去核那条路径。
- 对 F-71-LES-01 的驳回把候选-35 限缩过头（窄审抓出）。驳回复核发现前先把那条候选原文读完。
