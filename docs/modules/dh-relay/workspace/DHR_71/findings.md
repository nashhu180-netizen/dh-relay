<!-- findings.md — 问题清单。边做边记。 -->
# findings — DHR_71

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|---|---|---|---|---|---|
| F-7101 | P1 | 承接 DHR_35 `F-3520` 时序部分：`master@43e4af9` 定向五文件 55 条 5 红——`herdr-adapter.test.mjs:354`（10s `runtimeUntil` 超时）、`agent-node.test.mjs:198`/`:231`（1s `untilAsync` 超时）在不同轮次出没不定；`:510` 只在隔离跑里 45s 超时；另一次 `agent-node.test.mjs` 整跑 652s 不收口（隔离单跑约 40s）。两条语义红 `:242`/`:279` 归 DHR_72，本卡只 skip | DHR_35 `evidence/targeted-tests-20260901T1335Z.txt` 等三份（在 `wt/DHR_35`） | 本机重取基线完成：5 红逐条复现且**与 F-3520 同名同位**，无 EPERM、无挂死（合跑 126s）。归类结论见 F-7102 / F-7104——**F-3520 把 `:198` 与 `:354` 归成「时序脆弱」是错的，两条都是语义红**；真正的时序红只有 `:231` 一条，已修。用户 2026-09-02 经 `DHR-B-36` 采纳该校正，隔离清单 2→4 | 已归类并落 B-36（拆成 F-7102 / F-7103 / F-7104 / F-7105） |
| F-7102 | P1 | **`agent-node.test.mjs` `DHR_33 窄路径`（基线 `:198`）是语义红，不是时序红**。该用例末行断言 `attempt_failed(E_EXECUTOR_KILLED)`，而现役 driver 在 `stop` 时写的是 `human_input_requested(E_EXECUTOR_KILLED)`（`relay-core/runtime/workflow-driver.mjs:425`，全仓唯一的 herdr 侧 `E_EXECUTOR_KILLED` 写点）。与 `herdr-adapter.test.mjs:279` **完全同根**：都是 DHR_64 `0dd371d`「Receipt-bound 才是唯一 Result 入口」之后的必然结果，用例停在 DHR_64 之前的语义上 | `evidence/baseline-20260902T0316Z.txt:67-84`（断言在 `:228` 失败，`actual: undefined`）；`grep -n "E_EXECUTOR_KILLED" relay-core/runtime` 只有 `workflow-driver.mjs:425` 一处且 kind 是 `human_input_requested` | **已按 B-36 隔离**（冻结表 **S4**，加 skip，理由写在用例上），**移交 DHR_72** 按 Receipt-bound 语义重写。断言一个字未动（B-36 反向禁改）；用例体内本卡已落的有界等待与 `try/finally stop` 按 DevPlan 要求保留 | 已隔离，移交 DHR_72 |
| F-7103 | P1 | **652s 挂死的根因定位到了，且不是泄漏的定时器**：挂住时活跃句柄只剩一个 `FSReqPromise`（无 `Timeout`、无 `ChildProcess`）——挂的是用例 `t.after` 里那次 `rm(repoRoot, { recursive: true, force: true })`，它在 Windows `%TEMP%` 上撞见仍在写 `state.json.<uuid>.tmp` 的 driver 时**不返回也不报错**。node:test 的 after 钩子按**登记顺序**执行，而 `rm(repoRoot)` 在用例体第一行就登记了，所以补一个 `t.after(() => driver.stop())` 排在它后面、救不了场（实测 4 轮 2 挂）。与 `DHR-BL-17` 同族（同一处 `%TEMP%` 原子重命名 / 临时目录竞争），只是这里表现为 fs 调用挂住而非 EPERM | `evidence/hang-repro-20260902T0338Z.txt`：A 组（无兜底 stop）exit=124/150s；B 组（`t.after` 兜底）4 轮 2 挂；E 组探针 `[diag-alive] ["FSReqPromise","PipeWrap","PipeWrap"]` 连打 6 次；C/D 组诊断跑里可见 `rm FAILED ENOTEMPTY` 与 `EPERM ... rename state.json.<uuid>.tmp` | **已在允许路径内修**：把 `driver.stop()` 从 `t.after` 改成用例体内的 `finally`，保证 driver 在任何 after 钩子之前收口。F 组同一负例 5 轮 **0 挂**（对照 B 组 4 轮 2 挂）；增量三轮绿闸亦零挂死。生产侧无改动；`%TEMP%` 本身的脆弱性仍归 `DHR-BL-17` | resolved（测试侧）· BL-17 仍 open |
| F-7104 | P1 | **`herdr-adapter.test.mjs` `#10 恢复届`（基线 `:354`）同样是语义红**：用例等 `attempt_orphaned`，而 DHR_64 `0dd371d` 已把恢复届「探活 missing → 直写 orphaned Result」换成 `human_input_requested(E_EXECUTOR_HOST_LOST)`（`workflow-driver.mjs:534-540`），现役 driver 在该路径上**不可能**产生 `attempt_orphaned`。原来的 10s 超时只是表象 | 有界化后的超时输出直接把现场打出来了：`events(5)=["run_created","node_started","attempt_started","host_observation_changed(alive)","human_input_requested:E_EXECUTOR_HOST_LOST"]`、`node_states=[{...,"status":"waiting_human"}]`（`evidence/gate-round1-20260902T0405Z.txt` 失败段）；`git show 0dd371d -- relay-core/runtime/workflow-driver.mjs` 可见 `recordResult(orphaned, E_EXECUTOR_ORPHANED)` → `appendRecoveryEventIfOpen(human_input_requested, E_EXECUTOR_HOST_LOST)` 的替换 | **已按 B-36 隔离**（冻结表 **S3**，加 skip），**移交 DHR_72** 重写。断言一个字未动；用例体内有界等待（15s）与 `try/finally stop` 保留 | 已隔离，移交 DHR_72 |
| F-7105 | P2 | `dhr69-false-ready.test.mjs:179`「假就绪恰写一次 blocked Attention」在本机时红时绿：首轮隔离单跑红（`timeout:false-ready waiting_human`，文件级 `until` 的 45s 默认预算），首轮三轮绿闸里 绿 / 红 / 绿 | 首轮：`evidence/isolated-dhr69-false-ready-20260902T0322Z.txt`（10/9/1）对照 `evidence/gate-round{1,3}-20260902T0405Z.txt`（绿）与 `gate-round2-20260902T0405Z.txt`（红） | **B-36 已把该用例体等待划入 DHR_71 允许路径**，本卡据此有界化：上限按真实 Claude `paneRun` 调用链推导 = 1.5 × 6 × 10_000 = **90_000ms**——原 45s 预算**低于**该链 60s 的生产合法上限，属「上限比生产上限还小」的典型错误，一次合法的慢启动就会被判失败。超限打完整 dump；负例（上限临时改 1ms）证明它在上限内 fail 而不是挂住（525ms fail + 现场） | resolved（等待已有界化；增量三轮绿闸该条均绿，耗时 7.9 / 3.4 / 12.3s） |
| F-7106 | P1 | **本卡自造的证据泄漏（已闭合）**：`dumpDriverScene` 初版把 `fake.sent[-1]` 原样打印，于是 `evidence/hang-repro-*.txt` 里出现了原始 Receipt ID（`relay submit-result --receipt-id rcpt-<uuid>`）与 agent 名。违反 `design/12` §2 与 AGENTS.md 宪章 #6「不能事后靠扫描凭据兜底」，与 DHR_35 `F-3521` 同类 | 第一次 B 组负例输出（已脱敏，见 `evidence/hang-repro-20260902T0338Z.txt:37` 现为 `rcpt-~1fceb7b9cf69`） | 已在**工具侧**闭合：`bounded-wait.mjs` 新增 `redactIds()`，在**打印这一步**就把 uuid 形态换成 `~<sha256 前 12>`（沿用 F-3521 口径，同一 ID 恒定映射，关联性仍可核）；已落盘的那份证据回溯脱敏，全 evidence 目录（含 `.junit.xml`）复扫 `rcpt-[0-9a-f]{8}` / `herdr-[0-9a-f]{8}` **0 命中** | resolved |
| F-7107 | P3 | **B-36 同步后 brief 与 DevPlan 各有若干叙述段仍是「2 条」旧口径**，与同文件的验收口径段（已改 4 条）自相矛盾：`brief.md:18`「除冻结隔离清单内的 2 条 skip 外」、`:22`「两条语义红（`:242`/`:279`）只 skip 不重写」、`:45` Out of scope 仍把 `dhr69-false-ready` 列为「在绿闸里但不在允许路径」、`:46` 红线「只许 skip 恰 2 条」；DevPlan §3.2 DHR_71「目标」段同样仍写「2 条 skip」 | 上述行号（`master@1b4b1ce`、`wt/DHR_71@0117247`） | **只登记不改**：brief 是 DevPlan 的只读副本、DevPlan 是主控的权威文件，worker 不改（AGENTS.md 编排协议段）。施工按 brief 顶部「B-36 同步」段 + DevPlan「做什么 / 验收口径 / 变更范围」三段的 4 条口径执行，冲突以 DevPlan 为准（brief 自述）。建议主控收口时顺手统一叙述段措辞，免得下一个 reader 按旧口径核销 | **resolved**（主控 2026-09-02：DevPlan 目标段 master `25d697d`、brief `:18/:22/:45/:46` 已统一为 4 条 / `dhr69` 仅 `:179`） |
| F-7108 | P2 | **`%TEMP%` 停顿形态（BL-17 一族）：`persistState()` 的原子 rename 在 `%TEMP%` 上长时间不返回，driver 卡在 `attempt_started` 之后、第一次 Herdr CLI 调用之前**。识别特征：`dumpDriverScene` 打出 `events(3)=["run_created","node_started","attempt_started"]` + fake 六个计数器**全 0** + 等待打满上限。代码位置 `relay-core/store/store.mjs` `registerReceipt`（`:484`）：`:497` `emitEvent({kind:'attempt_started'})` 落账成功 → `:498` `await persistState()` 停住 → `registerReceipt` 不 resolve → `launchHerdrAgent` 后续链一次都没开始。与 BL-17 已知的 `EPERM: rename` **文本形态是同一根因的两种表现**（同一轮里也出现过 EPERM 形态，栈 `store.mjs:554:9`，也是一处 `persistState`） | 本届 2 次复现：`evidence/gate-round2-20260902T0714Z.txt`（`dhr69-false-ready:180`，上限 90_000 打满）、`evidence/gate-round2-20260902T0810Z.txt`（`herdr-adapter:532`，上限 120_000 打满）；EPERM 形态见 `evidence/gate-round2-20260902T0739Z.VOID-bl17-eperm.txt`；现场取证（`%TEMP%` 24,592 目录 / 66,950 文件）见 progress「`%TEMP%` 现场取证」 | **只登记不修**，归 `DHR-BL-17`。**不是「上限给少了」**：`herdr-adapter:532` 的上限已是按链推导的 120_000（合法链 80s × 1.5），打满 120s 时 fake 计数器仍为 0——环境停顿超过任何合法上限，抬上限无意义。生产侧 `persistState` 是否要加超时/重试**不在本卡允许路径**（`store/**` 明确 Out of scope），归 BL-17 或另卡。用户已授权主控清理 `%TEMP%` 超 1 天的 `dh-*` / `.tmp` 目录，清理后重跑三轮 | open（范围外 → BL-17） |
| F-7109 | P1 | **启动期 Attention 写入前的另一种持久化停顿**：`dhr69-false-ready:180` 已完成首次启动观测，事件账为 `run_created → node_started → attempt_started → host_observation_changed(alive)`、fake 计数为 `agentGets=1 / paneGets=1 / paneSplits=1`，但 90s 内未写出应有的 `human_input_requested`，节点仍 `running`。这**不符合 F-7108**「止于 `attempt_started` 且 fake 全 0」签名，不能改写或关闭 F-7108。 | 清理原 `%TEMP%` 后的 `gate-round{1,4}`；隔离子进程 TEMP 的 `gate-isolated-temp-round1-20260902T234348Z.txt` 通过（51/4/0，333.403s），但紧随的 `gate-isolated-temp-round2-20260902T235550Z.txt` 仍复现（50/4/1，572.898s），同一 4-event / fake-nonzero 快照。 | 本卡只记录并保持 blocked；不把隔离 TEMP 当作收口办法。现场只证明停顿发生在首次 Herdr 调用之后、启动期 Attention 落账之前；是否仍为 Store `persistState()`、调度/资源争用或其他持久化点，需要 DHR-BL-17 的专卡诊断。`store/**` 不在 DHR_71 允许路径。 | open（范围外 → DHR-BL-17 专卡诊断） |

## E 阶段复核发现（两路：教训 / 一致性）

| ID | 级别 | 提出者 | 问题 | 证据 | 处理 | 状态 |
|---|---|---|---|---|---|---|
| F-71-CON-01 | P1 | `dhr71con`（一致性 · C-71-01） | `agent-node.test.mjs` 两条 Codex 启动用例的首条 `checkpoint_recorded` 等待取 30s，只按 `herdrReadyTimeoutMs` 推，漏算了同一条 Codex 启动链的 `paneSplit`(10s) + `agentStart`(60s)——合法链至少 80s，与同链的 `herdr-adapter:510` 已取 120s 横向矛盾 | `review-consistency-codex.md` §2 | **采纳**：`:198`(S4，体内等待本卡拥有) / `:231` 两处 30_000 → 120_000（= (10_000+60_000+10_000)×1.5），progress 机器证 B 清单同步；重跑三轮 | open → 派 worker 修 |
| F-71-CON-02 | P1 | `dhr71con`（一致性 · C-71-02） | `herdr-adapter:510` 顺序两段等待最坏 120s + 75.015s = 195s，超过绿闸命令 `--test-timeout=180000`：最坏合法路径会先被 Node 外层超时截断，第二段 `untilEvent` 的 dump 出不来，违背「超限即有界失败并打印现场」 | 同上 | **采纳**：绿闸命令改 `--test-timeout=300000`（覆盖单用例最坏 195s，且 agent-node 文件级 180s 常量仍 > 其最坏 120s 不动）；task_plan 是一次性消耗品不回改，命令变更记 progress；重跑三轮 | open → 派 worker 修 |
| F-71-CON-03 | P2 | `dhr71con`（一致性 · C-71-03） | `dhr64-driver-observation` 夹具与 `dhr70-submission-gate:350` 仍是先登记 `rm`、后登记 `driver.stop()`，失败/超时清理路径与 652s 同型顺序隐患；不在本卡允许路径 | 同上 §3 | **只登记不修**：入 backlog（BL-17 补一条测试侧巡检清单），另卡处理 | open（范围外 → backlog） |
| F-71-LES-01 | P1→**驳回（措辞部分采纳）** | `dhr71les`（教训 · 候选-35/55） | 认为 agent-node 两条「先等 `checkpoint_recorded` 再断言同一事件存在」、`:510`「先等 `waiting_human` 再断言同一状态」与候选-35「等不变量不等期望值」冲突，等待成功成了断言的主要证明 | `review-lessons-codex.md` | **主控裁决**：候选-35 针对的是「先写事件账、后写快照」的非原子落盘导致**读到过期快照**——DHR_71 的等待目标正是事件本身或已收敛的派生状态（`store.readState()`），不存在快照滞后；等待有上限 + dump，目标不来就 fail，不是"等出我要的值"。等待之后的断言并非只重复目标：`:231` 断言 Receipt 文件内容，`:510` 断言 Attention 恰 1 条、detail 含 `herdr_status=blocked`、无 HOST_LOST、`sent=0`、`paneKills=0`——都是独立性质；`:198` 那条重复 `some(checkpoint_recorded)` 是冗余断言，不是缺陷（且 S4 已 skip，断言归 DHR_72）。改等待目标为「独立不变量」超出 task_plan 冻结的「等目标事件」口径。**措辞采纳**：本卡不声称"按候选-35 消除等待脆弱性"，只声称"等待有界 + 现场可见"，review/DONE 措辞按此收紧。按候选-26，本驳回另派 fresh 窄审攻击（见 E-7113） | 驳回（待窄审） |
| F-71-LES-02 | P2 | `dhr71les` | 三轮绿闸结论未随带并发/负载条件（候选-31） | 同上 | **采纳**：review/DONE/E9 措辞固定为「在该五文件命令、`--test-concurrency=1`、本机负载下」 | open → 收口措辞 |
| F-71-LES-03 | P2 | `dhr71les` | review brief 把 HEAD 写死为 `ecdc941`，实际 `28af9af`（候选-65 形态；期间无 `relay-core/test/**` 变化） | 同上 | **采纳**：本次不影响结论；后续派单只写「当前 HEAD 自报」+ 冻结的代码锚 | resolved（记教训） |

> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞。
