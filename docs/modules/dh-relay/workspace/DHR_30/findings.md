<!-- findings.md -->
# findings — DHR_30 Relay CLI 与可选 DSH/Pi Bridge

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|----|------|------|------|------|------|
| F-001 | P1 | DHR_30 的正式 CLI 验收要求经同一 Runtime/RPC 执行 `list/status/inspect/events/start/stop/resume`，但 DHR_52 只交付 RPC seam、DHR_51 host 不开 RPC。 | `relay-core/rpc/server.mjs`；`runtime/host.mjs`；P5 §3.2 | 用户选 A；A-13/A-14 将 repo service、operation ledger、正式 RPC/Read Model 合同纳入 DHR_30，B-18 更新计划。 | resolved |
| F-002 | P2 | master 历史重写后，dev_plan 各处登记的历史 verify SHA（0e2dd54 / 5971d8d / 95e2e6f / b96b5b7 / df8184d 等）不在当前 master 可达历史内，仅靠 `keep/dhr30-pre-rebase-260827` 保留线可达。 | E-005；`git log master` 与 P5 §3.1 对照 | 用户 E10 裁决（2026-08-28）：整批入验收池待裁；本卡不改历史。 | open（已入验收池） |
| F-003 | P3 | design/evidence 编号 10 被两个文件占用：`10-P4-多控制面Pilot报告.md`（master）与 `10-DHR30-Runtime服务与CLI合同-交叉审核记录.md`（本卡）。 | rebase 后 `design/evidence/` 目录 | 用户 E10 裁决（2026-08-28）：整批入验收池待裁。 | open（已入验收池） |
| F-004 | P1 | `rpc/server.mjs` 用 `Promise.resolve(invoke(...))` 分派，handler **同步抛错**时异常抛回 `handleFrame` 被当成不可预期错误 `dropConnection`——所有同步 throw 的稳定拒绝（`authorize` 的身份不符最典型）都表现为静默断连，客户端连 reason 都收不到，违反 design/07 §5.2。 | E-011；`test/rpc-service.test.mjs` 「contracts 是连接唯一首请求」用例首跑红 | 已改为 `Promise.resolve().then(() => invoke(...))`，两个分派分支都改；用例钉住「失败的身份确认不得断连」。 | resolved |
| F-005 | P1 | launcher 只按字段比对盘上 descriptor 时无法判断「当前性」：上一届留下的陈旧 descriptor 与本届同形（同端点、同 capability_hash、state=ready、合法 UUID），于是 service 崩溃重启窗口里 launcher 会拿旧 generation 认为已连上。 | E-012；`test/service.test.mjs` 「service 被强杀后重启」用例首跑红（新旧 generation 相同） | 已加 `handshakeIdentity()`：launcher 必须向端点上真正在跑的 service 发 `contracts` 回证，以**服务端自报**的身份为准；陈旧 descriptor 会被服务端以 `E_SERVICE_IDENTITY_MISMATCH` 挡下并继续等待覆盖。 | resolved |
| F-006 | P2 | `runtime/service.mjs` 运行期调用 `discoverRepo()` 会把内存里的 ledger 对象整个换掉；正在进行的 operation 之后的所有 phase 推进都写进一个已无人引用的对象，Receipt 永远提交不上 → 同键重试会**再执行一次**（并发同键因此产生两个 Receipt）。 | E-011；`test/rpc-service.test.mjs` 幂等与并发两条用例首跑红 | 已拆成 `bootstrapDiscovery()`（仅启动时读/修 ledger 与 runs.json）与 `scanRuns()`（运行期只重扫盘上 Run 与 legacy）。 | resolved |
| F-007 | P2 | design/08 §1 要求 RPC error response 携带 `receipt`，但冻结的 `relay.rpc/v1` `error_response.data` 是 `{reason, detail}` + `additionalProperties:false`——契约与合同互斥，failed Receipt 无处可回。 | design/08 §1 与 `contracts/relay.rpc.v1.schema.json` 对读 | 已按合同为 `data` 增可空 `receipt`，同批重生成 capability baseline / fixture manifest / audit token（`capability_hash` 变为 `2971189e…`）。**返工轮补正**：当时把它做成了 optional，与「一律」不符，已由 F-012 收为 required 并恒发（`capability_hash` → `a990fdda…`）。 | resolved |
| F-008 | P2 | design/08 §3 的 `next_seq` seed 口径只算「完整 v2 Run 根」，但一个建到一半的 `R00N-…/` 目录同样意味着该号已发出；按字面口径 seed 会把它漏掉，下一个 Run 拿到同号后撞进半成品目录（`run.json` 是 `wx` 独占创建，直接 EEXIST）。 | E-010；`test/discovery.test.mjs` 「残缺根只进 report」用例 | 实现取「所有 run-id 形态的目录名 + ledger 保留号 + 索引 max」的最大值，比合同字面更保守；已在 `discovery.mjs` 注释说明。批次A小审裁断：**保守 seed 正确且必要，建议回写 design/08 §3**。用户 E10 授权（2026-08-28 AskUserQuestion「随收口一并回写」）后已回写 design/08 §3 保守 seed 口径。 | resolved |
| F-009 | P2 | design/08 §3 说「Run 根已存在而 ledger 不在时作为孤儿 Store fail-closed」，但 design/07 §4 又说「Run Store 才是 Run 真相」。若把孤儿 Run 从 Read Model 里隐藏，等于用索引缺失否定事件账里的事实。 | design/07 §4 与 design/08 §3 对读；`discovery.mjs` `orphan_store` 分支 | 本卡取「进 report + 仍可列出只读 + 其号计入 seed 绝不重发」。批次A小审裁断：该解释成立，但**实现没做到自述的只读**（见 F-013），且孤儿投影/拒绝码应先入合同再实现。**返工轮进展**：只读已由 F-013 落成机器事实（投影 `read_only:true` + `E_ORPHAN_STORE_READ_ONLY`），拒绝码已入 `contracts/reason-codes.md`（含与 `E_LEGACY_READ_ONLY`/`E_RUN_NOT_FOUND`/`E_SERVICE_NOT_READY` 的边界与重试语义）。**孤儿投影语义已回写 design/08 §3**（用户 E10 授权 2026-08-28）：fail-closed=拒写不隐藏、只读可订、控制拒 E_ORPHAN_STORE_READ_ONLY、号计入 seed。 | resolved |
| F-010 | P3 | `subscribe` 对 legacy v1 Run 返回 `E_LEGACY_READ_ONLY`。design 只明确「legacy 只投影 list/status/inspect」与「所有 control action 拒绝」，未直说 subscribe 用哪个码。 | design/07 §5.1、design/08 §1；`service.mjs` subscribe 分支 | 按「legacy 没有 v2 事件流可订」取 `E_LEGACY_READ_ONLY`；批次A小审裁断：**该码正确**，语义已补写进 design/08 legacy 投影段（用户 E10 授权 2026-08-28）：subscribe 对 legacy 同码拒绝，非 RUN_NOT_FOUND 非 NOT_READY。 | resolved |
| F-011 | P3 | `cli/main.mjs`（步骤 4，批次 B 范围）仍带一套自己的 `ensureService`：只探连通性、不做 descriptor 回证、不走新的 `runtime/launcher.mjs`，也没有 `pending-operations.json`（design/08 §2 要求客户端持久化 request record 后再重试）。 | `cli/main.mjs:25-59` 与 `runtime/launcher.mjs` 对读 | 本批不动（范围外）。批次 B 应删掉 CLI 内联发现逻辑改用 `ensureRuntimeService()` + `readLocalUserCapability()`。 | open |

| F-012 | P1 | 【批次A小审 R-A-01】design/08 §1 规定所有 RPC error data 带 `receipt: Receipt\|null`，实现把它做成可选且非 operation 错误直接省略——冻结 schema 与运行时双双偏离合同。 | codex 小审；design/08:32-33,49；`relay.rpc.v1.schema.json`（886eba1）；`rpc/server.mjs:119-125` | 冻结 schema 的 `error_response.error.data.required` 收为 `["reason","receipt"]`；`sendError()` 改为恒发 `receipt: receipt ?? null`（非 operation 错误 null，operation 可预期失败带 failed Receipt）。补契约反例 `dhr30-error-data-omits-receipt`（省略 receipt → `E_MISSING_FIELD@/error/data/receipt`）+ 运行期形状回归「每一份 error data 都带 receipt」（连接闸 / 未知 run / cursor 缺口三处断言 `receipt===null`，重启后 stop 断言 failed Receipt 且同键重试同一份）。同批重算三份基线，`capability_hash` 2971189e… → a990fdda…。9b20cfc；E-015 | resolved |
| F-013 | P1 | 【批次A小审 R-A-02】孤儿 Store 未 fail-closed：被投影成 `read_only:false` 的 v2 条目，`resume` 可建 actor 写入孤儿 Store——与 F-009 登记的「只读列出」自述不符。 | codex 小审；`discovery.mjs:142-147,193-197`；`service.mjs:279-314,451-470` | `scanRuns()` 接 ledger 并算 `claimed`/`orphans`：孤儿投影为 `source:"runtime-v2"` + `read_only:true`（不隐藏、也不可写）；`inspectRun` 的 detail 判据由 `read_only` 改为 `source==='legacy-v1'`，孤儿仍能读到真 `relay.run-state/v1`。`runControl` 在认领 run_id **之前**拒绝、`ensureActor` 兜底拒绝，两道闸都回新码 `E_ORPHAN_STORE_READ_ONLY`。回归「孤儿 Store 只读」：list/status/inspect/subscribe 可用、stop/resume 被拒、事件账零字节变化、无 `host-lease.json`。f81b44a；E-016 | resolved |
| F-014 | P1 | 【批次A小审 R-A-03】discovery 只看 `events.jsonl` 存在与否，不解析校验 seq；损坏事件账会被当完整 v2 Run 进 Read Model，违反「损坏 Store fail-closed」。 | codex 小审；`discovery.mjs:39-48,123-147`；design/07:180-184 | `store.mjs` 新出口 `readEventLog({root,run})`——就是 `openStore` 用的**同一段** `loadEvents`（逐行可解析 / 协议与 run 归属 / seq 从 0 严格连续 / 逐条过冻结 `relay.event/v2`），但只读、不像 `openStore` 那样重写 `state.json`。`classifyRunRoot()` 在 run/state schema 之后调它，抛错即 `corrupt_store` 进 fail-closed report。回归覆盖 JSON 坏 / torn tail / seq 缺口三类，并同时断言 `next_seq` 仍把损坏根计入 seed（与 F-008 保守口径不冲突）。eef216d；E-017 | resolved |
| F-015 | P2 | 【批次A小审 R-A-04】订阅中的 Run 在 actor stop/lost-lease 后经 `resume` 换 actor 时旧 barrier 不迁移，新 actor 的事件不再推给仍存活的连接。 | codex 小审；`service.mjs:185-188,402-408`；`test/rpc-service.test.mjs:263-304` | 订阅归属改为 (连接, run) 而非 (连接, actor)：subscription 记 `actor`/`barrier`/`lastSentSeq`/`buffer`。`detach()` 在 `actor.done` 摘掉旧 barrier；`attach()` 重挂时先回缓冲态，以新 barrier 的 `next_seq` 为切分点从新 Store 句柄补 `lastSentSeq < seq < next_seq` 的漏发事件、unshift 到缓冲队首后再 `drain()`；`emit()` 加 `seq <= lastSentSeq` 去重。回归 subscribe→stop→resume：推送 seq 序列与盘上事件账逐一 deepEqual（不重不漏不乱序），resume 的 `lease_acquired` 与两份 `operation_committed` 均推达。ddaa58f；E-018 | resolved |
| F-016 | P3 | `test/runtime.test.mjs`「detached 宿主存活可读…强杀 -9 后 Store 重建」在**全量并发跑**时偶发超时红（单跑 1.8s，全量下曾达 11.5s 后失败；紧接着的两次全量复跑均绿）。本轮四条修复都未触碰该路径。 | 本轮第 2 次全量 `npm test` 输出（124 pass / 1 fail）与随后两次 125、126、127 全绿 | 未处理（范围外）：疑为真实 detached 进程 + 固定等待窗口在机器负载高时不够。建议后续把该用例的等待改成有界轮询而非固定 sleep。用户 E10 裁决（2026-08-28）：入验收池待裁；F-038 的遗留进程清理已显著缓解载荷。 | open（已入验收池） |
| F-017 | P2 | `test/rpc-service.test.mjs`「ledger 每个 phase 崩溃后重试都收敛到同一 run_id 与同一 Receipt」把期望 run_id 硬编码为 `R001-crash-20260827`（:465），而 service 用 `localDateStamp()`（**本地**日期）发号；本地时钟跨过 2026-08-27 午夜后该用例**确定性红**（actual `R001-crash-20260828` ≠ expected），隔离复跑同样红。与批次 B 无关：该文件本批零改动，E-019 时全量绿。 | 批次 B 全量 `npm test` 133/135（E-024）；隔离 `node --test test/rpc-service.test.mjs` 同一断言红；`date` 显示本地已 2026-08-28 00:03 | 主控直修（E-025）：期望侧改为 `R001-crash-${localDateStamp()}` 现算并加注释，`test/rpc-service.test.mjs` 补 import；全量 135/135 绿。残余窗口另见 F-024。 | resolved |
| F-018 | P0 | 【批次B小审 R-C-01】首次并发两个 CLI 可各自生成 `client_id`，后写者覆盖 `<repoHash>.client.json`；先落 pending 后崩溃的进程，其残条会被**新** `client_id` 重放——复合幂等键变了，服务端当新请求**二次执行**。 | codex 小审；`cli/client.mjs:50-68`、`cli/main.mjs:147-152`、design/08:97-100 | identity 文件改 `wx` 独占创建，`EEXIST` 即重读盘上赢家采用之，绝不覆盖（temp+rename 禁用）；pending record 持久 `client_id`，启动收敛按 record 内身份重放原幂等键，旧格式残条缺 client_id 一律 fail-closed（E_STORE_CORRUPT）不换键重放。双 CLI 并发首启身份唯一回归钉住。2a7eebf；测试 3a9183d。定向复审（E-033）closed-with-note：实现合规；回归杀伤力不足归入 F-023 二轮补强；「旧格式残条 fail-closed 阻断升级」主控裁决可接受——CLI 未发布、盘上无旧格式存量。二轮补记：RW2-3① 补强回归实抓一处残余竞争——EEXIST 输家在赢家 `wx` 建档与内容落盘之间重读到空/半截 JSON，SyntaxError（无 `.code`）被误判 `E_STORE_CORRUPT` 而退出 1；已修（2784f38）：EEXIST 后 25ms×20 短退避重读等赢家写完，耗尽才 fail-closed，「绝不覆盖」语义不变。 | resolved |
| F-019 | P1 | 【批次B小审 R-C-02】pending-operations.json 是无锁 read-modify-rename：两个 CLI 并发 `add`/`remove` 会互相覆盖丢条；record 也未持久 request_digest 与完整请求。 | codex 小审；`cli/pending.mjs:43-67`、`cli/main.mjs:147-150`、design/08:97-100 | 一轮返工：`wx` 独占锁 + 10s 陈锁回收 + 完整请求落账（2a7eebf/3a9183d）。定向复审（E-033）**not-closed · R-D-01**：陈锁回收不安全——持锁超 10s 未刷 mtime 会被回收，旧持锁者随后**无条件 unlink** 可删掉新持锁者的锁，第三进程并入临界区、两把逻辑锁并存仍丢账（`pending.mjs:60-76`）。二轮 RW2-1 已落（c36ca54）：锁内容 `{pid, token, at}`（token=randomUUID），释放与提交（rename/收账 unlink）前重读锁文件核验 token——非己锁绝不 unlink、易主放弃本次提交整段 read-modify-write 从抢锁重试；陈锁回收 unlink 后以 `wx` 抢建、抢不到按普通竞争退避（防双回收）；测试以「强制临界区中途回收」场景钉零丢失（a987fcd）。二轮定向复审（E-040）仍 not-closed（**R-E-01**：token 核验到 rename/unlink 之间无原子围栏——持锁进程冻结/长 I/O 超 10s 被回收后醒来，仍可在极短窗提交进新持锁者的临界区；mtime 只降频率、不能证明持有者已死）。**主控直修（E-041）**：陈锁回收改**双判据**——mtime 陈旧**且**锁上登记的持有者 pid 已死（kill 0 报 ESRCH；EPERM 按活）才回收。活进程哪怕冻结超阈也绝不回收，醒来仍是唯一持锁者——能被回收的持有者都永远不会再提交，「校验后易主」微窗从根上不存在（CLI 是本机私有访问，pid 语义可用；pid 撞号的代价只是锁暂不可回收 → E_PENDING_LOCK_BUSY 稳定拒绝，非正确性损失）。token 释放/提交双核验保留作兜底。①′ 钉子用例：陈旧 mtime + 活 pid（本进程 pid 冒充冻结持有者）→ 稳定拒绝且外锁健在，只看 mtime 的实现必红。CLI 17/17、全量 144/144 绿。三轮定向复审（E-042）仍 not-closed（**R-F-01**：判死与 unlink 非同一文件对象——A 判死旧锁的瞬间 B 已回收并 wx 建新活锁，A 的 unlink 删掉的是 B 的锁；文件系统给不出「检查对象后原子删除」）。**已满 3 轮，按 G6 停下摆用户。** 主控推荐方案：**取消自动回收**——陈锁一律 fail-closed 稳定拒绝，错误信息带持有者 pid/时间与锁路径、指引手工删除（崩溃残锁是罕见事件，手工恢复换绝对互斥）；备选 = 引内核语义锁（Node 无跨平台原生支持，需引依赖）。待用户裁决。 | open |
| F-020 | P1 | 【批次B小审 R-C-03】`pending-operations.json` 落在业务仓 `.dh-relay/` 下，创建与写入未设 owner-only 权限，与 design/08 §2 私有要求不符（identity 文件已做 0600，pending 没做）。 | codex 小审；`cli/pending.mjs:14-16,43-49`、design/08:62-64 | 一轮返工：文件/临时/锁 `mode:0o600`、目录按需建时 `0o700`（2a7eebf）。定向复审（E-033）**not-closed · R-D-02**：实际 mutating 路径先连 service，`.dh-relay/` 早已被 descriptor 写入用**默认 ACL** 建好——`mkdir(mode:0o700)` 对已存在目录不收权，pending 目录并非可靠 owner-only（`runtime/endpoint.mjs:62-67`、`pending.mjs:46,101`）；且无权限断言测试。二轮 RW2-2 已落（c36ca54）：每次写前对目录 mkdir + **显式 `chmod 0o700`**（已存在也收），pending/临时/锁三类文件 mode 0600 + 显式 `chmod 0o600` 兜底（pending 在 rename 后 chmod，顺带收紧上一届宽权限账面）；权限断言已补（a987fcd）：POSIX 断言 `stat` 真实 mode（0700/0600，落真实 CLI 落账路径与「已存在宽权限」场景各一处），win32 降级断言写入路径无异常。二轮定向复审（E-040）仍 not-closed（**R-E-02**：合同 owner-only 没给平台例外，而 win32 chmod 近似 no-op、测试降级不构成证明；**R-E-03**：临时/锁文件权限断言非确定性）。**主控直修（E-041）**：win32 改 `icacls` 真收 DACL——目录去继承 + 整表重授「仅当前用户 (OI)(CI)F」（文件按对象继承同样 owner-only），设不上 fail-closed（E_STORE_CORRUPT）、每进程每目录一次；win32 测试断言真实 DACL（icacls 输出每条显式 ACE 都属当前用户）。R-E-03：writeRecords 增测试专用 `beforeRename` 钩子，确定性停在「临时文件已写、锁在手」瞬间 stat 断言 0600（POSIX），替换掉碰运气的高频轮询观测（三轮复审判 closed-with-note）。三轮定向复审（E-042）仍 not-closed（**R-F-02**：`/inheritance:r + /grant:r` 不清除**既有显式** ACE，「整表仅当前用户」名不副实，测试未预置 foreign ACE 也未逐对象断言；**R-F-03·P2 合同冲突**：design/08:59-64 要求 `.dh-relay/` 的 descriptor 沿业务仓 ACL 可发现，收紧整目录与之相抵——owner-only 的 pending 与共享的 `.dh-relay/` 不能同层共存）。**已满 3 轮，按 G6 停下摆用户。** 主控推荐方案：pending（连同锁/临时文件）迁独立私有子目录（如 `.dh-relay/private/`）——目录级 owner-only 与 descriptor 共享互不干扰；属合同 §2 路径口径回写，需用户确认后实施；备选 = 合同给 win32 加 ACL 例外条款（不推荐，弱化私有保证）。 | open |
| F-021 | P1 | 【批次B小审 R-C-04】非 follow 的 `events` 用固定 400ms 静默窗判 backfill 收齐；RPC 合同没有这个时限，慢管道会静默丢事件。 | codex 小审；`cli/main.mjs:238-245`；`runtime/service.mjs:508-512` | 非 follow 显式带 `after_seq`（用户传入或 0——服务端缺省不补发），按订阅快照 `next_seq` 逐条计数，收齐 `(after_seq, next_seq)` 全部补发即完成；`E_CURSOR_GAP` 退化为纯快照；30s 只作 fail-out 报错退出码非 0，绝不作完成判据。慢管道回归（发 3 条、停 900ms 超旧静默窗、再发齐）钉住零丢失。2a7eebf；测试 3a9183d。定向复审（E-033）closed-with-note：乱序帧被单调 seq 去重丢弃后走超时报错而非错误完成——合同要求 service 严格连续发送，属 fail-safe 限制，不另立 finding。 | resolved |
| F-022 | P2 | 【批次B小审 R-C-05】客户端把**缺失**的必填 `data.receipt` 静默归一为 null——协议违约被误报成「结果未知，可安全重试」，违反 fail-closed 消费。 | codex 小审；`cli/client.mjs:163-170`、design/08:49-50 | `call()` 归一前检查 `'receipt' in data`：缺失即抛 `E_PROTOCOL_VIOLATION`（措辞含服务端 reason 与「协议违约：error data 缺 receipt」），走传输层失败路径使 pending 保留、CLI 报协议错误而非「结果未知」；`receipt:null` 是合法值不受影响。假 service 伪造缺字段负例钉住（含「不得出现结果未知措辞」断言）。2a7eebf；测试 3a9183d | resolved |
| F-023 | P2 | 【批次B小审 R-C-06】CLI 测试缺口：无首次双 CLI 身份竞争、无 pending 并发写、无慢 backfill 场景；成功 `resume --json` 未覆盖；「凭据缺失」只直调 helper 非 CLI 子进程 E2E。 | codex 小审；`cli/test 对照 cli.test.mjs:297-304,406-430,508-513` | 一轮返工 CLI 8→15 用例全真实进程（3a9183d）。定向复审（E-033）**not-closed · R-D-03**：两组并发回归杀伤力不足——①identity 并发只查最终文件唯一，改回普通覆盖写仍可能过测，未证两 CLI 实际采用**同一**赢家身份；②双 CLI mutating 只断言残条「至少一条」，未证两条 request 都落账零丢失（`cli.test.mjs:604-624,664-710`）。二轮 RW2-3 已落（a987fcd）：①identity 并发改用可记录 handshake 的假 service——断言两次 contracts 首请求 `client_id` 相同、identity 文件内容即该 id，另加预置身份「逐字未变 + 握手用预置 id」负向钉子；②双 CLI mutating 改「诱饵门控」（假 service 把诱饵收敛答复扣到两进程都重放才放行，消除合法收敛路径的概率干扰）+ 轮询到**两条 request_id 都在账且锁已释放**才 SIGKILL，超时即红，收敛语义断言维持。补强实抓 F-018 一处残余竞争（identity EEXIST 后重读撞赢家半截建档），修法见 F-018 行（2784f38）。 | resolved |
| F-024 | P2 | 【批次B小审 R-C-07】F-017 直修后仍剩极窄跨午夜窗口：`reservedRunId` 在测试开头现算，若午夜落在「计算日期」与「accepted 相位 service 发号」之间仍红。 | codex 小审；`rpc-service.test.mjs:463-470`、`runtime/service.mjs:304-310` | 主控直修（E-027）：期望二分——钉死相位（fixture 保留号）逐字比对，`accepted` 相位（service 现发号）只验 `R001-crash-\d{8}` 形态、盘上断言以实际 Receipt 的 run_id 为准；不再依赖测试进程时钟猜日期，窗口彻底消除。隔离 11/11、全量 135/135 绿。 | resolved |
| F-025 | P3 | 【批次B小审 R-C-08】argv 并非全严格：`list`/`start` 等忽略多余 positional（`relay list junk` 不报 usage）。 | codex 小审；`cli/main.mjs:33-55,112-120,139-156` | `POSITIONAL_COUNTS` 按命令声明 positional 形状（list/start=0、其余=1），多余/缺失一律 usage 退出码 1；七组负例（含 `relay list junk`、`relay start --run x junk`）钉住。实现 2a7eebf；测试 3a9183d | resolved |
| F-026 | P1 | 【批次 C C-4 阻塞】`run_list` 的源头投影在 `runtime/discovery.mjs` 中按目录扫描顺序直接 `push`，`runtime/service.mjs` 直接返回 `known.v2`；源头 Run Store 的 `group` 从 `running` 改为 `needs_you` 后字段会更新，但条目位置不移动，未满足 task_plan C-4 的「列表分组/排序位置随之移动（排序由源头给）」镜像合同。 | `relay-core/test/read-model-mirror.test.mjs` 首跑：1 pass / 1 fail；失败断言 `afterIndex !== beforeIndex`，实际均为 0；静态链 `runtime/discovery.mjs:172-180` → `runtime/service.mjs:listRuns`。 | 主控直修 `336b389`：`orderRunSummaries()` 按 `needs_you → running → done → failed`、词表外首现堆、null 殿后、堆内 `run_id` 升序；本轮按该规则修断言用例，明确证实 `[R001,R002]` 同属 running 的基线变为 R002 进入 needs_you 后的 `[R002,R001]`，并保留仅改 `run_status` 的逐字不变镜像证。 | resolved |
| F-027 | P3 | 【批次 C C-5 完工门取证环境】worker 侧全量 `npm test` 报 11 项 `EPERM`（锁文件创建失败），一度被判为产品缺陷阻塞完工门。 | worker（codex，`--sandbox workspace-write`）实跑 150 项 139 pass / 11 fail，失败路径 `%TEMP%\...\.dh-relay\private\pending-operations.lock` 与 `C:\Users\nash\.dh-relay\runs.json.lock`。主控 2026-08-28 在无沙箱 PowerShell 同树同提交复跑：150 项 **149 pass / 1 fail**，唯一红项是 `rpc-service.test.mjs:352` subscribe 超时（F-016 同族偶发，隔离重跑 `node --test test/rpc-service.test.mjs` 11/11 绿）；`EPERM` 一项不复现。 | 裁定为**施工沙箱取证限制、非产品缺陷**：两条报错路径（系统临时目录、用户主目录 `~/.dh-relay`）均在 codex `workspace-write` 沙箱可写根之外，被沙箱按设计拒写，与 F-019/F-020 的 private/ 收权无关。据此：完工门证据以主控无沙箱复跑为准（149/150 + validator 48/48 + audit 0 违规 + 三份基线零漂移）；派 headless worker 时不再要求其自证全量门。 | resolved |
| F-028 | P1 | 【批次C小审 R-G-01】Bridge 声称「凭据只读、缺失即拒绝、绝不创建凭据」，但 `connectDshBridge` 先调 `ensureRuntimeService()`；本机无凭据时 launcher 判 `state:'absent'` 会 spawn service，service bind 后调 `readOrCreateLocalUserCapability()` 建出凭据，Bridge 随后读到刚建的凭据继续连接——**净效果是「调一次 Bridge 就让凭据凭空出现」**。 | codex 批次C小审；`adapters/dsh-bridge/index.mjs:34-40`、`runtime/launcher.mjs:145-155`、`runtime/service.mjs:570`。 | **主控部分驳回**：adapter 越权非缺陷；保留的真问题是注释误导。RW3-1 已于 `0287c1e` 精化为「adapter 自身只读、不铸造；本机尚无 service 时，首次成功 bind 的 service 依既定引导路径创建凭据」，并把 `E_LOCAL_USER_UNAUTHORIZED` 明确为「service 已就绪但凭据仍缺失」。 | resolved |
| F-029 | P1 | 【批次C小审 R-G-02】Bridge 订阅的失败路径两处不闭合：①socket close 时 `openSession` 的 `pending` 请求不被 reject，只能吃满 120s 超时，导致「500ms×20 有界退避」实际可被拉到约 40 分钟；②`reconnect()` 重试耗尽后直接 `return` **静默停止**，API 无 `onError` / 终态回调，宿主永远不知道订阅已死。 | codex 批次C小审；`adapters/dsh-bridge/index.mjs:64-73`（close 不 reject pending）、`:176-202`（耗尽直接 return）。 | RW3-2 已于 `0287c1e` 修复：close/error 遍历 pending，清 timeout 后以 `E_CONNECTION_CLOSED` reject；订阅重试耗尽 `onClosed('retries-exhausted')` 并摘除活动集。`0ee57b5` 以真实 Runtime service/socket 钉住断线后事件与 `events.jsonl` seq 逐字一致、耗尽仅通知一次、pending 5s 内失败；续传红测还实抓并修正快照高水位误滤 cursor 补发。 | resolved |
| F-030 | P2 | 【批次C小审 R-G-03】C-4 镜像断言②用 `{...beforeAlpha, run_status:'running'}` 构造期望——生产投影**新增**字段会同时出现在改前与改后对象里被一并接受，断言对「字段面增/漏」没有杀伤力；另测试的轮询 `call()` 无超时，schema 不合导致 RPC 断连时会**挂起**而非精确报红。 | codex 批次C小审；`test/read-model-mirror.test.mjs:102-113`、`:44-54`；`rpc/server.mjs:183-190`（schema 不合即断连）。 | RW3-3 已于 `0ee57b5` 完成：改前与改后均 `Object.keys(...).sort()` 逐字钉住 `group/read_only/run_id/run_status/source/updated_at` 六字段；RPC 轮询加 15s 明确 `RPC call timed out` 失败。实际投影字段与合同一致，未迁就实现改字段集合。 | resolved |
| F-031 | P3 | 【批次C小审 R-G-04】pending 锁稳定拒绝时的恢复指引把 pid 当成可据以删锁的判据，但 pid 只是**瞬时存活线索**、不是所有权凭据（撞号 / 期间易主皆可能），「判死」到「人工删除」之间仍有人工竞态。 | codex 批次C小审；`cli/pending.mjs:160-165`。 | **主控直修**：两条拒绝文案均补明「pid 不是所有权凭据」，并把恢复步骤改为「先停光本机所有 relay CLI → 复核锁面内容仍属该已死进程 → 才手工删除」。既有测试只匹配 `lock-holder-dead` / `lock-holder-alive` 前缀，不受影响；CLI 17/17 复跑绿。 | resolved |
| F-032 | P1 | 【返工定向复审 R-H-01】`reconnectAttempts: 0` 时断线后的 `reconnect()` 根本不进入循环——既不 `closeSubscription()`、不摘活动订阅，也不触发 `onClosed`，零预算路径仍是静默死亡（F-029② 的漏网边界）。 | codex 返工定向复审；`adapters/dsh-bridge/index.mjs:209-225`（通知只存在于循环内 catch）。 | **主控直修**：循环走完仍 `active` 即统一 `closeSubscription('retries-exhausted')`。新增回归「reconnectAttempts=0 零预算断线也走 onClosed」（真实 service + 真实 socket），变异验证：删掉该行此用例 5.6s 红。 | resolved |
| F-033 | P2 | 【返工定向复审 R-H-02】`0287c1e` 删掉 gap 分支的 `continue` 后，重连途中收到 `E_CURSOR_GAP` 会消耗一次重试预算并吃退避；`reconnectAttempts: 1` 时最后一击遇 gap 直接 `onClosed('retries-exhausted')`，违反「gap → 整体重快照」合同（task_plan C-1 / design/08）。初始订阅的 gap 不受影响（外层 catch 先清 cursor）。 | codex 返工定向复审；`adapters/dsh-bridge/index.mjs:214-223`。 | **主控直修**：gap 分支恢复不占预算的重试（`attempt -= 1; continue`）——cursor 清空后 `after_seq=null` 的下一次 subscribe 不可能再 gap，无死循环风险。新增回归「重连途中 gap 不消耗预算」（fake service 精确扣第二次 subscribe 回 gap；断言三次 subscribe 的 cursor 序列 `[null, 3, null]` 与 attempts=1 下不触发耗尽），变异验证：去掉 `attempt -= 1; continue` 此用例 8.5s 红。 | resolved |
| F-034 | P1 | 【轮2全卡复核 R-J-01】唯一 Store 写者纪律「未真正封死」：`host-main.mjs` 可直启 `runHostSession()`、`startrun.mjs` 公开 `createRunWithNumbering()` 直建 Store，绕过 service 的 contracts 闸 / ledger / Receipt；`service.mjs` 还 re-export 了 `openStore`。建议删除/私有化这些入口。 | codex 轮2复核；`runtime/host-main.mjs:1-24`、`runtime/startrun.mjs:50-83`、`runtime/service.mjs:618`。 | **主控半采纳半驳回**。采纳：①删 `service.mjs` 零消费者的 `export { openStore }`（grep 证无人 import）；②新增 `test/control-plane-imports.test.mjs` 两钉——控制面（cli/adapters/rpc）禁引 Store/宿主/发号原语（变异证有牙）+ 生产代码零调用 host-main/createRunWithNumbering/startDetachedHost 钉死现状。**驳回**「删除/私有化 DHR_51 宿主原语」：design/07 §4 原文限定词是「控制面进程」（CLI/Bridge/Pi 只发 RPC——这条已守住，取证 cli/adapters 零 store import）；`host-main`/`startrun` 是 DHR_51 交付的宿主侧/引导原语，其唯一写者仲裁是 **lease fencing**（writeGuard 失租拦截，双 launcher/失租/fencing 有整套回归）——import 可达 ≠ 纪律破坏，抢到 lease 的宿主本就是合法唯一写者（「关窗不停工」设计）；且生产代码零调用（grep runtime/cli/adapters/rpc/store 零命中，仅 DHR_51 遗留测试在用，测的是它们自己的交付物）。design/07 §4 措辞澄清（「唯一写者=fencing 仲裁，import 禁令限控制面」）作为契约文档回写，与 F-008/009/010 同批留待用户确认。**轮2修复窄审（E-056）复判**：宿主入口（host-main/startDetachedHost）不删的半支**确认成立**（复核者独立验证 runHostSession 先取 lease 再 writeGuard 开 Store）；但驳回中 `createRunWithNumbering` 一支被推翻——它是 fencing 外真实写路径 → F-036；import 钉的 service 豁免口 → F-037。两条已修（`de79d05`），措辞澄清回写项相应收窄为「唯一写者=fencing 仲裁**限宿主入口**；createRunWithNumbering 生产全面禁调（§3.3 已有明文）」——已按用户 E10 授权（2026-08-28）回写 design/07 §4。 | resolved |
| F-035 | P2 | 【轮2全卡复核 R-J-02】B1 双根实现正确（`service.mjs` 合并 v2 + legacy），但测试只分别验证两根，缺「v2 与 legacy **同份清单**并列」的完整验收断言。 | codex 轮2复核；`test/rpc-service.test.mjs:153-184`。 | **主控直修**：同用例补四断言——`include_legacy=true` 同份清单同时含 v2（runtime-v2）与 legacy（legacy-v1 且 read_only）、默认清单含 v2 且零 legacy；同场景上文已有 legacy stop/resume 双拒 `E_LEGACY_READ_ONLY`。全量 157/157 绿。**轮2修复窄审（E-056）判 closed**：四断言真钉「同份清单并列」，未退化成分别验证。 | resolved |
| F-036 | P1 | 【轮2修复窄审 R-K-01，推翻 F-034 驳回一支】`createRunWithNumbering` 是 fencing 外真实写路径：`startrun.mjs:77-78` 直调无 writeGuard 的 `createStore` 写 `run_created`，全程不取 lease、不经 contracts/operation ledger/Receipt；虽不能覆盖已有完整 Run（run.json 用 wx），但可在 fencing 外建全新 Store，并可**接管尚无 run.json 的残缺目录**（createStore 的 mkdir recursive 会采纳既存目录）。 | codex 轮2修复窄审；`runtime/startrun.mjs:77-78`、`store/store.mjs:355-359`。 | **主控直修（`de79d05`）**：建 Run 根改非递归 `mkdir`，EEXIST 即抛 `E_REQUEST_CONFLICT:run-root-exists`——残缺目录整体拒绝、零落盘、索引不推进；新增回归钉住三点（变异验证：守卫改回 recursive 此用例红）。头注补地位声明：DHR_51 宿主/引导原语，生产建 Run 唯一合法路径是 service operation ledger。宿主入口维持不删（窄审确认受 fencing 保护）。 | resolved |
| F-037 | P2 | 【轮2修复窄审 R-K-02】F-034 采纳半条的第二钉声称「生产代码零调用 createRunWithNumbering」，却豁免 `service.mjs`——service 将来新增该 import/call 两钉仍全绿，违反 design/07 §3.3「禁止 RPC handler 或 CLI 直接调用旧 createRunWithNumbering」明文。 | codex 轮2修复窄审；`test/control-plane-imports.test.mjs:71`。 | **主控直修（`de79d05`）**：删 `base !== 'service.mjs'` 豁免（service 现只引纯路径函数 `defaultIndexPath`，删豁免不破绿）；变异验证：service 加 `createRunWithNumbering` import 钉子精确报红。注释同步改为「任何生产代码无豁免」。 | resolved |
| F-038 | P3 | 【E-061 过程发现】CLI/launcher 族测试与冒烟会经 launcher 拉起 **detached** service-main 进程，测试收尾不回收：实测累计 13 个遗留进程全部指向已删除的 temp 测试仓，长期驻留（跨多轮全量跑）推高并发负载——与全量跑时的时序 flake（F-016 同族、E-057/E-061 两度出现）高度相关。 | `Get-CimInstance Win32_Process` 实查：13 个 `node service-main.mjs --root C:\Users\nash\AppData\Local\Temp\dhr30-*` 进程；逐一核对命令行后 `Stop-Process` 清理，清后计数=0。 | 已清理（E-061）。系统性修复（测试收尾杀 detached service / service 空闲自退）超出本卡范围：**登记为测试卫生缺口，随 E10 摆用户**——建议随 DHR_31 顺带或入 backlog。本卡全量门证据不受影响（清理后 158/158 绿）。用户 E10 裁决（2026-08-28）：系统性回收入验收池待裁。 | resolved（清理完成；系统性回收已入验收池） |

> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞
