# 教训复核（E5）· DHR-BL-10

- **复核身份**：codex CLI · `/codex` 插件 · 只读 · `--fresh` 独立 thread · 2026-08-27
- **独立性**：未参与实施；未继承其它任一复核会话
- **指令集**：[../review-briefs/review-lessons.md](../review-briefs/review-lessons.md)
- **口径**：16 条既有候选逐条裁决，禁止跳过

## 结论：有违反（4 条，均指向同一缺口）

判 **违反候选-1 / 候选-5 / 候选-6 / 候选-12**，四条共同指向：
**第 34 行（实拉分支）的大小写拒绝行为没有直接断言与变异对照**，代码同构与静态复核不能替代该执行路径证据。

复核方给出的安全修法（不真实拉起任何 CLI）：复用已有的 PATH/PATHEXT stub 技术，放 `claude`/`codex` sentinel stub，
非 DryRun 传入 `CLAUDE`/`CODEX`，断言非零退出且两个 sentinel 均未被调用；再对第 34 行去掉 `-CaseSensitive` 做变异证明断言会红。

**处置**：返工轮 4 完全按此实施 → 断言 `worker entry real-launch branch rejects miscased cli without invoking any cli`
（及 CODEX 同形一条），变异对照红相位 `SUITE FAIL (2)` 复现、还原复绿 → F-017 resolved。

## 其余裁决

- **候选-11 遵守了**：第 31/34 行同构改造 + F-010 对 `tools/host/`、`tools/adapters/` 双口径巡检
- **候选-13 未触及**，并特别指出：网络中断后未 stash/reset，**反而遵守了既有铁律**
- 其余 10 条判「本卡未触及」，逐条附理由

## 对 F-010 全库巡检的独立抽查（复核方自己动手核的 6 处）

`fake-adapter.ps1:28`（整数计数）、`relay-host.ps1:85`（generation 数值）、`psmux-adapter.ps1:13/15/27`（退出码/计数/空串）、
`Invoke-RelayBackendPreflight.ps1:89/135/175/210`（退出码/计数/布尔）—— 分类为「无害」**正确**；
`Invoke-RelayBackendPreflight.ps1:117` 的 `-like "$name|*"` 确为不敏感通配，但两侧同源于 session-name 语境、非冻结枚举或派发，
F-010 将其列为**范围外观察而非漏报，合理**。

→ **F-010「风险性不敏感比较为 0」的结论可接受**，其分类方法可信。

## 对主控反驳的独立判断

主控反驳施工方那条「已知局限」（理由是"测试红相位会真的拉起 CLI"）—— **复核方判主控反驳成立**，
依据是仓内已有 PATH/PATHEXT stub 技术可安全覆盖。

## findings 编号偏差的归属

判**不应新立候选**：应归入**候选-4**（派活前机械核验 brief 内部引用/锚点）并扩展该候选的核验范围至「预写的 findings/证据编号」。

## 教训候选裁决

| ID | 一句话 | 裁决 | 理由 |
|---|---|---|---|
| L-001 | 第三方 CLI 用 PATH + 环境自带认证 | **不够格** | 是良好设计选择，但未由错误/反例归纳出可迁移教训；凭据不入仓本已受宪章约束 |
| L-002 | `--help` 不是契约 | 措辞需改 | 收窄为「每个**拟使用**的参数须实测」，避免要求验证未使用参数 |
| L-003 | 换模型复核 ≠ 换 CLI 账号 | **不够格** | 「天然隔离」过强，独立性仍需按进程/配置/凭据/会话四层实核（注：本条由**主控**起草） |
| L-004 | 顺带修复先查教训库 | 措辞需改 | 「顺带修复」不准确，本案是扩展三分支时引入语言默认语义变化；应改为「涉及**语言默认语义**的重构/扩展，先核对适用的既有教训」 |

**建议新增候选：无**——先在本卡补齐第 34 行的安全 stub 反例与变异证据，该缺口应按候选-1/5/6/12 收敛，不应以新教训替代修复。

**处置**：返工轮 4 已按此裁决修订 `lesson_candidates.md`（L-001/L-003 → `rejected`，L-002/L-004 改措辞），并追加 L-006。
