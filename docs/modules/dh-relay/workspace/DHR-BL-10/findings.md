<!-- findings.md — 问题清单。🟢 边做边记。 -->
# findings — DHR-BL-10

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|----|------|------|------|------|------|
| F-001 | P1 | as-built/relay-core 开篇称「v1 现役且被 v2 只读作 Oracle」。若 Oracle 面覆盖 `tools/host/` 宿主层，则改 `relay-worker-entry.ps1` 会动到 v2 的比对基线，本卡范围需重新评估 | grep 命中 4 处：`as-built/relay-core.md:6`（两套契约层「并列共存、互不迁移——v1 现役且只读作 Oracle」）、`relay-core.md:244` 与 `relay-core/README.md:36`（「仓根 `tools/`（PowerShell）只读：行为、契约与测试命题作 Oracle，文件级实施不迁移」）、`relay-core.md:285`（Oracle 复验实例全为 P1 fixture）。反证三链：① `grep -rn "worker-entry\|relay-worker" relay-core/` **零命中**；② `relay-core/contracts/` 内 `claude\|codex` **零命中**，`compat-matrix.md:77` 明写 psmux/CLI「属 process executor 的一种实现细节，不进协议」；③ `capability_hash` 是 v2 新增字段（`compat-matrix.md:33`「— \| capability_hash \| 新增」），由 `relay-core/capability-baseline.json` 的 12 份 relay-core 自有契约 digest 算出，与仓根 `tools/` 任何文件无关 | **宿主层不在 v2 实际消费的 Oracle 比对基线内，不阻塞**。「只读作 Oracle」是 relay-core（v2 工作流）自己的地面规则（README 六条之首句即约束 v2 开发；同性质的纪律条款如「禁改 dh-crew」），约束的是 v2 侧不改仓根 `tools/`，不是冻结 v1 自身维护线——v1 明写「现役」，DHR-BL 系列维护任务就是其正规通道（本卡已经过入口闸用户确认）。v2 实际消费的比对输入只有两类：`tools/contracts/` 的 relay/v1 契约字段（compat-matrix 44 行对照）与 P1 fixture 钉死的行为命题（迟到判定/脱敏/lease，`relay-core.md:285`）；本卡改动不触及任何一类（允许路径已排除 `tools/contracts/` 与 `relay-core/`） | resolved |
| F-002 | P3 | zcode `--help` 列出 `--max-turns <n>` 但实际调用报 `Unknown option '--max-turns'`（zcode 0.16.5）。属上游 CLI 帮助文本与实现不一致 | 主控 2026-08-27 实测，见 `progress.md` E-001 | 不处置——本卡命令行不使用该参数；已在 backlog `DHR-BL-10`「现状证据」登记，供后续接入者避坑 | 遗留→backlog DHR-BL-10（已确认） |
| F-003 | P3 | 桌面端 `~/.zcode/v2/config.json` 明文存 BigModel / Z.ai 的 apiKey。属 zcode 桌面端既有设计，非本卡引入 | 主控 2026-08-27 排查 zcode 配置时发现 | 不处置——`brief.md` 已显式列为 out of scope；已向用户口头提示 | 遗留→backlog（已确认） |
| F-004 | P3 | 完成条件6的密钥闸若按 naive `git diff` 全文 grep 会命中**上下文行**：既有已提交的 findings F-003 行含「apiKey」字样 prose（描述桌面端明文存储的事实），使本卡 diff 出现假命中。按新增行口径 `grep -E '^\+'` 为零命中；另注 `risk-accepted` 一词会碰 `sk-` 模式（review.md 实例） | 施工 2026-08-27 双口径对照复测，见 progress E-008 | 不处置——本卡新增行确证零密钥零安装绝对路径；建议后续把 diff 闸固定为「仅扫描新增行」，避开 prose 与模式碰撞噪声 | resolved |
| F-005 | P3 | as-built/relay-psmux-host.md「测试与守卫」表快照过时：写「固定 15 套件」、agent-tool=32 断言；现势为 **16 套件**（缺列后来新增的 `relay-policy.ps1`）、agent-tool 实跑 **36** 断言。属文档快照维护面，超出本卡步骤 7 只准改两行的范围 | 施工 2026-08-27 全量回归输出（16 个 `=== <suite> ===` 头）＋本套件 `ASSERTIONS 36`，见 progress E-007/E-004 | 不顺手改——登记供复核/收口裁量：随 verify 快照一并刷新，或转 backlog | 遗留→backlog DHR-BL-12（已确认）|
| F-006 | P3 | **主控复看发现的行为变化，两棒 worker 均未登记**：原实现用 `$Cli -ceq 'claude'`（**大小写敏感**）二分支，而 PowerShell 的 `ValidateSet` 校验默认大小写不敏感且不规范化取值——于是 `-Cli CLAUDE` 旧码会落进 `else` 分支去拉 **codex**（潜在错派）。改成 `switch`（默认大小写不敏感）后 `-Cli CLAUDE` 正确拉 claude。命令行**字符串**逐字未变（完成条件 3 成立），但**派发语义对非规范大小写的入参变了**，方向是修复 | 主控 2026-08-27 逐行比对 `git diff -- tools/host/relay-worker-entry.ps1`；判据：PowerShell `ValidateSet` 默认 `IgnoreCase=$true`、`switch` 默认大小写不敏感、`-ceq` 大小写敏感 | **返工轮2 反转（主控裁定）：经教训复核备料发现与候选-5 冲突，已改为 fail-closed，见 F-009**——原「代码轮 1 判可接受随带修复＋补不敏感派发断言」的处置作废；本行最初揭示的 fail-open 错派根因（ValidateSet 大小写不敏感且不规范化取值＋旧 `-ceq` 二分支把 `CLAUDE` 静默送进 codex 分支）至此才被真正根治 | resolved |
| F-007 | P2 | 代码轮1 R1：zcode 断言全走 `-DryRun`，只验 worker-entry 第 31 行的打印；真正执行的第 34 行是独立代码路径，实拉 argv 形态无断言钉住，两段可能不一致；dry-run 打印本身也无变异级有效性证明 | codex 代码轮1 审查结论 `approved-with-P2`；返工轮1 落实：①非 DryRun stub 断言 `worker entry launches zcode with prompt-form argv`（PATH/PATHEXT 前置 .ps1 stub 抓实拉 argv）转绿，stub 固定串自证变红后还原复绿；②dry-run 打印锚点（worker-entry:31）施加变异 `--prompt`→`--invalid` 恰好该断言红，九字段登记 review.md 变异点表（E-012/E-013） | 返工轮1 收敛：B 断言 + D 变异验证均落地，未发现新问题 | resolved |
| F-008 | P2 | 代码轮1 R2：派发从 `-ceq` 改 `switch` 后语义修复无回归断言，存在改回去的风险 | 返工轮1 第1棒补 `-DryRun -Cli CLAUDE`/`-Cli CODEX` 全大写两条断言并 PASS（第2棒复跑 39 断言 SUITE PASS 确认在列） | 返工轮1 收敛：新语义已被断言钉住。**返工轮2 更正**：该两条断言语义与教训库候选-5 相反，已反转为 fail-closed 拒绝断言（见 F-009）；「防改回 `-ceq`」不再靠断言、而由 `switch -CaseSensitive` 实现本体承担 | resolved |
| F-009 | P2 | **本条返工根因**：返工轮1 把 F-006 揭示的缺陷用**大小写不敏感的 `switch`** 收敛并加「不敏感派发」断言——方向与本仓教训库候选-5 明文相反（冻结枚举必须敏感比对 fail-closed）；主控在教训复核备料时发现冲突后裁返工轮2 反转。另注意原 `-ceq` 时代并非正确的 fail-closed：`CLAUDE` 静默落 codex 分支本身就是错派 | `knowledge/教训库-候选.md` 候选-5 原文；review-briefs/rework-round2-case.md 主控裁定；红态 E-014 / 绿态 E-015 / 全量 E-016 | worker-entry 第 31/34 行两处 `switch` 改 **`switch -CaseSensitive`** ＋ `default{throw "…unsupported -Cli value '<实际值>'…"}`（不自行 exit，走 `$ErrorActionPreference='Stop'` 既有失败路径），三条分支体逐字未动、两处同构；两条断言反转为 `worker entry rejects miscased cli/codex instead of silently dispatching`（退出码非 0＋报错文案带实际值＋claude/codex 两家命令行都不得出现=反向证据并入）。已知局限：实拉开关（第 34 行）的大小写拒绝无直接断言——红相位会真拉起 CLI，不可入套件；同构性由两处同改＋代码轮复核核对保证。**返工轮3 更正：该「已知局限」撤销**——任务 B 证明用 Start-Process 有界等待＋退出码断言即可安全覆盖真实 `-NoExit` launcher 形态（断言进程真退出，无需也不必真拉 CLI）；且实测进一步表明仅脚本级 `exit` 在 -File 形态不足以终结进程，定稿为 `[Environment]::Exit(4)`，见 E-018~E-022 与新断言 `worker entry exits nonzero under real -NoExit launcher on miscased cli` | resolved |
| F-010 | P3 | 候选-5 固定动作「修此类 finding 必全库扫同类算子」执行结果登记（brief 圈定 tools/host/ + tools/adapters/）：比对算子全量命中逐条判类，**字面匹配/枚举派发语义零「不敏感且有风险」** | E-017 巡检输出（brief 原口径空格后缀 grep ＋ 胶连态补扫双口径） | **已敏感 36 处**（agent-tool 5 / relay-host 5 / run-dogfood 4 / fake-adapter 3 / preflight 17 / psmux-adapter 2）；**不敏感但无害 ≈30 处**＝null/整数/布尔/空串/单字符比较（大小写语义不适用；其中 relay-host:85 代际字段经 schema `Test-RelayPositiveInteger` 证实为整数）；**误报 5 处**＝`'not-used-in-p1'` 字面量内含 `-in` 子串。范围外观察两点（只登记不顺手改）：① preflight:117 `-like "$name\|*"` 通配本身不敏感但两边同源变量、非枚举派发；② run-dogfood `$WorkerCli` ValidateSet 不规范化即透传 entry，传大写值现会被明确炸掉（fail-closed 预期方向），要不要在 dogfood 层规范化留 backlog 裁量。附注：tools/tests/ 断言侧存在不敏感字面比较（测试代码非生产契约层），contracts 生产枚举校验 DHR_01 时已修成 Ordinal | resolved（登记闭环） |
| F-011 | P3 | **主控裁定 3 被复核修正（主控接受）**：「教训被套到错误形状的代码上」说过头了——CLI 分派同样是冻结枚举，用 `-ceq` 本身**符合**候选-5；真正的错误是旧二分代码把「不是精确 claude」等同于「必为 codex」，即 **else 分支不完备**，而非比对算子选错。F-009 的处置结论（`switch -CaseSensitive` fail-closed）不受影响，仅归因表述更正 | review-briefs/rework-round3-noexit.md 转述的独立技术裁定修正口径；对照 `knowledge/教训库-候选.md` 候选-5 原文 | 如实登记修正口径并注明主控接受；不再沿用「算子选错」表述 | resolved |
| F-012 | P3 | **复核方观察·已评估不采纳**：「本 diff 同时新增 zcode，应作为独立行为变更单独验收」——实际情况相反，新增 zcode 才是本卡的主交付（backlog `DHR-BL-10`、brief 目标节），大小写修复只是接线时被迫触发的副产物；复核方缺卡片上下文导致主次颠倒 | review-briefs/rework-round3-noexit.md 所载观察原文 vs backlog `DHR-BL-10` 目标节 | 如实记录观察与不采纳理由；验收仍按本卡整体范围执行，不据此拆分 | 已评估不采纳 |
| F-013 | P3 | **返工轮3 实测出的形态学事实（范围外观察，只登记不顺手做）**：E-019 三段对照证明 `-NoExit` 下脚本级显式 `exit` 是否终止进程取决于形态——`-Command 'exit 4'` 生效，`-File <script>` 内末句 `exit 4` **不生效**（marker 证实脚本完整跑完而进程仍存活）。据此推断：两个真实调用方（run-dogfood.ps1:57 / psmux-adapter.ps1:96 均 `-File` 形态）下，正常完成路径的尾部 `exit $code`（relay-worker-entry.ps1:35）同样不会结束进程——pane 干完活也停留 idle，正是评审裁定所述「永远 idle 窗口」在**成功路径**上的对应面，属 backlog `DHR-BL-1`/`DHR-BL-6` 痛点域。本卡只根治 default 失败分支，成功路径收尾语义留待单独裁决 | E-019（诊断实测）＋E-022；拉起形态比对 run-dogfood.ps1:57 / psmux-adapter.ps1:96 | 建议 backlog 消费（候选动作：launcher 成功收尾同型硬退出，或由 psmux pane 管理兜底）；未获授权前不动第 35 行 | 遗留→backlog DHR-BL-11（已确认·2026-08-27 用户授权收口时一并转移）|
| F-014 | P1 | **需求复核 RQ-1「大小写 fail-closed 扩大交付行为面、未获用户授权」的最终处置**：AI 返工轮 2 自行实施 → 需求复核（codex）判 P1 → 主控摆给用户 → **用户 2026-08-27 在对话里点选「接受，补进需求」**。行为变更（`-Cli` 派发大小写敏感 + 非规范大小写值 fail-closed `[Environment]::Exit(4)`）就此正式入需 | backlog `DHR-BL-10`「范围追加（RQ-1）」段（含授权链）；brief.md 完成条件 9 | 需求原文与授权链已由主控回填 backlog 与 brief，本条只登记闭环 | resolved |
| F-015 | P3 | **需求复核 RQ-2「完成条件 6 口径」的处置**：密钥闸的检索模式字面量（如描述闸门写法的 `apiKey`、`.zcode`）出现在工作区工件（findings / progress / review-briefs）中不构成命中——它们是闸门自身的文档而非凭据。主控已把完成条件 6 改写为「生产代码（tools/）与 as-built 新增行」口径并新增 **6b** 登记此澄清 | brief.md 完成条件 6 / 6b；review-briefs/review-requirement.md；findings F-004（同源问题的首次登记） | 口径澄清正式化，完成条件表已更新 | resolved |
| F-016 | P2 | **需求复核 RQ-3「e2e 证据缺可归因来源」的处置**：七份静态工件能证明协议字段自洽，但不能独立证明由真实 zcode 进程产生。返工轮 4 重跑一棒并落 `evidence/e2e/provenance/`（launch-command / process / zcode-session / README）＋工件新档 `evidence/e2e/run3-prov/`：OS 进程生存期（pid/起止/退出码 0，存活期抓 Win32 原始命令行）⊂ zcode 仓外自有会话日志窗口（全新 jsonl，只记元信息）⊃ 工件 written_at，三时间线咬合 | E-027 / E-028；evidence/e2e/provenance/README.md（含独立重跑验证法与伪造难度分析） | provenance 四件套落仓，密钥闸 CLEAN | resolved |
| F-017 | P2 | **教训复核（E5）判返工轮 3 后状态违反候选-1/5/6/12 的收敛登记**：候选-1（每个分支都要有断言）——第 34 行实拉分支的 default 无直接断言；候选-6（断言要配变异对照）——该缺位无红相证明；候选-12（部分证据写成整条兑现）——「真实 launcher 形态 fail-closed」的证据只覆盖一处 switch；候选-5 为同源加重项。收敛：返工轮 4 任务 A 补三哨兵 stub ＋ 真实 `-NoExit -File` 形态（无 -DryRun）直接断言 ×2（CLAUDE/CODEX 各一，断言「进程真退 ∧ 退出码=4 ∧ 不拉起任何 CLI」），并按候选-6 做变异对照（去掉第 34 行 `-CaseSensitive` → 恰两新断言红 SUITE FAIL(2)；还原后 SHA256 与变异前一致） | E-024（绿）/ E-025（红相＋还原核验）；review-briefs/rework-round4-line34.md 转述的 E5 裁决 | 两处 switch 的 default 各自直接断言＋各自变异对照，候选-1/5/6/12 口径全部闭合 | resolved |
| F-018 | P2 | **返工轮 3 断言 ArgumentList 的引号配对缺陷（任务 B）**：`-WorkDir` 值写成 `` `"path`' `` ——开头转义双引号、结尾转义单引号。**实测后果比「不配对」更重**（探针 E-023）：`-DryRun` 开关被整个吞进 WorkDir 的值（子进程实收 `bound.WorkDir=<path>' -DryRun`、`bound.DryRun=False`）——即返工轮 3 断言虽写了 `-DryRun`，**实际一直在打第 34 行而非主控认定的第 31 行**。修复引号后该断言才真正按其意图覆盖第 31 行；第 34 行由任务 A 的无 -DryRun 断言接管。两分支覆盖最终各归其位（brief 完成条件 10）；过程中任何时刻第 34 行都有断言覆盖（修复前=返工轮 3 断言误打误撞、修复后=任务 A 断言） | E-023（缺陷/修复两形态对照探针）/ E-024（修复后复跑 42 断言全绿） | 引号改两端配对（`` `"path`" ``），复跑 SUITE PASS；探针脚本在仓外临时目录，未入仓 | resolved |

> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞
> 遗留（P0/P1 唯一合法路径）：状态列写 `遗留→<目标>（已确认）`——"已确认"三字代表用户已在对话里明确同意把这条留到别处。AI 不得自行把 open 的 P0/P1 划成遗留。
