<!-- lesson_candidates.md — 教训候选。🟢 收尾顺手记。AI 起草、人裁决。 -->
# lesson_candidates — DHR-BL-10

## 教训候选

| ID | 一句话教训 | 状态 |
|----|-----------|------|
| L-001 | 接第三方 CLI 进派活位时，先把它做成 PATH 命令 + 自带环境认证（与既有 CLI 同构），仓内代码就只剩一个分支名——绝对路径和凭据注入都不必进仓 | rejected（教训复核：描述的是做法不是教训——没有「由错误归纳出可迁移教训」，不够格） |
| L-002 | CLI 的 `--help` 不是契约：zcode 0.16.5 列了 `--max-turns` 但实现直接拒收。接新 CLI 时，**每个拟使用的参数**须实测，别照抄帮助文本 | ready-for-review（教训复核建议收窄：从「逐个参数」收窄为「每个拟使用的参数」） |
| L-003 | 「换模型做第二轮换人复核」与「换 CLI 账号」是两件事。历史上靠 `CLAUDE_CONFIG_DIR` 换账号来换模型，把两者耦在一起；换独立 CLI 后进程/配置/凭据/会话四层天然隔离，复核独立性更硬 | rejected（教训复核：「天然隔离」过强——独立性仍需进程/配置/凭据/会话四层逐一实核，不能当免费午餐） |
| L-004 | 涉及**语言默认语义**（如 PowerShell 比较算子/`switch` 的大小写行为）的重构或扩展，动手前先核对本仓教训库有没有相反口径的既有结论——本卡 F-006 的随带修复（`-ceq` 改不敏感 `switch`）因不查库，代码轮 1 复核也只给出「接受/恢复旧语义」两个选项，直到教训复核备料才被候选-5 逮住反转 | ready-for-review（教训复核建议改措辞：泛化为「涉及语言默认语义先查库」） |
| L-005 | `-NoExit` 下 `throw` 不等于进程退出；给由 launcher 拉起的脚本设计失败路径时，必须按**真实 launcher 形态**验证退出行为，不能只在裸 shell 里试。形态本身还会咬人：同一显式 `exit` 在 `-Command` 形态生效、在 `-File` 形态不生效（返工轮3 E-019 实测），要确定性终结进程须 `[Environment]::Exit(N)` | ready-for-review |
| L-006 | `-NoExit` + `-File` 形态下，脚本级 `throw` 与 `exit` **都不终结进程**，只有 `[Environment]::Exit()` 会——给由 launcher 拉起的脚本设计失败路径，必须按**真实 launcher 形态**验证（返工轮3/4：E-018~E-021 红绿双相 + 变异对照实测） | ready-for-review（返工轮4 按教训复核裁决追加） |

> 状态流：`ready-for-review`（AI 觉得可能重要）→ 人裁决 →`needs-promotion / promoted / rejected`
