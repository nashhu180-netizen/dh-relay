<!-- findings.md — 问题清单。🟢 边做边记。 -->
# findings — DHR_28

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|----|------|------|------|------|------|
| F-043 | **P2** | **契约真缺口：fail-closed 三条里的「未知版本」在契约层无落点**（批 3 反例实测发现）。`protocol_version` 只 `$ref` 到 `relay.common/v1` 的形态约束 `^relay\.[a-z-]+/v[0-9]+$`，于是 **`relay.rpc/v9` 被放行**——验收口径第 1 条要的"未知版本 fail-closed"只兑现了三分之二。**这条是 fixture 逼出来的**：批 2 五轮复核都没抓到，因为它只有在**真拿一份非法载荷去喂**时才现形。 | 本卡 E-032/E-033：`unsupported-version` 反例首跑 **ACCEPTED!** | **已修**：`relay.rpc/v1` 握手的 `protocol_version` 改为 `const: "relay.rpc/v1"`——本 schema 就是 v1 的信封定义，握手声明的版本只可能是它。并写明 **P7/P8 出 v2 信封时新增独立 `relay.rpc/v2` schema，不是放宽本字段**（放宽等于把刚补上的闸又拆掉）。反例现命中 `E_UNSUPPORTED_VERSION` | resolved |
| F-044 | P3 | **校验器三处缺陷（批 3 自查，由反例实测逐条暴露）**：①`toReason` 取 `err.schema` 拿 pattern，**ajv 实际放在 `err.params.pattern`** → 三条 G5 反例全部错报成 `E_BAD_VALUE`；②`relay.rpc/v1` 根部是 `oneOf` 四分支，全失败时错误互相串味，按"错误总数最少"选分支会选中 `request` 并错报 `E_UNKNOWN_METHOD`；③修 ② 时给子分支的 `$id` 带了 `#`（JSON Schema 规定 `$id` 不含 fragment）→ 四个分支**全部编译失败**、静默退回，比原来更差。 | 本卡 E-033 逐条实测 | **三条全修**：①改取 `err.params.pattern`；②判据改为**先比 root 层错误数、再以总数打平**——root 层的错意味着"根本不是这类帧"，深层的错意味着"是这类帧但内容不对"；③`$id` 改用查询串后缀。另**加分支编译缓存**：同一 `$id` 不能 compile 两次，原先只靠 `try/catch` 吞掉，导致一个进程内第一次之后**所有分支被静默跳过**——这类"catch 吞掉后静默降级"正是本卡反复在修的形态 | resolved |
| F-045 | P3 | **三条反例的期望是我自己写错的，不是校验器错**：①`capability-mismatch` 原期望 `E_CAPABILITY_MISMATCH`——但**能力不匹配本质是运行期比对**（客户端指纹 vs Runtime 指纹），契约层只能校验指纹**形态**；②`g6-attempt-failed-without-reason` 原期望 `E_MISSING_FIELD`——但该例把 `reason` 显式写成 `null`，**字段存在不是缺失**，`E_BAD_TYPE` 才准（"填了 null"与"没填"是两件事，正是本契约在 G4 上反复强调的区分）；③`rpc-notification-cross-payload` 的期望受 F-044② 的串味影响。 | 本卡 E-033 | **改期望，不改校验器去迁就**。三份 `.expect.json` 的 `why` 字段逐条写明「期望已按批 3 实测更正」及原因，**留痕不抹**——避免"换个期望就当没发生过" | resolved |
| F-046 | P3 | **判别字段错误应优先于下游噪音**：`method=runStateChanged` 却塞 `relay.event/v2` 载荷时，校验器报 `E_UNKNOWN_FIELD`——技术上没错（event 的字段确实不在 run-state 里），但对使用者是误导："我发的明明是合法 event，怎么说我字段未知？" | 本卡 E-033 | **已修**：`pickReason` 加判别字段优先——若 `protocol` 常量本身不对，说明**载荷根本是另一种协议**，其余错全是下游噪音，直接返回该结论。诊断信息因此指向根因而非表象 | resolved |
| F-038 | P3 | **`node_states` 改必填 + `minItems:1` 可能挤掉「列表投影」用例——本卡唯一一处"修过头"**（第 5 轮小审 Q4，**由小审自己主动认下**：该约束是它上一轮建议的，当时只想着堵空集合、没考虑列表）。P4 冻结的是**两份** pilot schema，`relay.pilot-run-list/v1`（列表）本就不携带节点明细；若 DHR_30 想让 `run-state/v1` 同时服务列表与详情，本约束会强制每条列表项驮上完整节点数组。 | 第 5 轮小审 Q4；DevPlan B-13 两份 pilot schema | **不撤销约束，改划边界**（按小审建议）：`node_states` 的 description 写明——本协议定位为**完整状态文档**故必填；**列表/摘要投影不复用本协议**，由 DHR_30 沿 `relay.pilot-run-list/v1` 血统另立。省得 DHR_30 拿它当列表 schema 再回头说约束太紧 | resolved |
| F-039 | P3 | **⑦档的定位，我原先给的理由是错的**（第 5 轮小审 Q1 纠正）。我说"实现在校验之前计算 run_status 时仍会遇到空集合"——**对行为正确的 Runtime 遇不到**：Run 创建时 `nodes` 已至少一个，`node_states` 从它派生全为 `pending`，「刚受理、尚未起节点」这个常见态由**⑤档**命中，轮不到⑦；我写在⑦括注里的那句场景描述其实是⑤的，会误导读者。 | 第 5 轮小审 Q1 | **已修**：⑦档括注重写——明确它在**合法线路文档上不可达且这是有意的**（`minItems:1` 使空集合不可表示），定位是**计算期防御档**（防止部分恢复 / 回放到一半 / 实现有 bug 时内部空集合落进⑥的真空真），性质同 `E_TERMINAL_STATE_CONFLICT` 一类兜底；并写明**不要因为"发不出来"删⑦、也不要因此删 `minItems:1`**——把"不可达是有意的"写下来比留着让人猜便宜 | resolved |
| F-040 | P3 | **`digest` 取 schema 全文（含 `description`）是否粒度过粗——结论：不改，且理由必须写下来**（第 5 轮小审 Q2）。小审给出决定性论据：**在这份契约里 `description` 不是散文，它承载规范性条款**——G2 七档聚合、G4「null 不得渲染成 1」、`group` 六条映射、locator 实装口径、`node_states` 覆盖全集、H6 断言语义，**全部只落在 description/$comment 里**。若剥离，把 G2 优先级从 `failed>running` 改成 `running>failed` 指纹不变 ⇒ 两实现算出不同 `run_status` 而握手认为一致，**即 F-033 缺口 A 换个位置复发**。 | 第 5 轮小审 Q2 举证 6 处规范性条款落点 | **保持全文粒度**，并在 `CANONICALIZATION.md` 新增「`digest` 覆盖全文是有意的」一节：写明代价（**改一个错别字都是能力变更、都会断握手**，这是"契约文件是冻结工件"的直接推论）+ 由此确立的**两条操作纪律**（契约文件不做随手润色、文字修订与协议修订同等对待走版本流程；改动后必须重算 digest 并同步双方）+ **长期方向**（剥离 description + 规范性条款结构化，是大重构、远超本卡，但"改字就断握手"本身在倒逼它） | resolved |
| F-041 | P3 | **审计器四条理论可绕形态，按小审建议只记录不修**（第 5 轮小审 Q3）：**a** `isObjectSchema` 漏 `patternProperties`/`propertyNames`/`unevaluatedProperties` 单独出现的对象（当前 12 份里无此写法）；**b** 厂商 token 不扫 `pattern` 值（`"pattern": "^dsh-.*$"` 可把私有概念写进结构，`PROSE_KEYS` 含 `default` 同理）；**c** OPEN-POINTS 同步检查是**全文子串匹配**——只证明"两个字符串出现过"，文档改写成「已收窄：`properties.structured` 不再开放」照样通过；**d** 只扫 `contracts/`（按目前布局属设计边界）。 | 第 5 轮小审 Q3 四条实测 | **c 已修**（四条里唯一"已在跑的检查给出了比它实际证明的更强的印象"）：收紧为**同一表格行内同时出现 file 与 path**，负向实测——把该登记行改写成散文说明 → 报「清单表格里没有同时登记该文件与该路径的行」exit 1。**a / b / d 只记录不修**，按小审建议：它们全是"理论可绕、当前 schema 中不存在该形态"，**再挖下去是为不存在的威胁加固**。本条即该边界的正式登记 | resolved（c）/ 已知边界（a·b·d） |
| F-042 | P3 | **D-31 的手写 `KEYWORD_TYPES` 与批 3 的 ajv 会形成两套 meta-schema 规则**（第 5 轮小审 Q4 附注，"轻度重复建设，不算过头但要防漂移"）。 | 第 5 轮小审 Q4 | **移交批次 3**：接进 ajv 后改成「ajv 可用则用其 `validateSchema`，不可用退回内置最小检查」，别让两套规则各自演化 | open |
| F-032 | **P2** | **【卡口】D-19 的修法引入了更危险的新洞——空集合的真空真**（cp2 小审 D-25，四轮里最好的一条）。新 6 档条款①~⑤ 都是「**任一**节点为 X」（空集合上全为假），⑥ 是「否则**全部** `succeeded`」——**空集合上是真空真**。而 `node_states` 当时**既不必填也无 `minItems`**，于是 `{run_status:'succeeded', group:'done', node_states:[]}` 乃至字段整个缺省都合法 ⇒ **一个从未跑过的 Run 报告"成功完成"**。**比它修掉的病更危险**：D-19 的症状是噪音（正常 Run 被误报成需要你，方向安全、人一看就知道不对），D-25 是**静默地朝令人安心的方向错**——这是这份协议能给出的最坏答案。`relay.run/v2` 的 `nodes` 虽有 `minItems:1`，但 run-state 是**独立文档**，此前无任何机制保证对齐。 | cp2 D-25；本卡 E-029 实测：改前空数组与字段缺省两种形态**均 ACCEPT** | **已修（双重加固）**：①条款补第⑦档并显式规定**空集合 → `pending`**（`node_states` 为空字面意思就是"Run 已受理、尚未起节点"，正是 `pending` 新加的语义，不必再造值）；⑥ 档改写为「否则**若存在至少一个节点**且全部 `succeeded`」；②`node_states` 改**必填 + `minItems: 1`**，把前提从"约定"升级成机器强制——同一条推理已在 F-022（`required` 该必填却设成可选）用过一次。17 例实测全过 | resolved |
| F-033 | P2 | **能力指纹两个缺口，会让"能力不同的两个实现算出同一指纹"**（cp2 小审 D-26）。**缺口 A**：`protocols` 按**名字**列不按**内容**——`"relay.run/v2"` 只是个名字，两个 Runtime 对着**不同修订版**的同名协议编译，指纹完全相同。**这不是假设——本批之内 `relay.run/v2` 就实质变过两次**（F-012 的 `$ref` 全量重写、F-022 的 `required` 改必填）：按 F-022 之前的 schema 构建的客户端对上之后的 Runtime ⇒ 指纹相同 ⇒ **握手通过** ⇒ 然后 Runtime 拒掉它发来的每一个节点。而 `E_CAPABILITY_MISMATCH` 的立意原话正是「能力集不同 = 双方对'能做什么'的理解不同」。**缺口 B**：清单没有 executor 维度——只托管 `process` 的 Runtime 与还托管 `dsh-agent`/`pi-agent`/`herdr-agent` 的，三键完全相同 ⇒ 指纹相同；**在 P5 特别活**，因为 `pi-agent` 是 DevPlan §4.2 明列的 P5-X 条件项、非必达，**两个都合规的 P5 构建**（带/不带 Pi Adapter）指纹会一模一样。ADR-002 整篇讲 executor 宿主归属，指纹里却没有该维度。 | cp2 D-26 A/B | **已修**：①`protocols` 由裸名字数组改为 **`{id, digest}` 对**，`digest` = 该 schema 全文的 `sha256(JCS(...))`；②新增 **`executor_kinds`** 键 = 该 Runtime 实际能托管的 `executor_kind` 子集；③四个数组的排序规则显式写死（JCS 只管对象键序不管数组序）；④两条弱维度（reason code 集合、`subscribe` 是否支持 `seq` 续订）记录备查、不现在加并写明理由 | resolved |
| F-034 | P2 | **`CONDITIONALLY_NARROWED` 自验只检查 `$ref`「存在」，不检查目标是否闭合**（cp2 小审 D-27，**小审把该函数逐字转写后在内存里复现**）。白名单声称的事实是「这里**实际是闭合的**」，而自验证明的只是「这里有个 `$ref`」——把两个分支的 `$ref` 都指向一个 `additionalProperties: true` 的目标，旧版自验照样"通过"。**与 F-028（D-20）是同一个毛病，只是下沉了一层**：又一次把"可验证的事实断言"当成"声明"来处理。 | cp2 D-27 内存复现；本卡 E-030 负向测试 | **已修**：新增 `resolveRef()` 按 `$id` + JSON Pointer 真解析，自验断言**目标 `additionalProperties === false`**；解析不到也报错。**负向实测**：把两个分支 `$ref` 都改指向 `request.properties.params`（`additionalProperties: true`）→ 报「分支 `$ref` 的目标不是闭合的」，退出码 1 | resolved |
| F-035 | P3 | **审计器四处可绕**（cp2 小审 D-28~D-31）：①`inIfThen` 用**扁平路径字符串**判断，一个**名为** `then`/`if`/`else` 的属性会被误当守卫静默跳过；②`listSchemaFiles` 只挑 `*.schema.json`/`*.shape.json`，一份叫 `relay.foo.v1.json` 的契约**完全隐身**——审计覆盖面由一条**没人强制**的命名约定定义；③`REGISTERED_OPEN` **没有**陈旧检测（`CONDITIONALLY_NARROWED` 有），且脚本**根本不读** `OPEN-POINTS.md`，注释写的"改一处必须改另一处"是靠人记；④**无 meta-schema 校验**——`"required": "node_id"`、`"minItems": "1"` 这类畸形关键字 JSON 解析得过、AST 遍历得过、审计器 exit 0，而多数校验器会**静默忽略**它们（与 F-024 的 `format` 空转同一类）。 | cp2 D-28~D-31；本卡 E-030 四条负向测试 | **四条全修**：①改为递归下传布尔守卫标记，只在**关键字位置**的 if/then/else 置位；②枚举全部 `*.json`，不符命名约定的**报错**而非跳过；③`REGISTERED_OPEN` 补陈旧检测，并**解析 `OPEN-POINTS.md` 断言代码白名单与文档同步**（靠人记住的约束改由脚本执行）；④加最小 meta-schema 关键字类型自检（不引依赖）。**修 ④ 时自己踩了 ① 同款坑**——首版把本 schema 里那个**名为** `required` 的属性（H6 的必经开关）误报成"关键字类型错"，已按"`properties`/`$defs` 下一层的键是名字不是关键字"修正。**四条负向全部实测退出码 1** | resolved |
| F-036 | P3 | **`group` 无裁量的最强理由此前没写出来**（cp2 小审 D-33）：`group` **参与 `state_signature` 计算**，若源头对它有自由裁量，同一状态在两个实现上会算出不同签名 ⇒ **P5-M3『强杀后重建相同状态签名』直接失效**。另：`group` 现在是 `run_status` 的**全函数**、携带零独立信息，这对 DHR_30 是**天花板**——将来若正式 Read Model 需要非纯函数的分堆须改契约。 | cp2 D-33（Q2 回答）；小审实测 6×4=24 组合恰好 6 个放行、4 个 group 值全可达，**确认不是过度约束** | **已补进 `group` 的 description**：写明 `state_signature` 这条最强理由 + 对 DHR_30 的天花板 + **一个已知编码决定**（design/06 H8 的 Approval 场景——活干完但人工批准未下——在本映射下唯一表达方式是记 `waiting_human`→`needs_you`；这大概率对，但它是**编码决定**而非自然推论，现在写下，别等 P8 撞上） | resolved |
| F-037 | P3 | **聚合条款对「全部」的全集没有定义**（cp2 小审 D-32）：`node_states` 若只列了 3 个节点中的 1 个，"全部 succeeded" 字面成立 → `succeeded`。schema 管不了跨文档完整性。 | cp2 D-32 | **已补进 `node_states` 的 description 并移交 DHR_29**：`node_states` 必须**覆盖对应 `relay.run/v2` `nodes` 的全集**，否则"全部"是空话。属实现期约束，schema 层无法表达 | resolved |
| F-025 | **P2** | **【卡口】G2 聚合条款只覆盖 7 个节点态里的 4 个，导致每个正常 Run 都被误报警**（cp2 小审 D-19）。`node_state.status` 有 7 值（`pending`/`running`/`waiting_human`/`succeeded`/`failed`/`orphaned`/`unknown`），而冻结的优先级表只有 4 档；`pending` 与 `orphaned` 落在表外走 `unknown`，再被 `unknown→needs_you` 硬拦——于是**刚创建的 Run（全 `pending`）与每两个节点之间的空档（如 `[succeeded, pending]`）都会浮顶到「需要你」**。这**恰好打掉 G2 的设计意义**：该条款给 `unknown→needs_you` 的理由是「不认识的状态必须让人看见」，而 `pending` 根本不是不认识的状态——让它走 unknown 通道等于往报警器灌噪音，真正不认识的反而被淹。**根因是转写**：条款逐字照抄自 P4 §4（照抄保真是对的），但 **v2 的枚举比 v1 多了两个值，条款没跟着扩**——照抄完没检查"它现在管的枚举还是不是原来那个"。 | cp2 D-19；本卡 E-026 实测：改前 `pending` 只能走 unknown 通道 | **已修（批 2 内改，趁枚举正在冻结）**：①聚合条款重写为**覆盖全部 7 值的 6 档优先级**（`failed`/`orphaned`→failed ＞ `running` ＞ `waiting_human` ＞ `unknown` ＞ `pending` ＞ 全 `succeeded`）；②`run_status` 枚举**新增 `pending`**（不并入 `running`——那一刻确实没节点在跑，谎报会让"还没开始"与"正在跑"不可区分）；③`orphaned` 归 failed 侧（『确认没了』比 unknown 明确，且 `result/v2` 的 `outcome` 已把它与 failed 并列）；④`run_status→group` **全部 6 条映射改为 `allOf` 逐条机器强制**（原先只硬拦了 `unknown` 一条）。14 例实测全过 | resolved |
| F-026 | **P2** | **【卡口】五个摘要/签名字段零规范化口径，跨实现根本对不上**（cp2 小审 D-24）。`capability_hash` / `request_digest` / `payload_digest`×2 / `plan_digest` / `state_signature` 全部只约束形态 `^[0-9a-f]{64}$`，描述里写"规范化摘要"，但 **`contracts/` 全文没有一处定义"规范化"是什么**（无 JCS、无字段序、无"哈希哪些字段/空值怎么处理"）。后果不是理论上的：两个实现各自序列化再 sha256 必然不同 ⇒ **握手永远 `E_CAPABILITY_MISMATCH`**、**H11 永远不成立**、**P5-M3 永远过不了**；而 README 开宗明义写的是"任何客户端都只认这一份契约来源"。**这是本卡第二次犯同一类错**：G2 理由段自己写着「只记不冻，两个实现会算出不同的 run_status」，同一推理没用在这五个字段上，而它们失效更硬（前者显示不一致，后者握不上手）——与 F-022（H6 的 `required` 该必填却设成可选）同类。 | cp2 D-24；`grep -rn "JCS\|8785\|canonical" contracts/` 改前零命中 | **已修（本卡定死，不留给下游）**：新建 `contracts/CANONICALIZATION.md`——①规范化 JSON 统一取 **RFC 8785 (JCS)**（逐条列出它定死的键序/数字格式/转义/空白/编码）；②**承接 v1 Oracle 并如实登记差异**：v1 `relay-schema.ps1` 的 `ConvertTo-RelayCanonicalValue`+`Get-RelayPlanHash` 思路一致（递归键排序→compact JSON→UTF8→sha256→小写十六进制），但键序依赖 PowerShell 默认比较、数字与转义交给 `ConvertTo-Json`、深度硬上限 50，故**与 v2 不可互认**；③五个字段逐个写明"对什么取摘要 + 排除哪些键"，其中 **`plan_digest` 必须排除 `resolved_at`、`state_signature` 必须排除 `updated_at`**——否则 H11 与 P5-M3 在前提上就不可能成立；④**`capability_hash` 的能力清单形状本卡定死**（`protocols`/`methods`/`notifications` 三键，数组按 UTF-16 码元升序，v0 形状不计入），并给出可复算的基线样例；⑤五个字段的 schema description 全部加上指向本文件的指针 | resolved |
| F-027 | P2 | **审计器压根没解析 `$ref`，本卡唯一那条 P1 没有回归护栏**（cp2 小审 D-21）。`tools/audit-contracts.mjs` 里注释写着"$ref 可解析性"，实际只做了计数并打印"目标种类: 3"——**从头到尾没 resolve 过任何一个 `$ref`**。后果：F-012（`$id` 绝对 + `$ref` 相对 → 全部编译不了，"批 3 一跑全炸、验收口径 1/2/3 一起落空"级别）若明天再犯，审计器 exit 0。 | cp2 D-21；本卡 E-027 负向测试 | **已修**：建 `$id` 注册表（含重复检测）+ 逐条 `$ref` 拆 base 与 JSON Pointer，base 必须命中注册表、Pointer 必须真能下钻到节点，任一失败 exit 1。当前 **85 条全绿、注册表 12 项**。**负向实测**：把一条 `$ref` 改回相对路径 → 审计器报「base 未命中任何已注册 $id」且退出码 1 | resolved |
| F-028 | P2 | **`CONDITIONALLY_NARROWED` 是真后门，且恰好掩盖 D-17 的失效模式**（cp2 小审 D-20，**小审自己在内存里复现了**）。`REGISTERED_OPEN` 登记的是"我们承认这里开着"（一个**声明**，白名单是恰当形式）；但 `CONDITIONALLY_NARROWED` 登记的是"这里实际闭合、只是闭合在条件分支里"——这是一个**可验证的事实断言**，而脚本一个字都没验、纯信硬编码字符串。小审复现：删掉 `notification.allOf` 的 event 分支后约束消失，审计器**仍归类为"实际闭合"并 exit 0**——等于给"加了 method 忘加分支"这种最容易发生的回归开了专用通道。 | cp2 D-20 内存复现；本卡 E-027 负向测试 | **已修**：`CONDITIONALLY_NARROWED` 改为**自验证**——断言①同级存在 `allOf` 且每分支有 `then.properties.<字段>.$ref`；②各分支 `if` 判别值的**并集覆盖判别字段 enum 全集**（这一刀正是堵 D-17）；另加**陈旧登记检测**（白名单登记了但扫描没扫到该位置 → 报错，防白名单越攒越脏）。**负向实测**：往 `notification.method` 枚举加 `heartbeat` 而不加分支 → 报「判别 enum 有 1 个取值没有对应分支：heartbeat」且退出码 1 | resolved |
| F-029 | P3 | **审计器的 `ROOT` 路径算法在含非 ASCII 或空格的路径下会坏**（cp2 小审 D-22）：`new URL('..', import.meta.url).pathname` 是**百分号编码**的——`D:/MyFiles/玩蜂/…` 会变成 `D:/MyFiles/%E7%8E%A9%E8%9C%82/…`，`My Files` 会变成 `My%20Files`。当前 `relay-core` 路径全 ASCII 所以没暴露，**但本项目的另一个工作目录恰好就是 `D:\MyFiles\玩蜂\…`**；仓库若被 clone 到含中文或空格的路径下，审计器直接 exit 2，而 exit 2 在 CI 里很容易被当成"环境问题"忽略。 | cp2 D-22 | **已修**：改用 `fileURLToPath(new URL('..', import.meta.url))`，一行，且自动给出平台原生分隔符 | resolved |
| F-030 | P3 | **`timestamp` 的 `pattern` 不做日历校验**（cp2 小审 D-23）：正则表达不了闰年与大小月，`2026-02-30` / `2025-02-29` / `2026-04-31` 均会被放行。**这不是缺陷**（正则本就表达不了），但**正因如此 `format` 必须留着**，别让人误以为 `pattern` 已经全管了。 | cp2 D-23；cp2 独立跑 29 例（15 放 + 14 拒）确认闰秒 `23:59:60`、负零时区 `-00:00`、超长小数秒均正确放行 | **已修**：`timestamp` 的 description 增补「已知限制」段，写明日历校验由 `format` 侧承担，并列出已实测的合法边界（闰秒 / 负零时区 / 任意精度小数秒 / 小写 t·z / `+14:00` / `+13:45`） | resolved |
| F-031 | P3 | **`if` 里的 `required: ["required"]` 现已技术冗余**（cp2 小审 Q1 补注）：`required` 字段改必填后它永远存在，`if` 的这道守卫看似可清理。 | cp2 Q1 第 1 条 | **有意保留，禁止清理**——它是"字段哪天又变回可选"时唯一的正确性保障：删掉后若有人把 `required` 挪出必填列表，`if` 会对缺字段的实例**恒真**、`then` 反过来对所有非必经节点施压，从 fail-open 一步跳到 over-reject **且静默**。零成本的保险。已在此登记，避免后续被当作"死代码"清掉 | resolved |
| F-022 | **P2** | **H6 是 fail-open by default —— 触发开关 `required` 原为可选 + `default:false`**（cp2 小审 D-11，本卡最重要的一条）。实现只要**不标** `required`，H6 这条安全断言一次都不触发；小审实测两个 payload 应拦却放行。**最扎心的是内部不一致**：本卡自己在 G1/G3 用的推理是「列为必填而非可选——可选会让所有实现都省掉它、降级重新变成常态」，这条对 `required` **更适用**（`title` 缺了只是屏幕难看，`required` 缺了是安全断言整个静默失效），却唯独没用在全卡唯一被 DevPlan 点名为机器证的安全断言（验收口径第 3 条）上。 | cp2 D-11 两个绕过 payload；本卡 E-023 复验：改前「required 缺省 + 只 dsh-agent」=ACCEPT，改后=REJECT | **已修**：`relay.run/v2` `$defs/node` 的 `required` 列表加入 `"required"`，并删掉 `default:false`。**语义不变**（显式写 `false` 照样不触发 H6），但消灭"忘了标 = 不检查"这条静默路径，让"这个节点必不必经"成为 Workflow 作者必须回答的问题。一行改动。 | resolved |
| F-023 | P3 | **`notification.params` 的闭合依赖「`method` 枚举值与 `if/then` 分支一一对应」**（cp2 小审 D-17）。当前严密（两值两分支全覆盖），但 P7/P8 若加第三个 method 而忘了加分支，`params` 会**静默退回完全开放**，且静态扫描看到的仍是"缺省开放"、分不清有意还是漏。 | cp2 D-17；`relay.rpc.v1.schema.json` `$defs.notification` | **已记入 `OPEN-POINTS.md`** 作为维护约束：加 notification method 值时必须同批加 `if/then` 分支并复跑 `tools/audit-contracts.mjs`。同处另记一条**语义决定**（小审实测确认）：推送帧 `params` 必须是完整自描述协议对象、不能只推增量字段——DHR_29 实现 subscribe 时按此办 | resolved |
| F-024 | P2 | **F-013 对 `date-time` 的定性不准，且原修法不满足验收口径**（cp2 小审 D-18）。F-013 原写「错在校验器未加载 format 词表」——只对一半。JSON Schema **2020-12 规范本身**把 `format` 放在 *Format Annotation* 词汇表，**默认即注解、不做断言**；只有实现了 *Format Assertion* 词汇表的校验器才校验。所以换任何独立校验器都可能空转，而验收口径第 1 条要的正是「可由**独立**校验器验证」——只加 `ajv-formats` 只能保证"我们这个校验器验了"。 | cp2 D-18；本卡 E-024：11 例 RFC3339 pattern 实测 | **已修（本卡就改，不拖到批 3）**：`relay.common/v1` 的 `timestamp` 在 `format` 之外**并列加 RFC3339 `pattern`**——`pattern` 是断言性关键字，任何合规校验器都必须执行，约束才真正静态可证。11 例实测全过（`2026-13-99`、无时区、空格分隔、`25:00:00`、`00` 月全部拒；小数秒与 `+08:00` 偏移放行）。F-013 随之降级 | resolved |
| F-014 | P2 | **"全域 `additionalProperties: false`"是不实陈述，且 4 处开口未登记**（cp2 小审 D-1）。AST 遍历实测开口 4 处：`result/v2.structured`（true）、`rpc/v1.request.params`（true）、`rpc/v1.response.result`（缺省开放）、`rpc/v1.notification.params`（true 且**零说明**）。而 `reason-codes.md` 与 `compat-matrix.md` 都把"全域"当结论写。**直接影响批 3**：反例 fixture 若照文档写"`params` 塞未知字段 → 期望 `E_UNKNOWN_FIELD`"会跑不过。 | cp2 D-1；本卡 E-022（`tools/audit-contracts.mjs`） | **已修**：①新建 `contracts/OPEN-POINTS.md` 逐条登记 3 处有意开放点（位置/为什么开/谁在何时收窄）+ O-1 的已知代价；②`notification.params` **本卡直接收窄**（改 `allOf`+`if/then` 分别 `$ref` 到已冻结的 `relay.event/v2` 与 `relay.run-state/v1`）——小审判它"最站不住"，成立；③两处"全域"改为"除 OPEN-POINTS 所列外全域"；④批 3 步 14 静态检查按该清单白名单 | resolved |
| F-015 | P2 | **G5 声称的"schema pattern 静态可证"在 RPC 面不成立**（cp2 小审 D-2）：`request.params` 的 description 明写「locator 类参数一律走 relay.common/v1 的 locator（禁绝对路径 G5）」，但该子 schema 只有 `type:object` + `additionalProperties:true`，**无任何 locator 约束**——`start`/`control` 参数里塞 `C:\Users\nash\brief.md` 在契约层完全放行。这是散文，不是机器闸，而验收口径第 2 条要的正是"静态可证"。 | cp2 D-2；`relay.rpc.v1.schema.json` `$defs.request.properties.params` | **已修（取小审建议①）**：description 改为**如实说破**——明写这两条在本卡"只是散文约定、不是机器闸"，落机器闸的责任归 P6/P7 的 per-method schema，在那之前 Runtime 实现须在参数解包处补运行时校验。**不留一句做不到的断言。** | resolved |
| F-016 | P2 | **compat-matrix 把 `proposed_by → trigger_by` 标「改名·语义等价」是错的**（cp2 小审 D-6）。主会话独立复核确认：v1 `proposed_by` 是**枚举** `orchestrator`/`replanner`（`relay-schema.ps1:121`），记的是"哪个**机器角色**提出了计划"；v2 `trigger_by` 是自由字符串、`trigger=human` 时必填，记的是"哪个**自然人**触发"。类型、取值域、主体三者全变。 | cp2 D-6；主会话复核 `grep -n "proposed_by" tools/contracts/relay-schema.ps1` → `:121 Test-RelayEnumValue $Plan 'proposed_by' @('orchestrator','replanner')` | **已修**：拆成两行——`proposed_at → created_at` 保持「改名」，`proposed_by → trigger_by` 改标 **改型** 并补说明；另补记 v1 的 `orchestrator`/`replanner` 概念在 v2 **无对应落点**，若 P8 重编排需区分应落 `relay.resolved-plan/v1` 而非塞回 `trigger_by`。§0 计数随之复算 | resolved |
| F-017 | P3 | **compat-matrix 的 event kind 账用数字凑平**（cp2 小审 D-8）：原写"新增 6、弃用 3"，`12−3+6=15` 凑巧对上，但逐值核有 **6 个 v1 kind 在 v2 无对应值**（`launch_receipt` / `launch_failed` / `checkpoint_rejected` / `control` 四个无落点，`result_stale`+`result_rejected` 合并为一）。尤其 `launch_receipt`——`relay.launch-receipt/v2` 作为协议存在，但**回执签发在事件账上不留痕**。 | cp2 D-8；`relay-schema.ps1:246` vs `relay.event.v2.schema.json` | **已修**：新增 §4b「event kind 逐值对照」，12 值逐条写去向，4 个无对应值**显式标为缺口并移交 DHR_29 判定**（是否补 `receipt_issued` / 区分"起不来"与"跑起来后失败" / 是否需要审计反复投递冲突 checkpoint）。本卡**不擅自加值**——加值会改动已冻结的 `relay.event/v2` | resolved |
| F-018 | P2 | **schema 与 ADR-002 净产出表打架**（cp2 小审 D-5）：ADR-002 承诺「每种 kind 配一个 ref 字段：`executor_ref`/`adapter_ref`/`bridge_ref`/`server_ref`」，批 2 实际收敛为单一 `executor_profile.ref`（按 `kind` 判别语义）。**收敛本身更好**（四个同类型字段按 kind 分名会诱导实现写 if-else 分支），但这是对已过 cp1 小审的 ADR 的偏离且**零登记**——批 3 或 DHR_29 照 ADR 找 `bridge_ref` 会找不到。 | cp2 D-5；`grep -rn "adapter_ref\|bridge_ref\|server_ref" relay-core/contracts/` → 仅 description 散文命中，**零字段** | **已修**：ADR-002 净产出表该行加删除线 + **批 2 修订注**（写明收敛后的实际字段、收敛理由、以及"若不登记下游会找不到"的登记原因）；`v1-gap-disposition.md` G5 行的字段清单同步改为实际使用点（`executor_profile.ref` / `log_locator` / `executor_ref` / `source_refs[].path` / `host_ref`） | resolved |
| F-019 | P3 → **处置已升级** | **H6 契约断言的边界**（cp2 两轮提出）。第一轮记为"两条边界·非缺陷"；**第二轮小审明确反对该定性**并给三条理由：①有成本极低的修法（`required` 改必填，一行）故不是边界而是缺陷；②与本卡 G1/G3 的设计推理不一致；③原边界①只写了"显式规避"这一半，漏了更隐蔽也更危险的**结构性必经**形态。**主会话复核后完全接受该反对意见**——把"可修的东西记成固有边界"本身就是一种把没做到写成结论。 | cp2 两轮报告；`contracts/OPEN-POINTS.md`「H6 契约断言的两条边界」节 | **不停在"记录边界"**：①可修的那条 → 已改必填（见 **F-022**）；②边界①补上**结构性必经**形态——DSH-only 的 `gate` 未标 `required`、却被标了 `required` 的 `final` 依赖，DAG 上事实必经但 H6 不触发；**且这是诚实实现者会自然写出的形状，不是故意规避**。JSON Schema 表达不了图的传递闭包，故**明确移交 DHR_30/31：被必经节点（传递）依赖的节点自身也应视为必经**，Workflow 定义期须先做可达性推导再校验 H6；③边界②补上 `{kind:"process", ref:"bridge/dsh-shim.exe"}` 这种"实质 DSH-only"形态——schema 层无解（ref 是不透明 locator），属固有边界 | resolved |
| F-020 | P3 | **`package.json` 的 `bin` 指向不存在的文件 + `npm test` 假绿**（cp2 小审 D-9）：`"bin": {"relay-validate": "./tools/validate.mjs"}` 该文件批 3 才建；且 `npm test` 在**零测试时仍退出 0**（`tests 0 / pass 0 / fail 0`），而 README「## 测试」段读起来像已可用。 | cp2 D-9 | **已在批 3 闭合**：①`tools/validate.mjs` 已建，`bin` 成立；②`npm test` 现真跑 5 条测试（原 `node --test` 不指路径会扫 `node_modules`、实测挂死，已改为指定文件），且测试内**断言 fixture 份数下限**（golden ≥10、negative ≥20）——空绿这条路被机器堵死，不是靠"记得别拿空绿当证据" | resolved |
| F-021 | P3 | **批 2 产物在小审进行中被改写**（cp2 小审 D-10）：小审 23:35 读到的是相对 `$ref` 版本，23:38–23:40 全部 11 份 JSON 被重写（F-012 的修复）。改得对，但**破坏了"批次冻结 → 小审"的边界**——小审是自己查 mtime 才发现的，主会话的通知发得比它发现晚。 | cp2 D-10；`ls --time-style=full-iso contracts/` mtime vs E-018 派出时刻 | **流程教训，已修行为**：主会话在发现后确实发了 SendMessage 通知 cp2 按当前盘上状态复核，但**时序上晚于它自查**。后续批次：①派小审前把批次产物定住；②确需中途修 P1，**立即**通知而非事后。升 `lesson_candidates.md` | resolved |
| F-012 | **P1** | **7 份 schema 原本编译不了**：`$id` 用绝对 URI（`https://dh-relay.local/contracts/relay.run/v2`）而 `$ref` 用相对**文件路径**（`./_shared/relay.common.v1.schema.json#/$defs/...`）。按 JSON Schema 规范，`$ref` 相对 `$id` 建立的 base URI 解析，于是被解成 `https://dh-relay.local/contracts/relay.run/_shared/...` → `MissingRefError`。**若不发现，批 3 的校验器一跑就全炸，且验收口径第 1、2、3 条全部落空。** | 本卡 E-020：ajv2020 编译报 `MissingRefError: can't resolve reference ./_shared/… from id https://dh-relay.local/contracts/relay.run/v2`，`missingSchema` 显示被错误拼接的 URI | **已修**：全部 `$ref` 改为按被引 schema 的 `$id` 引用（`https://dh-relay.local/contracts/relay.common/v1#/$defs/…`），规范正解。**修的过程中又踩一次**：批量替换时 `../_shared/` 内含 `./_shared/` 子串，先替换后者留下 `.https://…` 坏引用（仅 4 份 v0 形状），已二次修正。终态审计：全部 `$ref` 共 22 类目标、零 `.https://` 残留、13+12+10+8+6+6+5+3+3+2+1+1+1 条外部引用全部指向同一 `$id`。**主会话自查发现，非小审报出**（小审 cp2 并行进行中） | resolved |
| F-013 | P2 | **`"format": "date-time"` 当前是空转的**：ajv 默认不加载 format 校验器，探针输出 `unknown format "date-time" ignored in schema at path …#/$defs/timestamp`。即所有时间字段实际**只校验了是字符串**，`"2026-13-99"` 一类会被放行——这与 fail-closed 精神冲突（看着有约束，实则没有）。 | 本卡 E-021：`node probe.mjs` 输出首两行警告 | **移交批次 3**（校验器归批 3）：①校验器必须显式引入 `ajv-formats` 并开启 `date-time`；②`fixtures/negative/` 必须有一份**非法时间戳**反例钉住它，否则这条约束会再次静默失效。~~不在批 2 改 schema——schema 写法本身没错，错在校验器未加载 format 词表。~~ ⚠️ **该判断不准，已由 cp2 小审 D-18 纠正、处置由 F-024 接管**：2020-12 规范默认 `format` 即注解不做断言，**不是**校验器配置问题，换独立校验器同样空转。本卡已并列加 RFC3339 `pattern` 兜住下限；本条降级为"批 3 仍应加 `ajv-formats` 做更严格校验 + 补非法时间戳反例"，但**约束下限已不依赖它**。 | open（降级） |
| F-011 | P3 | **`relay.locator` 的 pattern 有一处有意从严（over-rejection）**：13 例实测中 12 例符合预期，唯 `a:b`（POSIX 下合法的相对路径）被拒。根因是守卫写作 `(?![A-Za-z]:)` 而非 `(?![A-Za-z]:[\\/])`。 | 本卡 E-019：13 例 node 实测逐例结果 | **不改，有意保留从严**。理由：放宽成 `(?![A-Za-z]:[\\/])` 会同时放进 Windows 盘符相对路径 `C:x`（『C 盘当前目录下的 x』）——那是真正危险的绝对性形态；而首段含冒号的相对路径本就在 Windows 上非法，跨平台协议应避免。**已把该取舍写进 schema 的 description**（不静默从严）。若 DHR_29/30 遇到真实需要 `a:b` 的场景，回本条重议。 | resolved |
| F-010 | P3 | **`dh wt new` 从 `origin/master` 切树，而非本地 `master`**：本卡实测——刚把批 1 提交进本地 master（`a10e294`）后跑 `dh wt new DHR_28`，新树落在 `origin/master`（`554e543`，落后一笔），**树里看不到批 1 产物**。若不察觉直接施工，会在过期基点上写代码、收口时撞冲突。 | 本卡 E-009：`git rev-parse master origin/master` 两值不等；`ls .dh-worktrees/DHR_28/docs/.../DHR_28/` → `No such file or directory` | **已绕过**（`git merge --ff-only master` 快进，非破坏性）。范围外（dh 工具侧行为），**不改工具**。建议升 `backlog.md`：`dh wt new` 应从本地 `master` 切、或在两者不等时显式告警。AGENTS 铁律「worker 进场第一动作自 rebase master」正是为这类情形而设，本次即按该铁律处置。 | open |
| F-009 | P3 | **仓根 `.gitignore` 用的是任意深度模式 `.dh-relay/`，不是 DevPlan §2.2 写的根锚定 `/.dh-relay/`**：实测两者都被忽略（`git check-ignore -v` 对 `.dh-relay/probe/y.txt` 与 `relay-core/.dh-relay/probe/x.txt` 均命中 `.gitignore:17:.dh-relay/`），现状**严格更宽**、需求已满足，故本卡**不改 `.dh-relay/` 那条忽略规则**（⚠️ 措辞已按 E4 需求复核 P3-3 改精确：本卡**确实动过** `.gitignore`——新增 Node 代码根时加了 `node_modules/`，那是必然配套且在范围内（`git ls-files relay-core/node_modules` = 0，`package-lock.json` 入仓的理由另有说明）。原措辞「不改 `.gitignore`」会让 DHR_29 以为本卡零触碰该文件）（也符合「Relay 不得自行改业务仓 `.gitignore`」的精神）。**风险留给 DHR_29**：P5-M8 要求「缺 `.gitignore` 锚定时 start fail-closed」，若 Runtime 的前置检查去 grep 字面量 `/.dh-relay/`，在本仓会**假阴性**（判成"没锚定"而拒启动）。 | 本卡 E-011；`.gitignore:17`；DevPlan §2.2「仓根须显式忽略 `/.dh-relay/`」 | **不在本卡改**。移交 DHR_29：其 `.gitignore` 前置检查必须**按 Git 的忽略语义判定**（如调 `git check-ignore`），不得字面量匹配单一模式；本条作为 DHR_29 开工输入。 | open |
| F-002 | P2 | **ADR-001 决策对照表换了权威源，导致折扣只落 TS 一侧**：task_plan 步1 要求按 C-001（DevPlan §2.3「语言选择原则」）逐条对照，ADR 改用了 design/05 §6.3 的表。两处 TS 侧措辞不同——DevPlan §2.3 = 「TypeScript 复用 **Agent SDK** 收益高且仍独立进程 + CLI」，design/05 §6.3 = 「**DSH Service** 复用收益很高」。ADR 用 B-11（DSH 只是可替换客户端）把 TS 侧权重调低，但该折扣只打得中 DSH-Service 半边、打不中 Agent SDK 复用（= ADR 自列的 E4）。净效果：唯一带方向性折扣的格子落在 TS 侧，Go 侧无对应折扣。 | 批次检查点1小审 F-1；`ADR-001:103-106` vs `dev_plan/P5-…:95`；`task_plan.md:36` | **已修**：对照表并列两份原文（DevPlan §2.3 + design/05 §6.3），B-11 折扣显式限定作用域为「DSH Service 复用」一项，明写不适用于 Agent SDK 复用（E4） | resolved |
| F-003 | P2 | **ADR-001 与 ADR-002 内部口径打架**：ADR-001 张力段把「放弃 SDK 直接复用」列为**选 Go 才付**的赌注；但 ADR-002 第②问已判 pi-agent 无论选哪种语言都走冻结 Adapter、并明写「即使选 L-TS 也走 Adapter」。若 ADR-002 立得住，该项是两边都付的代价、不构成 Go 的差异化成本。代价表用了条件式（L80「若…则大幅缩水」），但**张力段的结论句没把条件带下去**——而张力段正是摆给用户看的那一段。 | 批次检查点1小审 F-2；`ADR-001:80` 与 `ADR-001:109` vs `ADR-002:40,46` | **已修**：张力段改为「选 Go 的赌注 = 重写投影器 + Go 侧重建等价测试；SDK 直接复用按 ADR-002 两种语言均不采用，不计入语言差」；同时把 ADR-002「L-TS 下是否放宽直连」作为**次级裁决项**一并摆给用户 | resolved |
| F-004 | P3 | **ADR-002「只留合同接口」出处指错**：标为「DevPlan §3.2 DHR_30 逐字」，实际该字串只出现在 §3.1 索引表（L107）与 §1 前置条件（L58，作「只保留合同接口或转可选」）；§3.2 的 DHR_30 卡（L153）无此句。事实为真，仅出处错。 | 批次检查点1小审 F-3；`ADR-002:73` vs `dev_plan/P5-…:58,107,153` | **已修**：改标为「§3.1 DHR_30 索引行 / §1 前置条件 B-11 段」 | resolved |
| F-005 | P3 | **E2「约 250 行」与实现差 13%**：`project-v1.mjs` 实为 284 行。ADR 忠实转述了 P4 报告 L113 与 DevPlan L126 的原话（两处均写"约 250 行"），非本批编造；但 ADR 同时把实现文件路径列为可复跑指针，抽验者按指针一查就撞上差额。 | 批次检查点1小审 F-4；`wc -l …/src/read-model/project-v1.mjs` → 284 | **已修**：E2 加括注「P4 报告原文记'约 250 行'，2026-08-20 复核实测 284 行」——只补注，**不回改上游 P4 报告**（留痕原则） | resolved |
| F-006 | P3 | **ADR-002 前向引用 G5/G6 编号，但 G1~G6 要到批次 2 步 7 才分配**：小审按 P4 §4 表行序核过，目前 G5=`brief_ref` 绝对路径、G6=`terminal_state`，与 brief.md L26 的 ①~⑥ 序一致；但批 2 一旦改序，四处引用会**静默失效**。 | 批次检查点1小审 F-5；`ADR-002:27,66,106,108`；`10-P4…报告.md:98-103`；`brief.md:26` | **已修**：ADR-002 加锁定注「G1~G6 = P4 §4 表自上而下行序，锁定不得重排」；批 2 步 7 落表时须复核该锁 | resolved |
| F-007 | P3 | **张力段对两条轴严格度不对等**：语言轴写成对赌（两边并列），代码根轴直接下判断「R-In 的成本**明显**低于 R-Out」。虽落在被允许分析的段内，但对一条**用户待点选**的轴给出了带「明显」的定性结论。 | 批次检查点1小审 F-6；`ADR-001:109` | **已修**：改为与语言轴同构的对赌句式（R-Out 换到的 = 物理隔离/独立分发资格，代价 = 治理重建 + 跨仓收口），去掉「明显」 | resolved |
| F-008 | P3 | **`executor_kind` 枚举含厂商 token，将与批 3 禁词表冲突**：`pi-agent` / `dsh-agent` / `herdr-agent` 写进协议不算违规（DevPlan L121 的 H6 验收口径自己就逐字用 `dsh-agent`，禁的是"私有类型"不是厂商 token），但批 3 步 14 要建 `forbidden-types.txt` 全域 grep，届时这三个值必然误报。 | 批次检查点1小审 F-7；`ADR-002:105`；`dev_plan/P5-…:84,121`；`task_plan.md:63` | **已修**：ADR-002 净产出表加豁免注——禁词表针对**导入的类型名**，这三个作为不透明枚举**字面量**显式白名单（H6 断言依赖 `dsh-agent` 存在）；批 3 步 14 建表时按此执行 | resolved |
| F-001 | P2 | `dh dh-relay` 对 P5 计划报 R29 `EVENT_STALE` + 4×`EVIDENCE_STALE`（要求 planning-event / evidence marker 在当前 HEAD 变化集中新增或修改）。但 HEAD `fd68621` 只是**机械刷现状块**（2 行），按 dev-harness「状态回填等机械进度事实不形成新事件」本不该触发 R29——疑似工具侧对「纯状态回填」缺豁免。本卡 D 开工同样只做机械回填，收口提交时会复现同一组失败。 | `git show --stat fd68621` → 1 file / 2 insertions；`git show fd68621 -- <P5计划>` grep `dh:planning-event` → 0 命中；`dh dh-relay` 失败段含 5 条 R29 | 范围外（dh 工具侧规则问题，不属 DHR_28 变更范围）。**存量、非本卡引入**；收口 E1 若被该组失败拦下，按存量项登记并回链本条，不据此改 P5 计划的 planning-event 标记去"凑绿"。建议升 `backlog.md` 由 dh 工具侧处置。 | open |
| F-047 | P1 | **明文交付物 `fixtures/manifest.json` 未做，而计划行被改写成"不含 manifest"并标 100/present**。task_plan 步12 逐字要求「目录内放 manifest.json 列全部 fixture 与其 canonical sha256（承接 P4 §2.1 的 manifest 对证做法）」；批 3 的 visual_map 改动把交付物名与证据口径两处同时删掉后判 100/present，findings / OPEN-POINTS 均无登记，四态里明明有 `waived` 却没用。**更该记一笔**：主会话派 cp3 时，把 task_plan 步16 的必查③「fixture manifest 的 sha256 是否真对得上」换成了别的题目——计划里有 → 产物里没有 → 复核题面里也没有，三处同时消失。**实质后果不是记账问题**：没有 manifest，`--selftest` 的通过数只能证明「当下盘上这批自洽」，证明不了「这批还是过审时那批」；DHR_29/30 拿它当契约回归基线时，任何一份被悄悄改软都无机制可查。 | 批次检查点 3 小审 P1-1；`task_plan.md:61`（要求）/ `task_plan.md:65`（必查③）/ `visual_map.md:19`（被改写的行）；`find relay-core -iname "*manifest*"` → 0 | **已修**：①建 `tools/canonical.mjs`（RFC 8785 JCS + sha256 的**可执行实现**——CANONICALIZATION.md 此前只是散文，五个内容指纹字段在本仓内部同样无人能复算）；②建 `tools/fixture-manifest.mjs`（`--write` 生成 / 默认对证，份数**相等**而非「至少」、逐份 digest 比对、孤儿 `.expect.json` 报错）；③`npm test` 加两条用例（manifest 对证 + JCS 口径回归）；④visual_map 该行改回含 manifest 的原口径。红绿实测：改软任一反例载荷或其 expect → `exit 1` 并指名文件与 digest 差异。 | resolved |
| F-048 | P1 | **`run.state` 的七档规范性聚合条款零机器闸**：条款正文写着「规范性聚合条款（冻结，实现不得另立）」，紧挨着的 `group` 六条映射写着「由下方 allOf 逐条机器强制」并确实做了——聚合条款**一条 allOf 都没有**，而它完全落在同一份文档内、完全可编码。实测：`run_status=succeeded` + `group=done` + 节点里有一个 `failed` → **放行**；全部节点 `pending` 同样 → **放行**。**这条打破了批 2 已写进账本的结论**：E-029 记「『从未跑过的 Run 报告成功完成』这个协议能给出的最坏答案不再可能」，而上面第二例正是它的非空版本。批 2 五轮复核没抓到，是因为当时没有校验器；批 3 的校验器是第一个能测它的工具，一测就现形——与 F-043 同形，只是下沉一层。 | 批次检查点 3 小审 P1-2；`relay.run-state.v1.schema.json` 的 `run_status.description` vs 文件末 `allOf`；两份构造载荷实跑均 `{"ok": true}` | **已修**：条款①~⑥逐档写成 `if/then`（前档取值排除 + 本档 `contains`，⑥用 `items` 全称约束；⑦在 `minItems:1` 下不可达，由⑤承接并在 description 说明）。allOf 由 6 条增至 12 条。**14 例红绿矩阵全部符合预期**（7 例应 PASS / 7 例应 REJECT）。E-029 的越界结论已在 progress 内就地更正，不静默留着。 | resolved |
| F-049 | P2 | **未知版本的 reason code 只对 7 份协议里的 1 份兑现**：F-043 修的是 `relay.rpc/v1` 的 `protocol_version`（形态上 7 份全改成了 `const`，这点没问题），但 `toReason()` 只把 `protocol_version` 映射到 `E_UNSUPPORTED_VERSION`，其余 6 份的顶层 `protocol` 落进 `E_BAD_VALUE` 兜底桶。同一份载荷按调用方式不同还会给出两种码（按 payload 走 → `E_BAD_VALUE`；按 `--schema relay.run/v3` 走 → `E_UNSUPPORTED_VERSION`）。且 `pickReason()` 的判别字段优先是一条**硬编码 early return**、不走 `REASON_RANK`。附带：`reason-codes.md` 该行写的字段名是 `protocol_version`，与 6 份协议实际的 `protocol` 对不上。 | 批次检查点 3 小审 P2-1；`tools/validate.mjs` 的 `toReason()` / `pickReason()`；`reason-codes.md:12` | **已修**，并且比小审的建议多做了一步二分——**同名异版**（`relay.run/v2` ← `relay.run/v3`）判 `E_UNSUPPORTED_VERSION`，**异名**（`relay.run-state/v1` ← `relay.event/v2`）判 `E_BAD_VALUE`。理由：两者给客户端的行动不同（去升级 vs 去改发送目标），一律判版本问题会把「发错协议」说成「版本太新」。`pickReason` 的硬编码返回值改成 `toReason(disc, payload)`，本函数只管优先级。`reason-codes.md` 字段名口径同步改准并写上这条分界。 | resolved |
| F-050 | P2 | **禁词表漏掉 `pi`，中立性机器闸比它声称的窄**：`forbidden-types.txt` 自己开头引的权威原文是「不导入 DSH / Cordis / **Pi** / Herdr / DevHarness 私有类型」，表里只有四家；`audit-contracts.mjs` 的注释还在举 `pi.agent` 会被命中当例子。实测 `pi_session_ref` / `pi-native-adapter` / `pi_agent_handle` **均不命中**——往任何 schema 的 `properties` 里加 `pi_session_ref`，审计报 0、exit 0、`npm test` 第 5 条照样绿。**验收口径第 2 条对五家里的一家是空的**。E-034 的负向验证只插了 `cordis_hint`，恰好没碰到这个洞。 | 批次检查点 3 小审 P2-2；`tools/forbidden-types.txt`（生效词 4 家）；`audit-contracts.mjs:44` 注释 | **已修**：禁词表加 `pi` 并在表内写明五家齐的理由。附带风险已实测——边界规则下 `api` / `mapping` / `pipeline` / `capability` / `shipping` / `epic` / `topic` 均不命中，`pi-agent` 本就在白名单。红绿：插 `pi_session_ref` → `exit 1` 并指名位置。顺带更正注释里那个假例子（`han-dsh-ake` 是把 `handshake` 掰开的示意，不是测试串；真实单词 `handshake` 实测不命中，边界规则本身是对的）。 | resolved |
| F-051 | P2 | **禁词表 ↔ 白名单的「改一处须改另一处」只有注释、没有机器闸**：`forbidden-types.txt` 末段写「白名单由 `VENDOR_TOKEN_WHITELIST` 执行，改一处须改另一处」，但审计器里 `VENDOR_TOKEN_WHITELIST` 的 4 处出现（1 定义 + 3 消费）**没有任何一处把它和禁词表对校**。对比同文件的 `REGISTERED_OPEN` 与 `CONDITIONALLY_NARROWED`——两个都有陈旧登记检测，唯独厂商 token 白名单没有，这个不对称就是漏做的痕迹。失败场景：有人"清理"禁词表删掉 `dsh`（觉得 `dsh-agent` 反正在白名单里），`dsh_session_ref` / `dsh-native-plugin` 从命中变不命中，白名单同时成死代码但不报错，DSH 整家的中立性检查静默失效、`npm test` 全绿。 | 批次检查点 3 小审 P2-3；`tools/forbidden-types.txt` 末段 ↔ `audit-contracts.mjs:40` | **已修**：审计器启动时断言「每个白名单条目都必须被 `VENDOR_PATTERN` 命中」，不命中即报错 `exit 2`，与 `REGISTERED_OPEN` 的陈旧检测同一口径。红绿：删掉禁词表的 `dsh` → 立刻报「`dsh-agent` 不再被命中，白名单成死代码 + 该厂商的中立性检查已静默失效」。 | resolved |
| F-052 | P3 | **`.expect.json` 只钉 reason code、不钉出错位置**，而 `E_BAD_VALUE`（`toReason` 的兜底桶，判别力最低）在 21 份反例里占 9 份。当前这批 fixture 是干净的（小审逐条 diff + 变异复跑验过，无一份假阳性），但断言口径本身允许「换个地方错、码碰巧一样」照样绿：某条 allOf 分支被改坏时，只要该 fixture 里另有任何一处也落 `E_BAD_VALUE`，测试不会红。 | 批次检查点 3 小审 P3-1；`fixtures/negative/*.expect.json` 形状；`tools/validate.mjs` 的 negative 比对逻辑 | **已修**：校验器改为同时返回 `at`（**定下这个 reason 的那条错**的 instancePath，`required` / `additionalProperties` 类把缺失键/多余键接到路径尾部，否则「缺 a」与「缺 b」不可区分）；22 份 `.expect.json` 全部登记 `at`，selftest 与 `npm test` 双钉、**不给「老 fixture 可以不写」的宽容通道**。22 条位置逐条与各自 `why` 核对过，是各反例声明要测的那一处，不是从输出反抄。红绿：把某条 `at` 改成同 reason 的别处 → 该条 FAIL。 | resolved |
| F-053 | P3 | **negative 份数下限比实际少 1，且 negative 循环对非 `.json` 后缀静默 continue**：`j.negative.length >= 20` 而实际 21——删掉任意一份、或把它改名成 `.json.bak`，份数掉到 20 仍绿；golden 的 `>= 10` 是贴着实际数的，negative 这条不是。份数断言的全部意义就是防这个，松一格等于没有。 | 批次检查点 3 小审 P3-2；`test/contracts.test.mjs`；`tools/validate.mjs:175` | **已修**：改成与**盘上实际文件数相等**（不是「至少」），基准现算；manifest 那道闸再保证盘上这批就是过审时那批。孤儿 `.expect.json` 由 manifest 对证单独报错。 | resolved |
| F-054 | P3 | **RPC 四种帧里 `response` 没有 golden fixture**（有 request / error / notification），commit message 也自述「RPC 三种帧」。`response` 分支现在没有任何正例钉住，而它是 `bestBranchErrors` 参与比较的四支之一；将来若这一支写坏到编译不了，`compiled !== 4` 会让整个最佳分支选择静默退回整体错误，没有回归能抓。 | 批次检查点 3 小审 P3-3；`fixtures/golden/` 缺 `rpc.v1.response.json` | **已修**：补 `fixtures/golden/rpc.v1.response.json`，实跑 PASS。golden 由 10 份增至 11 份。 | resolved |
| F-055 | P3 | **改期望后，「`reason` 字段整个省略」这条路径失去反例覆盖**：`g6-attempt-failed-without-reason.json` 实际写的是 `"reason": null`（字段存在），改期望为 `E_BAD_TYPE` 本身是对的（小审独立从 schema 推过），但改完之后文件名 `without-reason` 与它实测的路径不符，而「attempt_failed 却没填 reason」这条路径（实测给 `E_MISSING_FIELD`，正确）没有任何 fixture 钉住。G4 反复强调的正是「缺省 ≠ 已知」这个区分。 | 批次检查点 3 小审 P3-4；`fixtures/negative/g6-attempt-failed-without-reason.json` | **已修**：原份改名 `g6-attempt-failed-reason-null`（`why` 写准它测的是「填了 null」），新增 `g6-attempt-failed-reason-omitted`（删掉 `reason` 键，期望 `E_MISSING_FIELD`）。两条路径实测分别得 `E_BAD_TYPE` / `E_MISSING_FIELD`，成对钉住。negative 由 21 份增至 22 份。 | resolved |
| F-056 | P3 | **G5 的 reason 判据挂在正则的写法上**：`toReason()` 按 locator pattern 的两段特征子串（`~%` 字符类 + scheme 前瞻）判定，而不是按 `$ref` 目标。风险不在「当前会不会误判」（当前正确），在于判据的稳定性依赖于**别处一段正则的字符怎么排**。 | 批次检查点 3 小审 P3-5；`tools/validate.mjs` 的 `toReason()` pattern 分支 | **已修**：改按 `schemaPath` 是否指向 `$defs/locator/pattern` 判定。红绿实测：把 locator pattern 改写成**语义等价但两段特征串都不在**的形式（8 个探针逐一对照确认等价），旧判据退化成 `E_BAD_VALUE`，新判据仍是 `E_ABSOLUTE_LOCATOR`。**如实登记小审框定的边界**：新判据只覆盖「按 `$ref` 引用共享 locator 定义」这条支持路径；若有人另起一份**内联的** locator 类 pattern，新旧判据都会落回 `E_BAD_VALUE`——该形态未在本卡关闭，随 F-042 一并交 DHR_29 的 schema 演进纪律。 | resolved |
| F-057 | P3 | **「发错协议」与「时间戳格式不对」同码**：判别字段异名落 `E_BAD_VALUE` 兜底桶。理论上值一个 `E_PROTOCOL_MISMATCH`，但加码要走 `reason-codes.md` + 反例 + 边界说明三件套，属**协议变更**，不该在收敛轮做。 | 批次检查点 3 小审轮2 留档项（复核者明示不作为 P3 上报） | **移交 DHR_29（已确认留档）**：诊断信息当前可恢复——`at` 钉住 `/protocol`、`detail` 明说 `must be equal to constant`。待 DHR_29/30 真正用起来后再决定是否值一个新码。 | 留档 |
| F-058 | P3 | **禁词表机器闸的固有边界**：F-051 的白名单↔禁词表对校只能保护「有白名单条目指着」的词。删掉 `dsh`/`pi`/`herdr` 会被拦（各 exit 2），但删掉 `cordis`/`devharness`/`psmux`/`subagent` **不会**被拦（实测 exit 0），因为没有白名单条目依赖它们。 | 批次检查点 3 小审轮2 留档项；`tools/audit-contracts.mjs` 的 `staleVendorEntries` 判据 | **已知边界（已确认留档）**：这是该闸的设计边界不是漏做——它防的是「白名单成死代码而无人察觉」。`forbidden-types.txt` 是入仓文件，删词必然出现在 diff 里，有人工审查兜底。轮1 P2-3 提的具体场景（清理禁词表删掉 `dsh`）已被拦住。 | 留档 |
| F-059 | P3 | **`at` 在 `E_BAD_VALUE` 桶上挂在 ajv 的错误顺序**：`pickReason` 取第一条映射到胜出 reason 的错，ajv 版本变化理论上能让 `at` 位移。 | 批次检查点 3 小审轮2 留档项；`tools/validate.mjs` 的 `pickReason()` | **已知边界（已确认留档）**：位移的后果是**变红不是变绿**（selftest 会 FAIL 并打印期望/实得两个位置），出现即可诊断；`package-lock.json` 已钉版本。不为此加抽象层。 | 留档 |
| F-060 | P3 | **`locOf` 拼接缺失键名时不做 JSON Pointer 转义**：键名含 `/` 或 `~` 时产出的 `at` 不是合法 Pointer（实测 `E_UNKNOWN_FIELD @ "/a/b~c"`）。 | 批次检查点 3 小审轮2 留档项；`tools/validate.mjs` 的 `locOf()` | **已知边界（已确认留档）**：纯显示问题，不影响任何判定；`at` 只做字符串相等比较、不做 Pointer 解析，且现有 22 份 expect 无一使用这种键名。 | 留档 |
| F-061 | P2 | **仓级存量：`design/README.md` 的设计验收契约不可判定**（`DESIGN_INPUTS_STRUCTURE_INVALID`，R0 / R26 / R29 三条，提示「只保留一份合法权威契约后重试」）。另有 R14「任务找不到 DevPlan 计划卡验收口径」对 **DHR_01/02/03/04/26/27/28/49 全部 8 张卡**命中——本卡 brief 的完成条件确系逐字复制自 DevPlan §3.2，故 R14 是工具侧解析问题而非漂移。 | E-048：工作树 `dh dh-relay` → 33 失败；主树同跑 → 44 失败，`DESIGN_INPUTS_STRUCTURE_INVALID` 在**主树同样命中 3 条** ⇒ 非本工作树引入；R14 命中面覆盖全部 8 张卡 ⇒ 非本卡特有 | 范围外（dh 工具侧 / design 治理，不属 DHR_28 变更范围）。**存量、非本卡引入**，与 F-001 同类。收口 E1 若被这几条拦下，按存量项登记并回链本条，**不据此去改 design/README 或 brief 来"凑绿"**。建议与 F-001 一并升 `backlog.md` 由工具侧处置。 | open |
| F-062 | P1 | **`digestExcluding` 被 `__proto__` 键静默吃掉，两份不同载荷算出同一摘要**。首版 `const copy = {}` 让 `copy["__proto__"] = X` 触发 `Object.prototype.__proto__` 的 **setter**（改 copy 的原型），而不是建一个同名自有属性——该键从此在 `Object.keys(copy)` 里**消失**。`JSON.parse('{"__proto__":{…}}')` 产出的正是自有可枚举属性，所以不是构造刁钻输入才能碰到的边界。**它是 `request_digest` / `payload_digest`(×2) / `plan_digest` / `state_signature` 五分之四的实现**，而 `request.params` 与 `result.structured` 恰是 OPEN-POINTS 登记的 `additionalProperties: true` 开放点 ⇒ **真实协议流量里可达**。后果是直接击穿这五个指纹字段存在的理由（防篡改 / 一致性握手）。`digest()` 本身不受影响（只**读** `value[k]`，自有属性优先）。**cp1/cp2/cp3 四轮复核全未抓到**。 | E5 教训复核；`tools/canonical.mjs` 的 `digestExcluding()`；主会话独立复验实测 `digestExcluding(evil,[]) === digestExcluding(plain,[])` | **已修**：`copy` 改用 `Object.create(null)`。复验五项全过（evil≠plain、evil≠evil2、排除空集时与 `digest()` 等价、排除功能未坏、`constructor`/`toString` 同名键保住）。补 `npm test` 回归用例，**退回修复即红**（实测）。**这是重蹈**——[[DHR_26]] 已登记「JSON 字典不能默认用 `{}` 承载外部/未知键」。 | resolved |
| F-063 | P2 | **验收口径第 2 条是全称命题却做成了黑名单**（重蹈 DHR_49 L-07）。「不导入五家私有类型」用 `forbidden-types.txt` 实现，黑名单只能证明「没有我想到的那几种」。同一根因已冒头两次：F-050 漏 `pi`、F-058 承认删掉四个词不被拦。DHR_49 L-07 逐字写着「凡是『零 X』『只用 Y』这类全称结论，断言必须是白名单」，且 **2026-08-20 22:19 进仓，比本卡批 1 早 12 分钟**。 | E5 教训复核；`git log -1 554e543` = 08-20 22:19 vs `a10e294` = 08-20 22:31 | **已修**：新增 `tools/structural-tokens.txt` 登记全部 162 个结构位置 token（`properties`/`$defs` 的键 + `enum` 项 + `const` 值），未登记即 `exit 1`；陈旧登记同样拦；清单缺失也拦（不静默降级）。黑名单保留为第二层，给已知厂商词更明确的报错。**四组红测**：插禁词表里根本没有的 `acme_workspace_ref` → 拦住（黑名单必漏）；清空禁词表后插 `cordis_hint` → 白名单层仍兜住（F-058 的边界随之消失）；陈旧登记 → 拦；清单文件删掉 → 拦。教训另记 L-016。 | resolved |
| F-064 | P1 | **验收口径 1 的「能力不匹配 fail-closed」既没兑现、也没有登记的承接人**。契约层只能校验 `capability_hash` 的**形态**（`^[0-9a-f]{64}$`）——`capability-mismatch` 反例实测命中的是 `E_BAD_VALUE @ /handshake/capability_hash`；一份**形态合法但与对端不同**的指纹，schema 必然放行（也只能放行）。问题在于：**DHR_29 与 DHR_30 的验收口径里一个字都没提能力/capability**（E4 逐条读过两张卡），findings 里也没有对应的 `open` 移交条目。DevPlan §4.1 的 **P5-M6**「协议版本、能力和未知输入均 fail-closed」承接卡只写了 DHR_28。 | E4 需求复核 P1-2 + E2 代码复核 P2-3；`fixtures/negative/capability-mismatch.expect.json` 的 `why`；DevPlan §4.1 P5-M6 行 | **部分处置 + 移交（待用户裁决计划层动作）**：①本卡已补 `capability-baseline.json`，把「两个实现能不能算出同一个指纹」这件事变成可复算的（E4 的 P1-1，另见 F-065）——**指纹的可复算性**由本卡兑现；②**指纹的运行期比对**契约层不可能兑现，移交 DHR_29；③完成条件 1 收口时**标「部分达成 + 移交登记」，不标达成**；④**是否在 DevPlan §4.1 给 P5-M6 补上 DHR_29/30 的承接标注**属计划层改动，已摆给用户，AI 不自行修改计划。 　—— **【2026-08-21 用户对话裁决】**：用户点选「**改 DevPlan 补承接 + 风险接受**」。已在 DevPlan `§4.1` 给 **P5-M6 的承接卡列补上 DHR_29**，并在 **DHR_29 的验收口径增一条机器证**——「RPC 握手期对 `capability_hash` 做**比对**并在不符时 fail-closed（`E_CAPABILITY_MISMATCH`），不得按能力交集降级工作」，附等价判据（构造两份指纹不同的握手断言被拒；并断言参考实现算出的指纹与 `capability-baseline.json` 逐字相符）。**为什么这样处置而不是只做风险接受**：问题的实质不是本卡欠账，而是**计划把一条三分句的口径整条压在了一张只能证其中两句的卡上**；只做风险接受能让本卡过闸，但敞口会飘着——P5 阶段闸走到 P5-M6 时可能在没有任何一张卡真正证过它的情况下记为通过。改计划是把它**钉到真正有手段验它的那张卡上**。本卡完成条件 1 仍判「否」，不因移交而改判。 | 遗留→DHR_29（已确认） |
| F-065 | P1 | **写进冻结契约文档的交付承诺静默没做（本卡此类第四次）**：`CANONICALIZATION.md` §三末句与 §四「留给下游」表第 1 行**逐字**把「对 §三 样例清单跑出可复算的固定 digest 作为回归基线」写成**批 3 交付项**，产物里零命中（`capability_manifest` / `executor_kinds` 全仓零命中），findings / OPEN-POINTS 零登记，四态里的 `waived` 又一次没用。与 F-047 同形，只是这次连计划行都没有，所以没人发现。**后果**：`capability_hash` 是口径 1 点名的字段，而 §一 自己写了失效后果——两个实现各按自己的方式算，握手**永远** `E_CAPABILITY_MISMATCH`；DHR_29 与 DHR_30 各自实现握手时谁都无法验证自己算对了。**讽刺的是**：本卡给 fixture 建了基线（manifest），给协议自己定义的基线反倒没建。 | E4 需求复核 P1-1（同时是 E2 代码复核 P3-3）；`grep -rni "capab" relay-core/{tools,test,fixtures,package.json,README.md}` → 只有反例与占位 | **已修**：新增 `tools/capability-baseline.mjs` + `capability-baseline.json`（代码根顶层——不放 `contracts/`，那里的命名约定闸 D-29 要求 `*.schema.json`/`*.shape.json`，放进去要么违规要么得给闸开例外，而「给闸开例外」正是本卡一路在避免的形态）。**它同时关掉 E2 的 P3-3**：manifest 钉的是 55 份 fixture、`contracts/` 一份都没钉，而 schema 才是本卡真正冻结的工件。红测：只在某份 schema 的顶层 `description` 末尾加一个空格 → `exit 1`，「改一个错别字也是能力变更、也会断握手」现在是机器可证的。 | resolved |
| F-066 | P1 | **F-048 的修法本身重新变成空绿（本卡第四次「下沉一层」）**：七档聚合条款升成 `allOf` 这一步是对的（cp3 轮 2 用 714 例、E2 用 2394 例各自穷举确认与条款等价），但 **`allOf` 自身没被任何东西钉住**——删掉 6 条聚合分支里的**任意一条**，`npm test` / `selftest` / `audit` / `manifest` 四道闸**全绿**。对照组：`group` 映射那 6 条各有一份反例钉住，删掉会红。于是「有人在 P7 演进 run-state 时改坏一条聚合分支」这条回归路径重新是空绿，而那时不会再有「批 3 的校验器第一次能测它」这种红利。前三次是 F-043→F-049、D-19→D-25、D-20→D-27。 | E2 代码复核轮 2 · P2-1；逐条删 `allOf[6..11]` 实测四闸全绿 | **已修**：把 E-041 的红绿矩阵落成 `npm test` 的一条用例（7 应过 / 7 应拒），它**同时钉住 `allOf` 与条款正文的对应关系**——矩阵是照 description 的①~⑦档写的，谁改条款不改 `allOf`（或反过来）这里就红。主会话逐条删 `allOf[6..11]` 复验，**六条全部真红**。 | resolved |
| F-067 | P2 | **验收口径第 2 条的第二个合取项零证据，而结论写成了整条口径兑现**。口径原文是两个合取项：「不导入五家私有类型」**且**「DSH 与 Runtime 不共享活动对象或内存状态（fixture 与 schema 静态可证）」。第一项有三层机器闸；**第二项在全卡范围内没有任何证据条目、没有任何断言**，而 E-034 的结论列写的是「验收口径第 2 条兑现」。**与 E-029 完全同形**：证据只覆盖一个合取项，结论写了整条。 | E2 代码复核轮 2 · P2-2；`progress.md` E-034 结论列；`review.md` 的 `DHR28-A2` 行 | **已修**：审计器新增一维（9→11 维度）——①每个 `type` 取值只能是 JSON 原生类型（没有任何字段能承载句柄/指针/对象引用）；②每个跨边界引用字段（`ref` / `*_ref` / `locator` / `path`）必须 `$ref` 到共享 `relay.common/v1#/$defs/locator`。「不共享活动对象」于是成为**可复算的结构事实**而非一段散文。红测：把 `executor_profile.ref` 的 `$ref` 换成内联 pattern → `exit 1`。**顺带关掉 F-056 那条残留**（内联 locator pattern 的形态，此前两版 reason 判据都盖不住，现在这一维直接不让它存在）。E-034 结论已改准。 | resolved |
| F-068 | P2 | **兼容矩阵与 `CANONICALIZATION.md` 直接矛盾**：`compat-matrix.md:25` 把 `plan_hash → plan_digest` 标「改名 · 语义等价」，而 `CANONICALIZATION.md` 在 §一和 §四**两处**写着「v1 与 v2 的摘要**不可互认**，兼容矩阵按**改型**处理」。实质也不等价——v1 是 PowerShell `Sort-Object` + `ConvertTo-Json -Depth 50`，v2 是 RFC 8785 JCS，同一份计划算不出同一个值。 | E14 一致性复核 F-E14-1 | **已修**：改判「改型」并写明「v1 的 `plan_hash` 值不可直接充当 v2 的 `plan_digest`，迁移须按 v2 口径重算」。§0 计数随之 改名 5→4 / 改型 23→24，**按文档自述的复算方式脚本复算 = `{保留2, 改名4, 改型24, 新增10, 弃用4}` 合计 44**。教训另记 L-017（改判分类要连计数一起改，否则宁可不改——这大概就是当初没改的原因）。 | resolved |
| F-069 | P1 | **G2 聚合条款有两份 D-19 之前的旧副本，且与冻结 schema 机器级互斥**：`compat-matrix.md:69` 与 `v1-gap-disposition.md:38` 都还写着四档口径「`failed > running > waiting_human > succeeded`，表外一律 `unknown`」。**后者尤其重**——它是验收口径第 5 条的交付物，且 DevPlan B-13 指定 DHR_29/DHR_30 拿它当处置依据。E2 实测：按该行口径构造的三份载荷（全 `pending` 的新 Run / `[succeeded, pending]` / `[orphaned, running]` 记 `unknown`+`needs_you`）**被本卡自己交付的校验器逐份拒收**；DHR_29 照它实现，产出的每一份 run-state 都过不了闸，而且实现的正是 D-19 判死的那个报警噪音病灶。 | E2 代码复核轮 2 · P1-1 与 E14 一致性复核 F-E14-2（两路独立指向同一处） | **已修**：两处均改为现行七档（①~⑥已由 `allOf` 机器强制，⑦在 `minItems:1` 下不可达为防御档），并各加更正注说明原因（v2 的 `node_state.status` 比 v1 多了 `pending`/`orphaned`）。**标「P4 投影期处置 · 原文照抄」的行保留旧文字不动**，那是留痕。E2 给的复跑判据（在处置表里 grep `orphaned` 与 `pending` 两词的出现行数）由 0 变为 >00。 | resolved |
| F-070 | P1 | **`run-state` 的 `group` 自称与 P4 pilot「保持同一口径」，该陈述不实**。E14 实读 P4 冻结件源文件（`relay-control-pilot/src/read-model/schema.mjs` 与 `project-v1.mjs`）比对，至少三处不同：①词表 pilot 是 `[needs_you, blocked, running, done]`、v2 是 `[needs_you, running, done, failed]`；②`failed` 的归堆 pilot 是 `→ needs_you`、v2 是 `→ failed`；③`progress` pilot 是 `{done, total}` 计数对、v2 首版擅自改成了 0..1 比率。**而 DevPlan B-13 明令「字段增删须留处置记录、不得静默漂移」，一条记录都没有**。DHR_30 会拿着「已经对齐过了」这句话去实现，然后撞上三处不同。另有 `trigger` 两个值静默改名（`api→system`、`parent_run→upstream`）、`labels` 整体消失。 | E14 一致性复核 F-E14-3 / F-E14-5 / F-E14-6，实读 P4 冻结件源文件（非转述） | **已修**：①`progress` **改回计数对**——计数对能算出比率、反之不能，是严格更强的形态；且 P4 已验证、被 B-13 列进 P5-M4 证据的跨屏断言「`progress` 等于详情节点实际计数」在比率形态上无法表达。`done <= total` schema 表达不了，随「`node_states` 覆盖 `nodes` 全集」一并交 DHR_29。②词表与归堆两处**保留为有意差异**并逐条写下理由（v2 有了 run 级 `run_status`，`failed` 必须能与 `needs_you` 分开）。③`trigger` 改名补进 G1 处置行。④**`labels` 采纳进 `relay.run/v2`**（见 F-071）。⑤`v1-gap-disposition.md` 末尾加「DHR_30 开工前必读」段，点明本卡已替它冻掉的三个展示字段及其天花板。 | resolved |
| F-071 | P2 | **`labels` 整体消失且无处置记录**。P4 冻结件的源文件里逐字写明 `labels` 是 design/04 §1「Core 不内置任务卡 / 计划等领域词义，所以私有标识只能以不透明 label 搭车」这条原则的**唯一载体**；本卡七份契约里 `labels` **零命中**。后果：DHR_31 的 basic-agent-task 想挂任务卡 id 时无处可挂，唯一去处会是 `relay.result/v2` 的 `structured`（OPEN-POINTS O-1 的开放载荷），而那里协议层不承诺中立性、也不受 locator 约束。 | E14 一致性复核 F-E14-6；`grep -rn "labels" relay-core/contracts/` → 0 命中 | **已修（采纳）**：`relay.run/v2` 加 `labels`，形状 `[{key, value}]`，`additionalProperties: false`。中立性不受影响——`key`/`value` 都是不透明字符串、Relay 只存不读，结构位置零厂商 token，禁词表与结构 token 白名单照常生效（往 label **值**里写什么是上层的事，不是协议结构）。golden fixture 已示范 `devharness.task = DHR_28` 的搭车用法。 | resolved |
| F-072 | P3 | **两条测试用例的名字比它的断言强**：①「fail-closed **三条**各有一份反例钉住」实际只断言两个码 + 第三条只查文件存在；②「契约静态审计 **9** 维度全绿」而审计器已是 11 维度。测试标题是下游最先读到的东西，`npm test` 全绿会让 DHR_29 相信三条 fail-closed 都被反例钉住了；inline 注释是诚实的，但**注释不进测试报告**。这正是本卡反复在堵的「形态冒充语义」，只是这次发生在**测试名**这一层。 | E4 需求复核 P2-2 | **已修**：①改名为「fail-closed：未知字段 / 未知版本有反例钉住；能力不匹配只有形态例」，并把欠账写进断言消息（回链 F-064）；②改为 11 维度。 | resolved |
| F-073 | P3 | **批 2 的六条核心证据挂在两个从未入仓的脚本上**：`progress.md` 的 E-021 / E-023 / E-025 / E-026 / E-029 / E-031 命令列写 `node probe.mjs` / `node rs.mjs`，而这两个脚本在 `relay-core/` 下零命中、`git log --all --diff-filter=A` 对本卡零命中——**从未入仓**。账本自己的口径是「每条『完成』结论挂一条**可复跑**的命令」，任何人照 E-021 复跑会得到 file-not-found。 | E2 代码复核轮 2 · P3-2 | **已修（登记等价证据，不追认脚本）**：这是**证据可复跑性**问题不是结论真伪问题——结论多数已被批 3 的 selftest 承接（H6 两形态 / G1 / G3 / G4 / G5 三形态 / d25 两形态 / 未知字段 / 非法时间戳都有 committed 反例）。六条证据行各加一句「探针为一次性脚本、未入仓；等价 committed 证据 = …」。其中 E-026 的「6 条映射逐条正反验」只有 2 条有 committed 反例、E-041 的 14 例矩阵此前无 committed 载体——后者已由 F-066 的矩阵用例承接，回链过去。 | resolved |
| F-074 | P3 | **brief 自称「完成条件 = 任务卡验收口径的逐字复制」，实际不是**。E4 机械比对（剥 markdown 链接与粗体后逐字符比）：口径 1~4 正文逐字相同（只差 DevPlan 的出处前缀，属结构性搬运），**口径 5 有 5 处词级增删**——删掉一个背景括注、加了①~⑥编号、补了三处字段名。**没有一处放松口径**（3 处补的正好是 P4 报告 §4 原表的字面，等于把 DevPlan 的转述还原回上游原文），但「逐字」是这套设计里防口径漂移的那道锁，一旦「差不多就行」被默许，下一张卡的漂移就没有基准可比。 | E4 需求复核 P3-1 | **已修**：brief 头改口径为「**逐字复制 + 标注型补注**」，并在口径 5 注明「字段名按 P4 §4 原表补注，非 DevPlan 原文」。不回退那三处补注——它们让口径更可执行，退回去反而是损失。 | resolved |
| F-075 | P2 | **`capability_hash` 不覆盖共享定义模块——F-033 缺口 A 下沉一层（本卡此类第五次）**。`capability-baseline.mjs` 的 `frozenSchemas()` 只枚举 `contracts/` **顶层**的 7 份协议，`_shared/relay.common.v1.schema.json` 不在内。而实际约束面的大半在那里：`locator` / `timestamp` / `sha256` / `run_id` / `node_id` / `attempt_id` / `executor_kind` / `executor_profile` / `reason_code` / `trigger` / `observation_status`。**E2 实测**：把 `locator.pattern` 改成 `^.*$`（G5 全域绝对路径禁令**彻底失效**，`C:\Users\…` 一律放行）之后，`capability_hash` **逐字未变**、基线与审计双双 exit 0。两个 Runtime 于是能带着完全不同的 locator 约束握手成功，然后互相拒对方的每一份载荷——这正是 F-033 缺口 A 的原话，只是对象换了一层。 | E2 代码复核轮 2 · P2-1；主会话独立复验：篡改后 `capability_hash` 仍为 `5c5685d0748b9f58…`，与干净态逐字相同 | **已修**：`frozenSchemas()` 增列 `_shared/relay.common.v1.schema.json`（`$id` = `relay.common/v1`，直接套现成的 `{id,digest}` 形状），并对 `_shared/` 加独立份数断言（应为 1 份）。`protocols` 由 7 条变 8 条，`capability_hash` 由 `5c5685d0…` 变 `3ccf3b10…`——**这个变化本身是正确的，它确实是一次能力变更**。`CANONICALIZATION.md` §三同步写明「共享模块必须计入、v0 形状不计入」及各自理由（同一条理由的两面：v0 未冻结所以排除，共享模块已冻结且承重所以纳入）。红测复跑：同一篡改现在 `exit 1` 并精确报「协议 relay.common/v1 的 digest 变了」。 | resolved |
| F-076 | P2 | **A2b 的等价判据说宽了：审计器第 11 维的①不支撑「不共享活动对象」这个命题**。判据原写「① `type` 只能是 JSON 原生类型 **⇒** 没有任何字段能承载句柄/指针/对象引用」——**这个 ⇒ 是假的**。E2 反例：`properties.session_handle = {type:"string"}` 在第 11 维下**零违规**（句柄用不透明字符串装就行，天然是 JSON 原生类型；它是被 A2a 的结构 token 白名单拦下的，不是被第 11 维）。①只会被 `type:"handle"` 这种**非法 JSON Schema** 触发，而 `--selftest` 对同一篡改本来就红 ⇒ ①相对既有闸的**独占战果 ≈ 0**。 | E2 代码复核轮 2 · P2-2（主会话在派出简报里主动提了这个怀疑，E2 确认并给出比怀疑更明确的反例） | **已修**：不删①，改写判据为**三条合取**——①降格表述为「信封编码的形式检查，保证协议是纯 JSON，**不单独承重**」；②（跨边界引用字段必须 `$ref` 共享 locator）保持，它是真承重的且顺带关掉了 F-056 残留；**补③**「新字段名必须在 `structural-tokens.txt` 有意登记」，使「没有任何字段承载句柄」成为**全称**断言而非举例。「未覆盖边界」由「无」改为「句柄若以不透明字符串搭载，形式检查不可判——由③兜底强制『新字段名必须有意登记且出现在 diff 里』，这是可判定的最强形态」。覆盖态维持「等价覆盖」（改判据后成立）。 | resolved |
| F-077 | P3 | **A1b 的「独立 oracle」列写「12 份契约的 digest 基线」，实际是 7 份**。12 是 `audit-contracts.mjs` 的**扫描面**（12 份 schema/shape），不是基线**覆盖面**。`npm test` 的用例名写的「7 份契约」是对的，只有元数据表写错。 | E2 代码复核轮 2 · P3-1；`capability-baseline.json` 的 `protocols` 实测 7 条，`_shared` / `v0-shapes` 零命中 | **已修**：与 F-075 一并处理，改为「**8 份** = 7 份顶层协议 + 1 份共享定义模块」，并注明原写 12 的来源与更正出处。 | resolved |
| F-078 | P3 | **两份派生反例已不是「单一违规」，而账本里还挂着 cp3 立的那条方法学标准**。`g2-pending-must-not-alarm` 与 `g2-unknown-must-float`：把各自声称要测的 `group` 改回合法值后**仍被拒**，第二处违规落 `/node_states`（分别违反聚合条款⑤与④）。那是 F-048 加 `allOf` 时**顺带产生**的，不是 `progress` 改动引入的。cp3 立的「单 token 变异 + 基准自身 PASS ⇒ 不可能被别的原因拒」这条标准，在这两份上**已不成立**。 | E2 代码复核轮 2 · P3-2；`d25-empty-node-states` 不受影响（补一个 succeeded 节点后 `ok:true`），仍是单一违规 | **已修（留痕，不改 fixture）**：两份 `.expect.json` 的 `why` 各补一句说明。**风险已被 F-052 的 `at` 断言兜住**——E2 实测删 `allOf[2]` / `allOf[4]` 时两条各自 FAIL 并打印「期望 @/group 实得 @/node_states」。**不改 fixture 的理由**：把它构造成单一违规要动 `node_states`，那会让它不再是 golden 的最小变异，得不偿失。 | resolved |
| F-079 | P3 | **结构 token 采集口径的四个洞**（只收 `properties`/`$defs` 的键 + `enum` 项 + `const` 值）：①`required: ["dsh_session_handle"]` 而无对应 property → exit 0；②`dependentRequired` 里的键 → exit 0；③`format: "dsh-handle"` → exit 0；④**新建 `relay.dsh-bridge.v1.schema.json`（文件名与 `$id` 都含厂商名）→ 本维度 exit 0**。 | E2 代码复核轮 2 · P3-3，四条逐个实测 | **已登记，不改代码**：前三条**造不出可用的私有类型**（`additionalProperties:false` 下该 schema 不可满足；`format` 在 2020-12 只是注解），实质危害极低。第四条实质最重，**但已被 `capability-baseline.mjs` 的份数断言兜住**（报「顶层应有 7 份，实为 8 份——协议集变了就是能力变了」exit 1）。⚠️ **这条依赖关系已显式写进 A2a 的「未覆盖边界」列**：谁将来放宽那条份数断言，就会同时打开这个洞。另：`patternProperties: {"^dsh_.*$": …}` 被黑名单正确拦下（顺带把 F-041b「厂商 token 不扫 pattern 值」关掉了一半）。 | resolved |
| F-080 | P3 | **`validate.mjs` 的双模守卫在 `process.argv[1]` 未定义时抛错**：`node -e` / `--input-type=module -e` / REPL / stdin 管道下 `argv[1]` 是 `undefined`，`pathToFileURL(undefined)` **抛 TypeError** ⇒ 模块半边在这些入口下**整个不可用**（是 import 直接炸，不是「不跑 main」）。附带（存量、非本次引入）：无参调用打印用法后 **exit 0**——CI 里若变量为空会静默绿，正是本卡一路在堵的空绿形态。 | E2 代码复核轮 2 · P3-4，实测 `node -e "import(…)"` 抛 TypeError | **已修**：守卫加前置判空 `process.argv[1] && …`；用法分支改 `exit 2`。复验：`node -e` 下 import 正常导出 `loadAjv,validateOne`；无参调用 exit 2；CLI 与 `--selftest` 行为不变。 | resolved |
| F-081 | P3 | **`labels` 无数组序规则、无 `key` 唯一性约束**。`labels` 语义上是**无序集合**（不像 `nodes` 有 DAG 顺序、`source_refs` 有引用顺序），两个实现按不同顺序发同一组 label 完全合规；`key` 可重复，做查表的消费者遇到 `[{a,1},{a,2}]` 行为未定义。而 `CANONICALIZATION.md` 对 `labels` **零提及**——§三那条「JCS 只管对象键序、不管数组序，故数组序必须显式规定」（F-033③ 原话）正对这里。 | E2 代码复核轮 2 · P3-5；`labels` 是 F-071 本轮才采纳进 `relay.run/v2` 的 | **已修**：`labels` 的 description 补两条规范性约束——数组按 `key` 的 UTF-16 码元升序（`key` 相同则按 `value`）、`key` 在同一 Run 内唯一。两条都是 schema 表达不了的语义约束，与「`done <= total`」「`node_states` 覆盖 `nodes` 全集」同类，一并交 DHR_29。**为什么现在定**：本卡冻结的五个指纹字段当前都不覆盖 `relay.run/v2`，所以当下无失败场景；但趁 `labels` 刚进协议、还没有消费者时定，比 DHR_30 撞上再改便宜得多。 | resolved |

| F-082 | P2 | **测试在系统负载下假红：用退出码冒充语义，方向与空绿相反**。F-066 新增的聚合条款矩阵用例靠「每例 spawn 一个 node，看退出码」判定，而 `run()` 把 `execFileSync` 抛出的**任何**异常（ENOMEM / EAGAIN 之类资源问题）都归成 `code = 1` ⇒ 解读成「校验器拒绝了」⇒ **「应 PASS」的用例在资源紧张时假红**。实测：同一份代码，一次 `npm test` 该用例 FAIL 且耗时 37 秒，紧接着单独重跑 PASS 且 6.4 秒。**空绿是该红不红，这是该绿不绿，同样让测试结果不可信**——而本卡一路在堵的正是「用形态/退出码冒充语义」这一类。 | 主会话在 E 段收敛期跑全闸时撞上；`npm test` 一次 fail 一次 pass，代码无改动 | **已修**：`validate.mjs` 改成「既是 CLI 也是模块」（导出 `loadAjv` / `validateOne`，CLI 行为由 `import.meta.url === pathToFileURL(process.argv[1]).href` 守住），矩阵用例改**进程内判定**：一次编译、零 spawn、判定只看返回值，并**显式断言返回的是布尔结论**（不接受「异常也算拒绝」）。矩阵用例 37s → **176ms**，`npm test` 44s → **2.6s**。红测复验：逐条删 `allOf[6..11]` 六条全部真红；CLI 与 `--selftest` 行为不变。⚠️ **本条曾只写在 commit message 里、没进 findings**——E7 as-built 核对时报「盘上找不到『假红』这个事实」才补登记，见 F-085。 | resolved |
| F-083 | P3 | **账本被工具性陷阱写坏三次，而三次都没进 findings**：①**`String.replace` 的替换串里 `` $` ``（美元号紧跟反引号）是特殊转义**，意为「插入匹配点之前的全部内容」——F-064 的正文含 `` `^[0-9a-f]{64}$` `` 正好构成该序列，一次把整张 findings 表复制了一遍（74 行变 135 行、F-026/F-018 各出现两次）；②**行尾混合**（CRLF 与裸 LF 并存）让 `dh` 只认表格前半段，误报 E-049~E-054「未在账本登记」；③**markdown 表被插入的空行劈成两段**，同因导致 `dh` 漏读，清掉 9 处。 | 主会话在 E 段收敛期三次撞上；E6 教训 miner 专门搜过 findings 但零命中（因为从没记进去） | **已修 + 已登记**：①全仓补丁脚本改用 **replacer 函数**形式（`s.replace(a, () => b)`），根除 `$` 转义面；②三份工作区文档统一行尾为 CRLF；③清掉全部表内空行并加了「清完复跑 `dh` 核对」的习惯。**教训另记**——这类陷阱的共同点是「**工具静默地做了你没要求的事**」，与本卡一路在堵的空绿同源，只是发生在**写账本的工具**上而不是被测对象上。 | resolved |
| F-084 | P2 | **主会话覆盖了 brief 已登记的决定而当时没注意**：`brief.md` 的「触及子系统」原写「无（新建 `contracts/` 与校验器；**as-built 首份快照归 DHR_29 起**）」，而主会话派 E7 时把「`relay-core/` 是新子系统、必须建首份 as-built」当**硬条**下达，两处直接对撞。若不处理，盘上会留下两个互相矛盾的声明。 | E7 as-built 核对时抓出；`brief.md:36` 原文 vs 主会话派出简报 | **已裁决：保留 as-built，改 brief 并留痕**。理由两条：①dev-harness 节点表把 E7「as-built 收敛 · 首份建」列为**硬条**；②更实际的是**交付物形态与立项预期不同**——`relay-core/` 最终不是骨架，是 12 份 schema + 6 个工具 + 5 份规范文档 + 5 道机器闸的完整冻结契约，DHR_29 开工第一件事就是读它，把首份快照推给它等于让接手的人先自己摸一遍。⚠️ **这是主会话改了 brief 的已登记决定**，故 brief 那一行**显式改口并写明理由**、不静默改写——教训 L-014「计划行只能标状态、不能改交付物本身」讲的正是这个形态，本条是它的正向执行。 | resolved |
| F-085 | P2 | **「只写进 commit message」= 没记录**：F-082（测试假红）与 F-083（工具性陷阱）两批事实，主会话当时都在 commit message 里写清楚了，但**没有任何一条进 findings**。后果是可复现的——E6 教训 miner 按指令专门搜这两类，`grep` 零命中，判「证据不够具体，没抽」；E7 as-built 核对时直接报「这条我在盘上找不到对应事实」。**commit message 不在 miner、as-built、复核者的检索面里**。 | E6 教训 miner 与 E7 as-built **各自独立**报同一件事；`grep -c "CRLF\|行尾\|replace\|假红" findings.md` → 0 | **已修**：F-082 / F-083 / F-084 补登记；教训另记 L-019。⚠️ **这条本身是本卡「证据落点」纪律的一个洞**：账本口径是「每条结论挂一条可复跑的命令」，但没说「**每条被修的缺陷都要有一条 finding**」——于是「修了、commit 里写了、但账本里没有」成了合法状态。与 F-073（证据挂在未入仓脚本上）同属「记录存在但检索不到」这一族。 | resolved |
> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞
> 遗留（P0/P1 唯一合法路径，DC_46）：状态列写 `遗留→<DC_xx / backlog / 下计划名>（已确认）`——"已确认"三字代表你已在对话里明确点头同意把这条留到别处；没有这三个字，`dh-check` R13 仍按 `open` 处理、拦下"带病收口"。AI 不得自行把 open 的 P0/P1 划成遗留。
