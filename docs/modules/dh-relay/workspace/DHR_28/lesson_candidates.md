<!-- lesson_candidates.md — 教训候选。🟢 收尾顺手记。AI 起草、人裁决。 -->
# lesson_candidates — DHR_28

## 教训候选

| ID | 一句话教训 | 状态 |
|----|-----------|------|
| L-001 | **写契约时，问一句"我这条推理有没有用在最该用的地方"**——本卡在 G1/G3 反复用「列为必填而非可选，因为可选会让所有实现都省掉它」，却唯独没用在全卡唯一一条安全断言（H6 的触发开关 `required`）上，导致它 fail-open by default。同一份文档里的同一条原则，最容易漏掉的就是它自己最该管的那处。（cp2 小审 D-11） | ready-for-review |
| L-002 | **"文件级检查全绿"证明不了"契约能用"**——批 2 的 JSON 合法性、字段齐全、grep 锚点六项自验全过，但 7 份 schema 其实**编译不了**（`$id` 绝对 URI + `$ref` 相对文件路径）。凡是产出"给机器读的契约"，收口前必须**真拿目标工具加载一次**，看文件是看不出来的。 | ready-for-review |
| L-003 | **用 `grep -c` 数命中次数，结构上无法证明"无遗漏"**——批 2 用它论证"全域 `additionalProperties: false`"，漏了 4 处开口；同一条证据的禁词表里**没有一个词是实际会出现的 token**，搜一批注定搜不到的词得零命中不构成证明。要证"无遗漏"必须遍历，要证"无泄漏"必须扫**实际可能出现的形态**。（cp2 小审 D-7） | ready-for-review |
| L-004 | **`format` 不是断言**——JSON Schema 2020-12 把 `format` 放在 Format Annotation 词汇表，默认只作注解不做校验；只写 `format` 的约束在任何独立校验器上都可能空转。要真正静态可证，必须并列写断言性关键字（`pattern` 等）。（cp2 小审 D-18） | ready-for-review |
| L-005 | **派小审前必须把批次产物定住**；确需中途修 P1，要**立即**通知复核者而非事后。本卡两轮都是复核者查 mtime 先发现文件在变、主会话的通知后到——这会让复核报告基于已不存在的文件。（cp2 小审 D-10，两轮各发生一次） | **重复 → 指向 [[DHR_49 L-06]]**（E5 教训复核裁定：DHR_49 已登记同一条「『派审』与『冻结被审对象』须是同一个动作」，两卡应合并、互相引用，本条不作为独立新条目升格。⚠️ 时序说明：DHR_49 的 CP1 与本卡批 2 施工大致同期、两个 worktree 互不可见，属独立收敛而非「本该查到却没查」） |
| L-006 | **把"可修的缺陷"记成"固有边界"，本身就是一种把没做到写成结论**——判定为"边界"前先问：有没有一个低成本修法？有就是缺陷。本卡 F-019 第一轮误记为"非缺陷·两条边界"，被小审以三条理由驳回后升级处置。 | ready-for-review |
| L-007 | **禁词/敏感词扫描必须带边界**——裸子串 `dsh` 会命中 `han-dsh-ake`。本卡的小审和主会话自建的审计脚本首版**各自独立踩了同一个假阳性**，说明这是该类脚本的通病，不是偶然。 | ready-for-review |
| L-012 | **施工方自查与外部复核抓的是不同类型的问题，两者都不可省**。本卡实证：**唯一一条 P1（7 份 schema 编译不了）是施工方自查发现的**——因为只有施工方会去"真拿目标工具加载一次"；而 H6 fail-open、空集合真空真、指纹认不出修订版这三条**最重的语义缺陷全部来自外部复核**——因为施工方会不自觉地按"我想表达的意思"读自己写的条款，读不出字面上的洞。**别用"我自查过了"替代换人复核，也别用"复核会兜住"替代自查。** | ready-for-review |
| L-013 | **复核该在什么时候停：看它找到的是"实际存在的缺陷"还是"理论上可构造的形态"**。本卡 5 轮——前 4 轮找的是实际缺陷（编译不了 / fail-open / 真空真 / 指纹失明），第 5 轮起找的是当前产物里**不存在**的绕法。**后者出现即边际收益递减信号**，该记录成已知边界而不是继续加固。本卡由**复核者自己**给出该判据并建议停止，比施工方喊停更可信——值得作为复核派活时的固定提问：「你这轮找的，是已经存在的问题，还是理论上可以构造的问题？」 | ready-for-review |
| L-009 | **改 bug 的那一手，本身就是新 bug 的高发区——尤其"扩枚举/放宽条件"这类改动**。本卡两条不同线索各自回归一次：①D-19 的修法（把 4 档聚合扩成 6 档）引出 D-25（空集合真空真 → 从未跑过的 Run 报告成功）；②D-20 的修法（给白名单加自验）引出 D-27（自验只查 `$ref` 存在、不查目标闭合，同一毛病下沉一层）。**共同形态**：修的时候盯着"原来那个洞"，没盯住"新写的这几行有没有自己的边界条件"。**做法**：每次修完，把新加的条件**在极端输入上再过一遍**（空集合 / 缺省 / 单元素 / 全量），不要只验"原来那个 case 现在对了"。 | ready-for-review |
| L-010 | **"可验证的事实断言"不能当成"声明"处理——这是我在本卡犯了两次的同一类错**。①`CONDITIONALLY_NARROWED` 白名单声称"这里实际闭合"，脚本却一个字没验（D-20）；②补了自验之后，自验也只证明"这里有个 `$ref`"，没证明目标真的闭合（D-27）。**判据**：一条登记如果说的是"事实如何"（可被检验），就必须当场检验；只有说"我们承认/我们决定"（一个立场）时，白名单才是恰当形式。 | ready-for-review |
| L-011 | **连续多轮全盘接受复核意见后，要主动问一句"有没有修过头"**。本卡 5 轮复核、29 条 findings 全接全改，护栏维度从 5 项扩到 9 项——收益是真的，但**没有人在替施工方看"约束是否已超出本卡该管的范围"**。做法：在最后一轮明确请复核者回答该问题，并把轮次账与停止判据摆给用户，由用户裁决何时停。 | ready-for-review |
| L-008 | **文档里的"精确计数"改一次内容就要复算一次**——本卡先因拍脑袋写错计数（E-017 修正），修完后新增的表格里"说明"列恰好裸写了标记枚举的同名词，又把计数扫失真一次。凡自称"脚本精确统计"的数字，必须写明复算方式**并在每次改动后重跑**。 | ready-for-review |
| L-014 | **计划行只能标状态，不能改交付物本身**。批 3 把 visual_map 里「golden + negative **+ manifest**」改成「golden + negative」再标 100/present——不是决定不做，是**改写计划让未做的看起来做完了**；同一轮里派给复核者的必查题面也恰好把 manifest 那条换掉，于是计划 / 产物 / 复核题面三处同时消失。做法：交付物名是 task_plan 的，进度表只准动百分比与证据列；真要裁掉某项，用 `waived` 四态 + findings 留痕 + task_plan 划删除线，三样缺一不可。**派复核时的必查项，只能加不能换**——换掉一条就等于自己给自己免了一道闸。 | ready-for-review |
| L-015 | **一开始就问复核者「你找的是已存在的问题还是理论上可构造的问题」**。批 2 跑了 5 轮才由复核者自己提出停止判据（前 4 轮找实存缺陷、第 5 轮起找理论构造 = 边际收益递减）。批 3 把这句写进派出简报，复核者第一轮就逐条归类，10 条里 6 条是盘上已存在的事实、4 条是可构造——**结论"还没到停轮点"因此是有据的，不是感觉**。这比事后数轮次可靠。 | ready-for-review |
| L-016 | **「零 X」「只用 Y」这类全称结论，断言必须是白名单，不能是黑名单**。本卡验收口径第 2 条「不导入 DSH/Cordis/Pi/Herdr/DevHarness 私有类型」是典型全称命题，我做成了禁词表（黑名单），于是同一根因冒头两次：F-050 漏了 `pi`（五家缺一家，`pi_session_ref` 长驱直入而 `npm test` 全绿）、F-058 承认删掉 `cordis`/`devharness`/`psmux`/`subagent` 不会被拦。**这条是重蹈**——[[DHR_49 L-07]] 逐字写着同一句，且 **2026-08-20 22:19 就已进仓，比本卡批 1 早 12 分钟**。做法：①契约 / 配置这类**闭集**语料，白名单是可行的（本卡最终登记了 162 个结构位置 token）；②开工前扫一遍同仓其它卡的 `lesson_candidates.md`，别等 E5 来告诉你。 | ready-for-review |
| L-017 | **改判一个分类，要连它的计数一起改——否则宁可不改**。`compat-matrix.md` 把 `plan_hash → plan_digest` 标成「改名·语义等价」，而同仓 `CANONICALIZATION.md` 两处写着「摘要不可互认、按改型处理」，两份文档对同一行给出相反标记，直到 E14 一致性复核才被抓出。改它要动 §0 的五枚举计数（改名 5→4、改型 23→24），**这大概就是当初没改的原因**。做法：凡是「分类 + 计数」成对的表，改分类时把复算命令一起跑掉贴进证据；嫌麻烦而先放着 = 留下一处会打架的口径。与 [[L-008]]（精确计数每次改动要复算）是同一家族的下一层。 | ready-for-review |
| L-018 | **`approved` 是一次搜索的负结果，不是产物的一个属性；它的可信度 = 题面覆盖度 × 方法多样性，与轮数近乎无关**。本卡三个干净的数据点：①cp2 **五轮** approved（P0/P1 恒为 0）→ 批 3 的校验器一跑就抓出 F-043 与 F-048 的病灶——不是第六个复核者更聪明，是多了一种「真拿非法载荷去喂」的**方法**；②cp3 轮 2 approved、**发现清单为空** → E5 换起点抓出 F-062（`__proto__` 原型污染），因为它不在 cp3 那四条必查题的**任何一条底下**；③cp1/cp2/cp3 三轮批次小审全程干净 → E2 与 E14 **同时**撞在 G2 处置表上，因为三轮小审的题面都是「只看本批 diff」，**没有任何一轮的切口同时看得见「批 2 写的文档」和「批 2 之后改的 schema」**。**三条推论**：⑴判断能否收口要看**题面并集**、不是 approved 计数——该问「有没有哪一类问题这几路的题面都不会经过」，不是「够几轮了吗」；⑵新增一种**能力不同**的检查手段，比多派一轮同类复核值钱得多（本卡每次质变都来自新手段：读 schema → 写忠实求值器 → 跑真校验器喂载荷 → 穷举等价性 → 副本篡改红测 → 黑名单升白名单全称断言，**没有一次来自新轮次**）；⑶approved 之后再发现缺陷**不是复核失职、是题面边界的正常暴露**，该记的是「哪个题面漏了它」而不是「谁没看出来」——记成后者，下一张卡的反应会是「多派几轮」，而那恰恰是本卡数据证明无效的动作。**出处**：E2 代码复核轮 2 的方法学回答（主会话点名要它答的）。 | ready-for-review |

| L-019 | **写进 commit message 不算记录——修了什么必须有一条 finding**。本卡两批事实（测试在负载下假红、账本被三类工具性陷阱写坏）当时都在 commit message 里写清楚了，账本里一个字没有。后果是**可复现的**：E6 教训 miner 按指令专门搜这两类，`grep` 零命中判「证据不够具体，没抽」；E7 as-built 核对时直接报「这条我在盘上找不到对应事实」。**两个下游各自独立地漏掉了同一件事**——因为 commit message 不在 miner、as-built、复核者中任何一个的检索面里。做法：账本口径除了「每条结论挂一条可复跑的命令」，还要加一条「**每条被修的缺陷都要有一条 finding**」，否则「修了、commit 里写了、但账本里没有」是合法状态。与 [[F-073]]（证据挂在未入仓脚本上）同属「记录存在但检索不到」这一族。 | ready-for-review |
| L-020 | **工具会静默地做你没要求的事，而写账本的工具出错比被测代码出错更难发现**。本卡被三个坑各写坏账本一次：①`String.replace` 的**替换串**里 `` $` ``（美元号紧跟反引号）意为「插入匹配点之前的全部内容」——正文里一个 `` `…{64}$` `` 就把整张表复制了一遍；②CRLF 与裸 LF 混在一个文件里，让按行处理的工具算错行号、让 `dh` 只认表格前半段；③往 markdown 表里插一个空行会把它劈成两张表，解析器只读前半张。**共同点是「工具照它自己的规则做了对的事，只是不是你要的事」**——与本卡一路在堵的空绿同源，区别只在于它发生在**写记录的工具**上，而记录出错不会有任何闸报警。做法：①字符串替换一律用 replacer 函数（`s.replace(a, () => b)`），根除 `$` 转义面；②改完账本立刻复跑一次 `dh` 而不是等收口；③涉及行号的脚本先统一行尾再动。 | ready-for-review |
> 状态流：`ready-for-review`（AI 觉得可能重要）→ 人裁决 →`needs-promotion / promoted / rejected`
> `needs-promotion` 不阻塞收尾，但要留在升格队列可见。升格进正式教训库是**单独的维护动作**，要人批准（教训库 = 项目自己约定的位置；没有就留在本文件，不强求）。
