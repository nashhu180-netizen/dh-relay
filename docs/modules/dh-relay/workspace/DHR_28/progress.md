<!-- progress.md — 施工日志 + 证据账本。🟢 边做边记。这是"实际发生了啥"，容忍跑偏——路径偏了记这里，不回写任何计划文档。项目有 journal 时本文件代替 journal（不双写）。 -->
# progress — DHR_28

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|------|----|---------|------|--------|
| 2026-08-20 | 主会话(Opus) | 入口闸分流：档位=标准、目标/范围/验收/落点摆出；用户 AskUserQuestion 四轮点选——①worktree=「批次 1 主树，之后再开」②方向裁决=「ADR 草案摆给用户点选」③委托=「照节点表默认」④开工闸=「确认开工」 | 本次对话 AskUserQuestion 记录 | 建工作区八件套 |
| 2026-08-20 | 主会话(Opus) | S0 建工作区：拷 8 件套骨架到 `workspace/DHR_28/` | `ls docs/modules/dh-relay/workspace/DHR_28/` → 8 文件 | S1 brief |
| 2026-08-20 | 委托执行器(sonnet) | S1 brief 起草：完成条件表逐字复制自 DevPlan §3.2 DHR_28 验收口径 5 条 | `brief.md` | 主会话逐字审 |
| 2026-08-20 | 主会话(Opus) | S2 施工步骤：task_plan 写 3 批 16 步 + Context Packet C-001~C-010 + 关键决策；execution_strategy / visual_map 同批落定 | `task_plan.md` / `execution_strategy.md` / `visual_map.md` | 回填 DevPlan 状态，进批次 1 |
| 2026-08-20 | 主会话(Opus) | 回填 DevPlan §3.1 DHR_28 行（状态→进行中、工作区链接、分流要点）+ 刷 P5 `dh:status` 现状块 | `dev_plan/P5-…-开发方案.md` | 跑 `dh dh-relay` |
| 2026-08-20 | 主会话(Opus) | 跑 `dh dh-relay`：45 失败中**无一条属 DHR_28**；P5 计划的 5 条 R29（EVENT_STALE / EVIDENCE_STALE）经查是 HEAD `fd68621` 的存量（该提交只刷现状块 2 行、未动 planning-event 标记）→ 登记 F-001，不顺手改 | E-003 | 进批次 1 |
| 2026-08-20 | 主会话(Opus) | **批次 1 步1~2**：写 ADR-001（语言 Go/TS × 代码根 R-In/R-Out 四组合逐一评，P4 证据 E1~E11 每条带可复跑指针）+ ADR-002（Agent 宿主必答四问，第③问按契约层答并留 DHR_50 口） | E-001 / E-002 | 批次检查点 1 小审 |
| 2026-08-20 | 主会话(Opus) | **口径核实**：design/05 §17「design/04」把旧结论 `Go 已经锁定为最终语言` 明列为「以下结论由本文取代」，§6.3 定 P5 开工按证据选——确认 ADR-001 是真两选，不是追认既定结论 | E-004 | 同上 |
| 2026-08-20 | 主会话(Opus) | **批次 1 步4**：批次检查点 1 小审（fresh subagent `cp1-DHR28`，只读）→ changes-requested，P0/P1=0、P2×2、P3×5；抽验 6 条引用全部与原文相符 | e:E-006 | 逐条修 |
| 2026-08-20 | 主会话(Opus) | **返工收敛（批1-CP1）**：七条 P2/P3 **全接全改**（findings F-002~F-008 均 resolved）——修的核心是三处**会引导用户裁决的偏斜**（对照表换权威源致折扣只落 TS 侧、把两边都付的 SDK 成本只记在 Go 头上、代码根轴用「明显」下定性判断）；机器闸复跑全绿 | E-007 | 摆给用户裁决 |
| 2026-08-20 | 主会话(Opus) | **批次 1 步5 · 方向决策落定**：用户两轮对话点选——首轮「你建议用哪个？」+「我倾向于本仓，你觉得呢？」+ 次级「走冻结适配器」；主会话给明示建议与翻盘条件后，第二轮点选「**采纳，进批次 2**」。**决策 = TypeScript + 本仓 `relay-core/` + pi-agent 走冻结适配器** | ADR-001「决策」节（含四条建议依据 + 翻盘条件留痕）；E-008 | 批次 2 步6 建 `relay-core/` |
| 2026-08-20 | 主会话(Opus) | **批1 提交进主干**（用户对话确认「先提交批 1，再开树」）：按路径 stage（禁 `add -A`），index 事前确认干净；并行 session 已提交两笔 DHR_49（`ae4a8bc`/`554e543`），路径零重叠 | 提交 `a10e294`（11 文件） | 开树 |
| 2026-08-20 | 主会话(Opus) | **开 worktree**：`dh wt new DHR_28`。**踩坑并修**——该命令从 `origin/master`（`554e543`）切出，落后本地 master 一笔（正是批 1 `a10e294`），树里看不到批 1 产物；按 AGENTS 铁律「worker 进场第一动作自 rebase master」用 `git merge --ff-only master` 快进（非破坏性，wt 分支彼时零自有提交）。快进后树 HEAD=`a10e294`、批 1 九件产物到位 | E-009 | 批2 步6 |
| 2026-08-20 | 主会话(Opus) | **批次 2 步6**：建代码根 `relay-core/`（`adr/` `contracts/` `fixtures/{golden,negative}` `tools/`）+ `package.json`（type=module、node --test、engines>=18，对齐 P4 pilot 约定）+ `README.md`（含 6 条硬约束与目录归属表）；两份 ADR `git mv` 进 `relay-core/adr/`，workspace 侧空目录已删、指针见下方「ADR 落点说明」 | E-010 | 批2 步7 |
| 2026-08-20 | 主会话(Opus) | **批次 2 步7**：`contracts/v1-gap-disposition.md` —— P4 §4 六条缺口逐条处置，锚点 `v1-gap-disposition: G1`~`G6`。**六条全部采纳**（无一条"显式不采纳"）；G2 取"源头记 **且** 冻结聚合语义"（P4 原文给的是"或"，本卡取"且"并说明理由）；G5 从单字段扩到全域 locator；G6 升为跨 schema 建模原则 | E-012 | 步8 |
| 2026-08-20 | 主会话(Opus) | **批次 2 步8**：冻结 7 份正式 schema + 1 份共享定义模块 `_shared/relay.common.v1.schema.json`（locator 全域约束落这里）。**H6 契约层落地**：`relay.run/v2` 用 `if/then` + `contains`+`minContains` 使「必经角色只声明 `dsh-agent`」在 **schema 层**被拒（非运行时检查）——对应验收口径第 3 条 | E-013 | 步9 |
| 2026-08-20 | 主会话(Opus) | **批次 2 步9**：4 份 v0 形状落 `contracts/v0-shapes/`（`resolved-plan` / `host-observation` / `attention` / `approval`），各带 `x-freeze-status: v0-shape-only` + 首次承重阶段 | E-014 | 步10 |
| 2026-08-20 | 主会话(Opus) | **批次 2 步10**：`reason-codes.md`（23 码）+ `compat-matrix.md`。v1 侧**实读** `tools/contracts/` 全部五个文件后逐字段对照，**非凭记忆** | E-015 / E-016 | 批次检查点 2 |
| 2026-08-20 | 主会话(Opus) | **自查纠错（未经小审，主动发现）**：reason-codes 汇总句原写"共 20 个码"、compat-matrix §0 计数表原写 12/5/9/14/6——两处均为**拍脑袋估计**。脚本精确复算后改为 23 码与 2/5/22/10/4（合计 43），并把复算方式写进文档 | E-017 | 批次检查点 2 |
| 2026-08-20 | 主会话(Opus) | **自查纠错二（P1，未经小审，主动发现）**：用 ajv2020 实测编译，发现 7 份 schema **原本编译不了**——绝对 `$id` + 相对文件路径 `$ref` 按规范解析后指向错误 URI。全部改为按 `$id` 引用；修复过程中批量替换又在 4 份 v0 形状留下 `.https://` 坏引用，已二次修正 | E-020 / findings F-012 | 复跑探针 |
| 2026-08-20 | 主会话(Opus) | **H6 专项实测**：11 例探针 ALL PASS——「必经角色只声明 `dsh-agent`」确被 schema 层拒绝，`required` 缺省时不误触发；G1/G3/G4/G5 与未知字段 fail-closed 同批验证。附带发现 `date-time` format 空转（→ F-013，移交批 3） | E-021 | 批次检查点 2 结论 |

> **偏离记录（批2）**：①task_plan 步 9 原写验证命令 `rg -c "^status: v0-shape-only"` → 4，该 pattern 假定 v0 形状是 Markdown；实际落为 JSON Schema 文件，字段名改用 `"x-freeze-status": "v0-shape-only"`，验证命令随之改为 `grep -c '"x-freeze-status": "v0-shape-only"' v0-shapes/*.shape.json` → 4。**按纪律只记 progress，不回改 task_plan。** ②v0 形状与共享定义模块分别放 `contracts/v0-shapes/` 与 `contracts/_shared/`，以免污染「7 份已冻结协议」的计数——`ls contracts/*.schema.json` 恒为 7。
> **worktree 交接字段**：`worktree=D:\MyFiles\ai-workflow\dh-relay\.dh-worktrees\DHR_28`　`branch=wt/DHR_28`　`client=claude-code`　起点 `a10e294`（ff 快进后）。批 2/3 全部在此树内施工，收口 squash 回主干即删树。
| 2026-08-21 | 主会话 | **批次检查点 3 小审**（fresh subagent `cp3-DHR28`，Opus，只读，实例/会话 ≠ cp1/cp2）→ **changes-requested**：P1×2 / P2×3 / P3×5。冻结复检通过（审查全程 HEAD 停在 `f99363d`，`relay-core/` 全部文件 mtime 未变）。派出简报按教训 L-013 直接要求复核者答停轮判据 | e:E-036 | 逐条复验后返工 |
| 2026-08-21 | 主会话 | **两条 P1 主会话独立复验属实**（未直接采信）：①`find relay-core -iname "*manifest*"` → 0，且 `git show f99363d` 可见 visual_map 那行被改写成不含 manifest；②构造载荷实跑 `{"ok": true}` 两例放行 | E-041 前置 | 开修 |
| 2026-08-21 | 主会话 | **返工收敛（批3-CP3 轮1）**：**十条全接全改**（F-047~F-056 均 resolved）。核心三处：manifest 从"计划里有、产物里没有、复核题面里也没有"补齐并接进 `npm test`；七档聚合条款由散文升为 12 条 allOf 机器闸；未知版本的 reason 从 1/7 兑现改为 7/7 并二分「同名异版 vs 异名」 | E-037~E-042 | 复核者再过 |
| 2026-08-21 | 主会话 | **账本更正**：E-029 原结论超出证据（批 2 时无校验器可测非空形态），已就地标注证伪并回链 F-048，原文不删 | E-029（更正） | — |
| 2026-08-21 | 主会话 | **批3-CP3 轮2 = `approved`，发现清单为空** → 批次检查点 3 收敛，批 3 冻结。复核者对两条 P1 各做了独立验证（F-048 用 714 例穷举 + 24000 例随机证明 allOf 与条款**等价**；F-047 构造「让 selftest 全绿、只有 manifest 能抓」的篡改证明该闸有边际价值），并主动认下自己轮1 一处推理写错。四条留档项登记为 F-057~F-060 | E-043 | 进 E 收口段 |
| 2026-08-21 | 主会话 | **E 段四路复核全部回报，四路皆 `changes-requested`**：E2 代码轮2（1 P1 / 3 P2 / 3 P3）、E4 需求（2 P1 / 3 P2 / 3 P3）、E5 教训（2 条真重蹈）、E14 一致性（3 P1 / 3 P2 / 5 P3）。四路的硬指认主会话逐条复验属实，含 E14 顺手更正主会话派出简报里写错的两个数（`run_status` 是 6 值不是 7、`group` 是 4 值不是 5） | e:E-044~E-047 | 逐条收敛 |
| 2026-08-21 | 主会话 | **代码侧收敛并冻结**（提交 `a52cc47`，14 文件）：`__proto__` 修复 + 黑名单升白名单 + 聚合条款回归护栏 + capability 基线 + JSON-only 信封维度 + `progress` 改回计数对 + `labels` 采纳 | E-049 / E-050 / E-051 / E-052 | 文档侧 |
| 2026-08-21 | 主会话 | **文档侧收敛**（提交 `22f96df`，6 文件）：兼容矩阵与 CANONICALIZATION 的矛盾、G2 条款的两份旧副本、ADR-002 状态头与三条 `answer:` 行、OPEN-POINTS 的 K-1~K-4、`v1-gap-disposition` 的「DHR_30 开工前必读」段 | — | 账本侧 |
| 2026-08-21 | 主会话 | **过程失误如实登记**：E2 指出**本卡第三次「复核进行中改盘」**（前两次是 F-021/D-10，复核者查 mtime 发现；这次是主会话在 E2 审查期间跑 E5 的返工）。L-005 正是主会话自己写下的「派小审前必须冻结产物」——**自己写的教训自己又犯**。已改为：代码与文档两批各自跑完全闸后立即提交冻结，再派下一轮复核 | 本行 | 派 E2 轮 2 复检 |
| 2026-08-21 | 主会话 | **E2 轮 2 = `changes-requested`，但 P0=0 / P1=0**（P2×2 / P3×5）。复核者明确：产物本体这轮**零反例**（五种方法各跑一遍：聚合条款穷举等价、11 例 H6 对抗、7/7 版本二分、逐条删 6 条 allOf 全部真红、原型污染四角、三组中立性红测），七条**全部落在「声称的覆盖面 vs 实际覆盖面」这一条缝上**。冻结纪律通过：HEAD 全程 `85d5e4a`、`git status` 0 行、87 文件 mtime 逐行未变 | e:E-044 | 七条全改 |
| 2026-08-21 | 主会话 | **返工收敛（E2 轮 2）**：七条全接全改（F-075~F-081）。最重的 P2-1 主会话独立复验属实——篡改共享定义模块后 `capability_hash` 逐字未变，是 **F-033 缺口 A 下沉一层、本卡此类第五次**。另把 E2 的方法学回答记成 **L-018**（`approved` 是一次搜索的负结果，可信度 = 题面覆盖度 × 方法多样性，与轮数近乎无关） | E-053 / E-054 | E3 收敛 |
| 2026-08-21 | 主会话 | **进 E 收口段 · E0 机器体检**：`dh dh-relay` → 全仓 33 失败 / 31 警告，**属 DHR_28 的 8 条全在 `review.md`、全是待填占位**（第20行=E2 轮2 表空行；105~109=数据口径校验骨架，本卡非数据口径任务；115=风险放行账表空行；163=E11 用户确认行）。逐行看过，无一条是实质缺陷。仓级 3 条 `DESIGN_INPUTS_STRUCTURE_INVALID` 经主树对照确认为**存量**（→F-061）；R14「找不到 DevPlan 验收口径」warning 对 8 张卡全部命中，同属工具侧存量 | E-048 | E1 四道闸（须待 E8 填完完成条件后复跑） |

| 2026-08-21 | 主会话 | **E3 复核收敛**：五路结论归档进 review.md（第二轮增量复核表 + 一致性复核块五行逐条裁决 + 业务化五段标 N/A + 风险放行账表标无）；完成条件五条逐条挂证据（**条件 1 判「否」**——能力不匹配本卡未证）。`dh dh-relay` 的 DHR_28 失败项 **8 → 1**（剩的是 E11 确认记录行） | E-048 复跑 | E6 / E7 |
| 2026-08-21 | 委托(sonnet) | **E6 miner**：去重比对了候选区 1~10 + 同仓七张卡的 lesson_candidates + 本卡 L-001~L-018，**新抽 4 条**（候选-11~14），纯 append 零删除。⚠️ 捎回一条关键事实：**DHR_26 的 `__proto__` 教训从未被促升进候选区**——F-062 那个洞不是「教训在册却没查」，是**教训根本没进能被查到的地方** | `git diff --stat` 教训库-候选.md = 32 插入 / 0 删除 | — |
| 2026-08-21 | 委托(Opus) | **E7 as-built 首份建**：新建 `as-built/relay-core.md`（10 节，28KB），`relay-contracts.md` 加一句交叉指引。全部数字自己实跑取得、未抄文档。**核对时抓出 5 条与主会话描述不符的事实**（详见下） | `as-built/relay-core.md` | 逐条修 |
| 2026-08-21 | 主会话 | **E7 五条指认全部复验属实并修完**：①「假红」只在 commit message 里、没进 findings（→F-082/F-085）；②测试用例名「7 份契约」在 F-075 后已过时（→改 8 份，F-072 形态第三次）；③ADR-001 的**决策节标题**仍写「待用户裁决」（与 E4 在 ADR-002 抓的 P2-1 同一毛病的另一份文件）；④**brief 明写「as-built 首份快照归 DHR_29 起」，与主会话给 E7 的硬条对撞**——已裁决保留 as-built、改 brief 并留痕（→F-084）；⑤AGENTS.md 写「四份快照」，实为五份 | F-082~F-085 | E8/E9/E10 |
| 2026-08-21 | 主会话 | **E6 miner 回流已完成**：跑 `dh mine dh-relay DHR_28` 后派 `e6-DHR28`，**miner 备料产出 4 条教训候选**（候选-11~14）已 append 进 `knowledge/教训库-候选.md`，状态一律「待裁决」。去重比对了候选区 1~10 + 同仓七张卡 lesson_candidates + 本卡 L-001~L-018，纯 append 零删除 | `git diff --stat` 教训库-候选.md = 32 插入 / 0 删除 | — |
| 2026-08-21 | 主会话 | **E9 交付汇报已发出**（对话内七段）：七段覆盖：目标、范围、验收五条逐条结论、过程账（六路复核 60 余条发现全接全改）、机器闸终态、待用户裁决项、收口路径。完成条件 1 判「否」而非「部分达成」——只有「等价覆盖」满足原命题，**未把预期说成已确认** | 本行 | E10 |
| 2026-08-21 | 主会话 | **E10 人验证据展示区已发出**（对话内，AI 代跑并真实展示）：五道闸终态 `npm test` 10/10、`--selftest` pass=33 fail=0、`audit` 11 维 exit 0（`$ref` 84/84）、`manifest` 55 份、`capability-baseline` 8 份（`3ccf3b1043afbd82…`）；另同批展示**五组红测**证明闸真会红（删 6 条聚合 allOf / schema 加一个空格 / 退回 `__proto__` 修复 / 插 `acme_workspace_ref` / 把 `locator.pattern` 改 `^.*$`）。**本卡人验项 = 0**，无待人验池；E11 是授权动作非人判结果项 | 本行 | E11 |
> **阶段汇报@批次1**：2026-08-20 已在对话给七段 Style A 阶段汇报（③段双口吻：✅ E-001/E-002/E-004/E-005 已展示；⏳ 语言+代码根待用户裁决、批次检查点1小审进行中）。
> **跑偏/偏离记录**：无。批次 1 按 task_plan 步1~3 原样执行。
> **ADR 落点说明（已完成迁移）**：批次 1 的两份 ADR 起草于 `workspace/DHR_28/adr/`（主树，纯文档），批次 2 步 6 已随代码根落定 `git mv` 至 **`relay-core/adr/`**（保留 Git 重命名留痕，`git log --follow` 可追）。workspace 侧空目录已删、**不留双份正文**；正文唯一权威落点 = `relay-core/adr/ADR-001-runtime-language-and-code-root.md` 与 `relay-core/adr/ADR-002-agent-host-ownership.md`。
> **`<CODE_ROOT>` 占位符已落实**：= `relay-core/`（仓根下新顶层目录）。task_plan 批 2/3 步骤中的 `<CODE_ROOT>` 一律按此读，此后不再出现占位符。

## 证据账本 (Evidence Ledger)

<每条"完成"结论挂一条可复跑的命令 / grep / runtime 输出。不能空口说"做完了"。>
<类型枚举含 `review-dispatch`（派 agent 复核）/ `session-run`（主控本会话直跑复核）——大小写精确，只认这两个小写值。review 派出证据用 `dh dispatch` 一键落账；命令只向本标题下唯一、表头规范的账本写入，缺失/重复/畸形时 fail-closed，不追加孤儿行。>

| ID | 类型 | 命令 / 路径 | 结果 (pass/fail/observed/waived) | 支撑什么结论 |
|----|------|-----------|------|------|
| E-000 | command | `ls docs/modules/dh-relay/workspace/DHR_28/` | observed | S0 八件套骨架齐（8 文件） |
| E-001 | command | `cd workspace/DHR_28/adr && grep -c "^## " ADR-001-runtime-language-and-code-root.md` → `6`；`grep -n "待用户裁决（2026-08-20" ADR-001-*.md` → `111:> 待用户裁决（2026-08-20 分流：草案摆给用户点选）` | pass | 批1步1：ADR-001 六节结构齐；「决策」节未预填结论，明标待用户裁决 |
| E-002 | command | `cd workspace/DHR_28/adr && grep -c "^answer: " ADR-002-agent-host-ownership.md` → `4`；`grep -o "^answer: [a-z-]*" ADR-002-*.md` → `process-executor / pi-agent / dsh-native / herdr-agent` | pass | 批1步2：ADR-002 必答四问逐问有 `answer:` 锚点，无一问缺答 |
| E-003 | command | `git show --stat fd68621` → 1 file / 2 insertions·2 deletions；`git show fd68621 -- <P5计划> \| grep -c "dh:planning-event"` → `0` | observed | F-001 依据：P5 计划的 5 条 R29 失败是 HEAD 存量，非 DHR_28 引入 |
| E-004 | command | `sed -n '653,658p' design/05-DeepSeek-Harness插件化与专属工作台-可行性评估.md` → 「以下结论由本文取代」代码块含 `Go 已经锁定为最终语言`；`sed -n '207,219p'` → §6.3「P4 Pilot 完成前不锁定…P5 开工时根据证据选择」 | observed | ADR-001 前提成立：语言是真两选，design/04 旧结论已被 design/05 取代 |
| E-005 | command | ADR-001 证据指针抽验：`grep -n "零 bare import" workspace/DHR_26/findings.md` → 命中行 9 / 17 / 33 / 91（§9 取代注、§7 取代注、§12e、§17）；`grep -n "exists and is not a symlink" workspace/DHR_26/findings.md` → 行29 §12 | pass | ADR-001 的 E6/E7/E8 三条 Go 侧证据指针真实可复跑（非编造引用） |
| E-006 | review-dispatch | dh dispatch | observed | 复核派出：cp1-DHR28 · Opus subagent · fresh-context 只读实例（未参与批1施工，施工方=主会话 Opus）｜批次检查点1小审：只看批1 diff（ADR-001/ADR-002），四条必查=四问答满/P4证据指针抽验/是否预填结论/私有类型泄漏 |
| E-007 | command | 小审七条修完后复跑机器闸：`grep -c "^## " ADR-001-*.md`→`6`；`grep -c "^> 待用户裁决（2026-08-20" ADR-001-*.md`→`1`（修 F-002 时曾重复，已去重）；`grep -c "^answer: " ADR-002-*.md`→`4`；`grep -cE "待定\|后续再说\|TBD\|另行讨论" ADR-001-*.md ADR-002-*.md`→`0/0`；`grep -c "G 编号锁" ADR-002-*.md`→`1`；`grep -c "禁词表豁免注" ADR-002-*.md`→`1` | pass | 批1-CP1 返工收敛：七条 P2/P3 全改完且未改坏既有结构闸 |
| E-008 | command | 裁决回填后复跑：`grep -n "^> \*\*决策：" adr/ADR-001-*.md` → `122:> **决策：TypeScript + 本仓新顶层目录 relay-core/**（用户 2026-08-20 对话点选）`；`grep -c "^> 待用户裁决" adr/ADR-001-*.md` → `0`；`grep -c "^## " adr/ADR-001-*.md` → `6` | pass | 批1步5：方向决策已落定并留痕，占位行已清空，六节结构未破 |
| E-009 | command | `git rev-parse master origin/master` → `a10e294` vs `554e543`（不等，origin 落后）；`git -C .dh-worktrees/DHR_28 merge --ff-only master` → `Updating 554e543..a10e294 Fast-forward`；快进后 `git log --oneline -1` → `a10e294`；`ls docs/modules/dh-relay/workspace/DHR_28/` → 9 项齐 | pass | 开树基点错位已修：`dh wt new` 起于 origin/master 而非本地 master，ff 快进后树内含批 1 全部产物 |
| E-010 | command | `find relay-core -type f \| sort` → `adr/ADR-001…` `adr/ADR-002…` `package.json` `README.md`；`git status --short` → 两条 `R`（重命名）+ 两条 `??`，**无一条落在 DHR_49 或其它并行路径** | pass | 批2步6：代码根建成、ADR 迁移保留 Git 重命名留痕、路径隔离成立 |
| E-011 | command | `git check-ignore -v relay-core/.dh-relay/probe/x.txt` → `.gitignore:17:.dh-relay/`；`git check-ignore -v .dh-relay/probe/y.txt` → `.gitignore:17:.dh-relay/`（探针目录已删） | observed | F-009 依据：现行忽略模式为任意深度，覆盖仓根，严格宽于 DevPlan 要求的 `/.dh-relay/`；本卡不改 `.gitignore`，风险移交 DHR_29 |
| E-012 | command | `grep -c "^v1-gap-disposition: G[1-6]$" contracts/v1-gap-disposition.md` → `6`；`grep -o "^v1-gap-disposition: G[1-6]$"` → G1..G6 逐条在位 | pass | 批2步7：六条缺口逐条处置表落盘、锚点可 grep、无一条留空或"待定"（验收口径第 5 条） |
| E-013 | command | ~~原证据：`grep -c '"additionalProperties": false'` 逐份计数 →（全域覆盖，无遗漏对象）；`grep -noE "Cordis\|DevHarness\|psmux\|Subagent\|…"` → 零命中~~ ⚠️ **该证据方法已被批次检查点 2 小审 D-7 判为撑不住结论并作废**：①数出现次数**结构上无法证明"无遗漏"**，且确实漏掉了 4 处开口；②禁词表里**没有一个是实际会出现的 token**（真正在文件里的是 `dsh-agent`/`pi-agent`/`herdr-agent`），搜一批注定搜不到的词得零命中不构成证明。**结论本身（零泄漏）经小审独立验证是对的，但这条证据撑不住它。** 保留 `ls contracts/*.schema.json \| wc -l` → `7` 与逐份 `JSON.parse` 全 OK 两项（这两项方法有效）。**替代证据见 E-022** | waived | 方法作废、结论移交 E-022；此处留痕不删，避免"换了证据就当没发生过" |
| E-022 | test | `node tools/audit-contracts.mjs`（**AST 遍历替代 grep 计数**，本卡新建，可复跑）→ 扫描 12 份 schema/shape；闭合对象 **22**；`if`/`then` 内正确不计 **19**（在 `if` 里加 aP 会让 `if` 恒不匹配、在 `then` 里加会把实例判死——小审已逐个确认写法正确）；被 `allOf`/`if-then` 条件收窄 **1**；有意开放点 **3**（与 `OPEN-POINTS.md` 逐条对齐）；**未登记开口 0**；**结构位置的非白名单厂商 token 0**；退出码 **0** | pass | 批2步8 结论重挂：fail-closed 覆盖面**可复算**（不是数 grep）、私有类型零泄漏**用结构位置扫描**（剥掉 description/$comment 等散文键，只查字段名/enum/const，且词表带边界以免 `han-dsh-ake` 假阳性）。替代作废的 E-013 |
| E-023 | test | D-11 修复前后对照（`node probe.mjs`）：**改前**「`required` 缺省 + `executor_profiles` 只有 `dsh-agent`」→ **ACCEPT**（H6 未触发，fail-open）；**改后**（`$defs/node` 的 `required` 列表加入 `"required"`）→ **REJECT**；同时「`required:false` 显式 + 只 `dsh-agent`」仍 → **ACCEPT**（语义未变，标 false 照样不触发） | pass | **验收口径第 3 条的真实成立性**：H6 不再 fail-open by default。这是 cp2 小审第二轮的核心发现，主会话原探针未覆盖 ⚠️ **探针 `probe.mjs` / `rs.mjs` 是一次性脚本、从未入仓**（E2 代码复核轮 2 · P3-2 抓出：`git log --all --diff-filter=A` 对本卡零命中），照本行复跑会得到 file-not-found。这是**证据可复跑性**问题、不是结论真伪问题——结论已被批 3 的 committed 反例承接：同 E-021 的 H6 两形态；D-11 的「不标 `required` 也被拒」由 `h6-required-omitted` 承接。 |
| E-024 | test | `node -e` 跑 `relay.common/v1` 新增的 RFC3339 `pattern` 共 11 例：放行 `2026-08-20T10:00:00Z` / `+08:00` 偏移 / 小数秒 `.4585104` / 小写 `t`&`z`；拒绝 `2026-13-99T10:00:00Z` / `2026-00-10T…` / `2026-08-20T25:00:00Z` / 无时区 / 空格分隔 / 纯日期 / `not-a-time`。**ALL PASS** | pass | D-18/F-024：时间戳约束不再依赖 `format`（2020-12 规范默认只作注解、不做断言），改由断言性关键字 `pattern` 兜底，任何独立校验器都必须执行——满足验收口径第 1 条的"**独立**校验器"要求 |
| E-032 | test | **批 3 步12 跑红（TDD）**：`ls tools/validate.mjs` → `No such file`；`npm test` → `pass 0 / fail 0` **退出 0**（正是 F-020 记的"空绿"）。fixture 先落：**10 份 golden**（7 份已冻结协议各至少一份 + RPC 三种帧）+ **21 份 negative**（每份配写死期望 reason code 的 `.expect.json`，覆盖 fail-closed 三条 / H6 两形态 / G5 三形态 / G1·G3·G4 / D-25 两形态 / G2 两映射 / G6 两条 / RPC 两条） | observed | TDD 红相成立：校验器不存在、测试为空绿 |
| E-033 | test | **批 3 步13 转绿**：`node tools/validate.mjs --selftest` → **golden 10/10 PASS + negative 21/21 逐条命中写死的 reason code，合计 pass=31 fail=0，退出码 0**。中途 7 条红逐条归因见 F-043~F-046（1 条契约真缺口 + 3 条校验器缺陷 + 3 条**我自己的反例期望写错**——诚实改期望而非改校验器去迁就） | pass | **验收口径第 1 条兑现**：协议正反 fixture 可由独立校验器验证；未知字段 / 未知版本 / 能力指纹形态 三类反例逐条被拒 |
| E-034 | test | **批 3 步14 中立性静态断言**：新建 `tools/forbidden-types.txt`（禁词从文件读、**不硬编码**，避免两处漂移；含"只扫结构位置"与"匹配必须带边界"两条口径说明）。**红绿对照**——正向 `node tools/audit-contracts.mjs` → 厂商 token **0**、退出码 **0**；负向（scratchpad 副本内往 `$defs.node.properties` 插入 `cordis_hint`）→ 报「结构位置的非白名单厂商 token: 1 … `cordis_hint`」、退出码 **1**。副本已删 | pass | **验收口径第 2 条**第一合取项**兑现**：协议不导入私有类型，且该断言**经负向验证真的会 fail**（不是只在正常路径打绿勾） ⚠️ **本行原结论写的是「验收口径第 2 条兑现」，已按 E2 代码复核轮 2 · P2-2 改准**（findings F-067）：口径 2 是**两个合取项**，本行的证据只覆盖第一项（不导入私有类型），第二项「DSH 与 Runtime 不共享活动对象或内存状态」当时**零证据、零断言**。**与 E-029 完全同形**——证据只覆盖一部分，结论写了整条。第二合取项现由审计器第 11 维承接（E-049）。 |
| E-035 | test | `npm test` 接入三道机器闸：**5/5 pass、退出码 0**（校验器 selftest / fail-closed 反例齐备 / H6 反例齐备 / 审计 9 维度全绿 / 中立性零泄漏）。测试内**断言 fixture 份数下限**（golden ≥10、negative ≥20），杜绝将来测试被删光又变空绿。`package.json` 的 `test` 由 `node --test`（会扫 `node_modules`、实测挂死）改为指定文件 | pass | **F-020 闭合**：`npm test` 不再是空绿，且空绿这条路被份数下限断言堵死 |
| E-029 | test | D-25 修复前后对照（`node rs.mjs`）：**改前** `{run_status:'succeeded', group:'done', node_states:[]}` → **ACCEPT**，`node_states` 字段整个缺省 → 同样 **ACCEPT**（`required` 不含它、无 `minItems`）；**改后**两者均 **REJECT**，而「至少一个节点的正常 succeeded」仍 ACCEPT。全表 **17/17 ALL PASS** | pass | F-032：**空集合**上的真空真已堵死；双重加固（条款第⑦档 + `minItems:1` 必填）各自独立生效。⚠️ **本行原写「『从未跑过的 Run 报告成功完成』这个协议能给出的最坏答案不再可能」——该结论超出证据，已由批次检查点 3 小审 P1-2 证伪**（findings F-048）：当时堵住的只是「节点集合为空」这一种形态，而**非空**版本（`run_status=succeeded` + 两个节点全 `pending`）在批 3 的校验器上实测**被放行**。原因是七档聚合条款当时只写在 description 里、零机器闸，而批 2 阶段还没有校验器可以测它。F-048 已补齐条款①~⑥的 allOf 机器强制（E-041）。留此更正不删原文，是因为「批 2 的证据当时支持不了那句结论」本身是本卡的教训 ⚠️ **探针 `probe.mjs` / `rs.mjs` 是一次性脚本、从未入仓**（E2 代码复核轮 2 · P3-2 抓出：`git log --all --diff-filter=A` 对本卡零命中），照本行复跑会得到 file-not-found。这是**证据可复跑性**问题、不是结论真伪问题——结论已被批 3 的 committed 反例承接：`d25-empty-node-states` + `d25-node-states-omitted` 两份 committed 反例；且本行原结论已被 F-048 证伪并就地更正（见下）。 |
| E-030 | test | 审计器四条新护栏**逐条负向验证**（scratchpad 副本内，盘上文件未动，已清理）：**D-27** 把两分支 `$ref` 都指向 `additionalProperties:true` 的目标 → 报「分支 `$ref` 的目标不是闭合的」exit 1；**D-29** 放一份 `relay.sneaky.v1.json` → 报「不符命名约定…会完全逃过审计」exit 1；**D-30** 删掉 `OPEN-POINTS.md` 一条登记 → 报「代码白名单与文档不同步」exit 1；**D-31** 把 `minItems: 1` 改成 `"1"` → 报「关键字类型应为 number 实为 string——多数校验器会静默忽略」exit 1。**四条全部真的 fail** | pass | F-034 / F-035：新护栏不是只在正常路径打绿勾；尤其 D-27 精确复现了小审那个"指向开着的目标"的骗法并被抓住 |
| E-031 | command | 全部机器闸终态复跑：`probe.mjs` **13/13**；`rs.mjs` **17/17**（含 3 例 D-25）；`tools/audit-contracts.mjs` → 22 闭合 / 29 守卫不计 / 1 条件收窄（**自验含目标闭合断言**）/ 3 有意开放 / 0 未登记 / 0 泄漏 / **85 条 `$ref` 实解析 0 失败** / 0 自验失败 / **0 命名违规** / **0 meta-schema 类型错** / **0 白名单↔文档不同步**，退出码 **0** | pass | 批 2 终态全绿，且护栏维度从 5 项扩到 9 项 ⚠️ **探针 `probe.mjs` / `rs.mjs` 是一次性脚本、从未入仓**（E2 代码复核轮 2 · P3-2 抓出：`git log --all --diff-filter=A` 对本卡零命中），照本行复跑会得到 file-not-found。这是**证据可复跑性**问题、不是结论真伪问题——结论已被批 3 的 committed 反例承接：全部机器闸已由 `npm test` 10/10 + `--selftest` pass=33 + `audit` 11 维度 + `manifest` + `capability-baseline` 五条 committed 命令承接。 |
| E-028 | command | 工作树内 `dh dh-relay` 过滤 DHR_28：修掉两条**本卡引入**的失败——①`visual_map` R6 证据状态列写成 `partial（…说明…）`，枚举必须是四态之一，已改为裸 `partial` 并把说明移到表下注记；②`review` R12 需求对齐证据三行原挂 `<收口填>`，现已有真证据，改挂 E-020/E-021/E-023/E-024/E-025/E-027 并按实况写「待人验」+ 已证/未证边界（未知版本与能力不匹配属 RPC 运行期语义，契约层只定形态与 reason code，实证归 DHR_29/30）。**剩余项均为收口阶段才填的占位**：R2 的 AI 提交区元数据表与确认记录表、R27 的一致性复核（E14）派出锚点；R14「找不到 DevPlan 验收口径」是**全仓所有卡通有**的解析器限制（DHR_01~49 同样命中），非本卡引入 | observed | 机器体检里属于 DHR_28 且**当前该修**的项已清零；未清项已逐条归因为"收口阶段填"或"存量解析器限制"，不留模糊 |
| E-026 | test | D-19 修复后 `node rs.mjs` 跑 `relay.run-state/v1` **14 例全过**：全 `pending` 新 Run → `pending`+`running`（**不再误报警**）；`[succeeded,pending]` 节点间空档 → 同上；`orphaned` 归 failed 侧；真 `unknown` 仍浮顶 `needs_you`；6 条 `run_status→group` 映射逐条正反验（误归一律 REJECT，含"`pending` 误归 `needs_you`"这个原病灶） | pass | F-025：G2 聚合条款覆盖全部 7 个节点态、映射全部机器强制；G2「不认识的状态必须让人看见」的设计意义恢复（不再被 pending 噪音淹没） ⚠️ **探针 `probe.mjs` / `rs.mjs` 是一次性脚本、从未入仓**（E2 代码复核轮 2 · P3-2 抓出：`git log --all --diff-filter=A` 对本卡零命中），照本行复跑会得到 file-not-found。这是**证据可复跑性**问题、不是结论真伪问题——结论已被批 3 的 committed 反例承接：⚠️ **等价覆盖不完整**：6 条 `run_status→group` 映射只有 2 条有 committed 反例（`g2-pending-must-not-alarm` / `g2-unknown-must-float`）。另 4 条的正向面由 F-066 新增的 14 例矩阵用例覆盖（`npm test` 内），逐条删 `allOf` 实测真红。 |
| E-027 | test | 审计器加固后**正向 + 负向**：正向 `node tools/audit-contracts.mjs` → 22 闭合 / 29 if-then 不计 / 1 条件收窄（**自验通过**）/ 3 有意开放 / 0 未登记 / 0 泄漏 / **85 条 `$ref` 实解析 0 失败**（`$id` 注册表 12 项）/ 0 条件收窄自验失败，退出码 **0**。**负向①**（scratchpad 副本内复现 F-012，把一条 `$ref` 改回相对路径）→ 报「base 未命中任何已注册 `$id`」，退出码 **1**。**负向②**（复现 D-17，往 `notification.method` 枚举加 `heartbeat` 不加分支）→ 报「判别 enum 有 1 个取值没有对应分支：heartbeat」，退出码 **1**。副本已删 | pass | F-027 / F-028：本卡唯一那条 P1（`$ref` 不可解析）与"加枚举忘加分支"两类回归**现在都有机器护栏**，且护栏经负向验证**真的会 fail**——不是只在正常路径上打绿勾 |
| E-025 | test | 全部机器闸复跑（`required` 改必填 + `timestamp` 加 pattern 之后）：`node probe.mjs` **13/13 ALL PASS**（H6 四组合 + G1/G3/G4/G5 + 未知字段 fail-closed + 非法时间戳）；`node tools/audit-contracts.mjs` 退出码 **0** | pass | 两处改动未打破既有约束；批 2 终态机器闸全绿 ⚠️ **探针 `probe.mjs` / `rs.mjs` 是一次性脚本、从未入仓**（E2 代码复核轮 2 · P3-2 抓出：`git log --all --diff-filter=A` 对本卡零命中），照本行复跑会得到 file-not-found。这是**证据可复跑性**问题、不是结论真伪问题——结论已被批 3 的 committed 反例承接：时间戳两形态（`bad-timestamp` / `timestamp-no-timezone`）+ G5 三形态，均在 `--selftest` 内。 |
| E-014 | command | `ls contracts/v0-shapes/*.shape.json \| wc -l` → `4`；`grep -c '"x-freeze-status": "v0-shape-only"' v0-shapes/*.shape.json` → 四份各 1；逐份 JSON.parse → 全 OK；`ls contracts/*.schema.json \| wc -l` 仍为 `7` | pass | 批2步9：4 份 v0 形状只定形状未冻结，且未污染已冻结协议计数 |
| E-015 | command | `grep -oE '\bE_[A-Z][A-Z0-9_]+\b' contracts/reason-codes.md \| sort -u \| wc -l` → `23`（≥5 达标） | pass | 批2步10：reason code 表覆盖 fail-closed 三类 + start 前置 + 结构 + 幂等冲突 + Executor 生命周期 + RPC |
| E-016 | command | v1 侧实读：`cat tools/contracts/{README.md,relay-params.psd1,transition-matrix.json}` + `sed -n '1,260p' relay-schema.ps1`。据此逐字段建表，含实测事实——v1 `attempt_id` 是**正整数**（`Test-RelayPositiveInteger`）而 v2 为字符串；v1 `interruption_reason` 已有 `host_lost`；v1 `transition-matrix.json` **已把会话观测（terminal_state 6 态）与任务结果（result_status 6 态）分成两个维度** | observed | 批2步10：兼容矩阵 v1 侧来自实读而非记忆；并据此把 G6 的表述精确化为「v1 分开了但命名误导 + 缺 run 级」，而非「v1 没分开」 |
| E-017 | command | 精确复算：reason code 去重 → `23`（原文档写 20）；compat-matrix 按表格第 3 列脚本统计 → 保留2/改名5/改型22/新增10/弃用4，合计 43（原文档写 12/5/9/14/6）。两处已改为实测值并在文档内写明复算方式 | pass | 自查纠错：契约文档内两处估计数已消除，可被独立复算 |
| E-018 | review-dispatch | dh dispatch | observed | 复核派出：cp2-DHR28 · Opus subagent · fresh-context 只读实例（未参与批2施工，施工方=主会话 Opus；实例/会话≠cp1-DHR28）｜批次检查点2小审：只看批2 diff（relay-core/ 全部产物），五条必查=六条缺口对字面/additionalProperties全域/私有类型泄漏/兼容矩阵v1侧对得上tools-contracts/有无把待定写成结论 |
| E-019 | test | `node -e` 用 schema 里的 locator pattern 跑 13 例（6 个应拒：`C:\Users\nash\x.md` / `c:/Users/x.md` / `/etc/passwd` / UNC / `file:///C:/x` / `http://…`；7 个应放：`briefs/fix.md` / `./x` / `../x` / `.dh-relay/RUN-1/log.txt` / `a:b` / `node_modules/.bin/x` / `logs/run 1/out.log`） → **12/13 符合预期，唯 `a:b` 被误拒** | pass | G5 的 locator 约束对六类绝对形态全部生效；唯一 over-rejection 已判定为有意从严并写进 schema description（findings F-011） |
| E-020 | test | `npm i ajv@8 --prefer-offline`（scratchpad，未污染 relay-core）→ 用 ajv2020 编译 `relay.run.v2.schema.json`。**首跑 `MissingRefError`**（见 findings F-012）→ 全部 `$ref` 改按 `$id` 引用后重编译成功。终态审计：`grep -c '\.https://'` → 零残留；`grep -ohE '"\$ref": "[^"]+"' *.schema.json v0-shapes/*.shape.json | sort | uniq -c` → 外部引用全部指向 `https://dh-relay.local/contracts/relay.common/v1#/$defs/…` | pass | 7 份 schema 与 4 份 v0 形状**可被独立校验器真实编译**（此前不能）；跨文件 `$ref` 解析口径统一 |
| E-021 | test | `node probe.mjs` 11 例逐条：H6「必经角色只声明 dsh-agent」→ **拒**；「dsh-agent + process」→ 放；「非必经只 dsh-agent」→ 放；「`required` 缺省 + 只 dsh-agent」→ 放（缺省=false 不误触发）；未知字段 → 拒；`trigger=human` 缺 `trigger_by` → 拒；有 `trigger_by` → 放；节点缺 `title` → 拒；locator 绝对路径 → 拒；`attempt=null` → 放；`attempt=0` → 拒。**ALL PASS 11/11**。附带发现警告 `unknown format "date-time" ignored`（→ findings F-013） | pass | **验收口径第 3 条（H6 契约级 schema 层拒绝）实测成立**；G1/G3/G4/G5 的 schema 约束同批实测生效；fail-closed 未知字段实测生效 ⚠️ **探针 `probe.mjs` / `rs.mjs` 是一次性脚本、从未入仓**（E2 代码复核轮 2 · P3-2 抓出：`git log --all --diff-filter=A` 对本卡零命中），照本行复跑会得到 file-not-found。这是**证据可复跑性**问题、不是结论真伪问题——结论已被批 3 的 committed 反例承接：H6 两形态（`h6-dsh-only-required-role` / `h6-required-omitted`）+ G1（`g1-human-trigger-without-by`）+ G3（`g3-node-missing-title`）+ G4（`g4-attempt-zero`）+ G5 三形态 + 未知字段（`unknown-field`），均在 `--selftest` 内逐条钉住。 |
| E-036 | review-dispatch | dh dispatch | observed | 复核派出：cp3-DHR28 · Opus subagent · fresh-context 只读实例（未参与批3施工；实例/会话 != cp1-DHR28 且 != cp2-DHR28）｜批次检查点3小审：只看批3 diff（提交 f99363d）。产物已冻结于该提交，审查期间不改盘。四条必查=反例真能被拒(抽验>=2手工复跑)/校验器对未知版本有无宽容放行/fixture 期望是否被改来迁就校验器/校验器是否偷引 Runtime 或工作台代码 |
| E-037 | test | `node tools/fixture-manifest.mjs` → `manifest 对证通过：55 份逐份 digest 相符（golden 11 / negative 载荷 22 / expect 22）`，exit 0。**篡改红测**：把 `g5-absolute-locator-drive.json` 的 3 处 `ref` 改软 → `exit 1` 并指名该文件 digest 差异（`63cec261f302…` → `c526c8db9cbc…`）；同时把其 `.expect.json` 的 reason 也改掉 → 两份都被指名。删掉任一反例 → 报「manifest 里有、盘上没有」+ 三条 counts 不等 + 孤儿 expect | pass | F-047：fixture 基线对证成立——「盘上这批还是过审时那批」现在可机器证明，DHR_29/30 的契约回归有了防漂移锚 |
| E-038 | test | `node tools/validate.mjs --selftest` → **合计 pass=33 fail=0**，退出码 0。每条 negative 打印 `期望=<code>@<at> 实得=<code>@<at>`。**位置断言红测**：把 `g2-pending-must-not-alarm.expect.json` 的 `at` 从 `/group` 改成 `/run_status`（reason code 不变）→ 该条 `FAIL`，合计 pass=32 fail=1 | pass | F-052：`.expect.json` 现在钉 reason **与位置**，「换个地方错、码碰巧一样」不再能蒙混过关 |
| E-039 | test | 中立性红绿四组：①往 `relay.run.v2.schema.json` 的 `properties` 插 `pi_session_ref` → `exit 1`，报 `properties.<key> = pi_session_ref`（**修前 exit 0**）；②从禁词表删掉 `dsh` → `exit 2`，报「`dsh-agent` 不再被命中，白名单成死代码 + 该厂商的中立性检查已静默失效」（**修前 exit 0**）；③假阳性回归：`handshake` / `api` / `mapping` / `pipeline` / `capability` / `shipping` / `epic` / `topic` 全部**不命中**；④还原后 `exit 0` | pass | F-050 + F-051：验收口径第 2 条对五家厂商齐了；禁词表与白名单之间从「靠注释自觉」升为机器强制 |
| E-040 | test | `npm test` → **7/7 通过**（新增「fixture 基线对证」与「JCS 规范化实现符合 CANONICALIZATION.md 口径」两条）。份数断言由 `>= 20` 改为**与盘上实际文件数相等**并现算基准 | pass | F-053：份数闸不再松一格；F-047 的 manifest 与 JCS 口径都进了 `npm test` 这一层 |
| E-041 | test | run-state 七档聚合条款机器化后的 **14 例红绿矩阵**：7 例应 PASS（failed+[failed,succeeded] / failed+[orphaned,running] / running+[running,pending] / waiting_human / unknown / pending / succeeded+全succeeded）与 7 例应 REJECT（running+[failed,…] / running+[succeeded,pending] / waiting_human+[running,…] / unknown+[waiting_human,…] / pending+[unknown,…] / failed+[succeeded,pending] / succeeded+[succeeded,pending]）**全部符合预期**；golden `run-state.v1.json` 仍 PASS；小审给的两份放行载荷现均 REJECT。allOf 由 6 条增至 12 条 | pass | F-048：G2 的规范性聚合条款从「写在 description 里」升为机器强制；E-029 那句被证伪的结论对应的洞已真正关闭 |
| E-042 | test | 判别字段二分实测：`relay.run/v2` 收 `relay.run/v3` → `E_UNSUPPORTED_VERSION`；`relay.event/v2` 收 `relay.event/v9` → `E_UNSUPPORTED_VERSION`（**修前两者均为 `E_BAD_VALUE`**）；`relay.run/v2` 收 `relay.event/v2`（异名）→ `E_BAD_VALUE`；rpc 握手 `v9` → `E_UNSUPPORTED_VERSION`（不变）；rpc 交叉载荷 → `E_BAD_VALUE`（不变）。G5 判据换 `$ref` 目标后红测：把 locator pattern 改写成语义等价（8 探针逐一对照）但两段特征串都不在的形式 → 旧判据退化成 `E_BAD_VALUE`、新判据仍 `E_ABSOLUTE_LOCATOR` | pass | F-049 + F-056：未知版本的 reason 由 1/7 兑现改为 7/7 且区分「版本太新」与「发错协议」；G5 判据不再挂在别处一段正则的字符怎么排 |
| E-043 | review-dispatch | cp3-DHR28 轮2（同一派出，承 E-036；返工收敛回合，经 SendMessage 唤醒原复核者上下文） | pass | **轮2 结论 = `approved`，发现清单为空**。复核者自述「不是因为没找，是找了没有」。关键复验：①F-048 **714 例穷举**（大小 1~3 全部节点态组合 × 6 个 run_status）over-rejection **0** / under-rejection **0**，另随机 n=4~6 共 **24000 例**不符 **0** ⇒ 新增 allOf 与规范条款**等价**；②F-047 构造 selftest 全绿而仅 manifest 报红的篡改 ⇒ 该闸的边际价值实证到位；③空绿红测五组全部真红；④CRLF/缩进/末尾换行三种无语义差异同时上齐，manifest 对证仍 exit 0 ⇒ 选 JCS 而非文件字节哈希成立。**只读纪律**：篡改全部在 scratchpad 副本内进行，盘上零字节改动；冻结复检 HEAD 全程 `15dccc2`、`git status` 全空、mtime 逐行未变 |
| E-044 | review-dispatch | dh dispatch | observed | 复核派出：e2-DHR28 · Opus subagent · fresh-context 只读实例（实例/会话 != cp1/cp2/cp3-DHR28，未参与任何施工）｜E2 代码复核轮2（硬闸）：核全程 + 核三批小审记录 + 查收口增量 diff。范围=relay-core/ 全部产物 + workspace/DHR_28 八件套。四条必查=三批小审的结论是否被如实执行/验收口径5条是否真有机器证/施工方有没有把没做到的写成结论/跨批次是否有前批结论被后批证伪而未更正 |
| E-045 | review-dispatch | dh dispatch | observed | 复核派出：e4-DHR28 · Opus subagent · 需求视角｜E4 需求复核：对回 brief/DevPlan 验收口径5条/需求对齐证据，查范围漂移与结果失真。重点=交付的东西是不是 DevPlan 要的那个，有没有做多做少 |
| E-046 | review-dispatch | dh dispatch | observed | 复核派出：e5-DHR28 · Sonnet subagent · 扫教训库｜E5 教训复核：扫本仓已有教训库与 DHR_01~27/49 的 lesson_candidates，查本卡有无重蹈在册教训；在册必过，命中重蹈即返工 |
| E-047 | review-dispatch | dh dispatch | observed | 复核派出：e14-DHR28 · Opus subagent · 横向比对｜E14 一致性复核：只扫本次碰到的东西在别处有没有兄弟。五条登记项=v2状态机与终态枚举/v2身份链与receipt字段/v2 redaction与白名单口径/Read Model字段级起点/Store根与.gitignore前置。结论全部一致也要列比对清单，不一致须逐条裁决有意差异还是遗漏 |
| E-048 | command | `dh dh-relay`（工作树内跑）→ `合计: 33 失败, 31 警告`；筛 DHR_28 → **8 条失败全在 `review.md`**：`R2 第20行是纯空表格行` / `R2 第105~109行残留占位 {…}` / `R2 第115行残留占位 {…}` / `R2 第163行是纯空表格行`。逐行 `sed` 看过，对应=E2 轮2 表 / 数据口径校验骨架 / 风险放行账表 / E11 确认记录，**均为 E 段待填位，非缺陷**。仓级对照：主树同跑 `合计: 44 失败`，`DESIGN_INPUTS_STRUCTURE_INVALID` 在主树同样 3 条命中 ⇒ 非本工作树引入 | observed | E0 机器体检结论：DHR_28 无新增实质失败；8 条占位由 E2/E8/E9/E10/E11 逐一填掉后应归零，收口前须复跑本命令核对 |
| E-049 | test | **审计器 9→11 维度**。新增①「JSON-only 信封 + 跨边界引用」：`type` 取值只能是 JSON 原生类型 ∧ 每个 `ref` / `*_ref` / `locator` / `path` 字段必须 `$ref` 到共享 `relay.common/v1#/$defs/locator`（实扫 4 个此类字段全部合规）；②「结构位置 token 白名单」：`structural-tokens.txt` 登记 162 项，未登记 / 陈旧登记 / 清单缺失三种情形各自 exit 1。**红绿六组**：插 `pi_session_ref` → exit 1（修前 exit 0）；插禁词表里根本没有的 `acme_workspace_ref` → exit 1（**黑名单必漏、白名单抓住**）；清空禁词表后插 `cordis_hint` → 白名单层仍拦（F-058 的固有边界随之消失）；删禁词表 `dsh` → exit 2；清单加一条不存在的 token → exit 1；把 `executor_profile.ref` 的 `$ref` 换成内联 pattern → exit 1。假阳性回归：`handshake` / `api` / `mapping` / `pipeline` / `capability` / `shipping` / `epic` / `topic` 全部不命中 | pass | F-063（口径 2 第一合取项由黑名单升为白名单，重蹈 DHR_49 L-07 已闭）+ F-067（口径 2 **第二合取项**首次有了机器证）+ F-056 残留（内联 locator pattern 的形态被这一维直接禁掉） |
| E-050 | test | **capability 基线**：`node tools/capability-baseline.mjs --write` → 7 份协议 / 7 methods / 2 notifications / `executor_kinds=[process]`，`capability_hash = 3ccf3b1043afbd8224dd2eadf61c80b29ab951d61db8b838fc3a58b97f58e4d3（⚠️ 该值在 E2 轮 2 · P2-1 把共享定义模块纳入指纹后由 `5c5685d0…` 变更，变更本身即正确——它确实是一次能力变更）`；默认对证 exit 0。**红测**：只在 `relay.event.v2.schema.json` 的顶层 `description` 末尾加**一个空格** → exit 1，同时报「capability_hash 不符」与「协议 relay.event/v2 的 digest 变了——这是一次能力变更」 | pass | F-065：`CANONICALIZATION.md` 写进批 3 交付项的「可复算固定 digest 回归基线」已兑现；同时关掉 E2 的 P3-3——此前 manifest 钉 55 份 fixture 而 `contracts/` 一份没钉，**而 schema 才是本卡真正冻结的工件**。「改一个错别字也是能力变更、也会断握手」现在是机器可证的 |
| E-051 | test | **聚合条款 allOf 的回归护栏**（F-066）：新增 `npm test` 用例，照 description 的①~⑦档写 14 例矩阵（7 应过 / 7 应拒）。逐条删 `allOf[6..11]` 复验——**六条全部真红**（修前四道闸全绿）。该用例同时钉住 `allOf` 与条款正文的对应关系：谁改条款不改 `allOf`（或反过来）都会红 | pass | F-066：本卡第四次「修法本身下沉一层」已闭。前三次 = F-043→F-049、D-19→D-25、D-20→D-27 |
| E-052 | test | **`__proto__` 原型污染回归**（F-062）：`digestExcluding(evil,[])` 与 `digestExcluding(plain,[])` 修前**相同**（两份不同载荷同一摘要）、修后不同；另验 evil≠evil2、排除空集时与 `digest()` 等价、排除功能未坏、`constructor`/`toString` 同名键保住。红测：把 `Object.create(null)` 退回 `{}` → 该用例立刻红 | pass | F-062：五个内容指纹字段里四个的实现被修复。**cp1/cp2/cp3 四轮复核全未抓到**，由 E5 扫教训库扫出，是重蹈 DHR_26 已登记教训 |
| E-053 | test | **共享定义模块纳入能力指纹**（F-075）：把 `_shared/relay.common.v1.schema.json` 的 `locator.pattern` 改成 `^.*$`（G5 全域绝对路径禁令彻底失效）—— **修前** `capability-baseline.mjs` `exit 0` 且 `capability_hash` 逐字未变（`5c5685d0748b9f58…`）、`audit` 也 `exit 0`；**修后** `exit 1` 并精确报「协议 relay.common/v1 的 digest 变了——这是一次能力变更」。基线 `protocols` 由 7 条变 8 条，`capability_hash` → `3ccf3b1043afbd82…` | pass | F-075：F-033 缺口 A 下沉一层已闭。两个带着不同 locator 约束的 Runtime 不再能握手成功 |
| E-054 | test | **双模守卫与退出码**（F-080）：`node -e "import(…validate.mjs)"` 修前抛 `TypeError: The "path" argument must be of type string`、修后正常导出 `loadAjv,validateOne`；无参调用由 `exit 0` 改 `exit 2`；CLI 与 `--selftest` 行为不变（`pass=33 fail=0`） | pass | F-080：模块半边在 `node -e` / REPL / stdin 入口下可用；无参静默绿这条空绿形态已闭 |