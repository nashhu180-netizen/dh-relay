<!-- progress.md — 施工日志 + 证据账本。边做边记；跑偏记这里，不回写计划文档。 -->
# progress — DHR_70

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|---|---|---|---|---|
| 2026-09-01 | 主会话 | D-start：用户「开工授权」+ 点选开树 / 本会话自干 / 委托沿用默认；主树建 v2 七件套骨架、冻结施工合同 | — | 切 `wt/DHR_70`，按 task_plan 第 1 步只读侦察 |
| 2026-09-01 | 主会话 | 建树踩坑：`dh wt new DHR_70` **起于 `origin/master`**，而本仓 origin 落后本地 104 个提交——新树初始不含 B-34 落盘与本工作区。已 `git reset --hard master` 修正 | `git worktree list` / `git rev-list --count origin/master..master` = 104 | 记 L-7002 |
| 2026-09-01 | 主会话 | task_plan 第 1 步只读侦察完成（零改动），三问全部有结论，H1 形态收敛为「gate 保留了、actor 没保留」 | E-7005 | 按第 2 步写 A2 红测 |
| 2026-09-01 | 主会话 | 第 2 步：新建 `dhr70-submission-gate.test.mjs`，A2 用例跑红，**红因与 DHR_35 实录同码**（`E_LEASE_HELD:actor-closed`）。H1 由"起点线索"升级为夹具确认的根因 | E-7006 | 第 3 步补 A1（lease 仍有效的晚交） |
| 2026-09-01 | 主会话 | 第 3 步：补 A1 **当场就绿**——现役已满足 P6-RI-A1，**未动任何实现**。同时给 A2 也加上 `untilRoundEnded`，使 A1/A2 成为只差"外部是否接管 lease"一个变量的对照组：`1 pass / 1 fail`，A2 红因不变 | E-7002、E-7006 | 第 4 步补 A3 三条负例 + B（实现前必须先绿，作"没修过头"基线） |
| 2026-09-01 | 主会话 | 第 4 步：补 A3-1 / A3-2 / A3-3+B，**四条负例实现前全绿**，`5 tests / 4 pass / 1 fail`，唯一的红仍是 A2。归因副产物：A3-1 证明「接管后第一次提交由 writeGuard 在落盘前 fence」，所以拒绝路径**不依赖** actor 是否已关闭——这条撑起第 6 步"只改路由、不碰单写者"的可行性 | E-7003 | 第 5 步补 C（`agent_get=idle` ∧ `pane_get=error` 不得单独判死 Attempt） |
| 2026-09-01 | 主会话 | 第 5 步：补 C，**当场就绿**。至此 `6 tests / 5 pass / 1 fail`，全卷唯一的红是 A2。H2 假说被否：`pane_get=error` 不是根因、也不是必要条件——A2 的夹具里根本没有 pane 信号 | E-7004 | 第 6 步在 `service.mjs` 做最小实现 |
| 2026-09-01 | 主会话 | 第 6 步最小实现落 `service.mjs` 一处（`submitExecutorResult` 首轮 drivers 循环 + `evictClosedActor`）：**A2 转绿，A1/A3/B/C 五条负例与正例全部保持绿**（6/6）。A3-1 的阶段二判据同步改为「不再是 writeGuard 的 fence」而非锁死 `actor-closed` 措辞——修后拒绝改由 `acquireLease` 给出裸 `E_LEASE_HELD`，拒绝这件事本身没变 | E-7007 | 第 7 步 package.json 追加 token + 全量回归对齐 |
| 2026-09-01 | 主会话 | 第 7 步：`package.json` 只追加 `test/dhr70-submission-gate.test.mjs` 一个 token；`dhr64-result-bridge.test.mjs` 隔离单跑 10/10 绿，**无需改动**。全量对比见 E-7007，另记 F-7003（master 基线本来就红） | E-7007、F-7003 | 第 8 步越界自查 + `dh dh-relay` |
| 2026-09-01 | 主会话 | 第 8 步：越界自查 `git diff --name-only` ⊆ 允许路径；`dh dh-relay` 0 失败。施工八步收口，进 E 阶段五路复核 | E-7007 | E 阶段：派轮 1 代码复核 |
| 2026-09-01 | 主会话 | **E 阶段·轮 1 派出并回收**（`dhr70rev1`，codex 只读沙盒 · terra high · fresh）：**零 P0/P1，一条 P2**。派活形态改动记 L-7005——本会话不在 Herdr pane（`HERDR_ENV` 空），走不了仓内 `herdr-派活操作.md` 的规程，降级为 `/codex` 插件 companion 后台任务，cwd 钉 worktree | E-7008 | 采纳 F-70-R1-01 并整改 |
| 2026-09-01 | 主会话 | **整改 F-70-R1-01**（提交 `8ed411d`）：A2 补「lease 真的换手了」三条断言——`holder_pid === process.pid`、`epoch === held.epoch + 2`、`lease_acquired` 序列恰新增一条。轮 1 指出原断言挡不住「绕开 lease 直接落盘」的错误实现，属完成条件 A2「新取 lease、新 actor」这半句**没被证明**，不是风格问题。定向套件 6/6 仍绿 | E-7010 | 派轮 1b 复验 |
| 2026-09-01 | 主会话 | 轮 1b 派出（`dhr70rev1b`，fresh、未继承轮 1 会话）：判 F-70-R1-01 闭合性 + `epoch+2` 是否过脆 + 是否引入新问题 | E-7009 | 1b 闭合后开 Review Batch 四路并发 |

| 2026-09-01 | 主会话 | **E6 miner 回流完成**：`dh mine dh-relay DHR_70` 出简报，派 fresh 只读实例抽教训候选，产出 2 条新候选（L-7008 纵向复核替代不了横向契约对照 / L-7009 派活是三事实闭环）+ 一份回链清单 | E-7021 | — |
| 2026-09-01 | 主会话 | **E9 交付汇报已发出**（七段 Style A + 项目摘要一句 + 收尾执行摘要），底表=本 progress 证据账本，不落盘 | E-7015、E-7016、E-7017、E-7022、E-7023 | — |
| 2026-09-01 | 主会话 | **E10 人验证据展示区已发出**：A1/A2/A3 各一段业务化五段（要证明啥—期望值—实际值—差没差—证据局限）+ 变异对照 + 全量回归 + 证据落点 + 「七路复核者均无法复跑测试、一律静态审」的边界申报 | E-7015、E-7016、E-7017 | — |
| 2026-09-01 | 主会话 | **E11 用户对话确认**：点选「认可，执行本地收口」，并裁决三条尾巴（F-7003/F-7004 入 backlog、F-7002 挂起、L-7001~L-7010 本次一并提炼） | review.md 确认记录 | 进 E12 精确 squash 合入 |

## 证据账本 (Evidence Ledger)

<每条"完成"结论挂一条可复跑的命令 / grep / runtime 输出。类型枚举含 `review-dispatch` / `session-run`（大小写精确）。>

| ID | 类型 | 命令 / 路径 | 结果 (pass/fail/observed/waived) | 支撑什么结论 |
|---|---|---|---|---|
| E-7006 | test | `node --test relay-core/test/dhr70-submission-gate.test.mjs`（A2 用例） | fail（**预期红**） | 非终态 Run + current Receipt，外部把 `host-lease.json` epoch+1（holder 用已死 pid、`expires_at` 置过去）→ 旧 actor 下一 tick `renew()` 失租关闭 → 提交返回 `{"reason":"E_LEASE_HELD","detail":"E_LEASE_HELD:actor-closed"}`，与 E-3526 同码。轮询期先经 `lease-lost` 再转 `actor-closed`，证明红因不是夹具搭错。单例耗时 6.3s（等 `runHostSession` 的 5s tick）。第 3 步起 A2 与 A1 一样先 `untilRoundEnded`，红码与形态不变（5.6s） |
| E-7005 | probe | 只读 `service.mjs:217/289/300/447` · `host.mjs:150/159` · `workflow-driver.mjs:41/43/93/95/601` + `git diff --name-only` | observed | ①`ensureActor` 命中 `actors` map 才复用，而 `actor.done.then` 会把结束的 actor 摘掉，所以它本身不会交回死 actor；②`submitExecutorResult` 首轮 `for (const [runId, driver] of drivers)` 里 `tryDriver` 直接 `throw withReason(error)`，`actor-closed` 会穿出调用栈，**走不到 458 行起的 durable 重建循环**；③`driveRun` 的 `done` 回调在 `hasOpenSubmissionGates` 为真时**故意保留 driver**（`:301`），但 driver 在构造时捕获了 `actor` 引用（`:95` `actor.submitControl`），actor 一死这条 gate 就成了死路——**gate 的生命周期与 actor 的生命周期不一致，这是 H1 的精确形态** |
| E-7002 | test | `node --test relay-core/test/dhr70-submission-gate.test.mjs`（A1 用例） | pass（**现役已满足，未改任何实现**） | lease 仍由现役 service 合法持有（断言 `holder_pid === process.pid`）、本届 herdr 等待已收口（等到 `observation_status=observation_lost`）、节点仍 `running` → 晚交 committed、`structured.source=receipt-bound-submission/v1`、`results/` 恰一个文件、节点转 `succeeded`、同 digest 重投 `idempotent:true` 不新增 Result。A1 与 A2 现共用同一夹具，**只差"是否外部接管 lease"一个变量** |
| E-7003 | test | 同上（A3-1 / A3-2 / A3-3+B 三个用例） | pass（**四条负例全绿，实现前基线**） | ①**A3-1 真接管**：epoch+1 且持有者活着（本测试进程 pid）、租约未过期 → 提交先被 Store writeGuard 在落盘前 fence（`lease-lost`），旧 actor 续租失败关闭后仍拒（`actor-closed`）；两阶段都 reason=`E_LEASE_HELD`、`results/` 空、`events.jsonl` 逐字节不变、节点仍 `running`。②**A3-2 非 current**：注册更晚的 `rcpt-dhr70-next` 使 `rcpt-dhr70` 退为陈旧，lease 完全正常 → 仍 `E_IDENTITY_MISMATCH`。③**A3-3 终态冲突**：先合法提交转 `succeeded`，再换 outcome 重投 → `E_TERMINAL_STATE_CONFLICT`，已落 Result 未被改写。④**B 未知 Receipt** → `E_IDENTITY_MISMATCH`、零 Result |
| E-7007 | test | ①`node --test relay-core/test/dhr70-submission-gate.test.mjs` ②`npm test --prefix relay-core`（master 版 `service.mjs` vs 本卡版，同机顺序跑） | pass（修后 6/6；全量无新增稳定失败） | ①定向套件 **6 tests / 6 pass / 0 fail**（修前 5 pass / 1 fail，唯一的红是 A2）。②全量：master 基线 291/275/**16 fail** → 本卡 297/282/**15 fail**（+6 用例、+7 pass、-1 fail）。两次的失败集合不完全相同，逐条隔离复核：`service.test.mjs` 13/13 绿、`rpc-service.test.mjs` 13/13 绿、`dhr64-result-bridge.test.mjs` 10/10 绿、`agent-node.test.mjs` 的 `DHR_61 D1` 单跑绿——**全量里那两条"新"红都是并发抖动**；`agent-node.test.mjs` 的「DHR_33 窄路径」在 master 与本卡上**隔离单跑同样红**（pre-existing），`identity-quota.test.mjs` 隔离单跑 12 tests / 3 pass / 9 fail（pre-existing，见 F-7003）。**结论：本卡零新增失败** |
| E-7004 | test | 同上（C 用例：agent idle ∧ paneGet 失败） | pass（**现役已满足，未改任何实现**） | driver 层注入 `statuses:['idle'] + paneAlive:false` 复现 E-3526 的观测串：末条 `host_observation_changed` 确为 `agent_get=idle;pane_get=error`；全程无 `E_EXECUTOR_HOST_LOST`、无 `attempt_failed`，只落 `E_EXECUTOR_RESULT_MISSING` Attention，节点停在 `waiting_human`（非终态）；`hasOpenSubmissionGates` 仍为 true，随后提交成功、恰一个 Result、节点转 `succeeded`。**结论：`pane_get=error` 与本卡故障无因果**，DHR_69 的「仅 idle∧blocked 才覆盖」已经挡住了它。service 不转发 `herdrCli`，故本例只能落在 driver 层 |
| E-7008 | review-dispatch | node codex-companion.mjs task --background --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-code1.md） | observed | 复核派出：dhr70rev1（codex --sandbox read-only · gpt-5.6-terra high · fresh · /codex 插件 companion）｜代码复核轮1：全 diff 35ff2db..b6a3b47，8 点必答（单写者是否被放宽/黏旧进程/await dead.done happens-before/Map 迭代删元素/牵连路径/测试防护力/越界/F-7003 诚实性） |
| E-7009 | review-dispatch | node codex-companion.mjs task --background --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-code1-reverify.md） | observed | 复核派出：dhr70rev1b（codex --sandbox read-only · gpt-5.6-terra high · fresh · 未继承 dhr70rev1 会话）｜轮1b 整改复验：F-70-R1-01 增量 diff b6a3b47..8ed411d，判闭合性/epoch+2 是否过脆/leaseEpochs 解析/是否引入新问题/越界/对轮1其余结论的独立意见 |
| E-7010 | test | `node --test test/dhr70-submission-gate.test.mjs`（cwd=`relay-core`，整改提交 `8ed411d` 上复跑） | pass（**6 tests / 6 pass / 0 fail**，37.3s） | F-70-R1-01 整改后定向套件全绿：A1 6.8s、**A2 11.7s**（补三条 lease 断言后变长，因为要等旧 actor 真失租再等新 actor 取到 lease）、A3-1 6.0s、A3-2 5.7s、A3-3/B 5.7s、C 0.8s。A2 新断言实测取到 `holder_pid === process.pid`、`epoch === held.epoch + 2`、`lease_acquired` 恰新增一条——**证明晚交确实经由重新取得的 lease 落地**，不是绕过 fencing 直写。这正是轮 1 指出「原断言挡不住绕开 lease 的实现」的闭合证据 |
| E-7011 | review-dispatch | node codex-companion.mjs task --background --fresh --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-code2.md） | observed | 复核派出：dhr70rev2（codex --sandbox read-only · gpt-5.6-terra high · fresh · Review Batch 并发）｜代码复核轮2（增量+变异点选点）：全程 + 轮1/1b 记录 + 增量 diff b6a3b47..HEAD + A1~D 逐条 + 越界；变异点必须由本实例选点（施工方自报即红） |
| E-7012 | review-dispatch | node codex-companion.mjs task --background --fresh --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-req.md） | observed | 复核派出：dhr70req（codex --sandbox read-only · gpt-5.6-terra high · fresh · Review Batch 并发）｜需求方向复核：权威链 backlog B-34 → design/12 → DevPlan §3.2 → brief 逐级比对；范围漂移双向、H=0 声明是否成立、契约无变化声明是否正确 |
| E-7013 | review-dispatch | node codex-companion.mjs task --background --fresh --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-consistency.md） | observed | 复核派出：dhr70con（codex --sandbox read-only · gpt-5.6-terra high · fresh · Review Batch 并发）｜一致性复核（横向）：review.md 冻结的五个比对对象 + 自选维度（map 删除时机守卫 / E_LEASE_HELD 判别手段 / await done 收口 / 命名） |
| E-7014 | review-dispatch | node codex-companion.mjs task --background --fresh --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-lessons.md） | observed | 复核派出：dhr70les（codex --sandbox read-only · gpt-5.6-terra high · fresh · Review Batch 并发）｜教训复核：正册不存在→N/A（可核查）；扫候选库 58 条给命中清单，重点核候选-48/49/51/56/58；核本卡 L-7001~L-7006 质量与重复 |
| E-7015 | test | `node --test test/dhr70-submission-gate.test.mjs`（cwd=`relay-core`，HEAD=`5d372e5`） | pass（**exit=0**，wall=43s） | F-70-R2-01 整改后定向套件 **6 tests / 6 pass / 0 fail**，`duration_ms=38944`。补记 exit code 与 wall-clock 以闭合 F-70-LES-02（命中候选-51/58：异步套件门槛要记退出证据，不能只记通过数） |
| E-7016 | test | ①`npm test`（cwd=`relay-core`）在 HEAD=`1e118a2` 上**连跑 3 次** ②`node --test --test-concurrency=4 test/agent-node.test.mjs test/identity-quota.test.mjs` 在 **master 版 `service.mjs`** 与**本卡版**上各跑一次（同参数同机） | observed（三轮 exit 均=1；对照组差集为空） | **闭合 F-70-LES-01（真重蹈候选-48/12/23/31）**。原结论「隔离单跑一次绿 ⇒ 两条新红是抖动 ⇒ 零新增失败」证据覆盖不足，现改为直接实证：①**同一份代码**三轮全量 `297 tests` 恒定，fail 分别为 **18 / 15 / 14**（wall 1024s / 511s / 565s，exit 均 1）——同代码不同失败数，抖动本身即被证明；②三轮**都**红的稳定集合 = **13 条**（DHR_34 族 9 条 + DHR_33 族 4 条），逐轮一致；③抖动项每轮各不相同（run1 五条、run2 两条、run3 一条），散落在 CLI / dsh-bridge / e2e / DHR_61 / DHR64 等与本卡无关的套件；④**本卡六条用例三轮零失败**（`grep -i dhr70` 在三轮失败集合中零命中）；⑤可比对照：把 `service.mjs` 换回 master 版跑同样两个套件，master 与本卡**失败集合逐条相同**（各 21 tests / 11 pass / 10 fail = DHR_34 九条 + DHR_33 窄路径一条，`comm -3` 差集为空），换回后 sha256 与换前一致（`7b7e3bcd…c68c6`）。⑥全量里多出的 3 条 DHR_33 稳定红在隔离条件下**两个版本都不红**，属负载相关。⑦稳定失败的**真实报错已取到**：`EPERM: operation not permitted, rename '…state.json.<uuid>.tmp' -> '…state.json'`，是 `%TEMP%` 原子重命名被拒（耗时 60~74s 正对应旧记录里的「等满 60 秒」），环境层原因、非逻辑缺陷。**结论收窄为**：本卡零新增失败——在可比条件下 master 与本卡的失败集合逐条相同；F-7003 的稳定红仍为 pre-existing 且已定位到 EPERM |
| E-7017 | test | 变异点施加/还原（锚点 `relay-core/runtime/service.mjs:474`，轮 2 实例 `dhr70rev2` 选点）：`sed -i "474s/!== 'E_LEASE_HELD:actor-closed'/=== …/"` → `node --test test/dhr70-submission-gate.test.mjs` → `git checkout -- relay-core/runtime/service.mjs` → 复跑 ×3 | **施加后红（exit=1，6 tests / 4 pass / 2 fail）；还原后绿** | **有效单测「改坏必红」成立，且比选点人预期更强**。①施加后**两条**红：**A2**（正例）与 **A3-1**（"不得放宽单写者"红线负例）——变异被正例与红线负例同时抓住。②A2 的红点与轮 2 预测**不同**：轮 2 预测停在 `assert.equal(settled.error, undefined)`，实际停在 `refusals >= 1`（"必须真的经历过旧 actor 的拒绝阶段"）。**红因已确认**（候选-45 要求）：判据反转是双向的，`===` 之后 `lease-lost` 反被吞掉并触发摘除重建，于是轮询首次即成功、拒绝阶段被整段跳过——这正是"放宽单写者"的形态，而抓住它的恰是为 F-70-R2-01 新补的那条断言。A3-1 红在 `assert.match(…, /lease-lost/)`：被吞掉后第一阶段不再是 writeGuard 的 fence。③**hash 链（登记用 git 对象 id）**：还原态 `d0c7ebf7bc7c3afdf79b0cf2626528c1ca3e3d56`（当前提交里的 blob）↔ 变异态 `934efded9bc99676e06dddb20f0a0e3c809f9d54`（`git hash-object -w` 写入对象库，审计者可 `git cat-file -p` 取回复现）；还原后 `git hash-object` 回到**同一对象**。**为什么改用 git 对象 id**：R31 闸门要求 hash 必须是仓库里真实存在的对象（`git cat-file -e` 可查），文件级 sha256 既不满足 40 位形态、也无法被第三方取回内容——原记的 sha256 链 `7b7e3bcd…c68c6` → `d40307ff…2895` → `7b7e3bcd…c68c6` 保留作旁证。复现跑于最终态：exit=1、`6 tests / 4 pass / 2 fail`，红的仍是 A2 与 A3-1。，`git status` 0 处改动，CRLF 1078/1078 未混（候选-53）。④还原后首次复跑撞 F-7004 环境 EPERM 而红一条，紧接两次复跑 exit=0 / 0 fail / 零 EPERM |
| E-7018 | probe | `grep EPERM` 于 `full-run-{1,2,3}.txt`、`master-two-suites.txt`、还原后三次定向复跑 | observed | **F-7004 的取证**：`EPERM: operation not permitted, rename '…\AppData\Local\Temp\<fixture>\.dh-relay\<run>\state.json.<uuid>.tmp' -> '…state.json'`。同一签名同时出现在 DHR_34 族稳定失败、DHR_33 族失败与**本卡 A2 的一次还原后复跑**里——说明它不是某个套件的逻辑缺陷，而是本机 `%TEMP%` 下原子重命名被间歇性拒绝（典型成因：实时扫描/索引占用文件句柄）。判据不靠"复跑一次绿"：有精确报错文本、跨套件同签名、且代码 sha256 逐字节未变三条独立支撑 |
| E-7019 | review-dispatch | node codex-companion.mjs task --background --fresh --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-code2-reverify.md；基线不写死，由复核者自报 HEAD） | observed | 复核派出：dhr70rev2b（codex --sandbox read-only · gpt-5.6-terra high · fresh · 未继承本卡前六路任一会话）｜轮2b 收口复验：五条整改闭合性 + 契约同步是否越权/是否顺手放宽 + 变异证据可信度（红因与轮2预测不同）+ LES-01 新证据是否仍过度声称 + E8 挂证据与 H=0 + 允许路径两次变更是否自开后门 + 是否引入新问题 |
| E-7020 | hygiene | `git diff --name-only master..HEAD` · `git diff --check master..HEAD` · 逐条对 DevPlan `dh:allowed-paths:v1 task=DHR_70` | pass（路径零越界）／**计数已作废** | ⚠ **本条的文件计数失实，已由 E-7022 取代**（轮 2b `dhr70rev2b` 逮到 F-70-R2B-01）：写的是 21 个文件、workspace 15 个，实为 23/17——因为这个数是在最后两笔提交落盘**之前**取的，却以「终态」名义进了放行材料。**路径逐条无越界这一结论不受影响**（复核者亦独立确认），作废的只是计数。原文按候选-10 保留不改。原记：终态越界自查：**21 个文件**——`relay-core/runtime/service.mjs`、`relay-core/test/dhr70-submission-gate.test.mjs`、`relay-core/package.json`（只追加一个 token）、`docs/modules/dh-relay/workspace/DHR_70/**`（15 个：七件套 + 6 份派单 + 6 份复核者原文）、`docs/modules/dh-relay/as-built/relay-core.md`、`docs/modules/dh-relay/design/12-…契约调整.md`、`docs/modules/dh-relay/dev_plan/P6-…开发方案.md`。**后三者是本卡内的两次允许路径变更**：①design/12 与 DevPlan 卡节 = **用户 2026-09-01 对话点选授权扩入**，范围限「Gate 生命周期」措辞同步与本卡允许路径登记/状态回填；②`as-built/relay-core.md` = 按 brief 既有「触及子系统（收口时更新其 as-built）」声明**补登**（DevPlan 原清单漏列，E7 又是收口必做步骤）。两次变更均在 DevPlan 内留明授权来源与限定，未借机改其它小节。`git diff --check` 无输出（无行尾/空白问题）；`relay-core/**` 之外零生产代码改动 |
| E-7021 | review-dispatch | node codex-companion.mjs task --background --fresh --model gpt-5.6-terra --effort high（cwd=.dh-worktrees/DHR_70，派单见 review-brief-miner.md） | observed | 复核派出：dhr70miner（codex --sandbox read-only · gpt-5.6-terra high · fresh · E6 只读备料，非复核路）｜E6 miner 回流：从 review/findings/progress 抽教训候选，对候选库 58 条 + 本卡 L-7001~L-7007 去重；产出只进本卡 lesson_candidates.md，不写 knowledge/（不在允许路径） |
| E-7022 | hygiene | `git add -A` 后在**最终提交态**取：`git diff --name-only master --cached`（26）· `… \| grep -c 'workspace/DHR_70/'`（20）· `git diff --check --cached master` | pass（**零越界**） | **取代 E-7020 的计数**（F-70-R2B-01 整改）。终态共 **26 个文件 / workspace 20 个**：生产与测试 3 个（`relay-core/runtime/service.mjs`、`relay-core/test/dhr70-submission-gate.test.mjs`、`relay-core/package.json` 只追加一个 token）+ `docs/modules/dh-relay/workspace/DHR_70/**` 20 个（七件套 + 7 份派单 + 7 份复核者/miner 原文）+ `as-built/relay-core.md` + `design/12-…契约调整.md` + `dev_plan/P6-…开发方案.md`。后三者的授权来源与限定见 E-7020 原文与 DevPlan 内登记，未变。`git diff --check --cached master` 无输出。**取数方法也一并改正**：在暂存态取而非中途取——暂存态内容与提交后逐字节相同，故该计数对最终提交成立（L-7010） |
| E-7023 | hygiene | `dh dh-relay`（cwd=`.dh-worktrees/DHR_70`，终态） | pass（**0 失败 / 78 警告**） | 收口体检。与主树 master 实测的 **0 失败 / 83 警告** 相比**少 5 条**——正是 DHR_70 `review.md` 那 5 条「待派出锚点」被本卡填实所致（83−5=78，账对得上）。DHR_70 自身现仅剩 1 条告警：`brief.md` 的 R14「找不到 DevPlan 计划卡验收口径」，本仓 30+ 张卡**人人都有**的存量形态，非本卡引入。**过程中自己造过 3 条失败并已全部修掉**：①R30 允许路径清单被我在条目里塞说明文字打坏解析（说明文字已挪到清单下方的散文块）②R17 完成条件 C 抄进 review 时漏了「观测」二字 ③R27 结论行的「派出=」同时塞了 `e:` 与 `log:` 两种锚点。另有 R21（见下）。**一条曾被误判的**：R29 EVENT_STALE/EVIDENCE_STALE 一度报 5 条，主控当时误判为「改允许路径=规划变更需走 B-adjust」，实为**规划工件已改但尚未提交**时的状态假象，提交后自动清零——该误判已在对话里当场收回 |
| E-7024 | probe | `review.md` 验收项元数据表 DHR_70-C 覆盖态：**部分 → 等价覆盖**（用户 2026-09-01 对话裁决） | observed | R21 闸门（design/05 §五「只有等价覆盖满足原命题」）曾因此报 1 条失败。**主控自我更正**：E8 初稿把「本卡没做端到端 A4」错当成「证据只部分覆盖本命题」——两者是不同命题。本命题是「**driver 决策逻辑**在 `agent_get=idle ∧ pane_get=error` 下不得判死 Attempt」；夹具 fake 的只有 herdr CLI，**driver 为真实代码**，注入的两个观测值是 DHR_35 实录 E-3526 里**真实出现过**的值 → 真实输入 × 真实逻辑 = 等价覆盖。真实 Herdr 会否产出该形态是**另一条命题**（P6-RI-A4，归 DHR_35），已如实记进「未覆盖边界」列。因该更正会把闸门由红翻绿，**未由主控单方改口**，理由在对话里明示后由用户裁决；轮 2b 曾评价原降级「诚实且必要」，但它评的是自报诚实度、未在四态语义与 R21 视角下判定，此差异一并留痕 |
