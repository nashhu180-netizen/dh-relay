<!-- dh:v1 · DHR_70 轮 2b 收口复验 · 复核者原文（主控捕获落盘，未删改） -->
# review-code2-reverify-codex — DHR_70 轮 2b

- **复核者**：`dhr70rev2b`
- **形态**：codex `--sandbox read-only` · gpt-5.6-terra · high · **fresh，未继承本卡前六路任一会话**
- **派发**：`/codex` 插件 companion，cwd = `.dh-worktrees/DHR_70`
- **派单**：`review-brief-code2-reverify.md`（**基线未写死，由复核者自报 HEAD**——L-7007 的整改）
- **自报 HEAD**：`843fbc4`｜**落账**：e:E-7019｜**耗时** 4m59s

---

## 形态自述（含你看到的 HEAD short sha）

只读静态复核，未运行测试、未修改文件。HEAD：`843fbc4`；工作区无未提交改动。全量 `master..HEAD` 实际为 23 个文件。

## 五条整改逐条闭合判定

- F-70-R2-01：闭合。A2 每次 `lease-lost` 都断言拒绝、零 Result，并逐字节比对 `events.jsonl`；`refusals >= 1` 能防止直接跳过拒绝阶段。Store 的合法状态变更由事件账回放，故事件账不变也约束状态投影不变。
- F-70-REQ-01：闭合。契约已同步，且实现只对精确 `E_LEASE_HELD:actor-closed` 摘除陈旧 route；`lease-lost` 仍原样拒绝。
- F-70-REQ-02：当前技术缺口已由 `8ed411d`（新 lease/new actor）与 `1e118a2`（拒绝阶段）闭合。但“提出时已过期”不准确：该 finding 对 `b6a3b47` 是真实问题，应表述为“后续整改已闭合”，不能把有效发现归因为派单基线。
- F-70-LES-01：三轮全量与同条件对照足以支持“本次样本中未见新增稳定失败”；不能无条件外推成全仓永久“零新增失败”。
- F-70-LES-02：闭合。E-7015 已记录 exit code 与 wall-clock。

## 契约同步正确性判定（含 git diff 核出的实际改动范围）

`design/12` 仅修改“Gate 生命周期”末句及紧随的 DHR_70 同步说明；未改 §4 验收清单、reason code 表或其它设计小节。DevPlan 仅在 DHR_70 允许路径段登记该授权及 as-built 补登。

新措辞与 `service.mjs` 一致：只有 `actor-closed` 触发摘除和 durable 重建；重建经 `ensureActor` 重新取 lease、按同一 Receipt 重建 gate；`lease-lost` 不被吞掉。未见放宽单写者。

授权对话本身不在当前可读材料内，无法独立复核；现有工件对授权范围的记录与实际 diff 一致。

## 变异证据可信度判定

可信。`!==` 反转为 `===` 后，`lease-lost` 被错误吞掉并进入摘除/重建路径，因此 A2 首次提交可成功，触发 `refusals >= 1`；A3-1 同源地失去 `lease-lost` 形态。该红因与“保护不得吞掉 fencing 拒绝”的唯一场景一致。施加前、施加后、还原后 hash 链自洽。

## 新发现

### F-70-R2B-01（P2）：终态越界证据的文件数错误

E-7020 与 `review.md` 均写“21 个文件、workspace 15 个”。实际 `git diff --name-only master..HEAD` 为 23 个文件，其中 DHR_70 workspace 为 17 个。逐条路径仍都落在允许集合内，因此这不是越界；但完成条件 D 的证据摘要不准确，不能以错误计数作为放行材料。

另：F-70-REQ-02 的“提出时已过期”归因应纠正为后续闭合，属 P3 留痕问题。

## 逐点结论（上面 7 点）

1. 五条整改：技术闭合；REQ-02 的历史归因需改正。
2. 契约同步：正确、未顺手放宽，实际 diff 未越过授权的小节。
3. 变异：解释、红因、hash 链均成立。
4. LES-01：结论须保留“当前样本/可比条件”边界，不能扩大为绝对全仓结论。
5. E8 与 H=0：A1~D 证据挂接基本属实；DHR_70-C 标“部分覆盖”诚实且必要；H=0 成立。
6. 越界：路径本身无越界；design/12、DevPlan 与 as-built 的变更理由可追溯，未见自开范围。但 E-7020 的 21/15 计数错误。
7. 整改未引入新的实现问题；新问题为放行证据计数失实。

## 轮 2b 总结论：changes-requested

Codex session ID: 01a05c84-3c99-7311-ba9b-7bf23160448c
Resume in Codex: codex resume 01a05c84-3c99-7311-ba9b-7bf23160448c

---

## 主控裁决（不属复核者原文）

三条**全部采纳**，无一驳回：

1. **F-70-R2B-01 (P2) 计数失实 —— 属实，是主控的错**。E-7020 的 21/15 是在最后两笔提交落盘**之前**取的数，却被当作「终态越界自查」写进放行材料。逐条路径确实无越界（这一点复核者也确认），但**用一个错数字当放行证据本身就是缺陷**。已在最终提交上重取并改正为 23/17，记 L-7010（回链候选-49：终态证据必须在最终提交上取，不止变异证据）。
2. **F-70-REQ-02 的归因表述 —— 属实，已改**。原写「提出时已过期」，把一条**对其审查基线 `b6a3b47` 而言真实存在**的发现，成因推给了派单基线。正确表述是「后续整改已闭合」；派单基线写死是**主控自己的流程错误（L-7007）**，不能拿来给复核发现减分。
3. **F-70-LES-01 的结论边界 —— 属实，已收窄**。改为「**本次样本 / 可比条件下**未见新增稳定失败」，不外推成全仓永久零新增。这与本卡整条 LES-01 的教训（不得让结论覆盖面超出证据）自洽——否则就是在修完一个过度声称之后又立一个新的。

**复核者另主动申报的边界**：授权对话本身不在其可读材料内，故无法独立复核用户授权的存在；它只能核「工件记录的授权范围」与「实际 diff」是否一致（结论：一致）。这条边界如实登记——**用户授权的事实由 E11 对话确认承担**，不由只读复核者背书。
