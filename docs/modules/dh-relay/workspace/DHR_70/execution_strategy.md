<!-- dh:v1 · execution_strategy.md — 分工。开工时定一次。 -->
# execution_strategy — DHR_70

## 操作模型

solo：本会话主控自干，过程可见、不分批（一个验收单元）。**先归因、后实现**——H1/H2/H3 用夹具钉死之前不动生产代码；A3/B 四条负例在实现前先绿，作为"没修过头"的基线。

## 子 agent 授权（若派单）

| 子 agent | 范围（只读 / 可写哪些文件） | 谁批准 |
|---|---|---|
| （施工期无） | — | — |
| 收口五路复核 | 只读全仓；不得改任何文件 | 用户已在 D-start 授权沿用默认委托节点 |

## 收尾铁律

- **不放宽单写者**：修 `actor-closed` 不许用"失租后也允许写"实现；A3 是这条的机器闸，实现前后都必须绿。
- **不黏旧进程**：任何断言都不得要求"打到写 Receipt 的那一届 service/actor"——那与重启恢复冲突（B-34 `W-03`①）。
- **不改协议面**：Receipt/Result/RPC 合同、reason code 表、Store mutation 语义一律不动；`package.json` 只追加一个测试文件名。
- `pane_get=error` 只作伴随信号；未经负例不得写成根因，也不得据它扩到 `herdr-executor.mjs`（要扩须另报用户）。
- 本卡**不跑真实 Agent**；DHR_35 的重跑要用户单独放行，不在本卡授权内。
- 证据不全 / 有 P0–P1 未关闭前，不许标"待验收"。
