<!-- lesson_candidates.md — 教训候选。收尾顺手记。AI 起草、人裁决。 -->
# lesson_candidates — DHR_70

## 教训候选

| ID | 一句话教训 | 状态 |
|---|---|---|
| L-7010 | **写进放行包的「终态」证据，必须在最终提交上重取**——不只是变异证据（候选-49 已覆盖变异那一半），越界自查的文件计数、失败集合、hash 一律同理。本卡的 E-7020 是在最后两笔提交之前取的数（21/15），却以「终态越界自查」的名义进了 review.md，实际是 23/17，被轮 2b 当场逮到｜触发：收口阶段把任何命令输出写进 review / 放行材料，而其后还会再产生提交｜出处：F-70-R2B-01、E-7020 → E-7022。**回链候选-49**（变异证据须以最终提交为基线）：本条是它的推广形 | ready-for-review |
| L-7008 | **增加同一维度的代码复核次数，替代不了一次横向的「实现 ↔ 设计契约」对照**——两者的盲区不同。本卡轮 1 / 1b / 2 三轮纵向代码复核全都没碰到 design/12「Gate 生命周期」末句与实现的字面冲突，是需求路与一致性路两个**横向**视角各自独立逮到的（且两路收敛到同一处）｜触发：实现为兑现设计**意图**而改变了既有契约的**字面**，或一张卡同时涉及代码、设计文档与验收声明｜出处：F-70-REQ-01、一致性裁决 1/2、E-7012、E-7013。与候选-14（核上游口径）、候选-33（对称实现路径横比）不重复：本条约束的是同卡内「实现 vs 冻结契约」的横向闭环，以及纵向复核无法替代它 | ready-for-review |
| L-7009 | **派活不是「命令回显了 job id」那一瞬间，而是「前置形态成立 + 对象版本明确 + worker 确已在跑」三个事实各自取证的闭环**——本卡三条派活教训（L-7005 前置不成立、L-7006 管道提前关导致 worker 空跑、L-7007 基线写死导致过期发现）其实是同一条的三个面｜触发：经终端复用器 / 后台 job / 插件 / 脚本派发施工或复核任务，且任务期间可能发生返工或环境降级｜出处：L-7005~L-7007、E-7008、E-7019、E-7021。与候选-4（brief 引用完整性）、候选-38（入口解析 vs 可拉起）、候选-40（模型身份取证）不重复：本条覆盖派发前提 / 基线 / 实际投递三段的共同闭环 | ready-for-review |
| L-7007 | 派单里的**基线 commit 不要写死**，写「当前 HEAD」并让复核者自报它看到的 HEAD：本卡并发四路的派单是在轮 1 整改**之前**写的，需求路照着 `35ff2db..b6a3b47` 审，于是把已经修好的缺口又报了一遍（F-70-REQ-02）。复核者很诚实地声明了"工作树已前进、后续提交不计入我的结论"，但主控白花一轮注意力去辨认哪些发现是过期的 | ready-for-review |
| L-7006 | 派后台任务的启动命令**不能用提前关管道的过滤器**（`\| head -1`、`\| grep -q`）：父进程打印完 job id 就被 SIGPIPE 掐死，而它还没来得及 spawn 那个 detached worker——回显里有 job id、`status` 里有记录，但 worker 根本不存在，job 永远停在 `queued`。判据同 `herdr-派活操作.md` 的「发完必须验证真提交了」：**看 worker 进程或 job 日志有没有第二行**，别只看回显。（附带坑：Git Bash 会把 `taskkill /PID` 的 `/PID` 当路径翻译成 `C:/Program Files/Git/PID`，cancel 类命令走 PowerShell） | ready-for-review |
| L-7005 | 仓内派活规程有**前置条件**，进 E 阶段前先验它成不成立：`herdr-派活操作.md` 整套规程的前提是「主控自己在 Herdr 管的 pane 里」（`HERDR_ENV=1`），本会话不是，四步流程第一步就走不通。规程文档写了这条前置，但它排在「前置检查」小节里、容易被当成环境说明跳过——**派活形态要在派之前确认，不是派失败了再降级** | ready-for-review |
| L-7004 | "对齐 master 基线"必须**先量再改**，且比对的是**失败集合**不是失败数——本卡量到 master 全量本来就 16 红，且同代码多次全量的红集合还不一样（`--test-concurrency=4` 抖动），只看数字会把抖动读成回归、也会把回归藏进抖动。**回链候选-31（"全量绿"结论必须带并发/负载条件）与候选-48（"环境假红"是要证明的结论）**：本条是那两条在"基线对齐"场景下的具体形，裁决时应合并或作为它们的用例挂上去，不另起平行表述（教训复核 `dhr70les` 建议） | ready-for-review |
| L-7003 | 负例断言只钉"该不该拒"，别钉拒绝的措辞：A3-1 锁死 `actor-closed` 字样，结果第 6 步一修就把这条本该不动的负例弄红了——它拒得好好的，红的只是我写的判据 | ready-for-review |
| L-7002 | `dh wt new` 从 `origin/master` 切树；remote 落后时新树会静默建在老基线上（本仓 origin 落后 104 个提交），建完必须核 `git log -1` 是不是本地 master | ready-for-review |
| L-7001 | 写"某某必须成功"之前，先回去核成功所依赖的那把锁还在不在——B-34 两条 P1（无条件补交越过单写者、黏死写 Receipt 的那一届进程）都是这条的变体 | ready-for-review |

> 状态流：`ready-for-review` → 人裁决 → `needs-promotion / promoted / rejected`
