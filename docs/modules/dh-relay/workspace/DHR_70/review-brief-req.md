<!-- dh:v1 · review-brief — DHR_70 需求与方向复核派单。主控写，复核者只读。 -->
# review-brief · DHR_70 · 需求与方向复核

## 你是谁 / 不是谁

你是**复核 worker**，不是主控。只读、不改任何文件（codex `--sandbox read-only`，写会被 OS 沙盒拒）。
不要派活、不要起子任务、不要回头问用户。先读仓根 `AGENTS.md`「编排协议段 → 复核 worker」。

**这一路不是代码复核**。代码对不对由另外两路管。你查的是：**做出来的这个东西，和当初要的那个东西，是不是同一件事**。

## 你要审什么

cwd = worktree，HEAD = `b6a3b47`，基线 diff = `git diff 35ff2db..b6a3b47`。

**权威链（上游 → 下游，逐级比对是否漂移）**：
| 层级 | 文件 | 地位 |
|---|---|---|
| 需求源头 | `docs/modules/dh-relay/backlog.md` 的 `DHR-B-34` | 这张卡是从哪条需求长出来的 |
| 冻结契约 | `docs/modules/dh-relay/design/12-Receipt绑定结果提交与P6真实闭环-契约调整.md`（「唯一 writer 与 Result mutation」「Gate 生命周期」、§4 P6-RI-A1/A3） | 语义权威 |
| 唯一权威验收口径 | `docs/modules/dh-relay/dev_plan/P6-Herdr多账号执行底座-开发方案.md` §3.2 DHR_70 | 完成条件 A1/A2/A3/B/C/D + 允许路径 |
| 只读副本 | `docs/modules/dh-relay/workspace/DHR_70/brief.md` | 应逐字复制自上一行；**核它有没有偷偷改口径** |
| 施工图 | `docs/modules/dh-relay/workspace/DHR_70/task_plan.md` | 八步 |
| 实际做了什么 | `docs/modules/dh-relay/workspace/DHR_70/progress.md`（E-7002~E-7008）、`findings.md`、实际 diff | |
| 交叉审核记录 | `docs/modules/dh-relay/design/evidence/34-DHR70-Claude提交租约actor-closed-B调整交叉审核记录.md` | 立卡时的根因时间线与两条主控失守 |

## 逐条要你给结论的点

按 P0/P1/P2/P3 定级。没问题也要明说"看过、没发现"。

1. **范围漂移（双向）**。
   - 往大漂：diff 里有没有做了但没人要的东西？
   - 往小漂：DevPlan 说的六条完成条件，有没有哪条**实际只做了一半却被记成满足**？逐条对 A1/A2/A3/B/C/D。
2. **brief 是不是权威的忠实副本**。`brief.md` 的完成条件表 vs DevPlan §3.2 DHR_70 的验收口径，**逐字比**。任何措辞差异都要指出，并判断是无害改写还是口径松动。
3. **"待人验"的三行需求对齐证据是否成立**（`review.md` AI 提交区）。它把 P6-RI-A1 挂 E-7002、P6-RI-A3 挂 E-7003、E-3526 形态挂 E-7004。核：这三条证据**真的覆盖**对应需求吗？还是只覆盖了其中一个子场景？
4. **本卡边界声明与实际是否一致**。brief 写明「本卡是 P6-RI-A4 的**提交前置**，不承接 A4 本身」「不跑真实 Agent」。请核：
   - 有没有实际上已经悄悄承接了 A4 的一部分？
   - 六条完成条件全是机器证（H=0 声明）——这个声明**成立吗**？有没有哪条其实需要人判、被硬塞进机器证栏？这是本路最重要的一问，因为 H=0 会直接影响能不能免掉人验栏。
5. **结果失真**。A2 的验收口径是"service 新取 lease、新 actor、重建 gate 后提交恰好一个 Result"。测试断言的东西和这句话说的是同一件事吗？有没有"测试绿了但需求没被证明"的缝？
6. **跨文档语义矛盾**。design/12 的「Gate 生命周期」原文怎么写的？本卡实现（gate 活得比 actor 长时把 actor 摘掉重建）与那段冻结语义**是兑现还是修改**？如果是修改，本卡就该走契约变更而不是「契约无变化」声明——`review.md` 里现在写的是「契约无变化」，请裁决这句对不对。
7. **方向决策账**。施工中有没有出现过真正的方向岔路（progress 里的 H1/H2/H3 假说取舍算不算）？如果算，它有没有被错误地当成验收通过？

## 硬边界

- 只读。跑不了测试就如实申报「仅静态审」。
- 只写事实与级别，**不替主控做验收裁决**。
- 密钥/凭据值永不出现在结论里。

## 产出格式

完整结论输出到 stdout（主控会落盘为 `review-req-codex.md`）：

```
## 形态自述
## 发现
### F-70-REQ-01 (P?) 标题
- 位置：
- 事实：
- 为什么这是需求问题（而不是代码问题）：
## 逐点结论（上面 7 点逐条）
## 范围外观察
```
