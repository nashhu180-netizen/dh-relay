<!-- dh:v1 · review-brief — DHR_74 教训复核派单。主控写，复核者只读。 -->
# review-brief · DHR_74 · 教训复核

## 你是谁 / 不是谁

你是**复核 worker**，不是主控。只读、不改任何文件（codex `--sandbox read-only`）。
不要再派活、不要起子任务、不要回头问用户。只做本轮复核，产出一份结论文本回给主控。

先读仓根 `AGENTS.md`「编排协议段 → 复核 worker」再动手。

**你审的是「这张卡踩过的坑有没有被如实、可复用地沉淀下来」**，不是代码对错，也不是验收口径达没达成。

## 你要审什么

cwd 已是 worktree `wt/DHR_74`，HEAD = `49196f8`。

| 读什么 | 地位 |
|---|---|
| `docs/modules/dh-relay/workspace/DHR_74/lesson_candidates.md` | **本轮主审对象**（3 条候选） |
| `docs/modules/dh-relay/workspace/DHR_74/progress.md` | 施工全过程日志 + 证据账本——教训的**原料**都在这里 |
| `docs/modules/dh-relay/workspace/DHR_74/findings.md` | F-7401 / F-7402 |
| `docs/modules/dh-relay/workspace/DHR_74/construction.DONE` | 交付声明与移交清单 |
| `docs/modules/dh-relay/knowledge/教训库-候选.md` | **既有教训库（候选-1 ~ 候选-68）**——判重与"加强版"归并的基准 |
| `docs/modules/dh-relay/workspace/DHR_71/`（progress / findings / review.md） | 前一张卡的同族现场，本卡是它的续集 |

## 逐条要你给结论的点

按 P0/P1/P2/P3 定级。没问题也要明说「看过、没发现」。

1. **三条候选的质量逐条判**。对每条给：**成立 / 部分成立 / 不成立**，并说明依据落在 progress 的哪条证据上。
   - 候选 1「进程空闲但事件循环被饿，必须用探针证据说话」——它的可操作判据（LOOP-LAG + fs 慢操作 + 进程级 CPU 增量三交叉）是否**足以让下一个人复用**？还是只是复述本卡结论？
   - 候选 2「flaky 预算类修复的机械前提是先证 fake 逐 poll 推进」——这条声称配对超时必须同倍率缩。请核它与本卡**实际改动**是否一致（`dhr69` 的 60_000 配对参数没缩、`agent-node` 是 ×4）：教训文本与自己的实践对不上，就是一条会误导后人的教训。
   - 候选 3「测试夹具是机器级负载的放大器」——治本位置的判断（夹具速率 / 心跳 / Store 持久化策略）是否越权替 DHR_72 和 Store 升级候选下了结论？
2. **判重与归并**。逐条去 `教训库-候选.md` 找同族条目，给出「新增 / 归并进候选-N / 已被候选-N 覆盖应撤」的建议。已知高相关的至少有：
   - 候选-31（"全量绿"结论必须带并发/负载条件）、候选-48（环境假红是要证明的结论）、候选-51（挂死≠失败 / 记 exit code + wall-clock）、候选-62（对齐基线比失败集合不是失败数）、候选-35（异步助手等不变量而非等期望值）、候选-39（新增测试要证明真被拾取）。
   - 请明确说：本卡三条里有几条是**真新增**，几条只是旧条的场景复述。
3. **漏记的教训**。progress 里有若干「过程事故」，请核它们该不该成教训，以及为什么没进候选：
   - 探针自证首试因 `D:/` 裸路径未转 `file://` 报 `ERR_UNSUPPORTED_ESM_URL_SCHEME`（Windows 路径进 ESM 的老坑）；
   - 门禁系列脚本的 verdict 标注因 **ms/s 解析 bug 误标 RED**——「判定脚本自己出错，差点把绿说成红」，这与候选-34（证据转录必须脚本采集）/ 候选-62 是同族还是新维度？
   - 步骤 3a 的策略修正：**单文件跑 10/10 绿、复现不出，必须回到全量门禁才现形**——「复现协议的粒度选错就等于没复现」，这条看起来是本卡最值钱的经验之一，为什么不在候选里？
   - 施工=主会话的**派工偏差**（`HERDR_ENV` 为空、不得从外部调度 herdr）已在 progress 登记——它与候选-52（派活层事故不落任何工件）/ 候选-63（仓内派活规程有前置条件）的关系。
4. **教训与证据的对应**。每条候选的「依据」列指向的证据 ID 是否**真的**支撑该条（抽查 `evidence/` 原始文件，别只信转述）。有没有把推断写成实证。
5. **去向建议是否可执行**。候选表的「去向建议」列只写了「教训路」「教训路 + backlog BL-17 关联」。请给更硬的落点建议：哪几条该进 `knowledge/教训库-候选.md` 正式编号、哪条该改写 `backlog.md` 的 `DHR-BL-17`、哪条该作为 DHR_72 / DHR_73 的**输入条款**写进它们的卡规格。
6. **本卡有没有"教训被结论替代"的风险**。F-7402 把内核级机制移交环境侧、门禁 6 连绿收口——若下一轮又红，后人靠现在这份 lesson_candidates 能不能快速重建现场？请指出为此**必须补记**的最小信息（例如：复现协议、判据阈值、探针注入命令原文、条件措辞的适用边界）。

## 硬边界

- **只读**、仅静态审。
- 只写事实与级别，**不替主控做验收裁决**，也不要替主控决定教训是否入库（给建议即可）。
- 范围外新想法写「范围外观察」。
- 密钥 / 凭据值、原始 Receipt ID 零出现。

## 产出格式

完整结论输出到 stdout（主控落盘为 `review-lessons-codex.md`）：

```
## 形态自述
## 发现
### F-74-LES-01 (P?) 标题
- 位置：lesson_candidates.md 第 N 条 / progress.md 某行
- 事实：
- 问题：漏记 / 误导 / 重复 / 证据不支撑
- 建议去向：
## 逐点结论（上面 6 点逐条）
## 范围外观察
```
