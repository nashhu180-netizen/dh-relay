复核所需材料已全部静态核对完毕（教训库 master 版含候选-69~74 已用 `git show master:` 读取；worktree 工作副本只到候选-68，因本枝基线 84f2514 早于 DHR_71 教训回流提交 `abff757`）。以下为完整结论。

---

HEAD: 0a24cb4
复核者: omp/deepseek-v4-flash

## 形态自述

- 只读静态复核。未运行测试、未跑门禁、未修改/新建/删除任何文件；bash 仅用 `git log/show/diff/rev-parse` 与 `sed/grep/wc` 只读查看。
- 开工时自报 HEAD：`0a24cb4`（worktree `wt/DHR_74`，`git status` 显示三个主控预置的未跟踪 `review-*-reverify-omp.md`，未触碰）。
- **材料版本差（重要）**：派单要求判重时把「今日 DHR_71 新入的候选-69~74」算进去，但本 worktree 的 `knowledge/教训库-候选.md`（基线 84f2514）只有候选-1~68；候选-69~74 在 master 上由 DHR_71 收口提交 `abff757`（"L-7101~L-7107 merged into candidates 69~74"）新增。本复核以 `git show master:docs/modules/dh-relay/knowledge/教训库-候选.md` 的 69~74 正文（第 562~610 行）为判重对象。建议主控知悉：本枝尚未 rebase 到含 `abff757`/`e795527` 的新 master，与整改 D 的「rebase 到新 master」只完成了一半（现基线 84f2514 含 DHR_71 代码改动，但不含其 verify/教训回流提交）。

## 发现

### F-74-LESB-01 (P2) 候选 6 的「负载条件」与自身冻结命令自相矛盾，且混入 F-7401 放大语境

- 位置：`workspace/DHR_74/lesson_candidates.md` 第 6 条。
- 事实：该条冻结命令是 **dhr69 单跑**（与 E-7405 `evidence/stalk-dhr69-r{1..15}-*.txt` 一致），负载却写「fake `working`、poll 1–5ms、**五文件全量门禁**」。而：① 停顿复现（E-7405/06/07）是 dhr69 单文件协议，五文件全量门禁是 E-7403b/E-7408/E-7409 的放大与验收协议，两者不是一回事；② DHR_69/A 用例夹具是 `statuses:['idle'], paneStatuses:['blocked']`（`relay-core/test/dhr69-false-ready.test.mjs:176-178` 附近 runtimeFixture 调用），不是 fake `working`——「fake working × 每 poll 心跳」是 F-7401 放大链的描述（`progress.md` 步骤 3b/5）；③ poll 1–5ms 是修前值，E-7405 追猎发生在 poll 已统一 20ms 之后（`progress.md` 步骤 5 先于停顿归因行；`git log` 74930dc 18:37 提交夹具改动，stalk 证据 18:37–18:45Z 之后）。下一人照该条组装复现包会在「跑哪个文件、喂什么状态、poll 多少」上被带偏。
- 建议：七要素齐备但负载行要拆准——停顿复现负载 = dhr69 单跑、DHR_69/A 夹具（idle+pane blocked、等 waiting_human 的 45s/90s 有界预算）、poll=20（修后）/2（修前）；放大复现负载 = 五文件全量门禁、fake `working`、poll 1–5ms（E-7403b）。入库前把两套分开写，或在行内注明各自对应证据 ID。

### F-74-LESB-02 (P2) 候选 5 的「依据：E-7408 日志留痕」指向不实——ms/s 误标事件的唯一仓内文字留痕已随整改被压缩删除

- 位置：`lesson_candidates.md` 第 5 条；`progress.md` 机器证 C「条数说明」。
- 事实：教训库-候选区自带的判据就是「每条候选的依据列指向的证据 ID 是否真的支撑该条」（`review-brief-lessons.md` 第 4 点）。候选 5 声称 ms/s 解析 bug 误标 RED 且「E-7408 日志留痕」。但：① E-7408 落点 `evidence/gate-round{1..6}-*.txt|.junit.xml` 是原始 node --test 输出，不含任何 verdict 脚本或 ms/s 解析痕迹（本复核全目录检索无 verdict/ms/s 字样）；② progress 机器证 C 现文只剩「第 1 轮在系列脚本崩溃前已完成且合格，第 2~6 轮由修复解析的续跑脚本完成」（line 35）——「ms/s 误标 RED」的显式表述（原见于 ddd69fe 版 progress 行「系列脚本的 verdict 标注曾因 ms/s 解析 bug 误标 RED…log 已留痕」）在整改 541e695 压缩措辞时被删掉了；该事件目前的文字出处只剩 `remediation-brief-r1.md` line 68 与两轮复核文档，证据目录里没有留痕文件。
- 建议：候选 5 的教训方向（判定脚本须先用已知样本自证）成立、独立性成立，但入库前补一条可复核留痕（误标样本 + 修复后判定输出落 evidence），或把依据改为 `remediation-brief-r1.md` + 本复核结论；progress 也可补一行恢复显式表述，否则下一位 reader 无法独立复核这条事实。

### F-74-LESB-03 (P2) 候选 2 的「必要性未证」只放在依据列，正文未吸收；候选 1 标题仍带「机器空闲」前提

- 位置：`lesson_candidates.md` 第 2、1 条。
- 事实（分两点）：
  - 候选 2 正文（教训本体）不含「本卡时间改动对绿闸必要性未证」；该事实只在依据列以半句带过（「DHR_71 独立基线证明本卡时间改动必要性未证」）。miner/后人若只取正文，仍可能把「按语义拆分缩放」读成对 flaky 的修复处方——尽管正文本身没写「能治 flaky」。建议把「必要性未证，仅放大侧指标下降（fs 写 56,775→17,650）」移一句进正文。
  - 候选 1 标题「机器空闲 ≠ 进程在前进」保留了「机器空闲」这一前提，而 E-7406 记录的停顿窗口 cpu 为 27–76%（低至 1%，`progress.md` E-7406 行），机器「空闲」从未在停顿窗口内被干净证明；轮 1 正是批评过把「机器空闲/进程空闲」写成可排除结论。正文已收窄正确，标题建议改成证据可支撑的形态（如「LOOP-LAG 只证 timer 未获调度」或「看着空闲≠在前进」的反向表述），并给 fs-probe README 显式指针（阈值 1s/5s/15s、drift>400ms 的权威定义在 `relay-core/test/helpers/fs-probe.README.md`）。

### F-74-LESB-04 (P2) 候选 4 缺「怎么判断粒度选对了」的操作判据

- 位置：`lesson_candidates.md` 第 4 条。
- 事实：轮 1 LES-04（P1，主控采纳为「最值钱的一条」）要求写明「先写明单文件/全量、并发、轮次和预期签名，简化跑法只可作对照，不能替代目标协议」。重写后行文为「复现粒度必须匹配目标负载：单文件 10/10 绿、回到五文件全量第 1 轮即现形；粒度选错等于没复现」——具体对照写清了（E-7403a 10/10 绿 vs E-7403b 首轮现形），但「怎么判断选对」只剩反向格言，未带「目标协议参数先行 + 简化跑法只作对照」这两句可执行判据。建议补上（一行即可），否则后人知道「选错等于没复现」却不知道怎样算选对。

## 逐点结论（派单 8 点）

**1. 候选 1（LOOP-LAG 覆盖面）— 通过（P2 见 LESB-03）**
轮 1 的三项要求都做到了：收窄成「LOOP-LAG 只能证明 timer 未按期获调度；fs、CPU、队列必须按各自覆盖面出结论」✓；阈值（1s/5s/15s、drift>400ms）与盲区清单（FileHandle/CJS/同步 API/队列）✓ 且与 `fs-probe.README.md` 一致（SLOW 1s/5s/15s、lag>400ms、覆盖边界四条）✓；关联候选-31/-48/-62 ✓。锚点 `workflow-driver.mjs:319-326` 经核实现树即「启动期 Attention（human_input_requested）写入」区（319 起 `if (blind||launchBlocked)` 块），且该文件在取证时基线 master@6050276 与现基线 84f2514 之间零改动（`git log master@6050276..master@84f2514 -- runtime/workflow-driver.mjs` 空），锚点无漂移。残留问题仅是标题「机器空闲」前提超出证据（LESB-03）。

**2. 候选 2（时间参数按语义拆分）— 通过（P2 见 LESB-03）**
① 与实际改动一致：`git diff 84f2514..HEAD` 实证——dhr64 `2→20/8→80/4→40`、dhr70 `2→20/20→200`、herdr-adapter `2→20` 及 `5→50/10→100/40→400/100→1000` 均为「等满 N 次 poll」语义随 poll 放大；dhr69 两夹具 `observationLostMs/doneTimeoutMs 60_000` 保持不动 = 墙钟上限不变；agent-node `5→20`（×4，绝对值对齐）。现文与 DevPlan 修订口径（worktree 与 master 版 P6 §3.2 均已改为「只在其语义是等满 N 次 poll 时随 poll 同比例放大；语义是墙钟上限时保持不变」，且注明「倍率一致不是要求，绝对值一致才是」）一致 ✓。② 与 DevPlan 授权一致 ✓（上条已列）。③ 必要性未证事实已挂在依据列（且 BL-17 补录 E-7119 行亦载），正文无「能治 flaky」暗示 → 误导风险低；但建议移入正文一句（LESB-03）。撤回重写目标达成：全文无本卡「×10 等比共缩」旧表述。

**3. 候选 3（夹具是放大器）— 通过**
越权处方已去掉：现文「放大链的治本候选需各自立卡验证，不预断 DHR_72/Store 是唯一位置」，不再写 DHR_72/Store 为治本位置 ✓。机制保留且量化（1–5ms 轮询 × 每 poll 心跳落盘 × 每 event 全量 replay → 单文件 2.8 万事件）与 E-7403b（writeFile 56,775/slow=6/max=3277ms）及源码（workflow-driver 心跳 appendCheckpoint、store 全量 replay）相符 ✓。去向「教训路 + backlog BL-17」成立：`backlog.md` DHR-BL-17 条目存在且已含 Store 升级条款语境与 E-7119 补录 ✓。措辞足够，无 P1。

**4. 候选 4（复现粒度）— 通过（P2 见 LESB-04）**
「单文件 10/10 绿、五文件全量第 1 轮即现形」的具体对照写清了，与 E-7403a（`probe-dhr69-r{1..10}` 10/10 绿、零 SLOW）与 E-7403b（`probe-gate-r1` 首轮捕获 2.8 万事件放大）逐字相符 ✓。但「怎么判断粒度选对」缺操作判据（LESB-04）。作为 P1 级最值钱教训，建议补轮 1 原话式的两句后入库。

**5. 候选 5（判定脚本单位解析误报）— 通过（P2 见 LESB-02）**
独立成条成立：候选-34 讲人工转录错位、候选-62 讲失败集合比较，本条是「判定脚本自身单位转换错误」——轮 1 的维度区分在本轮重读候选-34/-62 原文后维持成立，新入候选-69~74 亦无覆盖。可执行防线「判定脚本必须先用已知样本自证，再采信 verdict」给出 ✓。缺陷在证据指针（LESB-02）与正文略去了轮 1 建议的「保留原始 exit/pass/fail/wall-clock、冲突时先判脚本失效」后半句——建议补回。

**6. 候选 6（F-7402 最小复现包）— 通过七项核验，但负载项须修正（P2 见 LESB-01）**
按轮 1 LES-06 七项逐项核：
- 代码锚点 ✓ `workflow-driver.mjs:319-326` = 启动期 Attention 写入区，与 findings F-7402「启动期 Attention 写入不完成」及 BL-17 F-7109（停在 host_observation_changed(alive) 之后、human_input_requested 落账之前）一致；行号经核现树无漂移（见结论 1）。
- 完整命令 ✓ dhr69 单跑冻结命令 + `NODE_OPTIONS=--import ./test/helpers/fs-probe-register.mjs` 注入，与 E-7405 stalk 协议（`stalk-*.txt` 首行 `ACTIVE mode=loader-hook`）一致。
- 负载条件 ✗ 与冻结命令自相矛盾、混入 F-7401 放大语境（LESB-01）。
- 命中签名 ✓ `LOOP-LAG drift>400ms` 连续 6+ 次——E-7405 r9 原文恰 6 行 LOOP-LAG（3021/2827/2966/2853/2881/2905ms）后 DHR_69/A 45s 超时红，签名与证据吻合；EPERM/EXIT-PENDING 属门禁红形态（ownbase 签名扫描 S-EPERM/S-STALL 定义一致）。
- 阈值 ✓ 1s/5s/15s 与 drift>400ms 均与 fs-probe README/register.mjs TIERS 及 lagDetector 一致。
- 覆盖盲区 ✓ FileHandle/CJS/同步 API/actor 与 Store 队列/短时 CPU，与 README「覆盖边界」四条及 findings F-7402 未观测清单一致。
- 作废重跑规则 ✓ `.VOID-*` 改名留证重跑、作废轮不计结论，与 findings 移交条款①、ownbase 扫描「未复现 ≠ 已修复，撞签名按 .VOID-* 作废」一致。
缺项：无。需要修的是负载条件那一格（LESB-01）。

**7. 判重（含候选-69~74）— 逐条建议**
与既有库逐条比对（含 master 版新入 69~74）：
- 候选 1：**新增**（自标为候选-31/-48/-62 同族加强）。与候选-70（挂死先看活跃句柄，`unref()` interval 探针）互补不重复——-70 讲用什么工具定位挂死，候选 1 讲单支探针的结论边界。
- 候选 2：**新增**。与候选-73（等待上限必须由生产侧上限逐段推导、方向不能反）是同一预算纪律的两个轴：-73 管「上限怎么定」，候选 2 管「改 poll 后哪些预算要重推」。建议互相引用（候选 2 可在正文提 B-36/候选-73）。
- 候选 3：**新增**。候选-31/-48/-51/-62 与 69~74 均不覆盖「夹具每 poll fan-out 放大机器级负载」机制。
- 候选 4：**新增**。非候选-31（全量绿带负载条件）也非候选-48（环境假红判据）——粒度协议本身；69~74 无覆盖。
- 候选 5：**新增**。非候选-34/-62 重复；69~74 无覆盖。
- 候选 6：**不单独计为一条新教训**——它是候选 1/4 的落地附件（七要素与候选 1 的阈值/盲区、候选 4 的粒度协议高度重叠）。建议入库时并入候选 1（或 4）作「最小复现包」字段，或保留为附注条目并注明「附属、不单计」，避免 miner 把本卡误计成 6 条新教训（有效新增实为 5 条 + 1 包）。
- 结论：5 新增 + 1 附属包；无「应撤」、无「应归并进既有候选-N」的强重复。

**8. 漏记核（本轮新素材）**
① **「两卡证据被合并分支搅在一起、须各自在自己基线上取证」——值得成教训，建议新增**（P2 级建议，关联候选-62/-68/-49）。证据充分：review.md F-74-R1-02/F-74-REQ-03（P1 采纳）、progress E-7409「为什么重跑」、DevPlan 机器证 C 基线口径已把它写成卡级规则（「那种轮次只能证明组合版本，无法把绿单独归因给任一张卡」），但该规律只活在 DHR_74 卡内，未进可复用教训区；现有候选（-49 变异证据最终提交重取、-62 失败集合对齐、-68 终态证据最终提交重取）讲的都是「何时取/取什么」，没有一条讲「在哪个分支拓扑上取」。
② **「rebase 掉裹进邻卡改动的合并提交」——不单独成条**，作为 ① 的后续动作并入即可（progress E-7409 基线行已有范式：丢弃 `22dc16c`、留备份 ref `keep/dhr74-pre-rebase-260903`、只挑本卡提交）。可复用的动作要点：剔除合并提交 + 留备份 ref + 在自己基线上重取证。
③ **「运行中降档也要取证」——建议并入候选-40 作补充场景，不另立新候选**（P3 级建议）。理由：候选-40 已裁决 B——模型身份只作诊断登记、不作为复核有效性判据（fresh 实例 + 独立会话为准）；运行中 terra high→luna medium 降档不改变复核有效性，但与候选-40 的「形态取证」是同一纪律的时间维扩展（派发瞬间 ≠ 全程形态）。同时该事件目前只存在于本派单（全 workspace 检索无记录）——按候选-52 纪律应先以 `[工具链]` 条目登记进 progress/整改账，再谈教训。两动作建议一并做：登记事件（候选-52）+ 候选-40 补「运行中形态变更同样取证」一句。

## 范围外观察

- 本枝基线 84f2514 落后 master（`a8b96f4`）——master 已含 DHR_71 verify `e795527` 与教训回流 `abff757`（候选-69~74），本枝工作副本教训库仍停在候选-68；若整改 D 的「rebase 到新 master」尚未执行完毕，收口前需补，否则候选-69~74 判重只能靠 master 版核对（本复核已如此处理）。
- 候选 3/6 里「单文件 2.8 万事件」的表述沿用了 DevPlan 与 findings 口径（指全量门禁中单个测试文件/子进程内的事件量，非单文件孤立运行），与 E-7403a「dhr69 单文件 10/10 绿零放大」不冲突，但表意可再精确。
- evidence 文件名的日期标签（20260902T…）与 progress 行日期（2026-09-03…）相差一天、时刻一致，疑似时区/日期标注口径问题，属账目卫生，不影响本教训复核结论，未展开。

## 问题清单

- **无 P0/P1。**
- P2-1：候选 6 负载条件自相矛盾并混入 F-7401 放大语境（LESB-01）。
- P2-2：候选 5 证据指针「E-7408 日志留痕」不实，仓内唯一文字留痕已被整改压缩删除（LESB-02）。
- P2-3：候选 2 必要性未证仅存依据列、候选 1 标题保留超证据的「机器空闲」前提（LESB-03）。
- P2-4：候选 4 缺「怎么判断粒度选对」的操作判据（LESB-04）。
- P2-5（建议项，不替主控裁决）：漏记 ①「各自在自己基线上取证」建议新增候选，②并入其后续动作；③并入候选-40 并按候选-52 补 `[工具链]` 登记。
