<!-- review-lessons-codex.md — DHR_74 教训复核原文（fresh 只读 codex，gpt-5.6-terra high，--sandbox read-only）。
     采集：herdr agent read --source recent-unwrapped；TUI 硬换行原样保留，仅去掉行首缩进与 bullet 前缀。 -->
## 形态自述

- 只读静态教训复核；未运行测试、未写文件。
- 开工时 HEAD：26ccb40e655832118f67f68a9866c30c21dfc46c。这与派单
  称的施工候选 49196f8 不同；以下以派单指定的 DHR_74 工件、固定提
  交差异及原始 evidence 为依据。

## 发现

### F-74-LES-01 (P1) 候选 1 把“有限探针未见异常”写成了可排除的诊
断结论

- 位置：lesson_candidates.md 第 1 条；progress.md 步骤 2、停顿归
  因；findings.md F-7402。

- 事实：E-7405 的 r9/r12/r15 原文确有约 2.8–3.2s 的 LOOP-LAG，并
  记录已包装 fs API 的 slow=0。但 shim 只包装 open()，未包装其返
  回 FileHandle 的 writeFile()、sync()、close()；且未观测 actor/
  Store 写队列的入出队。CPU 采样原始日志相邻约 18–19 秒，不能与数
  秒 LOOP-LAG 窗口作进程级交叉归因。

- 问题：候选的三交叉是有价值的取证起点，却不足以支撑“进程空闲但事
  件循环被饿”或排除写队列/全部 fs/CPU 短争用。它也没有留下阈值、
  探针覆盖面和反例边界。

- 建议去向：候选 1 不应原样入库；改写成“LOOP-LAG 只能证明 timer
  未获调度，fs/CPU/队列须分别按覆盖面出结论”的新候选，并关联候选-
  31、-48、-62。

### F-74-LES-02 (P1) 候选 2 的“同倍率缩放”与实际施工相矛盾，会误
导后续 flaky 修复

- 位置：lesson_candidates.md 第 2 条；progress.md 步骤 5；
  findings.md F-7401；b9619c3。

- 事实：fake-herdr.mjs:49,81-88 证明 agentGet() 每次 poll 消费一
  个 status，故“先证逐 poll 推进”这个前提成立。可是实际 diff 中
  agent-node.test.mjs 两处为 5→20（×4），dhr69-false-
  ready.test.mjs 两个 fixture 为 2→20，而同一 options 的
  doneTimeoutMs / observationLostMs 仍是 60_000，并未 ×10。

- 问题：候选声称配对超时“必须同倍率缩”，而本卡实践恰未遵守；“12
  行 ×10”亦非事实。未经更正即入库会把未来人带向错误的预算语义。

- 建议去向：撤回本条，不进入教训库。若后续重做，应先逐 options 列
  出“poll 频率、超时窗口、状态列表长度、期望 poll 次数”，再用确定
  性护栏验证缩放比例。

### F-74-LES-03 (P2) 候选 3 的放大机制成立，但“治本位置”越过了本
卡可证明范围

- 位置：lesson_candidates.md 第 3 条；progress.md 步骤 3b/5；
  findings.md F-7401。

- 事实：E-7403b 原始输出记录 writeFile=56775、mkdir=28438、
  rename=28404、appendFile=28377；E-7404 记录 writeFile=17650。代
  码也显示 working 每 poll 写 checkpoint，checkpoint 会写工件、追
  加事件并 persistState() 全量 replay 后原子写 state。

- 问题：这支持“低 poll 间隔在真实 driver fixture 中可放大机器级
  fs 负载”，但单次修前/修后对照不能证明 Store 批量持久化或 DHR_72
  心跳节流是已确定的“治本”。把两者写成处方，会越权替高危 Store 变
  更及 DHR_72 作设计结论。

- 建议去向：可改写后作为新候选入 knowledge/教训库-候选.md：要求
  fixture 评估“每 poll 产生的事件、文件操作和持久化次数”，并把“可
  测放大链”与“待授权的生产改造”分栏。DHR_72 只接收负载测量与评估
  条款；Store 仅写入 backlog.md DHR-BL-17 的高危升级候选。

### F-74-LES-04 (P1) 最有复用价值的“复现粒度”教训漏记

- 位置：progress.md 步骤 3a、3b；lesson_candidates.md 缺项。
- 事实：带探针的单文件 dhr69 ×10 为 10/10 绿、零 SLOW；切换五文件
  全量后首轮立即捕获 2.8 万级放大现场。

- 问题：单文件成功不能证明全量门禁下的现象消失；复现协议若没有覆
  盖目标负载、文件集合、并发参数和重复次数，等于未复现。这不是候
  选-31仅要求结论附负载条件、也不只是候选-48的环境假红判定。

- 建议去向：新增候选：“复现必须匹配故障出现的最小充分负载粒度；先
  写明单文件/全量、并发、轮次和预期签名，简化跑法只可作对照，不能
  替代目标协议。”

### F-74-LES-05 (P2) 门禁 verdict 的单位解析误报漏记为独立教训

- 位置：progress.md 步骤 5、机器证 C；lesson_candidates.md 缺项。
- 事实：progress 明载系列脚本因 ms/s 解析 bug 把原始数字满足条件
  的轮次标成 RED；原始 gate stdout/JUnit 才能支持最终 6 轮统计。

- 问题：候选-34关注人工转录与原始证据错位，候选-62关注失败集合而
  非失败数量；本次是“判定脚本自身的单位转换错误”，维度不同。

- 建议去向：新增候选：“门禁 verdict 生成器必须对单位归一化有正反
  例自测，并同时保留原始 exit/pass/fail/wall-clock；脚本 verdict
  与原始数据冲突时先判脚本失效，不得以摘要覆盖原始事实。”

### F-74-LES-06 (P2) 当前候选不足以让下一轮重建 F-7402 现场

- 位置：lesson_candidates.md 全表；progress.md E-7405~E-7408；
  construction.DONE。

- 事实：候选表只引用证据 ID 和概括性结论；未固化冻结复现命令、
  NODE_OPTIONS 注入方式、LOOP-LAG 的 400ms 报警阈值、fs 包装边
  界、CPU 采样实际粒度，以及“六连绿仅限该五文件命令/本机负载”的适
  用条件。

- 问题：下一轮红时无法仅靠候选表区分“停顿重现”“探针未覆盖”“门禁脚
  本误判”和“不同负载协议”。

- 建议去向：候选 1 或复现粒度新候选必须补最小复现包：代码锚点、完
  整命令、负载条件、命中签名、阈值、覆盖盲区、作废重跑规则；不得
  把“内核级机制”写成已证实原因。

## 逐点结论（上面 6 点逐条）

1. 三条候选质量：
    - 候选 1：部分成立。LOOP-LAG 与已包装 fs 操作的观测事实成
      立；“三交叉足以复用”和排除性结论不成立。

    - 候选 2：不成立。fake 逐 poll 推进成立，但“实际改动同倍率缩
      放”的自证与 diff 冲突。

    - 候选 3：部分成立。负载放大链成立；将 DHR_72 / Store 直接定
      为治本位置不成立。

2. 判重与归并：
    - 候选 1 是候选-31（负载条件）、-48（环境结论须证
      明）、-62（基线对齐）的一次同族加强；其中“用户进程前进探
      针”可在改写后保留为新机制。

    - 候选 2 与候选-35/-39/-51不重复，但因自相矛盾应撤回，不应
      以“新候选”计数。

    - 候选 3 不被候选-31、-48、-51、-62覆盖；可改写为新的“fixture
      放大率”候选。

    - 严格按当前文本：0 条可原样入库。改写后有 2 条潜在真新增（候
      选 1 的受限探针协议、候选 3 的放大率机制）；候选 1 同时是旧
      族场景加强，候选 2 撤回。

3. 漏记教训：
    - D:/ 裸路径进 ESM 必须转 file://：值得记录为 Windows/ESM
      实务，但首试输出未入证据，当前只能记“施工日志自述、不可独立
      复核”，不宜作为强教训依据。

    - ms/s verdict 误标：应新增独立候选，非候选-34/-62的简单重
      复。

    - 单文件 10/10 绿、全量首轮现形：应新增“复现粒度必须匹配目标
      负载”的候选，优先级高。

    - HERDR_ENV 为空的派工偏差已正确登记；它受候选-63覆盖，候选-
      52仅补充“派活事故要落工件”。无需另立新候选，后续派单引用候
      选-63即可。

4. 教训与原始证据：
    - 候选 1：E-7405 支持 LOOP-LAG 与“已包装 fs API 未见 SLOW”；
      不支持“fs 全程健康”、写队列未卡、CPU 争用排除。

    - 候选 2：fake 行为受源码支持；E-7401/7408不证明所有参数同倍
      率，反被 b9619c3 diff 反证。

    - 候选 3：E-7403b/7404与源码支持 fs 放大；不支持 Store 或心跳
      策略必为根治。

    - 因此候选中存在把推断写成实证的情形，主要为候选 1、2 和 3 的
      处方部分。

5. 可执行去向：
    - 候选 1：重写后提交 knowledge/教训库-候选.md，关联候选-
      31/-48/-62；DHR_73 卡规格加入“复现停顿时记录 LOOP-LAG、探针
      覆盖面和队列不可观测边界，不得作排除性归因”。

    - 候选 2：撤回；若修订/补证后另起新候选，不借本卡“×10”表述入
      库。

    - 候选 3：改写后提交候选库；DHR_72 卡规格加入“变更 heartbeat/
      poll 时先记录每 poll 事件与持久化 fan-out、比较目标负载前后
      值，不预设节流为根治”。

    - Store 改造：仅在 backlog.md DHR-BL-17 记录“需用户授权的契约
      面升级候选”，不得写成 DHR_74 已裁决的方案。

    - 漏记的复现粒度与 verdict 单位解析：各自新增候选，前者可同时
      作为 DHR_73 输入条款。

6. “教训被结论替代”风险：

   有。F-7402 和六连绿的结论比可操作的现场重建信息更完整。最少须
   补：冻结复现命令及 cwd、NODE_OPTIONS 探针注入命令、LOOP-LAG 阈
   值、fs API/队列/CPU的覆盖与盲区、采样粒度、目标负载定义、命中/
   作废规则，以及“六连绿只适用于指定五文件和指定机器负载”的条件措
   辞。否则下一次红不能可靠比较同一形态。

## 范围外观察

- construction.DONE 和 F-7402 多处使用“全部排除”“fs 全程健康”“门
  禁阻塞解除”等强措辞；这是证据边界问题，已作为本轮教训复核的依
  据，不作验收裁决。

- 本轮未执行测试；所有结论均来自静态代码、提交差异和保留的原始
  evidence。

