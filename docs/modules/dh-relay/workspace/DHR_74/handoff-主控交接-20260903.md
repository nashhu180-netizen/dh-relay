<!-- dh:v1 · workspace/DHR_74/handoff-主控交接-20260903.md — 主控（Claude session）在用户切换 session 前的现场交接。
     下一任主控进场先读本文件，再按指针读工件。本文件只写"现在在哪、下一步做什么、有哪些坑"，不复制工件内容。 -->
# 主控交接 — DHR_74（2026-09-03，用户主动切 session）

## 一句话现场

**DHR_71 已收口合入 master**；**DHR_74 整改已闭合、机器证 C 已在自己基线上重取**，卡停在「**三路窄复核待派**」这一步——派单已写好并提交，复核者刚起就被用户叫停、已全部关闭，**零半截状态**。

## master 上今天发生了什么（按序）

| 提交 | 是什么 |
|---|---|
| `2b5810a` | **B-adjust（用户点选确认）**：DHR_74 时间参数授权按实际做法修订——`herdrPollMs` 统一抬到 **20ms 绝对值**（倍率各文件不同是正常的），配对超时**只在语义是「等满 N 次 poll」时**同比例放大，墙钟上限（dhr69 的 `60_000`）保持不变；同时补机器证 C 的**基线口径**：合格三轮必须跑在本卡自己的基线上，组合分支轮次降级为过程证据 |
| `4781429` | P6 状态行回填（三路复核裁决 / 合并序反转） |
| `84f2514` | **DHR_71 squash 合入**（`relay-core/test/**` 四文件：`bounded-wait.mjs` + 三套件有界等待 + 4 条语义红隔离） |
| `abff757` | DHR_71 E6 教训回流 → `knowledge/教训库-候选.md` **候选-69~74** |
| `e795527` | **`verify(dh-relay): DHR_71 定向回归绿闸验收通过`**（Verified-By: hyf，用户点选「同意收口，合入 master」） |
| `9be1d3b` 等三笔 | dh 体检修复：DHR_74 任务类型标签 `normal→常规`、标签行括号说明降为子项（R34）、允许路径块改成解析器认的纯路径 bullet 形态（R30） |

`dh dh-relay` 现在：**3 失败 / 85 警告**，三条失败全是 `workspace/DHR_74/brief.md` 缺三段——**已在 DHR_74 分支上补好**（`eb70880`），合入 master 后自动清零。

## DHR_74 分支现状（`wt/DHR_74`，worktree 在 `.dh-worktrees/DHR_74`）

- **分支已重建**：从 master@84f2514 起新枝、只挑本卡 10 笔提交，**丢弃了原来把 `wt/DHR_71` 裹进来的合并提交 `22dc16c`**（旧形态备份 ref `keep/dhr74-pre-rebase-260903`）。
- 改动面：`git diff master..HEAD -- relay-core/` = **12 行时间参数 + 探针三件套 + README**，`assert.` 零改动。
- 分支尖：`0a24cb4`（三份窄复核派单）。工作树 clean。

### 已闭合

1. **三路复核**（fresh 只读 codex ×3）17 条发现 → 主控裁决 **15 采纳 / 0 驳回**（P0=0 / P1=9 / P2=6），裁决在 `review.md`，原文在 `review-{code1,req,lessons}-codex.md`。
2. **整改轮 1**（`541e695`）：措辞全线降级到证据边界、账目改正（CPU 采样 18–19s 不是 2s）、探针盲区成文、教训重写 + 补两条漏记 + F-7402 最小复现包。主控补回它压缩时丢掉的三条移交指针（`fe2de18`）。
3. **机器证 C 重取**（`98f2585`，E-7409）：本卡自己基线三轮 `51 pass / 4 skip / 0 fail`、284.2–285.5s、四类签名零命中、三轮 skip 摘要一致。
4. **brief 补三段**（`eb70880`）。

### 待办（下一任主控从这里接）

1. **派三路窄复核**——派单已就绪、已提交：
   - `review-brief-code1-reverify.md`（9 点：整改闭合度 / 冲突重解有没有丢东西 / E-7409 站不站得住 / 有效单测判定 / 越界）
   - `review-brief-req-recheck.md`（8 点：A/B/C 达成度、**这张卡净交付到底是什么、值不值得按标准档收口**）
   - `review-brief-lessons-reverify.md`（8 点：重写后 6 条候选 + 判重要把今天新入的候选-69~74 算进去）
   - 派法：`herdr pane split w2:p1 --direction right --cwd <worktree> --no-focus`，右侧可上下叠多个（**用户 2026-09-03 明确：主控永远在左，右边可多格**）；`herdr agent start <名> --kind codex --pane <P> -- --sandbox read-only --model gpt-5.6-terra -c model_reasoning_effort=high`，prompt 只给派单路径 + 「结论输出到 stdout、不写文件」+ 「自报 HEAD」。
2. 收结论 → 落盘 `review-{code1-reverify,req-recheck,lessons-reverify}-codex.md` → 主控裁决 → 若无 P0/P1 则走收口（E6 教训回流 / E7 as-built 判定 / E9 汇报 / E10 展示区 → **用户对话确认后**才 verify + squash 合入 + 删树）。
3. 收口时**必须带的诚实结论**（不许悄悄变强）：
   - 机器证 A：**未捕获精确 op**，交付的是函数级落点 + LOOP-LAG 机制候选 + 复现率数据；
   - 机器证 B：**只有放大侧红→绿对照**（fs 写 56,775→17,650），停顿侧无对照；
   - **本卡的 ×10/20ms 时间参数改动对绿闸达标的必要性未证**——DHR_71 独立基线（不含本卡改动）三轮 285–289s 同样达标，本卡基线 284–285s，差 ≈1%（数据源：`workspace/DHR_71/progress.md` E-7119 与本卡 E-7409）；
   - F-7402 停顿机制**内核级未钉死**，移交环境侧 A/B（须用户执行）与 DHR_73。

## 坑与纪律（今天踩过的）

- **worker 进场会自 rebase master**（AGENTS.md worktree 纪律）：本卡分支曾含裹着 DHR_71 的合并提交，worker 一 rebase 必撞冲突。派活时要显式说「本轮不要 rebase / 不要合并」，否则它会自己开 rebase 又 abort。
- **codex 账号额度接近上限会中途降档**：整改 worker 启动参数是 `gpt-5.6-terra` + High，跑到一半状态栏变成 `gpt-5.6-luna medium`。**形态取证要看状态栏，不能只信启动参数**；降档后的产出质量本轮尚可（主控逐 hunk 核过），但必须登记。
- **整改 worker 会"压缩"工件**：它改 findings 时把三条移交指针（VOID 重跑规则 / 环境侧 A/B / DHR_73 消费 LOOP-LAG）一并删了。**整改后必须逐 hunk 核，看有没有把信息压没**。
- **dh 体检的解析器很严**：允许路径块必须是 marker 之后逐条**纯反引号路径** bullet（散文写行内即 `bad-path-syntax`）；任务类型人读标签必须是干净的「重核/常规/轻量」，括号说明留同一行会判 `label-marker-mismatch`。
- **`grep`/脚本在本机偶发挂住**（Bash 工具 2 分钟超时，实际脚本已跑完并落了盘）：扫描类操作优先用 Grep 工具，或落成脚本后台跑再读文件。
- **测试是负载敏感的**：跑门禁取证期间不要并发派 worker，否则 BL-17 形态的红会污染归因。

## 指针

- 裁决与整改清单：`workspace/DHR_74/review.md`
- 证据账本：`workspace/DHR_74/progress.md`（E-7401~E-7409）
- 问题：`workspace/DHR_74/findings.md`（F-7401 / F-7402）
- 权威口径：`dev_plan/P6-Herdr多账号执行底座-开发方案.md` §3.2 `DHR_74`（**授权与机器证 C 基线口径 2026-09-03 已修订**）
- DHR_71 收口全貌：`workspace/DHR_71/review.md` 人类签名区 + `progress.md` E-7119 / 收口链
