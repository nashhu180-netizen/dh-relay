<!-- dh:v1 · review-brief — DHR_74 需求方向复核派单。主控写，复核者只读。 -->
# review-brief · DHR_74 · 需求方向复核

## 你是谁 / 不是谁

你是**复核 worker**，不是主控。只读、不改任何文件（codex `--sandbox read-only`）。
不要再派活、不要起子任务、不要回头问用户。只做本轮复核，产出一份结论文本回给主控。

先读仓根 `AGENTS.md`「编排协议段 → 复核 worker」再动手。

**你不审代码实现细节**（那是代码轮 1 的活）。你审的是：**这张卡交付的东西，是不是用户当初要的那件事；口径有没有被悄悄放宽；措辞有没有超出证据。**

## 背景一句话

DHR_71 的绿闸（冻结五文件命令）被一类停顿反复打红（F-7108 / F-7109，backlog `DHR-BL-17`）。用户 2026-09-03 对话确认按标准档立 DHR_74 专卡：**诊断并修复，再重跑 DHR_71 门禁**。施工已收口（`construction.DONE` status: done），自称机器证 A/B/C 全达成、DHR_71 门禁阻塞解除。

## 你要审什么

cwd 已是 worktree `wt/DHR_74`，HEAD = `49196f8`。

| 读什么 | 地位 |
|---|---|
| `dev_plan/P6-Herdr多账号执行底座-开发方案.md` §3.2 `DHR_74` | **唯一权威验收口径**（目标 / 用户授权原文 / 升级条款 / 机器证 A·B·C / 非目标 / 变更范围） |
| `docs/modules/dh-relay/workspace/DHR_74/brief.md` | 施工合同（DevPlan 的只读副本，冲突以 DevPlan 为准——请顺手核两者有没有漂移） |
| `docs/modules/dh-relay/workspace/DHR_74/construction.DONE` | 施工方的交付声明 |
| `docs/modules/dh-relay/workspace/DHR_74/progress.md` | 证据账本 E-7401~E-7408 + 机器证 C 六轮表 + 步骤 1 基线 |
| `docs/modules/dh-relay/workspace/DHR_74/findings.md` | F-7401 / F-7402 |
| `docs/modules/dh-relay/workspace/DHR_74/evidence/` | 原始 stdout / JUnit（抽样核数字，别只信 progress 的转述） |
| `docs/modules/dh-relay/backlog.md` `DHR-BL-17` | 本卡承接的需求源头 |
| `docs/modules/dh-relay/workspace/DHR_71/findings.md` F-7108/F-7109 | 停顿两形态的权威登记（本卡只引用不复制） |

## 逐条要你给结论的点

按 P0/P1/P2/P3 定级。没问题也要明说「看过、没发现」。

1. **机器证 A（停点钉死）达成了吗**。口径原文要求：探针在复现轮打出**停住的精确 op / 路径 / 耗时 / 现场**，F-7108 或 F-7109 至少一种被定位到**函数级落点**；未复现则交付 ≥10 轮 × 2 种负载条件的复现率与对照数据并**如实登记未钉死**。
   - 施工交付的是「函数级落点 `workflow-driver.mjs:319-326` + 机制 = 事件循环整段饿死 + 内核级未钉死」。请判断：这是**达成**、**部分达成**、还是**以机制推断冒充停点钉死**？注意 A 的原文要的是「停住的精确 op」——LOOP-LAG 证明的是「没有任何 op 在跑」，两者是不是同一件事？
   - 兜底条款（≥10 轮 × 2 负载条件）是否也已满足（E-7403a 空载 10 轮 / E-7405 15 轮 / E-7406 8 轮 / E-7407 优先级对照 6 轮）？「2 种负载条件」的第二种到底是什么，有没有如实记录？
2. **机器证 B（修复落地）达成了吗**。口径要求：测试侧修复须**红→绿对照**（修复前复现 / 修复后同条件消失）。
   - 施工交付的对照是 E-7403b（修前 56,775 次 fs 写）vs E-7404（修后 17,650 次）——这是**放大量级**的对照。但**停顿本身**的红→绿对照在哪里？E-7404 那一轮修复后**仍然 11 红含停顿红**（progress 自承）。请判断 B 是否达成，以及 `construction.DONE` 写「机器证 B 达成（部分 + 登记）」的措辞是否诚实。
3. **机器证 C（门禁重跑）达成了吗**。口径：连续 3 轮 skip=4 ∧ fail=0 ∧ ≤370s，零 BL-17 签名，带负载条件措辞。
   - 交付 6 轮。请核六轮原始证据的数字、skip 是否**同一批 S1~S4**、时长、以及「零 BL-17 签名」是怎么扫的。
   - **关键**：机器证 C 是在**合并了 `wt/DHR_71` 之后**的合并分支上跑的（`22dc16c`）。DHR_71 的门禁本来就依赖它自己的隔离改动。请判断：这样跑出来的绿，能不能**同时**充当「DHR_74 修好了」与「DHR_71 门禁达标」两个结论的证据？有没有把两卡的功劳/责任搅在一起、导致 DHR_71 将来无法独立收口？
4. **升级条款有没有被规避**。原文：诊断若证明**必须**改 Store 写路径语义（rename 有界重试 / 持久化超时 / 批量持久化）→ **立即停手**升高危。
   - findings F-7401 明说 `store.mjs:426-428` 每 event 全量 replay 使成本二次增长、是放大链的一环。施工方选择「不改 Store，改夹具时间参数」。请判断：这是**正确地守住了升级条款**，还是**用测试侧参数绕开了本该升级的真问题**（即把「生产侧成本缺陷」降级成了「测试跑得慢」）？给出你的判断和理由——这条是本轮最重要的方向性问题。
5. **非目标有没有被越过**。原文非目标：不改 Store 落盘语义；不动 DHR_72 专属物（`workflow-driver.mjs` 轮询段、`fake-herdr.mjs`、S1~S4 断言重写、共享夹具的**结构与断言**）；不重写语义陈旧用例；不跑真实 Agent；不重跑 DHR_35；不 push、不部署。逐条核（`git diff --name-only 4742b97..49196f8` + `git log origin/master..HEAD` 看有没有 push 痕迹）。
6. **共享夹具的用户授权例外用对了吗**。DevPlan 授权的例外是「共享夹具 `runtimeFixture` / `recoveryFixture` 的 `mkdtemp` 根一行**与上述时间参数行**可动，复核必须**逐字核对仅限这些行**」。请逐字核：实际动的行是否全都落在这个例外里；有没有借例外顺手动了别的。
7. **措辞纪律**。brief 硬规定：没拿到证据不得写「根因已消除」「全绿」；未复现 ≠ 已修复；结论必带条件措辞（F-71-LES-02）。请扫 `construction.DONE` / `progress.md` / `findings.md` 的每一句强主张，列出**证据撑不住的措辞**（尤其「DHR_71 门禁阻塞解除」「四个替代解释全部排除」「机器证 A 达成」）。
8. **移交与待办是否可执行**。`construction.DONE` 列了 6 条移交（复核 / DHR_71 合并序 / DHR_72 心跳节流 / Store 批量持久化升级候选 / 环境侧 Defender 实验 / backlog BL-17 标 partially-resolved）。请判断：每条的**接收方是否明确**、有没有会掉地上的（特别是 F-7402 的内核级归因——它是被合理移交，还是被悄悄丢弃）？backlog `DHR-BL-17` 现在的文字与本卡结论是否一致。
9. **需求境证据（标准档硬闸）**。标准档进「待验收」前必须有需求境证据（需求 / 人验项 + 场景操作路径 + 证据 ID + 结论）。本卡是诊断修复卡、无 UI。请给结论：需求境证据是否具备、以什么形式具备（门禁六轮 + 探针现场算不算）、还缺什么才能让主控向用户申请验收。
10. **卡序与影响面**。本卡不得改变 D-B35-1 的卡序 71→72→73，且 findings 要喂 DHR_73 而不替代它。请核结论有没有暗中吃掉 DHR_73 的调查范围（F-7402 声称「DHR_73 的 F-3516/F-3519 与本形态同族的概率高」——这句是提示还是预判结论？）。

## 硬边界

- **只读**、仅静态审；跑不了测试就如实申报。
- 只写事实与级别，**不替主控做验收裁决**（不要写 approved/rejected）。
- 范围外新想法写「范围外观察」。
- 密钥 / 凭据值、原始 Receipt ID 零出现。

## 产出格式

完整结论输出到 stdout（主控落盘为 `review-req-codex.md`）：

```
## 形态自述
## 发现
### F-74-REQ-01 (P?) 标题
- 位置：文件 / 行 或 口径条目
- 事实：
- 与口径的偏差：口径要求什么 → 实际交付什么
- 依据：
## 逐点结论（上面 10 点逐条）
## 范围外观察
```
