<!-- dh:v1 · review-brief — DHR_74 教训 · 窄复核（只审重写后的候选与漏记补齐）。主控写，复核者只读。 -->
# review-brief · DHR_74 · 教训窄复核（reverify）

## 你是谁 / 不是谁

只读复核 worker（codex `--sandbox read-only`）。不派活、不起子任务、不回头问用户。
先读仓根 `AGENTS.md`「编排协议段 → 复核 worker」。

**这是窄复核**：轮 1 的六条（F-74-LES-01~06）主控已全部采纳，`lesson_candidates.md` 已按其建议重写。你只审**重写后的候选**。

## 必读

| 读什么 | 是什么 |
|---|---|
| `workspace/DHR_74/lesson_candidates.md` | **主审对象**（重写后 6 条） |
| `workspace/DHR_74/review-lessons-codex.md` | 轮 1 原文（你的前任的六条要求） |
| `workspace/DHR_74/review.md` | 主控裁决与整改清单 F |
| `knowledge/教训库-候选.md` | 既有候选-1~**74**（**注意：候选-69~74 是今天 DHR_71 收口时新入的**，判重要把它们算进去） |
| `workspace/DHR_74/progress.md` | 证据账本 E-7401~**E-7409**（新增独立基线三轮） |

## 本轮新增的事实（会影响教训措辞，必须纳入判断）

- **DHR_71 独立基线（不含本卡任何改动）连续三轮达标**（`51/4/0`、285–289s；DHR_71 progress E-7119），本卡基线为 284–285s——**差异 ≈1%**。
- 即：本卡「poll 统一 20ms」的时间参数改动，**对绿闸达标的必要性未证**；放大侧指标（fs 写 56,775→17,650）确有下降。

## 逐条要你给结论的点

按 P0/P1/P2/P3 定级，没问题也明说「看过、没发现」。

1. **重写后的候选 1（「机器空闲 ≠ 进程在前进」）**：轮 1 要求它收窄成「LOOP-LAG 只能证明 timer 未获调度，fs/CPU/队列按各自覆盖面出结论」并记下阈值与反例边界。现文做到了吗？阈值（1s/5s/15s、drift>400ms）与盲区清单是否**可被下一个人直接复用**？
2. **重写后的候选 2（时间参数按语义拆分）**：轮 1 判它「不成立、应撤回」。现文改成「只有『等满 N 次 poll』的超时同比例放大，墙钟上限保持不变」。请核：① 它与本卡**实际改动**现在一致了吗；② 它与 DevPlan 修订后的授权文字一致吗；③ 它有没有把「必要性未证」这个新事实吸收进去——教训里若暗示「这么改能治 flaky」，就还是会误导后人。
3. **重写后的候选 3（夹具是放大器）**：是否已去掉「治本位置在 DHR_72 / Store」的越权处方？现在的「治本候选需各自立卡验证」措辞够不够。
4. **新增候选 4（复现粒度必须匹配目标负载）**：轮 1 说这是最值钱的一条。现文够不够硬——有没有写清"单文件 10/10 绿、五文件全量第 1 轮即现形"这个具体对照，以及**怎么判断粒度选对了**。
5. **新增候选 5（判定脚本单位解析误报）**：够不够独立成条（轮 1 判它非候选-34/-62 的重复）；有没有给出可执行的防线（"判定脚本先用已知样本自证"）。
6. **候选 6（F-7402 最小复现包）**：轮 1 的 LES-06 要求「代码锚点 / 完整命令 / 负载条件 / 命中签名 / 阈值 / 覆盖盲区 / 作废重跑规则」七项齐全。请逐项核，缺哪项直接点名。
7. **判重（把今天新入的候选-69~74 算进去）**：本卡 6 条与 `教训库-候选.md` 现有条目（尤其候选-31 / -35 / -48 / -51 / -62 与今天新入的 **候选-70「进程跑完却不退先看活跃句柄」**、**候选-73「等待上限逐段推导且方向不能反」**、**候选-74「skip 不是豁免是记账」**）有无重复或应归并？请给「新增 / 归并进候选-N / 已被覆盖应撤」的逐条建议。
8. **还有没有漏记**（本轮新素材）：① 「两张卡的证据被合并分支搅在一起、必须各自在自己基线上取证」这条，值不值得成教训？② 「rebase 掉一个把别人的卡裹进来的合并提交」这个收口动作呢？③ 派出的整改 worker 中途**被账号额度降档**（terra high → luna medium）而主控是靠状态栏才发现的——形态取证类教训（既有候选-40 讲的是派发形态取证）要不要加一条"运行中降档也要取证"？

## 硬边界

只读、仅静态审；只写事实与级别，**不替主控决定教训是否入库**（给建议即可）；范围外想法写「范围外观察」；密钥 / 凭据 / 原始 Receipt ID 零出现。

## 产出

完整结论到 stdout（主控落盘为 `review-lessons-reverify-codex.md`）：形态自述 / 发现（`F-74-LESB-NN`）/ 逐点结论（上面 8 点）/ 范围外观察。开工前自报你看到的 git HEAD。
