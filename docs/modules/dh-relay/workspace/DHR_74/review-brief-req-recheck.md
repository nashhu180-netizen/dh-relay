<!-- dh:v1 · review-brief — DHR_74 需求方向 · 窄复核（只重核 A/B/C 达成度表述与收口资格）。主控写，复核者只读。 -->
# review-brief · DHR_74 · 需求方向窄复核（recheck）

## 你是谁 / 不是谁

只读复核 worker（codex `--sandbox read-only`）。不派活、不起子任务、不回头问用户。
先读仓根 `AGENTS.md`「编排协议段 → 复核 worker」。

**这是窄复核**：轮 1 的五条发现（F-74-REQ-01~05）主控已全部采纳。你只回答一个问题：**整改与新证据落地后，这张卡现在有没有资格进"待验收"、以及它对外声称的达成度还有没有虚的。**

## 这半天发生了什么（时间线，读之前先建立）

1. 三路复核 → 主控裁决 15 条采纳 / 0 驳回（`review.md`）。
2. 用户点选确认两件事：① DevPlan 的时间参数授权按**实际做法**修订（poll 统一 20ms 绝对值，配对超时只在「等满 N 次 poll」语义下同比例放大）；② 合并序改为 **DHR_71 先收口 → DHR_74 rebase 后重跑**。
3. 主控实测：**DHR_71 独立基线（不含本卡任何改动）连续三轮达标**（`51/4/0`、285–289s）→ 本卡的时间参数改动**对绿闸达标的必要性未证**。
4. DHR_71 已 squash 合入 master（`84f2514`）并 verify 收口（`e795527`）。
5. 本卡分支**重建**：从新 master 起新枝、只挑本卡 10 笔提交，**丢弃了原来把 DHR_71 裹进来的合并提交**。
6. 整改轮 1 闭合（`541e695` + 主控补回被压缩丢失的移交项 `fe2de18`）。
7. 机器证 C 在本卡自己基线上重取：三轮 `51/4/0`、284–285s、四类签名零命中（`98f2585`，E-7409）。

## 必读

`workspace/DHR_74/` 下：`review-req-codex.md`（你的前任）、`review.md`（裁决）、`remediation-brief-r1.md`、`remediation1.DONE`、`brief.md`（**刚补了三段**）、`progress.md`（E-7401~E-7409 + 需求境人验项）、`findings.md`、`construction.DONE`；
`dev_plan/P6-...md` §3.2 `DHR_74`（**授权与机器证 C 基线口径均已修订**）；`workspace/DHR_71/progress.md` E-7119（横比数据源）。

## 逐条要你给结论的点

按 P0/P1/P2/P3 定级。没问题也明说「看过、没发现」。

1. **F-74-REQ-01（机器证 A）是否闭合**。现在 `construction.DONE` 写「未达成『精确 op』，按兜底条款登记」。请核：① 兜底条款要求的「≥10 轮 × **2 种负载条件**」到底满没满足——现有是空载 10 轮、追猎 15 轮、CPU 采样 8 轮、High 优先级 6 轮，**第二种负载条件是否被明确定义并记录**？② 若仍不满足，缺口是"再跑一组"还是"如实登记未满足"？
2. **F-74-REQ-02（机器证 B）是否闭合**。现在写「放大侧红→绿对照成立、停顿侧无对照、必要性未证」。请判断：这个表述**是否已经诚实到位**，还是应当进一步说成「B 未达成」？口径原文要的是"修复前复现 / 修复后同条件消失"——放大量级的对照算不算这条要的"修复"？
3. **F-74-REQ-03（机器证 C）是否闭合**。E-7409 三轮跑在重建后的本卡基线上。请核：① 原始证据数字与 progress 转述是否逐条对得上；② 「同时记入 DHR_71 证据账」这条 DevPlan 要求现在怎么处理的（DHR_71 已收口，E-7119 是它自己的证据；本卡 E-7409 有没有该回填到 DHR_71，还是已因收口而不必）；③ 签名扫描现在有可复核命令与「空扫自证」，够不够。
4. **F-74-REQ-04（brief 漂移）是否闭合**。`brief.md` 刚补了「覆盖任务 / 完成条件 / 边界」三段并同步了修订后口径。请逐段核它与 DevPlan §3.2 是否一致、有没有把 DevPlan 没有的东西写进 brief（brief 是只读副本，多写等于新增授权）。
5. **F-74-REQ-05（需求境证据）是否闭合**。progress 末尾新增了人验项。请判断它够不够支撑标准档进「待验收」：有没有说清**在哪个基线、跑什么命令、预期什么、哪些口径仍未达成、请用户确认什么**。
6. **这张卡现在到底交付了什么**（本轮最重要）。给一个诚实的一句话结论：在「时间参数必要性未证、精确 op 未捕获、停顿机制未钉死」之后，本卡的**净交付**是什么（探针工具？诊断证据？放大量级下降？门禁基线口径？），以及它**值不值得**按标准档收口——还是应当降级为「诊断产出 + 工具入库」并把未闭合项显式转卡。
7. **升级条款是否仍守住**。`store/**`、`runtime/**`、`contracts/**`、`package.json` 零改动是否仍成立（分支重建后重新核一遍）。
8. **卡序与影响面**。本卡结论有没有吃掉 DHR_73 的调查范围；F-7402 的移交项（VOID 规则 / 环境侧 A/B / DHR_73 消费 LOOP-LAG 证据）是否都有接收方。

## 硬边界

只读、仅静态审；只写事实与级别，**不做验收裁决**；范围外想法写「范围外观察」；密钥 / 凭据 / 原始 Receipt ID 零出现。

## 产出

完整结论到 stdout（主控落盘为 `review-req-recheck-codex.md`）：形态自述 / 发现（`F-74-REQB-NN`）/ 逐点结论（上面 8 点）/ 范围外观察。开工前自报你看到的 git HEAD。
