"deepseek-v4-flash" is not a model this version of Claude Code recognizes, so auto-compact will keep this session within 200k tokens (the context window it assumes). If the model accepts more, append [1m] to the model name for 1M, or set CLAUDE_CODE_MAX_CONTEXT_TOKENS to its real window; to make it recognized, map it in the modelOverrides setting or update Claude Code; CLAUDE_CODE_DISABLE_UNKNOWN_MODEL_WINDOW_ENFORCEMENT=1 restores the previous wait-for-the-API behavior.
[claude-code:unrecognized_model] {"model":"deepseek-v4-flash","query_source":"generate_session_title"}
复核完成。所有探针均在 `525aa61` 的临时导出副本上进行，仓库工作树零改动（`git status` 全程只有施工方自己的批次 C/D 未提交文件，无我引入的任何痕迹）。E-003 证据已按**提交态**（非当时含批B改动的脏工作树）复核：恰好 18 条断言、SUITE PASS、exit 0，与 progress 记载一致。

---

1. **VERDICT: changes-requested**

2. 发现清单

- **P2 - 枚举匹配大小写不敏感，冻结枚举 fail-closed 失效** - `tools/relay/contracts/relay-schema.ps1:55-58` (Test-RelayEnumValue) - 探针实证：`Test-RelayEnumValue @{e='Worker'} 'e' @('worker','reviewer','replanner')` → `ok:true`（PowerShell `-notin` 默认不区分大小写）。`"role":"Worker"`、`"proposed_by":"Orchestrator"` 等大小写变体均可通过，而冻结表枚举是精确 token（`enum{worker,...}`），违反「未知枚举→unknown-enum:<名>」的 fail-closed 规则。影响 role/next_action/proposed_by/backend（批B 的 result_status/status/kind 同用此助手，隐患延续）。作者对 plan_hash 刻意做了 `ToLowerInvariant`（hex 大小写等价，正确），但枚举未做精确匹配。无任何断言覆盖该路径。
- **P2 - 嵌套 node 校验零断言钉住，套件对 Test-RelayNode 的破坏完全不敏感** - `tools/relay/tests/relay-contract-schema.ps1` / `relay-schema.ps1:98-121` - 变异探针实证：把 node role 枚举改为 `@('worker','reviewer','replanner','alien')` 后套件**仍全绿（0 FAIL）**。现有 18 条断言只覆盖计划顶层字段；node 的 role/next_action 枚举拒绝、node 内未知字段、resume_from 结构、depends_on 元素类型、`duplicate-node`、`unknown-dep` 均无正反例——即便整个 Test-RelayNode 被删，'good plan accepted' 等全部断言依旧通过（计划级正例不依赖节点校验真实执行）。
- **P3 - iso 校验 accept 非时间戳字符串** - `tools/relay/contracts/relay-schema.ps1:48-50` (Test-RelayIsoValue) - 探针实证：`'2026-08-15'`（纯日期）与 `'09:00'`（纯时间）均 `ok:true`（DateTimeOffset.TryParse 放行）。冻结表类型为 `iso`，proposed_at/granted_at/issued_at 若只写日期也能过校验（格式收紧处 fail-open）。
- **P3 - badhash fixture 与 A3 构造规格不符** - `tools/relay/tests/fixtures/plans/plan-v1-badhash.json` - A3 规定「内容同 good 但 plan_hash 改一位」，实际填 64 个 `f`（整串替换而非单字符变异）；语义等价（仍走到 hash 比对），属施工漂移。附带：cycle/unknown-field 两个 fixture 的 plan_hash 全零，依赖「环/字段检查先于 hash 比对」的检查顺序才不被误拒——当前顺序正确，但此脆弱性无测试锁定。
- **P3 - A3 要求「先用 Get-RelayPlanHash 算出回填，progress 记一笔」，progress 未记该步骤** - `docs/modules/dh-relay/workspace/DHR_01/progress.md` - 新增行只写「先因契约库不存在跑红，再实现转绿」，无 hash 回填记录；证据缺口，非功能问题。

范围核查通过：commit 只动 `tools/relay/**` 与 workspace 文档，无越界文件；fixtures 全假值（RUN-FAKE-001 / FAKE-A.md / 全 f 哈希），无真实凭据；E-003「18 assertions / SUITE PASS / exit 0」对提交态逐条复核成立。

3. 探针记录（全部在 `/tmp/dhr01-batchA`（525aa61 导出副本）上变异，仓库零改动；每轮后复原并复跑确认恢复 SUITE PASS，最终重新导出纯净态复核通过）

- 探针①：注释掉 Test-RelayPlanProposal 的环检测（`if ($false -and (Test-Cycle ...))`）→ `cycle rejected` 恰好变红，其余 17 条不动 → 已复原。
- 探针②：把 Test-RelayPlanProposal 顶层 allowed-fields 检查换成恒 ok → `unknown plan field rejected` 与 `proposal generation rejected` 两条同时变红（均依赖该检查），无第三条 → 已复原。
- 探针③：把 plan_hash 重算比对改为恒通过 → `bad hash rejected` 恰好变红（good plan 正例仍绿——正/反两条合起来才钉住该逻辑）→ 已复原。
- 探针④：node role 枚举加入 'alien' → **0 变红，套件全绿**（证明 Test-RelayNode 无断言钉住，见 P2-2）→ 已复原。
- 附加值域探针（无变异）：date-only/time-only ISO 被接受；'Worker' 枚举被接受；1.5 / double 1.0 / $null 均拒（bad-type）；大写 64-hex 哈希被接受（与 ToLowerInvariant 比对自洽）；纯空白字符串拒；int64 最大值通过。

另注：复核期间 HEAD 从 525aa61 推进到 b2884c9（批B 已提交、批C/D 在树内进行中），本报告所有断言与探针均针对 525aa61 提交态，未受工作树后续改动影响。
