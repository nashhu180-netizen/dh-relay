<!-- dh:v1 -->
# progress — DHR_52

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|---|---|---|---|---|
| 2026-08-22 | 主控 | 用户对话明确确认 DHR_52 标准档高危开工；S0~S2 落户。施工后端指定为 OMP `deepseek-v4-flash`，精确限权。 | DevPlan DHR_52 行；`execution_strategy.md` | 从本地 master 建任务 worktree，派施工 worker |
| 2026-08-22 | 主控 | 从含 DHR_52 户口本的本地 master `59590d8` 建隔离 worktree `D:\MyFiles\ai-workflow\dh-relay\.dh-worktrees\DHR_52`，分支 `wt/DHR_52`；未使用落后 16 提交的 `origin/master`。 | `git worktree list --porcelain`；E-001 | 派 OMP 施工 worker 执行 task_plan 步骤 1~6 |
| 2026-08-22 | 主控 | OMP `deepseek-v4-flash` 首次全量施工会话在约 11 分钟内持续占用 CPU 但零流式输出、零代码产物；按 20 分钟上限前的可观测性止损中止。范围审计只见本文件的主控记录。 | `Get-Process omp`；`git diff --name-only` | 改为可观察的短回合、单步骤 worker 调用 |
| 2026-08-22 | worker | 施工步骤 2：建 `relay-core/rpc/capabilities.mjs`（读/复算权威快照 `capability-baseline.json`，复算走 `tools/canonical.mjs` 的 `digest`，不另立 canonicalization；严格相等比较 peer hash，不同即 `E_CAPABILITY_MISMATCH`）与 `relay-core/test/rpc.test.mjs`（基线平等 + 不匹配拒绝 5 用例）。未触碰 transport/server/subscribe/F-057。 | `node --test test/rpc.test.mjs` → tests 5 / pass 5 / fail 0 | transport/server/subscribe/F-057 由后续棒施工 |
| 2026-08-22 | 主控 | 回收后独立范围审计：新增文件仅在已授权 `relay-core/rpc/`、`relay-core/test/`，过程账仅 `progress.md`；独立复跑 5/5。 | `git status --short`；`node --test test/rpc.test.mjs` → 5 pass / 0 fail | 派 transport 短回合 |
| 2026-08-22 | worker | 修复已确认的传输生命周期缺陷：`createTransportServer(...).close()` 原先只 `await server.close()`，客户端仍连着（服务端 socket 未关）时永久挂起。修复：server 用 `acceptedSockets` Set 跟踪本 server 接受的在线 socket，close() 先逐一 `destroy()` 再 await `server.close()`；清理仅限本 server 自己接受的连接。另修两处测试写序竞态（partial 写入 / 坏 JSON 用例在 client connect 同一 tick 内、服务端 accept 前写 socket，Windows named pipe 上数据被丢弃 → `until` 超时）：改为等 `onConnection`（服务端 accept）后再写，与往返用例同一确定性模式。 | `node --test test/rpc.test.mjs` → tests 17 / pass 17 / fail 0 / duration_ms 507.808（首跑）、397.8317（复跑），完整 summary 且 exit 0 | server/subscribe/F-057 由后续棒施工 |
| 2026-08-22 | 主控 | 独立复跑 transport 后的 `rpc.test.mjs`：完整汇总 17/17、exit 0、421.7965ms。此前残留的 `node --test` 是早一轮失败 worker 遗留，不是 OMP 仍在思考；已只终止该已识别进程树。 | E-009 | 启动 OMP xhigh 的 15 分钟 B 档 server/subscribe 施工与可观测性记录 |
| 2026-08-22 | 主控 | B 档以 `--mode json --print-thoughts` 运行满 15 分钟；结构化事件流持续出现 `thinking_*` 与读取/工具调用，证明 worker 在进行模型推理和检索，不是仅存活。但到 CLI `Deadline exceeded` 时，受限代码路径无新增 server/subscribe 产物。 | E-010；`git diff --name-only` | 以同一小范围重写 30 分钟 C 档 brief，要求先落最小可测 server 骨架，再迭代订阅；观察窗只做状态检查、不自动杀进程 |
| 2026-08-22 | worker | 施工步骤 4：建 `relay-core/rpc/server.mjs`（`createRpcServer({runId,store,capability,endpoint,handlers})`：信封校验走 `tools/validate.mjs` 的 `validateOne`（短 id `relay.rpc/v1`，只报既有 reason）；capability 严格比对走 `capabilities.mjs` 指纹（形态合法但不等 → `E_CAPABILITY_MISMATCH`，不降级）；只分派 `handlers[method]`；断连只清连接本地记账，不碰 Store/不发 cancel。传输坏帧 → `E_TRANSPORT_FRAME_INVALID`、信封不合 → 校验器既有码、未实现方法 → `E_UNKNOWN_METHOD`，零新增 reason code）并扩展 `relay-core/test/rpc.test.mjs`（6 条真实 named-pipe/UDS 握手用例，先 accept 后写）。环境：worktree 缺 `node_modules`，按既有 `package-lock.json` 跑 `npm ci`（仅生成忽略目录，未改 package 文件）。**TODO（按 brief 允许）：`subscribe` 未实现**，确定性回既有 `E_UNKNOWN_METHOD`，事件/runStateChanged 推送与订阅状态留后续棒。 | E-011 | subscribe/推送/F-057 由后续棒施工 |
| 2026-08-22 | 主控 | C 组独立验收最小握手路径：`git diff --check` 无空白错误，精确范围审计无越界；主控独立复跑 23/23。静态检查发现 server 为获得“连接级回包”而移除了 transport 预装的全部 `data` listener；虽当前测试通过，连接归属不该靠 `removeAllListeners` 这种耦合方式，须先改成显式的连接级 handler seam，再扩 subscribe。 | E-012 | 修 transport/server seam 并加回归；再施工 subscribe |
| 2026-08-22 | worker | 施工步骤 4.1（seam 修正，E-012 落单）：`transport.mjs` 增连接级 handler seam——`createTransportServer` 可选 `createConnectionHandlers(socket)` 按连接生成 handlers（帧/错误/关闭回落具体连接），未提供工厂时回退共享 handlers（既有调用方行为逐字节不变）；`createTransportConnection` 回调签名扩为 `onFrame(frame, conn)`/`onFrameError(err, conn)`/`onClose(conn)`/`onSocketError(err, conn)`（close 唯一参即连接对象），只用一个参的既有调用方不受影响。`server.mjs` 删除 `conn.socket.removeAllListeners('data')` + 自建 framer 接管 hack，改用 seam 获得连接本地 onFrame/onFrameError/onClose；传输层 framer 保持原位，close 语义与严格 capability/JSON-RPC 拒绝路径零改动（`handleFrame` 未动）。测试 +2 条：传输 seam（工厂产物帧/关闭回落具体连接）+ 服务端并发连接（交错请求/坏帧/断连各回各的连接，真实 named-pipe/UDS，不依赖监听器移除）。 | E-013 | subscribe/推送/F-057 由后续棒施工 |
| 2026-08-22 | worker | 施工步骤 4.2：`server.mjs` 实现 subscribe 连接本地 seam——`handlers.subscribe(params, sink)` 注入契约（内部 seam，非正式客户端 Read Model）：sink.event / sink.runStateChanged 先对冻结 `relay.rpc/v1` 校验完整通知帧，无效载荷（交叉协议 / 缺 protocol / 未知字段）fail-closed 不发并返回 false，有效载荷逐字原样发送返回 true；subscribe 可返回 `{ result, unsubscribe }`，result 回客户端，unsubscribe 经 `connectionSubscriptions` Map 挂到连接记账，客户端断连（含服务端 close 销毁在途 socket）时对每个订阅恰调用一次，删账后迟到 resolve 的 subscribe 立即自行清理（互斥保证恰一次）；断连仍只清连接本地状态，不写 Store、不发 cancel。测试 +3 条（真实 named-pipe/UDS，先 accept 后写约定）：合法 event/runStateChanged 推送 + 通知帧复验冻结信封；三种无效载荷反例 fail-closed（零帧 + 后续有效载荷顺序确定）；断连 unsubscribe 恰一次 + Store 零调用 + 零 cancel。F-057 未裁决：交叉协议落既有 `E_BAD_VALUE`（与 negative 夹具期望一致），零新增 reason code，contracts 与基线零改动。 | E-014 | F-057 裁决与收口由主控/后续棒处理 |
| 2026-08-22 | worker | 施工步骤 4.3（订阅生命周期回归修复）：`server.mjs` 的 `connectionSubscriptions` 只在 subscribe 解析处被读、从未在连接建立时登记——存活连接的 subscribe 解析 `get(conn)` 恒为 undefined，直接走「连接已关」else 分支把 unsubscribe 立即误调一次（响应都还没回客户端）；断连路径也永远找不到账目。修复：`onConnection` 里 `connectionSubscriptions.set(conn, [])`，账目条目随连接生命周期（建立即登记空表、断开即删），subscribe 解析以条目是否存在判定连接存活，与 onClose 删账互斥，unsubscribe 总被恰好调用一次。测试：新增确定性真实传输回归用例——响应返回后 / sink 推送送达后 / 再等 20ms 后均显式断言 unsubscribe 零调用，断开后 `until` 等到恰一次、Store 零调用、不重调 subscribe；修复前该用例在「响应返回后」即红（1 !== 0）。另修一处既有用例的隐性时序依赖：`subscribe 注入 seam` 用例尾部在 `handle.close()` 后同步断言 unsubscribe 已调，实际断开清理经 socket close 事件异步到达（server.close 回调先于连接级 onClose 触发）——修复前该断言靠误调蒙混过关，改为 `until` 等齐再断言。契约 / reason code / Runtime / package 零改动。 | E-015；E-016 | F-057 裁决与收口由主控/后续棒处理 |
| 2026-08-22 | 主控 | 恢复 DHR_52 后独立复跑当前 worktree：RPC 定向、既有 core 回归以及 validate/audit/fixture/capability 五道机器闸均通过；仅读取与执行，未触及冻结 contracts、reason codes 或三份基线。F-057 仍待用户对话内裁决，不能把局部绿灯记为完成。 | E-017；E-018 | 等待 F-057 的明确「新增」或「维持既有码」裁决，再按对应分支推进收口 |
| 2026-08-22 | 主控 | 用户在对话中明确裁决 F-057「新增」。`E_PROTOCOL_MISMATCH` 的窄边界固定为：完整且可识别的另一已冻结协议对象被发到错误协议位置；缺字段、未知字段、`E_UNSUPPORTED_VERSION` 与本协议内普通坏值维持既有码。 | 用户对话明示「新增」；F-201 | 更新冻结契约三件套、重生成基线并复跑机器闸 |
| 2026-08-22 | 主控 | 已按 F-057 裁决完成冻结契约三件套：`reason-codes.md` 新增新码及四类排除边界；校验器仅在错误位置的完整对象独立通过另一已冻结顶层协议时映射新码，v0 shape/shared 定义不触发；新增反向反例，原反例期望改为新码。fixture manifest 重生为 57 份；capability baseline 与结构 token 清单重生成后语义零漂移（未改 JSON Schema，hash 保持 `970b54601ae582a5…`）。 | E-019；E-020；E-021 | 进行独立复核、需求/教训/一致性复核及 as-built/miner 收口备料 |
| 2026-08-22 | 主控 | 自动收口 E0：`dh dh-relay` 复现 42 项模块体检失败；其中设计输入与历史卡为卡外存量，DHR_52 自己的 review 模板占位/证据字段已补齐后待复跑。按用户指定后端连续三次启动 OMP `deepseek-v4-flash` / `thinking=xhigh` 只读复核；均出现受限运行但在预算内未给出可验结论，主控终止，不能登记为任何轮次复核通过或 finding。 | E-022；E-023 | 等可回收的独立复核结论；不得以 OMP 思考/运行状态替代 E2/E4/E5/E14 |
| 2026-08-22 | 主控 | 自动收口 E6：已运行只读 `dh mine dh-relay DHR_52`。候选区已有 14 条待裁决候选，其中 reason-code/分支断言相关候选-1、边界变异候选-6、同类覆盖面候选-11 与本卡有关；因独立复核尚未产出 P0/P1 结论，未新增候选，避免把运行噪音当教训。 | E-024 | 复核结论可回收后按 miner 去重口径决定是否追加候选 |
| 2026-08-22 | 主控 | 用户授权 Claude Opus 后按机器只读形态尝试独立代码复核：默认 profile 的模型选择钩子未进入复核；独立 `claude-account9` profile 到达 API 但返回 402。二者均无 `approved` / `changes-requested` 结论、无仓库写入，E-025 仅为派出记录。 | E-025；E-026 | 等待可用的 Claude Opus 只读后端或用户另行授权替代 reviewer |
| 2026-08-22 | 主控 | 用户澄清应使用默认 `.claude` 后，Claude Opus 5（high effort、禁 Write/Edit/NotebookEdit/Bash、strict empty MCP）实际只读检索 18 个目标文件；因持续扩展检索且未输出 `approved` / `changes-requested`，中断后仍无结论。复查 worktree 状态仅见既有变更，`git diff --check` 无错误。 | E-027 | 该运行不计任何复核轮次；仍待可回收的独立 reviewer 结论 |
| 2026-08-23 | 主控 | 用户授权后，`codex-ninth` 的全新 `gpt-5.6-sol` 实例以启动横幅 `sandbox: read-only` 完成静态复核；先前经 PowerShell `codex` function 的尝试实际是 `danger-full-access`，已中止且不计轮次。正式结论为 `changes-requested`：2 个 P1、2 个 P2。 | E-058；E-059 | 等待用户明确授权本轮返工；不得以旧机器绿灯或旧 approved 收口 |
| 2026-08-23 | 主控 | 用户授权仅返工 F-219~F-222、补回归并复核；已修超长未终止帧 discard-until-LF、原对象 JSON 安全预检与冻结校验、通知写入背压断连、以及启动时冻结协议集合区分未来同名版本。新增 4 条定向回归；未改冻结 contracts / reason codes / 基线。 | E-060；E-061 | 按授权派新鲜独立只读复核；不提交、不合并、不 verify |
| 2026-08-23 | 主控 | 新鲜 OS 只读复核确认 F-219~F-222 未再提出 P0/P1，但新发现 P2 F-223：数组的非索引自有属性（如 `toJSON`）可绕过当前原对象预检并在序列化时改变帧。因本轮授权只列 F-219~F-222，未实施该新增修复。 | E-062；E-063 | 等待用户明确授权 F-223 修复与回归；不得以当前 P2 未闭合声明复核通过 |
| 2026-08-23 | 主控 | 用户授权 F-223 后，数组 JSON 预检改为逐项读取 data descriptor，并拒绝 array accessor、稀疏项与任何非索引自有键（含 `toJSON`）；补通知和 response 两条真实 socket 回归。 | E-064 | 派发新鲜独立只读复核；不提交、不合并、不 verify |
| 2026-08-23 | 主控 | 新鲜 OS 只读复核给出 `changes-requested`：F-223 的原对象预检与后续原对象 `JSON.stringify` 之间仍存在 TOCTOU，Proxy/自定义数组原型可改变输出，升级 P1；另有 F-224 回归覆盖缺口。未实施超出本轮具体授权的新方案。 | E-065；E-066 | 等待用户明确授权：构造 descriptor-derived plain snapshot 后校验/序列化，并补 Proxy/custom prototype/accessor/extra-property 回归 |
| 2026-08-23 | 主控 | 用户授权后，JSON 预检改为递归构造仅含 data descriptor 的 plain snapshot；冻结 schema 校验与后续传输均使用 snapshot，不再序列化生产者原对象。补 Proxy 隐藏 `toJSON`、自定义数组原型 `toJSON`、数组 accessor 与额外属性回归，覆盖 notification 和 response。 | E-067 | 派发新鲜独立只读复核；不提交、不合并、不 verify |
| 2026-08-23 | 主控 | E5/E14/E6 自动收口：正册教训库为空，候选仅作去重参考；一致性横向比对 capability、reason 分类、跨边界 snapshot 与订阅生命周期共 13 处；`dh mine dh-relay DHR_52` 成功并备得两条候选。均无 P0/P1/P2。 | E-074；E-075；E-076 | E7 as-built、E8/E9 证据包与 E10 展示 |
| 2026-08-23 | 主控 | E7 as-built 快照更新为当前 RPC 实现；E8 逐条完成条件引用最新机器闸；E9 将 F-214~F-217 统一裁决入验收池 `ACC-2026-08-23-01`，不在本卡顺手修。阶段汇报@E9 已向用户展示。 | E-073；E-077 | E10 向用户展示 releasePacket 与单一确认口 |
| 2026-08-23 | 用户 / 主控 | E11：用户对 `DHR_52-E10-v1` 回复“认可”，授权精确本地 squash、主干复验、verify、回填和清理；不含 push/deploy/环境操作/下一卡。 | 用户对话；`review.md` 人类签名区 | E12/E13 连续机械收口 |
| 2026-08-23 | 主控 | E12 主干复验：DHR_52 已 squash 到 `1571321`，但全量 `npm test` 89/90；DHR_51 lost-lease 用例失败，定向复跑仍失败。该用例的初始写账与外部换手 lease 竞态将 `E_LEASE_HELD:lease-lost` 直接抛出，需改 runtime/ 才能收敛。 | E-078；F-225 | 停在 verify 前，待用户授权 DHR_51 范围维护后重跑 E12/E10 |

## 证据账本 (Evidence Ledger)

| ID | 类型 | 命令 / 路径 | 结果 (pass/fail/observed/waived) | 支撑什么结论 |
|---|---|---|---|---|
| E-001 | observed | `git rev-parse master` = `bd6b3b5`; `git rev-list --left-right --count master...origin/master` = `16 0` | observed | DHR_52 必须从本地 master 而非过期 origin/master 建树 |
| E-002 | observed | `omp --model deepseek-v4-flash --no-session --tools read,grep,glob,lsp -p <DHR_52 scout prompt>` | observed | OMP 与指定模型可实际启动；侦察工具白名单无写入 |
| E-003 | observed | OMP 全量施工会话（`--thinking high`）约 11 分钟零回传后由主控终止；任务树无 worker 代码 diff | observed | 该次 worker 尝试未完成，不能作为任何实施或测试证据 |
| E-004 | pass | `node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 5 / pass 5 / fail 0 / duration_ms 245 | pass | capabilities.mjs 复算指纹逐字等于 capability-baseline.json 的 capability_hash；形态合法但不同的 peer hash 拒绝为 E_CAPABILITY_MISMATCH |
| E-005 | pass | 主控独立复跑 `node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 5 / pass 5 / fail 0 | pass | worker 的 capability helper 与定向测试可复现 |
| E-006 | pass | `node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 17 / pass 17 / fail 0 / duration_ms 507.808 / exit 0 | pass | close() 在客户端仍连着时确定性完成（专用用例通过），整套 17 用例（capability 5 + 分帧 3 + 坏帧 3 + 真实 socket 6）跑出完整 summary，无挂起 |
| E-007 | pass | 复跑同上 → tests 17 / pass 17 / fail 0 / duration_ms 397.8317 / exit 0 | pass | 修复后结果稳定可复现，非单次侥幸 |
| E-008 | observed | 独立探针（5 次）：client connect 后同一 tick、服务端 accept 前写 socket → 5/5 数据丢失；accept 后写 → 正常到达 | observed | 两处测试失败根因是写序竞态（Windows named pipe 特性）而非传输缺陷；传输用例统一「先 accept 再写」确定性顺序 |
| E-009 | pass | 主控独立复跑 `node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 17 / pass 17 / fail 0 / duration_ms 421.7965 / exit 0 | pass | capability 与 transport 当前定向证据独立可复现；server/subscribe 尚未实施 |
| E-010 | observed | OMP B：`deepseek-v4-flash` / `thinking=xhigh` / JSON + thoughts / 15 分钟；连续 `thinking_*`、工具调用事件，最终 `Deadline exceeded`；结束后 `git diff --name-only` 仅见既有文件 | observed | 15 分钟足以确认模型持续思考和检索，但对“server + subscribe”这个交付包不足；不能把思考事件当作施工或测试证据 |
| E-011 | pass | `node --test test/rpc.test.mjs`（cwd `relay-core`，worktree 内先 `npm ci` 装锁定 ajv 依赖）→ tests 23 / pass 23 / fail 0 / duration_ms 1413.95 / exit 0 | pass | server.mjs 最小握手路径全绿：合法 request 只调 `handlers.contracts` 且响应过冻结信封；`DIFFERENT_SHA256` → `E_CAPABILITY_MISMATCH` 且零 handler 调用（不降级）；subscribe → `E_UNKNOWN_METHOD`；缺 handshake → `E_MISSING_FIELD`；坏 JSON → `E_TRANSPORT_FRAME_INVALID`(id null)；断连 Store 零调用、handler 零调用、服务端继续服务新连接 |
| E-012 | pass | 主控独立：`git diff --check`；`node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 23 / pass 23 / fail 0 / duration_ms 1138.6408 / exit 0 | pass | C 组最低实现与定向测试可复现；尚不构成 DHR_52 完成，连接级 handler seam 和 subscribe/F-057 仍未闭合 |
| E-013 | pass | worker：`node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 25 / pass 25 / fail 0 / duration_ms 1445.5038 / exit 0；复跑 25/25 exit 0；`grep` 复核 `server.mjs` 已无 `removeAllListeners`；`git status --short` 变更仅在 `relay-core/rpc/**`、`relay-core/test/rpc.test.mjs`、`progress.md` | pass | seam 修正后全绿：既有 23 条行为不变（capability 5 + 分帧/坏帧 6 + 真实 socket 6 + server 6），新增 2 条——传输 seam 连接级回落、服务端并发连接请求/回复/坏帧/断连各回各的连接（真实传输，不靠监听器移除）；服务端只经 `createConnectionHandlers` 拿连接本地帧/错误/关闭，不再接管 socket 数据流 |
| E-014 | pass | worker：`node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 28 / pass 28 / fail 0 / duration_ms 1842.711 / exit 0；复跑 28/28 exit 0（duration 1624.83）；`git diff --check` 无空白错误；`git status --short` 变更仅在 `relay-core/rpc/**`、`relay-core/test/rpc.test.mjs`、`progress.md`（`execution_strategy.md` 的 M 为先前工作区既有状态，本棒未触碰） | pass | subscribe 连接本地 seam 全绿：① subscribe 只回注入的 `{result}`（不发明读取模型），sink.event/sink.runStateChanged 对完整冻结对象返回 true 且逐字原样推送，通知帧本身再过 `relay.rpc/v1`（notification 分支）；② 三种无效载荷（交叉协议 / 缺 protocol / 未知字段）fail-closed——sink 返回 false、真实等待后零通知帧；③ 客户端断开对每个订阅的 unsubscribe 恰调用一次（含服务端 close 路径不重复）、Store 零调用、不调 subscribe 发 cancel；④ 未注入 subscribe 仍回 `E_UNKNOWN_METHOD`（既有用例不变）。契约与 reason code 零改动：交叉协议落既有 `E_BAD_VALUE`（与 negative 夹具期望一致）、缺 protocol → `E_MISSING_FIELD`、未知字段 → `E_UNKNOWN_FIELD`，F-057 保持 open 未裁决 |
| E-015 | pass | worker：`node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 29 / pass 29 / fail 0 / duration_ms 1905.4531 / exit 0；修复后复跑 29/29 exit 0（duration 1855.0564） | pass | 订阅生命周期回归修复全绿：① 新增时序回归用例——subscribe 响应返回后、sink 推送送达后、再等 20ms 后 unsubscribe 均零调用（连接存活），客户端 destroy 后 `until` 等到恰一次，Store 零调用、不重调 subscribe（不发 cancel）；② 既有 28 条全部保持绿；③ 既有 `subscribe 注入 seam` 用例尾部改为等断开清理异步到达后再断言（修复前靠误调蒙混） |
| E-016 | fail | worker：临时还原修复（移除 `onConnection` 的账目登记）后仅跑回归用例 `node --test --test-name-pattern="回归" test/rpc.test.mjs` → `AssertionError: 响应返回后、连接存活：unsubscribe 必须零调用 / 1 !== 0`（rpc.test.mjs:738）；随后还原修复，全套 29/29 复绿 | fail | 回归用例确定性地抓住原缺陷：修复前 subscribe 解析即误调 unsubscribe（响应返回时已 1 次），修复后连接存活期间恒为 0——旧用例只断言断开后总次数，确实抓不到该时序 |
| E-017 | pass | 主控独立：`node --test test/rpc.test.mjs`（cwd `relay-core`）→ tests 29 / pass 29 / fail 0；`npm test` → tests 50 / pass 50 / fail 0 | pass | DHR_52 的 RPC 实现与既有 contracts/store/runtime 回归当前可复现；不等于 F-057 已裁决或任务已完成 |
| E-018 | pass | 主控独立（cwd `relay-core`）：`node tools/validate.mjs --selftest` → 33/33；`node tools/audit-contracts.mjs` → 12 份 schema、所有违规 0；`node tools/fixture-manifest.mjs` → 55 份相符；`node tools/capability-baseline.mjs` → 8 份相符，hash `970b54601ae582a5…` | pass | 当前未改冻结契约/基线时，五道机器闸与既有基线一致；F-057 若批准新增码，必须在契约变更后重跑并重生成所需基线 |
| E-019 | pass | 主控：`node tools/validate.mjs --selftest`（cwd `relay-core`）→ golden 11/11、negative 23/23，合计 34/34；`node --test test/rpc.test.mjs` → 29/29 | pass | 正反向完整协议错位均命中 `E_PROTOCOL_MISMATCH@/params/protocol`；sink 测试还显式钉住缺字段/未知字段/本协议普通坏值保留既有码 |
| E-020 | pass | 生成：`node tools/fixture-manifest.mjs --write` → 57 份；`node tools/capability-baseline.mjs --write` → 8 份、hash `970b54601ae582a5…`；`node tools/audit-contracts.mjs --write-tokens` → 164 tokens | pass | 派生基线已按契约变更纪律重生成；capability hash 未漂移，因为冻结 JSON Schema 未改 |
| E-021 | test | 主控独立：`git diff --check`；`npm test` → 50/50；`node tools/validate.mjs --selftest` → 34/34；`node tools/audit-contracts.mjs` → 0 违规；`node tools/fixture-manifest.mjs` → 57 份相符；`node tools/capability-baseline.mjs` → 8 份相符 | pass | F-057 变更后的全量机器闸均可复跑；仍须两轮独立复核及 E4/E5/E14/E6/E7~E10 才可进入待人验 |
| E-022 | observed | 主控：`dh dh-relay` → 42 failures / 39 warnings；DHR_52 命中 review 模板的 R2/R12/R17/R27，另有模块 design input 与历史卡失败 | observed | E0 已执行；仅 DHR_52 自身记录缺口允许在本卡修，卡外失败不应被顺手修改 |
| E-023 | observed | 三次 OMP `deepseek-v4-flash` / `thinking=xhigh` 只读复核尝试（不开放 write/shell）：运行进程存在但没有可回收的 `approved` / `changes-requested` 结论，均由主控终止 | observed | 运行态不是复核结论；不能作为 E2/E4/E5/E14 的派出或通过证据 |
| E-024 | observed | `dh mine dh-relay DHR_52`（只读）→ review/findings/progress 工件齐；正册 0 条、候选区 14 条待裁决，输出去重对照与候选模板 | observed | E6 miner 备料已完成；候选抽取须等待独立复核结论，候选不等于入正册 |
| E-026 | observed | Claude Opus 机器只读复核尝试：默认 profile 未进入 review；`CLAUDE_CONFIG_DIR=claude-account9` 的独立 profile 返回 API 402；均无可回收结论、无 worktree 写入 | observed | Claude Opus 后端当前不可用于 E2/E4/E5/E14；E-025 只证明派发，不证明复核完成 |
| E-027 | observed | 默认 `C:\Users\nash\.claude`：Claude Opus 5 / high effort / 禁 Write/Edit/NotebookEdit/Bash / strict empty MCP；实际读 18 个目标文件后仍未给出结论，主控中断；随后 `git status --short` 与 `git diff --check` 复查 | observed | Claude Opus 实例未产出可采纳复核结论且未写 worktree；不能计作 E2/E4/E5/E14 通过 |
| E-028 | waived | 用户对话明确指示“不然就算了，别复核了”；主控停止后续 OMP / Claude 复核尝试 | waived | 用户放弃独立复核，不等于 E2/E4/E5/E14 通过；本卡不得以“已复核”或“可待人验”收口 |
| E-025 | review-dispatch | dh dispatch | observed | 复核派出：claude-opus-r1（Claude Code --disallowedTools Write/Edit/NotebookEdit/Bash + strict empty MCP，未参与施工）｜E2 代码轮1：全卡静态复核 RPC/transport/server、F-057 reason 边界与 fixtures；只读。 |
| E-029 | review-dispatch | dh dispatch | observed | 复核派出：codex-fresh-r1（独立 Codex 实例，未参与施工，只读）｜E2 代码轮1：DHR_52 RPC 实现、F-057 分类边界、fixture 与回归证据；只读，直接回传正式结论。 |
| E-030 | review | 新鲜独立 Codex 实例只读复核；检查目标实现、schema、fixtures、workspace，并复跑定向/全量机器闸 | changes-requested | 第一轮正式结论：P1 F-204（F-057 错误优先级）与 P1 F-205（错误响应 `detail` 超过 4096）；未发现 P0。测试绿不替代代码复核。 |
| E-031 | pass | 主控修 F-204/F-205 后独立运行：`node --test test/rpc.test.mjs` 30/30；`node tools/validate.mjs --selftest` 34/34；`npm test` 50/50；audit 0 违规；fixture manifest 57 相符；capability baseline 8 相符，hash 不变；`git diff --check` | pass | F-204 仅完整另一冻结对象入新码，其余回到具体错误排序；F-205 错误响应 detail ≤4096 且响应自行过冻结信封。仍须第二轮独立复核，不得据此收口。 |
| E-033 | review | 新鲜独立 Codex 第二轮增量复核 | changes-requested | P1 F-206：handler 非 JSON 结果可写非法 response 或抛出；未报告第一轮 P1 的复现。 |
| E-034 | pass | 主控修 F-206 后独立运行：`node --test test/rpc.test.mjs` 31/31；`node tools/validate.mjs --selftest` 34/34；`npm test` 50/50；audit 0 违规；fixture manifest 57 相符；capability baseline 8 相符，hash 不变；`git diff --check` | pass | response 先 JSON-safe 序列化并过冻结信封，函数/BigInt 均只断开当前连接而服务端继续服务；subscribe 清理顺序保持连接本地恰一次。待新鲜第三轮复核确认。 |
| E-036 | review | 新鲜独立 Codex 第三轮复核 | changes-requested | P1 subscribe getter 异常；P2 npm test 漏 RPC、非法 UTF-8、断连后 sink 假成功。 |
| E-037 | pass | 修 F-207~F-210：`node --test test/rpc.test.mjs` 33/33；`npm test` 83/83 | pass | subscribe 异步 fulfillment 异常 fail-closed；标准 test 门面覆盖 RPC；非法 UTF-8 拒绝；关闭后 sink 返回 false。仍待复核。 |
| E-039 | pass | 第四轮复核返工后：`node --test test/rpc.test.mjs` 33/33；`npm test` 83/83；`git diff --check` | pass | handler getter、notification JSON-safe、async unsubscribe 与 socket 关闭窗均已收紧；仍待新鲜复核确认。 |
| E-032 | review-dispatch | dh dispatch | observed | 复核派出：codex-fresh-r2（独立 Codex 实例，未参与施工及第一轮复核，只读）｜E2 代码轮2：独立增量复核当前 RPC/F-057/错误响应上限与生命周期，不读取第一轮结论。 |
| E-035 | review-dispatch | dh dispatch | observed | 复核派出：codex-fresh-r3（独立 Codex 实例，未参与施工及前两轮复核，只读）｜E2 返工后代码复核：RPC/F-057/JSON-safe response 与 subscribe 生命周期；不读取前轮结论。 |
| E-038 | review-dispatch | dh dispatch | observed | 复核派出：codex-fresh-r4（独立 Codex 实例，未参与施工及前三轮复核，只读）｜E2 返工后代码复核：current RPC response/subscription/UTF-8/F-057 边界；不读取前轮结论。 |
| E-040 | review-dispatch | dh dispatch | observed | 复核派出：codex-fresh-r5（Codex CLI 新鲜会话；--sandbox read-only）｜E2 新鲜静态代码复核当前未提交 DHR_52 RPC diff：握手 capability fail-closed、JSON-RPC response/notification 边界、订阅与断连清理、UTF-8/NDJSON transport、F-057；不读取既有 review/progress/findings。 |
| E-041 | observed | codex-fresh-r5 启动横幅实际为 `sandbox: danger-full-access`（`cpa` profile 覆盖参数），无正式结论；派后比对仅 E-040/E-041 账本增量 | invalid | 该尝试不具备机器只读资格，不计 E2 复核；改用 `--ignore-user-config --sandbox read-only` 新鲜实例重派。 |
| E-042 | observed | 第二次尝试虽带 `--ignore-user-config --sandbox read-only`，实际仍为 `sandbox: danger-full-access`；确认 PowerShell 的 `codex` function 强制追加 `--dangerously-bypass-approvals-and-sandbox` | invalid | 无额外任务树写入、无正式结论；不计 E2。后续须直接调用 `C:\Users\nash\.local\bin\codex.cmd`，不得经 function。 |
| E-044 | observed | codex-fresh-r6 直接入口启动横幅为 `sandbox: read-only`，但该实例按自身环境规则读取了含 DHR_52 前轮复核结论的外部 memory 摘要 | invalid | 虽未读取工作区的 review/progress/findings，仍不满足“不继承/不注入前轮上下文”；不计 E2，改派无该记忆注入的 Claude account9 只读形态。 |
| E-046 | observed | `dh agent setup claude-account9 --json` 为 ready，但实际 Claude 新鲜只读会话返回 `API Error: 402 Insufficient Balance` | blocked | 无正式复核结论、无任务树写入；不计 E2。当前可用的独立、机器只读且无前轮上下文的 reviewer 后端缺失。 |
| E-048 | observed | claude-default 新鲜只读会话（E-047）运行超过两分钟仍未回传可回收结论；进程存活不等于复核完成 | pending | 未写任务树、未给 `approved`/`changes-requested`；不计 E2，不作通过或收口判断。 |
| E-049 | review | claude-default 新鲜会话最终结论已从本地 transcript 回收 | changes-requested | 无 P0/P1；P2-1 要求恢复 F-204 前的判别字段优先，P3 覆盖/范围/as-built 观察。 |
| E-050 | pass | 主控裁决并补 P3-2/F-213：`node --test test/rpc.test.mjs` 35/35；`npm test` 85/85；`git diff --check` | pass | P2-1 与 F-204 已定边界冲突，驳回；异步 reject/迟到 resolve 已有真实 socket 回归；标准 test 门面范围已精确回填。仍待新鲜复核。 |
| E-043 | review-dispatch | dh dispatch | observed | 复核派出：codex-fresh-r6（Codex CLI 新鲜会话；C:\Users\nash\.local\bin\codex.cmd；--sandbox read-only）｜E2 新鲜静态代码复核当前未提交 DHR_52 RPC diff：握手 capability fail-closed、JSON-RPC response/notification 边界、订阅与断连清理、UTF-8/NDJSON transport、F-057；不读取既有 review/progress/findings。 |
| E-045 | review-dispatch | dh dispatch | observed | 复核派出：claude-account9（新鲜 Claude Code 会话；禁 Write/Edit/NotebookEdit/Bash + strict empty MCP）｜E2 新鲜静态代码复核当前未提交 DHR_52 RPC diff：握手 capability fail-closed、JSON-RPC response/notification 边界、订阅与断连清理、UTF-8/NDJSON transport、F-057；不读取既有 review/progress/findings。 |
| E-047 | review-dispatch | dh dispatch | observed | 复核派出：claude-default（新鲜 Claude Code 会话；禁 Write/Edit/NotebookEdit/Bash + strict empty MCP）｜E2 新鲜静态代码复核当前未提交 DHR_52 RPC diff：握手 capability fail-closed、JSON-RPC response/notification 边界、订阅与断连清理、UTF-8/NDJSON transport、F-057；不读取既有 review/progress/findings。 |
| E-051 | review-dispatch | dh dispatch | observed | 复核派出：claude-default-r2（新鲜 Claude Code 会话；禁 Write/Edit/NotebookEdit/Bash + strict empty MCP）｜E2 第四轮返工后新鲜静态复核：当前 RPC/F-057 既有码排序、异步 handler reject、断连后 subscribe 迟到 resolve、package test 门面边界；不读取既有 review/progress/findings。 |
| E-052 | review | claude-default-r2 新鲜增量复核 | approved | 无 P0/P1/P2；RPC 35/35、npm 85/85、validator/audit/fixture/capability 全绿。P3 F-214~F-217 进入收口尾巴，未裁决前不收口。 |
| E-053 | review-dispatch | dh dispatch | observed | 复核派出：claude-default-e4｜E4 需求复核：新鲜只读会话按 brief/task_plan/DevPlan 与当前实现、测试核验需求逐条对齐；不读取既有 progress/findings/review，不作验收或收口裁决。 |
| E-054 | review-dispatch | dh dispatch | observed | 复核派出：claude-default-e4-retry｜E4 重派：E-053 的空 MCP 配置被 CLI 参数解析拒绝，未启动审查、未改任务树；本次改用已验证的直接只读 Claude 调用，审查范围与 E-053 相同。 |
| E-055 | review | claude-default-e4-retry 新鲜需求复核 | approved | 无 P0/P1；四项需求均有机器证据、范围合规。P2 F-218：断连不取消的真实 Store state/events signature 仍是结构性替代证据，待裁决；未作验收或收口判断。 |
| E-056 | pass | 补 F-218 真实 Store 断连同态回归：`node --test test/rpc.test.mjs` 36/36；第二次 `npm test` 86/86；`git diff --check` | pass | 首次全量的 runtime lost-lease 用例单次 85/86 后立即复跑全绿，记时序波动而非忽略；本次只改 RPC 测试，仍待新鲜复核。 |
| E-057 | review-dispatch | dh dispatch | observed | 复核派出：claude-default-r3｜新鲜增量复核：仅审查 E-056 新增的真实 Store 断连 state/events 同态与重连 inspectRun 回归，以及它是否确实闭合 E-055 的 P2 F-218；同时确认未改变 RPC 行为或越界。只读，不读取既有 progress/findings/review。 |
| E-058 | review-dispatch | codex exec --sandbox read-only -m gpt-5.6-sol | observed | 复核派出：codex-cli-fresh-r3 (--sandbox read-only)｜E2 fresh independent static code review: current uncommitted DHR_52 diff; focus F-218 Store byte-signature/reconnect regression, RPC lifecycle, F-057 mapping, and scope. |
| E-059 | review | `codex-ninth exec --sandbox read-only -m gpt-5.6-sol`；启动横幅 `sandbox: read-only`；只读静态审，未运行测试、未修改文件 | changes-requested | 新鲜独立结论：P1 F-219（超长 NDJSON 帧按 chunk 清缓冲，尾部可被误作新帧执行）与 F-220（通知/响应在 schema 校验前 JSON 序列化，静默删除 undefined/函数等字段）；P2 F-221（通知写入无背压上限）与 F-222（未来同名异版可能误报 `E_PROTOCOL_MISMATCH`）。 |
| E-060 | fail | `node --test test/rpc.test.mjs`（返工前，cwd `relay-core`）→ tests 39 / pass 34 / fail 5 | fail | 新增四类回归在修复前可稳定复现：未终止超长帧尾部被执行、undefined/toJSON 通知被净化、nested undefined response 未断连、write 返回 false 仍报告成功、未来 `relay.event/v3` 误报 `E_PROTOCOL_MISMATCH`。 |
| E-061 | pass | cwd `relay-core`：`node --test test/rpc.test.mjs` → 39/39；`npm test` → 89/89；`node tools/validate.mjs --selftest` → 34/34；`node tools/audit-contracts.mjs`；`node tools/fixture-manifest.mjs`；`node tools/capability-baseline.mjs`；`git diff --check` | pass | F-219~F-222 修复与旧有 contracts/store/runtime/RPC 回归全绿；审计、fixture manifest、capability baseline 对证全绿，且无 diff 空白错误。 |
| E-062 | review-dispatch | codex-ninth exec --sandbox read-only -m gpt-5.6-sol | observed | 复核派出：codex-ninth-fresh-r4 (--sandbox read-only)｜E2 post-repair independent static review: inspect only the current DHR_52 diff for F-219~F-222 fixes and regressions; do not read progress/findings/review, write files, run tests, or make acceptance/closure decisions. |
| E-063 | review | `codex-ninth exec --sandbox read-only -m gpt-5.6-sol`；启动横幅 `sandbox: read-only`；只读静态审，未运行测试、未修改文件 | changes-requested | F-219~F-222 未发现新增 P0/P1；新 P2 F-223：数组预检仅检查索引，数组自有 `toJSON`/accessor 可在 JSON.stringify 时改变已校验内容，须拒绝数组额外键/accessor 或复验序列化 clone。正式 `VERDICT: changes-requested`。 |
| E-064 | pass | cwd `relay-core`：`node --test test/rpc.test.mjs` → 39/39；`npm test` → 89/89；`node tools/validate.mjs --selftest` → 34/34；`node tools/audit-contracts.mjs`；`node tools/fixture-manifest.mjs`；`node tools/capability-baseline.mjs`；`git diff --check` | pass | F-223 修复与通知/response 数组 `toJSON` 回归通过；既有 RPC/contracts/store/runtime 回归、审计、fixture manifest、capability baseline 与 diff 格式检查全绿。 |
| E-065 | review-dispatch | codex-ninth exec --sandbox read-only -m gpt-5.6-sol | observed | 复核派出：codex-ninth-fresh-r5 (--sandbox read-only)｜E2 post-F-223 independent static review: array JSON-preflight repair and regressions; no workspace process-account reads, writes, tests, or closure decision. |
| E-066 | review | `codex-ninth exec --sandbox read-only -m gpt-5.6-sol`；启动横幅 `sandbox: read-only`；只读静态审，未运行测试、未修改文件 | changes-requested | P1 F-223：Proxy/自定义数组原型可在 descriptor 检查后、原对象 JSON.stringify 前改变帧，仍可能与 AJV 已验值不同；P2 F-224：只覆盖 own `toJSON`，未覆盖 accessor/extra property/Proxy/custom prototype。正式 `VERDICT: changes-requested`。 |
| E-067 | pass | cwd `relay-core`：`node --test test/rpc.test.mjs` → 40/40；`npm test` → 90/90；`node tools/validate.mjs --selftest` → 34/34；`node tools/audit-contracts.mjs`；`node tools/fixture-manifest.mjs`；`node tools/capability-baseline.mjs`；`git diff --check` | pass | F-223 snapshot 防线与 F-224 对抗性回归通过：Proxy/自定义原型不再改变 notification/response 线上内容，array accessor/extra property fail-closed；既有全部机器闸保持绿。 |
| E-065 | review-dispatch | codex-ninth exec --sandbox read-only -m gpt-5.6-sol | observed | 复核派出：codex-ninth-fresh-r5 (--sandbox read-only)｜E2 post-F-223 independent static review: inspect only the current DHR_52 diff for the array JSON-preflight repair and regressions; do not read progress/findings/review, write files, run tests, or make acceptance/closure decisions. |
| E-068 | review-dispatch | codex-ninth exec --sandbox read-only -m gpt-5.6-sol | observed | 复核派出：codex-ninth-fresh-r6 (--sandbox read-only)｜E2 post-F-223/F-224 independent static review: inspect only descriptor-derived JSON snapshots for response/notification, Proxy and custom array prototype behavior, and regressions; do not read progress/findings/review, write files, run tests, or make acceptance/closure decisions. |
| E-069 | review | `codex-ninth exec --sandbox read-only -m gpt-5.6-sol`；启动横幅 `sandbox: read-only`；只读静态审，未运行测试、未修改文件 | changes-requested | P1 F-223：snapshot 数组仍继承 `Array.prototype.toJSON`，可在校验后改写序列化；P2 F-224：Proxy 的 `length` 应只经一次 descriptor 读取，且无效载荷需断言零帧输出 / accessor 不执行。P3：既有 F-057 diff 为范围观察，本轮未触碰。正式 `VERDICT: changes-requested`。 |
| E-070 | pass | cwd `relay-core`：`node --test test/rpc.test.mjs` → 40/40；`npm test` 首次 89/90（detached-host 机器面断言），其单项复跑 1/1，随后全量复跑 → 90/90；`node tools/validate.mjs --selftest` → 34/34；`node tools/audit-contracts.mjs`；`node tools/fixture-manifest.mjs`；`node tools/capability-baseline.mjs`；`git diff --check` | pass | F-223 将数组 snapshot 原型隔离并一次性读取 `length` descriptor；F-224 追加零帧输出、getter 未执行与变长 Proxy 回归。契约/清单/基线/diff 闸全绿，仍待新鲜独立复核。 |
| E-072 | review | `codex-ninth exec --sandbox read-only -m gpt-5.6-sol`；启动横幅 `sandbox: read-only`；只读静态审，未运行测试、未修改文件 | approved | 无 P0/P1/P2/P3。确认 response/notification 同走 descriptor snapshot、数组/对象 null prototype、`length` 单次 descriptor 读取，以及 zero-frame/getter/Proxy/global `Array.prototype.toJSON` 回归。正式 `VERDICT: approved`。 |
| E-071 | review-dispatch | codex-ninth exec --sandbox read-only -m gpt-5.6-sol | observed | 复核派出：codex-ninth-fresh-r7 (--sandbox read-only)｜E2 post-F-223/F-224 hardening independent static review: inspect only null-prototype descriptor snapshots, one-read array length, and zero-frame/getter regressions; do not read progress/findings/review, write files, run tests, or make acceptance/closure decisions. |
| E-073 | pass | 主控独立（cwd `relay-core`）：`npm test` → 90/90；`validate --selftest` → 34/34；audit 0 违规；fixture manifest 57 相符；capability baseline 8 相符；`git diff --check`。 | pass | DHR_52 最终工作树机器证可复跑；E7 as-built 使用同一快照。 |
| E-074 | observed | `dh mine dh-relay DHR_52`（cwd worktree）→ exit 0；正册 0 条，候选 14 条；本卡命中候选-1/-6/-11。 | observed | E6 已执行；两条新候选待写入候选区，不替代 E5。 |
| E-075 | review | 主控只读：正册为空；对候选-1/-6/-11 与 F-057/F-219~F-224 的分支、变异、覆盖面证据逐项对照。 | approved | E5 无在册教训重蹈；无 P0/P1/P2/P3。 |
| E-076 | review | 主控只读横向比对：capability、协议错位、跨边界发送、连接订阅 4 维度 / 13 处同类实现与回归。 | approved | E14 无 P0/P1/P2；F-214/F-215 为 P3 尾巴，已分流。 |
| E-077 | observed | `ACC-2026-08-23-01`（worktree `docs/acceptance/验收池.md`）。 | observed | E9：F-214~F-217 统一入验收池，不阻断本卡。 |
| E-078 | fail | 主干 `npm test` → 89/90；定向 `node --test --test-name-pattern='host：租约被换手后旧会话自动停机且不释放别人的租约' test/runtime.test.mjs` → 0/1，均报 `E_LEASE_HELD:lease-lost`。 | fail | E12 集成复验失败，verify 不可执行；DHR_52 RPC 定向用例在全量中通过。 |

## OMP 可观测性实验

| 运行 | thinking | 时限 | 可观测推进 | 结果与判定 |
|---|---|---:|---|---|
| A0 全卡尝试 | high | 20 分钟（主控约 11 分钟止损） | OMP 存活且 CPU 增长；无流式输出、无工具/文件/测试外部信号 | 零产物，不计施工证据；不能据 CPU 推定质量。 |
| A1 capability 小步 | low（历史对照，用户后续明确禁用） | 5 分钟 | 新增 helper/test + 完整 5/5 汇总 | 产物可复跑，但该 thinking 档不再使用。 |
| A2 transport 修复 | xhigh | 5 分钟 | 受限路径内修复/测试增量；worker 曾遗留 test 子进程，主控按父子进程树清理 | 修复经主控 17/17 独立复跑；5 分钟不足以区分 OMP 已退出与子进程残留。 |
| B server/subscribe | xhigh | 15 分钟 | JSON `thinking_*` + 读取/工具调用事件持续；无受限代码增量 | 强确认正在思考/检索；15 分钟 deadline 时零 server/subscribe 产物，不计施工证据。 |
| C 最小 server | xhigh | 30 分钟上限（约 14 分钟自然结束） | JSON `thinking_*`、工具错误后恢复、约第 12 分钟写 `server.mjs`，随后写 6 用例并跑 23/23 | 长窗口应配极小交付包：模型在真实思考后能自行转成可复跑代码；上限不是必须耗尽。 |
