<!-- dh:v1 -->
# DHR_68 · Progress

## 日志

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|---|---|---|---|---|
| 2026-08-31 | 主控 | 用户对话明文「授权开工」，D-start。已只读核实三条缺陷在现役代码中的精确落点，并在起草期发现 B-32 记录未写准的一处实现细节：Windows 上 `spawnSync` 超时命中 `child.error.code === 'ETIMEDOUT'` 而非 `child.signal`（依据 DHR_35 实测 `spawn:ETIMEDOUT`），故超时语义位不能只按 `child.signal` 判。主树建标准档八件套并回填 DevPlan 户口，`d6358dc`。 | E-6800 | 建 `wt/DHR_68`，先跑真实逐命令形态对照（验收 D）。 |
| 2026-08-31 | 主控 | `wt/DHR_68` 从 `master@d6358dc` 切出。跑 `scripts/probe-command-shapes.mjs`，用**真实 herdr 0.8.2** 把本卡触及的 10 个动词各跑一次，逐条记录 exit code / stdout 是否 JSON / 长度 / 顶层键名。三条关键确认：①`pane run` 成功 = exit 0 + **stdout 空**（缺陷 B 坐实）；②失败时 herdr 把 `{"error":{"code":…}}` 写 **stderr** 且 exit 1，现役 `missing` 正则靠 message 里的 "not found" 命中，`agent_not_ready` 不会误命中 `missing`；③`agent send-keys`/`pane close` 的 result 只有 `{type:'ok'}`，`agent list` 里**自动识别**的 agent **没有 `name` 字段**。全部 probe pane 已关闭。 | E-6801 | 按对照表校正 fake，再写红测。 |
| 2026-08-31 | 主控 | 读 `herdr agent start --help` 得到本卡最重要的一条**新事实**：herdr **自己**的 `agent start --timeout` 默认就是 **30 000 ms**（max 300 000）。也就是说旧的 10 秒通用上限**低于 herdr 自身的合法等待窗口**——不是"偶尔慢"，是结构性砍断。这把 60 秒从"按 max 29482 ms 拍的余量"升级成"给 herdr 默认窗口留一倍余量"的可证依据。同时确认：不能靠传 `--timeout` 解决，因为验收 B 要求 `agent start` 的 argv 不变。 | E-6802 | 60s 只做 spawnSync 上限，argv 一字不动。 |
| 2026-08-31 | 主控 | 按对照表重写 `fake-herdr.mjs` 的返回形态（验收 D）：`paneRun`→`{ok:true,value:''}`、`paneKill`/`agentPrompt`/`agentSendKeys`→`{type:'ok'}`（**其中 `agentPrompt` 分错类了，见下方 E-6813**）、`agentStart`/`agentGet`/`agentRename`→带 `agent` 子对象的真实记录、`agentList` 默认项去掉真实不存在的 `name`、失败 detail 改成真实的 `exit:1:{"error":{"code":…}}` 串。改动后**只**新增一处既有断言不符（`{ok:true,value:{}}` vs 真实 `{type:'ok'}`），已按真实形态改正。`herdr-adapter.test.mjs` 在 master 上本就有 3 例失败，改 fake 前后完全一致（同名、同断言/超时原因），故为基线不可归因本卡。 | E-6803 | 写 A/B/C 红测。 |
| 2026-08-31 | 主控 | A/B/C 五条红测就位并取得**行为**失败终态（不是加载失败）：A 断言"超时+agent 存在必须保留 launch"得 `false !== true`；A-cli 断言 `timedOut` 语义位缺失；B 断言 `pane run` 不解 JSON；C 断言启动期 blocked 返回 handle；C-driver 断言事件序列。 | E-6804 | 依次改 CLI → executor → driver。 |
| 2026-08-31 | 主控 | 三层实现落地，均在允许路径内：**CLI** 增 `HERDR_START_TIMEOUT_MS = 60_000` 常量（代码常量，不读环境变量、不进 registry、不加 flag）与 `startTimeoutMs` 参数，仅 `agent start` 使用；超时按 `child.error.code==='ETIMEDOUT' \|\| child.signal` 双路径标 `timedOut`；非零退出按 `/agent_not_ready/i` 标 `notReady`；`paneRun` 改 `json:false`。**executor** 在 `!start.ok` 前插入超时**只读对账**（Codex 走 `agentGet`、Claude 走 `agentList` 按新 pane 过滤，命中即沿用 handle，未命中才关同一 pane，**永不重发启动调用**）与启动期 blocked 保留 handle；新增返回位 `launch_blocked`。**driver** 仅动启动期 blocked 事件路径：blocked 时扣住 completion instruction、Attention 的 detail 从硬写 `'unknown'` 改成真实观测、离开 blocked 后补发恰好一次。Result 判定一行未动。 | E-6805 | 跑绿测与回归。 |
| 2026-08-31 | 主控 | `herdr-adapter.test.mjs` 定向：**24 例 21 通过**，失败 3 例与 master 基线**同名同因**（`timeout:judge result` / `Expected values to be strictly equal` / `timeout:orphaned recovery`），逐条比对确认不可归因本卡。 | E-6806 | 跑全量回归。 |
| 2026-08-31 | 主控 | 全量 `npm test` 两棵树各跑一次：master 基线 273 例 15 失败，本树 278 例 16 失败，但**失败集在两次运行间本身就不稳定**（master 独有 `basic-agent-task`、`service：并发同键 start`；本树独有 `DHR_61 D1`、`CLI pending 账 50 路并发`），单例耗时普遍冲到 60~74 秒——`--test-concurrency=4` 下本机负载导致的抖动，不是稳定回归。据此改用**串行定向**复跑做诚实对比。 | E-6807 | 串行比对 herdr 血缘文件。 |
| 2026-08-31 | 主控 | 本树 `DHR_68/C driver` 在并发全量里失败、单独跑通过——查明是**我自己测试的采样竞态**（blocked 只持续约 6 ms，取样取到了后续的 `running`），不是生产代码问题。已改为：①状态序列用粘性全 `blocked` 让现场确定性驻留；②等**派生状态**而不是事件数组（事件先入内存、状态稍后收敛）；③拆成"扣住不发"与"补发一次"两个夹具，后者只断言单调条件；④等待窗口放宽到 45 秒以扛住并发负载。 | E-6808 | 记教训候选：竞态在测试侧，别改生产代码去迁就。 |
| 2026-08-31 | 主控 | 用户对话点选确认 F-6802 的解释为**「延后补发」**（启动期 blocked 扣住指令，agent 首次离开 blocked 后补发恰好一次），与已实现语义一致，F-6802 转 resolved。 | E-6809 | 继续收口备料。 |
| 2026-08-31 | 主控 | 卫生检查：`git diff --check` 干净（**这句当时只查了未暂存工作区，说过头了，见下方 E-6813/E-6814 的更正**）；改动文件严格落在 DevPlan `dh:allowed-paths:v1 task=DHR_68` 的 5 条精确路径内（`herdr-cli.mjs`/`herdr-executor.mjs`/`workflow-driver.mjs`/`herdr-adapter.test.mjs`/`fake-herdr.mjs`）+ 本工作区；凭据形态扫描（`sk-`/`eyJ`/`Bearer`/`api_key`/`auth.json`/`cookie`/`password`/40+ 位十六进制）对改动文件与全部证据**零命中**。 | E-6810 | 派五路复核。 |
| 2026-08-31 | 主控 | **主动放弃**一处更强的证据形态：验收 D 理想上还应让可执行桩 `fake-herdr-bin.mjs` 能吐"exit 0 + 空 stdout"，但该文件**不在本卡允许路径**，故未改。改以三段链条替代：真实 probe 证明形态（E-6801）→ `fake-herdr.mjs` 编码该形态 → 桩测断言 `typeof paneRun().value === 'string'` 证明 wrapper 不解 JSON。 | E-6811 | 复核时如判定链条不足，另开允许路径最小扩展的 B-调整，不擅自越界。 |
| 2026-08-31 | 主控 | 复核轮 1 派出：Herdr pane `w1:p2V` 拉 codex `--sandbox read-only -c model_reasoning_effort=high --model gpt-5.6-terra`，agent `dhr68rev1`，fresh 且未参与施工。**只读形态取证**：派出基线 `0b345e0`（工作区 clean），回收后 HEAD 仅多出主控自己那笔 F-6807 提交、`git status --short` 为空，复核者零写入。长中文 prompt 复现已知坑（停在输入框未提交），补 `agent send-keys enter` 后 seq 94、status→working。 | E-6812 | 收结论。 |
| 2026-08-31 | 主控 | 轮 1 判 **FAIL**，两条发现**全部采纳、无驳回**：**F-68-R1-01(P1)** fake 的 `agentPrompt` 返回 `{type:'ok'}`，而真实 probe 记录 `agent prompt` 的 `result_keys=["agent","type"]`（`agent rename` 同形），只有 `agent send-keys`/`pane close` 才是纯 `{type:'ok'}`——我把三者混为一类，fake 与注释都错了。**F-68-R1-02(P2)** `git diff --check` 在**已提交范围**上报出证据文件尾随空白，与 E-6810 的"干净"矛盾——我此前只查了未暂存工作区，说过头了。 | E-6813 | 修两条并加强验收 D。 |
| 2026-08-31 | 主控 | 整改落地 `8030709`：①`agentPrompt` fake 改为返回真实的 `{agent:{…},type}`，注释改正；②把**验收 D 从人眼比对升级为机器断言**——新增 `DHR_68/D` 测试直接读 `evidence/real-herdr-command-shapes.json` 当 oracle，逐命令比对 fake 的返回键集、json/非 json 分类、`pane run` 空 stdout 与失败 detail 形态，以后 fake 再漂就会直接红；③剥离三份 `.txt` 证据的尾随空白（仅空白、内容未动）并重跑 `git diff --check d6358dc..HEAD` 取得 exit 0。定向 26 例 23 通过，失败 3 例仍与 master 同名同因。 | E-6814 | 逐文件比对 herdr 血缘回归。 |
| 2026-08-31 | 主控 | 逐文件串行比对（master vs 本树）：`dhr64-result-bridge` 10/10 = 10/10；`dhr64-driver-observation` 3/3 = 3/3；`attempt-contract` 14/14 = 14/14；`profile-identity` 4/4 = 4/4；`agent-node` 两树同样有 `DHR_33 窄路径` 稳定失败。唯一差异项 `DHR_61 D1` 经**两树各跑 3 次**证明是**双向抖动**（run1 两树都失败、run2/run3 两树都通过）——其 `untilAsync` 窗口只有 1000 ms，不是本卡引入。至此 herdr 血缘范围内无可归因本卡的回归。 | E-6815 | 派 Review Batch 其余四路。 |
| 2026-08-31 | 主控 | Review Batch 四路并发派出（均为 fresh、只读、未参与施工的 codex `--sandbox read-only` + gpt-5.6-terra high）：轮 2 `dhr68rev2a`(w1:p2W)、需求 `dhr68revb`(w1:p2X)、教训 `dhr68revc`(w1:p2Y)、一致性 `dhr68revd2`(w1:p20)。**只读取证**：派出基线 `2ff73a5`，四路回收后 `git status --short` 为空、无任何复核者写入。过程踩两个已知坑：长中文 prompt 需补 `send-keys enter`；一个实例（`dhr68revd`）输入通道整体冻结，按 knowledge 规程**不纠缠**、关 pane 换 fresh 实例 `dhr68revd2` 重派。 | E-6816 | 收四路结论。 |
| 2026-08-31 | 主控 | 四路结论：**需求 PASS**（P0/P1=0，逐条命题-证据对齐，"有没有说过头"逐项核过未发现超证据断言）；**一致性 PASS**（五组比对全一致，无新增不一致或越界）；**轮 2 FAIL**、**教训 FAIL**。三个 open 项四路口径一致：F-6805 三段链条**足以**支撑验收 D（验收对象是 `fake-herdr.mjs` 而非未授权的可执行桩，桩仍输出 JSON 只记 P3）；F-6806 记账不修正确；F-6807 **不在本卡修**、维持独立 P2。 | E-6817、E-6818、E-6819 | 整改轮 2 与教训两路。 |
| 2026-08-31 | 主控 | **F-68-R2-01 (P1) 采纳并修复**：`instructionPending` 的补发判据原是白名单 `['working','idle']`，blocked **直接跳到 done** 时整条漏发，随后 done 分支去等 Result，会误落 `E_EXECUTOR_RESULT_MISSING`。改为判据「不再是 blocked」（`observation.herdr_status !== 'blocked'`），并补定向测试。**自证红绿**：判据改回白名单则该测试立即红（`timeout:deferred instruction on blocked->done`），改回后绿。 | E-6821 | 执行轮 2 指定的变异点。 |
| 2026-08-31 | 主控 | **有效单测变异点**（由第二轮复核实例 `dhr68rev2a` 选点，施工方未参与）：`herdr-executor.mjs` 的 `if (!reconciled) return closeFailedPane(start);` 取反。施加后其指定的 `DHR_68/A adapter：启动超时先对账…` **断言失败**；还原后 DHR_68 定向 **8/8**、`herdr-adapter` 27 例 24 通过（3 例仍为 master 同名同因基线）。 | E-6820 | 落教训候选与 review 登记。 |
| 2026-08-31 | 主控 | **教训两条 P1 采纳**：C-68-01 要求把「fake 形态漂移」显式标注与 [候选-6]/[候选-36] 的边界后独立入候选；C-68-02 要求 C-driver 竞态回链 [候选-35]。已在 `lesson_candidates.md` 落实，并采纳其建议新增 L-6802（Windows `spawnSync` 超时走 `ETIMEDOUT` 不投递信号）、L-6803（给自带等待窗口的 CLI 动词设外层超时前先读它自己的 timeout 合同）；异步测试竞态一条按其结论**不新增**（候选-35 已覆盖）。**落点纪律**：`knowledge/教训库-候选.md` 不在本卡允许路径，故只在工作区登记，正式入库需按 B-27 先例做路径最小扩展并经用户确认。 | E-6822 | 收口备料。 |
| 2026-08-31 | 主控 | 按用户要求重排 Herdr 布局为「左会话 + 右侧上下四格」。`herdr pane move` 在同 tab 内实测是 no-op（`changed:false`），改用先关旧 pane、再 `pane split --current --direction right` 起顶格、其后逐格 `--direction down` 的方式重建，并用 `pane resize` 调平（14/12/11/9 行）。四路复核结果均已在重建前收回，无数据丢失。 | E-6823 | — || 2026-08-31 | 主控 | **阶段汇报@复核闭环** 已在对话里发出（覆盖：D-start 与三条缺陷修法、B-32 未写准的两处实现细节、轮 1 FAIL 两条整改、F-6807 范围外登记、回归对照结论）。随后跑收口机器检查：`dh gate` 对 DHR_68 结构闸全过，唯一 ❌ 是「有 verify 提交命中」（需用户 E11 确认才可代签）；`dh dh-relay` 暴露本卡工件 7 条格式失败（R6 证据状态非法、R12 需求对齐证据缺 E-ID 与结论、R17 完成条件未逐字预填、R27 派出证据残留不可解析片段、R16 证据账本缺 `类型=test`），已逐条修正——证据账本改成规范五列并补齐 `test`/`review-dispatch` 类型行。修完本树 `dh dh-relay` **0 失败**；master 侧那 8 条失败经核实**全部是 DHR_68 自己 D-start 时落盘的空 review.md 占位**，收口 squash 回主干即消，与其它卡无关。 | E-6825 | 收复验与 miner 结论。 |
| 2026-08-31 | 主控 | 整改复验派出 `dhr68rev3`（fresh，未参与施工、未继承任何前轮会话；codex `--sandbox read-only`，pane `w1:p31`），复验基线 `4b0eaa3`。结论：**F-68-R2-01 闭合**（`instructionPending` 在首次非 blocked 观测时清零并补发，blocked→working/idle/done 三条出边各恰好一次，`unknown` 仍走 observation-lost 分支不误发，未动 `waitForExecutorResult` 与 done/idle 判定）；**教训两条 P1 落实**；**无 P0/P1、无越界**。但抓到 **F-68-RV-01 (P2)**：我重跑绿测覆盖证据文件，尾随空白又回来了，轮 1 的卫生整改不能称为持续闭合。已修并重跑 `git diff --check d6358dc..HEAD` 取得 exit 0。 | E-6826 | 收 miner 结论。 |
| 2026-08-31 | 主控 | 只读 miner `dhr68miner`（fresh，pane `w1:p32`）对候选区 58 条做去重：确认 **L-6801/6802/6803 均不重复**，并指出主控**漏了两条**——已补 **L-6804**（对已提交交付物做卫生验收必须显式指定基线到 HEAD 的范围，且凡会被重新生成的证据、检查要排在生成之后；本卡在这条上栽了两次）与 **L-6805**（「延后动作」的恢复条件语义是"离开某状态"就写排除式，别用有限白名单——正是 F-68-R2-01 的根因）。另判定 **F-6807 是 [候选-11] 的直接实例**、不新增候选，已回链；F-6804/F-6806 的"不新增"方向亦被确认（分别由候选-35、候选-31/48 覆盖）。 | E-6827 | E9/E10 备料收尾。 |
| 2026-08-31 | 主控 | E7 as-built：`as-built/relay-core.md` **不在本卡允许路径**，故不改。其 L280 对 Herdr adapter 的描述在抽象层面仍成立、无错误，但本卡三条新行为未反映进快照——**显式登记为 F-6808（P3）留给后续卡**，不静默跳过该节点。 | E-6829 | 出 E9 交付汇报 + E10 证据包。 |

## 证据账本

| ID | 类型 | 命令 / 路径 | 结果 | 支撑什么结论 |
|---|---|---|---|---|
| E-6800 | setup | `docs/modules/dh-relay/workspace/DHR_68/`；DevPlan §3.2 DHR_68；主树 `d6358dc` | pass | D-start 落户，目标/允许路径/完成条件/停止条件冻结；起草期核出 `ETIMEDOUT` 细节（F-6801）。 |
| E-6801 | probe | `node scripts/probe-command-shapes.mjs > evidence/real-herdr-command-shapes.json`（真实 herdr 0.8.2，10 个动词各一次） | pass | **验收 D 的证据本体**：`pane run` 成功 = exit 0 + 空 stdout；错误 JSON 写 stderr 且 exit 1；`agent prompt`/`agent rename` 返回 `{agent,type}`，`send-keys`/`pane close` 只返回 `{type}`；自动识别的 agent 无 `name` 字段。 |
| E-6802 | probe | `herdr agent start --help`（herdr 0.8.2） | pass | herdr 自身 `agent start --timeout` 默认 30 000 ms、max 300 000——旧 10s 通用上限低于依赖自身等待窗口，是结构性砍断；60s 因此有可证依据（F-6803）。 |
| E-6803 | test | `node --test relay-core/test/herdr-adapter.test.mjs`（改 fake 形态前后各一次，并与 master 同命令对比） | pass | fake 形态校正只引出一处既有断言不符；master 上本就有的 3 例 DHR_33 失败同名同因，不可归因本卡。 |
| E-6804 | test | `node --test --test-name-pattern="DHR_68" relay-core/test/herdr-adapter.test.mjs`（实现前） | fail | A/B/C 五条红测取得**行为**失败终态（非加载失败），证明能力确实缺失。见 `evidence/red-run-dhr68.txt`。 |
| E-6805 | code | `git diff d6358dc..051b4f8 -- relay-core/` | pass | 三层实现落地且全在允许路径：CLI 启动专用 60s + `timedOut`/`notReady` 语义位 + `paneRun` 非 JSON；executor 超时只读对账 + 启动期 blocked 保留 handle；driver 仅动启动期 blocked 事件路径。 |
| E-6806 | test | `node --test relay-core/test/herdr-adapter.test.mjs` → `evidence/green-run-herdr-adapter.txt` | pass | 27 例 24 通过；失败 3 例与未改代码的 master 同名同因。DHR_68 自有 8 例全绿。 |
| E-6807 | test | `npm test`（两棵树各一次）→ `evidence/npm-test-worktree.txt` | inconclusive | `--test-concurrency=4` 下基线本身不稳（master 273/15 失败、本树 278/16 失败，失败集互不包含，单例耗时冲到 60~74 秒），**不能用作本卡判定**（F-6806）。 |
| E-6808 | test | `node --test --test-name-pattern="DHR_68" …`（竞态定位与修法验证） | pass | `DHR_68/C driver` 的失败是测试采样竞态，非生产代码问题；改测试侧（粘性 blocked、等派生状态、拆单调断言、放宽窗口）后稳定通过。 |
| E-6809 | decision | 本轮对话 AskUserQuestion | pass | 用户点选确认 F-6802 的语义为「延后补发」，与实现一致。 |
| E-6810 | hygiene | `git diff --name-only` 对照 `dh:allowed-paths:v1 task=DHR_68`；`grep -rnEi "sk-\|eyJ\|Bearer\|api[_-]?key\|auth\.json\|cookie\|password\|[A-Fa-f0-9]{40,}"` | pass | 改动逐条落在 5 条允许路径 + 本工作区；凭据形态扫描对改动文件与全部证据零命中。（其中 `diff --check` 一句已由 E-6814 更正并重跑） |
| E-6811 | decision | `findings.md` F-6805 | pass | 可执行桩 `fake-herdr-bin.mjs` 不在允许路径，**不越界**，改用三段证据链；后由三路复核独立裁定链条足够。 |
| E-6812 | review-dispatch | `herdr agent start dhr68rev1 --kind codex --pane w1:p2V -- --sandbox read-only -c model_reasoning_effort=high --model gpt-5.6-terra` | pass | 代码复核轮 1 派出（fresh、未参与施工）。**只读取证**：派出基线 `0b345e0` 且工作区 clean，回收后 HEAD 仅多出主控自己那笔提交、`git status --short` 为空。 |
| E-6813 | review | `dhr68rev1` 回复正文 | fail | 轮 1 判 FAIL：F-68-R1-01 (P1) fake `agentPrompt` 形态与真实不符；F-68-R1-02 (P2) 已提交范围 `diff --check` 报尾随空白。两条全采纳。 |
| E-6814 | test | `8030709`；`node --test relay-core/test/herdr-adapter.test.mjs`；`git diff --check d6358dc..HEAD` | pass | 轮 1 整改闭合：fake 形态改正、验收 D 升级为机器断言（`DHR_68/D` 读真实对照表当 oracle）、证据尾随空白清理后 `diff --check` exit 0。 |
| E-6815 | test | 逐文件串行 `node --test`（master vs 本树）：`dhr64-result-bridge` / `dhr64-driver-observation` / `attempt-contract` / `profile-identity` / `agent-node` | pass | herdr 血缘范围内无可归因本卡的回归；唯一差异项 `DHR_61 D1` 经两树各跑 3 次证明是双向抖动（等待窗口仅 1000ms）。 |
| E-6816 | review-dispatch | `herdr agent start dhr68rev2a\|dhr68revb\|dhr68revc\|dhr68revd2 --kind codex --pane w1:p2W\|p2X\|p2Y\|p20 -- --sandbox read-only -c model_reasoning_effort=high --model gpt-5.6-terra` | pass | Review Batch 四路并发派出（轮 2 / 需求 / 教训 / 一致性），均 fresh、只读、未参与施工。派出基线 `2ff73a5`，四路回收后 `git status --short` 为空、零写入。 |
| E-6817 | review | `dhr68revb` 回复正文 | pass | 需求方向复核 PASS：P0/P1=0；四条命题-证据逐条对齐；"有没有说过头"逐项核过未发现超证据断言；确认未见自动信任/自动按键/自动重试。 |
| E-6818 | review | `dhr68revc` 回复正文 | fail | 教训复核 FAIL：C-68-01/C-68-02 两条 P1，要求显式标注与候选-6/36 的边界并回链候选-35。已在 `lesson_candidates.md` 落实。 |
| E-6819 | review | `dhr68revd2` 回复正文 | pass | 一致性复核 PASS：超时语义 / 失败语义位 / 启动期 blocked 定义 / fake 与真实形态 / 允许路径五组比对全一致，无新增不一致或越界。 |
| E-6820 | test | 变异：`herdr-executor.mjs` 的 `if (!reconciled) return closeFailedPane(start);` 取反；`node --test --test-name-pattern="DHR_68"` | fail→pass | **有效单测**：由第二轮复核实例 `dhr68rev2a` 选点，其指定的 `DHR_68/A adapter` 测试断言失败；还原后 DHR_68 定向 8/8。 |
| E-6821 | test | 自证红绿：`instructionPending` 判据改回白名单 `['working','idle']` → `node --test --test-name-pattern="blocked 直接跳到 done"` | fail→pass | F-68-R2-01 整改有效：改回白名单即红（`timeout:deferred instruction on blocked->done`），改回 `!== 'blocked'` 即绿。 |
| E-6822 | doc | `lesson_candidates.md` | pass | 教训两条 P1 落实（标注与候选-6/36 边界、回链候选-35），新增 L-6802/L-6803，异步竞态一条按复核结论不新增。入库需按 B-27 先例扩路径并经用户确认。 |
| E-6823 | ops | `herdr pane move`（同 tab 实测 `changed:false`）→ 改用 `pane split` + `pane resize` 重建 | pass | 按用户要求重排为「左会话 + 右侧上下四格」；四路复核结果均在重建前收回，无数据丢失。 |
| E-6824 | miner | `dh mine dh-relay DHR_68` → `evidence/miner-brief.md` | pass | 只读 miner 备料已生成（含候选区 58 条去重对照）。 |
| E-6825 | check | `dh gate`（本树）、`dh dh-relay`（本树与 master 对比） | partial | `dh gate` 对 DHR_68 结构闸全过，唯一 ❌ 是「有 verify 提交命中」——需用户 E11 确认后才可代签。`dh dh-relay` 的失败增量已按 R6/R12/R16/R17/R27 逐条修正。 |
| E-6826 | review-dispatch | `herdr agent start dhr68rev3 --kind codex --pane w1:p31 -- --sandbox read-only -c model_reasoning_effort=high --model gpt-5.6-terra` | pass | 整改复验（fresh，未继承任何前轮会话）：F-68-R2-01 闭合、教训两条落实、无 P0/P1 无越界；另出 F-68-RV-01 (P2) 卫生未持续闭合，已修。复验基线 `4b0eaa3`。 |
| E-6827 | miner | `herdr agent start dhr68miner --kind codex --pane w1:p32 -- --sandbox read-only`；输入 `evidence/miner-brief.md` | pass | 独立 miner 去重：L-6801~L-6803 均不重复；补出主控漏掉的 L-6804、L-6805；判定 F-6807 属候选-11 实例、F-6804/F-6806 不新增。 |
| E-6828 | hygiene | `git diff --check d6358dc..HEAD`（在证据文件最后一次生成之后） | pass | 全提交范围行尾空白干净，F-68-RV-01 闭合。 |
| E-6829 | doc | `as-built/relay-core.md` L280 只读核对 | partial | 既有描述无错但未含本卡三条新行为；因不在允许路径不改，delta 登记为 F-6808。 |
