=== dh mine: dh-relay / DHR_68 ===
（只读备料·不抽教训——抽取归 subagent。把下面整份简报交给一个 fresh-context subagent）

① 工件清单（让 subagent 读这些，按筛子抽『别再踩第二次』的教训）：
  ✓ D:\MyFiles\ai-workflow\dh-relay\.dh-worktrees\DHR_68\docs\modules\dh-relay\workspace\DHR_68\review.md　— 两轮独立复核的 P0/P1 发现（最肥食材）
  ✓ D:\MyFiles\ai-workflow\dh-relay\.dh-worktrees\DHR_68\docs\modules\dh-relay\workspace\DHR_68\findings.md　— 维护/复核问题清单的根因与级别
  ✓ D:\MyFiles\ai-workflow\dh-relay\.dh-worktrees\DHR_68\docs\modules\dh-relay\workspace\DHR_68\progress.md　— 返工/换方向的过程记录（『原以为…其实…』、>2 轮返工）

② 去重对照（动手前先扫，撞正册标『疑似 L-00X』、撞候选区跳过）：
  正册 教训库.md（0 条在册）：
    （空 / 文件不存在——无在册教训，无需对照正册）
  候选区 教训库-候选.md（58 条候选；57 条待裁决）：
    · 候选-1 · 判定函数的每个 reason 码 / 分支都要有断言钉住，并用静态守卫兜底（人肉小审逮不干净）　｜触发：写校验器 / 身份链裁决 / 状态机这类"多分支返回原因码"的契约代码，或复核此类代码
    · 候选-2 · 穷举 / 矩阵测试的期望值不能来自被测数据本身（自证循环）——测试里独立硬编码设计文档的 oracle　｜触发：给状态转换矩阵、枚举表、白名单等"数据即规则"的东西写穷举测试
    · 候选-3 · 验收口径写"多类工件任一残留即失败"时，每类工件要有真实形态的夹具，不得同一字符串换标签循环冒充多类覆盖　｜触发：脱敏 / 清洗 / 校验类需求写"对 N 类工件都生效"，或复核此类测试
    · 候选-4 · 主控预写 task_plan / brief 里的"见下方 XX 表 / 见 §X"内部引用，派活前先机械核一遍目标真实存在　｜触发：主控（或任何人）预写施工计划、给 worker 派活前的自检
    · 候选-5 · PowerShell 默认比较大小写不敏感（`-in/-eq/-ne/-notin`），冻结枚举必须用 `-cin/-ceq/-cne` 或 Ordinal 比对，且修一处要全库 grep 同类算子　｜触发：用 PowerShell 写枚举校验、状态快照判定、任何"精确字面匹配"的 fail-closed 逻辑；返工修同类 bug 时
    · 候选-6 · 钉边界的断言必须配变异对照，「测试全绿」不等于「断言在咬」　｜触发：写契约测试 / 校验器 / 任何「守住某条边界」的断言，或复核此类代码
    · 候选-7 · 校验器不能拿被校验对象当期望来源，重建 oracle 也不能复用被测代码　｜触发：写"对拍 / 校验 / 一致性检查"类脚本，或复核此类脚本
    · 候选-8 · 「禁用后不存在」不等于「卸载后被清理」，探针不能和被卸载对象同生共死　｜触发：验证插件 / 服务 / 组件的生命周期清理
    · 候选-9 · 把正确性押在安装拓扑上迟早会咬　｜触发：写插件 / 扩展包，决定"依赖谁提供、怎么解析"
    · 候选-10 · 文档纠错要 append + 原处加 superseded，不要偷改历史结论　｜触发：后续轮次推翻了早前批次的结论 / 证据 README
    · 候选-11 · 补丁只治好被指出的那个实例，不代表同类全部实例都被治好——机制的覆盖面要按"同类目标清单"逐项巡检　｜触发：给契约/协议演进中反复出现的机制（版本号判定、能力指纹、审计扫描范围、白名单登记）打补丁，且该机制天然对应"N 份同类文件 / N 个同类分支"
    · 候选-12 · 证据只覆盖论断的一部分（一种形态 / 一个合取项），结论却写成整条已兑现——本卡收敛阶段被同一复核者一次性揪出的七条共同病灶　｜触发：验收口径 / 结论陈述包含多个合取项（"A 且 B"）或需要覆盖多种形态（"这条路径下所有情形"）时，只测了其中一部分就写"该条已兑现 / 该风险已消除"
    · 候选-13 · 写下教训不等于教训生效——本卡内主会话违反了自己刚写下的那条教训　｜触发：施工过程中现场识别并记录了一条流程教训（如"派复核前必须冻结产物"），随后同一张卡内继续施工 / 返工
    · 候选-14 · 声称"与上游 / 同仓其它系统保持同一口径"，必须去读对方源文件逐字段核对，不能凭转述或记忆认定一致　｜触发：契约 / 协议设计文档里出现"沿用 XX 的口径""与 XX 保持一致"这类陈述，而 XX 是另一个系统 / 另一批已冻结产物
    · 候选-15 · 校验与发送必须使用同一份稳定快照　｜触发：把生产者提供的对象送入 JSON / RPC / 消息边界
    · 候选-16 · 超长 NDJSON 帧必须丢弃到换行　｜触发：实现流式文本帧协议的长度上限
    · 候选-17 · headless worker 的沙箱不是完工门取证环境——但「环境假红」要凭两条判据裁，不能见红就归咎沙箱　｜触发：派 headless worker（codex workspace-write / 类似受限沙箱）施工并要求其自证完工门
    · 候选-18 · 文件系统上「检查后原子删除」不可得——陈锁自动回收无论加多少判据都留替换竞态　｜触发：设计跨进程文件锁的 stale 回收（mtime / owner token / pid 存活判据）
    · 候选-19 · Windows 收权必须 icacls 三步且测试要预埋外来 ACE——/grant:r 不清既有显式 ACE　｜触发：Windows 上把目录/文件收成 owner-only（DACL）
    · 候选-20 · 私有落点收权要与共享根的可发现性合同分层——独占子目录而非收紧共享根　｜触发：要在双方共享的目录根（descriptor / 发现文件所在）下存私有数据
    · 候选-21 · 断连时必须显式败掉全部在途请求并给终态回调——静默死订阅与吃满超时是同一病灶的两面　｜触发：实现带重连的 RPC/订阅客户端（socket close、重试退避、宿主回调 API）
    · 候选-22 · 重连快照不得抢先推进已送达高水位——服务端快照后还有按旧 cursor 的补发　｜触发：断线重连的事件流客户端维护 after_seq 高水位与快照合并
    · 候选-23 · 并发测试偶发红先做二分归因——证明是构造有洞才改构造，重试永远不是修复　｜触发：多进程/多连接并发用例出现偶发失败（收敛目标被抢、信号落在竞态窗口内）
    · 候选-24 · 协议义务型恢复不占瞬态失败重试预算——两类预算分开计数　｜触发：客户端重试循环里同时存在瞬态失败（断线/超时）与协议规定必须执行的恢复动作（如 cursor gap 后整体重快照）
    · 候选-25 · 返工不断暴露新根因时停止沿原补丁路径加码——G6 止损的「根因翻新」触发形　｜触发：同一条 finding 连续返工，每轮修复都通过了上一轮的复核点、却又暴露出新的根因层
    · 候选-26 · 主控驳回复核发现也是待审对象——驳回论证须派 fresh 窄审逐支攻击　｜触发：主控（或任何裁决方）驳回一条复核 finding，且驳回论证覆盖多个入口/分支（"这些都受 X 机制保护"类论证）
    · 候选-27 · 测试期望与被测代码不得各自读实时时钟——日期边界必然分叉　｜触发：断言里出现按"现在"推导的日期/时间（发号、时间戳、TTL），测试进程与被测进程各自取时
    · 候选-28 · 列表分组与排序是源头合同——源头冻结完整顺序规则，客户端只渲染　｜触发：Read Model / 列表投影跨多个客户端渲染，谁负责排序未落字面合同
    · 候选-29 · 可嵌入库不替宿主选持久化落点——持久账归宿主，库只复用宿主给的幂等标识　｜触发：交付被第三方宿主嵌入的客户端库（adapter/SDK），涉及重试账、缓存、凭据等持久状态
    · 候选-30 · 路径逃逸的词法防护挡不住符号链接——防逃逸必须对根与目标双边 realpath 后判定　｜触发：写"仓内合法路径/禁止越界访问"这类 fail-closed 防护（ref 解析、文件路径校验、沙箱边界判定），或复核此类代码
    · 候选-31 · "全量绿"结论必须带并发/负载条件——套件变大后既有紧预算会偶发超时，闸门形同看运气　｜触发：node --test 一类默认并行跑文件的测试运行器，套件持续新增会起真实进程/服务的测试文件
    · 候选-32 · 冻结契约字段缺口先查"消费者是否已优雅降级"，不要急着扩协议或编造字段　｜触发：客户端/面板需要的字段集与冻结契约能提供的字段集不是子集关系，第一反应是"要不要开新接口/编个兜底值"
    · 候选-33 · 同一抽象概念的多个对称变体（如 pi-agent / dsh-agent）必须逐一参数化核验，测试构造器写死其中一个会让另一个的 bug 永远不会变红　｜触发：系统里同一类事件/流程存在 ≥2 条并行实现路径（不同 executor kind、不同渠道、不同后端），需要写覆盖它们的测试或做跨路径一致性检查
    · 候选-34 · 证据转录必须由脚本现场采集写入，不得手抄/摘要——手抄在独立复核复算时才会露馅　｜触发：把命令输出、哈希值、退出码这类可复算证据写进 evidence/转录文件
    · 候选-35 · 异步测试助手要等"不变量成立"而不是等"期望值出现"，否则断言自证循环且偶发红　｜触发：测试里对异步/多步写入的产物（先写事件账、后写快照这类非原子多步落盘）轮询判定"已完成"
    · 候选-36 · 机器证必须以「验收口径点名的那份产物 + 它真实所处的环境」为输入至少跑一次，仓内副本 + 注入环境只能是补充　｜触发：验收口径的对象是仓外 / 用户级 / 部署态的产物（注册表、配置、清单），而测试跑的是仓内的一份副本
    · 候选-37 · 文本锚定断言对全文做正则会被否定句喂饱——断言要行级定位 + 否定词护栏 + 反向断言　｜触发：拿设计文档 / evidence / 日志的全文做正则锚定断言；尤其文档鼓励写「不支持 XX」「不可证 XX」的体系
    · 候选-38 · 「名字能解析」≠「能执行」≠「能被非交互 spawn」——入口探测分三层，且要登记拉起方式约束　｜触发：给 CLI 入口 / 命令别名做可用性探测并写进注册表、契约或调度器；入口可能是 shim / 函数 / 别名 / 包装脚本
    · 候选-39 · 新增测试文件后必须证明它真被默认测试命令拾取（对比用例数增量），否则「有效单测」是假证据　｜触发：往既有仓新增测试文件，且该测试要作为验收/有效单测的证据
    · 候选-40 · 派发形态冻结了「用哪个模型复核」，就必须有可核验方式确认实际运行模型——否则「换人复核」前提未证　｜触发：编排协议/派发形态冻结执行者或复核者的模型身份，并以此主张复核独立性
    · 候选-41 · 上游卡审计出的、会阻断下游已冻结验收口径的事实，必须升进 findings 并点名被阻断的口径 ID　｜触发：产出物要被下游卡消费的卡（审计/契约冻结/fixture/schema 卡）；批次依赖为链的项目尤甚
    · 候选-42 · 任务卡「目标」段与「验收口径」段必须互相核对，逐字复制的 brief 会把上游摘录遗漏固化成永久丢失　｜触发：验收口径从上游设计文档摘录进任务卡，再被 brief/task_plan 逐字复制下发
    · 候选-43 · 机读清单必须能表达「这条当前不可用」——「能力」与「当前可用性」是正交维度　｜触发：把审计事实冻结成机读注册表/清单供调度器消费
    · 候选-44 · 布尔字段会把「未证」吞成「否」——审计类产物里「不知道」必须有独立表达　｜触发：审计/探测产物的数据结构设计；三态事实（支持/不支持/未证）要落字段
    · 候选-45 · 变异点必须选在「该保护唯一负责的场景」上——变异后仍红要先确认红的原因　｜触发：登记有效单测「改坏必红」变异点；被测保护与其它防线（schema、类型检查）有重叠覆盖时
    · 候选-46 · 护栏用例必须能区分「对」与「错」两种实现——两条分支可观测结果相同的断言是零护栏　｜触发：为「必须走 A 不能走 B」类分叉/优先级规则补测试，且 A、B 在测试构造下都提前短路
    · 候选-47 · 返工清单每个分组都要逐项落表——门槛句只点名部分分组，未点名的那组会整段蒸发　｜触发：复核结论拆成分组返工清单（代码/测试/文档），门槛句写"A/B 逐项落表"
    · 候选-48 · 「环境干扰/环境假红」是要证明的结论——单次绿会把真抖动盖过去　｜触发：worker 报测试挂死/失败，主控换环境复跑一次通过即裁"环境干扰"
    · 候选-49 · 有效单测变异证据必须以最终提交为基线——中间态取的变异证据会被后续返工作废　｜触发：heavy 卡变异取证后还有返工轮
    · 候选-50 · 测试对家目录配置的隐式依赖——外部配置路径一律强制注入，缺省即失败　｜触发：被测代码有"默认读 ~/.xxx/config"回退，测试未显式注入
    · 候选-51 · 挂死≠失败——起常驻轮询对象的 fixture 必须注册停止钩子，套件门槛记 exit code + wall-clock　｜触发：测试里 start 带内部轮询循环的常驻对象（driver/watcher/service）
    · 候选-52 · 派活层事故不落任何工件——progress 需要「工具链/派活事故」条目类型　｜触发：主控经 Herdr/插件/网关派活，出现终端层、模型层、命令层事故
    · 候选-53 · 只读复核做字节级变异不能用会重写全文的编辑工具——向 CRLF 文件写 LF 新行留下 git 不可复原的混合行尾　｜触发：Windows + autocrlf=true 仓库；复核实例做临时变异；worker 用 agent 工具编辑既有文件
    · 候选-54 · 派工前的 fresh 只读预审（审 brief/task_plan 本身）是投入产出比最高的一道闸　｜触发：heavy/高危卡，主控冻结架构裁决后、派工前
    · 候选-55 · task_plan 里冻结引用的既有教训会被 worker 自建 helper 悄悄绕开，且历轮复核都不核它　｜触发：task_plan/brief 写「一律用 X」「按候选-NN 做」类冻结引用
    · 候选-56 · formal validator 的负例通过不等于 runtime consumer 已闭合——消费点要用实现级 mutation 证明严格选项真正生效　｜触发：formal validator 已能拒绝坏 alias/config，但 runtime loader/driver 另行调用 validator；验收要求证明消费链启动前 fail-closed
    · 候选-57 · 脱敏 synthetic fixture 也要保持正式同类目标的完整 ID/字段形状/关系图，局部副本不能支撑任一/全部声明　｜触发：验收禁止读取 live registry/config，却要求测试覆盖正式 registry/清单全部条目及 fallback/可选字段关系
    · 候选-58 · 新增异步测试的有效性要同时过 runner 拾取与终态屏障两道门，平台外部探测的 timeout 必须有界且足够　｜触发：往显式 test list 的既有仓新增测试文件，同时测试启动带外部命令探测/轮询的 driver/watcher

③ 候选格式（抽出的每条按此写，字段对齐正册、裁决时直接搬）：
    ### 候选-N · <一句话标题>
    触发场景：<…> · 建议分类：<领域知识/编码陷阱/行为流程> · 来源：<review 第2轮 P1 / 病历-yy 根因>
    疑似重复：<无 / L-00X>
    - 现象：<一句>
    - 反思：<一句根因>
    - 建议后续动作：<下次怎么做>
    状态：待裁决

④ append 落点：D:\MyFiles\ai-workflow\dh-relay\.dh-worktrees\DHR_68\docs\modules\dh-relay\knowledge\教训库-候选.md
    抽完在 workspace/DHR_68/progress.md 记一行指针：『本次 miner 产出 N 条候选 → 候选区』（gate 软报告靠它判跑没跑）。

⑤ 派单模板（spawn subagent 时的一句话指令）：
    读 DHR_68 工作区的 review.md / findings.md / progress.md，按筛子抽『别再踩第二次』的教训候选：
    review 两轮 P0/P1（根因类、非一次性笔误）/ findings·病历根因 / progress 返工信号；
    排除一次性手误·环境抖动·流水账·已在册（先扫上面②两张去重）。
    每条按③格式写、append 到 教训库-候选.md 状态『待裁决』。判据：下个类似任务会不会再撞？会→抽，纯偶然→扔。

（只读派生、不落盘；候选 ≠ 入册，正册只进人裁决过的——miner 只能往候选区堆）
