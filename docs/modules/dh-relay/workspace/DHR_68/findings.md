<!-- dh:v1 -->
# DHR_68 · Findings

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|---|---|---|---|---|---|
| F-6801 | P2 | B-32 把缺陷 A 记为"10 秒 spawnSync 上限"，但未写明 Windows 的命中分支：现役 `invoke` 只在 `child.signal` 为真时判超时，而 DHR_35 实测的失败串是 `spawn:ETIMEDOUT`（走 `if (child.error)`）。只按 `child.signal` 实现超时语义位会漏判，A 的对账分支永远不会触发。 | `workspace/DHR_35/evidence/f3508-root-cause/production-defects.json`（分支 `wt/DHR_35`）的 `observed_failure`；`herdr-cli.mjs` `invoke` 失败分支 | 已按 `child.error?.code === 'ETIMEDOUT' \|\| child.signal` 双路径实现并由 `DHR_68/A herdr-cli` 定向断言（E-6804 红 / E-6806 绿）。不改 DevPlan 文字（验收口径本身没错，只是实现细节需补）。 | resolved |
| F-6802 | P1 | 验收项 C 只写"driver 不发 completion instruction"，没写**解除 blocked 之后**由谁补发。若严格按字面实现（永不发），真实 Codex/Claude 在用户完成信任后将永远收不到 Receipt-bound 提交指令，DHR_35 的闭环仍然跑不通——本卡等于把"启动失败"换成"启动后静默挂死"。 | 卡片验收口径 C；`workflow-driver.mjs` L256~L263（`receiptBound` 时唯一一处发指令） | **主控取解释**：`instructionPending` 延后补发——启动即 blocked 时不发，状态首次离开 `blocked` 时补发恰好一次。理由：这仍属"启动期 blocked 的事件路径"，不触碰 Result 判定，且是唯一能让 C 不与 P6-RI-A4 相矛盾的读法。2026-08-31 用户对话点选确认「延后补发」（E-6809），与已实现语义一致。 | resolved |
| F-6803 | P2 | 起草与 B-32 都把缺陷 A 的依据写成"实测 max 29482 ms"，属于经验数字。实读 `herdr agent start --help`（herdr 0.8.2）发现 herdr **自己**的 `agent start --timeout` 默认就是 30 000 ms（max 300 000）——旧的 10 秒通用上限**低于 herdr 自身的合法等待窗口**，是结构性砍断而非"偶尔慢"。 | E-6802 | 已把该事实写进 `HERDR_START_TIMEOUT_MS` 的注释作为 60 秒的可证依据；同时确认不能改用传 `--timeout` 的办法（验收 B 要求 `agent start` 的 argv 不变）。 | resolved |
| F-6804 | P2 | `DHR_68/C driver` 在并发全量里失败、单跑通过。查明是**测试的采样竞态**：blocked 只持续约 6 ms，`runtimeUntil` 等的是事件数组、而事件先入内存、派生状态稍后收敛，负载高时取样取到后续的 `running`。生产代码无问题。 | E-6808 | 改测试而**不**改生产代码：粘性全 blocked 序列 + 等派生状态 + 拆成两个只断言单调条件的夹具 + 等待窗口 45 秒。 | resolved |
| F-6805 | P3 | 验收 D 理想上还应让可执行桩吐"exit 0 + 空 stdout"，但 `relay-core/test/helpers/fake-herdr-bin.mjs` **不在本卡允许路径**。 | DevPlan `dh:allowed-paths:v1 task=DHR_68` | **不越界**。改以三段链条替代：真实 probe 证明形态（E-6801）→ `fake-herdr.mjs` 编码该形态 → 桩测断言 `typeof paneRun().value === 'string'` 证明 wrapper 不解 JSON，另加 `DHR_68/D` 读对照表做逐命令机器比对。**轮 2 / 需求 / 一致性三路独立裁定链条足以支撑验收 D**（验收对象是 `fake-herdr.mjs` 的返回形态，不是未授权的可执行桩；桩仍输出 JSON 只记 P3）。 | resolved |
| F-6806 | P2 | 本仓 `npm test` 在 `--test-concurrency=4` 下**基线本身就不稳定**：master 273 例 15 失败、本树 278 例 16 失败，且失败集在两次运行间互不包含（master 独有 `basic-agent-task`、`service：并发同键 start`；本树独有 `DHR_61 D1`、`CLI pending 账 50 路并发`），单例耗时冲到 60~74 秒。这不是本卡引入的，但它让"全量绿"无法作为任何卡的收口证据。 | E-6807 | 本卡改用串行定向复跑做对比，不以全量结果做判定；把"默认测试入口并发不稳"记为 backlog 候选，不在本卡修（越界）。 | open · 记账不修 |
| F-6807 | P2 | **恢复届（recovery）有同类缺陷、但本卡范围内没修。** `workflow-driver.mjs` 的 `if (receiptBound && recovery)` 分支在重新接管既有 Attempt 时**无条件**发 completion instruction；若该 agent 此刻仍停在产品的目录信任框上（正是本卡 C 要处理的 blocked 现场），这条指令同样会被打进信任对话框。与启动期属同一缺陷类。 | `workflow-driver.mjs` 的 recovery 发指令分支；对照本卡对启动期的 `instructionPending` 处理 | **不擅自扩范围**：DevPlan 限定 driver「只可改启动期 blocked 的事件路径」，恢复届不是启动期。本卡只登记，不改。轮 2 / 需求 / 一致性三路**均支持主控判定 ②**：recovery 不属于「启动期 blocked 事件路径」，应保留为独立缺陷，后续经 B-调整或新卡处理。**本卡不修，维持 P2 跟踪**，已在 DevPlan 下一步与 backlog 方向登记。**miner 补充定性**：本条是 [候选-11]「补丁只治好被指出的那个实例」的直接实例，承接卡应按「同类目标清单逐项巡检」处理，不要只修 recovery 一处。 | open · 范围外跟踪 |
| F-68-R2-01 | P1 | `instructionPending` 的补发判据原是白名单 `['working','idle']`，**漏掉 `done`**。信任确认后 agent 若很快转 `done`，提交指令整条不发，随后进 `waitForExecutorResult` 空等，最终误落 `E_EXECUTOR_RESULT_MISSING`——与 F-6802 已冻结的「首次离开 blocked 后补发恰好一次」不符。 | 复核轮 2 `dhr68rev2a`；`workflow-driver.mjs` 补发分支 | 判据改为「不再是 blocked」（`observation.herdr_status !== 'blocked'`），并补 `DHR_68/C driver：blocked 直接跳到 done…` 定向测试。自证红绿：改回白名单即红，改回新判据即绿（E-6821）。 | resolved |
| C-68-01 | P1 | DHR_67 的 fake 形态漂移是 [候选-6]/[候选-36] 已覆盖的「假绿」重蹈，本卡 `lesson_candidates.md` 初版未标注与二者的边界。 | 教训复核 `dhr68revc` | 已改写为 L-6801，显式写明与候选-6（断言不咬）、候选-36（被测对象≠验收对象）的边界，并给出可执行检查动作。 | resolved |
| C-68-02 | P1 | `DHR_68/C driver` 的采样竞态与 [候选-35] 同构，初版候选未回链。 | 教训复核 `dhr68revc` | 已在「不新增」节回链候选-35，并记下"竞态在测试侧就改测试、不要松动生产逻辑"的修法。 | resolved |
| F-68-RV-01 | P2 | 轮 1 的卫生整改**未持续闭合**：修好尾随空白后我又重跑了一次绿测覆盖 `evidence/green-run-herdr-adapter.txt`，行尾空白随新输出回来，`git diff --check d6358dc..HEAD` 再次非零。 | 整改复验 `dhr68rev3` | 已对 evidence 下全部 `.txt`/`.md` 重新剥离行尾空白并重跑，`git diff --check d6358dc..HEAD` exit 0。教训沉淀为 L-6804：**凡会被重新生成的证据，卫生检查必须排在生成之后**。 | resolved |
| F-6808 | P3 | **E7 as-built 无法在本卡更新**：`docs/modules/dh-relay/as-built/relay-core.md` 不在本卡允许路径。其 L280 对 Herdr adapter 的描述（launch/observe/capture/reconcile/stop、`done` ≠ succeeded）在抽象层面仍然成立、无错误，但本卡新增的三条行为——启动专用 60s + 超时只读对账、`pane run` 非 JSON、启动期 blocked 保留 handle 并转一次人工暂停——**未反映进快照**。 | DevPlan `dh:allowed-paths:v1 task=DHR_68`；`as-built/relay-core.md` L280 | **不越界**。显式登记该 delta，留给下一张能碰 as-built 的卡（或收口时经用户授权做路径最小扩展）补写。不静默跳过 E7。 | open · 范围外登记 |
