# task — DHR_50 DSH 对证与三态补录

> 开工确认：2026-08-21 用户明示“开工，开worktree，人验了再喊我”。
> worktree：`.dh-worktrees/DHR_50`；branch：`wt/DHR_50`。

## 完成条件 ★前置（继承任务卡验收口径）

1. P4-CM4：对同一份冻结 fixture 的 CLI 输出与 DSH 面板分别完成字段级对证；DSH 已有两屏，故列表与详情都必须覆盖。真实终端转录或等价渲染截图必须留档；若拟记 `pass`，必须先有两轮独立只读对证复核。
2. P4-CM6b：对 DHR_26 / DHR_49 / DHR_50 的桌面轨范围完成审计，证明未引入 Relay 运行写权、未 fork DSH。
3. 只在 `design/evidence/10-P4-多控制面Pilot报告.md` 的附录追加：CM4、CM6b、主报告 `DM-deferred-facts:` 的诚实差额、P4-X 状态与用户三态 / H2 / H3 人判材料；不得回改主报告正文或由 AI 代裁三态。

## 施工步骤 ★精简（🔵 开工那一刻写）

1. 只读收集 DHR_25 / 26 / 27 / 49 及仓外 Pilot 的 fixture、CLI 转录、DSH 渲染证据和版本事实，建立逐字段对证表与证据索引。
2. 对证完成后，若结果可判为 CM4 通过，安排两轮互不继承上下文的只读复核；否则如实记录覆盖边界或 N/A。
3. 对桌面轨允许范围做 CM6b 审计，并将主报告落盘后的新增 DM 事实同 `DM-deferred-facts:` 清单逐项核对。
4. 在报告附录和本工作区登记机器事实、约束与待用户判断项；向用户展示可判断材料，等待三态 / H2 / H3 的明确对话表态。

## 进度 + 证据（边做边记）

| 时间 | 做了什么 | 证据（命令/路径/结果） |
|------|---------|----------------------|
| 2026-08-21 | 主树落户，待建任务 worktree | 本文件；`dev_plan/P4-DSH工作台最小Pilot-开发方案.md` `dh:status`；用户开工确认 |
| 2026-08-21 | 建树后已 rebase 本地 `master`；只读重跑 CLI 与整理 DSH 两屏证据 | `evidence-index.md` E-501~E-505；`cm4-field-comparison.md`：可见字段未见差异，CM4 尚待两轮独立复核 |
| 2026-08-21 | 完成 CM6b 范围的运行时写权 / DSH fork 审计 | `cm6b-audit.md`：运行时写/网络词边界扫描 0 命中；Pilot 无 `.git`；结论待随附录统一冻结 |
| 2026-08-21 | 两轮互不继承上下文的 CM4 只读复核已回收；Round 1 的 P2-1 已补表处置 | `review-round1.md`（0 P0 / 0 P1 / 1 P2）+ `review-round2.md`（0 P0~P3）；`cm4-field-comparison.md`；CM4 通过 |
| 2026-08-21 | 用户完成 DSH 三态与 H2/H3 人判 | 对话确认：`passed-with-constraints`；H2 选 DSH 为 Windows 日常首选控制面并指定已登记运行任务的一键终端跳转方向；H3 不适用；报告附录 A.6 |

## 验收

- AI 自评：完成条件逐条达成。CM4 = E-501~E-505 + 两轮独立只读复核；CM6b = E-506/E-507；诚实差额、v1 缺口归属与人判 = 报告附录 A.3/A.4/A.6。
- 关闭方式：用户已在对话中明示三态，并回答适用的 H2；H3 因 DSH 未判否而不适用。AI 未代签、未替用户选择终态。

> 本卡为轻档，但 CM4 拟记 `pass` 时仍强制两轮独立只读对证复核；CM4 判 `N/A` 时该例外不适用。
