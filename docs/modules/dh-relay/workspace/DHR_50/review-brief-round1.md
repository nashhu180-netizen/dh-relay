# DHR_50 · CM4 只读对证复核 Round 1

你是**只读复核者**，不是主控，不做施工、不改代码、不运行会写入的命令、不问用户。只审以下结论：DHR_50 是否可将 P4-CM4 记为 `pass`。

必读：`task.md`、`cm4-field-comparison.md`、`evidence-index.md`、DevPlan §3.2 DHR_50；可按索引只读外部证据。核查字段表是否诚实区分 UI 可见、payload 等价覆盖与未覆盖边界；核查列表/详情是否确为同一 fixture；核查 DM2 的目标机缺口没有被偷换成已证。

在 `review-round1.md` 写：复核身份/日期/只读范围、逐项结论、P0~P3、对“CM4 能否 pass”的明确建议与理由。不要修改其他文件。
