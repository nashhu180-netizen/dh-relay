<!-- dh:v1 -->
# progress — DHR_72

## 日志

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|---|---|---|---|---|
| 2026-09-04 | 主会话 | 用户授权 D-start 至 E10；`dh wt new DHR_72` 建树后发现分支起点为 origin/master，已在零改动现场 rebase 到 `master@bbeffc4`。 | E-7201 | 读冻结代码与测试，补测试先行方案。 |
| 2026-09-04 | 主会话 | 代码侦察确认 `driveHerdrNode` 的 `done || idle` 分支在 `E_EXECUTOR_RESULT_MISSING` 后直接 return；同时发现 DHR64 idle 回归仍等待 `driver.done`。 | E-7202 | 等待最小 B-adjust 决定；不越权改 DHR64 测试。 |
| 2026-09-04 | 主会话 | DHR-B-37：fresh 只读审核确认 done/idle 共用提前 return，用户确认两种尚无 Result 的状态都持续观察；仅补入 DHR64 的两条回归用例。 | E-7203 | 写 DHR_72 红测，再最小修改循环。 |
| 2026-09-04 | 主会话 | DHR64 红测后最小循环实现已使 DHR64 3/3 与 DHR72 idle→working→checkpoint 专属测试转绿；默认 test script 扫描发现 identity-quota 的多条旧 `await driver.done` 断言会与新合同冲突。 | E-7204 | 等待是否将该历史测试模块纳入本卡最小回归改写的范围裁决。 |
| 2026-09-04 | 主会话 | DHR-B-38：fresh 只读审核确认 `identity-quota.test.mjs:131-336` 的九条用例依赖已删除的 `idle/done → herdrJudge → terminal Result` 旧合同；用户确认拆开，DHR72 不改该文件，另卡决定正式 Result 是否驱动 quota fallback。 | E-7205 | 本卡继续持续观测与直接回归；配额生产语义和历史用例均不被静默弱化。 |
| 2026-09-04 | 主会话 | 解除 F-3520 的四条 skip：三条 `herdr-adapter` 改为正式 Receipt-bound 提交、现役 stop/recovery 断言；一条 `agent-node` 改为 killed Attention。`:305` 改为 Attention 后仍有后续 poll。 | E-7206 | 定向五个直接回归文件退出 0；四条 skip 已解除。 |
| 2026-09-04 | 主会话 | 新增 DHR72 专属“idle→working→checkpoint→下一 idle episode”与 poll/timeout 比例守卫；临时把专属 fixture `herdrPollMs 20→5`，守卫按预期断言失败，已还原 20 后转绿。 | E-7207 | 机械守卫已可拦截 poll 回退；待第二轮 fresh 选择生产代码变异点。 |
| 2026-09-04 | 主会话 | 以 `node --test --test-timeout=5000 test/identity-quota.test.mjs` 试跑旧 quota 文件，30 秒无终态输出后主动终止，未将其伪报为绿。 | E-7208 | 这是 B-38 所记录的旧合同阻塞证据；不改该文件、不更改 package script。 |
| 2026-09-04 | fresh reviewer | 第一轮代码静态审回收：P0=0/P1=0/P2=1；P2 限定 poll 守卫是 fixture/回归配置守卫，不能扩大表述为全局生产配置防护。 | E-7209 | 审查不重跑测试，结论与范围已写入 review。 |
| 2026-09-04 | 主会话 | B-39 经三路 fresh 复核发现与用户确认：迁移三条直接生命周期回归，保留 quota 另卡；同步补齐允许路径文字。 | E-7210 | 三条定向用例均已转绿，仍待 B-33/五出口补证与第二轮复核。 |
| 2026-09-04 | 主会话 | 补 DHR72 自有 B-33 负例（`agent_get=idle ∧ pane_get=blocked`）及 committed、stop、host_lost、Attempt 已终态、actor-closed/lease-lost 六种退出后的零追加事件直接断言；先后修正 Receipt gate 与 actor 关闭测试的同步点。 | E-7211 | B-33 与各出口隔离命令均通过；整份专属测试两次受 BL-17 `state.json.*.tmp → state.json` 的 `EPERM` 阻断，不能报为全绿。 |
| 2026-09-04 | 主会话 | 用户许可后从 `DHR_35@3836fd3` 复制 runner/redactor，并仅改为 DHR72 的 DSH-off checkpoint 取证；不发送 Receipt Result。真实冻结 Codex Profile 一次运行在 `checkpoint-observation` 失败；临时 agent 已 `done`，事件仅到 `attempt_started`，未见 `checkpoint_recorded`；未对 Herdr 的 `agent_blocked` 提示输入或重试。证据已脱敏。随后移除复制脚本遗留的 `send-keys enter`，并将 `agent prompt` 非零退出改为立即失败。 | E-7212 | 此次是失败实录，不得充当 DHR72 的 checkpoint 成功证据，更不得用于 DHR35；启动并未停摆，故不套用 F-3516 的自动重试。 |
| 2026-09-04 | 主会话 | 重跑专属连续观测首测时复现 `state.json.*.tmp → state.json` 的 Windows `EPERM`；将测试内高频 `readState()` 改为 checkpoint 后等待 fake 状态稳定再单次读取持久快照，并给 Receipt-bound 回归补齐“`attempt_started` + `hasOpenSubmissionGates`”同步条件。修后 DHR72 专属套件 `8/8`、poll 比例守卫 `1/1`、冻结五文件绿闸 `55/55`（0 skip/0 fail），`agent-node` 边界钉独立复跑 `1/1`；冻结套件中出现一次终态后 Attention 写入被 Store 正确拒绝的 fail-closed 日志，未追加事件。 | E-7213 | 机器证 A~E、G、H 的当前测试证据已恢复；机器证 F 仍缺真实 Codex `checkpoint_recorded`，不把本轮单测结果写成人验或真实实录。 |
| 2026-09-04 | 主会话 | 按已确认的 B-39 范围，把 DHR_72 brief 补齐「覆盖任务」与显式允许路径，并同步 DevPlan 主清单；review 的完成条件改为与 brief 逐字一致，真实实录行明确记为“不满足”。`dh` 复跑已不再报告 DHR_72 的 R3/R12/R17/R30；全模块仍有 DHR_74 的 R30/R31，以及本次未提交 DevPlan 同步期间的 R29。 | E-7214 | 不改 DHR_74 状态/verify；本次只形成 DHR_72 本地检查点，真实 Codex checkpoint、第二轮 fresh 复核、需求/教训/一致性复核与 E11 仍未完成。 |
| 2026-09-04 | 主会话 | 用户明文要求主会话自行处理交互。隔离 fresh Codex 显示已是 YOLO、稳定 fixture 无信任框；长多行 prompt 会停在输入框并报 `agent_prompt_stalled`，等价短单行 prompt 可真提交，5 秒 `node` 命令退出 0。 | E-7215 | 交互/审批假设已排除；该手工 probe 不属于 Relay checkpoint，也不冒充机器证 F。 |
| 2026-09-04 | 主会话 | 又做三次 DSH-off 实录定位：无论 PATH shim、Herdr 注入的真实二进制，还是把 runtime service 放进受管 pane，账本都只到 `attempt_started`，无首条 `host_observation_changed`。三份 `host-lease.json` 均显示 lease 先到期，`attempt_started` 后落盘；最后一份为 `expires_at=09:11:23Z`、`attempt_started.at=09:11:32Z`。实验性 runner 改动已回退，证据已脱敏。 | E-7216、E-7217 | 根因边界已落在同步启动期间的 lease/host/service 生命周期，不在 DHR_72 允许路径；按 F-3516 停止新增实录，机器证 F 保持未满足。 |
| 2026-09-04 | 主会话 | 收尾重跑 DHR72 专属 8 例得到 6 pass / 2 timeout：首例等 `idle attention`、stop 例等 checkpoint 超时；本轮没有生产/测试代码 diff，不能把此前 E-7213 的 8/8 绿证改写成当前绿，也不能据此归因。 | E-7218 | 保留原始失败事实；不因负载猜测重写测试或扩大本卡范围。 |
| 2026-09-05 | 主会话 | 在干净 `wt/DHR_72@bfc6e72` 上 rebase `master@084a00d`；DevPlan 冲突保留主干的 DHR75/76 最新状态，`package.json` 合并保留 DHR75/76/72 三组测试入口。rebase 后专属首跑 7/8，失败为 checkpoint 后已进入下一 idle episode 的采样竞争；fixture 改为显式切换第二个 idle episode 后专属 8/8、比例守卫 1/1、冻结五文件 55/55。 | E-7219 | 生产代码未因本轮测试竞争调整；进入真实 F。 |
| 2026-09-05 | 主会话 | 真实 F 的前五次新基线运行均已启动隔离 Codex，但 runner 停在 `agent-instruction`：逐步把多行 prompt 收短、补 Herdr 手册规定的 Enter 回退、局部捕获 PowerShell native 非零退出，并把 Enter 后观察窗从 400ms 调整为手册规定的 5s；每次失败现场均为 agent `idle`，不冒充 F-3516 启动停摆或 checkpoint 证据。 | E-7220 | runner 输入通道验证逻辑闭合后重跑。 |
| 2026-09-05 | 主会话 | 第六次 DSH-off、冻结 `herdr.codex.main` 实录 exit=0：采集时 `run_status=running`、checkpoint_count=1；保存账本最终含 4 条 `checkpoint_recorded`、2 条 `host_observation_changed`、0 条 Result 终态。DSH=0、临时 fixture 内容=0；run6 窄范围不可逆脱敏后 dry-run 0 命中。 | E-7221 | 机器证 F 已满足；进入 heavy 自动收口复核，不替代 DHR35。 |

## 证据账本

| ID | 类型 | 命令 / 路径 | 结果 | 支撑什么结论 |
|---|---|---|---|---|
| E-7201 | session-run | `dh wt new DHR_72`；`git rebase master`；`git status --short --branch` | pass：`wt/DHR_72@bbeffc4`，工作树干净 | D-start worktree 已隔离且基线为最新 master。 |
| E-7202 | session-run | `relay-core/`: `node --test --test-concurrency=1 --test-timeout=30000 test/dhr64-driver-observation.test.mjs` | exit=0；3 pass；idle 用例现有断言等待 `driver.done` | 若 DHR_72 落实长期 idle 不结束，该用例必须改为观察 Attention/持续轮询后显式 stop；但文件不在允许路径。 |
| E-7203 | review-dispatch | `b-adjust-review.md`（fresh-context 审核实例 `/root/dhr72_badjust_review2`） | pass：零写入；建议 done/idle 同改且证明后续 poll | B-37 已经用户确认；DHR64 两条用例纳入 DHR_72 的最小允许范围。 |
| E-7204 | session-run | `rg -l "await .*driver\\.done" relay-core/test` 后逐文件核对 | observed：`identity-quota.test.mjs` 多处以 idle/done→`driver.done` 作为旧终态合同 | 完整默认测试入口还需要该模块同步，现允许路径未覆盖。 |
| E-7205 | review-dispatch + user-decision | fresh `/root/dhr72_quota_review_retry` 只读 `identity-quota.test.mjs:131-340` 与 `workflow-driver.mjs`；用户明文「可以」确认拆卡 | pass：九条旧 quota 用例不可仅改测试后保留原 fallback 断言；零写入 | DHR72 明确排除该文件；新卡须先定 Receipt-bound Result 与自动 fallback 的业务语义。 |
| E-7206 | session-run | `node --test --test-concurrency=1 --test-timeout=300000 --test-reporter=spec test/herdr-adapter.test.mjs test/agent-node.test.mjs test/dhr64-driver-observation.test.mjs test/dhr69-false-ready.test.mjs test/dhr70-submission-gate.test.mjs` | exit=0；55 tests / 55 pass / 0 skip / 0 fail | DHR72 的冻结五文件绿闸已恢复；新专属/守卫另有 E-7207。 |
| E-7207 | mutation | 先 `node --test --test-concurrency=1 --test-timeout=60000 --test-reporter=spec test/dhr72-poll-guard.test.mjs`；临时将 `dhr72-continuous-observation` 的 poll `20→5` 后重跑；再还原并重跑 | green 1/1；变异 0/1，`AssertionError: ... poll interval`；还原 green 1/1 | DHR74 R31 移交的比例守卫有实际红证，但 DHR72 自身生产代码变异点仍待第二轮 fresh 选定。 |
| E-7208 | bounded probe | `node --test --test-concurrency=1 --test-timeout=5000 --test-reporter=spec test/identity-quota.test.mjs`，30 秒未产生终态输出后终止 | non-terminal，未称 pass/fail | 仅证明旧用例与持续观察合同相撞；不作为 DHR72 回归失败归因。 |
| E-7209 | review-dispatch | fresh `/root/dhr72_code_review1_retry` 只读根 AGENTS、DHR72 工件、当前 diff 与相关 driver/tests | P0=0；P1=0；P2=1 | 只读静态审，未替代 E-7206/E-7207 的已运行证据；P2 边界已如实保留。 |
| E-7210 | B-adjust + session-run | `dhr64-result-bridge` missing、`dhr70` C、`herdr-adapter` blocked→done 分别按 Receipt Result/显式 stop 改写；三条 `--test-name-pattern` 定向命令 | 3/3 pass；每条均 exit=0 | B-39 不改 quota，不改生产语义；DHR70 提交后另断言零后续事件。 |
| E-7211 | session-run | `node --test --test-concurrency=1 --test-timeout=60000 --test-reporter=spec test/dhr72-continuous-observation.test.mjs`；隔离 `--test-name-pattern='fails closed'` | B-33、committed、stop、host_lost、terminal 在整份运行中各 pass；actor-closed/lease-lost 修正同步点后隔离 2/2 pass。整份两次均出现 `EPERM: operation not permitted, rename ...state.json.<uuid>.tmp -> ...state.json`，首次连续观测用例 timeout，exit=1。 | 新增直接断言覆盖 B-33 与五出口；BL-17 形态在 DHR72 临时根复现，不能把隔离绿或局部通过写成整份绿。 |
| E-7212 | DSH-off real run | `run-windows-checkpoint.ps1 -ProfileId herdr.codex.main -TimeoutSeconds 300`；预检 `dsh_process_count=0`，运行后 `dsh_process_count=0` | 失败阶段 `checkpoint-observation`；事件类型为 `run_created`、`lease_acquired`、`operation_committed`、`node_started`、`attempt_started`，无 `checkpoint_recorded`；agent 状态 `done`。redactor 处理 2 个标识、7 个文件、2 个文件名，仅保留不可逆关联符。 | 未向 `agent_blocked` 交互提示输入；该证据只证明本次 DHR72 实录未达到 checkpoint，不能代替 DHR35，且实录成功证仍缺。 |
| E-7213 | session-run | `node --test --test-concurrency=1 --test-timeout=60000 --test-reporter=spec test/dhr72-continuous-observation.test.mjs`；`node --test --test-concurrency=1 --test-timeout=60000 --test-reporter=spec test/dhr72-poll-guard.test.mjs`；冻结五文件命令（`herdr-adapter`、`agent-node`、`dhr64-driver-observation`、`dhr64-result-bridge`、`dhr70-submission-gate`）；`agent-node` 边界钉定向复跑 | pass：DHR72 `8/8`；守卫 `1/1`；冻结套件 `55/55`、0 skip、0 fail；边界钉 `1/1`。修前首测和隔离 TEMP 对照均复现 `EPERM`，修后上述测试未再出现该签名；Receipt gate 竞态路径出现终态后旧 Attention 被拒日志，但无后续事件。 | 支撑机器证 A~E、G、H；不支撑机器证 F 的真实 Codex checkpoint，也不支撑 E11 人判。 |
| E-7214 | session-run | `dh dh-relay`（DHR_72 worktree） | DHR_72 的 R3/R12/R17/R30 未再报告；整体 exit=1 仍由 DHR_74 R30/R31 与当前 DevPlan scope 同步触发的 R29 构成，未修改 DHR_74。 | 只证明任务卡结构与范围同步结果；不把全模块体检、真实 checkpoint 或任何人验写成通过。 |
| E-7215 | bounded Herdr probe | 稳定 fixture 中 fresh Codex：长多行 prompt；随后 fresh agent 用等价短单行 prompt 跑 `node -e "setTimeout(() => {}, 5000)"` | 长 prompt=`agent_prompt_stalled` 且文本留在输入框；短 prompt 真提交，命令 exit=0；产品显示 YOLO、无信任/审批提示 | 排除“仍待用户点信任/审批”作为当前硬阻塞；不支撑 Relay checkpoint。 |
| E-7216 | DSH-off real runs | `evidence/windows-checkpoint-20260904T085203Z/`、`...T090013Z/`、`...T091018Z/`；每次 `ProfileId=herdr.codex.main`、`TimeoutSeconds=300` | 三次均 failure；事件类型均仅 `run_created, lease_acquired, operation_committed, node_started, attempt_started`，无 host observation/checkpoint/Result；redactor dry-run 为 0 | 证明失败发生在 DHR72 轮询入口之前，不能用来否定或证明持续观测实现；机器证 F 仍缺。 |
| E-7217 | evidence audit + isolated adapter/store probes | 对三份 `failed-run/host-lease.json` 与 `events.jsonl` 比时；真实 `launchHerdrAgent` 隔离调用；失败副本上单独追加同形 host observation | 三次 lease 均早于 attempt 到期；最后一次相差约 8.5 秒。adapter 隔离调用可返回 ready idle，Store 副本可接受同形 host observation；问题收敛到 service/host 在同步真实启动期间失租后的 driver 写权，而非 Herdr 产品交互、adapter 返回形状或 Store schema | 修复需进入 brief 明确排除的启动/recovery 或 host/service/launcher；按边界停止，不在 DHR72 伪续租或改禁改路径。 |
| E-7218 | session-run | `node --test --test-concurrency=1 --test-timeout=60000 --test-reporter=spec test/dhr72-continuous-observation.test.mjs` | exit=1；8 tests / 6 pass / 2 fail；失败均为等待事件 timeout，无新增代码 diff | 当前收尾复跑不是绿；E-7213 仍只是较早的已提交绿证，本卡因机器证 F 与本轮非绿均不可进入复核收口。 |
| E-7219 | test | `git rebase master`；专属 `dhr72-continuous-observation`；`dhr72-poll-guard`；冻结五文件命令（`herdr-adapter`、`agent-node`、`dhr64-driver-observation`、`dhr64-result-bridge`、`dhr70-submission-gate`） | rebase 成功，merge-base=`084a00d`；首跑 7/8 后以显式 fake episode 切换修复采样竞争；复跑 exit=0：8/8、1/1、55/55，0 skip/0 fail | 新主干基线的机器证 A~E、G、H 当前为绿；DHR75/76 测试入口未丢。 |
| E-7220 | bounded real-run remediation | `evidence/windows-checkpoint-20260905T-run1/` 至 `run5/`；`scripts/run-windows-checkpoint.ps1` | 五次均在 `agent-instruction` 失败，host-view 均为隔离 Codex 已启动且 idle；未进入 checkpoint 判定。逐步落实短单行 prompt、native exit 捕获、最多两次 Enter 与每次 5s lifecycle 验真 | 这些是 runner 控制面失败，不计作机器证 F，也不写成 F-3516；失败证据均已脱敏。 |
| E-7221 | DSH-off real run | `run-windows-checkpoint.ps1 -ProfileId herdr.codex.main -EvidenceRoot .../windows-checkpoint-20260905T-run6 -TimeoutSeconds 300`；`redact-receipt-ids.mjs --root ...run6 [--dry-run]` | runner exit=0；summary elapsed=42281ms、run_status=running、checkpoint_count=1；保存 events 中 checkpoint=4、alive observation=2、Result=0；DSH=0、fixture children=0；脱敏 dry-run files_changed=0/renamed=0 | 等价满足机器证 F；仅证明 DHR72 新基线真实 checkpoint 可达，不消费 DHR35 P6-M1。 |
| E-7222 | check | `dh dh-relay` 分别在 `master@084a00d` 与 `wt/DHR_72` 执行 | 当前实时 master 为 3 failure（DHR75 R18/R31、DHR74 R31），与开工交接的“仅 DHR74 R31”不一致；任务分支另因 pending 卡共享 diff 归属出现 DHR75/DHR74 R30，并曾报 DHR72 R16（证据类型未写成 `test`，本行前已更正） | 不改 DHR75/DHR74 伪清零；DHR72 只修自己的证据类型，后续复核须按实时输出裁决。 |
| E-7223 | review-dispatch | Herdr fresh read-only code_round_2 candidate eb89e61 | observed | 复核派出：dhr72_code2_0905｜review-batch-brief-20260905 code_round_2 |
| E-7224 | review-dispatch | Herdr fresh read-only requirement_direction candidate eb89e61 | observed | 复核派出：dhr72_req_0905｜review-batch-brief-20260905 requirement_direction |
| E-7225 | review-dispatch | Herdr fresh read-only lessons candidate eb89e61 | observed | 复核派出：dhr72_lessons_0905｜review-batch-brief-20260905 lessons |
| E-7226 | review-dispatch | Herdr fresh read-only consistency_review candidate eb89e61 | observed | 复核派出：dhr72_consistency_0905｜review-batch-brief-20260905 consistency_review |
| E-7227 | mutation | `dhr72_code2_0905` fresh 选择 `workflow-driver.mjs:389-400`：idle/done 分支写 Attention 后临时插入 `return`；运行指定单测后用原补丁精确还原 | 变异 exit=1，`timeout:working checkpoint`；还原后 1/1、exit=0；生产 Git blob=`505ff9454863bd319429b33c27e5ca2d3cf7ae69`，`git diff --exit-code`=0 | 第二轮有效单测闭合；未提交变异代码。 |
| E-7228 | review-remediation | 一致性初审 P1：结构化 launch `receipt_id` 与 operation 文件名误标为 `att~`；改为字段/协议感知归一并全卡应用 | files_changed=21、files_renamed=10；随后 dry-run=0/0；`receipt_id:att~` 零命中、错误 launch 文件名=0；原复核者 `dhr72_consistency_0905` RECHECK PASS | Receipt/Attempt 角色语义恢复；仍只主张最终证据根已脱敏，不反推原值从未短暂落盘。 |
| E-7229 | review-collect | fresh 代码二轮、需求、教训、一致性四路结论回收并落 `review.md` | code2 P0/P1=0、P2=1；requirement P1=2（#7 已整改，#6 待 master H）；lessons P0/P1=0、P2=2；consistency P1=2（Receipt 已复验闭合，review 脱节由本行后落账闭合） | 自动复核在 E10 前只剩 DHR_72 入 master 后才能执行的 H 回签，不伪造 DHR_74 清零。 |
| E-7230 | lesson-miner | `dh mine dh-relay DHR_72` + fresh `/root/dhr72_miner_review` 去重抽取 | 产出 2 条：候选-82（native prompt/Enter 验真）、候选-83（Windows atomic rename reader 竞争）；正册 0 条，已对照候选-35/67/72/77 等 | 仅 append 候选区，状态待裁决；未改教训正册。 |
| E-7231 | final-regression | 收口整改后串行重跑专属测试、poll 守卫与冻结五文件；复跑全卡 redactor dry-run 与结构化 ID 扫描 | 专属 8/8、守卫 1/1、冻结 55/55（0 skip/0 fail，exit=0）；dry-run files_changed=0/files_renamed=0；原 Receipt/Attempt 字段与 `receipt_id:att~` 均零命中 | 最终候选机器证保持绿；未执行真实 F 第二遍，E-7221 的已保存实录仍为本卡 F。 |
| E-7232 | report | `review.md#E10-放行证据包releasepacket-dhr72-v1` | ready | 阶段汇报@E9 与 releasePacket-DHR72-v1 已备料，将在本轮对话展示；shownVersion=`428f21e`；H 尚待 merge 后执行，当前全仓 5 failure 如实列出；等待 E11 明确裁决。 |
| E-7233 | human-gate | 用户对 releasePacket-DHR72-v1 明文“认可”（2026-09-05） | accepted | E11 仅放行本地 squash/主干回归/H 回填；强闸不绿时保留任务树与未 verify 状态，不含 push/deploy/DHR_73/DHR_35。 |
| E-7234 | mutation | `master@13c072d`：按轮 2 选点在 missing-Result Attention 后插入 `return`，运行指定用例后精确还原 | 变异 exit=1，`timeout:working checkpoint`，apply blob=`fba0ac031801c1636d29a1fb16d851fe8eb33e84`；还原后 1/1、exit=0，revert blob=`505ff9454863bd319429b33c27e5ca2d3cf7ae69`，生产 diff=0 | 对照原 E-7227 补齐 scanner 要求的可反查 Git 对象；没有提交变异代码。 |
| E-7235 | integration-test | `master@13c072d`：专属、poll 守卫、冻结五文件 | pass | 专属 8/8、poll 1/1、冻结 55/55（0 skip/0 fail），全部 exit=0。 |
| E-7236 | docs | E7 as-built 已更新并覆盖现状快照 | pass | `docs/modules/dh-relay/as-built/relay-core.md`第 13 节已记录持续观测与五出口。 |
| E-7237 | report | E9 交付汇报已发出 | pass | releasePacket-DHR72-v1 已在对话展示，用户随后明文“认可”。 |
| E-7238 | human | E10 人验证据展示区已发出 | pass | 用户认可真实 F 只证明 DHR_72，不替代 DHR_35。 |
| E-7239 | check | `master@13c072d` 上 `dh dh-relay`，并按 scanner 合法格式补齐 DHR_72 变异登记后复跑 | fail | DHR_72 自身 R18/R31 已清；全仓仍 5 failure，其中 DHR_74 R31 仍为 `mutation-table-missing`，说明当前 scanner 未实现 DHR_72 H 跨卡传递。本轮不补造 DHR_74 表、不改状态绕闸、不生成 verify；保留 DHR_72/DHR_74 任务树。 |
