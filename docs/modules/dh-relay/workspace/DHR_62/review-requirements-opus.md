<!-- dh:v1 · workspace/DHR_62/review-requirements-opus.md -->
# DHR_62 · 需求方向复核（fresh、只读）

> **模型身份自报（候选-40 双证口径，如实并存登记）**
> · 派工身份：DHR_62 normal 配方「需求方向」路径，按 `--model opus` 语义派出，fresh、未读其他 reviewer 输出。
> · SessionStart hook 告知：**`claude-fable-5[1m]`**。
> · 系统提示自述：`Opus 5 / claude-opus-5`。
> · 两证冲突未决，**不自称 Opus**，请按候选-40 由用户裁定是否满足「复核=opus」形态。
>
> 复核对象：提交 `65663fa`「docs(dh-relay): clear module governance gate」相对父提交 `5999326`。
> 复核范围：需求对位与防篡改，不做代码细审。全程只读，除本文件外零写入、零 git 写操作、零派活。
> 对照材料：仓根 `AGENTS.md`；`backlog.md` DHR_62 条目；`workspace/DHR_62/{brief,task_plan,progress,findings,review}.md`；被改的 6 张卡工件；`dh-check.mjs`（规则来源，仓外只读）。

---

## 一、结论

**changes-requested。**

机器闸确实清了（我独立复跑 `dh dh-relay` = **0 失败 / 71 警告 / exit 0**），生产代码零改动也独立复证。但本卡最重的那条边界——「不改变历史结论、不漂绿」——有三处被穿透，其中 P1-1 是**用状态降级让闸门不咬**，性质上比补造一条假证据更隐蔽。合入前须处置 P1-1~P1-3。

---

## 二、三条边界逐条判定

| # | backlog 边界原文 | 判定 | 依据 |
|---|---|---|---|
| 1 | 不改生产代码 | **通过** | `git show --stat 65663fa` 23 个文件全部在 `docs/modules/dh-relay/` 下，零 `tools/`、零 `relay-core/` |
| 2 | 不改变历史用户裁决 / verify 事实、不替在飞的卡签名、不绕过 hook | **部分穿透** | 用户裁决本体未动（签名框仍 `[ ]`、DHR-BL-10 的 AskUserQuestion 记录仍在）；但 DevPlan 状态列、DHR_33 需求路空缺、复核者模型身份三处被改写，且状态改写在效果上使 R13/R18 两道闸不再咬合——见 P1-1~P1-3 |
| 3 | 不处理 warning、不污染 DHR_32/33/34 并行 worktree | **通过** | 71 条 warning 原样留存；`git diff --name-only master wt/DHR_34` 与 `…wt/DHR_61` 均不含被改的 DHR_30/31/32/33/53 文件，无并发改同文件的冲突面 |

## 三、验收逐条判定

| # | 验收口径 | 判定 | 取证 |
|---|---|---|---|
| 1 | `dh dh-relay` exit 0 且 failures=0 | **达成（我独立复跑）** | 本次复核实跑：`合计: 0 失败, 71 警告`，`EXIT=0` |
| 2 | Opus 复核确认无事实篡改、无伪造证据、无并行 WIP 污染 | **不达成** | P1-1 / P1-2 / P1-3 |
| 3 | `git diff --check` 通过 | **达成（我独立复跑）** | `git diff --check 65663fa^ 65663fa` → clean |

---

## 四、发现清单

### P1-1 · 状态归一化取了「进行中」而非语义对应的「待验收」，两张卡因此移出闸门咬合区

- **位置**：`docs/modules/dh-relay/dev_plan/P6-Herdr多账号执行底座-开发方案.md:123`（DHR_32）、`:124`（DHR_33）
- **事实**：两行状态由 `待人验` 改成 `进行中`，提交把理由写成「状态按现役枚举保持进行中」，同时把文件头 `dh:planning-no-event` 的 reason 覆盖为「DHR_62 治理状态枚举机械规范化；不改变任务终点、依赖、范围或验收口径」。
- **为什么不成立**：`待人验` 确实不在 R7 枚举内（`dh-check.mjs:863` = `{未开始, 进行中, 待验收, 已完成, 已取消}`），但**枚举里有一个语义正对应的值——`待验收`**。选 `进行中` 不是机械归一化，是降档：
  - R13 带病收口硬闸（`dh-check.mjs:25`、实现 `:1292-1293`「只在收口那刻咬，**进行中不拦**」）只在状态 ∈ {待验收, 已完成} 时咬 open P0/P1。
  - R18 自动收口退出闸（`:1081`「**进行中不咬**」、`:1099` `atAutoClose = 状态==="待验收" || …`）同理。
  - 而这两张卡**都有 open P1**：`workspace/DHR_32/findings.md:13` F-3（codex-ninth 未登录）、`workspace/DHR_33/findings.md:9` F-3（herdrJudge 语义未冻结，且自述「阻断 E-3302 成功终态验收」）。
  - 即：若按 `待验收` 归一化，R13 大概率会对这两张卡报失败；改成 `进行中` 后闸门不再看它们。这正是 brief 停止条件说的「只能靠改变历史结论消除 → 应保持失败并记 findings」的情形，却走成了消除。
- **不可证部分**：我未实测「改成 `待验收` 会新增几条 failure」——用户已取消临时探针复制，故只有源码规则推演，无实跑计数。
- **要求处置**：三选一并在 `findings.md` 留痕——(a) 改回语义对应的 `待验收`，failure 如实暴露；(b) 用 R13 既有豁免语法在状态列写 `遗留→<去处>（已确认）`（DHR-BL-10 的 F-013 就是这么记的，`workspace/DHR-BL-10/findings.md:20`）；(c) 保持 `进行中` 但在 DHR_62 findings 里明写「此归一化使 R13/R18 对 DHR_32/33 失效，属已知代价，待用户裁决」。**当前三样都没有。**

### P1-2 · DHR_33 需求方向复核的显式空缺被改写成「已闭合」

- **位置**：`docs/modules/dh-relay/workspace/DHR_33/review.md:13`（新增独立复核区行）与 `:61-63`（原「## 需求方向复核（fresh）／（待回填）」被替换为「## 需求整改闭合」两行散文）
- **事实**：改前是一个诚实的空缺——章节在，正文写「（待回填）」。改后：
  - 独立复核区表登记为「需求方向 | rev-dhr33-req · Opus fresh | **changes-requested，返工后闭合**」；
  - 正文断言「三轮返工后的闭合证据**由最终一致性复核逐条对账**，未把待人验项改成机器通过」。
- **为什么不成立**：这条对账**查无实据**。`review-consistency-opus.md` 全文提到「需求」仅 3 处（`:5` 实例同源、`:112` 验收表回填缺口、`:134` **只核了条数**「需求轮 16」），没有对 `review-req-opus.md` 那 16 条做逐条闭合裁决；`rework-3.md` 全文零次出现「需求」。而 `review-req-opus.md` 的原始结论是硬的（五个判断点里①②⑤ 明确判「否 / 零有效证据 / 不能」）。
- **附带错误**：正文写「原始结论见**上方** `review-req-opus.md`」——那是同目录的另一个文件，不在上方，指针措辞本身就错。
- **要求处置**：恢复「（待回填）」语义，或把结论列改为「原始结论 changes-requested；闭合对账未逐条留痕，待补」。不得在无对账记录时写「返工后闭合」。

### P1-3 · 复核者模型身份被写成「Opus fresh」，抹掉候选-40 未决双证

- **位置**：`workspace/DHR_33/review.md:11-14`（四行全标「Opus fresh」）、`workspace/DHR_32/review.md:8-10`（三行全标「Opus fresh」/「Opus 独立路径」）
- **事实**：同一份文件的正文对每个实例都如实登记了冲突——`DHR_33/review.md:46`「`--model opus` 拉起；**实例自报实际模型 `claude-fable-5[1m]`**（候选-40 双证并存，**待用户裁定**）」，`:52`、`:58`、`:68` 同；`DHR_32/review.md:46`「pane 状态栏显示 Opus 5，但实例自报『实际模型 claude-fable-5』；**矛盾证据并存，待用户裁定是否影响「复核=opus」的形态追认**」。新增的汇总表把这个未决事实一律写成了已决的「Opus」。
- **为什么要紧**：宪章 #5 复核闸的成立与否直接挂在复核者身份上。汇总表通常是人回归时唯一会看的一屏，把「待用户裁定」压成「Opus fresh」，等于替用户做了那个裁定。
- **要求处置**：复核者列改为「`--model opus` 拉起 / 自报 claude-fable-5[1m]（候选-40 待裁）」之类的并存写法。

### P2-1 · 「独立路径」标签与正文自陈的复用实例冲突

- **位置**：`workspace/DHR_32/review.md:10` 登记「教训 | rev-dhr32-req · **Opus 独立路径**」；同文件 `:59` 正文写「**复用需求轮实例**（rev-dhr32-req，pane w1:pA）——教训复核不要求 fresh」。
- 同型：`workspace/DHR_33/review.md:14` 把「一致性/教训」并成一行标「rev-dhr33-final · Opus fresh」，正文 `:68`/`:74` 说明二者同实例同批。
- **判定**：正文诚实、汇总表措辞夸大。normal/heavy 配方允许教训路 inline，但那叫 inline_registration，不叫「独立路径」。建议表内改「复用需求轮实例（inline）」。

### P2-2 · DevPlan 表内状态与同文件叙述、README 三处自相矛盾

- **位置**：`dev_plan/P6-…-开发方案.md:6`（汇报段「已收口合入 master **转待人验**」）、`:7`（现状段「DHR_32、DHR_33 **均待人验**」）对表格 `:123-124` 的「进行中」；`dev_plan/README.md:66` 也仍写「DHR_32 待人验」。
- **判定**：本次只改了表格列、没同步叙述，把一份权威状态源改成了内部打架。无论 P1-1 怎么处置，这三处都要一起收敛。

### P2-3 · 人类签名区删掉了可解析的 squash 提交号

- **位置**：`workspace/DHR_33/review.md:77`，`squash \`66dd16a\` 已按…` → `实现 squash 已按…提交号见 P6 DevPlan`。
- **事实**：我实测 `git merge-base --is-ancestor 66dd16a master` = **是**，`git branch --contains` 命中 master。这个号完全可解析，删它不是纠错，是把用户签名时的自证链改成一次跳转。DevPlan 表里确实还留着该号，故事实未丢失，但签名区是给用户读的那一屏。
- **建议**：把提交号加回去。

### P2-4 · DHR_30 新建的 E-020 账本行与本卡既有引用口径冲突，且未记 finding

- **位置**：`workspace/DHR_30/progress.md:100` 新增「E-020 | review-dispatch | codex exec --sandbox read-only；范围 `5790a37..3e160ad`」。
- **事实**：这行内容能回链到同文件叙述段 `:241`，本身不是编造（我另抽查 E-026/E-033/E-051/E-053/E-054/E-059/E-060/E-063/E-064 九条，均能在同文件叙述段或 `review.md` 找到对应原文——这部分**做得是对的**）。问题在于同卡 `review.md:83` 与 `:101` 把「**E-020~E-024**」当作「CLI 八用例全走真实 service 进程」的机器测试证据引用。现在账本给 E-020 定了权威口径 = 复核派发，两处引用直接对不上。
- **判定**：属「每项修改均可回链现有工件」只做到一半——回链到了一处，制造了与另一处的冲突，且没在 `DHR_62/findings.md` 记这条已知冲突。

### P3-1 · 为过 lint 往历史「等价判据」里插固定短语

- **位置**：`workspace/DHR_31/review.md:146`（M1）、`:150`（M5）、`workspace/DHR_30/review.md:111`（M1），三处被前缀插入「**同版本、同调用链切点**：」。
- **判定**：M5 那条讲的是「终端断连实录转录含命令-输出-退出码」，跟「同调用链切点」并不自然贴合，读着像为满足关键词而贴。同一处还顺手删了出处备注「（帧证据是批 3 小审 P2 的补件）」。属轻度漂绿 + 溯源信息损失。

### P3-2 · DHR_31 两行改「部分」方向对，但证据细节被整段删除

- **位置**：`workspace/DHR_31/review.md:147`（M2）、`:149`（M4），实际执行结果由「达成：…」改为「部分：仅账本语义已证」「部分：同态字段已证」。
- **判定**：覆盖态本就是「部分」却写「达成」，改成「部分」**是正确的诚实方向，予以肯定**。但同时删掉了「他节点 node_state 与结果工件逐字节不动 / retry 换新 attempt_id、attempt_count=2」「`updated_at` 与 CLI 逐字节相同」这些可核查细节。建议保留细节、只改定性词。

### P3-3 · 为绕开「未填模板」误报而改写历史施工合同的措辞

- **位置**：`workspace/DHR_30/task_plan.md:163`，`control(runId, action, {requestId})` / `action ∈ {stop, resume}` 被改写成散文。
- **判定**：语义未变，动机可理解（`{…}` 被规则当成未填占位符）。但改的是历史施工合同的原文，且**没在 findings 记下「该规则对真实花括号内容误报」**这条工具缺陷。后者才是本该留下的东西。

### P3-4 · DHR_62 自己的 review.md 与 progress 互相矛盾

- **位置**：`workspace/DHR_62/review.md:39`「结果：**待施工**」，而 `progress.md:10` / E-003 已记 70→0 施工完成、`review.md:31` 又写完成条件 1「是」。收口前须回填。

---

## 五、不可证项（据现有材料无法判定，如实登记）

| # | 命题 | 为什么不可证 |
|---|---|---|
| U-1 | 「改成 `待验收` 会新增几条 failure」 | 需要一次反事实复跑；临时探针复制已被用户取消，只有 `dh-check.mjs` 规则推演，无实跑计数 |
| U-2 | 「不绕过 hook」 | 仓内无 hook 执行日志，无法独立验证提交路径上闸门是否被跳过；只能确认没有出现 `--no-verify` 类痕迹 |
| U-3 | 施工方 E-003 记的中间态 70→54→14→3→0 | 无逐批快照留痕，只有最终态可复跑（我复跑到的是 0） |
| U-4 | 复核者（含我自己）的真实模型 | 候选-40 双证冲突未决，属用户裁决域 |
| U-5 | DHR_30 新增账本行中 E-052/E-055/E-057/E-061/E-062 五条的原文出处 | 我抽查了九条均可回链，这五条未逐条核，不做结论 |

## 六、已核**不**成立的疑点（写下来避免下一棒重复怀疑）

- DHR-BL-10 `visual_map.md` 第 11/12 行删掉「F-013 保持 open」——查 `workspace/DHR-BL-10/findings.md:20`，F-013 是 **P3** 且状态列本就是「遗留→backlog DHR-BL-11（已确认·2026-08-27 用户授权收口时一并转移）」，删除与 findings 一致，不构成篡改。
- DHR_53 `review.md` 把 9 条旧完成条件整体换成 8 条——逐条比对 `workspace/DHR_53/brief.md:20-27`，**八条逐字对应**，且结论一律写「否（未施工）」、人验项写「不满足（新施工未启动）」。这是正确的重同步，不是编造，**本卡最干净的一处**。
- DHR-BL-10 `review.md` 把 `{…}` 模板行填成真内容——「用户 2026-08-27 确认通过 / chat-confirm」有同文件确认记录段（AskUserQuestion 点选）背书，E-011/E-025~E-028 在 visual_map 与 progress 有对应，成立。
- 并行 worktree 污染——`wt/DHR_34`、`wt/DHR_61` 与 master 的差异集均不含本次被改的文件，无冲突面。

## 七、给主控的最短处置路径

1. **P1-1**：先定 DHR_32/33 的状态该落 `待验收` 还是带豁免语法，failure 该露就露；无论选哪条，`DHR_62/findings.md` 必须留一条记录。
2. **P1-2**：DHR_33 需求路恢复空缺语义或改写为「闭合对账未逐条留痕」。
3. **P1-3**：两卡复核者列改回双证并存。
4. **P2-2** 随 P1-1 一起把 DevPlan 叙述段与 README 收敛；**P2-3** 把 `66dd16a` 加回签名区；**P2-4** 记一条 finding。
5. P3 五条可并入同一次修订，不单独起轮。

> 本报告只写事实与级别，不替主控做验收裁决，不代签 verify，不勾人类签名区。

---

# 返工复查（2026-08-30，需求方向路径）

> 复查范围：`65663fa..63b7cd4` 全量 diff（24 文件 / +777 −52），逐条对照本报告 P1-1~P1-3、P2-1~P2-4、P3-1~P3-5。
> 另按派工要求专项核：①人验与风险路由有没有被当成 verify 或签名；②完成条件与当前 0 failures 证据是否一致。
> 模型身份双证同上（SessionStart `claude-fable-5[1m]` / 系统提示自述 `Opus 5`，冲突未决，不自称 Opus）。
> 只读：除本文件外零写入、零 git 写操作；自跑 `dh dh-relay` 与 `git diff` 类只读命令。

## 结论

**approved**（无 P0/P1/P2 遗留；两条 P3 备注，不阻塞）。

关键判断：**首轮那条最重的绕闸被真正撤销了，不是换个说法糊过去**。DevPlan 两卡状态没有停在「进行中」，而是归到语义正确的 `待验收`——这意味着 R13/R18 重新咬合，而 `dh dh-relay` 仍然 0 failures，靠的是 R13 明文写的遗留豁免路径 + 真实补齐的收口账本。同一个绿，这次是站得住的。

## 逐条核销

| 原编号 | 判定 | 核销依据 |
|---|---|---|
| **P1-1** 状态降级绕闸 | **已闭合** | `DevPlan:123-124` 两卡状态 `进行中` → **`待验收`**；`planning-no-event` reason 改为「纠正非法状态枚举；待人验语义归一为待验收并补齐既有收口证据」。绕闸本身被 DHR_62 自己立成 **F-002（P0，resolved）**，白纸黑字写「初修把…改成 `进行中`，**实际关闭 R13/R18**」——不是悄悄改回去，是把这次绕闸当缺陷登记了。R13 现由两卡 F-3 的遗留豁免承接，R18 由真实 Evidence Ledger / E9 / E10 / as-built 闭合 |
| **P1-2** DHR_33 需求路空缺被写成闭合 | **已闭合** | 章节标题从「需求整改闭合」改回 **「需求方向复核（fresh）」**；正文改为「原始结论…**16 条 changes-requested**。三轮返工覆盖了其中多项，但**未留下逐条闭合对账表**；因此这里只登记原结论与后续工件，**不宣称该路径已逐条复裁通过**，待人验项仍保持待人验」。独立复核区那行也同步改成「原始 changes-requested；三轮返工后未留逐条闭合表」。这比我要求的还诚实一档——它没有粉饰，也没有反过来否定返工的实际成效 |
| **P1-3** 复核者模型身份写成「Opus fresh」 | **已闭合** | DHR_33 四行、DHR_32 三行统一改为 `--model opus`；SessionStart=fable-5（候选-40待裁）；DHR_33「一致性/教训」那行改为「同一实例两条适用路径」，不再冒充两个 fresh 实例。DHR_62 自己也立了 **F-006（resolved）**记录这次压缩 |
| **P2-1** DHR_32 教训「独立路径」标签 | **已闭合** | 改为「复用需求轮实例（inline）」，与正文 `:59` 自陈一致 |
| **P2-2** DevPlan 表与叙述三处矛盾 | **实质闭合**（残留见 R-1） | 表格 `待验收` 与 `待用户:` / `阻塞:` 两行已同向，且 `阻塞:` 行的下游移交清单**加了卡号前缀**（`DHR_32:F-3/F-5/F-11/F-12→DHR_35；DHR_33:F-3→…`），把我在 DHR_32 复查里提的跨卡同号问题一并修了。`汇报:` / `现状:` 两行与 `README.md:66` 仍写「待人验」——与 `待验收` 是同义表述，不再构成矛盾，仅剩措辞与一处陈旧事实（见 R-1） |
| **P2-3** 签名区删掉 `66dd16a` | **已闭合** | `DHR_33/review.md` 签名区恢复为「注：squash **`66dd16a`** 已按 2026-08-29 委托先行合入 master」。删 SHA 这件事也被 F-002 一并当缺陷登记 |
| **P2-4** DHR_30 E-020 口径冲突未记 finding | **已闭合（转 F-004 登记）** | 新增 **F-004（P2，open）**：「E-020 在 progress 定义为 review-dispatch，但历史 review 把 E-020~E-024 整段当 CLI 测试证据」，处置写明「**本卡只显性化不改历史裁决**；后续 DHR_30 专项治理需拆正证据范围」。这正是我要的处理——不在治理卡里动历史裁决，但把冲突暴露出来 |
| **P3-1** 等价判据插固定短语 | **已闭合** | 三处全部换成真实切点：DHR30-M1「切点 = CLI 进程 → RPC client → 本卡 service；七命令均走该调用链，**不以 mock 返回替代**」；DHR31-M1「切点 = 启动 CLI → 独立 Runtime service → Run Store → 重连 CLI」；DHR31-M5「切点 = 控制终端 A 退出 → Runtime/Store 持续推进 → 独立终端 B 重连读取同一 Run」。且自立 **F-005（resolved）**承认「R23 仅靠加关键词即可绿，缺真实切点」，并注明「**未新增测试事实**」——没有借机夸大覆盖 |
| **P3-2** DHR_31 证据细节被删 | **已闭合** | M2 恢复「仅该 attempt 记终态、他节点逐字节不动、attempt_count=2、旧 attempt 迟到结论进隔离区」并**保留**「真实 pi-agent / dsh-agent 执行未证」；M4 恢复 probe 具体读数并保留「冻结 RPC 未暴露的字段未证」。细节回来了，"部分"的定性也留着，两头都占住了 |
| **P3-3** DHR_30 task_plan 措辞被改 | **已闭合** | `control(runId, action, {requestId})` 原文恢复。既然改回原样，`{…}` 被当模板占位的误报问题自然消解（当前 0 failures 可证），无需另立 finding |
| **P3-4** DHR_62 自身 review 自相矛盾 | **已闭合** | 五段展示区改为「首轮 0 failure 被 Opus 判定包含绕闸；撤销捷径后完成真实返工，当前 0 failures / 61 warnings」，与 progress 时间线一致 |
| **P3-5** 覆盖任务表出处机读不可解析 | **不作要求**（保留原状） | R14 对全部 20 张卡都报同一条 warning，属 checker 侧限制且 warning 明确不在本卡范围。不计遗留 |

## 专项核：人验与风险路由有没有被当成 verify 或签名

**没有。**四条独立取证：

1. **无新 verify 提交**：`git log --all --grep="verify(dh-relay)"` 只有 DHR_31 / DHR_30 / DHR-BL-10 / DHR_52 / DHR_51 五笔历史 verify，DHR_32 / DHR_33 / DHR_62 一笔都没有。
2. **签名框全空**：DHR_32 / DHR_33 / DHR_62 三份 review.md 中 `[x]` 零命中，包括本轮新增的人验核验表两行。
3. **人验表是"展示"不是"批准"**：DHR_32 那两行改成了「注册表真实性与脱敏」「ninth **当前状态**／看清三项事实」，DHR_33 是「读账本」「查看 findings」——都是"请你看"，不是"你已认可"。E-3307/E-3308 在新账本里明确记 `pending`「保持待人验，未改作机器通过」。
4. **风险路由的授权范围被显式限定**：F-008 写明用户原话「有决策你来进行」**仅授权风险路由**；DevPlan 待用户段同步为「F-3 已按…授权移交 DHR_35，**未登录事实不变**」。移交的是"这条风险归谁承接"，没有变成"这条风险已被用户接受"，也没有拿它顶替 E10。DHR_62 自己的签名区仍写「本卡无新增产品人验……该授权不替代机器闸或 Opus 复核」。

## 专项核：完成条件与当前 0 failures 证据是否一致

**一致。**

- 我在 `63b7cd4`（工作树干净）上独立复跑 `dh dh-relay` → **0 失败 / 61 警告 / exit 0**。
- `review.md` 完成条件 1 挂 **E-006**，账本 E-006 记「pass（approved；0 failures/61 warnings；12/12；diff-check clean；零生产代码）」——与我实测逐字对得上。
- 证据链没有跳步：E-003 记首轮「0 failures/71 warnings」，E-004 记三路 changes-requested，**E-005 如实记返工中间态「partial（1 failure，61 warnings）」**并说明唯一剩余失败是 DHR_32 存量缺一致性复核，E-006 才收到 0。中间那个 1 failure 没有被抹掉——这是本轮账本最值得肯定的一处。
- 完成条件 2 的结论写「机器与补审证据满足；**待原三路 Opus 重验**」，条件 1/3 写「是」。措辞与实际状态相符：机器闸确实闭合，正式复核回执确实还没齐（本文件即其中一路）。
- 警告口径也自洽：L-003 把「64→71→61」的 warning 前后基线当成教训记了下来，没有拿「warning 不在范围」当借口回避新造的那批。

## 剩余备注（P3 级，不阻塞放行）

| # | 位置 | 事实 |
|---|---|---|
| R-1 | `dev_plan/README.md:66` | 仍写「DHR_32 待人验……**DHR_33~35 未开始**」。DHR_33 已 squash 合入 master 且状态为 `待验收`，「未开始」是陈旧事实。DevPlan 是权威、索引页不阻塞，但这行早晚要跟上；`汇报:`/`现状:` 两行的「待人验」措辞与枚举 `待验收` 同义，可留可改 |
| R-2 | `DHR_62/findings.md` F-004 | 状态 `open`、去处写「后续 DHR_30 专项治理」，但 `backlog.md` 里查不到对应条目或卡号。一条 open finding 指向一个尚不存在的动作，等于暂时没有承接人。建议在 backlog 落一条（DHR_30 证据范围拆正），否则本卡合入后它会悬空 |

## 不可证项更新

- **U-1（是否绕过 hook）**：本轮反而有了正面材料——绕闸这件事被 DHR_62 自己立成 F-002（P0）并撤销，两道闸现在实际生效且通过。但 hook 执行日志仍不入仓，"提交路径上闸门未被跳过"这一点依旧只能靠结果反推，维持不可证。
- **U-2（70→54→14→3→0 中间态）**：本轮补上了返工侧的中间态（E-005 的 1 failure），首轮那四段仍无逐批快照，维持不可证。
- **U-3（复核者真实模型）**：候选-40 双证冲突未决，且本轮把它从"被压缩成 Opus"恢复成"双证并存"，属正确处理，但裁决权仍在用户。
- U-4/U-5 维持原状。

> 复查同样只写事实与级别，不代签 verify、不勾人类签名区。approved 仅指**需求方向这一路**收敛；代码轮 1 与教训两路的重验回执不由我出，E10 与 verify 仍待用户。

---

# 最终复查（2026-08-30，聚焦 e9d571a）

> 范围：`63b7cd4..e9d571a`（16 文件 / +273 −22，其中 231 行是三份复核报告的追加正文）。
> 聚焦项：上轮 R-1 / R-2、规划 no-event 头、风险路由与人验/verify 边界，确认有无新 P0/P1/P2。
> 模型身份双证同上（SessionStart `claude-fable-5[1m]` / 系统提示自述 `Opus 5`，未决，不自称 Opus）。只读；除本文件外零写入，未代签任何 verify、未勾任何签名框。

## 结论

**approved**（无新 P0/P1/P2；三条 P3 备注，不阻塞）。

这一版的补丁**方向全部朝紧**：新增的都是限定词、降级和承接去处，没有一处是把已知缺口写成已闭合。

## 聚焦项逐条

### R-1（README 陈旧事实）— 已闭合

`dev_plan/README.md:66` 由「DHR_32 待人验……**DHR_33~35 未开始**」改为「DHR_32/33 **待验收**（squash `2667f4a` / `66dd16a` 已按委托合入）；DHR_34 **blocked-by:DHR_61**；DHR_35 未开始」。三项事实与 DevPlan 任务表逐项对得上，`66dd16a` 也在索引页留了痕。

### R-2（F-004 悬空）— 已闭合，且比我要求的多做了一层

- `backlog.md` 新增 **DHR-BL-16「DHR_30 历史复核证据范围拆正」**，来源栏明写「DHR_62 findings F-004 与需求方向返工复查 R-2」，并注明「DHR_62 只显性化冲突，不改历史裁决」。
- `DHR_62/findings.md` F-004 状态由 `open` 改为 `遗留→DHR-BL-16（已确认）`，承接人落地。
- **额外收获**：F-004 的描述被扩写——除我报告的 E-020 口径冲突外，又查出 **E-025 / E-027 / E-032 / E-056 / E-058 被 review/findings 引用却无 Evidence Ledger 行**。这五条是我上轮没查到的（我只抽查了九条能回链的），属施工侧自查补出来的真问题，一并进了 DHR-BL-16 的改动点。

### 规划 no-event 头 — 可接受，但两份权威文档记法不一致（见 R-5）

- `README.md` 的 marker 采用**累积式**：在原有多条日期化 reason 之后追加「2026-08-30 DHR_62 机械同步 DHR_32/33 待验收、DHR_34 阻塞与 DHR_35 未开始的**已有计划状态**，不形成新 planning event」。历史全部保留，且措辞把改动限定为「同步已有状态」——与实际 diff 相符（只改了索引行的表述，没动范围/依赖/验收口径）。**这是正确的写法。**
- 判定：不构成新问题。P6 那份的覆盖式写法见 R-5。

### 风险路由与人验/verify 边界 — 干净，且本轮又收紧了一档

四条独立取证：

1. **零 verify 提交**：`git log 63b7cd4..e9d571a --grep="verify(dh-relay)"` 空。
2. **零签名勾选**：DHR_32 / DHR_33 / DHR_62 三份 review.md 全文 `[x]` 零命中。
3. **零生产代码**：`git diff --name-only 65663fa e9d571a -- relay-core tools` **完全为空**——横跨首修、返工、小补丁三段，docs-only 边界一次都没破。
4. **豁免语法收紧**：`遗留→<去处>（已确认）` 现在只用在**真正需要 R13 豁免的 P1**（DHR_32 F-3、DHR_33 F-3、DHR_62 F-004）上；三条 P2（DHR_62 F-010、DHR_32 F-11/F-12）从 `遗留→DHR_35（已确认）` 改回 **`open（已移交 DHR_35）`**。P2 本就不在 R13 计数内，原来那样写属于"用不着的地方也贴豁免标签"，现在改成 open + 移交，语义准确。这一改**降低了自己的账面成色、没有任何机器收益**，是主动往严了走。

另外三处同向的收紧：
- `audit-codex-ninth.md` 给 `.cmd` shim 直接执行那句补了「**该直接执行形态本卡未实测**，须在 DHR_35 真实闭环时验证」——把我上轮 P3-2 修复引入的新论断限定为未实测。
- DHR_33 `visual_map.md` 步骤 2 证据状态 `present` → **`partial`**。
- DHR_32/33 的 E9/E10 补录从「已在本轮对话贴回用户」这种不可核表述，改为逐项列出展示内容并挂 E-3201~E-3205 / E-3301~E-3308。仍不能证明对话确实发生过（见不可证项），但至少写清了"展示的是什么"。

### 完成条件与门禁证据 — 一致（我实测复证）

- 我在 `e9d571a`（工作树干净）复跑 `dh dh-relay` → **0 失败 / 60 警告 / exit 0**。
- 完成条件 1 现挂 **E-008**，账本 E-008 记「0 failures/**60** warnings；diff-check clean；零生产代码」——与实测逐字相符。warning 61→60 的原因也说清了：DHR_62 自身的 R16 由新增的 **E-007**（`node --test test/profiles.test.mjs` 12/12）真实闭合，不是靠删引用。
- 条件 2 结论改为「机器与两路正式复核满足；**教训路最小返工待复核**」，与 progress「code1 approved / 需求 approved / 教训窄幅 changes-requested」同步。**没有把两路 approved 说成三路。**

## 剩余备注（P3 级，不阻塞）

| # | 位置 | 事实 |
|---|---|---|
| R-3 | `DHR_62/review.md` 业务化五段展示区 | 「结果」行仍写「当前 0 failures / **61** warnings」，实测与 E-008 均为 **60**；「风险」行仍写「原三路正式复核**尚未重验**，未收到回执前不放行」，而 E-008 已记两路 approved、一路窄幅 changes-requested。同文件的完成条件表和账本都已更新到 E-008，只剩这一屏没跟上——而这一屏恰是人回归时最先读的。另：完成条件 1 写「是」却挂 result=`partial` 的 E-008（partial 指复核回执、不指门禁），建议注明或把 E-008 拆成门禁/回执两维 |
| R-4 | `DHR_62/brief.md` 两处合同修订 | ①「触及子系统」由「仅治理工件，**不触及 as-built**」改为「允许最小更新 `as-built/relay-core.md`」；②warning 口径由「不在范围」细化为「**存量**不在范围，本卡**新增** warning 必须解释或消除」。两处方向都诚实（前者对齐 R18 对收口材料的实际要求，后者是收紧不是放宽），但改动发生在 as-built 已被修改之后，属事后对齐合同，且 brief 内与 findings 里都没有变更留痕。按宪章 #1，宜在 brief 加一行「2026-08-30 DHR_62 治理补录：范围因 R18 收口要求扩至 as-built 最小更新；warning 口径拆分存量/新增」 |
| R-5 | `dev_plan/P6-…-开发方案.md` 的 `planning-no-event` 头 | 采用**覆盖式**：`65663fa` 覆盖掉了原「DHR_61-S0 开工状态与工作区链接回填；B-23 已由 `bbf78f9` 落盘」，`63b7cd4` 又覆盖一次。同一批改动里 README 已改成**累积式**（历史全留 + 日期分段）。两份权威文档记法不一致，且 P6 早先那条 B-23 记录至今没回来。建议 P6 比照 README 改为追加式并补回被覆盖的记录 |

## 不可证项（维持）

| # | 命题 | 状态 |
|---|---|---|
| U-1 | 是否绕过 hook | 仍无 hook 日志；正面材料是绕闸被自立为 F-002（P0）并撤销、两道闸现已实际生效且通过，但仍属结果反推 |
| U-2 | E9/E10「已在对话贴回用户」 | 本轮把展示内容写具体了，但对话本身不入仓，是否真的展示过仍不可证——这也是签名框必须由用户自己勾的原因 |
| U-3 | 首轮 70→54→14→3→0 中间态 | 无逐批快照；返工侧的中间态（E-005 的 1 failure）已留 |
| U-4 | 复核者真实模型 | 候选-40 双证冲突未决，裁决权在用户 |
| U-5 | 仓外注册表现值、evidence 原始输出、全量 `npm test` | 维持原状 |

> 最终复查同样只写事实与级别。**approved 仅覆盖需求方向这一路**；教训路的窄幅返工回执不由我出，E10 与 `verify(dh-relay):` 仍待用户，我未代签、未代勾。

---

# 确认（2026-08-30，聚焦 86054de 核销 R-3/R-4/R-5）

> 范围：`e9d571a..86054de`（12 文件 / +230 −10，其中 218 行是三份复核报告的追加正文，实际工件改动仅 12 行）。
> 只读；除本文件外零写入，未代签 verify、未勾任何签名框。模型身份双证同上（SessionStart `claude-fable-5[1m]` / 系统提示自述 `Opus 5`，未决）。

## 结论

**approved**（无新 P0/P1/P2；一条 P3 备注，不阻塞）。

## R-3 / R-4 / R-5 核销

| 备注 | 判定 | 依据 |
|---|---|---|
| **R-3** 展示区数字与复核态陈旧 | **已闭合** | 五段「结果」行改为 **0 failures / 60 warnings**，与我在 `86054de` 实跑的 `dh dh-relay`（0 失败 / 60 警告 / exit 0）逐字相符；「风险」行改为「需求与教训路已 approved，代码轮 1 的三条窄幅事实修正待聚焦复查」。我核对了另两份报告的末尾裁决——`review-lessons-opus.md:250` = **approved**，`review-code1-opus.md:224` = **changes-requested（窄幅）**「无 P0/P1，没有任何闸门回退，三条 P2 全部是本补丁新写文字里的事实性错误」。该行描述属实（残留见 R-6） |
| **R-4** 合同事后修订无留痕 | **已闭合** | `brief.md` 末尾加「**失序补录（2026-08-30）**：首轮返工先按 R18 收口要求补了 as-built，后经复核才把『as-built 最小更新』与『存量/新增 warning 分流』回填本 brief；**本行不伪装成开工前已冻结**」；`progress.md` 同步加一行标注「**失序补录**……不伪装时序」。宪章 #1 要求的标记两处都到位，措辞也没有替自己开脱 |
| **R-5** P6 no-event 头覆盖式 | **已闭合** | reason 恢复为累积式：「**DHR_61-S0 开工状态与工作区链接回填；B-23 已由 `bbf78f9` 落盘**；DHR_62 纠正非法状态枚举，待人验语义归一为待验收并补齐既有收口证据；不改变任务终点、依赖、范围或验收口径」——被 `65663fa` 覆盖掉的那条 B-23 记录回来了。README 的 marker 同步补记了「P6 任务列表补回 DHR_61」，且 `README.md:66` 任务列由 `DHR_32~35` 改为 `DHR_32~35、DHR_61`，与 DevPlan 任务表一致 |

## 顺带核到的三处同向修正（均属往严了走，不计问题）

1. `backlog.md` DHR-BL-16 的议题描述被**收窄到可证粒度**：从「E-025/E-027/E-032/E-056/E-058 被 review/findings 引用而无账本行」改为「在 progress 散文**有定义但未进 Evidence Ledger**，其中 **E-025/E-027 被 findings 引用、E-056 被 review 与 findings 引用**」——原表述把五条一概说成"被引用"，实际只有三条被引用。改动点还加了「给 DHR_30 当时事后插入的 15 行加『收口补录』来源标记」。
2. `DHR_62/findings.md` F-004 由 `遗留→DHR-BL-16（已确认）` 改为 **`open（已移交 DHR-BL-16）`**，与本卡对 P2 统一采用「open + 移交」、只对需要 R13 豁免的 P1 用「遗留（已确认）」的纪律一致。
3. `DHR_33/progress.md` E10 补录里列的两项核验名改为 `review.md` 人验表**实际存在**的两行（「DSH-off 与 Herdr 状态映射」「Linux/Oracle 差异与下游遗留」）——此前写的是不存在的行名。另 `L-007` 加了「该取证动作必须**先在 brief/execution strategy 显式授权**」的前置要求。

## 人验 / verify 边界（复证，仍干净）

- `git log e9d571a..86054de --grep="verify(dh-relay)"` → 空。
- DHR_32 / DHR_33 / DHR_62 三份 review.md 全文 `[x]` → 零命中。
- `git diff --name-only 65663fa 86054de -- relay-core tools` → **完全为空**，横跨首修、返工、两次小补丁四段，docs-only 边界一次未破。

## 剩余备注（P3，不阻塞）

| # | 位置 | 事实 |
|---|---|---|
| R-6 | `DHR_62/review.md` 需求对齐证据行与完成条件 2 | 两处仍写「**教训路最小返工待复核**」，而教训路已 approved、当前待复查的是**代码轮 1**。本补丁把五段更新到了当前复核态，却没同步它上方这两行——与上一轮 R-3 正好是镜像（那次是表对、屏错，这次是屏对、表错）。方向仍是保守的（说"有一路未闭"是真的，只是点错了名），机器完成条件 1/3 不受影响。建议以后把「五段 + 需求对齐行 + 完成条件表」当一组同步。顺带：`findings.md` F-001 处置里的「实跑 0 failures/**61** warnings」是写入时的时点读数，与现在的 60 不冲突，但若要一眼可读，可补一句"（该读数为返工中间态，终态见 E-008）" |

## 不可证项

维持上轮口径不变（U-1 hook 日志、U-2 E9/E10 对话展示、U-3 首轮中间态、U-4 复核者真实模型、U-5 仓外注册表与全量 `npm test`）。本轮无新增不可证项。

> **approved 仅覆盖需求方向这一路。** 代码轮 1 的三条窄幅事实修正是否闭合，由该路自己出回执；E10 与 `verify(dh-relay):` 仍待用户，我未代签、未代勾。
