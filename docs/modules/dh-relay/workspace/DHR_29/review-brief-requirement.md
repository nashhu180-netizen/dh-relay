# DHR_29 需求复核 brief（五路之需求路）

## 身份与边界

你是需求复核实例，fresh-context、未参与实施。只读，不得修改任何文件；结论只写事实与级别，不替主控做验收裁决。

## 任务

把 **DevPlan §4.1 DHR_29 验收口径**（`docs/modules/dh-relay/dev_plan/P5-Relay-v2持久内核与DSH桥接-开发方案.md`，搜 `#### DHR_29`）逐条与实际交付对账：

- 机器证基线三条：幂等/冲突拒绝/迟到隔离（含 P1 迟到结果 fixture 复验「不弱于 v1」——注意这条用的是哪个 Oracle、有没有真做）
- 加固断言：N 次回放逐字节同签名；快照+增量 ≡ 全量
- 四条实现期约束反例：node_states 全集（F-037）、done<=total（F-070）、labels 排序唯一（F-081）、structured 脱敏（宪章#6）
- K-2 反例：attempt 字典序与 seq 相反的两份迟到结果按 seq 判
- 契约修订批次 1 六项：K-3 结构判据、§4b 四值裁决、F-042 合一、F-056 残留形态、卡号指名更正（动指纹三份）、v1-gap-disposition done<=total 归属更正
- 方向决策账 K-1：产生者已定（human_input_requested），落账在哪、P7 Attention 未被提前冻结的证据

对每条给出：**满足 / 不满足 / 部分满足 / 无法从在库材料判定**，附证据指针（progress.md E-xxx 或具体测试名）。特别留意「看起来过了但验收原文要的是另一个东西」的错位。

## 输出格式

结论 `approved`（无漂移）/ `有漂移`；逐条对账表；发现按 P0~P3 列出。报告全文作为最终答复返回，不写任何文件。
