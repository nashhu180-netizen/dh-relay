<!-- 教训库-候选.md — 教训 miner(W12) 的候选区/软着陆区。miner 收口时把抽出的候选 append 到这里、状态「待裁决」；
     人裁决挑中的才编 L-NNN 搬进正册 教训库.md。正册永远只进人裁决过的——这是"软入库"的底线。
     首次 miner 运行时按本模板建于 docs/modules/<模块>/knowledge/。设计见 dev-harness/references/W12-教训miner-设计.md。 -->
# dh-relay · 教训库候选区（待裁决）

> **它是什么**：miner 自动抽出、还没人裁决的教训候选。**不是正册**——正册 `教训库.md` 只进人裁决过的。
> **怎么用**：① miner 收口往「候选清单」append，状态「待裁决」；② 人裁决——挑中 → 编 L-NNN 搬进正册、本行改标「已入册 L-00X」；弃 → 本行改标「弃 + 一句理由」（留着防 miner 下次又抽同一条），**不直接删**。
> **不强求每次收口都裁**，候选可攒，攒一批 / 下次立项查漏时顺手清。
> **去重**：miner 动手前先扫正册 + 本区的「触发场景」；撞正册标「疑似 L-00X」、撞本区跳过。`dh mine` 会把两边现有触发场景列出来对照。
> **编号**：候选用「候选-N」临时号（本区内递增、可复用）；入册时才换正式 L-NNN。

## 候选清单

<!-- 每条一个 ### 块。字段一一对得上正册（教训库.md），裁决时直接搬。无候选时本区空着，不强凑。 -->

### 候选-1 · 判定函数的每个 reason 码 / 分支都要有断言钉住，并用静态守卫兜底（人肉小审逮不干净）
触发场景：写校验器 / 身份链裁决 / 状态机这类"多分支返回原因码"的契约代码，或复核此类代码 · 建议分类：编码陷阱 + 行为流程 · 来源：DHR_01 findings F-002(批A node 级)/F-005(批B stale-plan、wrong-node)/F-016(轮2 launch_id)/F-017(FreezeSet 只 1 跳)/F-020(系统性)/F-021(守卫精度)；吸收 lesson_candidates L-002
疑似重复：无（dh-relay 正册为空；与 dh-crew 教训库 L-001/L-010 同属"全绿掩盖"家族，但不是同一条）
- 现象：同一类缺口在三批施工里三次复发——某个 reason 分支删掉整段代码套件照样全绿；每轮换人复核各逮一条，直到轮 2 才立系统性守卫；守卫首版又被复验发现"修了没锁"（全变量插值 reason 静默漏抓、注释里出现码就算覆盖）。
- 反思：TDD 只保证"写了的断言"是红过的，不保证"每个分支都有断言"；靠人眼在几百行 diff 里数分支必漏。守卫本身也是代码，同样会有精度盲区，须用变异探针证明它真会红。
- 建议后续动作：① 契约/判定类任务的 task_plan 验收项写死"每个原因码 ≥1 条断言"；② 保留并沿用 `tools/tests/relay-contract-reason-coverage.ps1` 模式（源码原因码集合 vs 断言行原因码集合，差集非空即红；非字面 reason 直接 fail-closed）；③ 新守卫加完必做两个探针（注入未覆盖码→红；码只在注释→红）才算"锁住"，不能只看 ALL PASS。
状态：待裁决

### 候选-2 · 穷举 / 矩阵测试的期望值不能来自被测数据本身（自证循环）——测试里独立硬编码设计文档的 oracle
触发场景：给状态转换矩阵、枚举表、白名单等"数据即规则"的东西写穷举测试 · 建议分类：编码陷阱 · 来源：DHR_01 findings F-011（轮1 批B~D 小审探针 3a）
疑似重复：无
- 现象：transitions 套件 72 组合穷举以 `transition-matrix.json` 自己为期望：删掉 `running→idle` 合法边 84 断言照样全绿，错加设计未列的边也放行；只有 4 类特殊边有独立锚。
- 反思：把数据文件当 oracle 等于"用答案对答案"，穷举规模再大也证明不了矩阵和 design §3.1 一致，只证明代码读得懂自己的 json。
- 建议后续动作：测试里独立硬编码设计文档的合法边集合，先断言"矩阵边集合 == oracle"（多一条 / 少一条都红），再拿 oracle 做穷举期望；收口前用"删一条合法边"变异探针验一次真红（本卡 E-009 做法）。
状态：待裁决

### 候选-3 · 验收口径写"多类工件任一残留即失败"时，每类工件要有真实形态的夹具，不得同一字符串换标签循环冒充多类覆盖
触发场景：脱敏 / 清洗 / 校验类需求写"对 N 类工件都生效"，或复核此类测试 · 建议分类：编码陷阱 + 行为流程 · 来源：DHR_01 findings F-006（E4 需求复核）；吸收 lesson_candidates L-003
疑似重复：无
- 现象：A8 要求 handoff/result/event/session-tail 四类 final 工件零残留，redaction 套件却是同一段文本贴四个标签循环断言，实际走完整脱敏链路的只有 session tail；`Test-RelayArtifactClean` 还靠 `hunter` 关键字启发式判 kind，是贴着夹具写的。
- 反思：把"N 类"退化成"1 类 × N 个标签"能让断言数好看、但没有覆盖各类工件的字段位置 / 格式差异；这类假覆盖 TDD 自己发现不了，只有从需求角度反查才露馅。
- 建议后续动作：需求条目里凡出现"多类 / 任一 / 各自"字样，验收表就按类拆行、每类一份该形态的最小夹具（含凭据的 handoff 头、result summary 字段、event reason 字段…）；确实只做通用函数不接线的，review 明写"本卡只证通用函数、接线归下游卡"，别用循环冒充。
状态：待裁决

### 候选-4 · 主控预写 task_plan / brief 里的"见下方 XX 表 / 见 §X"内部引用，派活前先机械核一遍目标真实存在
触发场景：主控（或任何人）预写施工计划、给 worker 派活前的自检 · 建议分类：行为流程 · 来源：DHR_01 findings F-000（P1）+ progress "主控失误补录"；吸收 lesson_candidates L-001
疑似重复：疑似与 dh-crew 教训库 L-013（派活前"钉死 brief"）同族——本条是它的一个具体子项：引用完整性
- 现象：task_plan A2 写"字段表见下方【schema 冻结表】"，表根本没写；codex 首派读完即 blocked（拒绝自行发明 fail-closed 白名单），主控补表后续派，白耗一轮派活。
- 反思：主控写计划时"表我待会补"的意图没有落地，也没有一步机械检查把悬空引用挡在派活前；worker 侧行为其实是对的（escalate 而不是猜），问题全在主控。
- 建议后续动作：派活前对 brief/task_plan 跑一遍"引用目标存在性"检查（正则抓"见下方 / 见 § / 详见【】"→ 在同文件或引用文件里能定位到）；可作为 dh-check 或 controller-brief 校验器的一条静态规则；契约类任务尤其要在派活前把字段白名单本体给全，不给"稍后补"。
状态：待裁决

### 候选-5 · PowerShell 默认比较大小写不敏感（`-in/-eq/-ne/-notin`），冻结枚举必须用 `-cin/-ceq/-cne` 或 Ordinal 比对，且修一处要全库 grep 同类算子
触发场景：用 PowerShell 写枚举校验、状态快照判定、任何"精确字面匹配"的 fail-closed 逻辑；返工修同类 bug 时 · 建议分类：编码陷阱 · 来源：DHR_01 findings F-001（批A 小审 P2）+ F-008（E4 复核发现修复未覆盖 relay-transitions.ps1）
疑似重复：无
- 现象：`Test-RelayEnumValue` 用 `-notin`，`"role":"Worker"` 等大小写变体过校验，冻结枚举 fail-closed 形同虚设；批A 修成 Ordinal 后，同仓 `Get-RelayStallVerdict` 的 `-in @('running','idle')`、`Get-RelayExitWithoutResultVerdict` 的 `-ne 'exited'` 仍是不敏感比较，被下一轮复核再逮一次。
- 反思：PS 的大小写不敏感是语言默认、写的时候没有痛感；同一个坑往往散落在多个文件，"修一处"只修了被指出的那处。
- 建议后续动作：契约层枚举/字面比对一律 `-c` 前缀算子或 `[string]::Equals(...,Ordinal)`，并给每个枚举补一条大小写变体反例断言；修此类 finding 时的返工动作固定包含"`rg '\-(in|notin|eq|ne) '` 全库扫同类算子"一步，而不是只改被点名的行。
状态：待裁决

### 候选-6 · 钉边界的断言必须配变异对照，「测试全绿」不等于「断言在咬」
触发场景：写契约测试 / 校验器 / 任何「守住某条边界」的断言，或复核此类代码 · 建议分类：编码陷阱 · 来源：DHR_26 review 轮2 P2 + 施工者自查
疑似重复：无（候选-1 讲的是「每个分支都要有断言」，本条讲的是「已写的断言可能根本不咬」）
- 现象：DHR_26 两次栽在同一件事上——第一次测试只跑自造 fixture、接上真数据整棵插件树灭掉；第二次新写的契约断言里词边界符被写成字面退格字符 0x08，正则匹配不到任何东西，违规文件照样全绿。
- 反思：断言的存在性和有效性是两回事，人肉读正则读不出 0x08 这种非打印字符；只有"故意写一份违规输入看它红不红"能证明断言在工作。
- 建议后续动作：把变异做成可复跑的 harness（本卡两个：`contract-mutation-check.mjs` / `transcript-mutation-check.mjs`），每条断言配一条对应的变异体，收口证据里附 harness 输出而不是"测试通过"。
状态：待裁决

### 候选-7 · 校验器不能拿被校验对象当期望来源，重建 oracle 也不能复用被测代码
触发场景：写"对拍 / 校验 / 一致性检查"类脚本，或复核此类脚本 · 建议分类：编码陷阱 · 来源：DHR_26 review 轮2 P1 + 两次收敛复检
疑似重复：**疑似与候选-2 同源**（候选-2 是"穷举测试的期望值不能来自被测数据"）；本条是同一根因在**校验器**上的形态，裁决时可考虑合并为一条通用条目
- 现象：`verify-transcript.mjs` 前两版从转录自己声明的字段推出待核清单，于是"把 details 全删掉"也判 IDENTICAL；改成外部清单后仍有大量字段不参与裁决，篡改 schema / 诊断 / 信封照样 IDENTICAL。
- 反思：期望必须由调用方独立给出、并从原始数据重建；重建时若复用被测代码的加载器，oracle 会与 bug 同步。
- 建议后续动作：校验器一律「外部期望 + 从源数据独立重建 + 键集封闭（多一个字段就红）」三件套，并配变异对照。
状态：待裁决

### 候选-8 · 「禁用后不存在」不等于「卸载后被清理」，探针不能和被卸载对象同生共死
触发场景：验证插件 / 服务 / 组件的生命周期清理 · 建议分类：领域知识 · 来源：DHR_26 review 轮2 P1
疑似重复：无
- 现象：`disabled: true` 启动时插件的 `apply()` 根本没执行，那份 `present:false` 只证明「从未注册」；而包内探针在 `plugin remove` 后自己也没了，报 `Cannot find package`，removed 态只剩配置树行数当证据。
- 反思：清理证据要求"先真的注册、再卸、再观测"，观测者必须活过被观测对象的消失。
- 建议后续动作：把探针放到被卸载对象**之外**（独立包/独立进程），并另做一次"注册→dispose→断言消失"的框架内复现；两条角度都留转录。
状态：待裁决

### 候选-9 · 把正确性押在安装拓扑上迟早会咬
触发场景：写插件 / 扩展包，决定"依赖谁提供、怎么解析" · 建议分类：领域知识 · 来源：DHR_26 findings §12d/§12e
疑似重复：无
- 现象：「不打包 Cordis、靠宿主 profile 的 module fallback 解析」只在包被物理拷贝进 profile 树时成立；目录（`link:`）安装下 Node 按软链真实路径向上找 `node_modules`，必然 `Cannot find package`。
- 反思：这个设计把正确性押在了"用户用哪种装法"上，而这个前提从未被任何测试或文档保证过，是靠 tgz 装法碰巧满足的。
- 建议后续动作：根因修法是让运行时零外部依赖（用宿主注入的公开 API，如 `ctx.provide`），而不是在文档里禁掉一种装法；并用契约测试钉死"零 bare import"。
状态：待裁决

### 候选-10 · 文档纠错要 append + 原处加 superseded，不要偷改历史结论
触发场景：后续轮次推翻了早前批次的结论 / 证据 README · 建议分类：行为流程 · 来源：DHR_26 review 轮2 P1-D
疑似重复：无
- 现象：目录安装被证伪后改用 tgz，后来根因修掉、两种装法都成立；但证据总索引与批内 README 仍在教人"必须装 tgz"，读者按它操作会走回头路。
- 反思：证据目录是按时间分批的，读者可能绕过总索引直接读某一批；只在总索引上纠错不够。
- 建议后续动作：被推翻的批次**顶部加醒目 superseded 提示**、原文保留作根因记录；progress 这类 append-only 账本追加纠错条（明说哪句不实），不改旧句。
状态：待裁决

### 候选-11 · 补丁只治好被指出的那个实例，不代表同类全部实例都被治好——机制的覆盖面要按"同类目标清单"逐项巡检
触发场景：给契约/协议演进中反复出现的机制（版本号判定、能力指纹、审计扫描范围、白名单登记）打补丁，且该机制天然对应"N 份同类文件 / N 个同类分支" · 建议分类：编码陷阱 · 来源：DHR_28 findings F-049（未知版本 reason 码只对 7 份协议里的 1 份兑现）/ F-075（`capability_hash` 先漏共享定义模块，是 F-033 缺口 A「能力不同却算出同一指纹」的第五次复发）；review.md 里 capability 覆盖面"裸名字→`{id,digest}`→含共享模块"走了三步，每步都被复核逼出来
疑似重复：与 DHR_28 L-009 同源（"改 bug 的手本身是新 bug 高发区"）但角度不同——L-009 讲的是新写条件要在极端输入上重测，本条讲的是补丁要覆盖机制对应的全部同类目标清单，不是新代码本身的边界值。裁决时可考虑是否合并为 L-009 的具体子款
- 现象：F-043 修好了 `relay.rpc/v1` 一份协议的未知版本判定，其余 6 份协议的判别字段没有同步映射到 `E_UNSUPPORTED_VERSION`，直到复核逐条实测才发现 1/7；`capability_hash` 第一次修复只把"裸名字"升级成 `{id,digest}` 覆盖了 7 份顶层协议，但共享定义模块 `relay.common/v1` 完全不在覆盖范围内，locator 全域约束的实际改动可以在指纹不变的情况下被篡改。
- 反思：这类机制补丁在写的当下容易只盯着"最初被举证的那一份"，而机制本身描述的是"对所有 N 份同类文件都要生效"；不主动列出 N 份清单逐一核对，遗漏的部分只能靠下一轮复核撞见。
- 建议后续动作：给这类机制打补丁时，先列出该机制理应覆盖的完整目标清单（如"7 份顶层协议 + 1 份共享定义模块"），补丁收口前逐项过一遍，而不是只验证最初报出的那个反例。
状态：待裁决

### 候选-12 · 证据只覆盖论断的一部分（一种形态 / 一个合取项），结论却写成整条已兑现——本卡收敛阶段被同一复核者一次性揪出的七条共同病灶
触发场景：验收口径 / 结论陈述包含多个合取项（"A 且 B"）或需要覆盖多种形态（"这条路径下所有情形"）时，只测了其中一部分就写"该条已兑现 / 该风险已消除" · 建议分类：编码陷阱 + 行为流程 · 来源：DHR_28 progress.md E-029（"从未跑过的 Run 报告成功完成"这个最坏答案不再可能——实际只堵住了空集合形态，非空全 `pending` 形态仍被放行，见 F-048）/ E-034→F-067（验收口径 2 是两个合取项，证据只覆盖第一项"不导入私有类型"，结论写成整条口径兑现）；review.md e2 代码复核轮 2 结论原话"新发现七条全部落在同一条缝上：声称的覆盖面 vs 实际覆盖面"（F-067/F-075/F-076/F-077/F-078/F-079 同源）
疑似重复：与 DHR_28 L-006（"可修的缺陷记成固有边界"）不同角度——L-006 讲的是缺陷被误判为边界，本条讲的是证据覆盖不全但结论被写成全称 / 整条已兑现；两条都属于"没做到写成结论"的家族，但触发条件不同，建议保留为独立条目
- 现象：E-029 的探针只测试了 `node_states` 为空数组 / 字段缺省两种形态，结论却写"这个协议能给出的最坏答案不再可能"（暗示所有形态都被堵住）；E-034 的审计器只验证了"不导入私有类型"这一个合取项，`progress` 结论列直接写"验收口径第 2 条兑现"。
- 反思：作者自己在 E-034 行里承认"与 E-029 完全同形"——同一个人在同一张卡里，用同一种方式把"证据覆盖了子集"写成"结论覆盖了全集"，说明这不是偶然笔误而是收尾时的一种系统性简化冲动（写结论时倾向于按"论断"的粒度下笔，而不是按"证据"的粒度）。
- 建议后续动作：结论陈述前，先把论断拆成"证据实际覆盖了哪几个分句 / 哪几种形态"，逐项列出"达成 / 部分达成 / 未测"，禁止在还有未测分句时写"整条兑现"；这条检查本身可以做成收口自查的固定问题——"这句结论里的每个'且'/'所有'，各自的证据在哪一行？"
状态：待裁决

### 候选-13 · 写下教训不等于教训生效——本卡内主会话违反了自己刚写下的那条教训
触发场景：施工过程中现场识别并记录了一条流程教训（如"派复核前必须冻结产物"），随后同一张卡内继续施工 / 返工 · 建议分类：行为流程 · 来源：DHR_28 progress.md 2026-08-21 条目——"本卡第三次『复核进行中改盘』"：前两次（F-021/D-10）是复核者查 mtime 发现的，第三次是主会话自己在 E2 审查期间跑 E5 返工造成的，账本原话"L-005 正是主会话自己写下的『派小审前必须冻结产物』——自己写的教训自己又犯"
疑似重复：与 DHR_28 L-005（本身已判定重复 → 指向 DHR_49 L-06）不同层——L-005/L-06 讲的是"派审"与"冻结被审对象"须是同一个动作这条规则本身；本条讲的是"记录了这条规则"这个动作本身不会自动变成"下次会检查"的机制，即使记录者就是违反者本人
- 现象：批 2 阶段复核者两次靠对比 mtime 才发现产物在审查期间被改写（F-021/D-10），主会话据此写下 L-005；批 3 收敛阶段，主会话自己在 E2 审查窗口期跑了 E5 的返工修改，触发第三次同类违规——这次没有外部复核者提醒，是 E2 自己核实 HEAD/mtime 时撞见的。
- 反思：把教训写进 `lesson_candidates.md` 是一次性的"记录"动作，不会自动转化为下一次施工时的"检查"动作；尤其当违规者与记录者是同一个会话时，容易凭"我已经知道这条规则"的自信跳过显式核对，而知道规则和当场执行是两件事。
- 建议后续动作：现场写下的流程教训，若与"本卡后续步骤会不会再碰到同一情境"相关，应立即转成一条可勾选的检查项（如"派复核前先跑一次 `git status` 确认无未提交改动"）落进 task_plan 或 progress，而不是只写进 `lesson_candidates.md` 等收尾时才被看到。
状态：待裁决

### 候选-14 · 声称"与上游 / 同仓其它系统保持同一口径"，必须去读对方源文件逐字段核对，不能凭转述或记忆认定一致
触发场景：契约 / 协议设计文档里出现"沿用 XX 的口径""与 XX 保持一致"这类陈述，而 XX 是另一个系统 / 另一批已冻结产物 · 建议分类：编码陷阱 + 行为流程 · 来源：DHR_28 findings F-070（`run-state` 的 `group` 字段自称与 P4 pilot "保持同一口径"，E14 实读 P4 冻结件源文件 `relay-control-pilot/src/read-model/` 后发现至少三处不同：词表不同、`failed` 归堆不同、`progress` 首版擅自从计数对改成比率）+ F-071（`labels` 字段整体消失且零处置记录，同样是 E14 实读源文件才发现）
疑似重复：无（与候选-7「校验器不能拿被校验对象当期望来源」角度不同——候选-7 讲测试 oracle，本条讲文档里的口径对齐陈述）
- 现象：`run-state` schema 的 `group.$comment` 写"与 pilot 保持同一口径"，但实际逐字段核对后发现三处偏离（词表、归堆、`progress` 形状）；`labels` 字段是 P4 冻结件里 design/04 原则的唯一载体，本卡七份契约里直接消失，此前四轮复核（cp1/cp2/cp3/e2 首轮）都没有人去读 P4 的源文件核对，直到 E14 一致性复核专门做"横向比对"才抓到。
- 反思："保持同一口径"这类陈述本质上是一个可验证的事实断言（两份文档描述的是不是同一件事），但写的人往往是基于对上游的印象 / 转述下笔，没有真的把两份文件并排读一遍；这类断言不检查就直接采信，风险和"把没验证的当声明处理"（DHR_28 L-010）是同一类，只是发生在跨系统对齐这个具体场景。
- 建议后续动作：凡是写"与 XX 一致 / 沿用 XX 口径"这类陈述，收口前必须有一步"读 XX 的源文件、逐字段列对照表"的动作留痕（而不是转述 XX 文档里的散文描述），本卡 E14 的处置方式（实读 P4 冻结件源文件而非转述）可作为范式。
状态：待裁决

### 候选-15 · 校验与发送必须使用同一份稳定快照
触发场景：把生产者提供的对象送入 JSON / RPC / 消息边界 · 建议分类：编码陷阱 · 来源：DHR_52 F-220/F-223，E-059/E-066/E-069/E-072
疑似重复：无
- 现象：先 `JSON.stringify` 再校验，或校验生产者原对象后再次序列化，都会让 Proxy、accessor、`toJSON` 或原型变化在两次观察间改写实际帧。
- 反思：被校验的值与被发送的值不是同一份稳定对象时，冻结 schema 只证明了已失效的观察。
- 建议后续动作：递归读取 data descriptor 生成隔离原型 snapshot；校验与实际发送都只使用 snapshot，并以 Proxy/accessor/toJSON 反例断言零帧。
状态：待裁决

### 候选-16 · 超长 NDJSON 帧必须丢弃到换行
触发场景：实现流式文本帧协议的长度上限 · 建议分类：编码陷阱 · 来源：DHR_52 F-219，E-059/E-060
疑似重复：无
- 现象：超长未终止帧若直接清空 buffer，下一 chunk 的合法尾部可能被当作新帧执行，行为取决于网络分块。
- 反思：长度超限是整帧失效，不是已收到前缀失效；尚未见换行前仍处在同一坏帧。
- 建议后续动作：进入 discard-until-LF 状态，并以分块发送坏帧尾部的回归证明零 handler 调用。
状态：待裁决

### 候选-17 · headless worker 的沙箱不是完工门取证环境——但「环境假红」要凭两条判据裁，不能见红就归咎沙箱
触发场景：派 headless worker（codex workspace-write / 类似受限沙箱）施工并要求其自证完工门 · 建议分类：行为流程 · 来源：DHR_30 F-027，E-046；教训复核 R-N-01 收窄
疑似重复：无
- 现象：worker 在自己沙箱跑全量测试报 11 项 EPERM（锁文件建在系统临时目录与 `~/.dh-relay`）据此停工；主控无沙箱同树复跑 149/150，一项不复现。
- 反思：测试要写的路径超出沙箱可写根时，失败反映的是取证环境而非产品；但「假红」是一个要证明的结论，不是默认——见红就归咎沙箱会反过来掩盖真缺陷。
- 建议后续动作：裁「环境假红」须同时满足两条：①失败路径确实越出沙箱授权可写根；②无沙箱等价复跑不复现。派活说明书里明写「只跑定向测试，全量与四道闸由主控无沙箱跑」；worker 遇环境性失败先按上两条判定再登记。
状态：待裁决

### 候选-18 · 文件系统上「检查后原子删除」不可得——陈锁自动回收无论加多少判据都留替换竞态
触发场景：设计跨进程文件锁的 stale 回收（mtime / owner token / pid 存活判据） · 建议分类：领域知识 · 来源：DHR_30 F-019 三轮返工（R-D-01→R-E-01→R-F-01），E-042/E-043
疑似重复：无
- 现象：三轮加固（mtime 阈值 → owner token 双核验 → pid 存活双判据）后复核仍指出判定与 unlink 不是同一文件对象，竞态恒在。
- 反思：**路径型锁文件**（以路径为身份的锁）在 POSIX/Win32 语义下无法「按代次原子比较并删除」——判 stale 与 unlink 不是同一文件对象，"判 stale 再删"天然 TOCTOU；正确解是取消自动回收——外锁稳定拒绝 + 按持有者存活二分给人工恢复指引，把"自动正确"换成"稳定拒绝 + 人可恢复"。（教训复核 R-N-02 修正：`wx 抢建 + 代次核验`只解决**抢建**竞态，解决不了**删除-替换**竞态，不能当自动回收方案推荐。）
- 建议后续动作：新锁设计一开始就问"敢不敢不回收"；答案是"必须回收"时，要么仍取消自动删除改人工恢复，要么换真正支持原子所有权转移/撤销的锁原语（OS 句柄锁、租约服务），不做路径型删除式回收。
状态：待裁决

### 候选-19 · Windows 收权必须 icacls 三步且测试要预埋外来 ACE——/grant:r 不清既有显式 ACE
触发场景：Windows 上把目录/文件收成 owner-only（DACL） · 建议分类：编码陷阱 · 来源：DHR_30 F-020（R-E-02→R-F-02），E-041/E-043
疑似重复：无
- 现象：`icacls /inheritance:r /grant:r user:F` 后既有的外来显式 ACE 原样保留，收权名存实亡；chmod 在 win32 近似 no-op 且不报错。
- 反思：/grant:r 只替换同 trustee 的 ACE；不先 /reset 清场，收权断言测不出漏洞。
- 建议后续动作：三步 `/reset` → `/inheritance:r` → `/grant:r <user>:(OI)(CI)F`，任一步失败 fail-closed；测试预埋一条外来 ACE 并断言真实 DACL 每条显式 ACE 都属当前用户。
状态：待裁决

### 候选-20 · 私有落点收权要与共享根的可发现性合同分层——独占子目录而非收紧共享根
触发场景：要在双方共享的目录根（descriptor / 发现文件所在）下存私有数据 · 建议分类：领域知识 · 来源：DHR_30 F-020（R-F-03 合同冲突），design/08 §2
疑似重复：无
- 现象：把 pending 账直接放 `.dh-relay/` 根并收紧整目录，与「descriptor 沿业务仓 ACL 可发现」合同相抵，复核判合同冲突。
- 反思：可发现性与私密性是两个合同，落在同一目录上必然打架；分层（根沿业务 ACL、`private/` 子目录 owner-only）后互不干扰。
- 建议后续动作：设计私有落点时先问"这个目录还有谁要读"；有共享读者就开独占子目录，别动共享根权限。
状态：待裁决

### 候选-21 · 断连时必须显式败掉全部在途请求并给终态回调——静默死订阅与吃满超时是同一病灶的两面
触发场景：实现带重连的 RPC/订阅客户端（socket close、重试退避、宿主回调 API） · 建议分类：编码陷阱 · 来源：DHR_30 F-029/F-032（R-G-02/R-H-01），E-051/E-053
疑似重复：无
- 现象：socket close 不 reject 在途请求→逐个吃满 120s 超时把"500ms×20 退避"拉到 40 分钟；重试耗尽（含 attempts=0 零预算路径）直接 return→订阅静默死、宿主无从得知。
- 反思：失败路径的完备性要按「每条退出路径都必须让调用方观察到终态」逐条枚举，零预算/循环体不进入这类边界最易漏。
- 建议后续动作：连接收尾函数统一幂等收拢（reject 全部 pending + 清计时器 + 终态回调）；测试补零预算与"close 后请求须在秒级失败"两类钉子。
状态：待裁决

### 候选-22 · 重连快照不得抢先推进已送达高水位——服务端快照后还有按旧 cursor 的补发
触发场景：断线重连的事件流客户端维护 after_seq 高水位与快照合并 · 建议分类：编码陷阱 · 来源：DHR_30 批次C返工（worker 自抓）+ E-051 独立验证
疑似重复：无
- 现象：重连拿到快照就把高水位推到 snapshot_seq，服务端随后按旧 cursor 补发的事件（seq ≤ snapshot_seq）被误判重复丢弃。
- 反思：首订阅（快照即基线）与重连（快照 + 补发）语义不同；同一行赋值对两种场景一视同仁就丢事件。
- 建议后续动作：仅首订阅（cursor 为空）时以 snapshot_seq 设基线；重连保留旧高水位，由补发事件自然推进。（原挂此条末尾的「gap 恢复不占重试预算」按教训复核 R-N-03 拆出为候选-24——那是 F-033 的另一根因，不属高水位语义。）
状态：待裁决

### 候选-23 · 并发测试偶发红先做二分归因——证明是构造有洞才改构造，重试永远不是修复
触发场景：多进程/多连接并发用例出现偶发失败（收敛目标被抢、信号落在竞态窗口内） · 建议分类：行为流程 · 来源：DHR_30 批次B返工二轮（E-037 两类概率干扰实抓）；教训复核 R-N-04 收窄
疑似重复：候选-12 邻近但不同（那条是证据覆盖面，这条是测试构造确定性）
- 现象：双 CLI 并发用例两类偶发：后启进程把先启进程残条当收敛目标；SIGKILL 落在锁释放窗留下新鲜"陈锁"。加重试掩盖而不解决。
- 反思：偶发红有两种来源——测试构造允许多种合法交错（构造洞），或产品本身存在真实竞态（真缺陷）。前提是先画交错时序做二分：能找出一条**合法**替代交错解释失败 → 构造洞，用诱饵+扣答复的同步点把交错收敛到唯一路径；找不出合法交错 → 按产品缺陷追。一律当构造洞处理会漏真竞态。
- 建议后续动作：遇偶发先画交错时序二分归因；构造洞改构造（同步点/诱饵/判据补全）；连跑 10+ 轮只作**回归证据**，不作正确性证明（它证明不了"再也不会红"）。
状态：待裁决

### 候选-24 · 协议义务型恢复不占瞬态失败重试预算——两类预算分开计数
触发场景：客户端重试循环里同时存在瞬态失败（断线/超时）与协议规定必须执行的恢复动作（如 cursor gap 后整体重快照） · 建议分类：编码陷阱 · 来源：DHR_30 F-033（E-052/E-053）；教训复核 R-N-03/R-N-11 拆出
疑似重复：无（候选-21 讲退出路径完备，候选-22 讲高水位语义，本条讲预算归属）
- 现象：重连途中收到 E_CURSOR_GAP 消耗一次重试预算并吃退避；`reconnectAttempts:1` 时最后一击遇 gap 直接判"耗尽"，违反「gap → 整体重快照」合同。
- 反思：gap 恢复是服务端合同**要求**客户端做的动作，不是一次"失败"；两类事件共用一个计数器，合同义务就会被预算饿死。
- 建议后续动作：瞬态失败与协议义务恢复分开计数（义务恢复不减预算、不吃退避），并证明义务路径自身收敛（如 cursor 清空后不可能连续 gap）；用最小预算（attempts=0/1）边界用例钉住。
状态：待裁决

### 候选-25 · 返工不断暴露新根因时停止沿原补丁路径加码——G6 止损的「根因翻新」触发形
触发场景：同一条 finding 连续返工，每轮修复都通过了上一轮的复核点、却又暴露出新的根因层 · 建议分类：行为流程 · 来源：DHR_30 F-019 三轮返工链（R-D-01→R-E-01→R-F-01）；教训复核 R-N-06
疑似重复：与 harness G6「修完引入新问题×2 → 换人」邻近但触发形不同——那是**恶化**信号，本条是**根因翻新**信号（每轮都"对"但问题换了层）
- 现象：陈锁回收连续三轮加固（mtime→owner token→pid 双判据），每轮都修掉了上轮指出的点，复核却继续挖出更深一层的同族竞态——问题不在补丁质量，在方案路线。
- 反思：连续返工不收敛而根因在"翻新"时，大概率是方案级死胡同（如"检查后删除"这条路线本身），继续沿原路径加码只是把死胡同走深。
- 建议后续动作：同一 finding 第二轮返工仍暴露新根因层时，停止加码：保留累计轮次，换人复核或主控评估**换方案路线**（本例=取消自动回收），冻结未闭点与备选，到 3 轮上限即交授权方裁决。
状态：待裁决

### 候选-26 · 主控驳回复核发现也是待审对象——驳回论证须派 fresh 窄审逐支攻击
触发场景：主控（或任何裁决方）驳回一条复核 finding，且驳回论证覆盖多个入口/分支（"这些都受 X 机制保护"类论证） · 建议分类：行为流程 · 来源：DHR_30 F-034→F-036（E-054→E-056：驳回三依据中"import 可达但均受 fencing 仲裁"被独立窄审用真实调用链推翻——createRunWithNumbering 不取 lease 直建 Store）
疑似重复：无
- 现象：主控驳回"删宿主原语"建议时以"fencing 仲裁 + 生产零调用"概括全部入口；派窄审专门攻击驳回论证后，复核者逐支走真实调用链，发现其中一支（发号原语）根本不在 fencing 覆盖内，驳回被部分推翻，产出真 P1 修复。
- 反思：驳回是一个和实现同级的技术论断，会犯同类错误——按符号角色/名称归类（"宿主侧原语"）或按当前调用现状（"零调用"）下结论，都不能替代逐支走写路径证明；覆盖多分支的驳回尤其容易"以偏概全"。
- 建议后续动作：驳回 P1/P2 复核发现时，若论证含多个入口/分支，派 fresh-context 窄审**明示允许推翻驳回**并逐支核真实调用链；驳回被推翻不是丢脸是流程在工作，如实落账。
状态：待裁决

### 候选-27 · 测试期望与被测代码不得各自读实时时钟——日期边界必然分叉
触发场景：断言里出现按"现在"推导的日期/时间（发号、时间戳、TTL），测试进程与被测进程各自取时 · 建议分类：编码陷阱 · 来源：DHR_30 F-017/F-024（跨午夜确定性红 + 直修后残余窗口）；miner 原判"与候选-12 邻域重叠不重报"，教训复核 R-N-08 裁定独立成条
疑似重复：无（候选-12 是证据覆盖面，本条是时钟事实源）
- 现象：期望 run_id 硬编码测试启动时算的日期，service 发号用自己的本地时钟；本地时钟跨过午夜该用例确定性红；把期望改成测试进程现算仍留"计算与发号之间跨午夜"的窄窗。
- 反思：两个进程各自读实时时钟=两个事实源，日期边界上必然分叉；缩小窗口不等于消除。
- 建议后续动作：唯一事实源二选一——注入冻结时钟/fixture，或以被测方**实际返回值**为期望；边界相关字段只验形态与关系（如 `R\d{3}-slug-\d{8}`），不逐字比对现算日期。
状态：待裁决

### 候选-28 · 列表分组与排序是源头合同——源头冻结完整顺序规则，客户端只渲染
触发场景：Read Model / 列表投影跨多个客户端渲染，谁负责排序未落字面合同 · 建议分类：领域知识 · 来源：DHR_30 F-026（排序缺口）→ design/06 §14.4 冻结 + 双镜像断言；miner 原判"本卡合同已落档不成教训"，教训复核 R-N-09 裁定可迁移原则独立成条
疑似重复：无
- 现象："排序由源头给"写在任务卡里，实现却只更新字段不移动条目位置；各客户端若各自补排序，同一数据在不同客户端呈现不同顺序。
- 反思：排序/分组规则不冻结在源头，就会在 N 个客户端里被实现 N 次且互相漂移；"字段变了"与"位置随之变了"是两个可分别失守的命题。
- 建议后续动作：源头合同冻结**完整**顺序规则（分组词表序、未知组归堆、null 归位、组内键序）；用改前/改后双镜像断言分别钉"该动的动了"与"不该动的逐字不变"。
状态：待裁决

### 候选-29 · 可嵌入库不替宿主选持久化落点——持久账归宿主，库只复用宿主给的幂等标识
触发场景：交付被第三方宿主嵌入的客户端库（adapter/SDK），涉及重试账、缓存、凭据等持久状态 · 建议分类：领域知识 · 来源：DHR_30 Bridge 设计（pending 账由 CLI 自持，Bridge 的 requestId 由宿主提供、不自建账）；miner 原判"属本卡合同已落档"，教训复核 R-N-10 裁定可迁移原则独立成条
疑似重复：无
- 现象：若 Bridge 像 CLI 一样自建 pending 账，库与宿主会形成两套重试账，重试身份漂移、幂等失效。
- 反思：持久化目录/账本是宿主级资源决策（放哪、谁清理、生命周期）；库自作主张就与宿主打架。
- 建议后续动作：库 API 把幂等标识（request_id 等）作为**输入**由宿主提供并要求跨重试复用；库自身零持久落盘；文档明写"重试账归宿主"。
状态：待裁决

> **miner 映射完备性说明（教训复核 R-N-05 处置）**：workspace `lesson_candidates.md` L-01~L-09 至此全部有账——L-01~L-04/L-07 已并入候选-17~23（见 E-053 前落账），L-05 → 候选-27，L-08 → 候选-28，L-09 → 候选-29（三条 miner 原弃选理由录于 progress E-055 前 miner 行，教训复核裁定推翻并补报），L-06（范围外发现只记 findings/backlog 不顺手修）已被仓库宪章 AGENTS.md 吸收，**降级为"已宪章化"不另立候选**。

### 候选-30 · 路径逃逸的词法防护挡不住符号链接——防逃逸必须对根与目标双边 realpath 后判定
触发场景：写"仓内合法路径/禁止越界访问"这类 fail-closed 防护（ref 解析、文件路径校验、沙箱边界判定），或复核此类代码 · 建议分类：编码陷阱 · 来源：DHR_31 findings F-009（批2小审 P0，e:E-032）
疑似重复：无
- 现象：`resolveStepRef` 只做 `../`/绝对路径这类词法逃逸检查，随后的 `stat()` 会跟随符号链接；仓内 `steps/link.mjs → 仓外/evil.mjs` 能通过检查并被执行，现有测试只覆盖词法逃逸、无 symlink 反例。
- 反思：词法防护判断的是"写出来的路径长什么样"，符号链接改变的是"路径最终指向哪"——两者是不同层面的问题，词法层守住了不等于真实落点也守住了。
- 建议后续动作：任何"仓内/禁止越界"判定都对根与目标各做一次 realpath 再比较真实落点（而非路径字符串）；测试补真实 symlink（或 Windows 无权限时降级为目录 junction）逃逸端到端回归，并配变异核验（把守卫短路成 true 应立刻见红）。
状态：待裁决

### 候选-31 · "全量绿"结论必须带并发/负载条件——套件变大后既有紧预算会偶发超时，闸门形同看运气
触发场景：node --test 一类默认并行跑文件的测试运行器，套件持续新增会起真实进程/服务的测试文件 · 建议分类：编码陷阱 + 行为流程 · 来源：DHR_31 findings F-016（P2，多轮复核维持 open）
疑似重复：无（候选-23 讲的是单个并发测试用例构造是否存在合法交错的二分归因，本条讲的是测试文件数量增长后，既有测试里"够用"的紧超时预算在更高并行度下变得不够用，机制不同）
- 现象：套件从 15 个文件加到 17 个（新增两个各自起真实 service/子进程的文件）后，某条给 8 秒往返预算的既有断言开始偶发超时；单独跑该文件仍稳定绿，是并行争抢而非产品缺陷；限制 `--test-concurrency` 缓解了这一处，但换了个文件（`runtime.test.mjs:449`）又撞了同一类紧预算。
- 反思："全量绿"这句话背后藏着一个没写出来的前提——"在什么并发/负载下取的"；不写这个前提，闸门通过与否就取决于当时机器状态，而不是代码正确性；限一处并发只是缓解症状，没有消灭"紧预算撞并行度"这一类问题。
- 建议后续动作：套件新增会起真实进程/服务的测试文件时，主动扫一遍既有测试里"够用但没有余量"的超时预算；报"全量绿"时附带并发度/耗时，不要只写通过数；治标（限并发、加预算）之外记一条 backlog 追踪"是否还有下一个紧预算"，不要认为限一次并发就等于问题消失。
状态：待裁决

### 候选-32 · 冻结契约字段缺口先查"消费者是否已优雅降级"，不要急着扩协议或编造字段
触发场景：客户端/面板需要的字段集与冻结契约能提供的字段集不是子集关系，第一反应是"要不要开新接口/编个兜底值" · 建议分类：行为流程 · 来源：DHR_31 findings F-018（P1，e:E-084/E-086）
疑似重复：无
- 现象：面板渲染但活数据拿不到的字段实测 13 处，四条取得方式（编造 title / 开新 RPC / 绕过控制面直读文件 / 改面板不改协议）各自撞了不同的红线，遂停工交用户裁决；裁决走"缺的字段显式留空、不编造不兜底"后，落地发现面板对未知字段本就有 `"k" in run`/`Array.isArray`/`kv()` 守卫——省键即优雅降级，两处硬校验字段活数据全给得出，最终授权的"面板校验放宽"额度一次都没用上。
- 反思：先假设"缺字段=必须改协议或改消费方"，会把一个可能不存在的问题升级成真问题；正确顺序是先验证消费方对缺字段的真实反应（渲染守卫 vs 硬校验），往往"什么都不改"就够。
- 建议后续动作：撞到字段集不是子集关系时，先读消费方代码确认每个必需字段是渲染守卫还是硬校验；只对硬校验字段申请协议放宽或新接口，渲染守卫字段直接留空验证即可，不要为尚未证实卡壳的字段预先扩协议。
状态：待裁决

### 候选-33 · 同一抽象概念的多个对称变体（如 pi-agent / dsh-agent）必须逐一参数化核验，测试构造器写死其中一个会让另一个的 bug 永远不会变红
触发场景：系统里同一类事件/流程存在 ≥2 条并行实现路径（不同 executor kind、不同渠道、不同后端），需要写覆盖它们的测试或做跨路径一致性检查 · 建议分类：编码陷阱 · 来源：DHR_31 findings 批4小审 P1「dsh-agent 同形覆盖缺失」（e:E-081）+ F-022 一致性复核扫出 pi/dsh 两路 outcome 映射不对称（e:E-072/一致性复核表）
疑似重复：候选-11（同族：机制对 N 份同类目标覆盖不全）但触发形不同——候选-11 讲补丁只修了被举证的那一份、其余同类文件未同步；本条讲的是从一开始测试构造器/实现就只按一个变体写死，另一变体的对应值即使类型对但语义不对称也不会被任何断言发现，裁决时可考虑是否与候选-11 合并
- 现象：测试构造器固定用 `pi-agent`，误把 dsh 节点也当 pi-agent 托管不会触发任何红；批4小审逼出参数化后新增 dsh 同形用例才补上覆盖。一致性复核另扫出：pi「Adapter 消失」记 `outcome:failed`，dsh「宿主消失」记 `outcome:orphaned`——两者都是"executor 消失、从未拿到结论"，按协议分界两处该是同一个 outcome，reason code 本身配对没错，错的是这层映射两路没对齐，且没有任何断言横向比较过两路。
- 反思：为对称变体各写一组用例只保证"每条路径自己不报错"，不保证"多条路径对同一类事件给出同一套判定"；后者只有专门做跨路径横向比对（而非逐路径纵向测试）才能发现。
- 建议后续动作：涉及多变体的测试助手一律参数化 kind/variant，不写死默认值（缺省即断言失败，逼调用方显式传）；收口前专门加一步"同一类事件在各变体路径上的输出值横向对表"，而不只是分别验证每条路径自身合法。
状态：待裁决

### 候选-34 · 证据转录必须由脚本现场采集写入，不得手抄/摘要——手抄在独立复核复算时才会露馅
触发场景：把命令输出、哈希值、退出码这类可复算证据写进 evidence/转录文件 · 建议分类：行为流程 · 来源：DHR_31 findings 批5小审 P2 三连（E-087 树外文件数漏记、E-093 SHA256 手抄旧值与复算不符、probe 转录命令-输出-退出码错位），e:E-100/E-102
疑似重复：无
- 现象：批5转录先后三处被复核抓出证据缺陷，根因是同一个——都存在手抄成分：树外改动文件数手抄成"3 个"漏了后补的一个；`live-store.mjs` 的 SHA256 手抄的是加同步预热之前那版的旧值；probe 段落的命令与退出码顺序对不上。三处直到复核方现场独立复算/重排才发现，整份转录改由脚本现场采集生成后一次性闭合。
- 反思：手抄这类"复制一遍数字/结论"的动作本身就是一次没有校验的转录，人会抄错、抄旧、抄错位置，而这类错误在写的当下没有任何机制会提示；只有下一次独立复算才会撞见。
- 建议后续动作：可复算的证据（哈希、计数、命令输出）一律由脚本直接写入转录文件，不经人手誊抄或摘要；如确需人工整理措辞，摘要与原始数据分栏放，原始数据保持脚本产出不动。
状态：待裁决

### 候选-35 · 异步测试助手要等"不变量成立"而不是等"期望值出现"，否则断言自证循环且偶发红
触发场景：测试里对异步/多步写入的产物（先写事件账、后写快照这类非原子多步落盘）轮询判定"已完成" · 建议分类：编码陷阱 · 来源：DHR_31 findings 批4自查（E-054/E-055，"自查出的测试竞态"）
疑似重复：无（候选-15 讲的是校验与发送必须用同一份快照，本条讲的是轮询等待条件本身该等什么）
- 现象：测试轮询事件账判"完成"，紧接着读快照断言状态，产品写序是"先 append 事件账、后 writeAtomic 快照"，机器空闲时窗口只有几毫秒，全量并发起来就读到旧快照；根因是"等我要断言的值出现"这种轮询条件，一旦读到了就立刻断言，而此时另一份数据可能还没追上。
- 反思：等待条件如果和断言目标是同一件事（"轮询到我想要的值就断言它"），这个轮询本身不能证明系统真的到了目标状态，只能证明轮询这一路径观察到了；改成等一个独立于断言目标的不变量（快照签名与事件账重放签名一致），断言才是真断言。
- 建议后续动作：多步非原子写入的产物，测试帮助函数一律等"不变量成立"（如两份数据重放/摘要一致）而非"我要断言的字段值出现"；写这类 helper 前先想清楚"轮询目标"和"断言目标"是否是同一件事，是就要换成独立不变量。
状态：待裁决
