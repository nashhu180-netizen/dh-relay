# P7/P8 通用节点与跨项目任务编排：交叉审核记录

> 事件：`DHR-B-21` 候选审核。
> 状态：B-adjust 已生效；首轮 fresh review 的问题已修订，两次定向复审及 P5/P6 兼容边界窄审最终 `approved`（P0=0、P1=0），理解问答通过，用户已明文“更新”完成整版确认。
> 正式 planning input：`docs/modules/dh-relay/design/10-薄RelayPlan与显式节点边界-产品设计调整.md`。
> 候选：`docs/modules/dh-relay/design/drafts/DHR-B-21-P7P8跨项目任务编排-调整候选.md`。
> 机器事件：同一业务调整分别绑定两个正式工件，使用唯一事件 `DHR-B-21-P7` 与 `DHR-B-21-P8`；二者共同归属 `DHR-B-21`。

## 1. 权限边界

- 本记录证明计划候选接受过独立审核、理解问答和用户整版确认；正式权威已落入 P7、P8、`dev_plan/README.md` 与 DHR_53 workspace 卡面。
- 用户明文“更新”只授权上述文档更新和复验。
- B-adjust 确认不授权创建 archive ref、生成重建工件、清理/重建 DHR_53 worktree、初始化 PlanHome、恢复施工、提交或推送。

<a id="review-b21-p7"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-21-P7 artifact=dev_plan/P7-DevHarness单卡完整流水-开发方案.md kind=review -->
<a id="review-b21-p8"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-21-P8 artifact=dev_plan/P8-多卡编排与运行治理-开发方案.md kind=review -->
## 2. 首轮 fresh review

- reviewer：fresh 只读审核 Agent `/root/review_b21`。
- 结论：`changes-requested`。
- 统计：P0 = 0，P1 = 6，P2 = 3。
- 现场基线：
  - master：`d6d65d63e1619140f68c0087c394ad1baf61127f`；
  - DHR_53 branch：`wt/DHR_53`；
  - DHR_53 HEAD：`fec9ec1bbbab90813d7d3f3aad1dc847c317ca81`；
  - DHR_53 tree：`6452aebbab080ea6002ddd749b6cbc8061352744`；
  - `git status --porcelain=v1 --untracked-files=all`：空。

## 3. 主会话裁决与修订

| 审核问题 | 裁决 | 候选修订 |
|---|---|---|
| P8 DHR_44/45 仍继承历史 Hook、Outbox、周报、业务仓 `docs/relay` | 接受。它们没有正式 `design/10` 输入 | DHR_44 取消并保留历史；DHR_45 只承接 PlanHome archive/run summary、独立 Oracle 与跨项目 E2E |
| 缺少 `dev_plan/README.md`、P8 状态和 planning marker 的同步要求 | 接受 | 正式更新目标加入 P7、P8、README、B 审核留痕；正式事件只在用户确认后写入 |
| DHR_55、DHR_42、DHR_60 对 B-adjust/Plan activation/continue 的责任重叠 | 接受 | 固定为 Decision 关闭 → 外部逐项目 B-adjust → 编排 Agent选择并激活一个 generation → 另一次显式 continue；每步独立 Receipt |
| DHR_53 旧实现归档与重建步骤不够可执行 | 接受 | 增加 branch/HEAD/tree/clean/untracked/path-blob manifest/archive ref/精确 master SHA/只读隔离/非 destructive rebuild 要求，并与本次文档授权分离 |
| 缺少旧实现到新合同的清理矩阵 | 接受 | 增加 single task、Run Binding、task_type/Recipe、业务 node_type、DevHarness workflow 的迁移和扫描；明确保留合法 Herdr/Host/client adapter |
| H11/H12 不是可执行的人验步骤 | 接受 | 增加 5 分钟操作链、证据、期望状态与 activation 失败、错误绑定/HEAD 等失败例 |
| 受影响闭包定义不精确 | 接受 | 固定为问题 seed + 显式下游 + 共享消费者；路径重叠/容量互斥不作为业务依赖 |
| A32-A38 缺少逐项负例映射 | 接受 | 为每项增加主要拒绝条件和承接卡 |
| “零 Adapter”可能误删合法协议 adapter | 接受 | 收窄为零 DevHarness workflow/adapter 及换名等价层；Herdr/Host/client adapter 不在禁用范围 |
| 第一次定向复审发现“不加入当前 Plan”后的计划身份和继续动作不明确 | 接受。正式 design/10 已冻结“不加入时只记录外部处置引用，留给其他 Plan/Run；无关 Ready 节点可继续” | 明确当前 `plan_id + generation`、TaskRef/Node 图和 Run 身份不变，不自动创建/切换其他 Plan/Run；新任务留在外部 DevPlan，当前 Run 只显式 continue 无关 Ready 节点 |

## 4. 定向复审结论

- 第一次定向复审：首轮六项 P1 中五项闭合；H11“不加入当前 Plan”分支仍有一个 P1。
- 主会话依据正式 design/10 §5 与 HC-3AT-H11 补齐该分支，没有另行发明自动新建/切换 Plan 的语义。
- 第二次定向复审：最后一个 P1 已闭合；新增 P0/P1 = 0。
- 最终 verdict：`approved`。

<a id="understanding-b21-p7"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-21-P7 artifact=dev_plan/P7-DevHarness单卡完整流水-开发方案.md kind=understanding -->
<a id="understanding-b21-p8"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-21-P8 artifact=dev_plan/P8-多卡编排与运行治理-开发方案.md kind=understanding -->
## 5. 理解问答与前置阶段追问

- **理解问题**：Decision 将任务 A 拆成 A1/A2，外部 B-adjust 已完成，但编排 Agent 决定不把 A1/A2 加入当前 RelayPlan；当前 generation、A1/A2 与当前 Run 中无关 Ready 节点分别如何处理？
- **用户回答**：`generation不变，A1/A2不纳入，不受影响的节点继续运行`。
- **判定**：通过。补充口径为 A1/A2 留在已调整的外部 DevPlan，供其他 RelayPlan/Run 后续承接；当前动作不自动创建或切换其他 Plan/Run。
- **用户追问**：P5/P6 与 DHR_30/DHR_31 是否受影响。
- **现场核对**：P5 当前 DHR_30 进行中、DHR_31 未开始；P6 DHR_32~35 未开始且受 P5 Gate 阻塞。DHR_30 worktree HEAD `b8804e393dd76b72eaabcfd14c601fbb25d8e6a8`、状态 clean；其 `relay-core` 生产合同未命中 `task_id`、`task_type`、业务 `node_type` 或 `workflows/dev-harness`。
- **主会话裁决**：本 B 事件不改 P5/P6 卡面、状态或授权，但补充 DHR_30/DHR_31/P6 的兼容和禁止倒灌边界；该补充交同一 reviewer 做窄域定向复审。
- **窄域复审**：`approved`。P0=0、P1=0；结论与正式 design/10、现行 P5/P6/P7、DHR_30 现场一致。
- **用户补充确认**：`task_type` 是 dev-harness 自身的内容，不受本次调整影响。DevPlan 继续保留该字段并用它冻结复核 Recipe；dh-relay Core 不读取、不解释该字段。

## 6. 整版确认与正式落地

1. 用户在 2026-08-27 明文“更新”，完成 `DHR-B-21` 整版确认。
2. 主会话已更新正式 P7、P8、`dev_plan/README.md` 与 DHR_53 的 `brief.md`、`task_plan.md`、`progress.md`、`findings.md`。
3. P5/P6、DHR_30/DHR_31 的任务表、状态、依赖和授权未变；只在 P7/P8 记录兼容与禁止倒灌边界。
4. DHR_53 旧实现只标记为 `superseded-by-DHR-A-23` 并暂停；未创建 archive ref、未操作 worktree、未初始化 PlanHome、未恢复施工。
5. 本次授权不含提交或推送，因此本记录不伪造最终 SHA；提交须另行授权。

## 7. 后续独立闸门

- 若要恢复 DHR_53，先另行授权 archive ref/path-blob manifest 和从精确 master SHA 重建工作树。
- 重建后仍按 dev-harness 的 `task_type` Recipe 完成对应复核；该流程不会成为 dh-relay Core 的运行时耦合。
