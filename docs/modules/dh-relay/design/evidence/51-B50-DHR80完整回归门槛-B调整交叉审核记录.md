<!-- dh:v1 -->
# DHR-B-50 · DHR_80 完整回归门槛 B-adjust 交叉审核记录

> 状态：fresh 初审、两次定向复审、主控裁决、用户理解回答与最终确认均已完成，`DHR-B-50` 正式生效。正式 design/11、design/12 与 DHR_80 六条验收不变；本调整只覆盖已启动 `task_plan.md` 的 Batch 3 完整回归放行规则。它不宣称默认全量测试通过，不授权修复兄弟卡、verify、合并、push、真实 Agent 或 DHR_35。

<a id="review-b50"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-50 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=review -->
## 1. fresh-context 独立审核

- 原始需求与现场：用户要求继续 DHR_80 分批施工、Luna 执行、主控把关。唯一一次默认 `npm test` 的 `identity-quota.test.mjs` 子进程运行约两小时后仍挂起；用户明确授权停掉。父级随后给出 E-8029：exit 1、370 tests、365 pass、5 fail、0 cancelled。
- 审核实例：`/root/dhr80_badjust_review`，fresh context、未参与 DHR_80 起草或施工；只读核查 workspace、P6 DevPlan、DHR_77 F-7702/E-7721 与候选 diff，未修改文件、未运行测试、未进入交付闸。
- 独立事实：DHR_80 生产 diff 只有 `attempt-retry.mjs` 新增 immutable `result_submission_mode='receipt-bound/v1'` 一行；直接组合 E-8021 为 37/37，整改组合 E-8018 为 21/21；正式 DevPlan 验收 5 要求专项与受影响回归自然终态，并未要求默认全量必须绿色。DHR_77 历史仅能支持 DHR_76/C 预算断言与 DHR_34 `identity-quota` 挂起同形。

### 方案问题

初审 `CHANGES_REQUESTED`：P0=0、P1=2、P2=1、P3=0。

1. 原草案不追加 `task_plan.md` 会与“完整测试非绿则 construction blocked”冲突，构成事后降门槛；采纳为保留历史条款并追加 B-50 覆盖段。
2. 原草案把五项失败统一叫既有债，证据过强；采纳为只把 DHR_76/C 与 `identity-quota` 记历史同形，CLI 并发、DHR_69/F、DHR_76/B 保持未证明基线同形、未归因。
3. `constrained pass` 易被误读为普通通过；采纳为固定状态 `BATCH_3_CONSTRAINED`，只表示专项施工证据受限满足、允许进入 heavy fresh review。

第一次定向复审发现新增 backlog 与本 evidence 文件未列入 DHR_80 精确允许路径，P1=1；主控采纳，在同一 DevPlan 原子变更中仅追加 backlog 单文件和 evidence/51 单文件，不放宽目录。第二次定向复审结论 `APPROVED_REVISED`，P0/P1/P2/P3=0。

### 用户理解风险

- 37/37、21/21、audit、diff-check 与 `dh` 只支撑 DHR_80 直接影响范围和治理边界，不能替代默认全量健康结论。
- `package.json` 收录专项只证明默认命令能发现该测试，不证明默认命令全绿。
- heavy review 仍可要求 DHR_80 返工；本调整不跨越 E11、verify、合并或 push。

### 需要用户决定的问题

用户需决定是保留原门槛让 DHR_80 持续 blocked，还是接受受限施工路线：保留完整回归失败与未归因残余，只让专项证据进入 heavy fresh review。用户选择后者。

## 2. 主控裁决与讲解

主控采纳全部 reviewer findings，无驳回。请求从 v2 retry/submit RPC 进入 actor/gate，Result 经现役单写路径提交；本卡的一行 mode 让 fresh retry Receipt 可被该既有入口发现和恢复。专项、重启、幂等、冲突、fencing、失租、未认证、done/idle 零提交与零 Agent 启动均已有直接命令证据；失败的默认全量仍作为模块级残余保留。

故 B-50 不改变产品承诺、持久状态、公开协议或错误处理，只纠正施工计划里比正式 DevPlan 更强且已被历史挂死路径证明不可用的完整回归门槛。失败通过 E-8029、F-8007 与 DHR-BL-17 暴露，不被局部绿色抵消。

<a id="understanding-b50"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-50 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=understanding -->
## 3. 用户理解、确认与权限

- 初步取舍：主控说明建议做 B-adjust 后，用户明确回复“认可”。
- 理解问题：调整生效后，DHR_80 得到的是“专项证据足以进入复核”，还是“全量测试已经通过”？
- 用户回答：“进入复核？”主控确认正确：只允许进入 heavy 独立复核，全量仍为失败。
- 最终确认：主控再次询问是否按修订后的 B-adjust 落盘、标记 `BATCH_3_CONSTRAINED` 并进入复核，用户明确回复“好的”。
- 权限：允许 B-50 原子落盘、同一 Luna construction Handoff 更新和后续 heavy fresh review；不授权修兄弟卡、再次运行完整 `npm test`、verify、合并、push、真实 Agent 或 DHR_35。
