<!-- dh:v1 · A-27 整改后定向复审的施工说明书（主控写，复审实例读） -->
# A-27 目录信任自动放行 · 定向复审 brief

## 你的形态

**fresh、只读、未参与起草与整改的独立复审实例。** 你的判断只依据仓内文件，**不要采信主控在文档里的自评措辞**——主控上一轮就因为「把跑通当成安全」被审出来过。

## 硬边界（违反即本次复审作废）

- **只读**：不创建、不修改、不删除任何文件；不 `git add/commit/checkout/stash`；不改配置。
- **不拉真实产品 Agent**，不执行任何会弹目录信任框或权限框的命令。
- **不读取任何凭据 / token / 账号状态 / 登录态文件**；不读 `~/.codex/`、`~/.claude*`（本次复审不需要它们，格式实测是后续单独授权的事）。
- 你的输出**不得包含**任何凭据形态串、产品配置正文、pane 文本原文。

## 复审对象

| 文件 | 作用 |
|---|---|
| `docs/modules/dh-relay/design/drafts/DHR-A-27-目录信任自动放行-候选.md` | **主对象**，整改后的草案 |
| `docs/modules/dh-relay/design/evidence/31-A27-目录信任自动放行-交叉审核记录.md` | 上一轮 11 条议题的裁决 + §7 整改落点表 |
| `docs/modules/dh-relay/design/evidence/32-A27-目录信任能力矩阵-实测.md` | 本轮新增的实测证据（D-1 的裁定依据） |
| `docs/modules/dh-relay/design/evidence/a27-capability-matrix-results.json` | 32 的原始观测（已脱敏） |

参考（按需）：`AGENTS.md` 宪章、`docs/modules/dh-relay/dev_plan/` 的 `B-22` / `B-32`、`workspace/DHR_68/` 的收口记录。

## 背景（三句话）

1. 上一轮两名 fresh 审核均判**不通过**，共 11 个独立议题，主控裁决采纳 10、部分采纳 1、驳回 0。
2. 此后用户裁定 **D-2 = 全局默认开**、**D-1 = 路线 A（启动前预置产品信任表，零按键）**，并明文松绑 `B-22`② 的产品配置边界（仅限目录信任那一项）。
3. 主控已按裁决整改草案正文。**本次是定向复审，不是重头审。**

## 你要回答的问题

**核心问题：整改是真解决了，还是只是换了说法？**

请重点攻击这四个面，其余部分只在发现明显问题时才提：

### A. §0 的阻塞判定是否成立且充分

主控自查登记了 `F-6809`：herdr 对 **Claude** 的启动期目录信任框返回「start 成功 + idle」，导致 DHR_68 的 `agent_not_ready` 判据失效，进而使路线 A 的回退路径（回落人工暂停）对 Claude 不成立。

- 这个因果链成立吗？证据（evidence/32 §2）够不够支撑它？
- 把它列为「必须先收口的硬前置」是**过度**还是**不足**？还有没有别的同类前置被漏掉？
- 注意 evidence/32 §2 自己承认 `agent get` 与 `pane get` 存在采样时刻不一致，主控声称本行方向仍明确——这个论证站得住吗？

### B. §1.3.5 的覆盖收窄是否诚实、是否足够显式

原冻结的用户决定是「会话全程无条件覆盖」，主控把它收窄成「目标全程免按 + 实测覆盖 + 残余安全回退」。

- 收窄的理由（路线 A 下无条件承诺不可证）成立吗？
- 「残余情形」列得全不全？特别是：主控承认"中途进入未预置嵌套仓会不会真弹框"**是推断不是实测**——这个坦白够不够，还是说这个洞大到该阻塞设计？
- 收窄有没有在任何别的地方被偷偷恢复成无条件表述？（全文搜"全程""任何时刻"）

### C. §3.1 审计字段是否还有「超出证据」的地方

R-03 的原批评是「`work_dir_root` 记错对象、`granted` 超出证据」。主控改成了 `target_dir` + `dir_resolution` + `outcome` 三态 + `verified`。

- 这套字段现在**能证明什么、不能证明什么**？还有没有哪个字段名声称了它证不了的东西？
- `verified`（写后复读确认条目存在）真的等价于「信任生效」吗？产品可能读缓存、可能在别的文件里另有开关——这个缺口有没有被承认？
- `switch_epoch` 能不能真的支撑事后判定？

### D. §4 验收命题是否真的钉住了「不误伤」

R-10 的原批评是「只证明夹具能认出预期字符串，不证明不误认真实其它情形」。

- `A27-M1`（只写一个条目 + 零按键的静态与运行时双证）、`A27-M6`（幂等 + 容忍回写）、`A27-M7`（失败方向）、`A27-M8`（目录解析六形态）——这几条**作为命题**是否可证？证据形式配得上命题吗？
- `A27-M5` 的上限定义（按 `(product, target_dir)` 去重、随 Attempt 重置）有没有漏洞？
- §4.3 的变异与负例清单，有没有该有而没有的？
- 人类验收 `A27-H1~H5` 里，有没有还能拆出机器项的？有没有哪条其实人判不了？

## 额外的一条

主控在 §2.1 声称「路线 A 消灭的是整类风险」。**请独立判断这句话有没有过头**：路线 A 真的一个按键都不发吗？失败回退到人工暂停时，是谁按的、按的是什么？这中间有没有被主控跳过的一步？

## 输出格式

在终端里直接输出，**不要写文件**。按下面结构：

```
## 结论
通过 / 不通过（二选一，必须明确）

## 发现
| # | 级别 | 议题 | 位置 | 为什么是问题 | 建议改法 |
（级别用 P1/P2；P1 = 不改就不该放行）

## 我确认已被真正解决的原议题
（列 R-xx 编号 + 一句为什么算解决）

## 我没能验证的事
（诚实列出：因为只读、因为缺证据、因为超出我的判断范围）
```

**如果你认为整改是充分的，就直说通过**——不要为了显得尽责而凑发现。反过来，**如果你认为 D-1 选路线 A 这个决定本身有问题，也直说**，那是用户的裁决，但你的独立意见会被如实登记。
