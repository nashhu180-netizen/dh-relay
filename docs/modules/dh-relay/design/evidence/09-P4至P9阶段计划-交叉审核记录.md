<!-- dh:v1 -->
# P4～P9 阶段计划 · B 交叉审核记录

> 审核对象：`dev_plan/P4~P9` 六份开发方案（2026-08-18 由 Claude 按 dev-harness B 拆计划规范改写 GPT 5.6 pro 原稿后的版本）与 `dev_plan/README.md`；附 design/06 §10 两句措辞订正（DHR-A-09）。
> 本文只保存**审核过程、主会话裁决、讲解与理解确认**，不承载设计或计划本体，不作 B 拆计划输入。
> 事件：`DHR-B-04`～`DHR-B-09`（各计划文首 planning-event marker）、`DHR-A-09`（design/06）。每个事件在本文有 `review-*` / `understanding-*` 两个锚点。

## 1. 轮次总览

| 轮 | reviewer | 模型 / 配置 | 只读强制方式 | 审核对象 | 结论 |
|---|---|---|---|---|---|
| R-P4 | claude-grok（fresh，未参与改写） | `CLAUDE_CONFIG_DIR=~/.claude-grok` → 本地网关 127.0.0.1:38178 → `--model grok-4.5`（网关已不再暴露 `grok` 别名，首跑 502 后改指定具体模型） | `--disallowedTools Write Edit NotebookEdit Bash --strict-mcp-config --mcp-config '{"mcpServers":{}}'`（进程层） | P4 全文深审 + README 一致性 | **有条件通过**（3 P0） |
| R-P56 | claude-grok（fresh，独立会话） | 同上 + `Agent Task` 亦禁用（首跑 P5~P9 合审因子 agent 网关报错只回一句状态，作废后拆两路重跑） | 同上 | P5、P6、README（P4 作前置、P7 作后继） | **有条件通过**（0 P0，4 P1） |
| R-P79 | claude-grok（fresh，独立会话） | 同上 | P7、P8、P9、README（P6 作前置） | **有条件通过**（0 P0，5 P1） |

- 只读核对：派出前后 `git rev-parse HEAD` 均为 `8ab6d9f`，`git status --short` 变更集不变（8 个由主会话改写产生的文件），无 reviewer 写入。
- 三路 brief 共同部分：用户原始需求、用户口径「Relay CLI 与 DSH 并行、可配置替换、CLI 非保底/应急/回退」、拆计划输入白名单规则、固定三段输出（方案问题 / 用户理解风险 / 需要用户决定的问题）+ 总结论。brief 全文保存在派出会话 scratchpad（不入仓）。
- 凭据：只经 `CLAUDE_CONFIG_DIR` 指向的账号配置目录进入进程环境，未出现在 brief、命令回显或本文。

## 2. 主会话裁决总表（2026-08-18）

| 来源 | 发现 | 裁决 | 落点 |
|---|---|---|---|
| R-P4 P0 | DHR_25 把 design/06 H1/H4 全称挂成承接，实际只有 fixture 级 | 采纳 | P4 §1 承接设计改为「§10 P4 为直接依据，不承接验收命题节任何全称命题」；DHR_25 验收改挂 §10 P4 + P4-CM，并逐条注明不承接 H1/H4 |
| R-P4 P0 | §5 覆盖表虚盖 H9 | 采纳 | P4 §5 删 H9，写明 H1/H4/H9 归 P5/P6/P9 |
| R-P4 P0 | DSH 判否时 P4-CM4 可空集通过仍解锁 P5 | 采纳 | CM4 二分：DSH 通过→字段级一致；判否→记 N/A、不计入 CM 全绿、延后 P5；§4.5 解锁规则改为 CM1/2/3/5/6 全过 ∧ CM4 pass 或 N/A 如实登记 |
| R-P4 P1 | H3 在 DHR_25 过早挂靠 | 采纳 | DHR_25 去 H3；DHR_26/27 改「H3 子集」 |
| R-P4 P1 | 「基线轨 / 增强轨」措辞与用户口径摩擦 | 采纳 | P4 与 README 统一改「必备控制面轨（CLI）/ 桌面控制面轨（DSH，可判否）」；§4.5 注明「阶段默认 ≠ 回退」 |
| R-P4 P1 | DHR_25 轻档偏松（跨阶段契约） | 用户已定：轻档 + 人判签收 | DHR_25 档位改写、验收新增人判「最小字段够不够 P5 沿用」 |
| R-P4 P1 | 2~3 日预算与两张标准档 + DSH 探路不匹配 | 用户已定：工作量不作约束 | P4 §1 改「不设硬预算、只记实耗」；§4.4 删天数止损、改「用户喊停」 |
| R-P4 P1 | §0.1 白话夸大（直接沿用 / 随时切换 / Pi 标配） | 采纳 | P4 §0.1 改写 |
| R-P4 P1 | B1 只取子集须在覆盖表注明 | 采纳 | DHR_27、§5 注「B1 子集，非全项」 |
| R-P4 P2 | evidence/09 双文件撞号 | 采纳 | Pilot 报告改 `evidence/10-P4-多控制面Pilot报告.md` |
| R-P4 P2 | `<experiment-root>` 未钉死 | 采纳（标待决策） | §2.2 标「待决策，DHR_25 开工闸给定」 |
| R-P4 P2 | 「不另写一套状态推导」不可机证 | 采纳 | DHR_25 改为唯一 render 函数 + 禁旁路字段测试 |
| R-P56 P1 | P5 交付⑨ Agent 闭环与 P5-X 口径打架 | 用户已定：P5-X 可选项 | P5 §1 交付⑨标 P5-X 条件项 |
| R-P56 P1 | P6 覆盖表宣称 H7 但无卡验收 | 采纳 | P6 §6 删 H7，注归 P5-X / P7 |
| R-P56 P1 | DHR_35 漏依赖 DHR_34（Receipt 身份链） | 采纳 | DHR_35 依赖改 DHR_33、DHR_34；§6 依赖改单链 |
| R-P56 P1 | P6 §0.1 自动切号夸大 | 采纳 | P6 §0.1 第 3 条改写 |
| R-P56 P2 | P5 H2 只承接子集 | 采纳 | P5 §5 覆盖表注「H2 子集，无 attention/approve，其余交 P7」 |
| R-P56 P2 | B15② 回链语义偏窄 | 采纳 | DHR_30 改挂 B1 控制请求幂等子命题 |
| R-P56 P2 | §0.2「闸后才审」与本次闸前审矛盾 | 采纳 | P5~P9 §0.2 审核记录改为「闸前路线图级 fresh 审核已做；闸后再定向 B-adjust + 复审」 |
| R-P56 P2 | design/05 §15 仍偏 DSH 主线 | 记录，**待用户决定**是否另开 A′ 订正 | 未改 design/05 |
| R-P79 P1 | P7/P8 人判残留「备用入口 / 备用路径」 | 采纳 | P7 DHR_40、P8 DHR_45 人判改写 |
| R-P79 P1 | design/06 §10 P7「重编排可由 CLI 完成」vs P7 不含卡级重编排 | 用户已定：重编排从 P8 起，同步改 design/06 §10 P7 | P7 §1 不含 改写；design/06 §10 P7 句改写（事件 DHR-A-09，见 §15/§16） |
| R-P79 P1 | DHR_45 错用 H9 | 采纳 | 改 H3 / H1 / H2，H9 归 P9 DHR_46 |
| R-P79 P1 | DHR_40 整 ID 挂 B8，窗口护栏不可判 | 采纳子集化；design/02 决策 6 是否退役 **待用户决定** | P7 §1、DHR_40 改「B8 子集，确认闸以 06 H8 Receipt 为准」 |
| R-P79 P1 | design/02 H3/H4 Oracle 语义拉伸 | 采纳 | P7/P8 人判注「不再以不敲命令行 / 窗口作答为通过标准」 |
| R-P79 P2 | P7/P8 §1 H 清单与卡内引用不一致 | 采纳 | P7 §1/§6 补 H1/H3/H4 单卡子集；P8 补 H1/H2/H4 多卡子集 |
| R-P79 P2 | P9 H6/H7/H12 只靠 Dogfood 整体覆盖 | 采纳 | DHR_48 新增 H6/H7/H12 点名回验项 |
| R-P79 P2 | P7 复核 Profile 对 P6 名册依赖未写 | 采纳 | DHR_39 实施提示补写 |
| R-P79 P2 | P9 agent-console「应急只读」残留 | 采纳 | 改「只读启动器」 |
| R-P79 P2 | README「P4 DSH 优先」与硬约束#1 并存 | 采纳 | README 阶段地图改「CLI 必备控制面必过 + DSH 桌面控制面可判否」 |
| R-P79 P2 | design/README「当前只有 P4 可进 fresh 审核」与本次动作并读混乱 | 记录，**待用户决定**是否改 design/README 那句 | 未改 |

裁决方式：全部由主会话（Claude Fable 5，改写者）逐条核对 reviewer 引用的原文位置后判定；未核到原文的条目不采纳（本轮无）。「待用户决定」条目在各计划正文已显式标注，不写成已决策事实。

## 3. DHR-B-04 · P4 审核

<a id="review-b04"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-04 artifact=dev_plan/P4-DSH工作台最小Pilot-开发方案.md kind=review -->

### 3.1 审核方式与独立性

- 轮次 R-P4（见 §1 总览）；reviewer 为 fresh-context 独立会话，未参与本计划起草或改写；只读由进程层参数强制。
- 审核对象文件：`dev_plan/P4-DSH工作台最小Pilot-开发方案.md`。

### 3.2 reviewer 原文

已核完 design/06 §11、design/05 对应节与 P4 全文，下面按固定三段给出独立审核结论。

---

##### 1. 方案问题

**P0 · 验收回链语义越界（H1 / H4）** · `P4 §1`、`DHR_25` 验收口径、`§5` 覆盖表  
- 问题：卡片把产出挂到 design/06 **H1 / H4** 全称命题，但交付物远小于命题原文。  
- 独立事实：design/06 §11 H1 =「DSH 未安装或完全停止时，**Relay Runtime** 可以启动、查询、运行和恢复」；H4 =「客户端断开、DSH 插件卸载和 SSH 断开均不隐式取消 **Run**」。P4 明文非目标「不实现 Relay Runtime」，现场只有 fixture / 一次性 CLI。  
- 「SSH 断开不改变 fixture」对只读文件几乎恒真，不能证明「不断开 Run」。  
- 影响：B 覆盖关失真——看起来盖了 H1/H4，实际未盖；后续 P5/P9 易被误以为 H1/H4 已在 P4 卸责。  
- 建议：验收只挂 **P4-CM*** 本地命题；对 design/06 写清「本阶段仅覆盖子命题 X，全称 H1/H4 延后到 P5/P9」，禁止把全称 H-ID 写成已承接。

**P0 · H9 虚盖** · `P4 §5` 覆盖表 vs `DHR_25/26/27` 验收口径  
- 问题：§5 写「H1/H3/H4/**H9** 的 fixture 级子命题由 DHR_25/27 承接」，但三张卡验收口径**均未引用 H9**。  
- 独立事实：H9 =「Linux 无 GUI、仅 SSH 时，可以运行 **Runtime、CLI、Herdr 和一条代表性工作流**」。P4 只有 SSH 下读 fixture 的 CLI，无 Runtime / Herdr / 工作流。design/06 §10 P4 原文只要求「同一 fake/v1 Read Model 可由通用 CLI 在 Windows 和 SSH 读取」——这不等于 H9。  
- 影响：覆盖表与任务卡自相矛盾，属拆计划覆盖关不合格。  
- 建议：§5 删除 H9；SSH 只回链 design/06 §10「P4」+ P4-CM2。

**P0 · DSH 判否时 P4-CM4 可空转却仍解锁 P5** · `§4.1 P4-CM4`、`§4.5`、`DHR_27` 依赖「通过或已落判否证据」  
- 问题：CM4 =「CLI 和任何增强客户端均消费同一 Read Model」。DHR_26 `stopped-by-pilot` 后不存在第二客户端，CM4 可被读成空集通过，从而在「跨客户端一致性」未证时仍满足「P4-CM 全部通过 ∧ 进入 P5」。  
- 对照：§0.1 / DHR_27 目标强调 CLI↔DSH 对证；空转通过会拆掉 Pilot 的核心证明之一。  
- 建议：显式二分——（a）DSH 通过：CM4 要求跨客户端 JSON/字段一致；（b）DSH 判否：CM4 标 `N/A（无第二客户端）` 且**不得**计入「CM 全绿」，改由「CM1/2/3/5/6 全绿 + DM=stopped + 报告诚实」解锁，或要求至少一种非 DSH 第二投影（例如只读 JSON 文件对拍）才能算跨客户端一致。

**P1 · H3 在 DHR_25 上过早挂靠** · `DHR_25` 验收  
- H3 要求 DSH、Pi 与 CLI 读同一 Read Model。DHR_25 只有 CLI，最多证明「CLI 只渲染 Read Model」；跨客户端一致属于 DHR_27（且依赖 DSH 是否存在）。  
- 建议：DHR_25 去掉 H3 回链，改挂 P4-CM1/CM2/CM5。

**P1 · 「基线轨必须过 / 增强轨可判否」与用户口径：不构成产品硬冲突，但计划措辞仍有残留** · `§0.1`、`§4`、`README`「P4 的特殊阶段闸」、对照用户口径与 design/06 §1  
- **判断**：阶段闸不对称 **≠** 把 CLI 定位成保底/应急/回退。design/06 §1 本就规定 CLI = **必须存在的参考客户端**，DSH = **可替换、可缺席的首选桌面工作台**；README 硬约束#1 已改为「并行、可配置替换」。Pilot 里「CLI 不过则主线不立、DSH 可判否」对齐的是「必备 vs 可缺席」，不是「CLI 是差一等的退路」。  
- 仍有问题：通篇「基线轨 / 增强轨」+「DSH 优先」容易让人读成 CLI=保底、DSH=正式；与用户刚纠正的口径摩擦。§4.5「DSH 判否时 P5 以 Relay CLI 为默认」作为**阶段默认控制面**可接受，但不得在讲解里说成「CLI 顶上」。  
- 建议：对外/白话统一「必备参考控制面（CLI）∥ 首选桌面控制面（DSH，可判否）」；少用「增强轨」指 DSH。

**P1 · DHR_25 定轻档偏松** · `DHR_25` 档位  
- 形式：仓外、只读、不接线、不动 `tools/` → 按 dev-harness 可不进高危五类，轻档合法。  
- 实质：§0.1 / 实施提示称 Read Model「P5 正式内核直接沿用 / 可进入 P5 协议」——这是跨阶段契约冻结；轻档无两轮复核、无人判 schema 签收。  
- 建议：升**标准档**，或保持轻档但增加人判「最小字段是否够 P5 沿用」并由用户对话确认后才允许写进 P5 输入。

**P1 · 2~3 有效开发日 vs 串行三卡（含两张标准档）不匹配** · `§1` 批次、`§4.4` 止损、`DHR_26/27`  
- 独立事实：依赖 `25→26→27` 单链；26/27 为标准档（两轮换人复核 + 需求境截图）；§4.4 又把「2~3 日内无可见面板」设为 DSH 止损——与整阶段预算同一数量级。  
- 影响：极易在 DHR_26 探路耗尽预算，DHR_27 对证/报告被挤掉，或被迫降质。  
- 建议：预算改为「DHR_25 ≤1 日；DHR_26 单独预算并与阶段总预算脱钩」或砍 DHR_26 到「加载 Host + 静态面板」更小闭环；标准档复核时间必须计入。

**P1 · §0.1 白话与正文不一致 / 夸大** · `§0.1` vs `§1`/`DHR_25` 实施提示 / `§4.5`  
- 「P5 直接沿用」vs 正文「可进入 P5 协议设计」；  
- 「两条控制面随时切换」vs DSH 允许整轨判否（判否后 P4 并不交付可切换的双面）；  
- 「完成后拍板是否补 Pi」把 P4-X 可选探索写成标配产出。  
- 建议：白话改成与双轨终态枚举一致的可交付物清单。

**P1 · design/02 B1 只取子命题但写法易被当成整项 B1 已盖** · `DHR_27`  
- B1 全文是 relay/v2 契约矩阵 + 双根 discovery；P4 只做「legacy 根零新写」。回链写法已有「只取子命题」——保留，但须在覆盖表同样写「B1 子集，非全项」，避免查漏清单误判 B1 已关闭。

**P2 · `evidence/09-*` 双文件撞号** · 文首 planning-event vs `DHR_27` 变更范围  
- `09-P4至P9阶段计划-交叉审核记录.md` 与 `09-P4-多控制面Pilot报告.md` 同号不同文，后续易链错。建议报告改 `10-` 或带 `-report` 后缀并在 README 写死。

**P2 · `<experiment-root>` 未钉死** · `§2.1/2.2`  
- 变更范围全落抽象根；无默认路径约定则开工闸无法填落点。属待决，不是已决策事实（目前未伪装成已定，故 P2）。

**P2 · DHR_25「不另写一套状态推导」列为机器证** · `DHR_25`  
- JSON 相等可机证；「代码路径无第二套推导」通常要结构审查。轻档无两轮复核时该项易空口。建议改为可执行约束（单一 render 函数 + 禁读 fixture 旁路字段的测试）或升档后进代码复核。

**已核假设（无新增问题则亦须列出）**  
- `design/README.md` 白名单含 01/02/05/06；P4 未把 evidence/07、08 当拆计划输入——通过。  
- 状态列仅用允许枚举；CM1–6 / DM1–5 在表上均有承接卡——表结构通过，问题在语义。  
- design/05 §5 / §14 / §15 / §18 章节存在；§5 插件形态、§14 最小面板与 DHR_26/27 方向匹配；§18 止损与 §4.4 部分对齐（未声称盖满 §18 全条）。  
- 与 README 硬约束#1、活跃计划表对 P4 状态描述一致。  
- 未核验：仓外是否已有可用 DSH 树外构建链、本机 Linux SSH 靶机是否就绪（计划假设「条件允许」，现场未核）。

---

##### 2. 用户理解风险

**P1** · 读完以为 **H1/H4/H9 已在 P4 验收掉**，Linux Headless 正式路径已经成立；实际只证明「SSH 里能打印同一份假数据」。  
**P1** · 「基线 / 增强」叙事 +「DSH 判否也不挡 P5」→ 以为 **CLI 是正式产品里的保底通道**；与你刚定的「并行可替换、CLI 不是保底」口径相反。正确读法：CLI 是**必须先证明存在的参考控制面**；DSH 是**想用但允许证明失败的桌面控制面**。  
**P1** · §0.1「做完得到定死的状态说明书、P5 直接沿用」→ 以为 Pilot schema 已是终态合同；正文仍可能在 P5 改协议。  
**P1** · DHR_26 标准档「判否不算失败」→ 以为插件没做成也算本阶段成功；对你而言成功条件其实是：**CLI 侧证明成立 + DSH 侧有诚实三态结论 + 你拍板是否继续**。  
**P2** · 文件名仍叫「DSH工作台最小Pilot」、design/05 §15 仍写 DSH 中心主线 → 翻旧文会以为路线又变回「DSH 成败定生死」；冲突规则是 06 优先，但 05 §15 未改，易混。  
**P2** · 「2~3 天小实验」→ 低估两张标准档复核与 DSH 构建探路；到期无面板时容易误判「DSH 不行」其实是「时间不够」。

---

##### 3. 需要用户决定的问题

**P1** · DSH 整轨判否后，**跨客户端一致性（CM4）**算不算 P5 解锁必要条件？还是接受「仅 CLI 自洽 + DM 诚实判否」即解锁？（见方案 P0）  
**P1** · DHR_25 冻结的 Read Model：接受 **轻档 + 你口头确认 schema**，还是要求 **升标准档 / 加人判签收** 再允许进 P5？  
**P1** · 2~3 日预算：保总预算并缩小 DHR_26，还是给 DSH 探路**单独计时**、总日历延长？  
**P2** · `<experiment-root>` 落哪（路径约定）？Linux SSH 靶机是否必须本阶段具备，还是 CM2 允许「无靶机则 N/A 并挡解锁」？  
**P2** · DSH 判否后 P5 默认控制面：纯 CLI，还是「CLI + 预定补 Pi」？（计划理解问题候选已写出，确属产品取舍。）

---

**总结论：有条件通过（须先改完下列条件后再确认落盘；未改则按不通过处理）**  
1. 拆掉对 design/06 **H1/H4/H9 全称命题**的虚假承接；H9 移出 §5；卡片只挂 P4-CM/DM 与写明边界的子命题。  
2. 写清 **DSH 判否时 CM4 / P5 解锁**规则，禁止空集通过。  
3. 修正 §0.1 与正文一致（禁止「直接沿用」「双面随时切换」等夸大）。  
4. 用户拍板：**DHR_25 档位**与 **2~3 日 vs DSH 探路预算** 二选一写入计划。

### 3.3 主会话裁决

见 §2 裁决总表中「R-P4」各行；已采纳项均已回写到 `dev_plan/P4-DSH工作台最小Pilot-开发方案.md`（本 HEAD 变化集内）。

## 4. DHR-B-04 · 讲解、理解问题与确认

<a id="understanding-b04"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-04 artifact=dev_plan/P4-DSH工作台最小Pilot-开发方案.md kind=understanding -->

- **讲解**：已做——2026-08-18 主会话在对话中先给六阶段白话说明（各计划 §0.1 同文），再讲本轮审核发现与裁决（本文 §2），并把待拍板项列成选项。
- **理解问题与用户回答**（对话 AskUserQuestion，2026-08-18）：①DHR_25 档位？→「轻档 + 人判签收（推荐）」；②P4 工作量预算？→「工作量不重要吧」（主会话解读为不设硬预算、不按天数止损，已回写）。
- **用户确认**：已确认——2026-08-18 用户对话原话「P4确认，提交和推送」（在主会话汇报四项拍板落点、待决清单与 dh 检查结果之后）。DHR-B-04 事件闭合；DHR_25 开工仍需另行授权并确认 `<experiment-root>` 落点。

## 5. DHR-B-05 · P5 审核

<a id="review-b05"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-05 artifact=dev_plan/P5-Relay-v2持久内核与DSH桥接-开发方案.md kind=review -->

### 5.1 审核方式与独立性

- 轮次 R-P56（见 §1 总览）；reviewer 为 fresh-context 独立会话，未参与本计划起草或改写；只读由进程层参数强制。
- 审核对象文件：`dev_plan/P5-Relay-v2持久内核与DSH桥接-开发方案.md`。

### 5.2 reviewer 原文

##### P5 / P6 / README 一致性与承接审核（fresh，只读）

核验范围：`design/README.md` 白名单、`design/06` §1/§10/§11、`design/05` §4–§10/§14–§15、`design/02` §6.1 B1–B18 / §6.2 H3–H5、`dev_plan/README.md`、P5、P6；P4 作前置边界、P7 作后继边界。未改任何文件。

---

##### 1. 方案问题

**P1 · P5「交付」与阶段闸对 Agent 闭环口径打架**  
`P5:49` 交付⑨写「至少一个 Agent Executor 候选闭环」；同文件 `P5:154–159`、`P5:184–186`、批次说明 `P5:59` 又把 Agent 放进「尝试 / P5-X / 可选」，且解锁规则 `P5:191` 不要求 P5-X。  
→ 原始需求/规范：B 拆计划「目标/非目标/验收」须可判；阶段闸与交付清单应同真。  
→ 影响：收口时无法判定「没做成 Agent 闭环」算 P5 失败还是增强失败。  
→ 建议：要么交付⑨改成「P5-X 条件项」，要么把最小 Agent 闭环升进 P5-M 并挂卡。

**P1 · P6 覆盖表宣称承接 H7，但任务卡无对应验收**  
`P6:196` 写 H7 由 DHR_33/34/35 承接；三张卡验收只见 H1/H3/H4/H5/H9/H12 等，**无 H7**（DSH-only Executor 丢失只影响 Attempt）。H7 更自然在 P5 DHR_31 / P7。  
→ 影响：查漏清单「覆盖」自检作假，后续 reviewer 会误以为 H7 已在 P6 证完。  
→ 建议：从 P6 覆盖表删 H7，或补一张可判的 H7 机器证（若坚持本阶段证）。

**P1 · DHR_35 依赖漏标，与 Receipt 身份链验收冲突**  
索引 `P6:103`：DHR_35 只依赖 DHR_33，并称可与 DHR_34 并行收口；但 DHR_35 验收 `P6:152` 要求 Receipt 身份链可证（B4），而「Receipt 冻结 profile/account_alias/config_fingerprint/capability hash」是 DHR_34 目标 `P6:137`。  
→ 影响：可出现「DHR_35 先做完、身份字段未冻结」的假通过，或并行开工时互相抢 Receipt schema。  
→ 建议：DHR_35 增加对 DHR_34 的依赖；或把「launch 时写入身份链」从 DHR_34 前移到 DHR_33，并改写依赖说明。

**P1 · P6 §0.1 对额度治理的完成态夸大**  
白话 `P6:30`：「账号切换与额度耗尽有明确规则和记录，不再靠人肉盯」；正文 `P6:145`、`P6:186` 允许 P6-M4=`constrained`，完整自动 fallback 可延后 P8。  
→ 影响：用户以为 P6 结束就有可靠自动切号。  
→ 建议：白话改为「有正反样本与明确通过/受限结论；完整自动切号以 M4 实态为准」。

**P2 · H2 在 P5 被写成阶段承接，但命令子集不含 attention/approve**  
`design/06` H2 含 attention/approve；P5-M2/`DHR_30` 只验 list/status/inspect/events/start/stop/resume，正式 attention/approve 明示留给 P7/P8（`P5:91`）。虽有「basic-agent-task 级」限定，覆盖表 `P5:197` 仍写「H1~H5 由 … 承接」。  
→ 建议：覆盖表改「H2 子集（无 attention/approve）」；H2 剩余明确交 P7。

**P2 · P5 对 design/02 B15② 的回链语义偏窄**  
`P5:146` 用 B15② 只取「request id 幂等」；B15② 原文是 diagnoser 控制事件的 target + expected_state_hash 绑定（`design/02` §6.1 B15）。  
→ 建议：改挂更贴的协议幂等命题（自建 P5-M 或 B1/控制面幂等），勿截取 B15②。

**P2 · §0.2 与当前审核事件时序矛盾**  
P5/P6 §0.2 写「只在前序阶段闸通过后才进入正式 B-调整与 fresh 审核」；文首已是 `stage=B-adjust`，且本次即在闸前做 fresh 审。  
→ 建议：改成「闸前审的是预落盘路线图；闸后依据前序证据再做定向 B-adjust + 复审」。

**P2 · design/05 §15 阶段叙事仍偏「DSH Bridge 主线」**（背景，非 P5/P6 正文错误）  
`design/05` §15 仍写「P5 … 与 DSH Bridge」「P4 DSH 工作台最小 Pilot」，未写 CLI 基线轨；冲突规则以 `design/06` 为准，P5/P6 正文已跟 06。  
→ 建议：另开 A′/机械订正同步 05 §15，避免拆计划读者回看 05 时漂移。

**已核对为通过/可接受的项（不升 finding）**  
- 拆计划输入：P5/P6 §1 只链 06/05/02；P6 对 design/03、evidence/03 标明「非拆计划输入」。未把 evidence 当契约。  
- 引用的 H1–H12、B1/B4/B5/B6/B7/B11、05 §4/§6/§7/§8.2/§10.2 均存在且大体语义匹配。  
- 状态列均为「未开始」；阶段闸在备注；`<!-- dh:tasks -->` 有。  
- 任务 ID：`DHR_28~31` / `DHR_32~35` 与 README 纪律一致、无互撞。  
- P5-M1~M8、P6-M1~M7 均有承接卡。  
- 机器证/人判：人判集中在 DHR_31 / DHR_35，大体二分成立。  
- 用户口径「CLI 与 DSH 并行可配置替换、CLI 非回退」：README 硬约束#1、P5/P6 正文一致；未见把 CLI 写成应急回退。P6「psmux legacy 回退」指执行宿主，不是控制面。  
- 密钥：注册表禁凭据、审计脱敏、零凭据扫描写进验收；未见把凭据值写入工件的方案描述。  
- P4→P5→P6→P7 主边界成立：P4 不含 Runtime；P5 不含 Herdr/DevHarness 全卡；P6 不含 S0~E13；P7 接 Contract/Attention/E11。无明显「两阶段都交同一成品」的重复。

---

##### 2. 用户理解风险

**P1 · 以为 P5 做完就有「完整参考 CLI（含审批）」**  
H2 全文含 approve；P5 实际只交查询/启停/恢复。纯终端签字要到 P7。

**P1 · 以为 P6 做完 = Linux 日常可用来跑完整任务卡**  
P6 是 SSH smoke + Herdr 会话保活；完整单卡/双平台定型在 P7/P9。`design/06` §10 P6 也写明 smoke。

**P1 · 以为「额度用光自动换号、不用再盯」已是 P6 必达**  
见上：M4 可受限，完整自动 fallback 可进 P8。

**P2 · 文件名仍含「DSH桥接」**  
易读成 DSH 仍是 P5 承重目标；正文已改，但目录名会误导非技术读者。

**P2 · 以为本批计划「审过/确认过」就可以按卡开工**  
文内多次写明预留编号 ≠ 确认 ≠ 开工；仍要阶段闸 + 每卡入口闸。§0.2 旧表述又像「闸前不能审」，两边都容易误读。

**P2 · 以为 P5 交付清单里的 Agent 闭环与 Process 闭环同等必达**  
见 P1 交付/闸冲突。

---

##### 3. 需要用户决定的问题

1. **P5 Agent Executor 闭环：必达还是 P5-X？**（改交付⑨或改阶段闸；不拍板则 P5 完工定义悬空）  
2. **P6-M4（quota/fallback）以 `constrained` 解锁 P7，是否可接受？** 还是 P7 前必须 `passed`？  
3. **quota 命中预登记 fallback：自动切，还是每次 Attention 确认？**（P6 已列理解问题候选，会改 DHR_34 行为与人机负担）  
4. **DHR_35 与 DHR_34：强制串行，还是把身份链写入前移到 DHR_33？**（依赖怎么改，用户要选产品顺序：先看真实闭环，还是先锁身份再演示）  
5. **Linux 本机暂时 SSH 不可达时：允许 P6 带着「待真实 smoke」备注往前走多远？** 是否必须在解锁 P7 前补真实 SSH（正文倾向必须，需你确认）  

（P4 侧「DSH 判否后默认纯 CLI 还是先补 Pi」会影响 P5 Bridge/Executor 候选，但不在本次 P5/P6 正文内强制；若 P4 未拍，P5 开工前仍要回填。）

---

**总结论：有条件通过**  
条件：①裁定 P5 Agent 闭环为交付必达或 P5-X，并改齐 §1/闸/白话；②P6 覆盖表去掉或补证 H7；③修正 DHR_35↔DHR_34 依赖与 Receipt 身份链归属；④收紧 P6 §0.1 对自动切号的完成态表述；⑤（建议）H2 子集与 §0.2 审核时序表述一并改清。无发现凭据入工件、evidence 当拆计划输入、任务 ID 冲突、或把 CLI 写成回退路径的 P0。

### 5.3 主会话裁决

见 §2 裁决总表中「R-P56」各行；已采纳项均已回写到 `dev_plan/P5-Relay-v2持久内核与DSH桥接-开发方案.md`（本 HEAD 变化集内）。

## 6. DHR-B-05 · 讲解、理解问题与确认

<a id="understanding-b05"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-05 artifact=dev_plan/P5-Relay-v2持久内核与DSH桥接-开发方案.md kind=understanding -->

- **讲解**：已做——2026-08-18 主会话在对话中先给六阶段白话说明（各计划 §0.1 同文），再讲本轮审核发现与裁决（本文 §2），并把待拍板项列成选项。
- **理解问题与用户回答**（对话 AskUserQuestion，2026-08-18）：P5「至少一个 Agent Executor 候选闭环」必达还是 P5-X？→「P5-X 可选项（推荐，已按此改）」。
- **用户确认**：待补——P5 属闸前路线图级审核，整版确认在 P4 阶段闸通过后的定向 B-adjust 时再做；其余 P5 待决项（见 §5.2 reviewer 第 3 段）届时一并拍板。

## 7. DHR-B-06 · P6 审核

<a id="review-b06"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-06 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=review -->

### 7.1 审核方式与独立性

- 轮次 R-P56（见 §1 总览）；reviewer 为 fresh-context 独立会话，未参与本计划起草或改写；只读由进程层参数强制。
- 审核对象文件：`dev_plan/P6-Herdr多账号执行底座-开发方案.md`。

### 7.2 reviewer 原文

与上一事件同一轮 R-P56 输出（一次审核覆盖多份计划），原文见本文 §5.2，此处不重复；本计划相关条目已在 §2 裁决总表按「R-P56」标注。

### 7.3 主会话裁决

见 §2 裁决总表中「R-P56」各行；已采纳项均已回写到 `dev_plan/P6-Herdr多账号执行底座-开发方案.md`（本 HEAD 变化集内）。

## 8. DHR-B-06 · 讲解、理解问题与确认

<a id="understanding-b06"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-06 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=understanding -->

- **讲解**：已做——2026-08-18 主会话在对话中先给六阶段白话说明（各计划 §0.1 同文），再讲本轮审核发现与裁决（本文 §2），并把待拍板项列成选项。
- **理解问题与用户回答**：本轮未单独提问；reviewer 列出的 P6 待决项（P6-M4 以 constrained 解锁 P7 是否可接受 / quota 命中自动切还是 Attention 确认 / Linux SSH 不可达时允许走多远）留待 P5 阶段闸通过后的定向复审时一次一问。
- **用户确认**：待补。

## 9. DHR-B-07 · P7 审核

<a id="review-b07"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-07 artifact=dev_plan/P7-DevHarness单卡完整流水-开发方案.md kind=review -->

### 9.1 审核方式与独立性

- 轮次 R-P79（见 §1 总览）；reviewer 为 fresh-context 独立会话，未参与本计划起草或改写；只读由进程层参数强制。
- 审核对象文件：`dev_plan/P7-DevHarness单卡完整流水-开发方案.md`。

### 9.2 reviewer 原文

##### 1. 方案问题

- **[P1] `P7-DevHarness单卡完整流水-开发方案.md:175` / `P8-多卡编排与运行治理-开发方案.md:169` · 用户口径残留**  
  人判仍写「长期**备用**入口」「CLI/SSH **备用**路径」。用户已拍板：CLI 与 DSH 并行、可配置替换，**不是**保底/应急/回退。P9 与 `dev_plan/README.md` 硬约束#1 已改对，但 P7/P8 人判未改干净 → 验收时会把已定产品位再当成待判断题。

- **[P1] `design/06:291` vs `P7:49,211` · 阶段承接缝隙**  
  design/06 §10 P7 明文要求「Start Preview、Attention、**重编排**和 E11 Approval 均可由 Relay CLI 完成」；P7 却把「卡级重编排」整段推到 P8，且未说明「P7 单卡路径不会触发 replan / 或命中则暂停到 P8」。按 06 字面，P7 核心路径缺一块。

- **[P1] `P8:168` · H-ID 语义错配**  
  DHR_45 用 **H9**（Linux 无 GUI / 仅 SSH 代表性工作流）承接「DSH 关闭时 CLI 完成 replan/diagnosis」。该命题属 **H1/H2**（无 DSH 仍可控制）或 **H5/H8**（Attention/Receipt），不是 H9。H9 应留给 P9/DHR_46。

- **[P1] `P7:172` 引用 design/02 **B8** · 验收谓词可判性风险**  
  B8 原文大量绑定 psmux 窗口截图、`decision_ack`、固定短语窗口作答（design/02 决策6 甚至写「不再有 `relay approve`」）。P7 实际交付是 design/06 的 CLI/DSH **Approval Receipt**（H8），却整 ID 回链 B8，只挑了 Finalizer 幂等子集。未显式声明「B8 窗口护栏被 06/H8 取代、本卡只继承 Finalizer 谓词」→ 实施/验收可能按全量 B8 取证，CLI 路径不可判。

- **[P1] `P7:175` / `P8:169` 引用 design/02 **H3/H4** · Oracle 语义拉伸**  
  H3/H4 建立在「产品路径不要求敲命令、窗口作答、2~3 张卡自举」上；P7/P8 核心证明却是 **DSH 关闭 + CLI 全程**。称作「单卡/多卡子集」却未写清：人判问题已按 06 改写、不再用「不敲命令行」作通过标准。

- **[P2] `P7:51-54,217` / `P8:50-53,211` · §1 承接清单与任务卡引用不一致**  
  - P7 §1/§6 列 H2/H6/H7/H8/H10/H11/H12，但卡内还用 **H1/H3/H4**。  
  - P8 §1/§6 列 H3/H5/H8/H11，但卡内还用 **H1/H2/H4/H9**。  
  覆盖表自称「已盖住」不成立，后续查漏会漏扫。

- **[P2] `P9:184` · H2/H5/H6/H7/H12「三条 Dogfood 整体覆盖」过软**  
  无卡级机器谓词点名 H6（非 DSH-only Executor）、H7（DSH-only Attempt 隔离）、H12（换 Executor → fresh Attempt）。定型阶段回验可以，但不能只靠「整体 Dogfood」一笔带过。

- **[P2] P6→P7 前置边界基本成立，但复核 Profile 未钉死**  
  已核：P6 不含完整 DevHarness、含 Herdr/账号/SSH smoke；P7 不含多卡/Linux 定型——主边界成立。未写清：P7 复核角色所需 **Pi / DSH one-shot / fresh Herdr** Profile，是 P6 名册必有项，还是 P7 开工前补登。缺口在开工 B-调整前必须闭合。

- **[P2] `P9:86` ·「应急只读」残留**  
  agent-console 候选仍写「应急只读」。非 CLI 口径，但会把「应急/回退」话术带回控制面讨论。

- **[P2] `dev_plan/README.md:31` ·「P4 DSH 优先」与硬约束#1 并存**  
  硬约束#1 已改为并行可替换；阶段地图仍用「DSH 优先」。易读成 CLI 仍是次等路径（P4 Pilot 策略与产品位未分开写）。

- **[P3] 任务 ID / 状态列 / evidence 作输入**  
  DHR_36~48 与 README 纪律一致、无重复；状态均为「未开始」；§1 承接未把 `evidence/*` 当拆计划输入。P7/P8 status「看什么」提到 design/04，未进 §1 验收回链——暂不升格，但勿据此加需求。

- **[P3] 机器证/人判二分总体成立**  
  B13「人判留痕」标机器证（拒代填）合理；真正人判集中在 Px-H。主要风险是上述 B8/H3 语义，而非栏目标错。

##### 2. 用户理解风险

- **[P1]** 读 P7/P8 人判会以为「CLI 算不算备用」还要你再拍板；你其实已经定了「并行正式控制面」。P9 文案正确，三份计划口径打架。  
- **[P1]** P7/P8/P9 §0.2 都写「只在前置阶段闸通过后才进正式 fresh 审核」，但当前整包已在「B-调整后待 fresh 审核」。非技术读者会以为「现在审还早 / 审了也不算」。  
- **[P2]** §0.1 说「命令行也能走完全程」，正文又大量「DSH 首选 / 增强轨」——若不先看硬约束#1，容易以为日常仍必须开 DSH，CLI 只是故障时用。  
- **[P2]** 「完成后有一张真实卡 verify / 三卡 Oracle」≠ 已授权选卡或开工；真实卡、周报项目仍待你选——§0.2 有写，但 §0.1「手里有什么」读起来像结果已锁定。  
- **[P2]** `design/README.md` 仍写「当前只有 P4 可进 fresh 审核」（未核验是否另有流程约定）——与正在审 P7~P9 的动作并读会混乱。

##### 3. 需要用户决定的问题

- **[P1]** design/06 §10 P7 的「重编排可由 CLI 完成」：P7 是否必须具备**最小卡内 replan+CLI 批准**，还是明确改 06 为「P7 只保证 Start/Attention/E11，重编排从 P8 起」？这会改 P7/P8 范围与先后。  
- **[P1]** E11 人机确认真源：以 design/06 的 **Relay Approval Receipt（CLI/DSH 同合同）** 为准，并宣布 design/02 决策6/B8 窗口截图护栏在多控制面下如何继承/退役？还是 CLI 路径仍要等价窗口证据？不拍板则 DHR_40 验收不可收口。  
- **[P2]** P7 人判还问「CLI 是否够用」——在已定「并行正式控制面」后，人判应改为体验/证据是否够格，还是你仍想保留「默认入口选 DSH 还是 CLI」为 P7/P9 可逆决策？  
- **[P2]** P8 理解问题候选（并行卡在 replan 时继续 vs 全停）会改调度语义，需在 P8 确认前拍板。  
- **[P2]** P9：DSH 升级失败自动回滚 Profile vs 停等你决定；agent-console 三选一——均改发布与维护范围。

---

**总结论：有条件通过**（条件：①删尽 P7/P8「备用入口/备用路径」并与硬约束#1/P9 对齐；②裁定 06§10 P7「重编排」归 P7 还是 P8 并回写一侧；③DHR_45 去掉错用 H9，改 H1/H2 或 H5/H8；④DHR_40 对 B8 显式子集化或改以 H8+Finalizer 谓词为主，并注明对 design/02 窗口确认闸的覆盖关系；⑤补齐 P7/P8 §1/§6 的 H-ID 清单与卡内引用一致；⑥P9 为 H6/H7/H12 补可点名的回验项；⑦写清 P7 复核 Profile 对 P6 名册的依赖。）

### 9.3 主会话裁决

见 §2 裁决总表中「R-P79」各行；已采纳项均已回写到 `dev_plan/P7-DevHarness单卡完整流水-开发方案.md`（本 HEAD 变化集内）。

## 10. DHR-B-07 · 讲解、理解问题与确认

<a id="understanding-b07"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-07 artifact=dev_plan/P7-DevHarness单卡完整流水-开发方案.md kind=understanding -->

- **讲解**：已做——2026-08-18 主会话在对话中先给六阶段白话说明（各计划 §0.1 同文），再讲本轮审核发现与裁决（本文 §2），并把待拍板项列成选项。
- **理解问题与用户回答**（对话 AskUserQuestion，2026-08-18）：design/06 §10 P7「重编排可由 CLI 完成」vs P7 不含卡级重编排，以哪边为准？→「重编排从 P8 起（推荐）」，并同步改 design/06 §10 P7 那句。
- **用户确认**：待补——P7 属闸前路线图级审核；E11 真源（design/02 决策 6 / B8 窗口护栏是否退役）留待 P6 阶段闸通过后的定向复审时提问。

## 11. DHR-B-08 · P8 审核

<a id="review-b08"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-08 artifact=dev_plan/P8-多卡编排与运行治理-开发方案.md kind=review -->

### 11.1 审核方式与独立性

- 轮次 R-P79（见 §1 总览）；reviewer 为 fresh-context 独立会话，未参与本计划起草或改写；只读由进程层参数强制。
- 审核对象文件：`dev_plan/P8-多卡编排与运行治理-开发方案.md`。

### 11.2 reviewer 原文

与上一事件同一轮 R-P79 输出（一次审核覆盖多份计划），原文见本文 §9.2，此处不重复；本计划相关条目已在 §2 裁决总表按「R-P79」标注。

### 11.3 主会话裁决

见 §2 裁决总表中「R-P79」各行；已采纳项均已回写到 `dev_plan/P8-多卡编排与运行治理-开发方案.md`（本 HEAD 变化集内）。

## 12. DHR-B-08 · 讲解、理解问题与确认

<a id="understanding-b08"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-08 artifact=dev_plan/P8-多卡编排与运行治理-开发方案.md kind=understanding -->

- **讲解**：已做——2026-08-18 主会话在对话中先给六阶段白话说明（各计划 §0.1 同文），再讲本轮审核发现与裁决（本文 §2），并把待拍板项列成选项。
- **理解问题与用户回答**：本轮未单独提问；reviewer 列出的 P8 待决项（并行卡在 replan 时继续还是全停）留待 P7 阶段闸通过后一次一问。
- **用户确认**：待补。

## 13. DHR-B-09 · P9 审核

<a id="review-b09"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-09 artifact=dev_plan/P9-双平台定型与迁移-开发方案.md kind=review -->

### 13.1 审核方式与独立性

- 轮次 R-P79（见 §1 总览）；reviewer 为 fresh-context 独立会话，未参与本计划起草或改写；只读由进程层参数强制。
- 审核对象文件：`dev_plan/P9-双平台定型与迁移-开发方案.md`。

### 13.2 reviewer 原文

与上一事件同一轮 R-P79 输出（一次审核覆盖多份计划），原文见本文 §11.2，此处不重复；本计划相关条目已在 §2 裁决总表按「R-P79」标注。

### 13.3 主会话裁决

见 §2 裁决总表中「R-P79」各行；已采纳项均已回写到 `dev_plan/P9-双平台定型与迁移-开发方案.md`（本 HEAD 变化集内）。

## 14. DHR-B-09 · 讲解、理解问题与确认

<a id="understanding-b09"></a>

<!-- dh:planning-evidence:v1 event=DHR-B-09 artifact=dev_plan/P9-双平台定型与迁移-开发方案.md kind=understanding -->

- **讲解**：已做——2026-08-18 主会话在对话中先给六阶段白话说明（各计划 §0.1 同文），再讲本轮审核发现与裁决（本文 §2），并把待拍板项列成选项。
- **理解问题与用户回答**：本轮未单独提问；reviewer 列出的 P9 待决项（DSH 升级失败自动回滚 vs 停等 / agent-console 三选一）留待 P8 阶段闸通过后一次一问。
- **用户确认**：待补。

## 15. DHR-A-09 · design/06 §10 订正审核

<a id="review-a09"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-09 artifact=design/06-多控制面与Headless-SSH运行-设计补充.md kind=review -->

### 15.1 触发与审核方式

- 触发一：用户 2026-08-18 明确口径「Relay CLI 与 DSH 是并行、可配置替换的两种控制面，CLI 不是保底 / 应急 / 回退」，design/06 §10「P9」原句「Windows 验证 DSH 首选工作台和 CLI 回退」与之冲突。
- 触发二：R-P79 fresh 审核（本文 §9.2）指出 design/06 §10「P7」写「重编排可由 CLI 完成」，而 P7 计划把卡内重编排整块放到 P8，两边必须取一。
- 审核方式：不改设计结论（§1）、验收命题（§11）与任务拆分，只改 §10 两句阶段措辞；R-P4 / R-P56 / R-P79 三路 reviewer 均已核对 design/06 §1、§10、§11 与六份计划的一致性（本文 §3.2、§5.2、§9.2），其中 R-P4 明判「阶段闸不对称 ≠ 把 CLI 定位成保底」、R-P79 明判 §10 P7 与 P7 计划有缝隙。
- 主会话裁决：两处均采纳为 design/06 §10 措辞订正，不构成新的验收命题；design/README 白名单不变。

### 15.2 改动清单

| 位置 | 原句 | 新句 |
|---|---|---|
| §10「P9」 | Windows 验证 DSH 首选工作台和 CLI 回退； | Windows 验证 DSH 工作台与 Relay CLI 两条并行、可配置替换的控制面（含 DSH 关闭 / 卸载后切换到 CLI 继续操作）； |
| §10「P7」 | Start Preview、Attention、重编排和 E11 Approval 均可由 Relay CLI 完成。 | Start Preview、Attention 和 E11 Approval 均可由 Relay CLI 完成；单卡路径命中依赖阻塞时暂停并留持久 Attention，卡内重编排从 P8 起。 |

## 16. DHR-A-09 · 讲解、理解问题与确认

<a id="understanding-a09"></a>

<!-- dh:planning-evidence:v1 event=DHR-A-09 artifact=design/06-多控制面与Headless-SSH运行-设计补充.md kind=understanding -->

- **讲解**：已做——对话中说明「保底 / 应急」措辞的出处（GPT 原稿 P4-H1、P9、design/06 §10 P9），并说明改动只涉措辞、不改验收命题。
- **理解问题与用户回答**（2026-08-18）：①「design/06 第 298 行那句 CLI 回退要不要一并改？」→ 用户「可以改」；②「§10 P7 的重编排以哪边为准？」→ AskUserQuestion 选「重编排从 P8 起（推荐）」（选项说明含「同步把 design/06 §10 P7 那句改成 Start / Attention / E11」）。
- **用户确认**：以上两条回答即为对本次 §10 措辞订正的确认；design/06 其余内容不在本事件范围。
