# DHR-B-36 · DHR_71 隔离清单 2→4 与绿闸范围校正 · B-调整交叉审核记录

> 本记录承载 `DHR-B-36` 的审核过程、主控裁决与用户理解对齐。**它不是设计输入，不参与拆计划。**
> 待审对象：[`drafts/DHR-B-36-DHR71隔离清单与绿闸范围校正-候选.md`](../../dev_plan/drafts/DHR-B-36-DHR71隔离清单与绿闸范围校正-候选.md)（v3）；审核 brief：[`drafts/DHR-B-36-review-brief.md`](../../dev_plan/drafts/DHR-B-36-review-brief.md)。

<a id="review-b36"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-36 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=review -->

> **本事件的复审总账**：三名 fresh 只读实例、三轮，**5 个独立议题：采纳 5 · 驳回 0**。三轮均为机器强制只读（codex `gpt-5.6-terra` · `model_reasoning_effort=high` · `--sandbox read-only`，经 Herdr 交互式 pane 从主树派出，cwd=仓根 `master@81f5a53`），派出前后 `git status --porcelain` 均仅为草案 + brief 两个未跟踪文件、HEAD 未动——零写入取证通过。三名均为静态核对、未运行测试（只读沙盒）。

## 0. 触发与授权

- **触发**：DHR_71（2026-09-02 D-start，`81f5a53`）施工 worker（Opus · Herdr pane）按 task_plan 的 blocked 判据停手：`wt/DHR_71@b268456` `construction.DONE status: blocked`。三轮绿闸 **51/2/2 → 50/2/3 → 51/2/2**（pass/skip/fail），机器证 B（等待有界 + 负例）/ C（652s 定位并在测试侧修掉）/ D（零越界）达成，机器证 A 差 fail=0。
- **主控独立核出的事实**：①「DHR_33 窄路径」（`agent-node:198`）红因是断言 `undefined !== 'E_EXECUTOR_KILLED'`，非超时，与 B-35 已 skip 的 `:279` 同根（`workflow-driver.mjs:425`）；②「#10 恢复届」（`herdr-adapter:354`）超限 dump 打出 `human_input_requested:E_EXECUTOR_HOST_LOST`，对应恢复届 `probe.missing` 分支（`:539-540`），DHR_64 `0dd371d` 已删直写 orphaned；③652s = `t.after` 的 `rm` 撞 driver 正在写的 `state.json.<uuid>.tmp` 永不返回（`hang-repro` A/B 组 exit=124、E 组仅剩 `FSReqPromise`、F 组 `try/finally stop` 5 轮 0 挂）——主控 D-start 时给的第一嫌疑（缺 `t.after(stop)`）**被负例证伪**；④`dhr69-false-ready:179` 三轮 绿/红/绿，文件不在 DHR_71 允许路径。
- **用户裁决**：2026-09-02 点选「**走小 B-adjust**」（备选：只扩隔离清单不碰 dhr69 / 先看详细证据）。

## 1. 第一轮 fresh 只读审核（v1 → v2）

| 复审者 | 形态 | 独立性 | 结论 |
|---|---|---|---|
| `b36rev1` | codex `gpt-5.6-terra` · high · `--sandbox read-only` · Herdr pane `w2:pV` · cwd=主树 | fresh，未参与起草 | **不通过**（4×P1） |

| 议题 | 问题 → 事实 → 影响 | 裁决 |
|---|---|---|
| R1-01 | 四条 skip 只有行号，不是可一一核销的冻结清单 → DHR_72「0 skip、55/55」无法据此证明恰解除四条 | **采纳**：改为「文件 + 完整用例名 + `master@81f5a53` 行号」表（S1~S4），按用例名核销 |
| R1-02 | 串行顺序不等于互斥：S3/S4 用例体两卡都要碰，责任边界未冻结 | **采纳**：写入反向禁改——DHR_71 只拥有等待 / `try/finally stop` / skip，DHR_72 只拥有去 skip / 改终态断言，双向不得回改 |
| R1-03 | P6 §0.2 B-35 事件段仍写 F-3520 为「2 语义 3 时序」，与草案 4/1 矛盾 | **采纳**：原句留痕，追加 B-36 校正句 |
| R1-04 | `dhr69:179` 判「抖动」证据不足；「上限由生产推出」无可验收公式；45s 覆盖不了合法启动时间 | **采纳**：冻结上限公式（v2 版为统一公式，v3 按 R2-01 改为逐链推导）；`:510` 两处一并改正；负例用临时缩小上限；D-B36-2 改为如实陈述三条路代价 |

- 已核事实：F-7102/F-7104 确属语义红；652s 归因与并入 BL-17 合理，BL-17 保持 open。第三段未新增决定点。

## 2. 第二轮定向复审（v2 → v3）

| 复审者 | 形态 | 独立性 | 结论 |
|---|---|---|---|
| `b36ver1` | 同形态 · Herdr pane `w2:pW` | fresh（第二个实例） | **不通过**（1×P1） |

- R1-01 / R1-02 / R1-03 **到位**（S1~S4 四个用例名与 `:242/:279/:354/:198` 逐字一致；反向禁改与 B-35 X-03 的 `runtimeFixture`/`recoveryFixture`/`fake-herdr.mjs`/`workflow-driver.mjs` 唯一归属相容；§2.3 原句留痕）。
- **R2-01（R1-04 未闭合）**：`dhr69:179` 夹具默认 **Claude** profile，走 `paneRun → agentList/rename → observe` 链，blocked 由 `idle ∧ pane blocked` 在 `observeHerdrAgent` 派生，不进 `startBlocked` 分支、不消耗 `HERDR_START_TIMEOUT_MS`；与 `:510`（Codex `agentStart` `notReady`）不是同一路径，统一公式不成立 → **采纳**：§2.1 改为「按各自真实调用链逐段相加 × 1.5」规则 + 两条链逐段表（`:510` 120s、`dhr69:179` 90s，同数不同因），并明写为保守上限、不解释成因。
- 独立核实：`herdr-executor.mjs:99`、`herdr-cli.mjs:17/:41` 引用成立。

## 3. 第三轮定向复核（v3，仅 R2-01）

| 复核者 | 形态 | 独立性 | 结论 |
|---|---|---|---|
| `b36ver2` | 同形态 · Herdr pane `w2:pX` | fresh（第三个实例） | **PASS，无 P0/P1** |

- 七项逐段核对全「对」：`:510` `startBlocked`（executor `:75`）、Codex 不进 rename（`:85-87`）、ready deadline 0（`:99`）、blocked 不读 `paneGet`（`:114-125`）→ 80s × 1.5 = 120s；`dhr69:179` 默认 Claude（fixture `:33-55`）六次通用 10s 调用 → 90s；派生关系与 `observeHerdrAgent :118-132` 一致；`herdr-cli.mjs:17,41-42,70` 取值一致。

<a id="understanding-b36"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-36 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=understanding -->

## 4. 讲解、理解问答与用户确认（2026-09-02）

- **讲解要点**（对话中已讲）：DHR_71 不是没修好，而是取证证明 4 条（不是 2 条）旧用例停在 DHR_64 之前的语义上，按 B-35 分工不该由 DHR_71 重写，故隔离清单 2→4、语义债务完整移交 DHR_72；四处改动（DHR_71 清单/路径/上限规则/措辞、DHR_72 机器证 E 与反向禁改、P6 §0.2 校正、BL-17 补形态）；不动卡序、依赖、DHR_72 运行契约、夹具归属、design/12。
- **理解问题**：「B-36 落盘后，DHR_71 收口时定向套件跑出『55 条 · 51 pass · 4 skip · 0 fail』，这意味着什么？」
- **用户回答**：「**DHR_71 达标；4 条是转给 DHR_72 的债**」——正确；同时排除了「skip = 永久豁免」与「有 skip 就不算绿闸」两条错解。
- **用户确认**：点选「**确认落盘**」，含 D-B36-1（隔离清单 2→4）与 D-B36-2（`dhr69:179` 归 DHR_71，上限按真实 Claude 链推导）。
- **授权边界**：本确认只授权本次 DevPlan（DHR_71/DHR_72 卡、§0.2 校正、事件 marker）、backlog `DHR-BL-17` 补录、本审核记录与草案状态落盘，以及 DHR_71 worker 按新合同**续做增量**（仍在既有 D-start 授权至 E10 之内）。**不授权** DHR_72/73 D-start、生产代码改动、真实 Agent、DHR_35 重跑、verify、合并、推送、部署或环境操作。
