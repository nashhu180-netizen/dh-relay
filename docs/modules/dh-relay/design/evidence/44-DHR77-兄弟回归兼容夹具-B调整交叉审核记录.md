<!-- dh:v1 -->
# DHR-B-45 · DHR_77 兄弟回归兼容夹具 B-adjust 交叉审核记录

> 状态：fresh-context 审核 approved、用户确认完成，`DHR-B-45` 可原子落盘。本轮只增加两条直接受 `host_ref` 来源/事件合同影响的测试路径，并把不可见长施工改成完整批次间主控检查；不授权生产实现、DHR_76/DHR_34 修复、复核、verify、合并、DHR_35、推送或部署。

<a id="review-b45"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-45 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=review -->
## 1. fresh-context 独立审核

- 审核实例：`/root/b45_fresh_review`，fresh context、未参与 DHR_77 施工；按只读指令静态核验主树候选 diff、`wt/DHR_77@9ea4714` 与 master 对照。运行环境具写能力，故不声称机器强制只读；审核前后主树除候选四文件外无新增改动，任务树保持 `9ea4714` 洁净。
- 结论：`approved`；P0=0、P1=0、P2=0、P3=0。可按 B-45 落盘并仅启动第二施工批次。
- allowed paths：DevPlan、brief 与 task-plan 一致，只新增 `relay-core/test/dhr69-false-ready.test.mjs`、`relay-core/test/dhr75-host-lease-during-herdr.test.mjs`。
- 最小性：DHR_69/C 的共享 recovery fixture 直接落 alive event 却缺 `host_ref`；DHR_75/B 的 fake `agent get` 仅给 status/seq、缺 `terminal_id`。分别补脱敏输入与 launch 对应 ID 即可，不能放宽 schema/来源校验或改原断言。
- 隔离与节点：DHR_76/C、DHR_34 identity-quota 明确保持 master 同形基线，不称完整 npm 绿；两批仍属于同一 DHR_77 原子目标，批次 2 后才进入 heavy review。OpenCode `glm-5.3-flash`、reasoning `max` 仅是执行形态，不扩大路径、验收或权限。

## 2. 主控检查点与方案

首个完整施工批次 `wt/DHR_77@9ea4714` 的定向 106/106、DHR_72 8/8、DHR_64+DHR_70 21/21、CLI/client 19/19、audit 0 violation 与根测试 exit 0 均有终态；完整 `npm test` 为 338 tests、333 pass、4 fail、1 cancelled，不能称绿。

主控随后逐项做同名窄测试与 master 对照：

- DHR_69/C：任务分支 0/2，`E_SCHEMA_INVALID:E_BAD_TYPE`；master 2/2。原因是两条 recovery fixture 直接写 alive event/v2 而未带 `host_ref`。
- DHR_75/B：任务分支 0/1，首个观测为 `observation_lost` 且 60 秒内无 checkpoint；master 1/1。原因是 fake `agent get` 只返回状态、不返回新来源合同要求的 `terminal_id`。
- DHR_76/C frozen 60s budget：任务分支与 master 均约 56 秒同形失败。
- DHR_34 confirmed quota fallback：在 master 单独运行也长时间无终态，主控约 2 分钟后终止。

裁决：前两项是本卡协议变化直接影响的兼容夹具，加入精确 allowed paths 并只补输入；后两项为主干基线，保持范围外。task-plan 新增第二完整批次，新 worker 批次结束即停，主控检查后才进入 heavy 复核。

<a id="understanding-b45"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-45 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=understanding -->
## 3. 用户理解、确认与权限

- 主控向用户展示四项对照，并提出六条 B-45：增加两个精确测试路径；worker 只补兼容夹具；批次结束由主控检查；DHR_76/identity-quota 只记基线；定向通过后才启动 heavy 代码轮 1；当前仍为施工未完成。
- 用户明文回复：“同意新增”。随后要求“下次要施工，尝试拉 opencode 让 glm-5.3-flash 推理强度Max进行”。
- 该确认授权候选通过 fresh 审核后原子落盘，并启动 fresh OpenCode `glm-5.3-flash`、reasoning `max` worker 执行第二完整批次；不授权 DHR_76/DHR_34 修复、复核、verify、合并、DHR_35、push 或 deploy。
