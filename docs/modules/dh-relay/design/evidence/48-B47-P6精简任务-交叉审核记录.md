# B47 · A33 精简任务交叉审核与整版确认记录

<a id="review-b47"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-47 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=review -->

对象：[B47 形成史](../drafts/DHR-B-47-DHR78单一启动内容与有限重发-候选.md)。用户要求“继续优化开发方案、开发任务”；当前正式输入为 master@6a3244d 的 A33 design/15。主控和 reviewer 均独立调用 resolver，结果 state=new、mode=manifest、diagnosticCode=null、8 份正式输入；本次承接 A9..A12/H3，其余既有分期不变。

## Fresh 审核

实例 `/root/b47_minimal_review`，fresh context、未参与起草，侦测型只读、非机器隔离。只读候选、正式输入及 P6 DevPlan、必要 Store/driver/schema 代码；未写文件、未测试、未运行 Agent。基线 HEAD=6a3244d，前后仅 B47 未跟踪候选。

确认事实：driver 当前三处 completion-only sender 需归零；relay.run/v2 node additionalProperties=false，instruction_ref 必须入 schema；Store 串行写队列可承接私有计数，没有已证实的 host/service/state/兼容矩阵变更需求。重核卡保留合理，但无需恢复 cursor、UTC 持久计时、timeline 或 checkpoint/Result 事务重构。

首审 CHANGES_REQUESTED：P0=0，P1=1。A9 包含核心发送侧与 DHR_35 外围第二 prompt 删除，不能既把外围工作留给 DHR_35，又说 DHR_78 独自完成整个 A9。

主控采纳最小修订：DHR_78 只承接 A9 核心分账及 A10..A12/H3；DHR_35 新增 A9 外围分账（instruction_ref、外围 prompt=0、新基线唯一入口）。模块 A9 全量结论须两份证据均闭合；DHR_78 就自己的分账收口，不改 DHR_35 工作树。

同一实例定向复审 PASS，原 P1 闭合、无新增 P0/P1；HEAD/status 不变。

<a id="understanding-b47"></a>
<!-- dh:planning-evidence:v1 event=DHR-B-47 artifact=dev_plan/P6-Herdr多账号执行底座-开发方案.md kind=understanding -->

## 展示与确认

用户已确认 A33 的正常一次补发、异常恢复不自动发送取舍。主控展示本次 B 的两张卡责任：DHR_78 实现核心、DHR_35 删除外围 prompt 并取得真实链，依赖顺序为 DHR_77 → DHR_78 → DHR_35。具体任务、精确路径和 A9 双方分账均已写入候选并完成复审。

在“是否将这版正式写入 P6 开发方案和任务表”的确认口，用户明确回复“确认写入”。据此更新正式 P6 DevPlan 与索引并保留候选形成史。DHR_78 状态为未开始，工作区在开工时建立；本次未运行真实 Agent，未修改生产代码。
