# DH Relay P2 · 完整流水 —— 产品设计与验收

<!-- dh:topic tier=标准 review=完整流水 -->
<!-- dh:planning-event:v1 id=DHR-A-11 stage=A-full artifact=design/02-完整流水-产品设计与验收.md review=evidence/02-交叉审核记录-完整流水.md#review-a11 understanding=evidence/02-交叉审核记录-完整流水.md#understanding-a11 -->

> 状态：完整流水历史目标，不再属于当前 `designInputs[]`。仍有效的 dev-harness、安全边界与独立复核原则由 [09 三 Agent 正式设计](./09-三Agent可交互终端与CLI优先编排-产品设计与验收.md) §13.2 承接；旧 psmux/窗口弱信任、常驻诊断角色及 P2 卡序不再约束当前主线。

## 文档信息

| 项目 | 内容 |
|---|---|
| 所属模块 | `dh-relay` |
| 文档类型 | 正式产品设计与验收（P2，与 `01-产品设计与验收.md` 共同组成正式设计输入包；P1 契约仍以 01 为准，本文按 `relay/v2` 版本化扩展，例外逐条登记于 §3「版本与兼容」，不再笼统称"只扩不改"） |
| 当前状态 | `DHR-A-04` 及之前**已由用户整版确认并提交**（`DHR-A-02` `cf5b973` / `DHR-A-03` `e85a5eb` / `DHR-A-04` `49615e2`）；**`DHR-A-05` 亦已确认并提交**（`25b2ec8`；R10 定向复审 9 条 P1 全采纳 + 用户拍板 D21～D24）。现役正式验收清单=**B1～B18 / H3～H5**。已授权按此重拆 B；**不授权任何卡开工** |
| 编辑模型 | Claude（Fable 5） |
| 最后更新 | 2026-08-16 |

> 承接：[design/01 §1.4 目标形态](./01-产品设计与验收.md)、DHR_03 销户记录 H2 方向决策账、[真实业务试点 IHSR_05](../workspace/DHR_03/evidence/H2-真实业务试点-IHSR_05.md)（RB-1～RB-5）、DHR_03 findings F-002/F-019/F-023；审核与裁决见 [evidence/02](./evidence/02-交叉审核记录-完整流水.md)。
## 1. 人话版目标

### 1.1 我们在做什么

我理解你要的是：P1 已经证明"没有常驻 AI 主控，靠文件状态也能接棒"，IHSR_05 也证明一张真实业务卡能在 relay 上跑完六棒。但那一趟还是靠主控会话手写计划、拼包装、盯窗口、亲跑 oracle、亲做收口。**P2 要把主控还在干的活搬进 relay 本身**：

- **relay 只管"已经立项、已经拆好计划、你在对话里授权开工的任务卡"**——立项（A）与拆计划（B）不在 relay 里，仍由你和主会话按 dev-harness 走。
- **你不敲命令行**（用户 2026-08-16 拍板 D14）：你在主会话对话里说"用 relay 跑这几张卡"，主会话先落一份**授权工件**（哪几张卡、什么时候、基于哪个版本的 DevPlan、各卡目标/验收/变更范围快照），再代你在**该仓**把 relay 拉起来；之后从建工作区、施工、机器闸、两轮代码复核、集成与端到端、需求/一致性复核、教训复核，到收口销户，全程由确定性 Runner 按接力计划接棒，每张卡各自 verify 销户。
- 你只在**agent 弹出的窗口里说话**：落户确认、偶尔的决策、真机端到端协作、收口确认、异常时的诊断问答；不敲命令行、不依赖飞书。
- 终端全关、隔天再来：磁盘上的接力计划和进度都在，主会话代你续（或双击快捷方式打开 `relay` 控制台按编号选一个 run），断掉的棒由诊断 agent 带你接上。

一句话：**P2 交付"一批已授权任务卡从落户到销户的无常驻 AI 主控接力"，可从任意仓发起、可跨天接续，并用真实卡自举跑通。**

> 措辞约定（审核 U2）："无常驻 AI 主控"不等于没有控制面——Runner/宿主、接力计划生成 agent、汇合 agent、诊断 agent 与 finalizer 脚本仍构成控制面，只是不再由一个常驻 AI 会话承担。

### 1.2 与 P1 / IHSR_05 试点的差异

| 项 | P1 PoC | IHSR_05 试点 | P2 目标 |
|---|---|---|---|
| 从哪发起 | dh-crew 仓内 `run-dogfood.ps1` | 业务仓自写 `run-relay.ps1`，现场却落在 dh-crew | **你在对话里说，主会话落授权工件并代起**（D14）；`relay` 也在 PATH、可双击快捷方式进控制台，但不要求你敲命令；运行现场落**该仓** `.dh-relay/<run_id>/`（不进 git）、接力留档落**该仓** `docs/relay/`（进 git、全仓唯一、README 为总目录），**各模块下零 relay 文件夹**（D21）；注册表与跨仓 run 索引在用户级 `~/.dh-relay/` |
| 一次跑多少 | 假需求 A/B/C | 一张卡 | 一份接力计划带 **2～3 张已授权卡**（试验多卡），按 DevPlan 依赖串/并行，每卡独立走完流水并 verify |
| 计划从哪来 | headless 编排 agent 写 v1 | 主控手写 plan-v1 | 一次性**接力计划生成 agent**按标准流水模板实例化（只填槽位：卡、brief 引用、profile_id、script_spec_id、开关、返工上限）；也允许手动 `propose`（RB-2）。通用自由规划留 P3 |
| 节点角色 | worker / reviewer / replanner | build / review×N / fix | 沿用三个 + 新增 `planner / workspace / build / fix / review_lead / integration / requirement_review / lesson_review / closer / diagnoser / gate(脚本) / finalizer(脚本)` |
| 复核 | 无 | 两轮 fresh 代码复核 | **三路复核**显式成棒：代码复核 R1 → fresh R2 → 汇合；需求与一致性复核；教训复核 |
| 机器闸 | 无 | 主控亲跑 oracle | **脚本节点**：宿主按注册表 script_spec 执行、不起 agent，结果是机器事实（`gate_outcome`） |
| 真机端到端 | 无 | 无 | 可选棒 **集成与端到端 agent**：合到**本地** `test` 分支、跑 e2e；需要真机时在窗口给你命令、你回贴输出（P1 §4.2 多轮配合语义）。**本质是人验/帮 AI 取证的动作**（用户 2026-08-16 原话），产出进证据账本、不由 agent 判通过；**推远端 `test` 是跟着项目走的条件分支**——仓有远端且开关开才做，没远端就只做本地合并与协作 |
| 每棒用谁跑 | 默认 claude | 包装写死 cwd/账号 | 计划只引用 `profile_id`；用户级注册表定义 cli/配置目录/工作目录根/模型/fallback；支持 codex（含 ninth 账号） |
| 额度耗尽 | 一律 `interrupted_unknown` | 靠人换 | 高置信识别才换预登记 fallback；否则 paused → 诊断 agent 问你 |
| 异常处理 | 用户手动触发诊断 | 主控人肉 | Runner **自动拉起只读诊断 agent**，它在窗口告诉你现场并问"续跑/换账号/放弃"，替你发控制事件 |
| 用户怎么知道该出手 | 看窗口 / 主控汇报 | 主控对话约 12 条汇报 | 只看弹出的窗口 + `relay`/`relay status` 控制台；飞书通知留 P3；主控对话消息=0 |
| 收口 | 无 | 主控亲做 E4～E13 | 收口 agent 备料 + 在窗口问你"认可本地收口？" → 你答"认可" → 它调 **finalizer 脚本**（暂存分支 squash/复验/回填/verify → 一次快进 master → 清树） |
| 接续 | 同 RunId 续（实证） | 同左 | 主会话代你续，或双击快捷方式打开 `relay` 控制台按编号选未完成 run；续跑时断棒自动 `interrupted_unknown` → 诊断 agent（登录自启留 P3） |

### 1.3 关键决策（动这些要重新讨论）

1. **relay 的边界 = 已授权任务卡；"授权"是一份可审计的工件，不是命令行参数**（用户拍板 D14）：用户在主会话对话里授权后，主会话落一份不可变 **authority snapshot**（授权时刻、卡号集合、各卡的 DevPlan 行与目标/验收/变更范围、所依据的 DevPlan 与 design 文件的 Git blob 哈希、授权证据指针），relay 只消费它；传入不在 snapshot 内的卡一律拒绝。**run 跑到一半你正当地改了某卡的 DevPlan 口径 → 只冻结那张卡「及依赖它的卡」并由诊断 agent 在窗口问你（重新授权 / 回滚 / 取消），无关卡照常；snapshot 工件本身被篡改则全 run 停**（D17；R9 审 S4 补依赖闭包与安全停点：冻结判定在**每次 launch 前、远端 push 前、finalizer 最终 CAS 前**各重验一次；已在跑的 worker 不强杀，其结果按失效隔离；若该卡已 push 远端 test，先走 B16 补偿路径再冻结；`finalizing` 中途命中失配 → 走 `closeout_blocked` 而非中断 CAS）。不做立项/拆计划；不改 DevPlan 任务的目标/验收（只回填状态与 SHA）。命令行入口 `relay start -Task …` 仍存在，但只作为脚本/调试通道，**产品路径不要求用户敲命令**。
2. **流水就是 dev-harness 节点表的接力版**：S0～S3（落户）→ 施工 → E2 代码复核（两轮换人）→ 集成/需求境证据 → E4 需求复核 → E6 教训复核 → E5～E10 备料 → E11 用户确认 → E12～E13 verify/销户。relay 不发明第二套流程；`review.md`/`progress.md`/证据账本仍是 dev-harness 工件，由对应棒写。
3. **Runner 仍不判质量**：汇合 agent 写 `verdict`（`approve / rework / escalate`），Runner 只按枚举分支，并强制"缺任一复核棒结果不放行、返工轮次有界"。
4. **脚本节点是机器事实的唯一来源**：dh check、测试、e2e 脚本、finalizer 以脚本节点跑；宿主脚本执行器扮演该 attempt 的 worker（receipt backend=script），只写宿主私有 candidate，Runner 照常校验晋级；结果带 `gate_outcome=pass|fail|error`；**不新增 result_status**（审核 S1）。
5. **返工默认 fresh fix worker**（用户拍板 D1'）；原会话恢复留目标形态。
6. **确认闸 = agent 窗口作答**（用户 2026-08-16 对话拍板，推翻审核推荐的一次性回执 CLI）：落户确认、决策、收口确认都在对应 agent 的窗口里明文回答，agent 把回答原文写进 result/review.md 确认记录，Runner 不读回答。**登记为已接受的弱信任**（输入者身份不可机器验证；IHSR_05 F-006 有误点/代答先例），配三道**由宿主持有**的护栏（审核 R4-04）：① agent 必须先写 `decision_required` checkpoint 挂起；Runner 摄入该 checkpoint 时记 `decision_checkpoint_event_id`，收口 result 引用的 event_id 须存在于事件账且早于 result 摄入，否则拒；② 收口确认要求用户输入固定短语「认可执行本地收口」原文（agent 抄录，弱信任）；③ **截图由宿主拍**——摄入 decision checkpoint 时与摄入确认 result 时各自动拍一张该 launch 绑定的 RELAY 窗口截图，宿主写哈希与 `evidence_refs` 进事件账，agent 不能自填。不再有 `relay approve` 命令、不再要求飞书。
7. **收口执行 = 收口 agent 前台 + finalizer 脚本后台，且 finalizer 只由宿主启动**（用户拍板 D2 折中；xhigh 审 S3）：收口 agent 备料、写交付汇报、在窗口拿到确认后只提交 `succeeded + next_action=finalize` 的 final result（含确认记录字段）；Runner 校验三护栏与 manifest 后**自己签发 finalizer 的 script receipt 并由宿主执行**，agent 不得直接调用 finalizer 脚本、不持任何 Git 收口权限。finalizer 按 closure journal 逐步幂等执行——**全部在从 master HEAD 新建的暂存分支上完成，最后一次 CAS 快进**（用户拍板 D6）：审计（预期 master HEAD、index 干净、路径所有权无重叠）→ squash → 合入态复验（机器闸复跑）→ 准备并校验销户回填 → 原子 verify 提交（含回填）→ 回读一致性 → master 仍为预期 HEAD 才快进 → 清树与暂存分支。崩溃从最后完成步续跑，master 永无"已合入未 verify"中间态；失败进 `closeout_blocked` 由诊断 agent 带你处置。永不 deploy；finalizer 永不 push（test 受控 push 只属 7a 集成棒，见决策 13）。
8. **三路复核显式成棒、代码复核顺序两轮**（用户拍板 D3 + 对话补充）：代码复核 R1 → fresh R2（读已落账 R1）→ fresh 汇合；需求与一致性复核 agent（fresh）；教训复核 agent；三者账号可同、会话必不同、启动命令不得含 resume/continue。返工复审 = 同节点新 attempt；复核工件按 attempt 不可变 `review-<node>-<attempt>.md`，canonical `review.md` 只由汇合/收口 agent 以 expected-hash CAS 写。
9. **计划只引用注册表条目**（审核 S2）：RelayPlan 只写 `profile_id / script_spec_id / 开关`；命令模板（含冻结 hash）、允许参数白名单、工作目录根、账号配置目录路径、fallback 关系、push 目标白名单放**用户级**注册表 `~/.dh-relay/registry/*.psd1`（只含路径/别名、零凭据）；**P2 不支持仓级覆盖**（用户拍板 D9，防仓内文件被改后接管账号映射；留 P3）；计划内不得出现任意 CLI 参数。
10. **relay 在业务仓只有两个「自有内容根」，各模块下零 relay 自建文件夹**（用户拍板 D21；起因：业务仓常有 20+ 模块，若每模块各建 relay 文件夹则记录四散，而**一个 run 的卡本就可能跨多个模块**，挂任何单一模块都不对）。**措辞精确化（R10 审 U1）**：这里说的是"relay **自有**内容根"，**不等于** relay 只写两条路径——完整副作用面见 §3「运行现场、tracked 写入与所有权」的副作用表（任务树、Git objects/refs/worktrees、用户级索引与注册表、CLI 配置与会话文件、psmux 进程等均在业务仓内容之外另行登记）。
    - **运行现场** `<repo>/.dh-relay/<run_id>/`——**不进 git**，装事件账、派工单、截图、closeout journal、授权 request/snapshot、宿主 PID/lease、锁。**换根的理由（R10 审 U2 更正口径）**：P1 正式契约 `design/01` 已把 `.dh-runtime/<dh-relay 命名空间>` 定义为 dh-relay 的**独立命名空间**，并非读写 dh-crew active state，故 **P1 当时并不违规**；P2 换根的正当理由是"由 legacy 共享 runtime 根迁到 relay 独占根，取得更强的物理隔离"，旧证据与旧表述不因此改判。仍经 canonical runtime resolver 解析、不写死路径。
    - **`.gitignore` 前置条件（R10 审 S6，实测 `.gitignore` 当前只忽略 `.dh-runtime/`、`.dh-relay/x` 为 NOT_IGNORED）**：仓根 `.gitignore` 必须含**锚定**的 `/.dh-relay/`；缺失 → `start_rejected`，**relay 不得自行修改业务仓 `.gitignore`**（那属仓接入变更，须另行授权）。
    - **legacy 根的双根发现（R10 审 S7，实测旧根 `.dh-runtime/relay/` 下现存 5 个目录、其中 3 个为可读 `relay/v1` run）**：resolver 走**双根**——v2 **只写** `.dh-relay/`；旧根**只供显式 discovery / status / 归档读取**，`resume` 硬拒（承接 D13"v1 存量 run 只读不续跑"），**绝不新写**。否则只认新根的控制台会看不见存量，与"v1 可读/可归档"的承诺不闭合。
    - **接力留档** `<repo>/docs/relay/`——**进 git**，全仓唯一：`README.md` 是**总目录**（每个 run 一行：run_id、**中文主题**、**开始时间与结束时间**、涉及哪些模块与哪些卡、终态、指针）。**部分取消的诚实口径（用户拍板 D22）**：终态为 `done-partial-cancelled` 时，该行必须写"部分完成"、**逐张列出被取消的卡**并标注其 `cancelled-exempt`（未跑教训复核），**绝不得表述为"全部销户"**；被取消的卡不得从总目录里隐去（隐去会让日后回看不知当时放弃过什么）。`runs/<run_id>/` 装该 run 的接力记录（棒表实际执行、每卡交棒时间线、诊断记录、对照表、宿主外 oracle 报告、精选截图）。
    - **卡级 dev-harness 工件位置不变**：8 件套、`review*.md`、`progress.md`、`findings.md`、knowledge 候选仍落 `<repo>/docs/modules/<该卡的模块>/workspace/<卡>/` 等 dev-harness 既定位置——它们是**那张卡本来就该有的工件**，relay 只是代笔；挪走等于 relay 自造第二套流程且 `dh` 体检会红（与决策 2 冲突）。relay **不在任何模块目录下新建自己的文件夹**，只在该卡 `progress.md` 留**一行指针**指向 `docs/relay/runs/<run_id>/`（该指针**不算第二份留档**）。
    - **run 级归档由独立 archive 节点承担**（R10 审 S1/S3 重写）：run 级留档跨卡，而 tracked 写入规矩是"落各卡任务树、随该卡 squash 合入"——多卡多树无法共写；且 finalizer 是"快进后即删暂存分支"，在它之后无分支可写。故设 **run 级 archive script 节点**：Runner 签发 receipt、宿主执行，**从当时 master HEAD 新建自己的独立暂存分支**，持**同仓 finalizer 锁**与**仓级 archive lease**；写 `runs/<run_id>/` 并对 `README.md` 按 `run_id` 做**幂等 upsert**（**不是盲目 append**；CAS 漂移则重取基线重算）；产出 `archive-manifest`（所有者 `run:<run_id>`，与卡级 release manifest 的精确路径集不重叠，故不撞决策 7 的路径所有权审计与 B12 重叠检查）。**归档 CAS 与 B10 oracle 均通过后，run 才进入 `done`/`cancelled`；失败进 `archive_blocked`，不得报 done。**
    - **取消与豁免（R10 审 S2）**：**所有 settled 终态**（`done-complete` / `done-partial-cancelled` / `cancelled`）都自动归档，不依赖用户手动补导出。进入 closeout 的卡须满足 B18（lesson_review 已终结）；**在 closeout 之前被取消的卡以 `cancelled-exempt` 记账**，不要求其补跑教训棒。
    - 用户级 `~/.dh-relay/runs.json` 只做**跨仓索引**（repo 路径 + run_id + 摘要 + **最后更新时间** + 终态，不含 config_dir 等路径），写入用临时文件 + 原子改名 + 文件锁。
    - **`run_id` 命名与语义（用户拍板 D23；此前设计只写"含仓 slug 前缀"，格式实为空白）**：格式 **`R<nnn>-<主题slug>-<yyyyMMdd>`**，例 `R007-kpi-alignment-20260818`。三段各司其职：
      - **`R<nnn>` = 仓级全局递增序号，唯一性的唯一来源**（每仓各自从 `R001` 起）。发号**搭在建 run 时已有的仓级锁上**（与 claim lease / archive lease 同一把），不新增机制；序号从 `runs.json` 该仓分段的最大值 +1，锁内读写。
      - **`<主题slug>` = 这趟接力的主题概要**（用户拍板 D24：**由主会话据卡的目标拟定**，用户不必自己起名；宿主建 run 前在窗口展示精确卡清单请用户核对时（D19 那一步）**一并展示主题**，用户可当场纠正）。**字符集冻结为 ASCII 小写字母/数字/短横线、长度 ≤30、首尾非短横线**——因为它进文件夹名、psmux 窗口标题与事件账主键，而本仓已有中文路径踩坑先例（见 `AGENTS`/教训库：中文路径下 `git status` 会报错误结果）；**中文标题不进 run_id**，只写进 `docs/relay/README.md` 的「主题」列与 run 的 `概览.md`。非法字符或超长 → 规范化失败即 `start_rejected`，不静默截断。
      - **`<yyyyMMdd>` = 创建日期（本地时间），只为人读**；唯一性不依赖它，故**不需要时分秒**。
      - **整个 `run_id` 是「创建时刻签发、此后永不改写」的主键**——它贯穿现场目录名、`events.jsonl` 每条事件、`authority-snapshot`、launch receipt、attempt result、closeout journal、`runs.json` 索引、`docs/relay/runs/<run_id>/` 留档与 psmux 窗口标题；跟随"最后更新"改名即须重命名目录、已写入的全部事件 run_id 失配、运行中窗口标题失效、宿主 lease 找不到自身，**整条身份链断裂**，与 P1「身份签发后不可改」冲突。
      - **不采用当日序号 `-01/-02`**（须数当日已有 run，语义弱且仍要锁）；**不再沿用 P1 的卡名前缀** `RELAY-IHSR05-…`（P1 一 run 一卡故可用卡名，P2 一 run 带 2～3 张卡，取任一卡名都不当）；**不再要求仓 slug 前缀**——`runs.json` 以 **(repo 路径, run_id) 复合键**索引，跨仓天然不撞，序号因此也回归"每个仓各自从 R001 数"的直觉。
      - **「最后动过」不进 run_id**，另由三处承载：`events.jsonl` 末条时间戳（权威）、`runs.json` 的最后更新时间（索引）、`docs/relay/README.md` 的结束时间列（归档时定稿）。**关联方式**=三处同名同键按字符串全等对应（`.dh-relay/<run_id>/` 现场 ↔ `docs/relay/runs/<run_id>/` 留档 ↔ `runs.json` 索引行），不靠时间推断。
11. **一 run 一批卡（2～3 张试验）与基线刷新**（用户拍板 D8、D11、D12）：卡间按 DevPlan 依赖串行/并行；接力计划生成时按 DevPlan 变更范围做**静态路径重叠检查，重叠卡强制串行**；每卡在棒 1 开工时刻从当时 master HEAD 建树；**同一仓同一时刻只允许一个 finalizer 在跑**。**基线落后**（前卡已快进）：finalizer 在暂存分支先 rebase 候选到新 master → 生成新 candidate generation（新 `base_sha + candidate_tree_sha + diff_hash`）→ 若 **patch 哈希不变**且机器闸复跑通过 → 旧复核工件仍有效，但旧 manifest/确认失效，**重新弹一次收口窗口重新确认**（新 manifest）；若 **patch 变了**（冲突解决或内容漂移）→ 自动重跑受影响证据（机器闸 → R1′→R2′→lead′，开关的 e2e）后再重新确认；只有 master 未漂移才是"一次确认"。rebase 冲突 → `closeout_blocked` → 诊断 agent 可拉修复 agent 解冲突后重走。**多卡下的重编排**（xhigh 审 S2，用户 2026-08-16 定）：保持 run 全局 generation（P1 身份链不改）；某卡 `dependency_blocked` 触发的重编排只允许在**该卡内**新增前置棒（不改其它卡、不加卡）；重编排 agent 计算期间其它卡照常；新计划生效那一刻 Runner 判影响面——**不受影响的卡**（节点未变、不依赖该卡）手上在跑的棒先交完再切换届次（不重做、不丢），切换后下一棒 fresh 继续；**受影响的卡**（依赖该卡）干完手上一棒后按 P1 依赖冻结等待。卡级 generation 留 P3。多卡 DAG 通用调度器、跨仓 run 留 P3。
12. **诊断 agent 自动拉起、代发控制事件属 P2 对 P1 §2.2/§4.4 的明确放宽**：任一棒 `interrupted_unknown`/paused/`closeout_blocked`/quota 无 fallback/续跑发现断棒 → Runner 拉一次性只读诊断 agent；它读现场、写诊断事件，在窗口问你"续跑 / 换账号重试 / 重来 / 放弃 / 丢弃暂存分支"，按你的回答代发**预定义子类型**控制事件（`actor=diagnoser:<node_id>`、`on_behalf_of=user`、`user_reply_verbatim`、绑定 launch/session）；Runner 消费控制事件改状态，诊断 agent 本身仍不直接写 active state、不施工。此代发同属弱信任，与确认闸一并登记。
13. **test 分支受控 push = 跟着项目走的条件分支**（用户拍板 D10；2026-08-16 用户澄清"push 远端这个动作是跟着项目的，项目有就做没有就不做"；xhigh 审 S8 收紧）。**两类前提必须分开记账（R9 审 S5：否则漏配授权/ACL 会被记成 N/A，等于给权限红线开新口子）**：
    - **① 项目适用性谓词（只有它不成立才可记「前提不成立·本次不适用」）**：授权冻结时刻，目标仓**确无可用远端**（`git remote -v` 为空或无 eligible remote）。此时该棒退化为"合到本地 `test` 分支 + 窗口协作"，B16 的 push 分支与 H5 按 **N/A** 登记——N/A 不进本次放行证据包、不算未验证、更不算风险放行。**实测 `dh-crew` 与 `infohub-vps-services` 当前 `git remote -v` 均为空。**
    - **② 安全前置条件（不成立一律记「未授权 / 未验证」并禁止 push，绝不可记 N/A）**：仓**有**远端但缺注册表白名单、缺 `integration=true`、缺 run 级 `-AllowTestPush`、缺 ACL 机器证据或缺 H5。此时 push 被拒、该项进未验证账，按红线处理（`AGENTS`/dev-harness G12 权限安全不可豁免），不得以"没配所以不适用"的说法绕过。
    - **表述红线**：B16 整项写"通过"必须以真实 Git 服务上跑通为前提；只在本地/裸远端验过机械链时，远端 provider 能力只能写"未在真实 Git 服务上验证"。
    - **P2 实际收口口径（用户拍板 D20）**：P2 **不专门搭真实测试 Git 服务**；B16 只在本地临时仓 + 裸远端上把机械链（白名单硬拒、指纹冻结、lease 冲突、force-with-lease 回滚、cancel-after-push 补偿、串行锁）验到位，**"真实 Git 服务的 ACL 能否锁死到 `refs/heads/test`"诚实标为未验证**，H5 在无真实远端时记 N/A；交付表述不得写"B16 通过"，只能写"机械链已证、真实服务器能力未验证"。将来接入带远端的项目时按该未验证项补证（不另立 A，属同一验收 ID 的证据补齐）。以下为适用时的约束——仅在上述谓词全成立时，7a 集成脚本节点可把候选合入并推到远端 **`test` 一个分支**——注册表 push 白名单冻结 `remote URL 指纹 + refs/heads/test`，脚本硬拒其它 remote/ref（master/main 永不）；**权限边界在远端**：目标 Git 服务侧须把 relay 使用的凭据 ACL 限制到该仓 `refs/heads/test`（不可由 relay 自证；`DHR-A-04` 后按验收二分原子拆分：机器项=B16 检查器校验脱敏 ACL 回读工件的存在/格式/覆盖范围/remote 指纹一致，人判项=H5 由你判断该回读的来源与时效；工件缺失即未验证、权限属红线不得风险放行）；推前 journal 冻结 remote URL 指纹、远端 test 原 HEAD、source SHA、完整 refspec；e2e 失败或用户在窗口说"回滚"时以 `--force-with-lease=test:<原HEAD>` 精确回退；lease 冲突（外部人员改过 test）或补偿失败 → `closeout_blocked`，不得宣称已回滚/run done；`cancel_card` 发生在 push 之后必须先走同一补偿路径；同仓 test 推送持宿主锁串行；部署到测试服务器仍由用户在窗口协作中亲手执行（agent 只给命令）。
14. **不改 dh-crew**：新代码落 `tools/`；dh-crew 的 dispatch/loop/notify 不动、不被 relay 读状态；飞书通知 adapter、登录自启降 P3（用户拍板 D9）。

### 1.4 范围与分期

**P2（本设计；用户拍板 D4 分卡逐层冻结，预计 14～16 张标准卡，具体由 B 拆）**：

1. v2 契约与兼容：新增角色、`script` 节点、`gate_outcome`、`verdict`、`rework`（re-ready 新 attempt）、profile/script 引用、按 attempt 不可变复核工件、release manifest、控制事件子类型（resume/retry/reset/cancel/abort）、run 生命周期、多卡 run 结构；P1 业务状态机与合法边不变，旧 run 可读。
2. 宿主基础债：RB-1 截图/通知非致命、F-019 决策态快照、F-023 三条（consumed 轮转 / spawner 异步 / Runner 提交临时名）。
3. 任意仓发起 + 用户级注册表 + run 索引 + `relay` 控制台（start/resume/status/propose/cancel）+ 现场经 resolver 归目标仓；codex-ninth 实棒（非秘密身份回执 + expected_identity 比对）。
4. 脚本节点（机器闸、e2e/集成脚本、finalizer 承载）。
5. 三路复核 + 汇合 + 有界返工/补证回路 + 集成/端到端可选棒（多轮协作 + test 受控 push/回滚）。
6. 收口 agent + finalizer 脚本（暂存分支一次快进 + 基线落后 rebase 重过闸）+ 宿主持有的确认闸护栏 + release manifest。
7. 诊断 agent 自动拉起 + 接续（先摄入未消费结果、再判断棒、控制台续跑）+ quota 高置信识别/预登记 fallback。
8. 多卡调度（依赖/静态重叠检查/finalizer 与 test 推送串行锁）。
9. 真实自举（一批 2～3 张与 relay 实现无关的真实卡）+ H3/H4/H5。
10. **授权工件与授权边界**（`DHR-A-04`，见 B14）：主会话落不可变 authority snapshot（含 DevPlan/design 的 Git blob 哈希）；relay 只消费它；不在 snapshot 内的卡、状态不可开工的卡一律拒绝；接力计划生成 agent 只按模板填槽位，Runner 晋级前以 snapshot 语义等价复核，不得改卡的目标/验收/变更范围。
11. **棒完整性**（`DHR-A-04`，见 B17 与 B18）：落户棒真产出 8 件套且逐文件非占位（自带谓词，不挂靠 `dh` R15）；教训复核棒真被调度并区分成功/无候选/失败三终态，收口棒不得先于它 ready。
12. **relay 在业务仓的落点纪律**（`DHR-A-05`/D21，见 B9）：业务仓内 relay 只有 `.dh-relay/`（现场，不进 git）与 `docs/relay/`（留档，进 git，README 为总目录）两个落点；各模块目录下零 relay 自建文件夹；新增 run 收尾脚本节点单写 run 级留档。
13. **诊断 agent 的可信只读承载**（`DHR-A-04`，见 B15）：注册表 profile 增能力位，宿主只以强制只读方式拉起诊断 agent；不支持强制只读的 CLI/profile 拒绝用于该角色。

（用户 D4 拍板"分 7 卡"时范围尚不含多卡/集成/诊断；v5 范围扩大后预计 8～10 张。**`DHR-A-04` 修订后**：机器验收项由 13 条增至 **18 条**（新增 B14/B15/B16/B17/B18，其中 B15/B16 由 B7/B13 原子化拆出），人判项增至 3 条（H3/H4/H5），故估算上调为 **14～16 张**——**这是估算不是闸**，最终以 B 拆的覆盖/颗粒度审计为准；硬约束只有一条：**每张卡只承载一个安全边界**。若 B 拆结果显著超出该区间且无法用颗粒度理由解释，才回 A 调整范围；任何情况下不得静默压缩验收。）

**P2 内降级实现**：quota 只做"高置信识别则换预登记 fallback，否则 paused→诊断"；接力计划生成 agent 只做模板实例化。

**不做（留 P3+）**：完整 quota 识别/恢复通道、计划内任意 CLI 参数、通用动态 planner、通用多卡 DAG 调度器与跨仓 run、仓级注册表覆盖、登录自启、飞书通知 adapter、原施工会话恢复返工、非 psmux 后端、跨机器、UI、自动决策、dh-crew 侧改窗口拥有会话（F-021）。

### 1.5 核心决策清单

| 决策点 | 选了什么 | 备选与取舍 | 不可逆程度 |
|---|---|---|---|
| relay 边界 | 只接已授权任务卡 | 把 A/B 也接进去 → 与 dev-harness 人闸冲突 | 高 |
| 流程来源 | 直接映射 dev-harness 节点表 | 自定义精简流程 → 与收口硬规则漂移 | 高 |
| 汇合判定归属 | 汇合 agent 写 verdict，Runner 只按枚举分支 | Runner 解析 review 判 P0/P1 → 违反"Runner 不判质量" | 高 |
| 机器闸形态 | 脚本节点 + `gate_outcome`，不加新 result_status | 新增 `failed` → 改共享状态机 | 高 |
| 返工会话 | fresh fix worker + handoff | 原会话恢复省上下文，但 psmux 无可靠 suspend/resume | 中 |
| 确认闸载体 | 全部在 agent 窗口作答（弱信任 + 三护栏） | 一次性回执 CLI（审核推荐）→ 更强身份证明，但用户不要命令行/飞书 | 高（用户拍板） |
| 收口执行者 | 收口 agent 前台 + finalizer 脚本后台 | agent 自己做 git → 幂等/防篡改难证；纯脚本无前台 → 用户没有对话口 | 高 |
| 合入方式 | 暂存分支做完后一次 CAS 快进 master | 直接上 master → 崩溃后中间态 | 高 |
| 复核形态 | 代码 R1→fresh R2→汇合 + 需求复核 + 教训复核，各自成棒 | 并行两 reviewer → 不满足交叉评估；把需求/教训塞进收口棒 → 不可见、不可单独验收 | 高 |
| 执行 profile/脚本 | 计划只引用用户级注册表 id | 计划内写命令/账号目录 → 注入、越权、暴露面 | 中 |
| 运行现场归属 | 目标仓 `.dh-relay/` + 用户级跨仓索引（D21 换根，与 dh-crew 的 `.dh-runtime/` 平级不嵌套） | 全落 dh-crew → 跨仓混、IHSR_05 已踩；挂 `.dh-runtime/relay/` → 蹭 dh-crew 现场根，与决策 14 矛盾 | 中 |
| relay 在业务仓的落点（D21） | 只两处：`.dh-relay/`（现场，不进 git）+ `docs/relay/`（留档，进 git，README 总目录）；**各模块下零 relay 文件夹** | 每模块各建 relay 文件夹 → 20+ 模块的仓里记录四散，且跨模块的 run 挂哪个模块都不对 | 中（用户拍板） |
| run 级留档的写者 | 新增 run 收尾脚本节点，run 终态时在最后一次暂存分支上单写并追加 README 行 | 各卡 finalizer 各写一段 → 三棵任务树并发写同一跨卡文件，必冲突 | 中 |
| run 粒度 | 一 run 一批 2～3 卡（试验），同仓 finalizer 串行 | 一 run 一卡 → 少一层调度但无法验证多卡；通用 DAG → 超 P2 | 中 |
| 异常处置 | 诊断 agent 自动拉起、窗口问答代发控制事件 | 用户手动触发 + CLI → 用户不要命令行 | 中 |
| 额度耗尽 | 高置信识别才换预登记 fallback | 宽松识别自动换号 → 误判 | 中 |
| 多卡基线 | 重叠卡强制串行；落后候选在暂存分支自动 rebase 复跑机器闸再快进；冲突转 closeout_blocked | 任何落后都停等人 → 串行多卡几乎每卡停一次 | 中 |
| test 分支 push | 允许 relay 推远端 `test` 一个分支（run 级 `-AllowTestPush` + 白名单 + 记原 HEAD + force-with-lease 回滚）；master 永不 | 只动本地 test、push 由用户亲手 → 更保守但多一次人工 | 高（P1 "永不 push" 的唯一受控例外） |
| 通知/自启/仓级覆盖 | 全部降 P3 | 保留在 P2 → 卡数 10～12 | 低 |
| 契约版本 | `relay/v2` 双解析器；只承诺 v1 枚举/合法边/只读解析不变；v1 存量 run 只读不续跑 | 笼统"只扩不改" → 不可证且 v1 解析器会被新字段打破 | 中 |
| 多卡重编排 | run 全局 generation 不变；生效时不受影响卡交完在跑棒再切、受影响卡依赖冻结 | 卡级 generation → 改 P1 身份链（P3） | 中 |
| rebase 后处置 | patch 不变→只重确认；patch 变→重跑机器闸与两轮复核后重确认 | 任何 rebase 全套重跑 → 成本高；不重确认 → 确认绑旧候选 | 高 |
| finalizer 触发权 | Runner 校验护栏后签发 finalizer receipt；agent 不可调用 | closer 直接调脚本 → agent 持 Git 收口权限 | 高 |
| 确认闸时序 | 宿主 decision_ack（精确单窗口截图）后 agent 才可展示确认；result 摄入再拍一次 | 只核文件顺序 → 截图可能在回答后同刻拍成 | 高 |
| tracked 写入 | 全部落任务树、主工作树只读、一仓同卡一个 active run | 棒直接写主树 → 弄脏 master/并发追加 | 高 |
| 怎么开跑（D14） | 你在对话里授权，主会话落授权工件并代起；命令行只作调试通道 | 你亲手敲 `relay start -Task …` → 你明确表示不想敲命令行，且"授权"只剩命令行参数、不可审计 | 高（用户拍板） |
| 授权真源 | 主会话落的不可变 authority snapshot（含 DevPlan/design 的 Git blob 哈希），Runner 以它复核 planner 输出（B14） | 信任 planner 只填槽位 → 越过 A/B 人闸无机器闸拦；只认命令行参数 → 授权后 DevPlan 被改也发现不了（TOCTOU） | 高 |
| relay 进程存活（D18） | 独立 detached 宿主，主会话结束/终端全关仍跑；配 PID/lease、单宿主、`relay stop`、过期接管 | 沿用现役"调用方进程内有界循环" → 我会话一关它就停，"无常驻主控"名存实亡 | 高（用户拍板） |
| 授权清单的可信度（D19） | 弱信任 + 宿主建 run 前窗口展示精确卡清单请用户核对 | 直接信 AI 转录 → 授权全链无任何机器闸；引入平台级授权令牌 → 现无此能力 | 中（用户拍板） |
| B16 的 P2 收口口径（D20） | 只在本地临时仓+裸远端验机械链，真实 Git 服务 ACL 标未验证 | 专搭真实测试仓 → 多一份环境与真实远端写入面；整块降 P3 → 将来要回 A | 中（用户拍板） |
| 授权失配的影响面（D17） | 分两类：snapshot 工件被篡改=全 run 停；run 中途 DevPlan 正当变更=只冻那张卡 + 诊断 agent 问你（重新授权/回滚/取消），其它卡照常 | 一律全 run 停 → 一张卡改口径就拖停整批；一律放行 → 授权工件失去意义，交付物可能不符新口径 | 中（用户拍板） |
| 远端 push 的定位 | **项目属性驱动的条件分支**：仓有远端就做、没有就退化为本地合并 + 窗口协作 | 当成 P2 必做能力 → 两个现有仓都没远端，写了也验不了；当成 P3 砍掉 → 将来有远端的项目又要回 A | 中（用户拍板） |
| 集成端到端棒的定位 | **人验/帮 AI 取证的动作**，agent 只组织证据、不判通过 | 当成普通机器闸 → 把需要人判的东西伪装成自动结论 | 高（用户拍板） |
| 诊断 agent 只读的落实 | 注册表能力位 + 宿主按该能力位拉起，不支持就拒绝拉起（B15） | 靠 prompt 劝阻 → 现役是 `--yolo`/`skip-permissions`，写不出机器反例 | 中 |
| 弱信任项的验收粒度 | 每个弱信任/受控例外独占一条验收 ID（B15 诊断代发、B16 远端 push），不与相邻能力合并 | 打包进 B7/B13 → 一张卡同时审两个权限主体，"一卡一安全边界"落不了地 | 中 |
| 外部权限证据 | 原子拆成机器项（B16 检查器验工件）+ 人判项（H5 判来源与时效） | 只写"用户确认" → 口头判断顶替权限硬证据 | 高 |

## 2. 全局地图与角色边界

### 2.1 每张卡的接力（棒表，含文件路径）

约定：在业务仓 `<repo>` 由主会话按授权工件代起 relay（D14）；`<run>` = `<repo>/.dh-relay/<run_id>/`（不进 git；D21 换根，不再挂 dh-crew 的 `.dh-runtime/`）；`<留档>` = `<repo>/docs/relay/runs/<run_id>/`（进 git，全仓唯一，跨卡共用）；`<ws>` = `<repo>/docs/modules/<模块>/workspace/<卡>/`（进 git）；任务树 `D:\wt\<卡>`；node_id = `<卡>-<角色>`。

| 棒 | 谁跑 | 读什么 | 写什么 | 交给谁 / 你要做的 |
|---|---|---|---|---|
| 0 | 接力计划生成 agent（一次性，一 run 一次） | **`<run>/authority-snapshot.json`（唯一授权真源）** + 其冻结引用的 DevPlan `<repo>/docs/modules/<模块>/dev_plan/P*.md` 与 `design/README.md` 白名单输入；模板 `tools/templates/standard-pipeline.json`；注册表 `~/.dh-relay/registry/` | `<run>/inbox/proposal-v1.json` → Runner 晋级 `<run>/plans/relay-plan.v1.proposal.json` + `active-plan.json` + `authority.json` | Runner 按依赖拉第一批卡的棒 1 / 你：无 |
| 1 | workspace agent | 自己的派工单 `<run>/launches/<launch_id>.json`、DevPlan 该卡、design | `D:\wt\<卡>`（dh wt new）；`<ws>/` 8 件套（含 worker 粒度 task_plan）；`<run>/attempts/<卡>-workspace/1/checkpoint.json`（decision_required 落户确认）→ `result.json` + `handoff.md` | 施工 / 你：窗口答"确认" |
| 2 | 施工 agent（cwd=`D:\wt\<卡>`） | `<ws>/brief.md`、`task_plan.md`、上一棒 handoff | 代码改动；`<ws>/progress.md`；`<run>/attempts/<卡>-build/<n>/result.json` + `handoff.md` | 机器闸 / 无 |
| 3 | 机器闸（脚本节点，宿主执行） | 注册表 script_spec（`dh <模块>` + 该卡测试命令）；cwd=`D:\wt\<卡>` | `<run>/attempts/<卡>-gate/<n>/result.json`（gate_outcome、exit、脱敏尾部）；证据行追加 `<ws>/progress.md` | pass→R1；fail→修复 agent（fresh，`<卡>-fix/<n>`）→回 3（连续 ≥3 次 fail 转 decision）/ 无 |
| 4 | 代码复核 R1（fresh，只读） | `D:\wt\<卡>` diff、`<ws>/*`、handoff | `<ws>/review-R1-<attempt>.md`（不可变）；`<run>/attempts/<卡>-R1/<n>/result.json` | R2 / 无 |
| 5 | 代码复核 R2（fresh，只读，可读 R1） | 同上 + `<ws>/review-R1-*.md` | `<ws>/review-R2-<attempt>.md`；`…/<卡>-R2/<n>/result.json` | 汇合 / 无 |
| 6 | 汇合 agent（fresh） | 两份复核工件 | `<ws>/review.md` 代码复核段（CAS）+ `findings.md` 归档；`…/<卡>-lead/<n>/result.json`（verdict） | approve→7a/7b；rework→修复 agent 回 3（≤3 轮，R1'/R2'/lead' 为新 attempt）；escalate→窗口问你 / 偶尔答 |
| 7a | 集成与端到端 agent（**可选棒**，模板开关 `integration=true`）。**本质=人验/帮 AI 取证的动作**（用户原话），agent 只组织命令与你的输出成证据账本条目、不判通过。**每个需人判的 E-ID 必须留你的原文 verdict**（在窗口里说"这条通过/不通过/还要再看"，agent 原文抄录进账本，不得改写、不得代判；R9 审 S6）；棒 7b 只审"覆盖与来源"，最终业务人判结论进 E10/E11 放行证据包由你在收口确认时一并看到——**不存在"有人给证据、无人有权判结果"的夹缝**；**推远端 `test` 是条件分支**——仓有远端 ∧ 白名单 ∧ `-AllowTestPush` ∧ ACL 证据与 H5 均成立才推，否则只做本地合并与协作 | `<ws>/brief.md` 人验/需求项、仓库 test 分支约定、集成/e2e script_spec | 调集成脚本节点：记录本地与远端 `test` 原 HEAD → 把候选合到本地 `test` → 若授权则 push（白名单仅 `test`，宿主锁串行）→ 跑 e2e 脚本节点；需要真机时先写 `decision_required` checkpoint，再在窗口逐条给你命令（含部署到测试服务器的命令）、你亲手执行回贴输出，多轮直到过；失败或你说"回滚"→ 脚本以 `--force-with-lease=test:<原HEAD>` 回退远端、本地 reset 回原 HEAD；过程与结果写 `<ws>/progress.md` 证据账本（需求境证据 E-ID）；`…/<卡>-integration/<n>/result.json` | 需求复核 / 你：窗口协作输入 |
| 7b | 需求与一致性复核 agent（fresh） | `<ws>/brief.md` 验收口径、`progress.md` 证据账本、`review.md`、DevPlan 该卡、gate/e2e 结果 | `<ws>/review.md` 需求复核段（E4：每条需求/人验项 → 场景路径 → 证据 ID → 结论）+ 一致性表（brief ↔ DevPlan 验收口径 ↔ 证据；verify 文案一致性由棒 8 收口 agent 自查、finalizer 按 manifest 校验）；`…/<卡>-requirement_review/<n>/result.json`（verdict：pass / needs_evidence / escalate） | pass→7c；needs_evidence→回指定目标棒（施工或集成）新 attempt，只重跑该棒及其下游机器闸；若目标棒改了代码则按 rework 全套（R1'→R2'→lead）；≤2 轮，超轮 escalate 窗口问你 / 无 |
| 7c | 教训复核 agent（不阻断） | `<ws>/findings.md`、`lesson_candidates.md`、`progress.md`；`dh mine` | `<repo>/docs/modules/<模块>/knowledge/教训库-候选.md` 追加候选（待人裁决，不入正册）；`<ws>/review.md` 教训复核段；`…/<卡>-lesson_review/<n>/result.json`（无 verdict，成功即 succeeded） | 收口 / 无 |
| 8 | 收口 agent | `<ws>/*`、gate/e2e 结果、三路复核段 | E5～E10 备料：`dh gate`、as-built、交付汇报与证据展示区写 `<ws>/review.md`；`<run>/attempts/<卡>-closer/<n>/release-manifest.json` + `verify-message.txt`；写 `decision_required`「已查看证据，认可执行本地收口？」→ **等宿主 ACK**（Runner 已接受该 checkpoint 且完成同 launch 精确截图）后才向你展示确认问题 → 你答固定短语 → 回答原文写 review.md 确认记录 → 提交 `succeeded + next_action=finalize` 的 `result.json`（不调用任何脚本） | Runner 校验护栏与 manifest 后签发 finalizer receipt / 你：窗口答「认可执行本地收口」 |
| 8' | finalizer（脚本节点，**Runner 签发 receipt、宿主执行**，agent 不可触发） | manifest、`D:\wt\<卡>`、预期 master HEAD | 暂存分支 `relay/closeout/<卡>`：审计→squash→（基线落后则 rebase→新 candidate generation→按决策 11 决定重确认或重跑）→复验→回填 DevPlan 行/`<ws>`→verify 提交→回读→快进 `master`→删 `D:\wt\<卡>` 与暂存分支；日志 `<run>/closeout/<卡>/journal.json`；`…/<卡>-finalizer/<n>/result.json` | Runner：有下一张卡→拉其棒 1；全部完成→run done / 无 |
| 全程 | Runner/宿主（确定性程序） | — | `<run>/events.jsonl`、`relay-state.json`、`launches/`、`psmux-handles/`、`shots/`、`requests/`、宿主 PID/lease（**均在 `<repo>/.dh-relay/`，不进 git**）；跨仓索引 `~/.dh-relay/runs.json` | — |
| run 尾 | **run 级 archive 脚本节点**（script 节点，Runner 签发 receipt、宿主执行，一 run 一次；`DHR-A-05`/D21，R10 审 S1/S3） | `<run>/` 全部现场 + 各卡 finalizer journal + 各卡 release manifest | 从当时 master HEAD 新建**自己的独立暂存分支**（持同仓 finalizer 锁 + 仓级 archive lease）→ 写 `<留档>` = `<repo>/docs/relay/runs/<run_id>/`（棒表实际执行、每卡交棒时间线、诊断记录、对照表、oracle 报告、精选截图）→ 对 `<repo>/docs/relay/README.md` 按 `run_id` **幂等 upsert**（CAS 漂移则重取基线重算）→ 产出 `archive-manifest`（owner=`run:<run_id>`）→ 一次 CAS 快进 → 删该暂存分支 | **归档 CAS 与 B10 oracle 均过才进 `done`/`cancelled`；失败进 `archive_blocked`，不得报 done** / 无 |

### 2.2 非业务角色

| 角色 | 是什么 | 出现时机 | 你要做的 |
|---|---|---|---|
| Runner / 宿主 | 确定性程序（非 AI）：身份链、CAS、依赖、返工/补证轮次上限、脚本节点执行、多卡调度、finalizer 串行锁、quota 识别、run 索引；每 tick 一行状态。**进程归属与存活语义见 §2.4**（现役 `Invoke-RelayHost` 是调用方进程内的有界 tick 循环，无 detached/service 形态） | 宿主启动到 run 结束或宿主退出 | 无 |
| 重编排 agent | 一次性 AI：判断 `dependency_blocked` 是否成立、写 v2 proposal（P1 已有） | 某棒缺前置时 | 涉及方案/删改时转 decision 在窗口问你 |
| 诊断 agent | 一次性只读 AI：读现场、写诊断事件、在窗口问你并代发控制事件（resume / retry-with-profile / reset-attempt / cancel / abort-closeout） | `interrupted_unknown`/paused/`closeout_blocked`/quota 无 fallback/续跑发现断棒 | 窗口答一句 |
| 机器闸 / e2e / finalizer 脚本 | 宿主调用的确定性脚本（script 节点） | 计划指定处 | 无 |
| 用户 | 授权开工（**对话，D14**）、必要时双击快捷方式开 `relay` 控制台选 run、窗口作答（落户/决策/协作/收口/诊断） | — | 不巡检、不搬运交接、**不敲任何命令行** |

### 2.3 多卡调度

- 接力计划把每张卡实例化为一组节点（1～8'），卡间边来自 DevPlan 依赖列；无依赖卡可并行（受注册表 `max_parallel_cards`，默认 2）。
- 同一仓同一时刻只允许一个 finalizer 节点 running（宿主级锁）；后一张卡的 finalizer 以前一张快进后的 master HEAD 为预期 HEAD 重新审计。
- 任一卡 escalate/paused 只冻结依赖它的卡；其它卡照常。
- 某卡重编排（仅限本卡内加前置棒）：计算期间其它卡照常；生效时不受影响卡的在跑棒交完再切届次、下一棒 fresh 继续，受影响卡按依赖冻结（决策 11）。
- run 终态区分 `done-complete`（全部卡 finalizer succeeded）与 `done-partial-cancelled`（含被 cancel 的卡）；后者不得表述为"全部销户"；部分完成也留完整事件账。

### 2.4 任意仓发起与接续

- **产品路径（你不敲命令行，D14）**：你在主会话对话里授权 → 主会话在目标仓落 **`authorization-request`**（只是请求）→ 代你拉起宿主 → **宿主**校验该仓 AGENTS/DevPlan/模块 slug、request 内每张卡存在且状态可开工、树干净 → **在窗口把精确卡清单再展示一次给你核对**（主会话转录属弱信任，见 §3/§7）→ 通过后原子 `CreateNew` 建 `<run>` 并由宿主生成 `authority-snapshot.json`、锚定其哈希 → 写用户级索引 → 拉棒 0。任一校验失败 → `start_rejected`（含拒因枚举），**不建 run**。
- **relay 的进程归属与存活语义（用户拍板 D18=独立宿主后台跑）**：现役 `Invoke-RelayHost` 是**调用方进程内的有界 tick 循环**（`tools/host/relay-host.ps1`，无 detached/service 形态），**P2 必须新增 detached 宿主形态**——主会话代起后宿主脱离主会话独立存活，主会话结束、终端全关都不影响它继续调度；只有你主动 `relay stop`（控制台菜单项）或系统重启才停。配套契约：宿主 **PID/lease 文件**（含 run_id、启动时刻、宿主可执行指纹）写进 `<run>/`；**同一 run 只允许一个活宿主**（重复启动被 lease 拒绝并提示已在跑）；宿主异常死亡后 lease 过期可被新宿主接管（走 §2.4 恢复锁流程）；`relay status` 能报"宿主活着/已死/lease 过期"。**H3 的"关掉全部终端一次再续"因此有两种可展示形态**：关终端但宿主仍活（继续跑）与连宿主一起停（走恢复），两者都要演示。
- **命令行入口仍在但非产品路径**：`relay start -Task A,B,C` 只作脚本/调试通道，同样必须消费 snapshot（无 snapshot 或卡不在其中 → 拒），不得成为绕过授权的旁路。
- `relay`（无参数）= 控制台，可由主会话代起或双击快捷方式打开：读 `~/.dh-relay/runs.json` 列出所有仓未完成 run（卡到第几棒、断了哪些），按编号选一个即 `resume`；`relay status [-RunId]` 只读。
- **续跑判定**（P2 对 P1 A6 的恢复边补充；xhigh 审 S7 补竞态屏障）：宿主启动时先取 **run 恢复锁**：冻结新 launch → 原子 claim 并摄入所有未消费的 tmp/result/ack/inbox → probe 各在册 session → 对"进程/会话已消失"的 attempt **再做一次 claim/双空扫描**（防 result 在首扫后、probe 前原子落盘）→ 仍无 final result 者才 CAS 记 `interrupted_unknown`（A6 fail-closed 起点，不自动推进业务）→ 释放锁 → 拉诊断 agent。日常 tick 同样对"非 launched 节点"的迟到合法 result 做摄入并按身份链判 stale/接受，不得因扫描器只看 launched 而永久拒收。"已交棒 attempt 不重跑"仅指不因重启机械重跑，其结果是否仍有效由后续机器闸/finalizer 审计兜底；工作树与工作区现状保留。
- 诊断 agent 的选项：续跑（fresh 新 attempt，带 handoff/工作树现状）、重来（丢弃工作树未提交改动后新 attempt）、换账号重试（预登记 fallback profile）、放弃这张卡（cancel 该卡节点组）、丢弃暂存分支（closeout_blocked 时）。
- 登录自启（计划任务 `relay resume --all`）留 P3，P2 不做。

## 3. 契约增量（`relay/v2`，v1 只读兼容）

- **节点角色**：沿用 `worker/reviewer/replanner`，新增 `planner / workspace / build / fix / review_lead / integration / requirement_review / lesson_review / closer / diagnoser / gate / finalizer`。
- **节点类型**：`agent`（默认）| `script`。script 节点只引用 `script_spec_id`；宿主脚本执行器扮演该 attempt 的 worker（receipt backend=`script`），只写宿主私有 candidate，Runner 按完整身份链 + script_spec hash + 矩阵校验后晋级 final；result = `succeeded` + `gate_outcome=pass|fail|error` + `exit_code` + 脱敏限长 `output_tail` + `duration_ms`；计划 `on_gate_fail: <fix_node_id>`；`error` 按 `interrupted_unknown` 兜底。
- **多卡结构**：plan 顶层 `cards[]`（card_id、DevPlan 引用、节点组、卡间 depends_on、开关 `integration/e2e`），节点 node_id 前缀 = card_id；Runner 调度以节点为单位、锁以卡/仓为单位。
- **授权两件套：request（agent 写）+ snapshot（宿主写）**（`DHR-A-04`；R9 审 S1/S3 收紧）：
  - **`authorization-request`**（主会话写，落 `<repo>/.dh-relay/requests/<request_id>.json`，run 尚未存在）：授权时刻、授权来源（对话指针）、卡号集合、目标仓与模块 slug、开关意图。它**只是请求，不是授权事实**；主会话的转录属**已登记的弱信任**（机器无法证明它忠实抄录了你的话），故宿主在建 run 前必须在窗口把**精确卡清单**再展示一次给你核对（与落户确认同一形态，见 §4）。
  - **`authority-snapshot.json`**（**宿主**在校验通过后原子 `CreateNew` 建 run 时生成，位于 `<run>/`，内容哈希由宿主锚定进 Runner 拥有的 authority/事件账）：在 request 基础上补齐**各卡 `{card_id, module_slug, devplan_path, devplan_row, workspace_path, verify_scope, 目标, 验收口径引用, 变更范围, 状态}`**（`DHR-A-05`/R10 审 S8：**逐卡冻结 `module_slug`** 是跨模块 run 的唯一权威来源——`dh <module_slug>` 跑哪个模块、DevPlan 回填写哪份、workspace 推导到哪、`verify(<verify_scope>)` 的 scope 取谁，全部据此，不再从"最后一张卡"或路径猜；`docs/relay/` 的写入不归任何卡，归 `run:<run_id>`） 与**两级哈希**——① 整文件 **Git blob 哈希**（来源证明）、② **每卡规范化片段哈希**（该卡的 DevPlan 行 + 其引用的验收 ID 正文，**影响面证明**；因为一份 DevPlan 常承载多卡，整文件 blob 判不出"只影响哪张卡"——`dev_plan/P1-最小接力PoC-开发方案.md` 一文件即承载 DHR_01～03）。
  - **agent 不得生成或改写 snapshot**；`relay start -Task` 只能消费既有 request/snapshot，**禁止凭 `-Task` 自造授权**（旁路反例）。
  - **失配三分（D17 精确化）**：① 宿主锚定的 `snapshot_hash` 不符=工件被篡改 → **全 run fail-closed**（先校验这一层，它是后两层的信任根）；② 某卡的**片段哈希**变化 → **只冻结该卡及其传递依赖闭包**（无关卡继续，见决策 1 与 B14）；③ 整文件 blob 变化但相关卡片段哈希未变 → 只记事件、不冻卡。`active-plan.json` 与每张 launch receipt 记 `authority_snapshot_hash`。Runner 以 snapshot 复核 planner 输出的语义等价（卡集合、验收引用不得变），不信任 proposal 自述。
- **profile 引用**：节点 `profile_id`；用户级注册表条目 `cli / config_dir / work_dir_root / model / fallback_profile_id / account_alias / expected_identity / capabilities`（只是路径、别名与能力位；`capabilities.readonly` 声明该 profile 能否被以强制只读方式拉起及其 CLI 实现，如 codex `--sandbox read-only`）；**diagnoser 角色只能绑定 `capabilities.readonly=true` 的 profile，宿主按该实现拉起并在 receipt 记配置哈希；不支持则拒绝拉起**（`DHR-A-04`）；receipt 记 `profile_id + account_alias + cli + config_fingerprint`；拉起后尽力取 CLI 返回的脱敏账号主体与 `expected_identity` 比对，不符即 `launch_failed`，取不到登记"账号身份不可机器验证"（B4 据此判）；fresh 由新 launch/session + 命令模板无 resume/continue/旧 session 参数证明。
- **verdict**：`review_lead` result 允许 `verdict ∈ {approve, rework, escalate}`；`requirement_review` result 允许 `verdict ∈ {pass, needs_evidence, escalate}`；其它角色携带即拒。
- **返工/补证回路**：计划 `rework: { fix_node_id, max_rounds: 3, re_review_node_ids[] }`、`evidence_rework: { target_node_ids[], max_rounds: 2 }`；触发时目标节点重新 ready 为新 attempt（round→node/attempt 映射记事件）；超上限由对应 agent 写 `decision_required`。
- **复核工件**：reviewer/lead/需求复核每个 attempt 写不可变 `review-<node>-<attempt>.md`；canonical `review.md` 各段只由 lead / 需求复核 / 教训复核 / 收口 agent 以 expected-hash 顺序 CAS 写。
- **人机多轮协作**（P1 §4.2 语义）：integration 等棒进入需人配合阶段写一次 `decision_required` checkpoint；其后多轮往返 Runner 不感知；写出后续 `working` checkpoint 或 final result 才恢复调度；依赖节点期间冻结。
- **确认记录**（xhigh 审 S5 补时序屏障）：agent 写 `decision_required` checkpoint → Runner 接受后**宿主立即对该 launch 绑定的唯一 RELAY 窗口精确截图**（按 handle/window_title 单窗口，零窗口或占位图=失败）并写 `decision_ack`（含 event_id、截图哈希）→ agent 只有读到 ACK 才可向用户展示确认问题；用户回答后 agent 提交 result（`user_reply_verbatim`（抄录）+ `decision_checkpoint_event_id`）→ Runner 摄入时再拍一次同窗口精确截图并核对：ACK 存在、event 顺序、launch/session/attempt 一致、收口时 `user_reply_verbatim == "认可执行本地收口"`；缺任一项 → 不签发 finalizer receipt。截图与哈希只由宿主写入事件账，agent 自填一律忽略。
- **release manifest**：收口 agent 备料终点生成规范化 `release-manifest.json`：候选 squash 目标 SHA、预期 master HEAD、精确路径集、各复核 attempt 工件哈希、gate/e2e 结果哈希、release_mode、Risk-Count/Refs、verify 文案哈希、动作范围；finalizer 与宿主外 oracle（B10）都以它为准。
- **quota**：`quota_exhausted` 枚举转正；识别规则表按 CLI/版本冻结正反样本（脱敏）于注册表；仅高置信匹配才写；fallback 只用注册表预登记条目；无 fallback → paused → 诊断。
- **控制事件子类型**（扩 P1 §3.4，字段仍为 actor/source/nonce + 身份链绑定；xhigh 审 S6 补目标绑定）：`action ∈ {resume, retry_with_profile, reset_attempt, cancel_card, cancel_run, abort_closeout, rollback_test}`；每条控制 candidate 必须带 `target = {run_id, card_id, node_id, attempt_id, launch_id, generation}` + `expected_state_hash`（目标当前状态快照哈希）+ 参数（如 profile_id）+ `option_id`；**`DHR-A-04` 收紧**——diagnoser 的 checkpoint 必须冻结**规范化 option 载荷** `{option_id, action, params, target}` 及其哈希，代发的控制 candidate 与该载荷**逐字段全等**才受理（`option_id` 合法但 action/params 被换即拒）；由诊断 agent（`actor=diagnoser:<node_id>`、`on_behalf_of=user`、`user_reply_verbatim`、绑定其 launch/session）通过宿主提供的**单次、目标绑定、载荷绑定的窄工具**发出；`relay` 控制台（`actor=console`）走**另一套「直接用户命令」schema**（不依赖 diagnoser 声明的选项，但仍须带完整 target + `expected_state_hash`），两条路径各自有正向用例；Runner 唯一消费者，重验目标与 expected_state 后执行；旧 generation/错 run/重复 nonce/非预定义 action/选项不在声明列表/状态哈希不符一律拒绝并留 `control_rejected`。弱信任仅限"输入者是谁"，不扩成"agent 可任选控制动作"。
- **run 生命周期**（控制面维度，与 P1 task overall state 分离）：`active → archiving → done`；`active → archiving → cancelled`；卡级 `finalizing → closeout_blocked → finalizing | aborted`；run 级 `archiving → archive_blocked → archiving`（`DHR-A-05`）；finalizing 中不接受 cancel；**`archiving` 未成功前不得对外报 `done`/`cancelled`**。
- **运行现场、tracked 写入与所有权**（xhigh 审 S9）：运行现场经 resolver 落 `<repo>/.dh-relay/<run_id>/`（D21 换根）（含 `closeout/<卡>/journal.json`）；`~/.dh-relay/runs.json` 只存 repo 路径 / run_id / 摘要 / 更新时间。**所有 tracked 写入统一落在任务树 `D:\wt\<卡>` 内、随该卡 squash 合入；主工作树对所有棒只读**（finalizer 除外，且只经暂存分支快进）。**tracked 写入分两类（D21）**：① **卡级 dev-harness 工件**（8 件套、review 工件、`progress.md`、`findings.md`、knowledge 候选、as-built、DevPlan 回填）落各自模块的 dev-harness 既定位置 `<repo>/docs/modules/<该卡的模块>/workspace/<卡>/` 等，**relay 不在任何模块目录下新建自己的文件夹**，只在该卡 `progress.md` 留一行指向 `docs/relay/runs/<run_id>/` 的指针；② **run 级接力留档**（跨卡，故不能由各卡 finalizer 并发写）落 `<repo>/docs/relay/`，由 **run 级 archive 脚本节点**在 run 达 settled 终态时于**自己的独立暂存分支**上写 `runs/<run_id>/`、对 `README.md` 按 `run_id` 幂等 upsert，产出 `archive-manifest`（owner=`run:<run_id>`，路径集与各卡 release manifest 不相交），一次 CAS 快进合入；持同仓 finalizer 锁 + **仓级 archive lease** 防跨 run 并发；归档失败 → `archive_blocked`，run 不得报 done。
- **relay 的完整副作用面**（`DHR-A-05`/R10 审 U1：业务仓内只有两个 relay 自有内容根，但 relay 的写入面不止两条路径，逐项登记如下，B9 分两类策略验收）：
  | 面 | 具体位置 | 进 git？ | 归谁 |
  |---|---|---|---|
  | relay 运行现场 | `<repo>/.dh-relay/<run_id>/`（事件账/派工单/截图/psmux handles/journal/授权工件/PID·lease/锁） | 否（须 `.gitignore` 锚定 `/.dh-relay/`） | run |
  | relay 接力留档 | `<repo>/docs/relay/{README.md,runs/<run_id>/}` | 是 | `run:<run_id>` |
  | 卡的业务代码改动 | authority snapshot 里**该卡的变更范围**所允许的路径 | 是 | 该卡 |
  | 卡的 dev-harness 工件 | `<repo>/docs/modules/<card.module_slug>/{workspace/<卡>/,knowledge/,as-built/}` 与该卡 DevPlan 行 | 是 | 该卡 |
  | 任务树与 Git 副作用 | `D:\wt\<卡>`（仓外）、Git objects/refs/worktrees、本地 `test` 分支、暂存分支 | n/a | 该卡 / run |
  | 用户级 | `~/.dh-relay/{runs.json,registry/}` | 否 | 全局 |
  | 宿主外副作用 | CLI 的 profile/config/session 文件、psmux 进程与窗口、（仅 B16 适用时）远端 `refs/heads/test` | 否 | 全局 / run |宿主持 `(canonical_repo, task_id)` 跨进程 claim lease，**一仓同卡同时只允许一个 active run**。
- **通知**：P2 不做通知 adapter（留 P3）；用户靠弹出窗口与 `relay`/`relay status` 控制台知情。
- **版本与兼容**（xhigh 审 S1，用户拍板 D13）：P2 工件 `schema_version=relay/v2`，Runner/校验器双解析器；承诺不变的是 **v1 业务状态枚举、合法转换边集合、v1 目录的只读解析**；v2 新增字段/角色/控制事件与"Runner 核对确认记录字段与事件顺序、接受诊断代发控制事件"登记为 **v2 版本化例外**（P1 §3.4 "Runner 不读取回答"的语义收窄为：Runner 不解析对话内容，只校验结构化确认字段的等值与时序），不再笼统称"只扩不改"。v1 存量 run（DHR_03/IHSR_05 等）在 P2 下**只读状态/归档，不续跑**；迁移另立项。转换矩阵：**不新增业务出口**。

## 4. 固定流转补充

- **落户确认**：workspace 棒完成 8 件套后写 `decision_required`（档位/目标/范围/验收/落点摘要）；回答"确认"→ `succeeded`；回答"取消"→ 诊断 agent 路径 `cancel_card`。**8 件套齐备是该棒 `succeeded` 的前置机器条件**（缺件/占位则不得终结，见 B17）。
- **授权校验**：主会话只写 `authorization-request`（见 §2.4/§3）；**宿主**校验每张卡在 DevPlan 中存在、状态可开工、且在 request 内，通过后原子建 run 并由宿主生成不可变 `authority-snapshot.json`（agent 不得自造 snapshot）；接力计划生成 agent 的 proposal 只填模板槽位，Runner 晋级前以 snapshot 复核，不符即拒（见 B14）。
- **教训复核**：每卡必调度 `lesson_review` 棒并在事件账留 launch/result；它写 knowledge 候选与 review.md 教训段，不阻断放行，但收口棒不得在它终结前 ready（见 **B18**）。
- **机器闸失败**：`gate_outcome=fail` → 修复 agent（fresh）→ 回闸；连续 ≥3 次由修复 agent写 decision。
- **代码复核**：R1/R2 只读、fresh；汇合规则写在 brief（P0/P1=0 且 findings 归档 → approve；有 P0/P1 → rework；争议/超轮 → escalate）。
- **集成与端到端**：仓约定有 test 分支/需真机验证时开启；集成脚本节点管本地/远端 test 的合入、push（仅 `-AllowTestPush` 且白名单 `test`）、回滚；agent 只给命令、你执行并回贴（含部署测试服务器）；结果作需求境证据进账本。
- **需求与一致性复核**：按 brief 验收口径逐条要证据；缺则 `needs_evidence` 回目标棒（只重跑该棒及下游机器闸；改代码则走 rework 全套）；一致性表覆盖 brief/DevPlan/证据。
- **收口**：收口 agent 走 E5～E10 备料清单、产出 manifest 与 verify 文案，窗口问确认；拿到固定短语后调 finalizer；finalizer 在暂存分支执行授权包（squash / 复验 / 回填 / verify）后一次快进 master 并清树；不 push、不 deploy。
- **恢复**：`relay`/`relay resume` 同 RunId 续跑；断棒 → 诊断 agent；finalizer 崩溃从 journal 最后完成步续跑，master 未动；`closeout_blocked` 由诊断 agent 带你重试或丢弃暂存分支。

## 5. 操作剧本（自举）

1. 你在对话里授权一批 2～3 张**与 relay 实现无关**的真实标准档卡（B 阶段与你定）；主会话在该仓落 authority snapshot 并代你拉起 relay，之后主控不再介入（你不敲命令行）。
2. 棒 0 出计划；每张卡按依赖依次/并行走 1～8'；你只在窗口里：落户确认、（如有）决策、真机协作、收口确认。
3. 途中关掉全部终端一次，隔一段时间 `relay` 控制台续跑，诊断 agent 带你接上断棒。
4. 全部卡 verify 销户后 run done；宿主外 oracle 核对主干 commit/verify trailer/DevPlan/树。

## 6. 验收清单

### 6.1 AI 自动验收栏

| ID | 验收项 | 怎么证明 |
|---|---|---|
| B1 | `relay/v2` 契约与 v1 兼容：新增角色/script 节点/gate_outcome/verdict/rework/evidence_rework/profile 与 script 引用/复核工件/多卡结构/控制事件子类型（含 target/expected_state/option 绑定）/确认记录与 decision_ack/candidate generation/authority snapshot 引用/profile 能力位/option 载荷哈希/`acl_evidence_id` 字段有 schema + 矩阵测试；**计划内出现 `command`/`extra_args` 等任意 CLI 参数被 schema 拒**（`DHR-A-04` 消歧：形状层的拒绝归本项，语义层的授权等价归 B14、执行层归 B4）；v1 业务枚举与合法边集合不变；v1 目录只读可解析、resume 被拒 | 契约套件正反例（双解析器）；矩阵集合 == 独立硬编码 oracle；P1 15 套件全绿；**双根 discovery（`DHR-A-05`）**：用现存的 legacy 根 `.dh-runtime/relay/` 下真实目录（DHR_03 归档 2 个 + IHSR_05 v1 run 3 个）证明 v1 只读回放可解析、`relay resume` 硬拒、且 legacy 根**零新写**；v2 只写 `.dh-relay/` |
| B2 | 脚本节点由宿主执行、结果为机器事实 | fake+真实：exit 0→pass、非 0→fail 并按 on_gate_fail 分支、超时→error→interrupted_unknown；agent 结果伪造 gate_outcome 被拒；script_spec hash 不符拒执行；output_tail 先脱敏后限长 |
| B3 | 代码复核两轮 + 汇合 + 需求复核 + 有界返工/补证的**调度顺序与轮次上限**（`DHR-A-04` 消歧：**不含 `lesson_review`**，它归 B18；复核工件不可变性归 B11） | fake 回放：R2 只在 R1 succeeded 后 ready；lead 只在 R1、R2 后；rework 走 fix→gate→R1'→R2'→lead（新 attempt）；第 4 轮转 decision；requirement_review needs_evidence 回目标棒新 attempt，第 3 轮 escalate；非授权角色携带 verdict 被拒；各棒 launch/session 各不相同且命令模板无 resume/continue |
| B4 | 用户级注册表的**执行侧**生效、任意仓发起、真实拉起 codex-ninth 一棒（`DHR-A-04` 消歧：计划字段的 schema 形状归 B1、授权语义归 B14、profile 只读能力位归 B15） | 真实：在非 dh-crew 仓由授权工件拉起 relay，现场落该仓 `.dh-relay/`，索引写 `~/.dh-relay/runs.json`；同一 run 内 claude 与 codex（ninth）各一棒交棒；receipt 记 profile_id/account_alias/config_fingerprint 且脱敏账号主体与 expected_identity 匹配（取不到/不匹配则本项不通过，不得宣称 ninth）；注册表条目解析到真实 cli/config_dir/work_dir_root 并被实际使用（换 profile 即换配置目录，可观察）；plan/receipt/事件零凭据 |
| B5 | quota 高置信识别与预登记 fallback | fake：冻结样本→`quota_exhausted`→fallback fresh attempt；非额度错误样本→不判 quota；无 fallback→paused→诊断节点被拉起 |
| B6 | 宿主健壮性 | 截图/通知抛错不致宿主崩；`.consumed-*` 有轮转；spawner 不阻塞 tick；Runner 提交临时名与 worker tmp 不同名；决策态有 `state-at-decision-*.json` |
| B7 | 控制台与接续（`DHR-A-04` 收窄：诊断代发控制事件移出至 B15） | `relay`/`relay start/resume/status/propose/cancel` 可用；手动 propose v2 走同一 CAS；模拟"全部终端被杀"后 resume：恢复锁下先摄入未消费 tmp/result/inbox、再 probe、再二次 claim/双空扫描（**注入"首扫后、probe 前原子落 result"的并发反例：该 result 必须被接受而非判断棒**）；有派工单无 final result 且无会话的 attempt 才判 `interrupted_unknown`；工作树/工作区保留；诊断节点被拉起（只验"被拉起"，其权限边界归 B15）；索引写入原子且并发两宿主不互相覆盖；一仓同卡第二个 start 被 `(canonical_repo, task_id)` claim lease 拒绝。**宿主存活语义（D18）**：主会话代起后宿主 detached——**杀掉主会话/关闭其终端后宿主仍在推进**（可观察：事件账继续增长）；`<run>/` 有宿主 PID/lease（run_id + 启动时刻 + 宿主指纹）；**同一 run 第二个宿主被 lease 拒绝**；`relay stop` 能停且现场可 resume；**`run_id` 规范化（`DHR-A-05`/D23）**：`R<nnn>` 序号在仓级锁内由 `runs.json` 该仓分段最大值 +1 发出（并发两个 start 各得不同序号、无跳号无重号）；主题 slug 非 ASCII 小写字母数字短横线、超 30 字、首尾短横线 → 规范化失败即 `start_rejected`（**不得静默截断或转写**，逐类反例：中文、大写、空格、超长、首尾短横线）；`(repo 路径, run_id)` 复合键在 `runs.json` 中唯一。**双根发现（`DHR-A-05`）**：控制台/`relay status` 同时列出新根 `.dh-relay/` 的 v2 run 与 legacy 根 `.dh-runtime/relay/` 的 v1 存量 run（后者标只读、选中即拒 resume）；**`.gitignore` 缺锚定 `/.dh-relay/` 时 start 被拒**（`start_rejected`）且 relay 未擅自改业务仓 `.gitignore`；宿主被强杀后 lease 过期，新宿主可接管并走恢复锁流程；`relay status` 正确区分"宿主活着 / 已死 / lease 过期"三态 |
| B8 | 窗口确认闸护栏与收口幂等 | fake 回放 + **真实 psmux 临时仓**：无 decision_ack 时 agent 展示确认/提交 result → 拒；收口 result 缺 `decision_checkpoint_event_id`/回答原文非固定短语/ACK 截图或 result 截图缺失或为占位图/多窗口时截错 handle → 不签发 finalizer receipt、无 verify、无 git 写；先 result 后 checkpoint 的时序被拒；**真实 psmux 快速作答反例**（用户在 ACK 前作答→不接受）；agent 直接调用 finalizer 脚本 → 无 receipt 被拒；有效确认 → Runner 签发 finalizer receipt → journal 在暂存分支逐步完成后一次快进；每一步之后模拟崩溃再续跑不重复 squash/verify、master 在快进前未动；master 被外部提交后快进拒绝→`closeout_blocked`；abort 丢弃暂存分支不碰 master |
| B9 | 隔离、禁改与落点纪律（`DHR-A-05` 重写；R10 审 S4/S5） | **① dh-relay 自身开发时的隔离**（沿用）：生产代码变更仅 `tools/`；dh-crew 代码与 active state 零改动；dh-crew run-all 不受影响。**② 业务仓内容策略**（relay 跑在业务仓时）：本次 run 在业务仓的**内容写入**必须 ⊆ `{<repo>/.dh-relay/**（不进 git）} ∪ {<repo>/docs/relay/**（仅 archive 节点，owner=run）} ∪ ⋃各卡(authority snapshot 冻结的该卡变更范围 ∪ 由该卡 module_slug 派生的 dev-harness 路径)`——**必须包含"卡授权的业务代码路径"**（施工棒本就要改代码，漏掉会让合法真实卡误判失败）。**③ 宿主外副作用策略**：Git refs/worktrees/暂存分支、`~/.dh-relay/`、CLI 配置与会话、psmux 进程按 §3 副作用表各自登记，不混入②的内容白名单。**取证方式**（静态终态扫描抓不到"写后删除"，故联证）：受限写环境 + 前后快照 + `git diff`/`git status` + 独立 oracle 四者交叉。**落点反例**：本次 run **新建**的、带 relay archive/runtime schema 或 marker 的非规范落点即失败（如 `docs/modules/<模块>/relay/`、`workspace/<卡>/relay/`）；**正例反证**：业务仓本就存在名为 `relay` 的合法模块 `docs/modules/relay/`、或 run 之前已存在的同名目录，**均不得误杀**（各一条反例）；relay 不得**新写** `<repo>/.dh-runtime/`（legacy 根只读）；`.gitignore` 缺锚定 `/.dh-relay/` 时 `start_rejected`，且 relay 不自行改业务仓 `.gitignore`（用 `git check-ignore`、`git ls-files` 与"误暂存"反例验收）；跨模块 run 的留档唯一性按 `archive-manifest`/`run_id` 搜索证明（该卡 `progress.md` 里的指针行不算第二份留档） |
| B10 | 自举 oracle（宿主外） | 独立脚本（不 import relay 代码）重算并核对：每张卡 master 新 commit 含 `verify(<slug>)` trailer 与 Risk-Count、变更路径集 == manifest 路径集、squash 内容 == 候选 SHA 树；DevPlan 任务行状态=已完成且 SHA 一致；各复核 attempt 工件哈希 == manifest；任务 worktree 与暂存分支仅在 journal 终态后不存在；**（`DHR-A-05`）** 每卡的 `verify(<scope>)` 的 scope == 该卡在 authority snapshot 里冻结的 `verify_scope`（跨模块 run 逐卡各自校验，不取"最后一张卡"）；`docs/relay/README.md` 的 run 行与 `docs/relay/runs/<run_id>/` 内容 == 现场事件账独立重算的结果（涉及模块集合、卡集合、终态、每卡 verify SHA 均须对得上）；**README 按 run_id 幂等**——同一 run 重跑归档不产生第二行、内容一致（重复归档反例）；`archive-manifest` 的路径集与各卡 release manifest 路径集**不相交**；`archive_blocked` 时 run 未标 done |
| B11 | 复核身份链与工件不可变 | fake 回放：旧 attempt 再写 `review-<node>-<attempt>.md` 不影响 canonical review.md；lead/需求复核写 review.md 时 expected-hash 不符即拒 |
| B12 | 多卡调度、重编排影响面与基线刷新 | fake 回放 + **真实临时 Git 仓三卡剧本（一对并行、一对重叠、一次 master 漂移）**：三卡按 DevPlan 依赖串/并行；静态路径重叠的两卡被强制串行；某卡 dependency_blocked 重编排时不受影响卡的在跑棒交完被正常接受、切届后下一棒 fresh 继续，受影响卡按依赖冻结；同仓 finalizer 串行锁生效；第二卡 finalizer 发现基线落后 → 暂存分支 rebase → 新 candidate generation → patch 哈希不变→只重确认（旧确认失效、必须重弹窗口）；patch 变→重跑机器闸与 R1′→R2′→lead′ 后重确认；rebase 冲突 → `closeout_blocked`；一卡 escalate 只冻结依赖卡；cancel_card 不影响其它卡；宿主外 oracle 从 start 时的 authority 快照独立推导授权卡/路径集，不信任 closer manifest |
| B13 | 人机多轮协作与人判 verdict 留痕（`DHR-A-04` 收窄：远端 push 与补偿移出至 B16） | fake+真实：integration 棒写 `decision_required` 后多轮往返期间 Runner 不感知、不计数、不因"无进展"误杀该 launch；写出后续 `working` checkpoint 或 final result 才恢复调度；依赖节点在此期间冻结、无依赖节点继续；多轮协作的命令与你回贴的输出成为需求境证据写入 `<ws>/progress.md` 证据账本（E-ID 可回链）；**每个需人判 E-ID 必须携带用户原文 verdict 字段，缺失 → 该 E-ID 判"未取得人判结论"、不得由 agent 或需求复核棒代填**（反例：agent 自填 verdict 被拒；用户只说"再看看"→ 记未定，不得当通过）；协作超时/中断走 `interrupted_unknown` 而非静默 succeeded |
| B14 | 授权工件与授权边界（`DHR-A-04` 新增；只管授权语义，schema 形状归 B1、profile 执行归 B4） | **authority snapshot 契约**：不可变工件（路径、schema、内容哈希均冻结），含授权时刻、卡号集合、各卡 DevPlan 行与目标/验收/变更范围、所依据的 DevPlan 与 design 文件 **Git blob 哈希**（**逐卡记录**：每张卡各自绑定其 DevPlan 行与 design 验收项的 blob 哈希）、授权证据指针；plan 与每张 receipt 绑定 snapshot 哈希。**两类失配影响面不同（用户 2026-08-16 场景题拍板 D17）**：① **snapshot 工件自身被篡改**（内容哈希不符）=攻击面 → **全 run fail-closed**，不推进任何卡；② **run 中途 DevPlan/design 被正当修改致某卡 blob 哈希不符** → **只冻结受影响的那张卡**（其余卡照常跑完），拉诊断 agent 在窗口告知"该卡验收口径已变"，选项=按新口径重新授权 / 回滚该卡 / 取消该卡；不得静默按旧口径跑到底，也不得自动吞掉新口径续跑（否则授权工件失去意义）。**拒绝路径逐类反例**：卡不在 snapshot 内、卡在 DevPlan 中不存在、卡状态非可开工枚举、start 时 blob 哈希即不符、run 中途 blob 变化（分别验"只冻该卡"与"其它卡不受影响"）、snapshot 被篡改（验全 run 停）→ 均留 `start_rejected` / `card_frozen`（含拒因枚举）。**planner 约束**：proposal 的卡集合/`card_id`/DevPlan 引用/目标与验收引用必须与 snapshot **语义等价**，加卡、换卡、改验收引用三类各一反例；晋级前 Runner 以 snapshot 复核而非信任 planner 输出。**创建边界**：agent 只能写 `authorization-request`，**snapshot 只由宿主在建 run 时生成并锚定**（agent 写 snapshot、CLI 凭 `-Task` 自造授权各一条旁路反例）。**弱信任补偿（D19/D24）**：宿主建 run 前必须在窗口展示**精确卡清单 + 本趟主题（中文标题与其 ASCII slug）**请用户核对，用户答"确认"才建 run；未展示即建 run、或用户答"不对"仍建 run → 反例；用户当场改主题后 run_id 按新 slug 生成 → 正向用例。**正向路径**：你在对话授权 → request 落盘 → 宿主校验 → 窗口核对清单 → 建 run + 生成 snapshot → plan 晋级 → 首棒 ready，全链哈希可复算 |
| B15 | 诊断 agent 的可信只读承载与控制事件权限边界（`DHR-A-04` 由 B7 拆出） | **① 选项载荷绑定**：diagnoser 的 checkpoint 必须冻结**规范化 option 载荷**（`option_id` + `action` + `params` + `target`）及其哈希；代发的控制 candidate 与该 option 载荷**逐字段全等**才受理——`option_id` 合法但 `action` 或 `params`（如 `profile_id`）被换 → 拒（换参反例）；`option_id` 不在声明列表 → 拒。**② 目标与状态绑定**：必须带完整 `target = {run_id, card_id, node_id, attempt_id, launch_id, generation}` + `expected_state_hash`；缺字段、旧 generation、错 run、重复 nonce、非预定义 `action`、状态哈希不符 → 拒并留 `control_rejected`（每类各一反例）。**③ 单次授权**：宿主窄工具的同一授权二次调用被拒。**④ console 路径另立 schema**：`actor=console` 走"直接用户命令"契约（不依赖 diagnoser checkpoint 声明的选项，但仍需 target + expected_state_hash），两条路径各有一条正向路径用例。**⑤ 可信只读承载**：注册表 profile 增能力位（如 `readonly=true` 及其 CLI 具体实现，codex `--sandbox read-only` / claude 只读权限模式），宿主只以该能力位拉起 diagnoser 并在 receipt 记配置哈希；**profile 不支持强制只读 → 拒绝拉起该角色**（反例）；不得以 prompt 约束替代（现役 `relay-worker-entry.ps1` 用 `--dangerously-skip-permissions`/`--yolo` 拉起，为本项的反面基线） |
| B16 | 远端 `test` 受控 push 与补偿（`DHR-A-04` 由 B13 拆出；**三态严格分账，见决策 13**：**只有**"授权冻结时刻该仓确无 eligible remote"才登记`N/A（前提不成立·本次不适用）`；**有 remote 但缺白名单 / 缺 `integration=true` / 缺 run 级 `-AllowTestPush` / 缺 ACL 机器证据 / 缺 H5** → 一律记 **`未授权·未验证`、禁 push、该卡不可收口**，属权限红线**不得记 N/A、不得风险放行**） | 真实临时仓 + 裸远端：**三态各有反例**——① 无 eligible remote → 该棒降级为本地合并、记 N/A；② 有 remote 但缺白名单/缺开关/缺 run 级授权/缺 ACL 工件/缺 H5（各一反例）→ 集成脚本拒 push 且该项记未验证、卡不可收口（**验"未被误记为 N/A"**）；③ 全前置成立时才进入下列 push 路径；适用时只接受注册表冻结的 remote URL 指纹 + `refs/heads/test`（推 master/main、其它 ref、其它 remote 各一反例、均硬拒）；journal 冻结 remote 指纹 / 远端原 HEAD / source SHA / 完整 refspec；e2e 失败或 `rollback_test` → `--force-with-lease=test:<原HEAD>` 精确回退 + 本地 reset；**外部并发改 test 致 lease 冲突 → `closeout_blocked`，不得宣称已回滚/run done**；**cancel_card 发生在 push 之后必须先走同一补偿路径**，补偿失败同样 `closeout_blocked`；同仓 test 推送持宿主锁串行。**ACL 证据（机器项部分）**：检查器校验脱敏 ACL 回读工件存在、格式合法、只覆盖该仓 `refs/heads/test`、remote 指纹与注册表一致，并写 `acl_evidence_id`；**该 id 与 H5 共享、两者结论各自独立记录**；**首次 push 的前置闸**——同一 (run, remote 指纹) 的第一次 push 之前，机器项与 H5 必须**都已成立**才放行，缺失或过期一律在 push 前 fail-closed（不是等收口才发现）；权限属红线，不得风险放行 |
| B17 | 落户棒完整性（`DHR-A-04` 新增；**不挂靠 `dh` R15**——现役 `dh-check.mjs` 的 R15 只查文件存在、不查占位） | 本项自带谓词：冻结 8 件套**精确文件集**与**逐文件非占位判据**（每个文件各自的最小实质内容条件，如 brief 三段填写、task_plan 施工步骤非占位、visual_map 步骤表非空等，逐条写死在契约里）；workspace 棒只有在任务树已建 ∧ 全部文件存在 ∧ 全部通过非占位谓词时才允许 `succeeded`；**每个文件各一条缺失反例 + 各一条占位反例**；不满足时该棒不得终结，走 decision 或 fail-closed |
| B18 | 教训复核棒真实执行（`DHR-A-04` 由原 B17 拆出） | 每张卡的流水必然调度 `lesson_review` 棒且事件账有其 launch 与 result（缺 launch = 阻断收口的反例）；其终态三分——**成功**（写入 `knowledge/教训库-候选.md` 追加候选、不入正册，并写 `<ws>/review.md` 教训复核段）、**无候选**（合法终态，只写"本卡无新增候选"结论，不得被误判为失败）、**失败**（记事件、不改变 verdict、不阻断放行）；**收口棒不得在 `lesson_review` 未终结（三态之一）前 ready**，回放证明顺序 |

### 6.2 人类验收栏

| ID | AI/你做什么验证动作 | 对话里展示什么证据 | 你判断什么 |
|---|---|---|---|
| HC-P2-H3 | 用 relay 自举跑一批 2～3 张与 relay 实现无关的真实标准档卡，从 `relay start` 到全部 verify 销户，中途故意关掉全部终端一次再续；你只在窗口里作答/协作 | 时间线 + 事件摘要 + 每次窗口作答的 checkpoint/result/前后截图 + 诊断 agent 接续记录 + 每卡 verify 提交 + 用户显式动作清单（口径：每卡基础 2 = 落户 + 收口，加实际 decision/协作轮次，逐次列原因） | 无常驻主控接力是否真的"只在该出手时出手"，窗口作答/协作/接续体验是否可接受 |
| HC-P2-H4 | 与常驻主控模式、IHSR_05 试点并排对照（同 P1 H2 口径；预定上限：主控对话消息=0、每卡人工动作 = 2 + decision/协作数，**另列 run 级动作（start/控制台选择/resume）与每轮服务器操作**、无飞书） | 对照表（区分 `done-complete` 与 `done-partial-cancelled`，后者不给"全部销户"结论）+ 未覆盖范围 + 控制面故障恢复演示（宿主中途停→续；finalizer 中途停→续；全部终端关→控制台续） | 是否达到"取消常驻 AI 主控"的终态；还差什么进 P3 |
| HC-P2-H5 | （`DHR-A-04` 新增，与 B16 共享 `acl_evidence_id`、结论各自独立）**出现条件（三态严格分账，见决策 13 与 B16）**：**只有**"授权冻结时刻该仓确无 eligible remote"时才登记 `N/A（前提不成立·本次不适用）`——不进本次放行证据包、不算未验证、也不算风险放行；**仓有 remote 但缺白名单 / 缺 `integration=true` / 缺 `-AllowTestPush` / 缺 ACL 机器证据时，本项记 `未验证`（不是 N/A），禁 push、该卡不可收口，权限红线不得风险放行**；仓有 remote 且前置齐备时本项正式出现、须由你判断。你登录目标 Git 服务侧取得 relay 所用凭据的权限回读，AI 脱敏后落成工件；你只判断这份回读的**来源与时效**。**有效期**：每个 run × 每个 remote 指纹，**首次 push 之前**判一次；ACL 或 remote 指纹变化即失效重判；同一 run 内后续 push 不再打断你 | 脱敏 ACL 回读工件（只显示仓库标识与允许的 ref 模式，零凭据值）+ 检查器结论（是否只覆盖 `refs/heads/test`、remote 指纹是否与注册表一致）+ 取得时间与来源系统 + `acl_evidence_id` | 这份回读是否来自你**真实指定的测试仓**、是否是**当前生效**的配置（而非历史快照或另一个仓）。**你的判断只写 H5 自己的结论，不改写 B16 的机器结论**；两者合取才允许首次 push。判否或拿不到 → 该项未验证、首次 push 前 fail-closed，**权限属红线、不得风险放行**，只能暂停或更换测试仓/凭据 |

## 7. 边界

- **不做**：立项/拆计划、通用多卡 DAG 调度器、跨仓 run、仓级注册表覆盖、登录自启、通知 adapter、原会话恢复返工、非 psmux 后端、跨机器、UI、自动决策、任何 deploy、除 `test` 外的任何 push（master/main 永不）、完整 quota 恢复通道、通用动态 planner。
- **落点纪律（D21）**：relay 在业务仓只写 `.dh-relay/`（不进 git）与 `docs/relay/`（进 git）两处，外加各卡 dev-harness 既定位置；**不得在任何模块目录下新建 relay 文件夹**，不得写 `<repo>/.dh-runtime/`（dh-crew 现场根）。理由：业务仓常有 20+ 模块，且一个 run 的卡可能跨多个模块，记录必须集中并由 `docs/relay/README.md` 收束。
- **禁改**：dh-crew controller/loop/dispatch/notify 代码与 active state；DevPlan/workspace 只由对应棒或 finalizer 按 dev-harness 规矩写；DevPlan 任务的目标/验收不由 relay 改。
- **凭据**：注册表只存路径/别名；事件、计划、manifest、tail、通知零凭据。
- **信任登记**（`DHR-A-04` 起，每项各有独占验收 ID，B 拆时不得与相邻能力合并成一张卡）：
  - 窗口作答（落户/决策/协作/收口/诊断）的输入者身份不可机器验证，登记为用户接受的弱信任；收口另有宿主持有的三护栏（先挂起且事件可核、固定短语、宿主拍前后精确单窗口截图）→ **B8**。
  - 诊断 agent 代发控制事件同属弱信任，但能力被目标绑定 + 选项绑定 + 单次授权收窄 → **B15**。
  - test 分支 push 是 P1 "永不 push" 的唯一受控例外，且**只在项目真有远端时才适用**（run 级授权 + remote 指纹白名单 + 可回滚 + 补偿）→ **B16**；其远端 ACL 属外部事实，机器项只验回读工件（B16），主观来源与时效判断归 **H5**，两者共享 `acl_evidence_id`、结论独立、合取才放行首次 push。**三态严格分账（决策 13）**：只有"授权冻结时刻确无 eligible remote"才记 `N/A（本次不适用）`；**有 remote 却缺白名单 / 开关 / run 级授权 / ACL 工件 / H5 的，一律记 `未授权·未验证` 并禁 push、该卡不可收口——权限属红线，不得记 N/A、不得风险放行**。
  - 授权真源是**宿主**据主会话 request 生成并锚定的 authority snapshot（两级哈希）、planner 只能语义等价实例化 → **B14**；**主会话对你原话的转录属已登记弱信任**（机器无法证明忠实），故宿主建 run 前在窗口再展示一次精确卡清单请你核对；落户 8 件套完整性 → **B17**；教训复核棒真实执行与三终态 → **B18**。
  - 集成端到端棒产出的是**人验证据**：每个需人判 E-ID 必须留你的原文 verdict，agent 与需求复核棒均不得代判 → **B13**；最终业务结论在 E10/E11 放行证据包由你确认。
  - 诊断 agent 的"只读"不靠 prompt 而靠注册表能力位 + 宿主拉起方式 → **B15**（现役以 `--dangerously-skip-permissions`/`--yolo` 拉起，是本项要消灭的反面基线）。
- **表述**：P2 通过只能称"一批任务卡完整接力自举通过"，不宣称通用多卡生产编排。

## 8. 待决策（B 阶段处理）

- 自举用哪 2～3 张真实卡：留 B 阶段与用户定（原则：与 relay 实现无关、预期 diff/测试已知、标准档；至少一张开 integration 开关以验证多轮协作）。

## 9. A 方案审核与理解确认

- **事件类型**：A-完整（`DHR-A-05` 落点修订=用户拍板 D21，relay 在业务仓的落点收束为两处、新增 run 收尾脚本节点；`DHR-A-04` 已确认并提交 `49615e2`；`DHR-A-02` 首版落盘 `cf5b973`；`DHR-A-03` 同日修订=codex gpt-5.6-sol xhigh 第六轮交叉审核后的结构性收紧 + 拍板 D11～D13；`DHR-A-04` 同日再修订=**B 拆计划交叉审核回流**，补齐验收清单并原子化 B7/B13）——正式输入本文，`design/README.md` 白名单已含；P1 正式输入 01 不改。
- **`DHR-A-04` 状态**：**已确认并提交**（2026-08-16 用户点选「确认，提交并继续重拆 B」，提交 `49615e2`）。
- **`DHR-A-07` 性质**：**机械订正，无新产品决定**——把 §6.1 B16、§6.2 H5、§7 三处与 §1.3 决策 13 对齐（A-05 采纳 R10-S5 时的漏改），三态口径逐字统一；由 B 拆计划定向复审核出。不新增/删除验收 ID，B1～B18 与 H3～H5 不变。
- **`DHR-A-06` 性质**：**纯状态回填与措辞订正，无实质设计变更、未新增审核轮次**——A-05 的用户确认发生在提交 `25b2ec8` 之后，文档信息与 §9 残留"待用户确认"字样（B 重拆起草人核出），本事件仅把它改成事实；因 `dh` R29 要求"改正式输入必须同批发/改 planning-event"，故显式发号并如实登记，不伪装成新一轮审核。
- **`DHR-A-05` 状态**：**已确认并提交**（2026-08-16 用户在对话中答「没了」完成整版确认，提交 `25b2ec8`；R10 定向复审 9 条 P1 全采纳并回改）。
- **审核记录**：[五轮 fresh 审核与裁决](./evidence/02-交叉审核记录-完整流水.md#review-a)（R1 全面 + R2/R3/R4 定向 + R5 收敛）+ [第六轮 sol xhigh 交叉审核与修订裁决](./evidence/02-交叉审核记录-完整流水.md#review-a3) + [第七轮 B 拆计划交叉审核回流与定向复审](./evidence/02-交叉审核记录-完整流水.md#review-a4)（均为 headless codex read-only 独立实例，侦测型降级、非机器只读）。
- **主会话裁决**：采纳 S1（不加新 result_status，用 gate_outcome）、S2（计划只引用注册表）、S5（独立自举 + 宿主外 oracle）、S7、U2～U4、R2 全部、R3 全部、R4 全部；审核推荐的一次性回执 CLI（U1/D1）与本地 test 方案（R4 D10）被用户推翻，按用户拍板落地并诚实登记为弱信任 / 受控例外。
- **讲解记录**：[整版讲解与理解确认](./evidence/02-交叉审核记录-完整流水.md#understanding-a)；[修订讲解与拍板 D11～D13](./evidence/02-交叉审核记录-完整流水.md#understanding-a3)；[DHR-A-04 修订讲解与确认](./evidence/02-交叉审核记录-完整流水.md#understanding-a4)。
- **理解问题**：复核发现 P1 需要返工时，默认拉全新 fix worker 还是保留原施工窗口？
- **用户回答 / 拍板**（2026-08-16）：拉全新 fix worker；另九项拍板 D2～D10 见审核记录 §2.3/§3.3/§3.7。
- **调整与复审**：草案 v1→v5，五轮审核；无 open P0/P1；剩余为 B 阶段契约卡字段级冻结事项。
- **用户确认**：`DHR-A-02`/`DHR-A-03`——2026-08-16 对话点选「确认整版，落盘（含 8～10 卡）」及 D11～D13 拍板；`DHR-A-04`——2026-08-16 对话点选「确认，提交并继续重拆 B」（拍板 D14～D20，讲解与理解问答见 evidence §3.12），提交 `49615e2`；`DHR-A-05`——2026-08-16 对话确认（拍板 D21 落点收束、D22 部分取消诚实口径、D23 run_id 格式、D24 主题来源；R10 复审与裁决见 evidence §3.13，讲解与理解问答见 §3.14），提交 `25b2ec8`。任何一次确认均不授权 B 或任何卡开工。

## 文档更新日志

| 日期 | 编辑模型 | 更新内容 |
|---|---|---|
| 2026-08-16 | Claude（Fable 5） | 首版：P2 完整流水正式设计（一批任务卡的无常驻 AI 主控接力、任意仓发起、三路复核成棒、窗口确认闸、finalizer 暂存分支一次快进、test 受控 push、诊断 agent 与接续）；由 drafts/02 v5 晋级。 |
| 2026-08-16 | Claude（Fable 5） | A 修订（事件 DHR-A-03，用户指定 codex gpt-5.6-sol xhigh 第六轮交叉审核 9 P1 全采纳 + 用户拍板 D11 多卡重编排影响面/D12 rebase 后处置/D13 v1 只读）：`relay/v2` 版本化与例外登记；finalizer 只由 Runner 签发 receipt；确认闸加宿主 decision_ack 与精确单窗口截图；控制事件目标/expected_state/选项绑定；恢复锁与二次 claim；test push 白名单含 remote 指纹、lease 冲突与 cancel-after-push 补偿；tracked 写入全落任务树 + claim lease；B1/B7/B8/B12/B13/H4 补真实反例；决策清单增 7 行。 |
| 2026-08-16 | Claude（Fable 5） | **A-07 机械订正（B 定向复审回流，无新产品决定）**：`DHR-A-05` 采纳 R10-S5 时只改了 §1.3 决策 13 的正文，**漏改 §6.1 B16 表格行、§6.2 H5 行与 §7 信任登记**——三处仍写成"缺白名单/缺 `integration=true`/缺 `-AllowTestPush` 也算前提不成立·本次不适用"，与决策 13 自相矛盾，实现者顺验收表读会把权限红线吞成 N/A。本次按已拍板 D15/R10-S5 口径逐字对齐三处：**只有**"授权冻结时刻确无 eligible remote"才记 `N/A`；**有 remote 但缺白名单/开关/run 级授权/ACL 机器证据/H5 → 一律记 `未授权·未验证`、禁 push、该卡不可收口，属权限红线不得记 N/A、不得风险放行**；B16 的反例集相应改为三态各有反例（含"验未被误记为 N/A"）。由 B 拆计划定向复审（第 13 次 codex 调用）核出。 |
| 2026-08-16 | Claude（Fable 5） | A 修订（`DHR-A-05` 落点收束，已确认并提交 `25b2ec8`）。起因=用户指出「relay 作用到业务项目后会在每个模块下建 relay 文件夹；聚合分析那种 20 多个模块的仓会四散；有些接力计划涉及多个模块」。**D21**：业务仓内 relay 只保留两个落点——`<repo>/.dh-relay/<run_id>/`（运行现场，不进 git；**由 `.dh-runtime/relay/` 换根**，原路径挂在 dh-crew 的现场根下、与决策 14 矛盾）与 `<repo>/docs/relay/`（接力留档，进 git，`README.md` 为全仓总目录 + `runs/<run_id>/`）；**各模块目录下零 relay 自建文件夹**；卡级 dev-harness 工件位置不变（它们是卡本身的工件、非 relay 记录，挪走将与决策 2 冲突且 `dh` 体检红），只在该卡 `progress.md` 留一行指针。连带解跨卡写入冲突：**新增 run 收尾脚本节点**，run 终态时在最后一次暂存分支上单写 `runs/<run_id>/` 并向 README 追加一行、随该次 CAS 快进合入（各卡 finalizer 并发写同一跨卡文件必冲突）。落点纪律进 B9 反例集（模块下出现 relay 文件夹即失败、不得写 `.dh-runtime/`），留档一致性进 B10（README 行与 runs 内容 == 事件账独立重算、只出现一次）。§1.4 增范围项 12、§1.5 增 3 行、§2.1 增"run 尾"棒、§7 增落点纪律。**R10 定向复审 9 P1 全采纳并回改**：① run 尾改为**独立 run 级 archive 脚本节点**（自建暂存分支 + 同仓 finalizer 锁 + 仓级 archive lease；归档 CAS 与 B10 oracle 均过才进 done/cancelled，失败进 `archive_blocked`）——原写法在 finalizer 已删暂存分支之后无分支可写；② 所有 settled 终态自动归档，closeout 前被取消的卡记 `cancelled-exempt`、不要求补跑教训棒；③ README 按 `run_id` **幂等 upsert** 而非 append，产出 owner=`run:<run_id>` 的 `archive-manifest`、路径集与卡级 release manifest 不相交；④ **B9 重写**——业务仓内容白名单**必须含卡授权的业务代码路径**（原白名单漏掉施工棒改代码、会让合法真实卡误判失败），并拆"业务仓内容策略"与"宿主外副作用策略"，取证改为受限写环境+前后快照+git diff+独立 oracle 四者联证；⑤ "模块下 relay 文件夹即失败"收窄为**只禁本次 run 新建、带 relay marker 的非规范落点**，补"仓内本就有合法 `relay` 模块"与"预存同名目录"两条不得误杀的反例；⑥ **`.gitignore` 前置**——实测仓根只忽略 `.dh-runtime/`、`.dh-relay/` NOT_IGNORED，故要求锚定 `/.dh-relay/`，缺失即 `start_rejected` 且 relay 不自改业务仓 `.gitignore`；⑦ **resolver 双根**——实测 legacy 根现存 5 个目录（含 3 个可读 v1 run），v2 只写新根，legacy 根只供 discovery/status/归档读取、resume 硬拒、零新写；⑧ authority snapshot **逐卡冻结 `module_slug`/`devplan_path`/`workspace_path`/`verify_scope`**，跨模块 run 的 `dh` 体检、DevPlan 回填、workspace 推导与 `verify(<scope>)` 均据此，不再猜；⑨ 回填 planning-event 状态账（A-04 已确认并提交 `49615e2`）。**并按 R10 审 U1/U2 更正两处表述**：改称"两个 relay **自有内容根**"并补完整副作用表（relay 写入面不止两条路径）；换根理由改为"由 legacy 共享 runtime 根迁至独占根以取得更强物理隔离"，**不再宣称 P1 违反决策 14**——P1 `design/01` 本就把 `.dh-runtime/<dh-relay 命名空间>` 定义为独立命名空间。 |
| 2026-08-16 | Claude（Fable 5） | A 修订（`DHR-A-04`，已确认并提交 `49615e2`）。来源=B 拆计划交叉审核（R7）+ 两轮定向复审（R8/R9）+ 用户七项新拍板（D14～D20）。① **补验收项**：`B14` 授权工件与授权边界（不可变 authority snapshot、含 DevPlan/design Git blob 哈希、TOCTOU 反例、planner 语义等价）、`B17` 落户 8 件套完整性（自带逐文件非占位谓词，**不挂靠 `dh` R15**——R15 只查文件存在）、`B18` 教训复核棒真实执行与成功/无候选/失败三终态。② **原子化拆分**：`B7`→拆出 `B15`（诊断 agent 可信只读承载 + 控制事件载荷/目标/单次授权绑定 + console 另立 schema）；`B13`→拆出 `B16`（远端 test 受控 push 与补偿）。③ **验收二分**：远端 ACL 拆成 B16 机器项 + 新增人判项 `H5`，共享 `acl_evidence_id`、结论独立、合取才放行**首次 push**（前置 fail-closed，不留到收口）。④ **用户拍板 D14 起**：开跑方式改为"对话里授权、主会话落授权工件并代起"（不敲命令行）；远端 push 改为**项目属性驱动的条件分支**（仓有远端就做、没有就退化为本地合并 + 窗口协作，实测 dh-crew 与 infohub-vps-services 当前均无远端）；集成端到端棒定位改为**人验/帮 AI 取证的动作**。⑤ **消歧**：B1 只管 schema 形状、B4 只管 registry/profile 执行侧、B14 只管授权语义、B3 明确不含 lesson_review。⑥ 卡数估算 8～10 → **14～16**（估算非闸，硬约束只有"一卡一个安全边界"）；决策清单增 8 行；§7 信任登记逐项挂独占验收 ID；§4 补授权校验与教训复核两条流转；§3 增授权两件套（agent 写 request / 宿主写 snapshot）、profile 能力位、option 载荷哈希、`acl_evidence_id` 契约。⑦ **R9 定向复审 6 P1 全采纳**：授权工件创建边界闭合（agent 只写 request、宿主建 run 时才生成并锚定 snapshot、CLI 禁止凭 `-Task` 自造）；snapshot 哈希改**两级**（整文件 blob 证来源 + 每卡片段哈希证影响面，因一份 DevPlan 常承载多卡）；D17 失配改**三分**（工件被篡改=全 run 停 / 卡片段变=冻该卡及其依赖闭包 / 整文件变但片段未变=只记事件），并补安全停点（每次 launch、push、finalizer 最终 CAS 前重验；已 push 先补偿；finalizing 走 closeout_blocked）；D15 把**项目适用性**与**安全前置条件**分账（只有"确无可用远端"可记 N/A，缺白名单/授权/ACL/H5 一律记未授权·未验证并禁 push，不得绕红线）；D16 补人判结论的合法写者（每个需人判 E-ID 必须留用户原文 verdict，agent 与需求复核棒不得代判）；主会话转录卡清单登记为弱信任并加"宿主窗口再展示精确清单核对"。⑧ **用户拍板 D18/D19/D20**：**D18**=relay 改**独立 detached 宿主**（主会话结束/终端全关仍继续推进），配 PID/lease、同 run 单宿主、`relay stop`、lease 过期接管、`relay status` 三态，验收挂 B7；**D19**=主会话转录卡清单登记为弱信任，宿主建 run 前在窗口展示精确卡清单请用户核对，验收挂 B14；**D20**=P2 不专门搭真实测试 Git 服务，B16 只在本地临时仓+裸远端验机械链，"真实 Git 服务 ACL 能否锁死到 `refs/heads/test`"诚实标未验证、H5 记 N/A，交付不得写"B16 通过"。 |
