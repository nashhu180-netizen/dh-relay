<!-- dh:v1 -->
# DHR-A-27 · fresh 只读审核 brief

> 你是**审核 worker**，不是主控。只读：不改任何文件、不拉终端、不派活、不问用户。
> 结论**直接写在你的回复正文里**（不要用 heredoc 打印，TUI 会折叠）。
> 只写事实与级别，**不替主控做裁决**。级别：P0 安全/正确性；P1 会导致设计不成立；P2 论证或证据不足；P3 建议。

## 你在哪

- 仓库：`D:/MyFiles/ai-workflow/dh-relay`（分支 `master`，HEAD `e94a806`）
- **审核对象**：`docs/modules/dh-relay/design/drafts/DHR-A-27-目录信任自动放行-候选.md`
- 必读：仓根 `AGENTS.md` → 审核对象全文 → `docs/modules/dh-relay/backlog.md`（`DHR-BL-2` 与前一条 `DHR-BL-*` 里关于「不能让 relay 自动作答，IHSR_05 RB-3 代答落错选项」的登记）
- 相关既有冻结件：`design/06`（H1/H5 启动期人工暂停）、`dev_plan/P6-*.md` 的 `B-22`②（禁止读写产品配置）与 `B-32`（安全暂停语义）、刚收口的 `DHR_68`（`workspace/DHR_68/`，verify `aa7599e`）

## 背景

用户要求把「新目录信任」从 DHR_68 刚做完的「安全暂停 + 人工确认」改成**自动放行**。原话三句：①「不是等人处理，直接信任」；②「任何目录都信。因为目录是用户自己开的」；③理解对齐时「产品弹的所有目录都信任」。第三句把范围钉死为「只放行**目录信任**这一类框，但覆盖会话全程、不限启动那一次」。

这是**安全语义变更**，所以走 A-full，不走 B-adjust。本草案尚未确认。

## 请重点攻击这几处（主控自认为最脆的地方）

1. **§2 路线 B 的三条护栏够不够**：fail-closed 文本匹配、禁止盲发回车（必须导航到肯定项）、放行次数上限。`backlog` 里明确记着 `IHSR_05 RB-3` **代答落错选项**的前科——这三条能不能真的对冲掉它？有没有第四条必须补的护栏？特别是：怎么保证"命中的是目录信任框"而不是"长得像目录信任框的别的框"？
2. **§1.3.4 的硬边界能不能守住**：「只放行目录信任、其它审批一律不代答」。实现上如何保证不外溢？匹配器登记表本身会不会变成一个"越加越宽"的口子？
3. **§1.2 的事实登记是否准确**：答"信任"之后产品实际放开了什么（Claude 的 `.claude/settings.json` hooks、项目级 MCP；Codex 的 `.codex/config.toml`、`AGENTS.md`）。有没有写多或写少？有没有更严重的后果没被登记？
4. **D-1 的收窄论证是否成立**：主控主张「路线 A（预置）单独使用已不满足诉求，因为 agent 中途进入的嵌套仓路径 relay 在 launch 时不知道，无从预置」。这个论证对吗？有没有反例（比如产品是否支持通配/全局信任开关，使预置仍可覆盖）？
5. **§3.1 审计字段闭集**：够不够事后定责？`matcher_id` 有没有意义？有没有该记而没记的（比如"当时选中的是哪个选项标签"）？记了会不会反而泄露？
6. **§4 验收是否真的钉住了命题**：M1~M8 有没有"看起来很多但没打中"的；人判 H1~H4 是不是真的只能由人判、有没有该拆成机器项的（按 dev-harness 的验收二分）。
7. **有没有被漏掉的失败模式**：比如自动放行成功但 agent 随后仍 blocked（另一个框）、放行与 DHR_68 的 `instructionPending` 时序打架、开关在运行中被关掉时的半途状态。
8. **不可证的话有没有说过头**：草案里有没有超出证据的断言。

## 产出格式

```
## 结论：通过 / 有条件通过 / 不通过
## 逐节意见（§1 目标与冻结决定 / §2 机制与 D-1 / §3 审计与撤销 / §4 验收 / §5 反例 / §6 拟议 B / §7 与既有冻结件关系）
## 发现（表：ID | 级别 | 位置 | 问题 | 为什么是问题 | 建议）
## 对两个待决策的独立意见（D-1 路线；D-2 默认开的作用域）
## 我实际读了什么
```
