# 薄计划与显式节点边界 · 正式晋升决策记录

<!-- dh:planning-event:v1 id=DHR-A-18 stage=A-prime artifact=design/records/02-薄计划与显式节点边界-正式晋升决策记录.md review=../evidence/13-薄计划与显式节点边界-交叉审核记录.md#review-a18 understanding=../evidence/13-薄计划与显式节点边界-交叉审核记录.md#understanding-a18 -->

## 决策

- **DR-ID**：`DHR-DR-02`
- **日期**：2026-08-26
- **来源事件**：`DHR-A-17`
- **决定**：用户整版确认后，将`design/10-薄RelayPlan与显式节点边界-产品设计调整.md`作为唯一当前正式设计输入；上一版09转为历史形成稿并移入`design/archive/`。
- **边界**：本决定不授权B-adjust、开发、提交、推送、新根start或扩大DHR_30。

## 主要replacement

| 上一版口径 | 状态 | 当前replacement |
|---|---|---|
| 完整RelayPlan与自包含Contract Brief | `superseded` | 薄RelayPlan + Work Item Ticket定位 + Worker自读workspace |
| Worker可能在同一会话衔接后续阶段 | `superseded` | Node durable收口、Ticket撤权、Worker退出；下一阶段由编排Agent另一次`continue`启动fresh实例 |
| 固定三个Agent实例 | `superseded` | orchestrator/monitor/executor三类逻辑角色；Review Batch允许一个Monitor与最多四个Reviewer Executor |
| 串行执行代码轮2、需求、一致性、教训 | `superseded` | 代码轮1和整改闭合后，四条适用路径按同一Batch并发fan-out、逐路terminal、统一join |
| `HC-3AT-A20`可靠Message Router | `superseded` | `HC-3AT-A25` best-effort Role Relay；无队列、重投、TTL/预算、三阶段送达或聊天审计 |
| 必做路径缺失只能永久阻断 | `adjusted` | 默认仍阻断；只有精确持久用户Approval可形成`path_user_overridden`并以`proceeded_with_user_override`带缺口继续，不冒充pass/N/A或verify |

## 保留约束

- Workflow Engine仍是唯一业务转换裁决者，DHR_30 `HostSessionActor`仍是唯一物理Store写者。
- Result、Handoff、Attention、Approval、Receipt和完整身份链继续持久、可恢复、fail-closed；普通聊天没有业务权威。
- `task_type` Recipe、施工者不自审、fresh/dedicated隔离、需求境、人验与verify闸继续生效。
- tracked `dh_relay/plans/`、`archive/`和ignored `runtime/`的目录决定保留；Resolver迁移验收前禁止新根start。
- design/10 §10列出的legacy canonical ID继续active；mapping只指向当前正式输入。其余历史/冻结ID沿用records/01的不可变去向，其中涉及Message Router的replacement以本记录为准。

## 实施边界

本次只晋升设计并同步README、mapping、AGENTS和既有`.gitignore`精确规则。DevPlan与DHR_30不变。下一步若执行B-adjust，必须以design/10为唯一规划输入，重新拆分Resolver、Workflow/Actor、Launcher、Role Relay、Pair/Review Batch、恢复与E2E；不得把上一版DHR_55/DHR_56范围直接当作已批准开发计划。
