# 目录信任自动放行 · 正式晋升决策记录

<!-- dh:planning-event:v1 id=DHR-A-28 stage=A-prime artifact=design/records/05-A27-目录信任自动放行-正式晋升决策记录.md review=../evidence/31-A27-目录信任自动放行-交叉审核记录.md#review-a28 understanding=../evidence/31-A27-目录信任自动放行-交叉审核记录.md#understanding-a28 -->

## 决策

- **DR-ID**：`DHR-DR-05`
- **日期**：2026-08-31
- **来源事件**：`DHR-A-27`
- **决定**：用户整版确认后，将 `design/13-目录信任自动放行-产品设计与验收.md` 晋升为 `designInputs[]` 正式输入；原候选稿 `drafts/DHR-A-27-目录信任自动放行-候选.md` 就地改名晋升（形成史保留在正文内，不另存草案副本）。
- **边界**：本决定**不授权** B-adjust、开工、代码改动、产品配置写入、真实 Agent、verify、推送或部署。

## 被取代的口径

| 上一版口径 | 状态 | 当前 replacement |
|---|---|---|
| `B-32`：产品弹目录信任框 → relay 记一条 Attention、扣住提交指令、**等人按** | `superseded`（**仅在总开关开启态**） | 启动前把目标 git 项目根**预置**进产品信任表，框不出现、relay 零按键 |
| DHR_68 的启动期人工暂停是唯一行为 | `narrowed` | 降为**回退路径与关闭态行为**，原样保留、不下线 |
| 「relay 不写产品配置」（各设计与历次用户确认反复重申的边界，**不是** `B-22`② 的条文） | `narrowly-unblocked` | 仅放开两产品**目录信任那一个字段**的写入；凭据 / token / 登录态 / 模型与额度配置**一律仍禁** |
| `B-22`②（DHR_32 审计读取范围：允许读非敏感结构、禁读凭据值） | `unchanged` | **本设计不改动它**。原晋升稿把它写成"禁止读取/写入产品配置值"并宣称松绑，是把既有边界夸大后再放行，已按晋升整包复审更正 |

## 用户裁决账（均为对话明文）

| 决策 | 结论 | 是否覆盖了推荐 |
|---|---|---|
| D-1 机制路线 | **路线 A 单用**（启动前预置，零按键） | 与主控原倾向（路线 B）相反；主控原论证已被 [evidence/32](../evidence/32-A27-目录信任能力矩阵-实测.md) 实测推翻，故非覆盖而是纠错 |
| D-2 作用域 | **全局默认开** | **是**——覆盖了 `a27rev1`、`a27rev2` 与主控三方一致推荐的 ③（默认关 + 本机显式开一次）。代价已完整告知并如实登记 |
| §1.3.5 覆盖收窄 | **未覆盖情形停下来等人按**，不叠加路线 B | 与草案一致，无分歧 |

## 晋升时**仍然开着**的事项（不因晋升而闭合）

1. **`F-6809` 是硬前置**：Claude 卡在目录信任框上时 herdr 报「启动成功 + idle」，一个假 `idle` 同时打穿现役两条启动期检测，relay 会把提交指令发进框里 = 静默挂死。**这正是路线 A 的回退路径**，故必须先单独立卡收口，A-27 主卡才能开工。见 `design/13` §0。
2. **P-A1 / P-A2 未做**：两产品信任条目的配置格式实测、并发写协议实测。二者都要读产品配置——虽在本次松绑范围内，但**动手前须单独再向用户报一次**。见 `design/13` §2.2。
3. **一格未实测**：agent 会话中途进入未预置的嵌套仓会不会再弹框。用户已裁定该情形停下来等人按，故不阻塞设计；若将来要测，仍须单独放行真实产品 Agent。
4. **`F-6807` / `F-6808`**（DHR_68 遗留）不被本设计替代，仍按候选-11 另行处理。

## 三轮审核账

21 个独立议题：**采纳 20 · 部分采纳 1 · 驳回 0**。全部只读派发，每轮以 git 基线比对证零写入。其中三条是主控自己的错误（跨产品过度一般化、自相矛盾、只读半份代码就下诊断），已在 `design/13` 正文与 `evidence/31` 登记，未粉饰。
