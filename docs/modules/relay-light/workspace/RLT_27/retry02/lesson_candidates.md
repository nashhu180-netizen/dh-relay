# lesson candidates — RLT_27

待 coder 依据本次真实运行追加，无候选时明示无。

- 候选：终端 agent 的 `idle/done/blocked` 只能作为唤醒信号，不能作为 relay-light 节点完成证据；前台 wait 返回后仍须检查角色专属产物、唯一完成信号与 monitor 账本闭合，且不得向已 working 的 agent 用 `prompt --wait` 假定 turn 归属。
