<!-- dh:v1 -->
# progress — RLT_03

## 日志 (Log)

| 时间 | 谁 | 做了什么 | 证据 | 下一步 |
|---|---|---|---|---|
| 2026-09-10 | 主控 | 用户确认启动 RLT_03；建 `wt/RLT_03` 任务树和 v2 七件工作区，冻结 Terra High 施工 + Opus 审核 | `git worktree list`；`brief.md`；`task_plan.md`；`execution_strategy.md` | 派 Terra 进批 1 |
| 2026-09-10 | Terra construction | 按 worker 合同读取 C-001~C-008；完成 batch 1 的 relay-plan parser 与 structural lint（未进入 batch 2） | E-002~E-005；仅 `tools/relay-light/relay_log.py` 与 `test_relay_log.py` 为本批代码改动 | 等待 batch 1 独立复核 |
| 2026-09-10 | Terra construction rework=1 | 按 batch-1 Opus 的 P1-1/P1-2 与主控裁决返工；严格拒绝任一 stage_id 被别的 active stage 隔断后的表尾重现，不提前实现 RLT_09/A120 表尾放宽；补 A18/A90/A104/A109、superseded 与 lint CLI 覆盖 | E-006~E-010；仅两条允许代码路径及本工作区 evidence 有改动 | `READY_FOR_REVIEW batch=1 rework=1`，停止等待独立复核 |
| 2026-09-10 | fresh Opus recheck | 独立复验 batch 1 rework=1，结论 approved；主控授权同一 construction worker 进入 batch 2 | E-011；`reviews/batch-1-recheck-opus.md` | 施工 batch 2 |
| 2026-09-10 | Terra construction batch=2 | 先以真实 CLI 和深链 A86 辅助修复构造行为红，后最小实现 JSONL 纯追加、七键 schema、19 事件、add/status CLI 及 0/2/3/4 分流；未实现 batch 3 状态机 | E-012~E-015；仅两条允许代码路径及本工作区 evidence 有改动 | `READY_FOR_REVIEW batch=2`，停止等待独立复核 |
| 2026-09-10 | Terra construction batch=2 rework=1 | 按 fresh Opus changes-requested 修复 JSONL 尾行 fail-closed、agent 前缀派生 by、运行期计划 exit 分流与空账本 pending；补读侧 schema/seq、写失败和静态守卫 | E-016~E-020；未进入 batch 3 或完整 RLT_05 status 合同 | `READY_FOR_REVIEW batch=2 rework=1`，停止等待独立复核 |
| 2026-09-10 | fresh Opus batch-2 recheck | 独立复验 batch 2 rework=1，结论 approved；主控授权同一 construction worker 进入 batch 3 | E-021；`reviews/batch-2-recheck-opus.md` | 施工 batch 3，且不进入 batch 4 |
| 2026-09-10 | Terra construction batch=3 | 先取得运行时状态约束及 §5.3 未触发 agent 关闭口径的有效行为红，后最小实现 `(node, agent-name)` attempt、agent 状态转移、节点前置/关闭、触发和依赖闸；未进入 batch 4 或完整 RLT_05 status/A114 决策模式合同 | E-022~E-025；仅两条允许代码路径及本工作区 evidence 有改动 | `READY_FOR_REVIEW batch=3`，停止等待独立复核 |
| 2026-09-10 | fresh Sol batch-3 review / Terra construction rework=1 | 采纳 Sol 四项 closures：决策类事件绑定触发 agent、decision/user_decision 后必须 resume、合法重拉前因后的精确 +1、close agent done 但另一已启动 agent 未终态的 A17；未实现 A85/A114、未进 batch 4/RLT_05 | E-026~E-029；仅两条允许代码路径及本工作区 evidence 有改动 | `READY_FOR_REVIEW batch=3 rework=1`，停止等待独立复核 |
| 2026-09-10 | OMP GLM-5.3-Flash construction batch=4 | 验收矩阵闭合：16 组 brief 条件与 RLT_03 全部 43 个 HC-ID 逐项映射到 test/subtest（见下表）；补 F-022 superseded-pending 判别测试与 F-023 RLT_05 占位注释；另补 help/A24/A47/A88/A64 默认依赖 5 处覆盖缺口；全为 late-added coverage 与注释，无生产行为改动，未开 RLT_05 | E-030~E-033；仅两条允许代码路径及本工作区 progress/findings 有改动 | `CONSTRUCTION_DONE batch=4`，停止等待 node_closed |
| 2026-09-10 | OMP GLM-5.3-Flash construction batch=4 rework=1 | 按 batch-4 Sol 与主控裁决 1~4：先红后绿实现 A18 `plan_loaded` note 非空 `skill=`（退出 2）与 A69 决策 helper note 同 token 必填（退出 2）；补 A45/A64/A84/A87 指定直证与 A88 全合法隔离 fixture；按裁决 5 不实现 Sol P1-3 status 投影与 A88 映射检查，登记 F-030~F-032 规划合同冲突，矩阵"16/16、43/43 全覆盖"更正为未闭合 | E-034~E-037 | `BLOCKED batch=4 rework=1 reason=planning-contract-conflicts`，停止等待主控 |
| 2026-09-10 | OMP GLM-5.3-Flash construction batch=4 rework=1（主控审计补洞） | 主控独立审计证实 F-033 未真闭合：escalate note 无 helper 仍被接受、`decider=other#9` 被当合法 helper。先红后绿最小修复：新增 `_validate_decision_helper`，escalate 强制恰好一个 kind 与实例名一致的合法 token（空/缺/空值/kind 不匹配/双 token 均 A69 退出 2 且不落行），decision/user_decision 必须重复同一标识；保留自由文本、owner、helper lifecycle、resume 行为。Sol 原始真 CLI 序列 `escalate(note='')` 复验 exit 2；旧的 52-green 不再作为本漏口的闭合证据 | E-038~E-041 | 维持 `BLOCKED batch=4 rework=1 reason=planning-contract-conflicts`，停止等待主控 |
| 2026-09-10 | OMP GLM-5.3-Flash construction batch=4 rework=1（batch-4-recheck P2-1 补洞） | 按 `reviews/batch-4-recheck-sol.md` P2-1：在 decider/strategist 两路为 decision 与 user_decision 各补同 kind、合法但不同实例（`decider#2`/`strategist#2`）的反例，断言 rc=2 A69 且 ledger 不增行；状态顺序避开 A60/kind 次生杀死。仅在测试文件改动，未改生产实现 | E-042~E-043 | 维持 `BLOCKED batch=4 rework=1 reason=planning-contract-conflicts`，停止等待主控 |
| 2026-09-10 | 主控 RLT-A-04 / RLT-B-04 正式同步 | 用户明文「你来写入」；正式设计与 DevPlan 把混合 owner 拆为 A126~A130，并把 A73/A62 完整 status 归 RLT_05、A127 template 归 RLT_07；RLT_03 当前合同与矩阵同步为新 ID | design/01；DevPlan RLT-B-04；evidence/05 | `READY_FOR_CONSTRUCTION_REWORK batch=4 contract=RLT-B-04` |

| 2026-09-10 | OMP GLM-5.3-Flash construction batch=4 contract-rework=RLT-B-04 | 窄返工：生产错误编号与测试断言按 RLT-B-04 正式映射迁移——A64→A128、A86→A129、A88→A126、A90→A130；空账本 pending 注释与对应测试只保留 A128/A84、去掉 A73；先红后绿（5 处规则编号失配红→53 tests OK 绿）；矩阵按 RLT_03-owned 42 ID 闭合，清除退役编号与 partial/blocker 叙述；未实现 RLT_05 完整 status、RLT_07 模板、RLT_09 plan amend；未改 design/DevPlan/review、未 commit | E-044~E-045 | `CONSTRUCTION_DONE batch=4 contract-rework=RLT-B-04`，停止等待 node_closed |
| 2026-09-10 | 主控 | 核对 GitHub 无既有 RLT_03 Issue/PR，按用户要求补建 Issue；Draft PR 等 fresh Opus 复核闭合后创建 | [GitHub Issue #3](https://github.com/nashhu180-netizen/dh-relay/issues/3) | 先闭合 Opus P2，再精确提交、推送并建 Draft PR |
| 2026-09-10 | 主控 | 核对 GitHub 无既有 RLT_03 Issue/PR 后补建 Issue #3；PR 待 fresh Opus 结论落盘后以 Draft 创建 | `https://github.com/nashhu180-netizen/dh-relay/issues/3` | 等 Opus review，随后精确提交并创建 Draft PR |
| 2026-09-10 | OMP GLM-5.3-Flash construction batch=4 contract-rework=RLT-B-04 rework=1 | 按 `reviews/batch-4-contract-rework-opus.md` 闭合两项 P2（不改代码）：P2-1 更新 F-003 活动口径——退役 A86 改为 A129，冲突重述为 design §3.5(:409) 与 §11(:1132) 对 A129 是否含 §4.5 表尾豁免的文本分歧，本卡按 §11 严格执行、放宽归 RLT_09/A120，状态保持 open；P2-2 矩阵收口句去掉「无 partial」无限定宣称，改为 A128 四例外限定：A46/A72/A75 逐项闭合，A120 归 RLT_09（superseded 行隔开通过、同 stage 表尾追加按 §11 严格拒绝、须 RLT_09 显式反转）；历史记录旧号按原样保留 | E-046 | `CONSTRUCTION_DONE batch=4 contract-rework=RLT-B-04 rework=1`，停止等待 node_closed |

## 证据账本 (Evidence Ledger)

| ID | 类型 | 命令 / 路径 | 结果 | 支撑什么结论 |
|---|---|---|---|---|
| E-001 | session-run | `git worktree list` + `git branch --show-current` | observed | RLT_03 工作树已隔离创建 |
| E-002 | rebase / identity | `git rebase --autostash master`；随后 `pwd` / `git branch --show-current` / `git worktree list --porcelain` | exit 0；`wt/RLT_03`、`/home/nash/work/dh-relay/.dh-worktrees/RLT_03`、HEAD `baf2aad6`；autostash 已自动恢复 | 已按 construction worker 入场纪律 rebase，且身份/工作树与 brief 一致 |
| E-003 | invalid-TDD-red | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 1；`ModuleNotFoundError: No module named 'relay_log'`，`Ran 1 test` | 导入错误未触及任何规则断言，明确不作为本批有效 TDD 红证据 |
| E-004 | batch-1 green | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 0；12 tests passed (`Ran 12 tests in 0.012s`, `OK`) | marker/固定双表、默认依赖、superseded、close、DAG、stage/card/type 与 trigger 的 structural lint 已覆盖并转绿 |
| E-005 | static / scope | `python3 -m py_compile tools/relay-light/relay_log.py tools/relay-light/test_relay_log.py`；`git diff --check`；`git status --short` | all exit 0；无 whitespace 报告；主控预置的 DevPlan WIP 保持原样，新增代码仅在两条允许 tools 路径 | 本批语法与 diff whitespace 合格，未触碰 DevPlan / review / 提交边界 |
| E-006 | expected-red / valid behavior | `python3 -m unittest tools/relay-light/test_relay_log.py -v`（仅新增的 `C1,R1,C2` 表尾 A86 反例） | exit 1；`AssertionError: RelayError not raised`；`Ran 12 tests` | 生产模块已存在时，`C#1` 被 `R#1` 隔断后在表尾重现未被 A86 拒绝；这是本批有效行为红 |
| E-007 | batch-1 rework green | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 0；`Ran 16 tests in 0.266s`，`OK` | A86 任意复现均拒；A18 四必填缺失、无 `generated` 正例与 decision_mode 默认/auto/consult/非法值，A104 双反例+三段解析，A109 跨卡后续阶段的各自依赖闭合后可并行，superseded 形态均覆盖。A18/A90 的新增覆盖直接绿，属 late-added coverage，不伪造红 |
| E-008 | lint CLI smoke | `test_lint_cli_smoke_uses_success_and_plan_error_contracts`（同 E-007 unittest） | 合法 plan：exit 0、stdout `lint: ok`、stderr 空；缺 plan 与坏 marker：exit 3、stdout 空、stderr `error: HC-RL-A18 ...` | `RelayError(exit_code, code, message)` 最小落地；计划缺失/解析失败不再误走 `lint:`/exit 2 |
| E-009 | static | `python3 -m py_compile tools/relay-light/relay_log.py tools/relay-light/test_relay_log.py`；`git diff --check` | 均 exit 0 | rework 后 Python 语法与 whitespace 合格 |
| E-010 | scope / deferred IDs | `rg -n 'HC-RL-A(116|89|120)' tools/relay-light/relay_log.py tools/relay-light/test_relay_log.py` | 无匹配 | 未提前实现 RLT_05 A116/A89 或 RLT_09/A120 特判 |
| E-011 | fresh independent recheck | `reviews/batch-1-recheck-opus.md` | approved；R-P0=0、R-P1=0 | batch 1 rework=1 已由 fresh Opus 复验批准；本批按主控指定采纳 R-P2-1 与 R-P3-1~5 |
| E-012 | expected-red / valid behavior | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 1；`Ran 23 tests`，7 failures + 1 RecursionError；包括 add/status 尚未注册、`cards=,` 误退 2、superseded 链放行、1500 节点 DFS 裸 RecursionError | batch 2 和复验补洞均在生产模块已存在状态下先产生可判别行为红，非导入/语法红 |
| E-013 | batch-2 green | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 0；`Ran 23 tests in 2.699s`，`OK` | 连续 20 次 add seq=1..20、旧字节前缀不变、目录仅新增 `relay_log.jsonl`、每行精确七键；19 事件通过、未知/大小写变体拒绝；空/坏账本与 0/2/3/4 分流均覆盖 |
| E-014 | static / syntax | `python3 -m py_compile tools/relay-light/relay_log.py tools/relay-light/test_relay_log.py`；`git diff --check`；禁用 API 精确 grep | all exit 0；无 `.lower()`/`.casefold()`、lock、tempfile、replace 或文件锁 API | batch 2 无枚举归一、锁、临时文件或覆盖写入 |
| E-015 | pycache hygiene | 每次 unittest/py_compile 后删除精确生成的两条 `tools/relay-light/__pycache__/*.pyc` 并删除空目录；`find tools/relay-light -maxdepth 2 -type f` | 仅 `relay_log.py`、`test_relay_log.py` | 未改 `.gitignore`，但当前工作树无 pycache 提交候选 |
| E-016 | fresh batch-2 review | `reviews/batch-2-opus.md` | changes-requested：P1-1 及 P2-1~6；P3-1~4 记录 | 主控裁决限定 batch 2 rework=1，不进入 batch 3 |
| E-017 | expected-red / valid behavior | 新增尾行与空账本 pending 用例后运行 focused unittest | exit 1；尾行未换行 `status` 错误退 0，空账本 JSON 缺 `pending_nodes` | P1-1 与 RLT_03-owned A84 pending 半条在生产模块已有状态下产生可判别行为红 |
| E-018 | batch-2 rework green | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 0；`Ran 29 tests`，OK | 尾行未换行 read/status/add 均 fail-closed 4 且 add 字节不变；by 来自 agent 前缀；读侧七键/seq、runtime-plan 3 vs lint 2、空账本 pending 与真实写失败均有回归网 |
| E-019 | isolated mutation checks | 临时副本分别移除尾行闸、把 by 固定为 monitor、放宽七键精确比较；每个只跑对应 unittest | 三个目标测试均 exit 1 | 新增尾行、by、读侧七键测试具备判别力；临时副本未入仓且已删除 |
| E-020 | static / hygiene | `test_static_forbidden_primitive_and_pane_guards`；`py_compile`；`git diff --check`；每次运行后删除精确生成 pyc | guard 通过；语法与 whitespace 均 exit 0；工作树无 pycache | 禁用原语成为可执行测试，pane 对全 20 行整行 JSON 断言；未改 `.gitignore` |
| E-021 | fresh independent recheck | `reviews/batch-2-recheck-opus.md` | approved；无 P0/P1 未闭环项 | batch 2 rework=1 已获 fresh Opus 批准，主控授权进入 batch 3；报告的三条 batch-4 建议仅登记 findings |
| E-022 | expected-red / valid behavior | `python3 -m unittest tools/relay-light/test_relay_log.py -v`（新增 batch-3 状态测试） | exit 1；`Ran 36 tests`、6 failures：启动前 launch、尝试号重/跳、普通 agent 控制事件、关闭双判据、trigger/dependency、升级归属均被旧实现错误接受 | 生产模块已存在时，batch-3 测试直接揭示缺少运行时状态校验；为有效行为红，不是导入/语法红 |
| E-023 | batch-3 green | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 0；最终 `Ran 39 tests in 14.019s`，`OK` | A50/A49/A58/A59/A60/A68/A69/A70/A77/A78/A17/A74：按 `(node,name)` attempt、四个 agent 名豁免、终态封口、node_start/close、monitor_restart、decider/strategist 归属、触发和依赖闸均有回归覆盖 |
| E-024 | mutation / static / hygiene | 隔离 `/tmp/rlt03-b3-mutation.X5tZ7j`：分别移除 launch 前 node_start 闸、放宽 done 后 checkpoint、跳过 node_close 双判据；各跑对应单测；随后 `py_compile`、`git diff --check` | 三组 mutation 目标测试均 exit 1；真实源码 `py_compile` 与 `git diff --check` 均 exit 0；临时目录及 `tools/relay-light/__pycache__` 精确删除 | 新增状态、终态和关闭测试具判别力；无锁/临时/替换/ pane 合同保持，且无生成 pycache |
| E-025 | expected-red / authoritative-close correction | 新增 `test_node_close_ignores_an_untriggered_agent` 后运行 focused unittest，再最小修改 close 派生 | 修改前 exit 1；`Ran 39 tests`，新用例将 `node_close` 的 exit 2 与期望 0 冲突；修改后纳入 E-023 全绿 | §5.3 条件 1 是本节点已 `agent_launch` 的全部实例终态，而非 agent 表的全部配置行；`close=agent:x` 仍只认 x 的 `done` |
| E-026 | fresh independent review | `reviews/batch-3-sol.md` | changes-requested：P1 A69 ownership、P1 A60 resume 前置、P2 A58/A17 test discriminators；未授权 batch 4/RLT_05 | batch 3 仅进入 rework=1，四项闭环按 Sol 指定执行 |
| E-027 | expected-red / valid behavior | 新增 Sol 指定的 ownership、resume、A58、A17 覆盖后运行 focused unittest | exit 1；`Ran 42 tests`，3 failures：已 launch helper 的 decision 未给 A69，`decision -> done` 与 `user_decision -> done` 错误接受 | 生产模块已存在时获得可判别行为红；A58/A17 新覆盖在已有正确实现上直接绿，登记为 late-added discriminator coverage，不伪造红 |
| E-028 | batch-3 rework green | `python3 -m unittest tools/relay-light/test_relay_log.py -v` | exit 0；最终 `Ran 42 tests in 17.737s`，`OK` | helper identity 只留 note，决策类事件归原 triggering/blocked agent；原 agent 必须 resume 后 done；decider/strategist 的 launch→done 生命周期保留；A58 与 A17 指定反例通过 |
| E-029 | isolated mutations / negative sequences | 隔离 `/tmp/rlt03-b3r1-mutation.bvrS0x`：取消 exact +1、清空已 launch 终态循环、撤销 helper ownership guard、恢复 decision/user_decision→done；各运行目标 test | 四个目标均 exit 1；临时副本已精确删除 | A58、A17、A69、A60 新回归均具判别力；未触及 append-only/no-lock/no-temp/no-pane 合同 |
| E-030 | batch-4 baseline | `python3 -m unittest tools/relay-light/test_relay_log.py -v`（开工前）；`python3 tools/relay-light/relay_log.py --help` | 42 tests OK；help 仅列 `{add,status,lint}`，exit 0 | 进入 batch 4 前 42 条既有测试与三子命令 help 合同均为绿基线 |
| E-031 | batch-4 green / late-added coverage | `python3 -m unittest tools/relay-light/test_relay_log.py -v`（新测试落地后） | exit 0；`Ran 46 tests in 18.390s`，`OK`；保留原 42 条 | 新增 4 条 test：`test_empty_ledger_pending_nodes_exclude_superseded_rows`（F-022/A64/A73）、`test_lint_and_default_dependencies_skip_superseded_rows`（A64 lint+默认依赖）、`test_agent_rows_reference_existing_nodes_with_unique_names`（A24 agent.node 存在/同节点重名）、`test_help_lists_exactly_the_three_frozen_subcommands`；另在既有 A47/A88 测试内补跨节点 close 与 verify-signoff 反例。全部在既有实现上直接绿，按合同如实登记为 late-added coverage；`relay_log.py` 仅加注释与 docstring，无行为改动，故本批无有效行为红可造，不伪造 |
| E-032 | isolated targeted mutations | `/tmp/rlt03-b4-mutation.a7uc9xij`（7 项）与 `/tmp/rlt03-b4-m1-precise.wcomdifs`（pending 精确锚点重跑）：分别移除 pending superseded 过滤、注册第 4 子命令 watch、删除 agent.node 存在闸、删除同节点重名闸、放宽 close 同节点校验、把 verify-signoff 加进 NODE_TYPES、取消默认依赖跳过 superseded；各跑对应目标 test；临时副本与目录已全部删除 | 7 项全部 exit 1：pending 列表差异 `['W1','W2','C1'] != ['W2','C1']`；help 变 `{add,status,lint,watch}` 不再匹配 `\{add,status,lint\}`；两处 A24 闸删除后改报 A75/A24→A75 失配；close 跨节点 agent 被接受（RelayError not raised）；verify-signoff 被放行改报 A24；默认依赖指向 superseded 触发 A72。首轮 pending 变异锚点与 `lint_plan` 的 `active_nodes` 列表推导撞文本，改用 `_status_command` 唯一锚点重跑才得精确判别 | 7 处新增覆盖各自咬住对应守卫；变异未入仓、未触及真实源码 |
| E-033 | static / hygiene / scope | `python3 -m py_compile` 两文件；`git diff --check`；真实 CLI `--help` smoke；禁用原语 grep（`.lower(`/`.casefold(`/lock/tempfile/replace/pane）；RLT_05/09/10 ID grep；`git status --short`、`git diff --name-only master...HEAD`、`git diff --name-only` 三条分开执行 | py_compile 0；diff --check 0；help `{add,status,lint}`；禁用原语零命中；A61/A62 仅出现在 `_status_command` 注释（relay_log.py:718-721），其余 A43/A44/A65/A80/A81/A85/A89/A93/A94/A96/A97/A102/A103/A105-A114/A116/A118-A123 零命中；工作树仅入场既有 DevPlan WIP + 未跟踪 `workspace/RLT_03/` + 未跟踪 `tools/relay-light/`（仅两 py 文件）；master...HEAD 无已提交 diff | batch 4 无语法/whitespace/越界/越 ID 问题；RLT_05 生命周期未实现，注释仅为 F-023 要求的占位声明 |
| E-034 | expected-red / valid behavior | 新增 P1-1/P1-2 行为测试后、实现前运行 `python3 -m unittest tools/relay-light/test_relay_log.py`（含两个新测试） | exit 1；`FAILED (failures=6, errors=2)`，6 处行为断言红：空 note/无 skill token/`skill=`/`version=` 的 plan_loaded 返回 0，helper 链上缺 token/空 token/换 helper 的 decision 返回 0；errors=2 是把测试类名当模块名的 loader 噪音，非断言失败；既有 46 条全绿（fixture 更新为带 `skill=0.1.0` 的 note 后仍绿） | 生产模块已存在时获得可判别行为红，先红后绿成立 |
| E-035 | rework green | `python3 -m unittest tools/relay-light/test_relay_log.py`（实现后；再补 P2 覆盖后重跑） | 48 tests OK → 最终 `Ran 52 tests in 23.343s`，`OK`；命令无管道、无退出码掩蔽 | A18/A69 新闸生效；A45 status 退 4、A64 带/不带 superseded 行 status+lint 等值、A84 零字节与缺失账本 status/lint 全等断言、A87 `len(plan.cards)==2` 直接断言、A88 全合法 kickoff/verify-signoff fixture（唯一违规即 type）均为 late-added 直接覆盖，如实登记 |
| E-036 | targeted mutations（TemporaryDirectory 自动清理） | 隔离副本分别：删除 plan_loaded note 闸、删除 helper token 必填块、读失败吞掉返回空、active_nodes 不再排除 superseded、零字节不再视作空、cards 只取第一张、删除 A88 type guard；各跑对应目标 test | 7 项全部 exit 1：`2 != 0`（plan_loaded / helper token）、`4 != 0`（status 退 4）、`0 != 2 lint: HC-RL-A75`（等值破坏）、`0 != 4 error: ledger`（零字节）、`'HC-RL-A109' != 'HC-RL-A87'`（cards 契约咬合）、`RelayError not raised`（A88 唯一违规证实）；临时目录由 context manager 自动清理，未用 rm -rf | 新增两闸与四组指定直证均具判别力；仓内源码未被变异触碰 |
| E-037 | static / hygiene / help | `python3 -m py_compile` 两文件；`git diff --check`；真实 CLI `--help`；pycache 以精确路径 `rm` 两条 `.pyc` + `rmdir` 清理 | py_compile 0；diff --check 0；help 仅 `{add,status,lint}` exit 0；`tools/relay-light/` 仅余两个 py 文件 | 语法/whitespace/help/卫生全部合格；未触 DevPlan/design/review，未 commit |
| E-038 | expected-red / valid behavior | 新增 `test_escalate_requires_exactly_one_matching_helper_token`（decider/strategist 各 7 个坏 note 反例 + 断言不落行）后、实现前运行全量 unittest | exit 1；`Ran 53 tests`，仅新测试两个 subtest 失败（`2 != 0`，坏 note 的 escalate 被旧实现接受）；其余 52 条（含补 token 后的 batch-3 resume fixture）保持绿 | 主控审计指出的 escalate 空洞在生产模块已有状态下产生可判别行为红 |
| E-039 | audit-patch green | `python3 -m py_compile` 两文件 + `python3 -m unittest tools/relay-light/test_relay_log.py`（修复后） | py_compile 0；`Ran 53 tests in 25.985s`，`OK` | `_validate_decision_helper`（恰好一个 token、`AGENT_INSTANCE_RE` 合法、kind==实例名）接入 escalate 与 decision/user_decision 重复同一标识；自由文本/owner/helper lifecycle/resume 行为保留 |
| E-040 | Sol 原始真 CLI 序列复验 + 对抗矩阵 | 真 CLI、TemporaryDirectory：plan_loaded(skill=0.1.0)→node_start→coder#1 agent_launch→blocked→七类坏 note escalate→legal escalate(decider=decider#1)→decision 重复→user_decision 同 token+自由文本→user_decision 换合法 token | `escalate(note='')` rc=2 HC-RL-A69 且不落行（行数保持 4）；free-text-only/`decider=`/`decider=other#9`/`strategist=decider#1`/双冲突 token/双相同 token 全部 rc=2 + 不落行；legal escalate rc=0；decision 重复 rc=0；user_decision 同 token+自由文本 rc=0；换合法 token rc=2 | Sol 原始反例已被真实关闭；合法链与"换 helper 拒绝"同时成立 |
| E-041 | targeted mutations + hygiene | TemporaryDirectory 隔离副本 4 项变异：escalate 恢复 lenient `_decision_helper`、删除 kind==实例名检查、删除恰好一个 token 检查、删除 decision/user_decision 重复检查；另 `git diff --check`、`--help` smoke、pycache 精确路径清理 | 前 2 项与第 4 项均 `2 != 0` exit 1；第 3 项（single-token 检查删除）也 exit 1 但失败路径是空 token 表 `tokens[0]` IndexError 崩溃而非干净 rc 失配，如实登记；双 token 接受路径另由第 1 项变异（`2 != 0`）独立杀死；diff --check 0；help 仅 `{add,status,lint}`；pycache 精确删除 | 四处守卫均具判别力；旧 52-green 不再作为本漏口闭合证据 |
| E-042 | red-on-mutated-copy / green-on-real | 第一步：TemporaryDirectory 副本将 `relay_log.py` 的 `if note_helper != owners[agent]:` 精确失效为 `if False and ...`（token/kind 校验保持），对扩展后的 `test_decision_events_carry_the_same_helper_token_as_the_escalate` 跑 focused unittest；第二步：真实实现跑全量 unittest | 副本上 exit 1：decider/strategist 两个 subtest 均 `2 != 0`（`decision decider#2`/`user_decision strategist#2` 类不同实例反例被错误接受 rc=0，无次生 kill）；真实实现 exit 0；随后独立全量 `Ran 53 tests in 25.513s`，`OK`；`git diff --check` 0；pycache 精确清理 | 先红（守卫失效副本）后绿（真实实现），测试证据缺口按 Sol 要求闭合；生产实现零改动 |
| E-043 | precise equality mutation killed | 同一失效变异再对目标测试与全量 53 tests 复跑 | 目标测试 exit 1（`2 != 0` × 2）；全量 exit 1、`FAILED (failures=2)` 仅此两项 | batch-4-recheck P2-1 指出的"equality 比较删除后 53 tests 全绿存活"缺口已闭合；mutation 由 TemporaryDirectory 自动清理 |
| E-044 | expected-red / rule-ID discriminable | 测试断言迁移后、生产编号迁移前运行 `python3 -m unittest tools/relay-light/test_relay_log.py` | exit 1；`Ran 53 tests`，`FAILED (failures=5)`：`test_decision_mode_accepts_frozen_values_and_rejects_others`（`HC-RL-A130 != HC-RL-A90`）、`test_stage_must_be_known_and_grouped_contiguously`（`HC-RL-A129 != HC-RL-A86`）、`test_card_and_node_type_are_limited`（`HC-RL-A126 != HC-RL-A88`）、`test_forbidden_types_are_the_sole_violation_in_legal_plans` kickoff/verify-signoff 两 subtest（`HC-RL-A126 != HC-RL-A88`） | 新 ID 断言在旧实现上形成可判别红：旧实现仍以退役编号 A86/A88/A90 报错；仅编号归属迁移、行为与退出码不变，红全部为编号判别 |
| E-045 | rework green / static / scope | `python3 -m unittest tools/relay-light/test_relay_log.py`；`python3 -m py_compile` 两文件；`git diff --check`；`python3 tools/relay-light/relay_log.py --help`；`HC-RL-A(64\|73\|86\|88\|90)` 全仓 `tools/relay-light/` grep；`git status --short` | `Ran 53 tests in 25.989s`，OK；py_compile 0；diff --check 0；help 仅 `{add,status,lint}`；退役编号在 `tools/relay-light/` 零命中；测试后精确清理 `__pycache__`，目录仅余两个 py 文件 | RLT-B-04 编号迁移全量绿且无越界；空账本 pending 注释仅存 A128/A84、无 A73；design/DevPlan 的 `M` 为主控预置 RLT-B-04 WIP，本批未触碰、未 commit |
| E-046 | P2 closure / documentation-only | `git diff --check`；定向 grep：`findings.md` 中 F-003 行 `A129`/`§3.5`/`§11` 命中、open finding 中 `A86` 零活动残留（F-004/F-029/E-044 历史留痕旧号按原样保留）；`progress.md` 收口句命中「A128 限定」「A46/A72/A75」「A120」「须由 RLT_09 显式反转」；`git status --short` 复核 | diff --check 0；F-003 活动口径已重述且状态 open；收口句已为 A128 加四例外限定、移除「无 partial」无限定宣称；生产/测试/review/design/DevPlan/brief/task_plan 未触碰 | batch-4-contract-rework-opus P2-1/P2-2 均按指定闭合；本批零代码改动，无红绿重跑必要（Opus 建议的 53 tests 复跑留给主控/下轮按需执行） |

## Batch-4 验收矩阵（brief 16 组条件 → test/subtest 映射）

| # | brief 条件 | HC-ID | 测试 / 子测试 |
|---|---|---|---|
| 1 | 节点号全计划唯一（含 superseded） | A46 | `test_node_number_is_unique_even_when_superseded` |
| 2 | close 仅空或 `agent:<同节点已存在名字>` | A47 | `test_close_requires_an_active_agent_of_its_node`（`all_agents_done` 反例 + 跨节点 agent 反例） |
| 3 | depends_on 存在且无环 | A48 | `test_dependencies_must_exist_and_be_acyclic`（未知目标 + 成环）；`test_deep_acyclic_dependencies_do_not_raise_recursion_error` |
| 4 | 依赖 superseded 必拒；parser/lint 忽略 superseded 并遵守四个封闭例外 | A72/A128 | `test_dependencies_cannot_target_superseded_nodes`；`test_empty_ledger_pending_nodes_exclude_superseded_rows`；`test_lint_and_default_dependencies_skip_superseded_rows`；`test_status_and_lint_match_with_and_without_superseded_rows`；`test_superseded_forms_are_exact_and_point_to_an_existing_node`；`test_superseded_by_cannot_target_another_superseded_node` |
| 5 | 空节点或 agent 全 superseded 必拒 | A75 | `test_each_active_node_needs_an_active_agent`（零 agent + 全 superseded） |
| 6 | stage 枚举、分组连续、stage_id/card/k、同卡串行/跨卡并行 | A129/A104/A109/A87 | `test_stage_must_be_known_and_grouped_contiguously`；`test_stage_id_card_and_instance_are_structural`；`test_same_card_stages_are_serial_but_cards_can_be_parallel`（含 `len(plan.cards)==2` 直证）；`test_card_and_node_type_are_limited`（A87 反例） |
| 7 | parser/lint 拒绝 kickoff/verify-signoff node type | A126 | `test_card_and_node_type_are_limited`（kickoff + verify-signoff 反例）；`test_forbidden_types_are_the_sole_violation_in_legal_plans`（全合法隔离 fixture） |
| 8 | marker 四必需字段、固定双表、禁竖线、agent.node/重名/默认依赖；decision_mode 缺省 auto 且仅 auto/consult | A24/A18/A130 | `test_fixed_tables_and_cells_fail_closed`；`test_agent_rows_reference_existing_nodes_with_unique_names`；`test_marker_requires_only_frozen_fields_and_allows_missing_generated`；`test_valid_plan_defaults_decision_mode_and_resolves_default_dependency`；`test_decision_mode_accepts_frozen_values_and_rejects_others`；`test_plan_loaded_note_requires_non_empty_skill_token`（A18 账本侧） |
| 9 | trigger 三态与引用校验，on:done 不跨节点 | A35/A71 | `test_trigger_values_and_same_node_done_references_are_checked` |
| 10 | 20 次 add seq 1..20、无重复覆盖、旧行字节不变、无临时文件、无锁 | A37/A38/A39/A40 | `test_add_is_append_only_with_twenty_fixed_schema_events`；`test_static_forbidden_primitive_and_pane_guards` |
| 11 | 19 事件 fail closed、大小写严格、无 lower/casefold 归一 | A2/A41/A42 | `test_all_nineteen_event_words_pass_lexical_validation`；`test_add_rejects_unknown_and_case_changed_events_without_writing`；`test_static_forbidden_primitive_and_pane_guards` |
| 12 | 坏/缺计划三命令退 3；坏账本 status 退 4；空账本与首行 plan_loaded 语义 | A5/A45/A84 | `test_empty_and_bad_ledger_follow_status_and_add_exit_contracts`；`test_runtime_plan_semantic_errors_map_to_exit_three_while_lint_is_two`；`test_lint_cli_smoke_uses_success_and_plan_error_contracts`；`test_unreadable_ledger_status_exits_four`（A45 直证）；`test_missing_and_zero_byte_ledgers_match_for_status_and_lint`（A84 零字节/缺失两路直证） |
| 13 | 每行七字段、agent 格式、配对键、attempt 分配/跳号/重号、不含 pane | A55/A50/A49/A58/A51 | `test_add_is_append_only_with_twenty_fixed_schema_events`（schema+pane）；`test_ledger_read_rejects_extra_keys_and_noncontinuous_seq`；`test_attempts_are_keyed_by_node_and_agent_name`；`test_attempts_are_per_node_and_only_relaunch_after_authorized_causes`；`test_relaunch_attempt_increment_is_exact_after_terminal_causes`；`test_static_forbidden_primitive_and_pane_guards` |
| 14 | node/agent/event 入参与豁免、状态机/终态封口、node_start/node_close/monitor_restart 时序 | A59/A60/A68 | `test_agent_authorization_has_four_exempt_prefixes_and_active_nodes`；`test_agent_state_transition_table_rejects_invalid_next_events`；`test_agent_launch_requires_node_start_and_terminal_agents_are_sealed`；`test_node_close_requires_all_terminals_and_configured_agent_done`（close 一次 + monitor_restart 任意位置） |
| 15 | 控制事件分类、升级链归属、on:done/on:blocked/依赖/node_start 前置、关闭双条件 | A69/A70/A77/A78/A17/A74 | `test_decider_and_strategist_escalations_stay_with_the_triggering_agent`；`test_decision_and_user_decision_must_resume_before_original_agent_done`；`test_decision_events_carry_the_same_helper_token_as_the_escalate`（A69 helper note 必填）；`test_escalate_requires_exactly_one_matching_helper_token`（A69 escalate 恰好一个 kind 一致 token，主控审计补洞）；`test_runtime_trigger_and_dependency_gates`；`test_node_close_requires_all_terminals_and_configured_agent_done`；`test_node_close_names_a_nonterminal_launched_agent_even_when_close_agent_is_done`；`test_node_close_ignores_an_untriggered_agent` |
| 16 | 错误仅进 stderr 且统一 `error: <code> <message>`；add 0/2/3/4 可复现 | A63/A56 | 各 CLI 测试逐条 stderr/stdout 断言（上表 8~15 全部）；`test_add_reports_ledger_read_failure_as_exit_four`；`test_add_reports_genuine_append_failure_as_exit_four`；`test_non_newline_terminated_ledger_is_rejected_without_append`（退 4 各形态；0/2/3 断言分布在 E-012/E-013/E-018 对应测试） |

RLT_03-owned 42 个 HC-ID：A2 A5 A17 A18 A24 A35 A37 A38 A39 A40 A41 A42 A45 A46 A47 A48 A49 A50 A51 A55 A56 A58 A59 A60 A63 A68 A69 A70 A71 A72 A74 A75 A77 A78 A84 A87 A104 A109 A126 A128 A129 A130。上表 16 组 brief 条件已与 42 个 ID 全部对应并闭合（E-044/E-045：先红后绿、53 tests OK）：矩阵无退役编号、无 blocker。A128 限定：四个封闭例外中 A46/A72/A75 已逐项取证并闭合；第四例外 A120（表尾/隔断放宽）按 RLT-B-04 归 RLT_09——其中「被 superseded 行隔开通过」当前已成立，「同 stage 追加在表尾通过」当前按 A129 §11 严格拒绝，须由 RLT_09 显式反转（见 F-003）。非本卡 owner 的边界：完整 status 生命周期（A61/A62/A73/A85/A89/A92）归 RLT_05，五阶段模板（A127）归 RLT_07，运行中改计划（A120）归 RLT_09——三者在生产代码与测试均未实现。

## Node Signal
CONSTRUCTION_DONE batch=4 contract-rework=RLT-B-04 rework=1
