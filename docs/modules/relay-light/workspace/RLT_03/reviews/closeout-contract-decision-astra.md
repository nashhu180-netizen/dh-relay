# RLT_03 正式合同方向裁决顾问报告

日期：2026-09-10。**总裁决：REVISE。六项中 ACCEPT ×3（1、4、6），REVISE ×3（2、3、5），REJECT ×0。** 这是对提案的方向建议，不是正式合同生效、整卡验收或下一卡开工授权。

## 1. 身份、证据与边界

| 项 | 本轮直接观察 |
|---|---|
| 角色 | 正式合同方向裁决顾问；非主控、非施工、非验收签署者 |
| 工作树 / 分支 | `/home/nash/work/dh-relay/.dh-worktrees/RLT_03` / `wt/RLT_03` |
| HEAD | `e4b4cd68c163f32ef4324ea16d49dda53b5f44bc` |
| 可见启动配置 | 从本轮 shell 的 `/proc` 祖先进程读取，codex PID `1967715` 的白名单参数为 `--model gpt-6-astra`、`model_reasoning_effort=medium`；与用户提供的启动参数一致。此为进程配置证据，不声称证明服务端实际路由或推理过程 |
| pane / session | 环境白名单：`HERDR_PANE_ID=w15:pJ`、`HERDR_TAB_ID=w15:t4`、`HERDR_WORKSPACE_ID=w15`、`HERDR_SESSION=kpi-agg`、`CODEX_THREAD_ID=01a08b03-31ed-7e53-9e05-23e43c9a31b5` |
| 真实源码 SHA256 | `relay_log.py`: `f484ffba38504424d5be3c3080b71d83186e31c0d5c6b6e5ed01cbbf117ceb1e` |
| 真实测试 SHA256 | `test_relay_log.py`: `8c2098fcb0e59bb18dcaa9a6912da2373d71b315252fa32a85c531a7e2300dbd` |
| 前轮失败 | 上一个 pane 的 bwrap RTM_NEWADDR 输出不作证据。本轮 `cat`、`nl`、`rg`、`git show` 均已真实执行并读到文件 |
| 唯一写入 | 本报告；未改既有代码、工件或 Git 历史，未 rebase、commit、push、联网、派活或问用户 |

已先读仓根 AGENTS，再读 `workspace/RLT_03/{brief,task_plan,progress,findings,review}.md` 及用户指定三份 closeout recheck 报告。正式对照为 design/01、DevPlan 和 as-built；另读 A04/B04 候选与 evidence/05 追溯形成史，直接读取 `b7f4ecc` 及其父版本核实时间顺序。候选、主控记录和既有 reviewer 结论均不提升为正式业务合同。本轮没有重跑单测；代码轮 2 的 54 tests/变异通过是已读报告的证据，其两条源码 hash 与当前文件相同，不能冒称为本轮执行结果。

下文相对路径均以 `docs/modules/relay-light/` 为根；源码路径以仓根为根。裁决闭集就是以下六项，未将其他 P3 或流程整理纳入推荐包。

## 2. 七个关键 ID 的上下文与唯一 owner

| ID | 正式合同与上下文 | 当前唯一 owner / 冲突 |
|---|---|---|
| A5 | design/01:178–182 区分命令退出码；:1122 要三命令对计划缺失/解析失败退 3，却把节点号重复放进取证例。重复号已在 :400 明列 A46 lint 规则 | RLT_03，DevPlan:168、:516。不能把所有“坏计划”均解释成 lint 退 3 |
| A46 | design/01:400、:494、:496、:502、:505、:1125：含 superseded 的节点号仍占用，全计划不复用；A120 放宽也不得削弱 | RLT_03，DevPlan:144、:532；是 A128 例外之一，不转交 RLT_09 |
| A59 | design/01:277–286、:1167：node 活跃、agent 属于本节点、事件合法；agent 表成员豁免明确四名。:548 又限定 planner-amend 只豁免成员要求，其余校验照旧 | RLT_03，DevPlan:176、:536；“三类”是数量异文，不是第五类或豁免其他校验的授权 |
| A80 | design/01:1181：lint 签名、0/2/3、stderr `lint: <规则编号> <message>`、JSON 结构；结合 :182，规则违反是 2 | RLT_10，DevPlan:546；当前裁决只维护其已有退出码合同，不提前声明完整 A80 已验收 |
| A120 | design/01:1141 两正例（同 stage 表尾追加、superseded 隔行）与四反例（重复号、依赖 superseded、跨阶段反向依赖、同卡阶段并行）；§4.5:512–584 还规定方案过门、planner-amend 一次改完、白名单、三次 lint、当班监工接手、摘要和编排重读 | RLT_09，DevPlan:286–304、:566；第 3 批，依赖 RLT_03/05/07。RLT_16 与 RLT_19 分别承接卡内追加、新卡追加实跑，:99–100、:494 |
| A128 | design/01:1129：活跃 parser/lint 忽略 superseded；A46/A72/A75/A120 四例外封闭，要求逐项覆盖；差分只比活跃结构与退出码，不强求 A72/A48 编号不变 | RLT_03，DevPlan:148、:550。第四项引用别卡验收，不能当成本卡已独立完整证明 |
| A129 | design/01:408–409、:494–496、:584、:1132：阶段枚举与分组连续；正文允许表尾追加，验收负例未限定其边界；全部明确忽略 superseded | RLT_03，DevPlan:150、:551。终态需与 A120 共存；不能把普通物理相邻当作永续产品合同 |

形成史另核：`b7f4ecc^` 的正式 DevPlan 已将 A120 放在 RLT_09、第 3 批；旧 A86 的正文/负例已存在与当前 A129 同类张力。因此运行中追加不是本轮才从 RLT_03 移走，矛盾也不是此次编号迁移才产生。evidence/05 的 B04 Round 1 P2-7 与候选 B04:29 已明确“不提前实现 RLT_09”，但没有足够精确地解决静态 lint 的阶段性可接受集合。

## 3. 六项逐项裁决

### 1）收窄 A5 — ACCEPT

**分类：纯勘误（消除互斥的验收示例）；方向偏移：无，按下述命令边界落账。**

应写成：缺文件、缺 marker/表头等解析级失败，`add/status/lint` 均退 3；计划已解析但违反 lint 规则时，**lint 退 2**。节点号重复由 A46 负例证明，移出 A5 的“三命令退 3”示例。

还必须保留：同一个语义违规计划输入 `add/status` 时退 3，不能把提案“lint 规则违反为 2”泛化成三个命令都退 2。当前 `_lint_command` 与 `_runtime_plan`（`tools/relay-light/relay_log.py:395–415`）确实这样分流。`add` 自身 node/agent/event/时序非法仍按 A59 等退 2，二者不混。

推荐证明矩阵：合法计划三命令走各自正常路径；缺文件/缺表头三命令退 3；重复号 lint=2、add/status=3，且拒绝路径账本不增行。A5 ID 不改 owner；A46 保留；A80 不更名、不移卡、不据此宣称 RLT_10 完成。正式 DevPlan:168、brief #12、review #12、progress 矩阵的“坏计划”活动概括须同步收窄。

### 2）A128 的 A120 实现取证归 RLT_09 — REVISE

**分类：职责分域；方向偏移：分域本身无新增排期偏移，但原句“RLT_03 只闭合 A46/A72/A75”不充分。**

RLT_03 仍须完整证明 A128 自身的核心：活跃结构忽略 superseded、含/不含的结构与退出码对照，以及 A46/A72/A75 三个本卡例外。不能只数“三个例外”便漏掉 A128 主命题，也不能删掉第四项引用后声称原来的“四例外已全部实证”。

推荐替换为：

> A128 由 RLT_03 独占，证明 parser/lint 活跃结构忽略 superseded，并逐项证明 A46/A72/A75；A120 是跨卡兼容性引用，其新增表尾放宽及完整“两正四反”集成取证由 RLT_09 独占，不作为 RLT_03 已实现能力。四项约束清单不增不减。RLT_03 已有的 superseded 隔行通过仍须保留，RLT_09 在 A120 下复验，不重新发一个重复 owner。

这保留“验收 ID 正好一次”：A128 仍只登记 RLT_03，A120 仍只登记 RLT_09；交接的是清楚的能力增量和集成证明，不是把 A128 变成无人负责的跨卡部分签收。只有该分域获正式确认并同步后，才可用修订后的完整本卡命题评价 A128；当前原文下仍不可勾全闭合。

**运行中追加承诺不得缩水**：RLT_09 必须仍覆盖 design §4.5 的当前阶段内追加由当班监工接手、后续阶段由编排重读接手，保持 RLT_16/RLT_19 的实跑路径；不得换成停机重建计划、必须重开阶段、手工把新行插回中间或另设第二个人工批准闸。

### 3）A129 先严格、RLT_09 再放宽 — REVISE

**分类：改变产品行为（阶段性交付的 lint 接受范围限定），兼有职责分域；方向偏移：相对 design 无条件正文是阶段性收窄，相对既有第 3 批交付安排未新增推迟。不能登记成纯勘误。**

必须把“严格连续”限定为**去掉 superseded 行后的活跃 stage_id 连续**。当前代码先做 `active_nodes` 过滤（:311），再查 `stage_runs`（:324–329）；progress:122 也明确 superseded 隔行通过已经成立。把这项延迟到 RLT_09 会退化现有 A128/A129，不能接受。

建议保留正文的产品终态，在 §3.5 映射、§4.3、§4.5.4 与 A129 验收栏同步增加阶段性交付注记：

> RLT_03 交付基础 lint：忽略 superseded 后，同 stage_id 被其他活跃 stage_id 隔断的重现拒绝。RLT_09 按 A120 交付运行中表尾追加放宽后，以 A120 的合法追加正例覆盖该临时限制；只放宽连续性，不放宽其他四项硬约束。RLT_03 的临时拒绝不代表最终产品禁止运行中追加。

**“RLT_09 接管”必须是可审计的合同/实现/取证切换点**，不是聊天里说接管就算产品具备能力。它不是新增运行时 flag、版本配置或全局阶段状态；不应把 RLT 施工卡号写进生产运行分支。

对既有 reviewer 的“删除/反转该表尾反例”建议需要校正：`test_relay_log.py:346–375` 的表尾子例是 C1→R1→C2，且 **C2.depends_on=R1**。这不仅含物理 stage 重现，也把 C 阶段节点依赖指向后续 R 阶段；design:496/:1141 明确不放宽该硬约束。故不能保证原 fixture 原样翻成通过。RLT_09 须另造满足全部其他规则、只有表尾位置差异的合法追加正例；原多违规例仍应拒绝，必要时仅变更实际负责的规则断言。枚举非法反例继续由 A129 守住。

最低交接断言：① superseded 隔行始终通过；②其余条件合法的同 stage 表尾追加，在 RLT_09 前后按正式版本记录拒绝→通过；③ A120 四项硬约束全部保持拒绝及有效编号；④已有 A46/A72/A75/枚举及依赖回归保持；⑤ RLT_16/19 最终证明运行中能力。缺少这些限定时不接受第 3 项原句。

ID 处理建议：以“基础规则与既存 A120 优先例外”的适用域澄清保留 A129，不另增同义 ID；正式记录需明确旧证据适用版本及边界。若决定永久改变 A129 产品命题、删除硬约束或重分独立验收单元，则应按 design:1304 稳定 ID 规则另作正式变更，不能仍称本包勘误。

### 4）A59 三类改四类 — ACCEPT

**分类：纯勘误；方向偏移：无。**

精确四成员为 `orchestrator`、`monitor`、`planner-amend`、`strategist`，以 `<name>#<attempt>` 使用。正式 design:280/:1167、task_plan 批 3、生产常量 `RELAUNCH_EXEMPT_AGENT_NAMES`（:53–55）与测试 `test_agent_authorization_has_four_exempt_prefixes_and_active_nodes` 一致。DevPlan:176 是数量漏改。

建议不仅改数字，同时列四成员，注明豁免的仅是“agent 名在本节点 agent 表中”；node 活跃、event 白名单、状态机、写者、attempt 等其他约束照常。无代码改动或新增成员。

### 5）as-built 状态词与 owner — REVISE

**分类：纯勘误 + 职责分域同步；方向偏移：校正后无。**

接受两处节点状态 `pending/ready/open/closed/superseded`（as-built:17、:42）改为四态，并明确 superseded 为计划行废弃标记；完整 status 列表排除与计数仍由 RLT_05 的 A62/A73 承担，本次只修说明。

旧号封闭集已逐成员核对：RLT_04 位于 :9、:18、:33、:41、:43、:44、:45、:46、:48、:57，共 10 处；RLT_06 位于 :9、:25、:28、:31、:34、:35，共 6 处。原 04 账本/agent 状态机并到 03，原 06 配置/Recipe 并到 05，依据 DevPlan:54–58、:88–89、:138–239、:498。

但不接受只做 `RLT_04→RLT_03` / `RLT_06→RLT_05` 的字符串替换后宣称 owner 已现役化。as-built **:53 的关闭第 1 行**仍将 node_close 双条件守门给 RLT_05，而正式 DevPlan:183–184 将 A17/A74 给 RLT_03；应同批纠正为“RLT_03 写入前校验，RLT_05 派生可关闭状态/原因”。:45–46 升级链同理：RLT_03 基础状态顺序/归属，决策模式、生命周期/配置、提示词和实跑按现役卡承接，不将整条功能全塞回 03。:41 的 Herdr 状态边界是词表不混用，不能顺着旧号替换成 RLT_03 负责驱动 Herdr。

推荐逐句语义校正这一个 as-built 文件，保留现役 v1 对照来源、差异裁决和禁止改 v1 代码边界。不得把文档同步写成 RLT_05 实现完成。这些遗漏就在用户要求的“owner 更新为现役定义”内，无须扩大到其他对照内容。

### 6）b7f4ecc 治理同步一次性路径例外 — ACCEPT

**分类：流程例外；方向偏移：产品无，流程有一次显式例外，必须限制到既有提交的既有内容。**

独立 `git show` 确认提交全号 `b7f4eccbcd1e0a1ba522fd19fdd07b4918701131`。越出 RLT_03 原允许集（两条 Python + 本卡 workspace）的治理差分恰为以下五份，合计 **336 插入 / 44 删除**：

| 精确路径（仓根相对） | b7f4ecc 增/删 |
|---|---:|
| `docs/modules/relay-light/design/01-RelayLight-产品设计与验收.md` | 25 / 18 |
| `docs/modules/relay-light/design/drafts/A04-RLT03与RLT05验收边界修订候选.md` | 117 / 0 |
| `docs/modules/relay-light/design/evidence/05-交叉审核记录-RLT03与RLT05验收边界.md` | 87 / 0 |
| `docs/modules/relay-light/dev_plan/P1-RelayLight-开发方案.md` | 30 / 26 |
| `docs/modules/relay-light/dev_plan/drafts/RLT-B-04-RLT03与RLT05验收边界-调整候选.md` | 77 / 0 |

evidence/05 的 A04 确认是“嗯，授权”，B04 确认是“你来写入”；DevPlan:33 与 progress:21 交叉一致。它们授权正式写入，不授权 commit/push；后续 progress:24–27 另记录 Issue/提交/推送。用户本次明确前提是“已单独授权”且“不重写已推送历史”，据此支持精确登记；本轮未联网验证远端状态，不将本地 refs 当作远端最新证明。

例外有效的最低内容：事件 RLT-A-04/RLT-B-04、用户授权出处、上述提交全号、五个精确路径、该提交相对其父提交的治理 patch、作用域为该次既有打包。**授权的是这五份既有差分，不是五个文件此后任意内容。** 新增或后续变化仍重新按其真实授权范围校验。

原 `dh:allowed-paths:v1 task=RLT_03` 三条保持；不得添加 `design/**`、`dev_plan/**` 或整个模块目录来让机械检查自动全绿。releasePacket 应分别报告“RLT_03 本卡差分路径合规”与“已授权治理 patch 经一次性例外收纳”，不得报告全部差分原本就在本卡 allowed-paths。机械闸若不会消费例外，必须保留原始 fail 与有权裁决记录，不能伪称原闸 pass。

本例外不是 design §4.5 的运行中 planner-amend 白名单放宽。该白名单对 design 整目录禁入、整份方案不落笔的 A122 规则（design:551–566；DevPlan:289/:304）全部保留；用户在接力外正式批准治理变更与 planner-amend 自行改设计是不同权限来源。本报告不追认未被单独授权的其他写入，也不建议 rewrite/rebase/force-push 已推送历史。

## 4. 最小可落账推荐包（待有权确认后执行）

推荐采用 **1 + 修订后的 2/3 + 4 + 修订后的 5 + 6**，以一个明确的合同补充决定保持正文、验收、owner 和当前证据一致，零生产改动即可准备复验；本报告本身不使补充决定生效。

1. 正式 design/01：收窄 A5 示例及命令边界；A128 主命题/三本卡例外/A120 集成责任明确分域；A129 与 §3.5、§4.3、§4.5.4 同步标明基础交付限制和保留终态。A120 两正四反、§4.5 运行中能力不删不弱化。
2. 正式 DevPlan：同步 RLT_03 A5/A128/A129/A59 活动卡文；在既有 RLT_09 A120 卡文明确接手表尾增量、superseded 已有行为回归与合法 fixture 要求。A128/A129 owner 留 03，A120 留 09；依赖/批次/task_type/允许路径不扩大，验收 ID 不新增。
3. as-built：仅做第 5 项逐句状态/owner 校正，包含 :53 的现役归属遗漏。
4. 本卡 workspace：brief/review/progress 的活动完成条件及矩阵与正式补充决定同步；F-003/F-016 仅在正式文本生效且同路径复验收敛后闭合，保留旧红绿与历史裁决。RLT_09 的未实现能力仍明写未实现。第 6 项例外作为独立治理记录进入 progress/releasePacket，绑定原提交五份 patch；不改历史候选来抹去形成史。
5. 由独立需求/一致性路径核验上述正文与取证边界。已有代码证据可按当前 hash 核对复用；如再改代码或行为测试，重新评价受影响证据，不以本顾问报告替代复核。

**拟正式写入的路径须单独明列**：`docs/modules/relay-light/design/01-RelayLight-产品设计与验收.md`、`docs/modules/relay-light/dev_plan/P1-RelayLight-开发方案.md`、`docs/modules/relay-light/as-built/现役Runner一致性对照.md`，以及本卡已有 `brief.md`、`review.md`、`progress.md`、`findings.md`。若治理流程另要求新证据文件，先确定精确文件名并纳入该次授权；不能从第 6 项旧提交例外推导出本轮新写权限。施工 task_plan 的历史路线不由本顾问改写。

## 5. 需用户明文决定的内容

- **第 2/3 项阶段合同**：是否批准“RLT_03 只交付去 superseded 后的严格基础 lint，合法表尾追加按既有第 3 批 RLT_09/A120 实现取证”；明确保留当前 superseded 隔行能力和最终运行中追加全部承诺。该项是阶段性行为边界选择，不能由主控/顾问以勘误名义代定。
- **正式落账范围**：是否批准按第 4 节精确路径执行六项文本同步。第 1/4/5 项无需另造产品选择题，但本轮顾问只有报告写权限，不能据内容显然正确就写正式文档。
- **第 6 项流程例外**：是否接受 b7f4ecc 五份既有治理 patch 在本次打包中的一次性例外，保持原 allowed-paths 和未来路径闸；不重写已推送历史。这不是再次询问 A04/B04 内容是否允许写入，而是明确其与本卡代码同包的路径处置。

以上决定均**不包括 RLT_03 验收签字、verify、merge、push 或 RLT_05 开工**。当前 DevPlan RLT_05 仍“未开始”，依赖 RLT_03；合同补充及两路复验结束后，由主控另行展示真实收口证据和取得相应确认。本轮到报告交付为止。
