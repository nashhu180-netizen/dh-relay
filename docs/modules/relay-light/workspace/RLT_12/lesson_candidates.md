<!-- dh:v1 -->
# lesson_candidates — RLT_12 Windows Claude 首个真计划端到端 demo

> 仅在施工/复核出现**有证据且可复用**的现场时追加；建工作区（W 前置）阶段不预判教训结论。Linux 预演的 DR-F-001～006 是**下一张卡（RLT_21）的输入**，不在此重复包装成本卡教训；Windows 预演结论见 PR #22。AI 起草、人裁决。

## 教训候选

| ID | 一句话教训 | 状态 |
|---|---|---|
| L-002 | **「谁先记终态，谁就把返工路封了」——复核触发条件不该挂在终态事件上。** 现场：C1 的 coder#1 先记了 `done`（seq 16）；checker#1 则是**在同一条 `done` 事件里报出 FAIL p1=3**（seq 20 的 note 原文即 `check.C1.md FAIL p1=3 p2=0`——对 checker 而言 done 与 FAIL 不存在先后，这正是纪律失误的本体：复核方在报 FAIL 的同时把自己终态化了）。此时 `HC-RL-A70`（`on:done:<X>` 要求 X 的最新事件必须是 `done`）、`HC-RL-A60`（终态不可挂事件）与 `HC-RL-A49`（只有 `agent_lost`/`cancelled` 才准重拉）三条互锁，把节点内返工路当场封死（`findings.md` F-008；账本 seq 16/20/21）。可迁移规则：**凡以「某角色完成」为触发条件的流水，触发信号必须是非终态的「待复核 / 可交接」信号，不能复用终态；同时复核方在判 PASS 前也不记终态**，两头都留活口，返工边才始终存在。去重：Windows 预演 **DR-W-004** 已覆盖「`checkpoint` 不能落 terminal agent（A60 单点）」与「挂 live reviewer 的 `routed_to=<worker>#<n>` 绕法」（`evidence/win-dry-run/README.md:103`）——**本条新增的是 trigger 语义层：该绕法对带 `on:done:` 的复核路整体失效**（实证 `dispatch/monitor-X1.md:206-209`）。RLT_21 L-005 同源但落在**监工执行纪律**（PASS 前谁都不记 done、checkpoint 挂 live 实例）；本条落在**协议 / 计划层**（trigger 语义本身要改，纪律只是没有协议支持时的权宜），二者互补不重复 | ready-for-review |
| L-003 | **【已合并，不独立参与升格计数】指向 → RLT_21 `lesson_candidates.md` **L-006**。** 本条原判：「外部 agent 的审批菜单按文案定位，不按编号」。教训路复核判定与 L-006 **同源且差异不成立**——L-006 的可复用规则段**已经包含**旧派单只写抽象 `<数字>` 的对照证据（`monitor-W1.md:65-66`、`monitor-C1.md:75-76`、`monitor-C2.md:113-114`），「写进模板」是同一条教训的**落地方式**，不是第二条教训。本条仅保留一句作为 L-006 的补充：**派单模板里必须写动态定位（先 `read` 菜单原文 → 定位写着 `Yes (Approve once)` 的那一行的当前编号 → 发键或直接 `enter` 确认已高亮项 → 再 `read` 一次确认菜单消失），不得写占位数字**。现场与证据：`findings.md` F-010；派单 `dispatch/monitor-R1.md:347-350`。**证据形态标注**：支撑「按固定数字发键会误选」的两种菜单形态原文与两次误选到 `Edit command` 的实证，（该现场事实仅存于 2026-09-15 编排会话现场记录（Claude Code 主会话 wA:p1 读取监工 pane 所得），**非仓内文件、非账本内容**）——仓内 `dispatch/monitor-R1.md:347-350` 只有「C 阶段实测踩过」这句自述加一条已冻结的正确做法，**实证一个字不在仓里**，不得当作仓内实测结论引用。 | merged-into:RLT_21 L-006（不独立参与升格计数） |
| L-004 | **工具抖动不等于 agent 失联；改判之前先做对照。** 现场：**监工进程里** `herdr agent wait`/`send-keys`/`get` 间歇返回 `Os { code: 5, PermissionDenied }`，主控侧同样的命令始终正常（`findings.md` F-011）。可迁移规则（**限定监工进程侧**）：**在没有对照实验之前，不得把工具层报错记成 `agent_lost` 之类的状态事件**——状态事件一旦落账就会驱动重拉、计数与止损，误记的代价远高于多等一轮；判断顺序是**先用对照命令证伪「权限配置问题」，再决定是改流程还是改环境**。轮询与阈值口径**不在本条重复**，见 RLT_21 **L-007**（监工侧等待机制：`get`+`read` 轮询、退避重试、连续 3 次才按异常处理）。**范围限定（避免与预演结论打架）**：本条只约束**监工进程侧**；**编排侧的前台阻塞循环按 DR-W-009 维持现状，本条不推翻**——DR-W-009 的结论是「DR-F-005（后台 wait 进程被低内存杀掉）在 Windows 未复现，编排侧 devin 后台 `herdr agent wait` 与前台阻塞循环全程正常，保留前台阻塞循环为主」（`evidence/win-dry-run/README.md:108`）。**证据形态标注**：支撑「这是抖动不是配置问题」的**唯一**依据是那九条对照命令与逐条回报，（该现场事实仅存于 2026-09-15 编排会话现场记录（Claude Code 主会话 wA:p1 读取监工 pane 所得），**非仓内文件、非账本内容**）。**升格前置条件**：九条对照命令与回报未落仓内证据，**升格前须在下一次同类现场重做一次可留痕的对照**；在此之前本条只按「操作建议」升格，**不得按「冻结规则」升格**。去重：与 RLT_21 L-007 同源，L-007 冻结等待机制，本条只留「对照证伪优先于改判」这一层。 | ready-for-review |
| L-005 | **一个阶段实例切成多个节点时，阶段级结论只能由关最后一个节点的那班人写。** 现场：C1 与 C2 同属 `RLT_21:C#1`，`stage_result` 要求该实例节点全部 closed（`HC-RL-A112`），C1 收口时写必退 2（`findings.md` F-009；`dispatch/monitor-C1.md:191-195`；账本 seq 21 无 `stage_result`、seq 33 才有）。可迁移规则：**分批切节点时，在派单里就写死「前置节点只关节点不写阶段结论，阶段结论由末节点承接」，并把前置节点的结论口径显式交接给末节点的监工**；同时要接受「中途看阶段状态会显示无结果」这个副作用，不要为了让它好看去硬关节点。迁移性：中等——只对「一个阶段实例切多批」的计划成立，但这类切法在按验收编号分批时很常见 | ready-for-review |
| L-006 | **【教训路判：降级收窄】外部 agent 的运行时工具会在工作树里自建配置 / 缓存目录，它们同样是越界产物。** 现场：C2 的 Devin 实例把会话权限缓存写成 `.devin/config.local.json`，落在允许路径闭集外，由小审的四集合复核抓出（`findings.md` F-012；RLT_21 `check.C2.md:15-19`；账本 seq 25）。可迁移规则（收窄后只剩这一句）：**派单给外部 agent 时，必须把「工具自建的配置 / 缓存目录也算越界产物、发现即清理」写进纪律**。**降级理由（教训路 P2-4）**：原条目前半句「允许路径复核必须由 checker 真跑四集合（tracked / index / untracked / 累计 diff），不能只核施工者自报的改动清单」**在真计划开跑之前就已是冻结纪律**——`dispatch/monitor-C1.md:104` 与 `:121` 已写死，C2/R1/F1 派单同样带（`monitor-C2.md:154`、`monitor-R1.md:155`、`monitor-F1.md:284`）。本轮发生的是**这条既有纪律生效并抓到了东西**，不是学到了这条纪律，故本条不重复提出，只把它记作该纪律的一个正面命中案例。迁移性：偏弱（具体文件名与 Devin 绑定），但「外部工具会往工作树里写东西」这层是通用的。 | ready-for-review |
| L-007 | **「这张卡要跑几路复核」必须以卡上的 marker 为准，且各处来源要对齐。** 现场：仓根宪章（三路）、relay-light 的 `dh-mapping.toml`（两路）、dev-harness 的 `dh:review-policy:v1`（单轮完整 + 条件定向复查）三份来源对 normal 档给出三种答案，而 RLT_21 的卡只带 `type=normal`、不带策略 marker，复核员按宪章判、报「少跑代码轮 1」（`findings.md` F-013）。可迁移规则：**卡上的类型 / 策略 marker 是唯一裁决口径；配方表只定义某一阶段展开哪几行 agent，不等于整卡必做路径——两者必须用不同措辞写，并在立项冻结时核对三处是否一致**。缺 marker 时的回退路径也要写明，不能让复核员各按各的来源判。**回溯（教训路 P2-5）**：两源冲突**在开跑前就已由 `findings.md` F-005 登记**（`dh-mapping.toml` 两路 vs AGENTS 宪章#5 三路，状态「open 待主会话裁决」）；本条**新增的**是第三源（dev-harness 的 `dh:review-policy:v1`）、卡上缺策略 marker 的实证，以及冲突的**实际代价**（复核员按宪章判、报「少跑代码轮 1」，账本 seq 45/46/50）——不是把已知缺口重记一遍。**引用耐久性**：本条与 F-013 的纯行号引用会随后续提交漂移（实测 F-013 引的 `dev_plan/P1-RelayLight-开发方案.md:398`/`:404` 在 master `2a9c4f4` 上已是 `:400`/`:406`，写入时点准确、偏移来自 RLT_12 合入之后的提交）；**升格时纯行号引用须补锚点文本或钉 commit**，例如 `P1-RelayLight-开发方案.md:400@2a9c4f4`。（用户 2026-09-15 已裁决方向 = 以卡上 marker 为准、三份来源需对齐；对齐到哪一份未裁决） | ready-for-review |
| L-008 | **编排卡也要有记录者，否则全程亲历者的发现全部蒸发。** 现场：本计划四行 scribe 全部挂在业务卡 RLT_21 名下，编排是唯一全程亲历者却按分工不落盘，于是本卡 `findings.md` 与 `lesson_candidates.md` 从 D-start（`ddfc21d`）到五阶段闭合一个字没动，六条现场缺口只活在账本 note、派单和对话里（`findings.md` F-014）。**损失更大的那一半（教训路 P1-2 补）**：`progress.md` **同样只有 D-start 一条历史**——它的**证据账本 E-002～E-008 七个槽位到三路复核时仍全是「待实跑（预留槽位）」**，而同文件自己写着「任何人不得据此宣称已达成」；进度表三行也停在真计划开跑之前。验收能不能成立**直接取决于这些槽位**，比 findings 缺口严重得多。可迁移规则（**已扩写，覆盖编排卡七件套中所有需实跑才能填的文件**）：**一张卡只负责编排、另一张卡承接业务时，编排卡的进度表、证据账本、findings 与 lesson 会全部无人写——因为所有 scribe 行都挂在业务卡名下。计划里必须给编排卡指定记录位，且至少点名四处：`progress.md` 的进度表与证据账本、`findings.md`、`lesson_candidates.md`、`review.md` 的「达成?」列；其中验收槽位（E-ID）的回填责任要落到具体阶段的具体角色，不能留到收口再说。** 落地方式二选一——要么在每个阶段的收尾动作里加一条「编排把本阶段现场与本阶段产生的 E-ID 写进自己卡」，要么在收口阶段固定派一个只服务编排卡的记录者；账本 note 与派单正文是过程留痕，不是结构化发现登记，不能代替 | ready-for-review |
| L-009 | **完成信号文件写在 commit 之后，收口时必然变成 untracked 残留。** 现场：`done.<role>.<node>.md` 独立信号文件这个约定来自 Windows 预演 DR-W-010 的建议（`evidence/win-dry-run/README.md:109`），但它的**副作用在预演里没出现**——信号必然晚于 commit，于是每个节点都稳定地在收口积一批未跟踪文件。F1 收口时实测 `git status --porcelain` 列出六个 untracked 文件，并注明「`done.scribe.R1.md` 是 R1 scribe 写在 commit 之后的信号，**不是遗漏**」（`dispatch/monitor-F1.md:97-109`）。可迁移规则：**完成信号文件按约定晚于 commit，因此收口节点必须拿到一份点名清单逐个 `git add`，禁用 `git add -A` / `git add .`；开工前先自查一次 `git status --porcelain`，清单外多出的文件一律停下报编排，不得自行清理他人工作区。** 迁移性：强——只要用「独立信号文件」表达完成，这个副作用就必然出现。去重：RLT_21 L-001～L-007 与 DR-F/DR-W 十七条均无对应项。（本条由 2026-09-15 教训路复核 P2-7(a) 提出，编排采纳） | ready-for-review |
| L-010 | **「用户对一批工作的整体授权」不等于「每张卡的必做复核已执行」。** 现场：本卡 `review.md` 的「独立复核区」三行（code-round1 / requirement / lesson）复核者、结论、发现级别、报告**四列全部是「待填」**，三路状态全部是「待执行」，而该卡已于 2026-09-15 随用户整体授权 squash 合入 master（`7981556`）；三路复核**一次未跑**就过了合并闸，直到落章之后才补做（三路均 REVISE，合计 P1=8 / P2=16，见 `findings.md` F-020）。本卡的三份复核报告存在本身就是这条教训的直接后果。可迁移规则：**合并闸的判据必须是卡上 Recipe 各路复核区已填结论，而不是对话里的整体点头；复核未跑的卡即便被放行，也要在合并当次显式记为带风险放行并挂补做任务，不能让「待填」状态随 squash 静默进入 master。** 去重：与 L-008 不同源——L-008 讲编排卡**没有记录位**，本条讲**复核未执行却通过了合并闸**，成因与整改都不同。（本条由 2026-09-15 教训路复核 P2-7(b) 提出，编排采纳） | ready-for-review |

> **L-001 已退场，故表内无此行**：它是建工作区阶段（D-start `ddfc21d`）留的占位空位，正文始终是 `<待实跑后填>`，从未填入任何现场，**不构成候选**——按本文件表头「仅在施工/复核出现有证据且可复用的现场时追加」，空位不进升格队列，也不需要人裁决。ID 号不复用。本轮真计划 `rlt12-win-01` 的实跑候选从 **L-002** 起。
>
> **证据口径**：每条挂账本 seq 号 / 文件行号 / commit 作证据；与 RLT_21 的 L-001～L-007 已逐条去重（同源的在本条目内注明差异）。**注意口径与 L-001 占位文本不同**——L-001 原写「每条须挂本卡的 E-ID 现场证据」，实际 L-002 起无一条用 E-ID。这是**有意修订而非忽略**：E-002～E-008 是**人判验收槽位**（见 `progress.md` 证据账本），不适用于事故现场取证，**以本句为准**。
>
> **写入者纪律 · 对话证据下传（2026-09-15 教训路 P1-1 起）**：**凡承重事实仅存于编排会话记录、仓内无据的候选，条目内必须自带与 `findings.md` 同措辞的来源标注（「该现场事实仅存于 20XX-XX-XX 编排会话现场记录，非仓内文件、非账本内容」），并尽可能给出可独立核验的交叉佐证；标注缺失的条目不得进入升格队列。** 样板见 `findings.md` F-008（标注明确 + 说清为何无仓内证据 + 两条交叉佐证：stderr 与 `relay_log.py` 的 f-string 比对、RLT_03 同款原文）。本轮据此整改的是 **L-003**（菜单漂移实证）与 **L-004**（九条对照命令）。

> 状态流：`ready-for-review`（AI 觉得可能重要）→ 人裁决 → `needs-promotion / promoted / rejected`
> `needs-promotion` 不阻塞收尾，但要留在升格队列可见。升格进正式教训库（本仓 = `docs/modules/dh-relay/knowledge/教训库-候选.md`）是**单独的维护动作**，要人批准。
> 写入者纪律（SKILL.md 硬规则 5）：relay 流水下本文件的写入者是 **coder**，scribe 不碰；每份文件在一个节点内只有一个写入者。
