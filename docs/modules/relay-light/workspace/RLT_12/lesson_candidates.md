<!-- dh:v1 -->
# lesson_candidates — RLT_12 Windows Claude 首个真计划端到端 demo

> 仅在施工/复核出现**有证据且可复用**的现场时追加；建工作区（W 前置）阶段不预判教训结论。Linux 预演的 DR-F-001～006 是**下一张卡（RLT_21）的输入**，不在此重复包装成本卡教训；Windows 预演结论见 PR #22。AI 起草、人裁决。

## 教训候选

| ID | 一句话教训 | 状态 |
|---|---|---|
| L-001 | <待实跑后填；每条须挂本卡的 E-ID 现场证据> | ready-for-review |
| L-002 | **「谁先记终态，谁就把返工路封了」——复核触发条件不该挂在终态事件上。** 现场：C1 的 coder#1 与 checker#1 都先记了 `done`，checker 才报 FAIL p1=3，此时 `HC-RL-A60`（终态不可挂事件）与 `HC-RL-A49`（只有 `agent_lost`/`cancelled` 才准重拉）把节点内返工路当场封死（`findings.md` F-008；账本 seq 16/20/21）。可迁移规则：**凡以「某角色完成」为触发条件的流水，触发信号必须是非终态的「待复核 / 可交接」信号，不能复用终态；同时复核方在判 PASS 前也不记终态**，两头都留活口，返工边才始终存在。去重：RLT_21 L-005 同源但落在**监工执行纪律**（PASS 前谁都不记 done、checkpoint 挂 live 实例）；本条落在**协议 / 计划层**（trigger 语义本身要改，纪律只是没有协议支持时的权宜），二者互补不重复 | ready-for-review |
| L-003 | **外部 agent 的审批菜单按文案定位，不按编号。** 现场：Devin / codex 审批菜单选项数量随命令类型变化、编号跟着漂，按固定数字发键会误选（`findings.md` F-010；派单 `dispatch/monitor-R1.md:347-350`）。可迁移规则：**先 `read` 菜单原文 → 定位写着 `Yes (Approve once)` 的那一行的当前编号 → 发键（或直接 `enter` 确认已高亮项）→ 再 `read` 一次确认菜单已消失**；绝不选 bypass / 全放行 / `Edit command` / `Describe change` / `No`，也不为了省事降 `--permission-mode` 重拉。去重：与 RLT_21 L-006 **同源**，建议升格时合并为一条；本卡侧重**派单模板的写法**——旧派单只写抽象「`send-keys <名> <数字>`」（`monitor-W1.md:65-66`、`monitor-C1.md:75-76`、`monitor-C2.md:113-114`）就是误选的直接诱因，模板里必须写动态定位而不是占位数字 | ready-for-review |
| L-004 | **工具抖动不等于 agent 失联；改判之前先做对照。** 现场：监工进程里 `herdr agent wait`/`send-keys`/`get` 间歇返回 `Os { code: 5, PermissionDenied }`，主控侧同样的命令始终正常（`findings.md` F-011）。可迁移规则：**长等一律 `get`+`read` 轮询，不靠长阻塞等待；报错先退避重试同一条命令，连续失败达阈值（本轮口径 3 次）才按异常处理；在没有对照实验之前，不得把工具层报错记成 `agent_lost` 之类的状态事件**——状态事件一旦落账就会驱动重拉、计数与止损，误记的代价远高于多等一轮。去重：与 RLT_21 L-007 同源，L-007 冻结的是监工侧等待机制；本卡侧重**编排侧的判断顺序**——先用对照命令证伪「权限配置问题」，再决定是改流程还是改环境 | ready-for-review |
| L-005 | **一个阶段实例切成多个节点时，阶段级结论只能由关最后一个节点的那班人写。** 现场：C1 与 C2 同属 `RLT_21:C#1`，`stage_result` 要求该实例节点全部 closed（`HC-RL-A112`），C1 收口时写必退 2（`findings.md` F-009；`dispatch/monitor-C1.md:191-195`；账本 seq 21 无 `stage_result`、seq 33 才有）。可迁移规则：**分批切节点时，在派单里就写死「前置节点只关节点不写阶段结论，阶段结论由末节点承接」，并把前置节点的结论口径显式交接给末节点的监工**；同时要接受「中途看阶段状态会显示无结果」这个副作用，不要为了让它好看去硬关节点。迁移性：中等——只对「一个阶段实例切多批」的计划成立，但这类切法在按验收编号分批时很常见 | ready-for-review |
| L-006 | **越界文件往往不是施工者写的，是它跑的工具写的。** 现场：C2 的 Devin 实例把会话权限缓存写成 `.devin/config.local.json` 落在允许路径闭集外，由小审的四集合复核抓出（`findings.md` F-012；RLT_21 `check.C2.md:15-19`；账本 seq 25）。可迁移规则：**允许路径复核必须由 checker 真跑四集合（tracked / index / untracked / 累计 diff）扫一遍，不能只核施工者自报的改动清单**；派单给外部 agent 时，把「工具自建的配置 / 缓存目录也算越界产物、发现即清理」写进纪律。迁移性：偏弱（具体文件名与 Devin 绑定），但「自报清单不可信、要扫四集合」这层是通用的 | ready-for-review |
| L-007 | **「这张卡要跑几路复核」必须以卡上的 marker 为准，且各处来源要对齐。** 现场：仓根宪章（三路）、relay-light 的 `dh-mapping.toml`（两路）、dev-harness 的 `dh:review-policy:v1`（单轮完整 + 条件定向复查）三份来源对 normal 档给出三种答案，而 RLT_21 的卡只带 `type=normal`、不带策略 marker，复核员按宪章判、报「少跑代码轮 1」（`findings.md` F-013）。可迁移规则：**卡上的类型 / 策略 marker 是唯一裁决口径；配方表只定义某一阶段展开哪几行 agent，不等于整卡必做路径——两者必须用不同措辞写，并在立项冻结时核对三处是否一致**。缺 marker 时的回退路径也要写明，不能让复核员各按各的来源判。（用户 2026-09-15 已裁决方向 = 以卡上 marker 为准、三份来源需对齐；对齐到哪一份未裁决） | ready-for-review |
| L-008 | **编排卡也要有记录者，否则全程亲历者的发现全部蒸发。** 现场：本计划四行 scribe 全部挂在业务卡 RLT_21 名下，编排是唯一全程亲历者却按分工不落盘，于是本卡 `findings.md` 与 `lesson_candidates.md` 从 D-start（`ddfc21d`）到五阶段闭合一个字没动，六条现场缺口只活在账本 note、派单和对话里（`findings.md` F-014）。可迁移规则：**任何「一张卡负责编排、另一张卡承接业务」的结构，都要在计划里给编排卡指定一个记录位**——要么在每个阶段的收尾动作里加一条「编排把本阶段现场写进自己卡的 findings」，要么在收口阶段固定派一个只服务编排卡的记录者；账本 note 与派单正文是过程留痕，不是结构化发现登记，不能代替 | ready-for-review |

> **L-001 是建工作区阶段的占位行**（D-start 落盘时留的空位），本轮真计划 `rlt12-win-01` 的实跑候选从 **L-002** 起；每条都挂账本 seq 号 / 文件行号 / commit 作证据，与 RLT_21 的 L-001～L-007 已逐条去重（同源的在本条目内注明差异）。

> 状态流：`ready-for-review`（AI 觉得可能重要）→ 人裁决 → `needs-promotion / promoted / rejected`
> `needs-promotion` 不阻塞收尾，但要留在升格队列可见。升格进正式教训库（本仓 = `docs/modules/dh-relay/knowledge/教训库-候选.md`）是**单独的维护动作**，要人批准。
> 写入者纪律（SKILL.md 硬规则 5）：relay 流水下本文件的写入者是 **coder**，scribe 不碰；每份文件在一个节点内只有一个写入者。
