<!-- dh:v1 -->
# findings — RLT_12 Windows Claude 首个真计划端到端 demo

> 登记施工期发现的合同冲突、范围外事实与风险。F-001～F-003 是**开工前已知、用户 2026-09-14 已明确接受**的缺口（D-001 授权包第 3 项），不阻塞本卡；程序/协议根治归 RLT_21。状态变化由主会话/编排裁决，worker 不自改。**F-008 起**是真计划 `rlt12-win-01`（2026-09-15 实跑，W1→C1→C2→R1→X1→F1，账本 71 行）的现场发现，由编排在计划全部阶段闭合后补记——补记本身的由来见 F-014。**F-016 起**是本卡自身 normal 三路独立复核（2026-09-15，**落章并合入 master 之后补做**，三路均 REVISE）提出的条目，每条在「状态」列末尾注明来自哪一路的哪一条。

## 问题

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|---|---|---|---|---|---|
| F-001 | P2 | **监工无合法 `blocked` 终态 agent**：监工按 `attempt_max=3` 停止后想写 `stage_result outcome=blocked`，被 `HC-RL-A112` 拒绝（该阶段没有处于 `blocked` 终态的 agent）。监工正确停下不写伪终态，但协议此时没有可走的出口。 | Linux 预演 `evidence/linux-dry-run/README.md` DR-F-003 | 用户 2026-09-14 明确接受为已知缺口（D-001 第 3 项）；本卡**按缺口跑、如实记录实际遭遇**，不绕过程序校验、不伪造终态。根治归 RLT_21（`HC-RL-A137`：`blocked`/`failed` 允许节点未关但须合法 `ref=`） | 遗留→RLT_21（已确认） |
| F-002 | P2 | **环境性 `NOT_RUN` 无合法出口**：worker 从未起来（如 codex 沙箱在该环境拉不起，三次重试均 NOT_RUN）时，协议只能走 `blocked → agent_lost` 重拉，没有「环境性 NOT_RUN 交编排」的路径。 | Linux 预演 DR-F-001 / DR-F-003；Windows 侧沙箱形态另见 **PR #22** 的 DR-W-001（codex `--sandbox read-only` 在 Windows 能读不能写） | 用户 2026-09-14 明确接受（D-001 第 3 项）。本卡 plan-reviewer/checker/reviewer 改用 codex `-m gpt-5.6-sol --sandbox workspace-write` 规避，**只读约束由派活 prompt 承担**。根治归 RLT_21（`HC-RL-A138`） | 遗留→RLT_21（已确认） |
| F-003 | P2 | **`launch_fix=` 仅作 note 文本**：`relay_plan.md` 的 `launch` 列与实际启动方式不符时，只能在 `agent_launch` 的 `note` 里写 `launch_fix=<实际启动方式>`；它不是账本字段、不触发 `plan_amend`、不被 lint 校验、`status --json` 也不暴露。 | Linux 预演 DR-F-001 的建议行 | 用户 2026-09-14 明确接受（D-001 第 3 项）。本卡逐节点在 `relay_plan.md` 的 `launch` 列写实际启动方式，`roles.toml` 不改；确需临时改启动方式时按 `launch_fix=` note 记。根治归 RLT_21（`HC-RL-A139`） | 遗留→RLT_21（已确认） |
| F-004 | P1 | **运行中 RLT_21 改 `relay_log.py` 的自指风险**：本卡真计划 `rlt12-win-01` 用 `relay_log.py` 记账，而计划内唯一业务卡 RLT_21 的施工内容**正是改 `relay_log.py`**（`stage_result`/`agent_launch`/决策链校验与 `status` 输出）。若两者共用同一份程序，运行到一半程序行为变了，账本与 status 的判读基准就漂了。 | DevPlan §RLT_21「变更范围」；本卡 `brief.md`「RLT_21 边界」 | **已隔离**：账本程序以 **RLT_12 树**的 `tools/relay-light/relay_log.py` 为准（`--config-dir ~/.claude/skills/relay-light/`）；RLT_21 施工在**另一棵树** `.dh-worktrees/RLT_21`（分支 `wt/RLT_21`），其改动不进入运行中计划所用的程序。实跑期间**不得**对 RLT_12 树的 `relay_log.py` 做任何改动，也不得中途重跑 `install_skill.py --all` 把 RLT_21 的新 skill 内容同步进用户级副本（那会打破 E-001 的 A32 哈希基线） | open（实跑期间持续监控） |
| F-005 | P2 | **normal Recipe 的代码复核路落差**：`dh-mapping.toml` 的 `[stages] R` 的 `dh_nodes` 含 `E1`（代码轮 1），但 `[recipes.normal].reviewers` 只有 `["requirement", "lesson"]`，没有代码复核路名；`[recipes.heavy]` 同样只有 `code-round2` 没有 `code-round1`。而 AGENTS 宪章#5 的 normal Recipe 是「代码轮 1、需求方向、教训」三路。 | `tools/relay-light/skill/dh-mapping.toml`；`AGENTS.md` 宪章#5；`tools/relay-light/skill/SKILL.md` R 阶段模板 | 本卡**不自行发明第三个 reviewer 行**：R 阶段严格按 `[recipes.normal]` 展开 requirement、lesson 两行。dev-harness 侧「批次小审 = 代码复核轮 1 前移」由 C 阶段 checker 的 `check.C<n>.md` 承担——这与 SKILL.md「checker 不做复核」的措辞存在张力。是否需在 R 阶段另派独立 code-round1，**交主会话在编排时裁决并记录**；若判定为协议缺口，归 RLT_21 或后续卡 | open（待主会话裁决） |
| F-006 | P3 | **`dh relay-light` 对 RLT_12 剩两条警告，均为存量结构性限制，非本卡缺陷**：①R16「无测试证据：progress 证据账本未见 类型=test」——本卡交付物是真计划与实跑证据，不写单测，DevPlan 非目标明写「不把单测代替真实 Herdr 操作」，relay-light 全部已合入卡（RLT_01/02/05/07/08/09/10）同样报此警告；②R14「找不到 DevPlan 计划卡验收口径，无法核对 brief 是否漂移」——`dh-check` 的 `devPlanTaskCards` 只认 `> DH_<n> 计划|验收` 这种旧式引用块卡头，relay-light DevPlan 用 `#### RLT_<n> — …` 标题，故整模块解析不到，RLT_01/RLT_03 同样报此警告。 | `dh relay-light` 输出；`dev-harness/tools/dh-check.mjs` 的 `devPlanTaskCards` 正则 | 两条均**不修**：R16 按任务性质属预期；R14 属 dh-check 上游解析限制，改它要动 dev-harness（本仓禁改边界）。brief 与 DevPlan 的口径一致性改由人工/复核者逐字核对（brief 8 条已逐字复制并回链 DevPlan） | 已登记，不阻塞 |
| F-007 | P2 | **D 开工的 DevPlan 户口回填会触发 5 条 R29 STALE 红**：`dh relay-light` 在 DevPlan 未改动时 R29 零命中；一旦按动作-D「回填 DevPlan §3.1 + 刷新 dh:status」改这两处，立刻报 `EVENT_STALE planning-event 必须在当前 HEAD 变化集中新增或修改` 与 4 条 `EVIDENCE_STALE`（review/understanding 目标 `design/evidence/07-交叉审核记录-RLT05合同缺口候选.md` 不在变化集）。D-start 户口回填不是规划事件（不是 B-adjust），按宪章不该也不能顺手新增/改写 `planning-event` marker 或其证据文件来消红。 | 实测：`git checkout -- <DevPlan>` 后 `dh relay-light \| grep R29` 无输出；恢复回填后复现 5 条红。命中规则见 `dev-harness/tools/dh-check.mjs` R29 | **不修**：消红的唯一手段是伪造规划事件或改 dev-harness（本仓禁改边界），两者都比这条红更坏。按「失序不伪装」原则如实登记，交主会话/复核者判定是 dh-check 的规则缺口还是 D 开工流程需要额外动作 | open（待主会话裁决） |
| F-008 | P1 | **复核路 trigger 与终态规则互锁，导致「复核打回后节点内无合法返工路径」**。三条规则互锁：①`on:done:<X>` 的 trigger 校验要求 X 在本节点的最新事件**必须是 `done`**，否则退 2 报 `HC-RL-A70`；②`done` 属终态，终态实例不可再挂任何事件，退 2 报 `HC-RL-A60`；③ 重拉只在前一实例最新事件为 `agent_lost` / `cancelled`（或本阶段已 failed）时放行，否则退 2 报 `HC-RL-A49`。于是「记了 done 才准拉复核」与「记了 done 就不能返工」直接对撞。 | 规则出处：`tools/relay-light/relay_log.py:1662-1665`（A70）、`:1818-1819`（A60）、`:1806-1811`（A49）。实证：C1 节点 coder#1 已 `done`（账本 seq 16）、checker#1 已 `done`（seq 20）之后 checker 结论才是 **FAIL p1=3**（`check.C1.md` P1-1/P1-2/P1-3）；账本 **seq 21** 的 `node_close` note 原文即「节点内无合法返工路径（A49/A60），三条 P1 由编排裁决带入 C2 返工」。派单侧预案见 `dispatch/monitor-C1.md:149-152`、编排裁决见 `dispatch/monitor-C2.md:51-52`。两次尝试的逐字 stderr（**编排会话现场记录（2026-09-15，Claude Code 主会话 wA:p1 读取监工 pane 所得）**，非仓内文件、非账本内容）：监工试图 `add --node C1 --event checkpoint --agent checker#1` 时得 `error: HC-RL-A60 agent checker#1 is terminal`；编排裁决改以 coder#2 重拉、监工执行 `add --node C1 --event agent_launch --agent coder#2` 时得 `error: HC-RL-A49 agent coder is not eligible for relaunch`。两条文案与 `relay_log.py:1819`/`:1811` 的 f-string 一致，同款 A49 原文另见 `workspace/RLT_03/reviews/closeout-code-round-1-opus.md:105` | 处置：C1 如实关闭、三条 P1 整建制转入 C2。C2 起改**「PASS 前谁都不记 done」**，返工 `checkpoint` 挂仍 live 的 checker#1 名下（账本 **seq 25/26/27**，note 带 `routed_to=coder#1`），C2 因此 PASS（seq 29）。该绕法**对带 trigger 的复核路无效**：X1 两行 reviewer 的 trigger 仍是 `on:done:coder`（`relay_plan.md` agent 表 X1 两行），硬闸逼 coder 先 `done` 才能拉复核，返工边只能靠纪律维持（`dispatch/monitor-X1.md:206-209`） | **已定修法（用户 2026-09-15 裁决）**：触发条件从终态 `done` 改成非终态的「待复核」信号，施工者保持 live；复核方同样留活口，不判 PASS 不记 `done`；轮次上限沿用 `rework_max_rounds=2`（`tools/relay-light/skill/dh-mapping.toml:24-25`）。裁决只含这三句，**未**裁决信号名、事件名与码位。**落地须另起任务卡**——RLT_21 已收口，允许路径与验收已冻结 |
| F-009 | P2 | **一个阶段实例切多个节点时，前面的节点写不了 `stage_result`**：`stage_result` 现行实现对**所有 outcome** 都要求该阶段实例的节点全部 closed（`HC-RL-A112`），C1 与 C2 同属 `RLT_21:C#1`，C1 收口时 C2 还没开，照写必退 2。 | 派单冻结处置见 `dispatch/monitor-C1.md:191-195`（原文：「本节点不写 `stage_result`……`RLT_21:C#1` 的唯一 `stage_result` 由关掉 C2 的那班监工写」）；账本实证：C1 只有 `node_close`（seq 21）无 `stage_result`，`RLT_21:C#1` 的唯一 `stage_result` 落在 **seq 33**（C2 节点、monitor#2 写）。同一条限制也是 RLT_21 验收 A137 要拆的那条 | 处置：C1 只 `node_close` 不写 `stage_result`，把该阶段实例唯一一条 `stage_result` 留给收 C2 的那班监工，并在派单里写死。代价是 C1 的阶段级结论要等 C2 才落账，中途看 `status` 会觉得 C 阶段"没有结果" | 已登记（本轮按现状执行；A137 改分 outcome 校验后是否连带放宽本条，归 RLT_21 之后的卡） |
| F-010 | P2 | **外部 agent 的审批菜单编号会漂**：Devin / codex 的审批菜单**选项数量随命令类型变化**，编号跟着变；监工按固定数字答会误选到非批准项。 | 派单原文：`dispatch/monitor-R1.md:347-350`、`monitor-X1.md:434-437`、`monitor-F1.md:402-405`（均写「C 阶段实测踩过」，并禁止选 bypass / 全放行 / `Edit command` / `Describe change` / `No`）；旧派单只写抽象 `<数字>` 未冻结动态定位：`monitor-W1.md:65-66`、`monitor-C1.md:75-76`、`monitor-C2.md:113-114`。漂移实证（**编排会话现场记录（2026-09-15，Claude Code 主会话 wA:p1 读取监工 pane 所得）**，非仓内文件、非账本内容）：coder pane 上出现过两种菜单——长菜单 `1 Yes (Approve once) / 2 Yes, allow <cmd> commands / 3 Yes, always allow <cmd> commands in RLT_21 / 4 ... in all projects / 5 Yes, switch to bypass mode / 6 Edit command / 7 Describe change / 8 No`，短菜单 `1 Yes (Approve once) / 2 Yes, switch to bypass mode / 3 Edit command / 4 Describe change / 5 No`。监工按「只读命令一律选 3」的固定规则答，在**短菜单**上选中的是 `Edit command`，**两次**把 coder 卡在 `↵ run edited command · esc back` 的编辑框，均由编排手动发 enter / 1 解开。另有一次 scribe 的四项菜单 `1 Yes (Approve once) / 2 accept edits mode / 3 bypass mode / 4 No`，`send-keys` 发数字不生效、发 `enter` 确认已高亮项才通过 | 正确做法已冻结进 R1 及之后的派单：① `herdr agent read <名>` 读**菜单原文** → ② 定位写着 `Yes (Approve once)` 的那一行的当前编号 → ③ `send-keys` 该编号（或直接 `enter` 确认已高亮项）→ ④ 再 `read` 一次确认菜单已消失。不降 `--permission-mode` 重拉 | 已登记（R1 起冻结执行，X1/F1 未再复现；对应 RLT_21 教训候选 L-006） |
| F-011 | P2 | **监工侧 herdr 命令间歇报权限拒绝**：`herdr agent wait` / `send-keys` / `get` 在监工进程里间歇返回 `Os { code: 5, PermissionDenied }` 并打断回合；同样的命令在主控侧始终正常。经对照确认是**瞬时故障，不是权限配置问题**。 | 派单原文：`dispatch/monitor-R1.md:210-215`、`:312`、`:351-353`；`monitor-X1.md:422-428`；`monitor-F1.md:391-396`。对照实验（**编排会话现场记录（2026-09-15，Claude Code 主会话 wA:p1 读取监工 pane 所得）**，非仓内文件、非账本内容）：编排让监工依次跑 `herdr agent list`、`agent get monitor-c`、`agent get checker`、`agent get scribe`、`agent get coder`、`pane read wH:p1 --lines 2`、`pane read wH:p4 --lines 2`、`pane read wH:p2 --lines 2`、`agent send-keys checker escape` 共九条，监工逐条回报 1~9 **全部 ok**，据此判定此前的 `PermissionDenied` 是瞬时故障而非权限配置问题 | 处置（R1 起冻结）：长等一律改 `get` + `read` 轮询（每 30 秒一次、一轮约 9 分钟、未完进下一轮不空等），不用 `wait` 做长等；`Os code 5` 退避 5~10 秒重试同一条命令，**连续 3 次**同一命令失败才按异常处理；**不得把工具抖动记成 `agent_lost`** | 已登记（R1/X1/F1 三节点按此执行未再打断；对应 RLT_21 教训候选 L-007） |
| F-012 | P2 | **外部 agent 会在仓内生成允许路径外的文件**：C2 的 Devin 实例在工作树里生成了 `.devin/config.local.json`（本会话权限缓存产物），落在 RLT_21 允许路径闭集之外。 | C2 小审原始 P1-1；复审闭环见 RLT_21 树 `check.C2.md:15-19`（`Test-Path .devin` 为 `False`、四集合不再出现该文件、未改 `.gitignore`）；整改路由见账本 **seq 25**（`checkpoint` note：「routed_to=coder#1 P1-1 删除 C2 生成的 .devin/config.local.json 及空目录，四集合复核」）；产物性质登记见 RLT_21 `findings.md` F-008a 末句 | 处置：按四集合越界复核判为越界产物，已清理并复核确认。启示是 checker 的「四集合允许路径核对」必须真跑，不能只看 coder 自报的改动清单——越界文件往往不是 coder 写的，是它跑的工具写的 | 已闭合（C2 复审 CLOSED） |
| F-013 | P1 | **normal 档「复核几路」有三份互相打架的权威来源**：①仓根 `AGENTS.md` 宪章#5 = 代码轮 1、需求方向、教训**三路**；②relay-light 的 `dh-mapping.toml` `[recipes.normal].reviewers` = `["requirement", "lesson"]` **两路**；③dev-harness 的 `dh:review-policy:v1` marker 又是**另一套**（单轮完整 fresh 复核 + 条件定向复查，代码路唯一为 `code_review`，attempt≤2）。RLT_21 的卡只带 `dh:task-type:v1 ... type=normal`、**不带**复核策略 marker，于是复核员按宪章三路判，报「少跑代码轮 1」。 | ①`AGENTS.md:36`；②`tools/relay-light/skill/dh-mapping.toml:17-18`；③dev-harness skill（仓外，`C:\Users\nash\.claude\skills\dev-harness\`）`SKILL.md:59`、`SKILL.md:113`、`references/动作-D-开工.md:99`、`references/动作-E-收口.md:107`、`references/节点表.md:24-25`。卡上 marker 实证：`docs/modules/relay-light/dev_plan/P1-RelayLight-开发方案.md:398`（allowed-paths）与 `:404`（`dh:task-type:v1 task=RLT_21 type=normal`），全文无 `task=RLT_21` 的 `review-policy` marker。打回实证：RLT_21 `review.requirement.md:88-103`（P1-2「normal 整体必做的独立 code-round1 尚未执行」）与 `:124-134`（P2-3 术语混用），账本 **seq 45/46/50** | 本轮处置：X1 由编排裁决**把代码轮职责合并进 requirement 路**，不另建第三路，并补做有效单测变异实验闭合（RLT_21 `review.requirement.X1.md:42-58`：移除 `cancelled` 后目标断言 RED `Ran 1 test FAILED (failures=1)` exit=1，恢复后全量 `Ran 181 tests OK` exit=0；账本 seq 60/62） | **方向已由用户裁决（2026-09-15）**：该由**卡上的类型 / 策略 marker** 说了算，三份来源需要对齐。用户**未**裁决对齐到哪一份、也未裁决改哪个文件——具体口径与落点待后续立项 |
| F-014 | P2 | **本卡（编排卡）在自己的计划里没有记录者**：`relay_plan.md` agent 表的四行 scribe（C1 / C2 / R1 / F1）output 全部是 RLT_21 的 `progress.md` / `review.md`，cwd 全在 RLT_21 树；编排是本轮唯一的全程亲历者，但按分工不落盘。结果是本文件从 D-start 起到 F 阶段结束**一个字没动**，六条现场缺口只活在账本 note、派单正文和对话里。 | `relay_plan.md` agent 表 scribe 四行（C1/C2/R1/F1，output 列与 note 列均指 RLT_21 树）；`git log --oneline -- docs/modules/relay-light/workspace/RLT_12/findings.md` 与同路径 `lesson_candidates.md` 均只有一条 **`ddfc21d`**（D-start 户口），而计划三次落账的 commit 是 `a463608` / `b1284d7` / `fa799b5`（全部只动 `relay/rlt12-win-01/**`）。**补证（2026-09-15 教训路 P1-2）**：`git log --oneline fa799b5 -- .../workspace/RLT_12/progress.md` **同样只有 `ddfc21d` 一条**——且损失比 findings 缺口更大：该文件的证据账本 **E-002～E-008 七个槽位到三路复核时仍全是「待实跑（预留槽位）」**（其中 E-007/E-008 的证据早在 `dd3ac3c` 就已取得，纯属没回流），进度表三行也全部停在 `2026-09-14`，即真计划开跑之前 | 这是「编排卡该怎么给自己留档」的**结构性缺口**，不是某个 worker 的疏漏：五阶段派单模板里没有任何一行让人写编排卡自己的 findings / lesson。本条与 F-008～F-013 的补记本身就是它的直接后果 | 已登记（本次由编排派 subagent 补写 F-008～F-015 收口；模板层要不要固定一个「编排侧记录位」，交主会话裁决） |
| F-015 | P2 | **本计划收口遗留：RLT_21 的 F-009（A143 复算口径分歧）待用户二选一**。冻结的 light 档四类逐条计级得 `3 P1 + 2 P2`，而验收 oracle 期望 `1 P1 + 4 P2`，只有引入未冻结的「同根去重 / 残留折级」口径才可达；两者不可兼得。 | RLT_21 树 `findings.md` F-009（含逐条计级明细与「卡点 · 待用户裁决」状态）、`check.C2.md:27-37`（P1 CLOSED 转 P2 口径分歧）；账本 **seq 50**（R1 stage_result：「F-009 裁决=冻结逐条计级 3P1+2P2 正确，oracle 未闭合为 P2」）、**seq 62**（X1：「遗留 P2=F-009/A143 口径分歧待用户二选一」）、**seq 70**（F1 收口清单） | 本卡不代判。两个出口由用户二选一：修订 A143 的 oracle 期望为逐条计级结果，或显式冻结一套可执行的归并 / 去重规则。相关可迁移教训已由 RLT_21 L-004 登记 | 遗留→用户裁决（**未确认**：用户尚未在对话里点头，本条不按 DC_46「遗留（已确认）」计） |
| F-016 | P2 | **A32 基线已失效：仓内源与两个用户级安装副本现已不一致**。RLT_12 的 A32 是**时点准入证据**（绑定 HEAD `51d8062`，2026-09-14 23:30），在其取证时点成立；但 RLT_21（`124a5c9`）与 RLT-A-09 / A-10（`a6689f1` / `2a9c4f4`）在 RLT_12 之后推进了仓内源 `tools/relay-light/skill/**`，而两个用户级副本停在原地。当前 master `2a9c4f4` 下五文件里 **4 个已不一致**。 | 逐文件 sha256 三处比对，原始输出见 `review.requirement.md`「我实际跑过的命令」第 1 节；编排独立复核同结果：`SKILL.md`、`references/adapter-claude-code.md`、`references/adapter-codex.md`、`dh-mapping.toml` **四个哈希不同**，`roles.toml` 相同。两个用户级副本**彼此仍完全一致**（五文件全同），且其五个哈希与 `progress.md` E-001 原文**逐字相同**——可交叉验证 E-001 未造假、不一致纯由仓内源前进所致 | **下一张实跑卡（RLT_13 / RLT_17 等）开工前必须先重跑 `python tools/relay-light/install_skill.py --all`**，否则 A32 不成立、且实跑会用到陈旧 skill 文本。该动作是高危写用户级目录，按 DevPlan `:407`/`:429` 与 RLT_12 同闸：**先展示 `%USERPROFILE%` 解析后的两个绝对目标，取得用户当次明确授权后才执行**，并记录命令、退出码、五文件哈希与两份 manifest。本卡**不得**顺手重跑——F-004 已写明中途重同步会打破 E-001 的 A32 哈希基线。定级 P2 的理由（沿用 requirement 路判词）：这是后续卡的正常演进 + 授权闸未开，不是 RLT_12 的施工缺陷 | 遗留→下一张实跑卡开工首步（**未确认**：用户尚未就该重同步给出当次授权，本条不按 DC_46「已确认」计）。来源：`review.requirement.md` P2-1 |
| F-017 | P2 | **越界纠正提交声称「登记为 RLT_12 的编排越界事实」，`findings.md` 却无对应条目**。运行期间 RLT-A-09 的两份候选稿与复核记录曾被误提交进 RLT_12 树，而 `design/drafts/**` 不在本卡 allowed-paths 闭集内（闭集四条见 DevPlan `:378-382`，只有 `relay/**`、两个用户级 skill 目录、`workspace/RLT_12/**`）；随后由一条专门的纠正提交移出，改走规划分支。 | squash `7981556` 内含的越界纠正提交 message 原文：「把 RLT-A-09 候选稿与复核记录移出 RLT_12 树（越界纠正）……`design/drafts/**` 不在 RLT_12 的 allowed-paths 闭集……**本条同时登记为 RLT_12 的编排越界事实**」；但在本次补记前，`findings.md` F-001～F-015 中无任何一条记此事（F-012 记的是 `.devin/config.local.json`，与 `design/drafts/**` 无关）。净 diff 干净可证：`git show --stat 7981556` 的 16 个文件中无 `design/drafts/**` | **交付物是干净的，缺的是那条本该留下的登记，现补记**。经过：规划事件（RLT-A-09）的产物被写进了业务卡的工作树 → 越界 → 移出至规划分支。可迁移规则：**规划事件的候选稿与复核记录走规划分支，不进业务卡树**；承诺过「同时登记为 X」的纠正提交，必须真的在 X 里留下条目。本条本身正是 F-014（编排卡无记录者）的又一个实例 | 已闭合（越界已纠正、净 diff 无残留；登记缺口由本条补上）。来源：`review.code-round1.md` P2-5 |
| F-018 | P2 | **DevPlan 户口回填落在本卡 allowed-paths 闭集之外——判为闭集模板缺口，不是本卡违规**。`7981556` 的 16 个文件里，`dev_plan/P1-RelayLight-开发方案.md`（+3/-3）不在闭集四条内；而该回填是 dev-harness **动作-D 强制要求**的（§3.1 表行 + 头部 `dh:status`）。`brief.md:88` 自开的口子无权扩闭集——同文件 `:64` 自述「DevPlan 是口径的唯一权威定义，本表是施工现场的只读副本」，只读副本不能扩写它所复制的闭集。 | 闭集权威定义：DevPlan `:378-382` 四条，**不含 `dev_plan/`**；改动内容已逐行核过、严格限于两处无外溢（`dh:status` 的「现状」「下一步」两行 + §3.1 的 RLT_12 那一行），DevPlan 其余内容与 `dh:allowed-paths` 块本身零改动；两难处境另见本表 F-007（回填则触发 5 条 R29 红、消红唯一手段是伪造规划事件，故选择如实回填加登记） | **整改指向模板，不指向本卡**：在 DevPlan 的 `dh:allowed-paths:v1` 模板里为每张卡显式豁免「本卡自己的 §3.1 表行与头部 `dh:status` 块」，或在 dev-harness 动作-D 里写明户口回填不受卡级闭集约束——二者取一，别让下一张卡再撞。执行者在两难中选了如实回填 + 登记 F-007，是正确处置 | 已登记（闭集模板缺口，待模板侧承接；dev-harness 属本仓禁改边界，落点需另行立项）。来源：`review.code-round1.md` P2-1 |
| F-019 | P1 | **`close` 判据只认 `done` 事件、看不见结论：FAIL 关闭的节点与 PASS 关闭的节点在账本里长得一模一样**。C1 的 `node_close`（seq 21）是在 checker **FAIL p1=3** 的情况下关的，而双判据（全部在场 agent 有终态 + `close` 列 agent 已 `done`）**字面成立**——`done` 是事件不是结论，程序无从拒绝，`lint` 因此为 ok。**本条不判 C1 的 `node_close` 为违规**：没有伪造终态、没有绕过校验、没有把 FAIL 说成 PASS，编排裁决三处可追溯（账本 seq 21 note、`dispatch/monitor-C2.md:51-53`、本表 F-008）。真缺口是「`SKILL.md:61` 的『checker 通过才进下一批』这条保证**只靠纪律维持、没有程序支撑**」。 | 机器判据原文 `SKILL.md:155`；C 阶段模板原文 `SKILL.md:61`「`close=agent:checker`（checker 通过才进下一批）」；本卡派单写得更死：`dispatch/monitor-C1.md:190`「双判据成立（……`close` 列的 **checker 已 `done` 且结论 PASS**）后……」，实际执行偏离了这份冻结派单。账本 seq 20 的 `done` note 原文即 `check.C1.md FAIL p1=3 p2=0`。**附带同类**：`relay_plan.md:48` 的 X1 节点 `close=agent:requirement` 只钉一路，而 R1 是 **requirement 与 lesson 两路都 FAIL**（seq 43 / seq 46）；本轮两路最终都 PASS（seq 59 / 60）故未出事，但按计划字面，lesson 路若仍 FAIL，X1 照样能关 | 修法方向（**两条一并设计，别拆两次改**）：①让 `close` 列支持「`done` 且结论 PASS」语义，使 FAIL 关闭在账本层面可辨识——例如要求 `close` 列 agent 的 `done` note 带可机检的 `status=PASS` token；退一步，至少要求 FAIL 关闭时补一条**署名编排的裁决事件**，而不是把裁决塞进监工 note 的散文里。②X 类返工节点的 `close` 列必须写全**被打回的各路**，不是只写一路。与 F-008 同源但不是同一条：F-008 说「打回后没路可走」，本条说「关门时判据看不见结论」 | 遗留→**RLT_22**（与 F-008 一起设计；**未确认**：用户尚未就本条点头，不按 DC_46「已确认」计）。来源：`review.code-round1.md` P2-2 |
| F-020 | P1 | **先验收后复核：本卡落章时三路复核全未执行；且 verify 提交正文自相矛盾**。①本卡 `task_type=normal`、卡上无 `dh:review-policy:v1` / `dh:review-scope:v1` marker，按 AGENTS 宪章#5 走三路且三路均 `required`、无豁免；落章「已验收（带风险放行）」并 squash 合入 master（`7981556`）时，**三路一路未跑**（`review.md` 三路状态全「待执行」、独立复核区四列全「待填」）。复核是验收的前置闸，不是收口后的补充材料。②verify 提交 `dd3ac3c` 正文写「**人判材料（H1/H13/H5/H14/H10）已备齐**，见 workspace/RLT_12/progress.md 证据账与本次取证」——该句**不成立**：该时点与其后 E-002～E-006 一直是空槽、`evidence/win-real-run/` 从未落盘、H10 材料从未制作；同一提交**末尾**又写「人类验收未签，五条人判待用户判断」，前后自相矛盾。 | ①`review.md` 合入版的三路登记行与独立复核区（历史形态见 `git show 7981556:.../review.md`）；`AGENTS.md:36` 宪章#5「`normal` 为代码轮 1、需求方向、教训三路……施工者不复核自己的卡」。②`git log -1 dd3ac3c` 正文全文（两句原文如左）；`progress.md` 的 E-002～E-006「未取得」与 `find .../evidence -maxdepth 2` 只有 `linux-dry-run/`、`win-dry-run/` 两个**预演**目录 | 处置：**三路已于 2026-09-15 落章后补做**（`review.code-round1.md` / `review.requirement.md` / `review.lesson.md`，均 REVISE，合计 P1=8 / P2=16），结论已回填 `review.md` 独立复核区，并在该处**如实标注是落章后补做、不曾作为前置闸**。verify 提交**已合入 master、不可改**，据「失序不伪装」原则以 `progress.md` 日志的「更正记录」行与本条如实更正，**不改写历史提交**。可迁移教训已登记为 `lesson_candidates.md` L-010 | 已登记（三路已补做、矛盾已如实更正；**本轮整改尚未经三路复核者复审**）。来源：`review.code-round1.md` P1-3 + `review.requirement.md` P1-3 |
| F-021 | P2 | **非目标「不跑 Codex/Linux」缺「主控」限定词，字面读即越界**。DevPlan 与 `brief.md:28` 的非目标原文无限定词，而实跑中 codex 作为 **worker** 被启动 6 次；Issue #23 的对应行写的是「不跑 Codex/Linux **主控**」，有限定词。 | `brief.md:28`；`gh issue view 23`「范围」段「**非目标**：不跑 Codex/Linux **主控**；不含 watch；……」；账本 seq 7（`plan-reviewer`）、18 / 24（`checker`）、38 / 57（`requirement`）、39 / 58（`lesson`），note 均为 `launch=codex gpt-5.6-sol --sandbox workspace-write`；主控侧全程是 Windows 上的 Claude Code 主会话（seq 1 `plan_loaded` note：`编排=Claude Code 主会话 wA:p1`），无 Linux 参与 | **按 Issue #23 的口径没有越界**（也是唯一说得通的口径——RLT_13 才是「Codex 主控」卡）。缺的是权威文档与其只读副本的措辞：建议把非目标第一条改为「不跑 Codex/Linux **主控**（codex 作为计划内 worker 不受此限）」。这是**口径对齐**，不是范围变更，不改任何验收 ID | 已登记（措辞对齐待 DevPlan / brief 侧承接；本轮整改的 DevPlan 改动范围只限 §3.1 与批次表两处，未动非目标段）。来源：`review.requirement.md` P2-2 |
| F-022 | P3 | **归档的 `relay_plan.md` 有三处文本缺陷，会误导复用者**（RLT_13 若以本计划为范式会原样抄走）：①`:22-23` 把 X 阶段现场追加的执行者写成「**当班监工**」，同句引用的 `HC-RL-A121` 主语却是「编排」；实际执行者是编排（追加落在 `b1284d7`，与 seq 51 `stage_close`、seq 52 `stage_start` 同批），执行无误，是计划文本自相矛盾。②`:22` 写「X 阶段**不在本表预留节点行**」，而 `:48` 的节点表里确有 X1 行，事后读归档件的人无法自证 X1 是运行中追加的（账本 71 行无任何记录计划被改动的事件，`git log -- relay_plan.md` 只有 `7981556` 一次 squash）。③X1 追加除追加节点行与 agent 行外，还**改了一行既有内容**（F1 的 `depends_on` 由 `R1` 改指 `X1`），而 A121 的字面豁免只挂在 **agent 行**上。 | ①`relay_plan.md:22-23` 原文；追加所在提交 `b1284d7`。②`relay_plan.md:22` 对 `:48`；账本事件类型仅十种、无 `plan_amend` 一类。③逐字 diff 见 `review.code-round1.md` §4.4：`git diff a463608 b1284d7` 恰好三类改动——追加 1 节点行、修改 1 行既有节点行（F1 `depends_on`）、追加 4 行 agent 行；`git diff b1284d7 fa799b5` 为空 | 三条均**不判违规**：③是合规的，靠的是计划在生成时（2026-09-14）就把这次追加的完整形态冻结进 header 注释第 6 条（连「同时把 F1 的 `depends_on` 改指 X1」都逐字写了），且该改指是 A109（同卡后一阶段须以前一阶段为祖先）的硬性要求，追加后 `lint` 仍 ok；未写 `plan_amend` 事件也是正确的（`plan_amend` 服务的是 decider / strategist 提案那条工作流）。建议：①「当班监工」改「编排」；②`:22` 第 6 条末尾补一句「（2026-09-15 R1 打回后已按本条现场追加，见节点表 X1 行与账本 seq 52）」消掉矛盾并留回指；③把 A121 的表述扩为「……以及为维持 A109 祖先关系所必需的后继节点 `depends_on` 改指」——每次返工追加都会撞这条，现在只是靠计划作者提前写进注释才没出事 | 已登记（`relay/**` 与 design/01 的改动均不在本轮整改授权范围内，措辞修订待各自 owner 承接；A121 属 RLT_09 owner）。来源：`review.code-round1.md` P2-3 / P2-4 + `review.requirement.md` P2-4 |

> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞
> 遗留（P0/P1 唯一合法路径，DC_46）：状态列写 `遗留→<去处>（已确认）`——「已确认」三字代表用户已在对话里明确点头同意把这条留到别处。F-001～F-003 的「已确认」来源 = 用户 2026-09-14 D-001 授权包第 3 项。
