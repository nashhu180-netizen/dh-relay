# W1 · builder — 建 workspace + 分批可执行 task_plan

先读同目录 `README.md`（身份、允许路径、环境事实、worker 铁律、完成信号格式），再读本文件。**只做 W1，做完即停。**

## 你的产出（全部落在 `docs/modules/relay-light/workspace/RLT_11/`）

1. **`brief.md`** — 本卡业务合同：目标、非目标、验收口径（`HC-RL-A13` 原文引用）、允许路径、依赖、风险与停止边界。信息来源只有两处：开发方案 RLT_11 卡片段、设计 01 的 §12 / §15 / 验收表 A13 行。**逐条回链来源行号或小节号**，不要凭印象转述。
2. **`task_plan.md`** — **分批次、可独立执行、可独立验证**的施工计划。这是本节点的核心交付，要求：
   - 每批 = 一个 coder 能在一个回合内完整做完并自证的最小闭环；批与批之间**不得**有隐式依赖。
   - 每批写清：`批号 / 目标 / 要改的文件（必须在允许路径内）/ 具体动作 / 完成判据（可机械核验）/ 预期证据落点`。
   - 预计 2~3 批。参考切法（你可另行提出更好的切法，但要说明理由）：
     - **C1 退场核对**：用 RLT_12 的真计划产物（见 `docs/modules/relay-light/workspace/RLT_12/`，真计划 id `rlt12-win-01`）对照设计 §12 的「谁删 / 何时删 / 删失败怎么办」逐类核对，并核实 status 无自动删除、「只写不删」是显式选择且给了理由；结论写成一份核对表落到 `as-built/` 相应说明或本卡工作区（落点你定并写进计划）。
     - **C2 教训回流**：把 §15 明列的**三条**教训逐条提进 `docs/modules/dh-relay/knowledge/教训库-候选.md`，**逐条回链来源**（设计 01 §15 + 产生该教训的卡号）。沿用该文件既有条目的编号与格式，续号不复用。
     - **C3（如需要）**：A13 机器证的自证表——把「设计与实现均声明退场路径 / status 无自动删除 / 三条教训逐条落账并回链」三个断言各自对应到可核验的证据行。
   - **禁止**把「顺手整理既有教训库其他条目」「修 F-005 / F-010」写进任何一批。
3. **`progress.md`** — 建文件并追加你自己的 W1 完成信号（格式见 README）。
4. **`findings.md`** — 建文件；本节点发现的范围外问题写这里（没有就写「本节点无」）。

## 硬约束

- **只写不改**：本节点**不改**任何允许路径之外的文件，也**不提前执行** C 批的施工动作（不要现在就去动教训库）。
- 教训库文件 `docs/modules/dh-relay/knowledge/教训库-候选.md` 本节点**只读**，用于确认既有编号与格式。
- 不 commit、不 push、不建分支。git 操作由编排负责。
- 三条教训的原文以设计 01 §15 表格「教训候选（本卡新增）」行为准，**不要改写措辞**，回流时可扩写说明但原意不得漂移。

## 完成

`progress.md` 追加：
```
DONE task=RLT_11 role=builder node=W1 status=OK ts=<ISO8601>
  summary: brief 与分批 task_plan 已落盘，共 N 批
  artifacts: brief.md, task_plan.md, findings.md
```
写完即停，等编排派下一棒。
