# R1 · reviewer — 教训路复核（light 档必做两路之一）

先读同目录 `README.md`。你是**复核**，只读不改代码/文档，做完即停。**不要看施工者的自述结论，自己去核对原始来源。**

## 复核范围

本卡（RLT_11）全部改动。用 `git status --porcelain` 与 `git diff` 自己确认改了哪些文件。

## 教训路判据（逐条 PASS/FAIL，FAIL 给级别 P1/P2 与整改动作）

1. **三条教训逐字保真**：`docs/modules/dh-relay/knowledge/教训库-候选.md` 新增三条的教训表述，与设计 `01-RelayLight-产品设计与验收.md` **第 1378 行**的原句**逐字比对**是否一致？有无缩写、合并、丢限定条件（如「放后台再结束回合 = 监控断线」「停下 / 验收合格」「先改名再设计」）、或改义？
2. **只提三条**：是否严格只提 §15 明列的三条，没有夹带第四条、没有顺手整理既有条目？
3. **回链完整**：每条是否同时回链 ①设计 §15 ②产生该教训的现场卡号/证据，且卡号经核实非杜撰？
4. **编号口径**：是否在原末号之后连续续号、无复用、无跳号；既有条目是否**零改动**（逐字节层面，包括格式与空白）？
5. **教训质量**：三条是否写成了可复用的教训（现象 + 为什么 + 下次怎么做），而不是流水账？
6. **本卡是否遗漏应记教训**：本卡执行过程中（含 W2 审核抓出的 P1）有没有产生值得入库但没记的教训？有则记为发现，**不要自己去加**。

## 产出

写 `docs/modules/relay-light/workspace/RLT_11/review.lesson.md`：

```
## 结论
APPROVE | REVISE   （有任一 P1 即 REVISE）

## 逐条判据
| # | 判据 | 结论 | 级别 | 依据（文件:行） | 整改动作 |

## 范围外发现
```

`progress.md` 追加：
```
DONE task=RLT_11 role=reviewer-lesson node=R1 status=<OK|REVISE> ts=<ISO8601>
  summary: <一行，含 P1/P2 计数>
  artifacts: review.lesson.md
```

## 你不做的事

- 不改代码或文档（含不替施工者修错字）
- 不替编排做验收裁决，只写事实与级别
- 不 commit、不 push、不问用户
