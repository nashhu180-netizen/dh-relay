<!-- dh:v1 -->
# findings — RLT_21

> 只登记事实与建议；状态变化由编排/主控裁决后回填，不由施工者自改。

## 登记项

| ID | 发现 | 影响 | 状态 |
|---|---|---|---|
| F-001 | 承接 RLT_07 findings F-002：`decision_mode` 模式门未在 `relay_log.py` 实现——consult 下 `decision` 后直接 `resume`（缺 `user_decision`）rc=0、auto 下 decider 链出现 `user_decision` 不拒，A96/A114 负例腿要求 exit 2；`test_relay_log.py` 两条 `@unittest.skip` 钉住负例（`test_a114_consult_resume_without_user_decision_rejected` / `test_a114_auto_mode_rejects_user_decision_on_decider_chain`） | 由本卡 A142 在 `add` 路径补 mode 分支并去 skip 后关闭；RLT_07 原条目保持原状不改写 | C2 已实现（`_validate_decision_mode` + 两条负例去 skip 绿）· 待复核确认 |
| F-002 | 承接 RLT_07 findings F-003：`cancelled` 不在 `relay_log.py` `DECISION_EVENTS`（:59）中，不命中 A69 决策归属闸，与 SKILL.md 决策归属合同存在实现层空隙 | 由本卡 A142 一并实现 `cancelled` 归属校验后关闭 | C2 已实现（`cancelled` 入 `DECISION_EVENTS`，正反例绿）· 待复核确认 |
| F-003 | 仓根无 `.gitignore` 忽略 `__pycache__/`（RLT_10 findings F-002 挂账，DRILL_01 已在预演分支交付）；`.gitignore` 不在本卡 allowed-paths 内 | Python 测试副产品有再入树风险；建议由编排裁决归属（预演分支 cherry-pick 或另卡承接） | 已登记 · 待裁决归属 |
| F-004 | A137 实现语义（C2 返工后为准）：check.C1 P1-1 裁定 C1 的「条件兜底」读法偏离 oracle——blocked/failed **无论节点是否全关**均强制 `ref=<agent>#<n>:(blocked\|agent_lost)` 且须为本实例该 agent 最新事件；全关节点实例下合法 ref 目标只能是 agent_lost 实例（done/cancelled 终态不可引）。check.C1 P1-2 裁定 `stage_close` 对最新 `stage_result outcome=blocked` 先报 A118、再执行节点全关等其余前置（A89/A112 语义不变）。返工证据：RED `python -m unittest tools/relay-light/test_relay_log.py -k RelayStageResultRefTests` → `Ran 5 tests` `FAILED (failures=2)` exit=1（全关缺 ref 被旧合同接受 rc=0、stage_close 实报 A89）；GREEN 同命令 `Ran 5 tests` `OK` exit=0；受波及既有用例 6 处（A113 生命周期、finales、four-outcomes、R#1/R#2 独立性、monitor 重拉计数、planner-amend 交接）已补合法 ref 夹具，相关回归 `Ran 14 tests` `OK` exit=0 | C1 原读法已被 checker 推翻并按 oracle 收敛；`stage_close` 报码顺序现为 A118 先于 A89（仅当本实例已有 blocked stage_result 时） | 已返工 · 待复核确认 |
| F-005 | A138 实现语义：授权的 `user_decision` 记在被止损实例名下后，该实例最新事件即变为 `user_decision`——A49 重拉资格为此放宽「带 `launch_fix=` 的 `user_decision` 视同可重拉」；oracle 未钉码的三种拒法（未止损授权、token 不一致/缺失、第二个 fix 组）统一报 `HC-RL-A107` | 决策类事件落在终端实例名下是账本既有归属规则的边缘情形，资格放宽是必要伴随改动；拒码选择待复核确认 | 已实现 · 待复核确认 |
| F-006 | A140 实现语义：`ledger_silent` 只标「最近事件非终态」的在场 agent——已 `done`/`agent_lost`/`cancelled` 的实例久置不标（否则每个已关节点的历史 agent 都会刷屏）；阈值比较为严格大于 `silence_timeout_min` | oracle 字面为「超过阈值的 agent」，收窄到非终态是降噪读法；若复核要求终态也标需改实现 | 已实现 · 待复核确认 |
| F-007 | check.C1 P1-3 补录：**原始 RED 的逐字终端输出不可证**（C1 会话 pane 缓冲区无持久化，仅四行小结留存命令与 unittest 判定）。小结记录的原始事实：A137 组 `python -m unittest -v tools/relay-light/test_relay_log.py -k RelayStageResultRefTests` → `Ran 5 tests` `FAILED (failures=4)`（4 例均 `error: HC-RL-A112 ... still open`，即旧实现无分 outcome 校验）；A138 组 `-k RelayNotRunRetryTests` → `FAILED (failures=4)`（A107 闸缺失、授权 `user_decision` 撞 A60、status 无归因）；A140 组 `-k RelayLedgerSilenceTests` → `FAILED (failures=3, errors=2)`（配置字段/status 字段/三处模板原文均缺）。原小结把退出码记为 exit=0 系笔误——unittest 失败自然终态 exit=1。**重建 RED（补充证据，非冒充原始）**：临时目录重组 `a80fcde^` 的 `relay_log.py` + `a80fcde` 的 `test_relay_log.py` + `a80fcde^` 的 skill 五件，`python -m unittest test_relay_log.RelayStageResultRefTests test_relay_log.RelayNotRunRetryTests test_relay_log.RelayLaunchFixStatusTests test_relay_log.RelayLedgerSilenceTests` → `Ran 14 tests in 60.379s` `FAILED (failures=11, errors=3)` exit=1 | 原始日志缺位如实登记；重建 RED 证明四组用例在父实现下确为先红 | 已登记 · 重建证据待小审复核 |
| F-008 | A142 实现语义：模式门只作用于 **decider 链**——`consult` 下 decider 链 `resume` 前须有该 agent 最新 `user_decision`；`auto` 下 decider 链 `user_decision` 拒收；strategist 链不受模式影响（`user_decision` 永必需仍由既有 A97 `_validate_strategist_conclusion` 承担）；oracle 未钉码，两处拒码统一报 `HC-RL-A114`。`cancelled` 入 `DECISION_EVENTS` 后：helper 身份（`decider#n`/`strategist#n`）写 cancelled 被 `DECISION_HELPER_NAMES` 闸拦、链外 agent 在活跃决策链存在时写 cancelled 报 A69、无活跃链时普通 `cancelled` 不受影响 | `stage_result outcome=cancelled` 走的是 STAGE_NOTE_EVENTS 分支不经过本闸，决策归属闸只覆盖 agent 事件行的 `cancelled` | 已实现 · 待复核确认 |
| F-008a | C2 各条 RED 证据登记：**A141 原始 RED 在案**——`python -m unittest tools/relay-light/test_relay_log.py -k test_a141` → `Ran 1 test` `FAILED (failures=10)` exit=1（两 adapter 三段锚点全缺）；**A142 原始 RED 在案**——`-k test_a114` → `Ran 5 tests` `FAILED (failures=3)` exit=1（consult 缺 user_decision 的 resume rc=0、auto 下 decider 链 user_decision rc=0、checker#1 写 cancelled rc=0）；**A143 原始 RED 不可证**——SKILL.md 模板段先于结构测试落笔，会话内未先红；补证为重建（非冒充原始）：临时目录重组 `0e0f24a` 的 `test_relay_log.py` + `a80fcde` 的 `relay_log.py` 与 skill 五件，`python -m unittest test_relay_log.SkillAdapterTests.test_a141_dispatch_wait_and_sandbox_fallback_discipline test_relay_log.SkillTemplateTests.test_a114_consult_resume_without_user_decision_rejected test_relay_log.SkillTemplateTests.test_a114_auto_mode_rejects_user_decision_on_decider_chain test_relay_log.SkillTemplateTests.test_a114_cancelled_uses_triggering_agent test_relay_log.SkillCoreDocTests.test_a143_light_plan_review_severity_classification` → `Ran 5 tests in 17.214s` `FAILED (failures=14)` exit=1（五例在 C2 前基线全红）；另 `.devin/config.local.json` 为本会话权限缓存产物，已按整改删文件与空目录 | A143 重建 RED 证明结构测试在父基线确为先红；A141/A142 原始 RED 直接取自本棒 pane | 已登记 |
| F-009 | A143 预演 `review.plan.md` 两轮 P1 复算（分支 `origin/dryrun/rlt12-linux`，不重跑预演）：**R1 FAIL**（review.plan.md @6e7aa4b）P1-01 目标扩围 `*.pyc`、P1-02 未授权 commit 计划、P1-03 `progress.md` 多写入者，另 P2-01；**R2 FAIL**（账本 seq17 `P1=P1-03R,P1-04`；PASS 复审闭合表）P1-03R coder 入口仍允许写 progress、P1-04 builder 越权登记 lesson_candidates。**按冻结 light 模板逐条复算（check.C2 整改后口径，不做未冻结的同根去重）= 3 P1 + 2 P2**：P1-01→P2（目标/授权承接扩围，路径未越 allowed-paths 闭集、验收命令与原目标一致——四类不覆盖）；P1-02→P2（版本动作授权缺口——四类不覆盖；「commit SHA 作 checker 输入构成完成信号矛盾」可争议升 P1，见影响列）；P1-03→**P1**（写入者边界：计划派全员写 progress 与 scribe 独占矛盾）；P1-03R→**P1**（写入者边界：coder zero-context 入口残留允许写 progress——独立发现项逐条计级）；P1-04→**P1**（写入者边界：builder 越权写 coder 独占的 lesson_candidates） | **合同冲突登记（技术卡点）**：oracle 期望复算 = 1 P1 + 4 P2，只有引入「同根去重/残留折级」这一未冻结口径才可达；check.C2 整改明确禁止该口径。逐条冻结计级的诚实结果为 3 P1 + 2 P2（若 P1-02 按完成信号矛盾升 P1 则为 4 P1 + 1 P2）——oracle 复算期望与冻结四类逐条计级二者不可兼得，需编排/用户裁决（如修订 A143 复算期望或给出去重规则），worker 不改 oracle/验收。X1 requirement 路复核维持原判：合同口径冲突、非施工缺陷，收口责任在用户。**处置（2026-09-15 用户裁决后回填）**：用户在两个出口中取「**修订 oracle 期望值**」，不补「同根去重 / 残留折级」规则；design/01 的 `HC-RL-A143` 「怎么验」列已由 `1 P1 + 4 P2` 改为 `3 P1 + 2 P2` 并写明按冻结四类逐条计级（**RLT-A-10 最小澄清**，验收 ID 未新增未删除未改号，总账仍 140）。本条已记录的事实、逐条计级明细与复核结论**一字未改** | **已裁决闭合**（2026-09-15 用户裁决「修订 oracle 期望值」；原状态「卡点 · 待用户裁决（F1 收口登记）」，F1 由 scribe 按编排裁决登记，未改事实与计级） |
| F-010 | X1 节点补录 · check.C1 旧 P1-1（A137 全节点已关时未强制 `ref=`）的可复跑证据：闭合 commit `0e0f24a`（其 message 明记「返工 check.C1：blocked/failed 无条件强制合法最新 ref=」）。实现 `relay_log.py:2190-2193`——`blocked`/`failed` 落入 else 分支**无条件**调 `_validate_result_ref`（`:2089-2130`），不再以 `unclosed` 非空为前提；原反向钉住「全节点已关可无 ref」的用例已删改。相关用例 `test_a137_fully_closed_stage_result_still_requires_ref`（`test_relay_log.py:5300-5338`）：全关节点实例下 blocked/failed 缺 ref 各退 2 报 `HC-RL-A137`、合法最新 ref（`coder#1:agent_lost`）接受 | 可复跑命令（X1 实测）：`python -m unittest -v tools/relay-light/test_relay_log.py -k RelayStageResultRefTests` → 自然终态 `Ran 5 tests in 28.492s` `OK` exit=0。本条目只登记证据，CLOSED/OPEN 判定归 requirement 路（review.requirement.md P1-1 裁决口径） | 证据登记 · 待 requirement 路裁决 |
| F-011 | X1 节点补录 · check.C1 旧 P1-2（`stage_close` 对 `blocked` 未精确报 A118）的可复跑证据：闭合 commit `0e0f24a`。实现 `relay_log.py:2244-2246`——取本阶段最新 `stage_result` 后**先**判 `outcome=blocked` 报 `HC-RL-A118`，节点全关等其余前置检查在 `:2247-2254` 其后执行（A89/A112 语义不变）。相关用例 `test_a137_stage_close_after_blocked_still_rejected`（`test_relay_log.py:5285-5298`，断言 `code="HC-RL-A118"`）与 `:5330-5333` 同断言；既有 A89/A112 生命周期用例随全量回归无回归 | 可复跑命令（X1 实测）：同 F-010 的 `RelayStageResultRefTests` 组（`Ran 5 tests` `OK` exit=0 已含本用例）；本条目只登记证据，判定归 requirement 路 | 证据登记 · 待 requirement 路裁决 |
| F-012 | X1 节点补录 · check.C1 旧 P1-3（C1 原始 RED 证据缺口）的现状证据：「原始 RED 逐字终端输出不可证」与重建 RED 已由 F-007 如实登记（随 `0e0f24a` 落盘：临时目录重组 `a80fcde^` 实现 + `a80fcde` 测试 + `a80fcde^` skill 五件 → `Ran 14 tests in 60.379s` `FAILED (failures=11, errors=3)` exit=1，非冒充原始）。X1 复跑四组现态用例佐证 GREEN 侧仍在绿：`python -m unittest -v tools/relay-light/test_relay_log.py -k RelayStageResultRefTests` → `Ran 5 tests in 28.492s` `OK` exit=0；`-k RelayNotRunRetryTests` → `Ran 4 tests in 22.075s` `OK` exit=0；`-k RelayLaunchFixStatusTests` → `Ran 2 tests in 6.409s` `OK` exit=0；`-k RelayLedgerSilenceTests` → `Ran 3 tests in 0.073s` `OK` exit=0 | 原始/重建/现态三段证据边界保持 F-007 口径；本条目只登记证据，判定归 requirement 路 | 证据登记 · 待 requirement 路裁决 |
