<!-- dh:v1 -->
# review.lesson — RLT_22 独立复核记录（教训）

> 复核者：Devin SWE-2 Max（`rlt22-review2`），fresh 实例，未参与 RLT_22 施工、批次小审、编排与 code-round1 复核。
> 现场：`/home/nash/work/dh-relay/.dh-worktrees/RLT_22`，分支 `wt/RLT_22`，HEAD `fc0d05f`（本路开始时另有 `574711c` requirement 路登记提交），基线 master `72c6c4d`。
> 复核对象：`docs/modules/dh-relay/knowledge/教训库-候选.md` 与 RLT_12/RLT_21 `lesson_candidates.md` 中相关条目的遵守/重犯情况；本卡 `lesson_candidates.md` 候选的现场证据、去重与可复用性；三批登记位是否「无候选也写了本批无」；有无该登记未登记项。
> 时间：2026-09-16。

## 结论

**APPROVE_WITH_NITS　·　P0 = 0　·　P1 = 0　·　P2 = 0　·　P3 = 2**

本卡非「全程 absent」——LC-01/LC-02 两条候选均有现场证据（E-007；E-017→E-018）、已按批登记、状态 `ready-for-review`（AI 不预支人裁决，正确）；B1/B2/B3 三批登记位齐全（B1「本批无+小审整改新增 LC-01」、B2「本批无」、B3「新增 LC-02」）。教训库在册 87 条（候选-1~候选-87，其中候选-40 已裁决、余 86 条待裁决）。与本卡最相关的既往条目——RLT_12 L-002（终态触发封死返工）与 RLT_21 L-005（PASS 前双方不记 done）——即本卡立项动机的兑现对象，已**遵守**（协议层根治）。两处既往教训曾**轻度重犯但被卡内机制当场捕获并改正**，且已转化为本卡新候选（见下「遵守/重犯对照」）。另有两条「可登记未登记」项按 P3 登记，是否升格由编排/人裁。本路只登记事实与级别，不做验收裁决。

## 输入清单

`dispatch/README.md`、`dispatch/review.md`、`brief.md`、`task_plan.md`、`progress.md`（进度表 + E-001~E-021 证据账本 + 信号段）、`findings.md`（F-001~F-009）、`check.B1.md`/`check.B2.md`/`check.B3.md`、`review.code-round1.md`、`review.requirement.md`（本复核 requirement 路报告）、`lesson_candidates.md`（本卡）、`workspace/RLT_12/lesson_candidates.md`（L-002~L-010）、`workspace/RLT_21/lesson_candidates.md`（L-001~L-007）、`docs/modules/dh-relay/knowledge/教训库-候选.md`（候选-1~候选-87，714 行通读）、整卡 `git diff master...HEAD`。

## 本卡候选逐条核查

| 候选 | 现场证据 | 登记位 | 去重 | 可复用性 | 结论 |
|---|---|---|---|---|---|
| LC-01（解析层隔离断言须直钉函数 + 变异证明非空转） | `check.B1.md` P1-1（端到端正例从未触达 `_decision_helper`/`_validate_decision_helper`——`checkpoint` ∉ `DECISION_EVENTS`）+ `progress.md` E-007（变异 RED→复原 GREEN，`relay_log.py` 净零改动） | B1 批登记位已写「本批无；小审整改新增 LC-01」 | 与库内候选-6（断言配变异对照）/候选-46（护栏须区分对错）同族，条目内已写明 relay-light 语境的特有形态（checkpoint 不进决策事件集） | 规则可迁移：凡「A 不误命中 B」类隔离断言，须直接调内部解析函数断言返回值 | 有证据、已登记、`ready-for-review` 状态正确 |
| LC-02（多路送审拉起须「信号→拉起」逐路交错） | `progress.md` E-017（X 双路 fixture 先连写两信号再连拉，第二路 launch 撞 A144——latest-事件语义）→ E-018（修时序后 22 例全绿）；`check.B3.md` 有记录 | B3 批登记位已写「新增 LC-02」 | 与 RLT_21 L-001（记在既有实例名下的事件改变 latest 语义）同源不同位——L-001 是授权/簿记事件顶掉 latest，LC-02 是同实例后写信号覆盖先写信号；条目内未注去重关系，见 P3-1 | 规则可迁移：写 monitor/adapter 侧多路拉起逻辑先认 latest-事件语义再排时序 | 有证据、已登记、`ready-for-review` 状态正确 |

**三批登记位**：B1（`lesson_candidates.md:20`）、B2（`:21`）、B3（`:22`）三行齐全，「本批无」语义两处均如实写明。

## 相关既往条目：遵守 / 重犯对照

### 教训库（`docs/modules/dh-relay/knowledge/教训库-候选.md`，在册候选-1~候选-87）

| 条目 | 与本卡关系 | 判定 |
|---|---|---|
| 候选-1（每个 reason 码/分支都要有断言钉住 + 静态守卫） | A144 九拒绝例、A145 四类反例、A146 十一反例各自断言退出码+规则号 | **遵守** |
| 候选-2/候选-7（oracle 不得自证，期望值独立硬编码） | 模板/纪律原文断言把 oracle 句子逐字硬编码在测试里，非从被测文件推导 | **遵守** |
| 候选-6/候选-46（钉边界断言须配变异对照、护栏须能区分对错） | **曾踩中后被捕获改正**：B1 的 helper 隔离断言空转（LC-01 现场），小审逮出 → 解析层断言 + E-007 变异证明；code-round1 另做三处生产代码变异全部转红并登记 | **一次轻犯、卡内捕获改正、已登记** |
| 候选-12（证据只覆盖论断一部分却写成整条兑现） | 同 LC-01 病灶的论断面（「A69 不误命中」断言实际未触达扫描器） | 同上一并捕获改正 |
| 候选-23/候选-48（偶发红先二分归因；「环境假红」须证明） | 本复核期间一次后台复跑 6 个 FAIL，经归因证实为 code-round1 变异实验（`:1706`/`:2043` 两处，签名逐一吻合）在共享工作树的并发污染，非代码缺陷——归因证据见 `review.requirement.md` P3-2 | **遵守**（按证据归因而非归罪环境） |
| 候选-45/候选-49（变异点选唯一负责场景；变异证据以最终提交为基线） | code-round1 三处变异点均在本卡新增分支上，施加/还原均回 HEAD blob `5174ea1` | **遵守** |
| 候选-53（只读复核的字节级变异不用全文重写工具） | code-round1 变异为单点修改并以 `git hash-object` 验证复原 | **遵守** |
| 候选-61（负例断言钉规则号不钉措辞） | 全部反例断言 `^error: HC-RL-A1xx ` 规则号，不钉文案 | **遵守** |
| 候选-65/候选-68（复核者自报基线；终态证据在最终提交上重取） | 本报告与 code-round1 报告均自报 HEAD；本路两轮复跑取于最终提交态 | **遵守** |
| 候选-13（写下教训≠教训生效） | 见 P3-2 建议项 | 元提示，见下 |

### RLT_12 `lesson_candidates.md`（L-002~L-010）

| 条目 | 与本卡关系 | 判定 |
|---|---|---|
| L-002（谁先记终态谁封返工路——trigger 不该挂终态） | **本卡立项动机的兑现对象**（findings F-008 → RLT-A-09 → 本卡）：判定方 trigger 改非终态 `checkpoint` 信号、判定方 PASS 前不记 `done`、FAIL 经 live 判定方 `checkpoint` 路由回同一送审方实例——A144/A145/A146 三机制 + A149 纪律原文正是该规则的协议层落地 | **遵守（根治）** |
| L-008（编排卡要有记录位，证据槽位不得空转） | 本卡 `progress.md` 证据账本 E-001~E-021 全部实填（无「待实跑预留槽位」残留），三批施工+整改+复核各有进度行 | **遵守** |
| L-009（完成信号晚于 commit → 收口按点名清单逐个 add、禁 `add -A`、先自查 `git status`） | 本卡各 commit 均按路径显式 add（git log 逐 commit 核：无 `-A`/`.` 形态）；本复核两路 commit 同纪律 | **遵守** |
| L-010（整体授权≠复核已执行） | 本卡三路复核（code-round1 已落 `fc0d05f`、requirement `574711c`、lesson 本路）均在合并闸前实跑并各自落独立报告——RLT_12 的「未跑即合入」未重演 | **遵守** |
| L-004/L-005/L-006/L-007（工具抖动/阶段结论归属/外部 agent 越界产物/菜单定位） | 本卡无真计划实跑、无外部 agent 交互现场，条目场景未出现；「越界产物」纪律对应项——卡 diff 全落允许路径闭集，无外部工具落物 | **不适用（场景未出现）**，无违反 |

### RLT_21 `lesson_candidates.md`（L-001~L-007）

| 条目 | 与本卡关系 | 判定 |
|---|---|---|
| L-001（记在既有实例名下的事件改变 latest 语义） | 实现侧**遵守**：A144 绑定送审方**当前实例**最新事件（`_latest_for_instance`，brief/实施提示明令禁用 `_latest_by_name`）；测试侧**曾轻犯一次**：B3 X 双路 fixture「连写两信号再连拉」撞同一语义 → 当场改为逐路交错并登记 LC-02 | **一次轻犯、卡内捕获改正、已登记** |
| L-002（oracle 分号/但书句各子句独立生效） | A145/A146 各子句独立校验独立断言（token 唯一性/角色闭集/写入者非判定角色；`reviewed=`/`ready_seq=`/最新信号/已 done 四条件逐一） | **遵守** |
| L-003（多轮复核证据按轮留痕不覆盖） | `check.B1.md` 保留 FAIL→整改→PASS 两轮全文；复核产物按路径独立成文（`review.code-round1.md`/`review.requirement.md`/`review.lesson.md`） | **遵守** |
| L-004（计数/去重口径须冻结） | A147 计数口径在 oracle 层冻结（`(node, reviewer)` 组合、首轮计入、≥limit 且判定方无 done），测试按 limit 2/3 同一实现双点钉死 | **遵守** |
| L-005（PASS 前 coder/checker 均不记 done、同实例返工） | 同 RLT_12 L-002 一并根治——协议层 A144~A146 + 模板纪律原文 | **遵守（根治）** |
| L-006/L-007（菜单动态定位/监工轮询等待） | 本卡无 Windows 交互实跑现场 | **不适用（场景未出现）**，无违反 |

## 应登记未登记项核查

| 项 | 现场 | 评估 |
|---|---|---|
| 数字 token 守卫单用 `isdigit()` 会被 Unicode 数字绕过致 `int()` 崩溃 | code-round1 P2-1 + 在飞整改（`relay_log.py:2022` `isascii()` 加固 + 新增 `test_a146_malformed_ready_seq_exits_two_not_crash`） | **P3-1 建议登记**：可复用规则明确——「`str.isdigit()` 接收全 Unicode 数字而 `int()` 只吃十进制；数值 token 守卫须 `isascii()+isdigit()` 或 `int()` 进 try 归并规则号」；库内无同条款（候选-5 是 PowerShell 大小写，非同一机制） |
| 共享工作树上的活体变异实验会使并发测试证据失效 | 本复核后台复跑 6 FAIL 与 code-round1 变异窗口并发（`review.requirement.md` P3-2） | **P3-2 建议登记**：可复用规则——「变异实验与独立复跑须错峰或在独立 clone 进行；复核期并发复跑出现红先做归因再引用」；与库内候选-23/候选-48 互补（它们讲归因纪律，本条讲污染源本身） |

## 逐条明细

### P0

无。

### P1

无。

### P2

无。

### P3-1　LC-02 未在条目内标注与 RLT_21 L-001 的去重关系

LC-02 与 RLT_21 L-001 同源（latest-事件语义），按 RLT_12 先例（L-002/L-004 条目内的「去重」段），候选宜自注去重关系便于升格裁决。属登记质量建议，不阻塞。

### P3-2　两条可复用现场未进候选表（见上「应登记未登记项」）

isdigit 守卫缺陷的修复现场与「变异窗口污染并发证据」现场均有仓内证据可挂，是否补登记由编排/人裁；本复核不越权代写 `lesson_candidates.md`。

## 边界与口径

- 库版本：`docs/modules/dh-relay/knowledge/教训库-候选.md` 在册候选-1~候选-87 共 87 条，其中候选-40 已裁决（B）、候选-80 标注「附属条目不单计」、余 86 条待裁决——按候选口径报告，不误称「已批准教训数」。
- 本卡非 `lessons-absent`：两条候选在册，故不走可核查 N/A 形态；其要素（库版本 + 各条证据链）仍按 N/A 口径逐条写明以备核查。
- 本路只读：未改代码/测试/skill/他人工件；新增仅 `review.lesson.md` 与 `review.md`、`progress.md` 登记/信号行。
- 升格裁决权属人；`ready-for-review` 状态保持未被本复核预支。

## 裁决

**APPROVE_WITH_NITS**。两条候选证据链完整、三批登记齐全、核心既往教训（终态触发封返工）已被本卡根治；两次轻犯均被卡内机制当场捕获并转化为登记候选，无沉默重犯。P3 两项为登记质量与补登记建议。本结论仅为教训一路的事实登记，不替编排做验收裁决。
