<!-- dh:v1 · findings.md — 问题清单。🟢 边做边记。 -->
# findings — RLT_22

> 本表在 D 开工时只登记**已知边界与风险**（建工作区期由 builder 只读盘点所得），不预判施工期发现。状态变化由编排/decider 裁决，worker 不自改。
> 级别：P0 阻塞发布 / 数据丢失 / 安全 · P1 阻塞任务目标 · P2 质量 / 证据缺口 · P3 后续不阻塞。
> 遗留（P0/P1 唯一合法路径，DC_46）：状态列写 `遗留→<去处>（已确认）`——「已确认」三字代表用户已在对话里明确点头；没有这三个字，`dh-check` R13 仍按 `open` 处理。AI 不得自行把 open 的 P0/P1 划成遗留。

## 已知边界与风险（开工登记）

| ID | 级别 | 问题 | 证据 | 处理 | 状态 |
|---|---|---|---|---|---|
| F-001 | P2 | **本卡依赖的事实来源目前在未合并分支上**。卡「实施提示」指定的事实来源——`workspace/RLT_12/findings.md` 的 **F-008** 与真计划账本 `relay/rlt12-win-01/relay_log.jsonl`（C1 的 seq 16/19/20/21、C2 的 seq 25/26/27/29）——只存在于 **`wt/RLT_12`** 分支的工作树里，尚未合入 `master`，在本卡分支 `wt/RLT_22` 的 checkout 里**查不到该路径**。 | `design/evidence/10-交叉审核记录-RLT-A09-复核触发信号.md` 的「账本与 workspace 的落点」条原文；本树 `git log --oneline -1` = `544ccdb`，`docs/modules/relay-light/relay/rlt12-win-01/` 与 `workspace/RLT_12/` 在本分支不存在 | 施工期只读引用另一棵树 `D:\MyFiles\ai-workflow\dh-relay\.dh-worktrees\RLT_12\...`，**禁止修改该树任何文件**；引用时须写明「目前在未合并分支 `wt/RLT_12` 上」。A148 需要「`rlt12-win-01` 的 `relay_plan.md` 原样过 lint + 账本 71 行原样重放」，其 fixture 从该树**复制**进本卡测试夹具，不得跨树引用绝对路径。**2026-09-16 已闭合**：RLT_12 已 squash 合入 master（PR #25 `7981556`），`workspace/RLT_12/findings.md` F-008 与账本 `relay/rlt12-win-01/relay_log.jsonl` 均在本卡基线内可直读；A148 的 fixture 已按要求**复制**进 `workspace/RLT_22/fixtures/rlt12-win-01/`（计划 + 71 行账本），测试不跨树引用绝对路径 | closed |
| F-002 | P1 | **依赖卡 RLT_21 的程序改动不在本卡基线里，且变更范围与本卡高度重叠**。本卡基线 `544ccdb` 的 `relay_log.py` 不含 RLT_21 的任何交付（`grep -c "launch_fix\|silence_timeout_min"` = 0）。RLT_21 的变更范围是 `relay_log.py` / `test_relay_log.py` / `skill/**`，与本卡**逐项相同**；两卡都要改 `agent_launch` 一线与 `SKILL.md`、两份 adapter、`dh-mapping.toml`。DevPlan §3.1 把 RLT_22 的依赖记作 RLT_21，但本卡已在 RLT_21 合入前 D-start。 | 本树 `grep -c "launch_fix\|silence_timeout_min" tools/relay-light/relay_log.py` → `0`；DevPlan §3.1 RLT_21 行与 RLT_22 行；DevPlan §RLT_21 与 §RLT_22 的「变更范围」「允许路径」两节 | 施工前由编排裁决顺序：①等 RLT_21 合入 master 后本卡 rebase 再开工，或②本卡先行、收口时承担与 RLT_21 的合并冲突解决。**worker 不自行选边**；每批进场第一动作 `git rebase --autostash master`，rebase 后若 `relay_log.py` 出现 RLT_21 交付，在 `progress.md` 记一笔基线变化 。**编排 2026-09-15 已解决**：RLT_12（PR #25 `7981556`）、RLT_21（PR #27 `124a5c9`）、RLT-A-09（PR #28 `a6689f1`）依次 squash 合入 master，本分支已重建于 `a6689f1` 之上；`launch_fix` 与 `silence_timeout_min` 均在基线内，A144~A150 七条亦在 design/01，开工顺序问题消失。 | closed |
| F-003 | P1 | **卡正文与 design/01 对 A149 的最小账本序列范围不一致**。卡正文写「另对 **C 与 X** 各跑最小账本序列」；design/01 §11 `HC-RL-A149` 的「怎么验」列写「②最小账本序列测试：对 **W、C、X** 各跑一条合成账本」；`design/evidence/10` §三第 2 项又写「A149 增 W 模板改动与 **W 的最小账本序列测试**」。两处对 W 是否纳入最小账本序列口径相反。 | DevPlan 卡正文 `HC-RL-A149` 验收口径行；`design/01` §11 `HC-RL-A149` 行；`design/evidence/10-交叉审核记录-RLT-A09-复核触发信号.md` §三表第 2 行 | **AI 不代判**。design/01 是 oracle、DevPlan 是任务分账，二者冲突须由编排/用户裁一次：按 design/01 跑 W/C/X 三条（覆盖更全、与「W 纳入」裁决自洽），或按卡正文只跑 C/X。裁决落 `progress.md` 后施工；`brief.md` 完成条件第 6 条已按卡正文逐字抄录并标注本条。**编排 2026-09-15 已裁决并修复**：design/01 是 oracle 且用户已裁决「W 纳入」，卡正文已改为「W、C 与 X」（提交 `999f733`），两处口径一致，本条闭合 | closed |
| F-004 | P2 | **design/01 正文与新 A107 的「三套计数」尚未对齐**，且对齐落点不在本卡允许路径内。RLT-A-09 晋级只改了 design/01 的文件头、§11 抬头总账与 §11.1 表；正文仍有三处写「两套计数」。本卡允许路径不含 `design/**`，无法修。 | `design/01` 第 **779**、**873**、**966** 行仍为「两套计数 / 两套止损计数」，而第 **1184** 行 `HC-RL-A107` 已是「三套」；`design/evidence/10` §五原文：「候选稿 §7.3 点名的 design/01 正文同步……**不在本次范围内**，其中 §7.3 正文两处仍写「两套计数」，与新 A107 的「三套」尚未对齐，**需另行承接**」 | 本卡只按 §11 的 A107/A147 实现程序与测试，并把 `dh-mapping.toml` 的 `[limits.on_exceed].note` 说明文字由「两套」改「三套」（在允许路径内）。**design/01 正文三处不改、不越界**；归属卡由编排裁决。**编排 2026-09-15 已在规划分支 `wt/RLT-A-09` 补齐正文同步（提交 `999f733`），本树已 fast-forward 到该提交，三处「两套计数」已全部改为「三套计数」**，本条闭合 | closed |
| F-005 | P2 | **normal 档「复核几路」有三份互相打架的权威来源**（RLT_12 的 F-013 同源，本卡同样命中）：①仓根 `AGENTS.md` 宪章#5 = 代码轮 1 + 需求方向 + 教训**三路**；②`tools/relay-light/skill/dh-mapping.toml` `[recipes.normal].reviewers` = `["requirement","lesson"]` **两路**；③dev-harness 的 `review.md` 模板又把一致性复核写成「标准档必做」的第 4 路。本卡只带 `dh:task-type:v1 ... type=normal`，**不带** `dh:review-policy:v1`、**不带** `dh:review-scope:v1`。 | `AGENTS.md:36`；`tools/relay-light/skill/dh-mapping.toml:17-18`；dev-harness `templates/workspace/review.md` 的「第 4 路·一致性复核」段与 `SKILL.md` G6；本卡 DevPlan 卡正文全文无 `task=RLT_22` 的 `review-policy` / `review-scope` marker | `review.md` 已按「三路 + 一致性登记位」预填**路径登记与靶子**，结论列全空。实际跑几路由编排/用户裁决；**施工方与复核方均不得自行降路或合路**。用户 2026-09-15 已裁决「该由卡上的类型/策略 marker 说了算」，但未裁决对齐到哪一份、也未裁决改哪个文件。**编排 2026-09-16 裁决**：本卡按 `AGENTS.md` 宪章#5 的三路（code-round1 / requirement / lesson）实跑并已全部闭合，一致性路按 normal 不必做处理；三份权威的对齐**超出本卡允许路径**（涉及 `AGENTS.md` 与 dev-harness 模板），**转下一卡承接**，不在本卡内改 | 遗留→下一卡 |
| F-006 | P2 | **A102「批内往返不加 attempt」在新模型下成立需由 A145 正例证明，不得假定自动继承**。卡「非目标」明确写「不改 A102（批内往返不加 attempt，其在新模型下成立由 A145 正例证明，不假定自动继承）」；design/01 §11 A145「怎么验」列同样要求与 A102 共用 fixture、断言 attempt 恒为 1 且 `agent_launch` 仅一条。 | DevPlan §RLT_22「非目标」原文；`design/01` §11 `HC-RL-A145` 与 `HC-RL-A102` 两行 | 施工时必须**显式**写这条正例（带 `ready_for_review=` token 的 `checkpoint` 往返，断言 attempt 恒为 1、`agent_launch` 仅一条），不得只跑既有 `test_a102_checkpoint_round_trips_do_not_burn_attempts` 就宣称 A102 仍成立。**2026-09-16 已闭合**：B1 已写显式正例（三次带 `ready_for_review=` 的 checkpoint 往返后 builder 仍只有 `#1` launch、attempt 恒为 1，`agent_lost` 后合法重拉为 `builder#2`），`check.B1.md`「A102 显式正例」与 `review.requirement.md` 均已核实 | closed |
| F-007 | P2 | **RLT_12 与 RLT_21 均未 verify、未验收、未合并，DevPlan §3.1 的状态行与计划头部 `dh:status` 现状块存在待合并对齐**。§3.1 把 RLT_12 与 RLT_21 都记为「未开始 / 工作区 — / 完成时间 —」，而 `dh:status` 现状块写「RLT_12 真计划五阶段已全闭合，RLT_21 完成八笔提交、单测 181 全绿，两卡均未 verify、未验收、未合并」，两卡实际都已 D-start、证据在各自树内。 | `dev_plan/P1-RelayLight-开发方案.md` 第 **19** 行（现状）、第 **21** 行（下一步）、第 **108**/**109** 行（§3.1 RLT_12 / RLT_21 行）、第 **110** 行（RLT_22 行） | 本卡**不改 DevPlan**（不在允许路径内）。回填与对齐随 RLT_12/RLT_21 各自收口进行；本卡收口时只回填自己那一行，由编排统一做。施工期读 §3.1 状态列时以 `dh:status` 块与各卡工作树实况为准。**2026-09-16 已闭合**：RLT_12（PR #25）与 RLT_21（PR #27）均已 squash 合入 master，DevPlan §3.1 两行已在 master 上回填为「已完成」并带完成时间与 PR/SHA，前提事实消失 | closed |
| F-008 | P3 | **A148 要重放的旧账本含非 ASCII note，Windows 默认代码页下有编码坑**。`rlt12-win-01` 账本的 note 为中文；RLT_10 的 F-003 已登记 `relay_log.py` 在 cp1252 下打印中文会 `UnicodeEncodeError` 崩溃，程序侧防护当时挂账后续卡，本卡基线未必已修。 | `workspace/RLT_10/findings.md` F-003；账本文件 `relay/rlt12-win-01/relay_log.jsonl`（RLT_12 树） | 重放测试一律以 `encoding="utf-8"` 显式读写，不依赖平台默认；若程序侧输出路径确实崩溃，登记证据后发 `BLOCKED` 交编排，**不顺手改 `_status_command` 的输出编码**（超出本卡验收范围）。**2026-09-16 已闭合**：A148 的重放 fixture 与读写全程显式 `encoding="utf-8"`，未依赖平台默认；程序侧输出路径未触发崩溃，本卡未改 `_status_command`。Windows 侧由 CI 的 `relay-light-python` job 覆盖 | closed |
| F-009 | P2 | **`dh-mapping.toml` 的 `[limits.on_exceed].note` 实际仍写「两套计数」，但没有任何一条验收咬住它**。RLT-A-09 晋级后 design/01 §6.3 的配置样例已改「三套」，真文件未改；A149 只覆盖 `SKILL.md` 与两份 adapter 的原文，不覆盖 `dh-mapping.toml` 的说明文字。本卡「变更范围」已列出要改这处（在允许路径内），但改完没有机器证会验它。 | `tools/relay-light/skill/dh-mapping.toml` 的 `[limits.on_exceed].note`；`design/01` §6.3 样例；`design/01` §11 `HC-RL-A149` 的覆盖范围；`design/evidence/10` 停止线第 2 项 | 施工时照「变更范围」改这处文字，并在 `check` 文件里以人读方式登记（改了什么、与 §6.3 样例是否一致）。**是否为它补一条机器证，或明确不补**，由编排/用户在收口前裁一次；本卡不自行新增验收编号。**编排 2026-09-16 已裁决并落地**：B3 已把 `[limits.on_exceed].note` 改为「三套计数独立、不叠加、不互相重置」，与 design/01 §6.3 样例一致；**明确不为这处说明文字新增验收编号**（纯文档文字，A149 的覆盖范围不扩），改动事实由 `check.B3.md` 人读登记承接 | closed |

## 施工期登记

- B1（2026-09-16 · exec）：本批无新增 finding。F-001、F-005～F-009 维持开工登记状态不变；`tools/relay-light/__pycache__` 在本树不存在（`PYTHONDONTWRITEBYTECODE=1` 全程生效），无 pre-existing 项需区分。
- B1 整改（2026-09-16 · exec）：check.B1.md P1-1（`test_a145_helper_scan_sees_decider_only` 假绿——`checkpoint` 不在 `DECISION_EVENTS`，端到端正例从未触达 `_decision_helper`/`_validate_decision_helper`）已闭合：同用例追加解析层断言并以变异 RED（E-007）证明非空转；`relay_log.py` 净零改动。无新增 finding。
- B2（2026-09-16 · exec）：本批无新增 finding。F-001、F-005～F-009 维持开工登记状态不变。施工期一处 fixture 自修（`_start_ledger_for` 补 `node` 参数——X 双路计划的 `plan_loaded` 默认 `node=W1` 报 A59），非仓内既有缺陷。`tools/relay-light/__pycache__` 不存在（`PYTHONDONTWRITEBYTECODE=1` 全程生效）。
- B3（2026-09-16 · exec）：本批无新增 finding。三项登记：①F-001 指定的复制落点已执行——`rlt12-win-01` 两文件从 master 复制进本卡 `fixtures/rlt12-win-01/`（E-017/E-018），未跨树引用、未改源 fixture；②F-009 指向的 `dh-mapping.toml` `[limits.on_exceed].note` 已按变更范围由「两套」改「三套」并补送审轮数一句（diff 只落 note 两行、键值不动，E-021），且本批已新增机器证 `test_a149_dh_mapping_note_names_three_counters` 咬住它——F-009 的「补不补机器证」裁决项本批以「补」落地，该 finding 的状态勾销仍留编排；③X 双路最小账本测试首跑因把两路信号连写再连拉而撞 A144「绑当前实例最新事件」语义，修测试时序为「信号→拉起」逐路交错后成立——这是协议语义在测试侧的如实落点，非程序缺陷。F-005～F-008 维持原状。
