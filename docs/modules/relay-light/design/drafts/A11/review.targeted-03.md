<!-- dh:v1 -->
# RLT-A-11 · RV2 定向复审（第二轮 · 窄审 N-001～N-003）

- 日期：2026-09-16 · 复审人：reviewer#1（未参与起草；上一轮 review.targeted-02.md 出具者）
- 对象：`A11-候选.md` **v4**（C1c 整改产出，626 行；v3 为 622 行，净 +4 行）
- 依据：`review.targeted-02.md` N-001/N-002/N-003；`decisions.md` 文末「小决策」段（:37-42）；`findings.md` C1c 追加行；`progress.md` C1c 记录

## 第一部分 · TARGET 终态行

TARGET N-001 status=CLOSED
- 候选稿 `:257`（B-011 新文行）：「Issue 评论落记」已更正为「**Issue #37（边界同步更新已由编排直接落进权威正文，2026-09-16）**」。
- 证据：与 `decisions.md:42` 指令（「编排直接更新 Issue #37 权威正文（2026-09-16）」）语义一致，且与本轮复核建议句逐字相同；事实面与 `gh issue view 37` 实测相符（评论数 0、权威正文含【扩界】子条）。`rg '评论落记'` 全候选稿仅 `:622` 整改对照行命中——系元记录引用旧误文，非晋级文本残留。

TARGET N-002 status=CLOSED
- 候选稿 `:427`（B-020 wire format 段、写入者条 :426 之下）：新增「`HC-RL-A85` 的写入者二分枚举系 19 词时代口径、未含 `resource_close`；`resource_close` 的法定写入者以 §3.4 控制事件表为准。」
- 证据：与 `decisions.md:40` 所批选项 (a) 授权句语义一致（措辞略全，含义相同）；未新增改 A85 的 block，A2 仍是唯一旧行例外；A85/A68/A89 同族陈旧已登记 `findings.md:7`（C1c-F-001）。B-020 段较 v3 恰 +1 行（37→38 行），插入位置在写入者条与 note 事实条之间，未动既有 wire format 句。

TARGET N-003 status=CLOSED
- `findings.md:8`（C1c-F-002）：DevPlan `RLT_03 :185`「19 个事件词 fail closed」与 `RLT_22 :415/:432`「19 词白名单」按「不追溯改历史工件」原样保留、登记备查。
- 证据：`decisions.md:41` 指令逐项对应；`git status` 显示受跟踪的 DevPlan 零改动（全仓仅 `drafts/A11/` 未跟踪），「原样保留不加注」属实。

## 第二部分 · v3→v4 窄审（其它 B-block 逐字文本未触碰核查）

**改动面盘点**（v3 622 行 → v4 626 行，+4 行全部可归属）：

| 位置 | 性质 | 内容 |
|---|---|---|
| `:1`/`:5`/`:11` | 版本头原位改写 | v3→v4，第 11 行版本说明追加 v4 整改来源句 |
| `:257` | B-011 新文行原位改写 | 仅「评论落记」子句被替换为更正口径，该行其余逐字未动 |
| `:427` | B-020 新文内插入 +1 行 | A85 限定句（N-002 选项 a） |
| `:622-624` | 整改对照表追加 +3 行 | N-001 / N-002 / N-003 对照行 |
| `:626` | 文末脚注原位改写 | 「于 v4 整改完毕，待复审确认」 |

**机械复核**（脚本重跑上轮全部 verbatim 判据）：

- 9 个「旧文逐字」块仍与 `git show origin/master` 逐字一致且锚点各自唯一命中（B-022/23/24 对 DevPlan，余对 design/01）。
- 裁决钦定逐字新文全部在位：A2 替换行、A09 改写句、A155/A156（decision.1 §2.3）、B-021 核心单元格 ×2、B-013 两固定锚点片段。
- B-003 仍恰 8 行 `HC-RL-A151`～`A158` 顺序连续；`OWNER` 映射 8 行与 B-009/B-010 一致；总账 148 算术未动。
- 全候选稿 `评论`/`落记` 仅 `:622` 元记录行（3 处同线）；B-008 修订行、B-013 固定片段、§4 影响面零残留。
- `git status` 除 `?? drafts/A11/` 外零改动，无越界写入；B-block 编号仍为 B-001～B-024 恰 24 个。

**新发现**：无（N-004 起无项）。

## RV 判据脚本输出（重跑 task_plan RV 节脚本，判 targeted-02 第一部分仍全 CLOSED）

```text
RV_RECORDS_OK ['P1-1', 'P1-2', 'P1-3', 'P1-4', 'P1-5', 'P2-1']
```

结论：**APPROVE**（N-001～N-003 全 CLOSED；v3→v4 无夹带改动；无新 P1/P2）。
