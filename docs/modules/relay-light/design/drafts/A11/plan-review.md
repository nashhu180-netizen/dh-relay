# RLT-A-11 · W2 计划评审（第 1 轮）

- 对象：`brief.md`、`task_plan.md`；基线 `bd118f6`（本次核对 `HEAD = origin/master`）。
- 来源：Issue #37 正文、RLT_11 findings F-001/F-002 与裁决落记、DevPlan RLT-A-11/RLT_23/RLT_24、正式设计 §11/§12/§15、A09 候选与复核先例。未改代码，未运行实现测试。

## 结论

REVISE；P1=2，P2=1。两处 P1 均是计划中的机械完成判据尚不能可靠判断 Issue #37 的验收结果；修订后重新评审 W2。

## 逐项判据

| # | 判据 | 结论 | 级别(P1/P2/—) | 依据（文件:行/小节） | 整改动作 |
|---|---|---|---|---|---|
| 1 | 来源真实性 | FAIL | P2 | `brief.md:9` 将职责分层转述称作“用户裁决原话”，但 Issue #37「目标」第 2 条的原话是“不是只给账本就能复原现场……”；`findings.md:10,36` 与其余引用的内容一致，设计 §12/H10 行号也对得上。 | 把“原话”改成“裁决要点”，或附 Issue 中的逐字原话；不改变已裁决含义。 |
| 2 | 三项目标与五条验收 | FAIL | P1 | `brief.md:8-20` 覆盖三目标、五条验收；`task_plan.md:75-76,93-116` 的 DevPlan 机器判据只检查 `A151`/各新 ID 是否在整份 DevPlan 任意位置。即使 RLT_23/RLT_24 的「验收口径」行完全未更新，只在 RLT-A-11 条或顶部句提及 ID，命令仍会通过；Issue #37「验收口径」第 3 条要求两卡**正确引用**。`task_plan.md:93,122` 另有人工逐项对照，但没有给出可复跑的按卡判据。 | 在 C2/R 增加按 `#### RLT_23`、`#### RLT_24` 区块及各自「验收口径」行提取的检查：由候选稿冻结每条新 ID 的归属，分别断言所属卡含该 ID，且另一卡不误领；用“仅顶部/RLT-A-11 条提及”的反例证明检查会失败。 |
| 3 | 停止边界 | PASS | — | `brief.md:28-33`、`task_plan.md:5,13,74,91,124`；对照 Issue #37「非目标」及 `dispatch/README.md`「停止边界」。 | — |
| 4 | 允许路径与晋级限制 | PASS | — | `brief.md:22-26`、`task_plan.md:12-13,37-38,46-47,73-75,82-84,91-92`；对照 `dispatch/README.md`「允许路径」。 | — |
| 5 | 候选、fresh 审、整改、复审、裁决、晋级、开发后复核 | PASS | — | `task_plan.md:10-93,126-128`；A09 `design/drafts/A09-复核记录-fresh-01.md` 的三段格式与 `design/evidence/10-交叉审核记录-RLT-A09-复核触发信号.md` 的裁决、晋级链。`task_plan.md:62-69` 把用户裁决交给编排。 | — |
| 6 | 分批独立与机械完成判据 | FAIL | P1 | `task_plan.md:57-58` 用不带行锚的 `rg -n 'OPEN'` 判定定向复审可晋级；`task_plan.md:84-85` 用 `rg -n 'MISMATCH|OPEN'` 判定晋级审核。报告即使仅在说明或表头中写“无 OPEN”或“无 MISMATCH”，也会被判失败；反过来，报告遗漏某个审核编号时，`test -s` 和搜索无命中仍可能通过。`task_plan.md:65-67` 对 decisions 只搜索字段字样与全局禁词，未给出开放项编号集合的可执行比对。 | 给 RV、C2-audit、D1 各自定义固定记录行格式与编号集合比对命令；只对状态字段作精确匹配，要求每项恰有一条终态结论，并以“说明文字含 OPEN、缺一项编号、某项为 OPEN”的样例验证真假结果。 |
| 7 | 验收 ID 最大号、连续续号、只增不改、总账与 §15 | PASS | — | `brief.md:4,10`；`task_plan.md:17-18,93,99-117,122`；设计 §11 总账 140=125+15、表尾 A150，§15「验收 ID 稳定性」。当前 `HEAD` 与 `origin/master` 同为 `bd118f6`。 | — |
| 8 | A151~ 原子性与 RLT_23/RLT_24 覆盖、开放项 | PASS | — | `task_plan.md:16-19,37-39` 分列四条纪律与 schema、failed 分校验、§12 取证、71 行历史账本兼容；`task_plan.md:19,62-69` 将事件名/字段等未裁产品决定列开放项。对照 DevPlan `RLT_23` §目标/验收口径、`RLT_24` §目标/实施提示。 | — |
| 9 | 账本/workspace/git/Issue 职责分层与 H10 | PASS | — | `brief.md:9`、`task_plan.md:16,19,93`；RLT_11 `findings.md:10,36`；设计 §11.2 `HC-RL-H10`。H10 命题列是否改被留作开放项，且 `task_plan.md:19,66` 明确先处理与 Issue“只增不改”的冲突。 | — |

## 范围外发现

- W1 已在 `findings.md:6` 登记 RLT_24 将来“变更范围”写 §12、`dh:allowed-paths` 却未列设计文件的冲突；本 W2 不改该卡合同，交编排在 RLT_24 D-start 前处理。

## 第 2 轮

### 结论

APPROVE；第 1 轮 #1/#2/#6 均 CLOSED，新增 P1=0、P2=1。仅复核 W1b 改动及其引入的问题；未执行未来批次的候选稿或晋级核验。

### 逐项判据

| 项 | 结论 | 级别 | 依据（文件:行/小节） | 整改动作 |
|---|---|---|---|---|
| 第 1 轮 #1 来源措辞 | CLOSED | — | `brief.md:9` 已把“用户裁决原话”改成“Issue #37「目标」第 2 条所载裁决要点”；职责分层含义未变。 | — |
| 第 1 轮 #2 按卡验收 ID | CLOSED | — | `task_plan.md:17-18` 要求候选稿逐 ID 冻结 `OWNER` 归属与两卡逐字验收行；`:126` 要求 C2 跑 R 检查；`:188-209` 对新增 ID、RLT_23/RLT_24 各自验收行作集合相等检查，并用移去卡内引用的反例验证失败。全局提及 ID 已不能代替卡内引用。 | — |
| 第 1 轮 #6 机械完成判据 | CLOSED | — | `task_plan.md:6,19` 冻结 block/开放项编号；`:57-82`、`:91-116`、`:134-159` 分别对 RV、D1、C2-audit 的终态行作编号集合、重复与状态检查，并含说明文字、缺项和失败状态反例。原先会误伤普通说明文字的全局 `rg` 已移除。 | — |
| 新引入：条目合并与最低数量冲突 | FAIL | P2 | `task_plan.md:17` 允许有独立失败断言理由时合并条目；`:193` 却无条件要求 RLT_23/RLT_24 各至少四个 ID。若候选稿有经 fresh 审认可的合并，R 脚本仍会拒绝。当前按八个独立命题各发一条的路径不受影响。 | 将 C1 的“合并”例外收窄为不低于每卡四条，或让 R 按冻结的命题覆盖表核验有理由的合并；保持归属与按卡引用检查。 |

### 范围外发现

本轮未发现其他由 W1b 引入的范围外改动。第 1 轮已登记的 RLT_24 允许路径冲突仍由编排另行处理。

## 第 3 轮

### 结论

REVISE；W1c/W1d 窄审新增 P1=3、P2=0。A2 唯一旧行例外的批准文本与两类反例有效，扩展后的设计/DevPlan 落点也与 dispatch 路径表一致；但 Issue 同步闸、B-013 事实覆盖判据和 20 词一致性检查尚未闭合。本轮未读取或评判并行修改的候选稿 v3，也未执行 C2/R。

### 逐项判据

| # | 判据 | 结论 | 级别(P1/P2/—) | 依据（文件:行/小节） | 整改动作 |
|---|---|---|---|---|---|
| 1 | decision.1 §1 出口 A 与 decisions O-005 的落点 | PASS | — | `decision.1.md:15-28`、`decisions.md:30-35`；`brief.md:11,21,25,31-32` 与 `task_plan.md:5,18-19,125-126` 明列本事件冻结第 20 控制词/note 协议、A2 唯一旧行例外及 RLT_24 只实现的分工；`dispatch/README.md` 扩展段同范围。 | — |
| 2 | A2 精确例外及反例 | PASS | — | `task_plan.md:180-204`：冻结旧 A2 与 `origin/master` 逐字相等，批准新 A2 与 `decision.1.md:21-23` 逐字相等，其余旧 ID 行逐字保留并拒重复。用同一 `valid_old_rows` 判据在内存副本实跑：批准 A2 通过、改一条非 A2 旧行被拒、A2 改为“21 词”被拒。 | — |
| 3 | 新增允许路径 | PASS | — | `dispatch/README.md` 扩展段、`brief.md:25`、`task_plan.md:18,125-126,240-248,257`：设计仅加 §0.1、§3.2～§3.4、A2、§12 两格及原有落点；DevPlan 仅加 RLT_24 目标/非目标/变更范围三句，`allowed-paths` 与 master 逐字相同；仍限定四类文件路径。计划要求逐 hunk 对照已裁决候选块；实际晋级差异留给 C2/R 核对。 | — |
| 4 | decision.1 §3 的 B-013 固定片段与事实完整性 | FAIL | P1 | `decision.1.md:99-135` 要求两个锚点逐字匹配，并对五节事实、fresh P1/P2、O-ID、B-ID 建完整性判据及四种内存删项反例。`task_plan.md:6,126,134-159` 写了这些要求，但唯一可执行的 C2-audit 脚本只核 `PROMOTE B-ID status=MATCH` 集合；没有定义 evidence 中问题闭合/裁决/晋级项的固定记录格式、提取与集合比较函数，四种删项反例无可调用的判定器。人工写一行 `PROMOTE B-013 status=MATCH` 即可通过现有脚本。 | 为 evidence 的 fresh 问题、O-ID、B-ID 和固定锚点各定义可解析记录，给 C2/C2-audit 一段实际可运行的集合、重复、状态、文件/锚点校验；用同一验证函数跑四种删项内存反例。保留事实真实性的独立人工复核。 |
| 5 | §0.1/§3.3/§3.4/A2 的 20 词一致性 | FAIL | P1 | `decision.1.md:17` 特别要求改写 §3.4 A09 段“仍是那 19 个词”。`task_plan.md:227-235` 只查 §0.1/§3.3/§3.4 含任意“20 个/词”，§3.2/§3.4 含 `resource_close`；§3.4 同时保留旧“仍是那 19 个词”并另加一处“20 词”仍可通过。计划把该脚本称为“四处 20 词一致”有假阳性。 | 除新口径存在外，检查 §3.4 指定 A09 旧句已被批准的新沿革句逐字替换，且现行事件层计数段无未限定的“仍是 19 词”断言；以“新 20 词行存在但旧句残留”的内存反例证明会失败。 |
| 6 | Issue #37 扩界前置闸 | FAIL | P1 | `decision.1.md:18` 要求用户批准后更新 Issue #37，`dispatch/README.md` 明言 Issue **正文**是目标/非目标/验收权威；`decisions.md:34` 却只计划在晋级前以评论落记。`brief.md:21`、`task_plan.md:5,126` 允许按本地裁决解释原 Issue，未设晋级前核对远端边界的判据。本轮 `gh issue view 37` 读到的正文仍写“不动账本 schema”“验收 ID 行只增不改”，评论为空。 | 在 C2 前增加硬闸：由获授权的编排更新 Issue #37 的权威正文，逐项写明 A2 唯一旧行例外、设计词表/note 子协议及 RLT_24 卡分工；重新读取 Issue 核对这些字段与 `decisions.md` O-005 一致。未同步前保持候选/计划状态，不晋级。 |

### 范围外发现

无。本轮结论仅针对 W1c/W1d 的 brief/task_plan 与裁决、路径合同，不传递为候选稿 v3 的审核结论。

## 第 4 轮

### 结论

APPROVE；W2 第 3 轮 #4、#5、#6 均闭合，本轮新引入 P1=0、P2=0。W1e 的新增判据已用内存样例实跑；正式 evidence/11 尚未晋级，其实文与事实真实性留待 C2/C2-audit 核验。本轮不审候选稿，也不代签晋级结论。

### 逐项判据

| # | 判据 | 结论 | 级别(P1/P2/—) | 依据（文件:行/小节） | 整改动作 |
|---|---|---|---|---|---|
| 4 | B-013 固定片段与五节事实覆盖判据 | PASS | — | `task_plan.md:127-210,220-221,386-390` 定义 CLOSE/DECIDED/INCLUDED/ANCHOR 整行记录，按 fresh P1/P2、O-ID、B-ID 做集合、重复、状态、章节及文件/行号核验；两个固定片段各恰一次，事实真实性由 C2-audit 独立核。直接提取计划中的 `formats`、`records`、`validate` 函数，以合成的完整 evidence 与真实设计文件第 1 行作只读夹具实跑：完整样例通过；删除固定锚点、CLOSE、DECIDED、INCLUDED 各一项均触发断言；额外 OPEN 与失效行号反例亦被拒。正式 evidence/11 未创建，完整脚本的实文断言须在 C2/C2-audit 执行。 | — |
| 5 | 四处 20 词与 §3.4 A09 沿革句 | PASS | — | `task_plan.md:254,313-330,386-391` 的 `valid_word_count` 要求 §3.4 批准新句恰一次且拒旧句及“仍是 19 词”表述；提取原函数实跑：合成 20 词样例通过，在批准新句后残留原“仍是那 19 个词”句的反例失败。正式设计尚未晋级，R/C2 对实文的断言留在对应节点。 | — |
| 6 | Issue #37 权威正文扩界前置闸 | PASS | — | 本轮 `gh issue view 37 --json title,body` 读取正文：非目标【扩界】写明 `resource_close` 第 20 词、note 子协议、§0.1/§3.2～§3.4 与 §12 两格，设计冻结而实现/取证归 RLT_24；验收【扩界】列 A2 唯一旧行例外、原/新逐字校验及其他旧行不变（含 H10），并列 RLT_24 目标/非目标/变更范围与 allowed-paths 不变；“扩界记录”载用户 2026-09-16 选出口 A。与 `decisions.md:30-35`、`decision.1.md:15-28` 一致。 | — |

### 范围外发现

未见 W1e 或 Issue 正文同步引入新的 P1/P2。本轮只对计划判据及 Issue 边界作窄审，不评判并行中的候选稿或尚未产生的晋级实文。
