<!-- dh:v1 · RV 定向复审；只读，不替主控/用户做验收裁决。 -->
# RLT-A-11 候选稿 v3 定向复审（targeted-02）

- 对象：`design/drafts/A11/A11-候选.md` v3（622 行，C1b-part2 落稿）；基线 `HEAD = origin/master = bd118f6`。
- 输入：`review.fresh-01.md` 六项（P1-1～P1-5、P2-1）、`decisions.md`（2026-09-16 用户 O-001～O-005 全 CHOSEN A）、`decision.1.md` 采纳文本、`dispatch/README.md` 2026-09-16 扩展、`task_plan.md` RV 节判据。
- 方法：第一部分对 fresh 每个标题编号判终态；第二部分按派活做裁决落地核查（①各 B-block「旧文」与 `git show origin/master` 逐字一致且锚点唯一定位；②「新文」与 decision.1 §1/§2/§3 及 decisions.md 一致、无夹带；③20 词口径 §1.3/§3.2/§3.3/§3.4/A2 自洽且 §3.4 A09 旧句被改写；④A151～A158 连续、OWNER 与 B-009/B-010 一致、总账 148 算术；⑤H10 行不被改动；⑥grep 19 个/19 词有无遗漏）。

## 结论

REVISE——第一部分六项全部 CLOSED，但第二部分新发现 **1 条 P1（N-001）**：B-011 拟晋级文本把 Issue #37 边界同步机制写成「以 Issue 评论落记」，与 `decisions.md:34` 记录的权威正文直接更新、W2r4 的 `gh` 核验及本棒实测（Issue #37 评论数 = 0）不符；该句将逐字晋级入 DevPlan，须先纠正。另有 P2 两条（N-002、N-003），不阻断。

## 第一部分 · fresh 项终态

TARGET P1-1 status=CLOSED
- 候选行号：B-009（`A11-候选.md:227-236`，新文 `:235`）、B-010（`:238-247`，新文 `:246`）、§4 影响面 `:569-570`。
- 证据：两卡「验收口径」均为**原有单行替换**，各在父行内逐个列出本卡四个 ID（A151～A154 / A155～A158），未生成任何子行；行内仍含原判据要点（软表述禁令 / 预期覆盖项），机器证明细指向 design/01 §11.1 对应行。整改动作「压回原有一行、不生成 DevPlan 子行」已执行；`§4` 影响面「仅这两行允许动」随之成立。

TARGET P1-2 status=CLOSED
- 候选行号：§5 新增 `| O-005 |` 行（`:585`）与已裁决注记（`:587`）；B-014～B-024（`:294-513`）；§0.3（`:29`）、§3.1（`:522`）、§4（`:561,568,570`）。
- 证据：fresh 要求的「明确待裁前置」已落成 O-005 并经用户 2026-09-16 点选出口 A（`decisions.md:30-35`）；冲突三方各自处理——A2 由 B-014 作唯一旧行例外替换（旧文与 master `:1177` 逐字一致、新文与 `decision.1.md:22` 逐字一致）；Issue #37 边界由编排直接更新权威正文（`gh issue view 37` 实测正文含【扩界】子条与「扩界记录」节，见 N-001 旁证）；RLT_24 卡合同由 B-022/B-023/B-024 改为「只实现与取证已冻结设计」、allowed-paths 不动（`decision.1.md:26` 第 4 条）。原稿「全 A 不违反既有闸」类表述已删除（v3 全文 grep 无命中）。A157 已改为「执行已冻结 §12 路径并取证」，不再要求 RLT_24 写 design/01（`:168`）。

TARGET P1-3 status=CLOSED
- 候选行号：B-020 wire format 段（`:400-435`）；B-003 的 A155/A156（`:166-167`）；B-016（`:348`）、B-018（`:378`）；O-004 选项 A 描述（`:584`）。
- 证据：wire format 按 `decision.1.md` §2.1/§2.2 冻结——三个样例逐字一致、字段表（object_type/object_id/outcome/reason 必填与条件规则）、单空格分隔、百分号编码裸字符集 `[A-Za-z0-9._~-]`、禁重复/未知键/不设扩展键、写入者 pane→`monitor#<n>` 与 workspace/worktree→`orchestrator#<n>`、node 取相应阶段第一个有效节点、`add`/`lint` 同一协议时点表均落稿；A155/A156 与 §2.3 逐字提案**逐字相同**（脚本比对 count=1），reason 条件独归 A156、A155 明写「reason 条件仅由 A156 承接」，重叠已消除；O-004 选项 B（自由 note）按裁决落选，保留在 §5 作形成史。

TARGET P1-4 status=CLOSED
- 候选行号：B-013（`:271-292`，固定片段 `:280-284` 与 `:288-290`）；`task_plan.md:6,126-128,220-221`（W1c/W1e 判据）。
- 证据：B-013 已从「全文逐字块」改为**证据结构合同**——仅 `<a id="review-rlt-a11"></a>` + `dh:planning-evidence:v1` 注释与 `<a id="understanding-rlt-a11"></a>` 两个固定片段逐字晋级且各恰一次；五节标题列为完整性要求、不作全文匹配；「C2 晋级时按…填」「按实际复核轮次填」类占位句已不存在于 block 内；task_plan 已落 CLOSE/DECIDED/INCLUDED/ANCHOR 可解析记录与四种内存删项反例判据（W2r4 实测通过）。逐字身份与「按实际填」的互斥已解除。

TARGET P1-5 status=CLOSED
- 候选行号：B-012（`:260-269`，新文 `:268`）。
- 证据：拟晋级子句改为「**RLT-A-11 候选文本已在 `plan/rlt-a11` 分支晋级**……**仍待 R 复核、PR、CI 三硬门与合并收口——收口前 RLT_23/RLT_24 不开工**；『正式完成、开工闸解除』的状态回填由合并后的编排节点负责」——即整改动作要求的写法：C2 时点只记「已在分支晋级 + 门未闭合」，依赖解除回填留合并后节点；不再提前宣告完成或开工闸解除。

TARGET P2-1 status=CLOSED
- 候选行号：§1.1 RLT_12 清单标签（`:88`，「14 份」）、§1.2 映射表 review 行（`:114`，「`review.*.md` 五份及 `review.md` 一份」）。
- 证据：本棒实测 `git ls-files`——RLT_12 全目录 14 条、RLT_21 `review.*.md` 恰 5 条（review.lesson.X1/lesson/plan/requirement.X1/requirement）另有 `review.md` 1 条；8+29+14=51 路径总数与兜底分类结论不变，与 fresh 实测一致。

## 第二部分 · 裁决落地核查

### ① 旧文逐字与锚点唯一定位

对全部 24 个 B-block 逐块核对（脚本：`git show origin/master:<file>` + `str.count`）：

- **含「旧文逐字」块的 9 个 block 全部逐字命中 master 且 count=1**：B-014（A2 行 = design `:1177`）、B-015（§1.3 事件词表行 = `:96`）、B-016（§3.2 `note` 行 = `:218`）、B-017（§3.3 账本事件层行 = `:234`）、B-019（§3.4 A09 段子句 = `:295` 内子串）、B-021（§12 两行 = `:1326-1327`）、B-022/B-023/B-024（DevPlan RLT_24 卡 `:450/:452/:454`）。
- **锚点式 block 的定位全部唯一且行号属实**：B-001 锚（`| worktree | 人 / 主 session |`，`:1328`，§12 表尾）、B-002 锚（「程序不产生临时文件」段，`:1330`，§13 之前）、B-003 锚（`| HC-RL-A150 |`，`:1297`，§11.1 表尾）、B-004（`:1165`）、B-005（`:1383`）、B-006（`:7` 声明注释后）、B-007（`:9` 句内）、B-008（`:18` 索引段后、`:20` A-10 修订行前）、B-009（DevPlan `:438`）、B-010（`:453`）、B-011（`:49`）、B-012（`:21` 子句）、B-018 锚（`| \`plan_amend\` | 监工 |`，`:253`，控制事件表尾）、B-020 锚（§3.4 末：`:295`「待复核」段后、`:297` `### 3.5` 前）。上述锚字符串在各自目标文件均 count=1。
- B-013 为新建文件，无旧文（结构合同，见下）。

**结论：① 通过。**

### ② 新文与裁决一致性（无夹带）

- B-014 新文与 `decision.1.md:22`（§1.3 批准行）**逐字相同**，亦即 task_plan R 脚本的 `approved_a2`。
- B-019 新文与 task_plan `approved_a09` **逐字相同**，落实 `decision.1.md:17` 的 A09 段改写口径。
- B-003 的 A155/A156 与 `decision.1.md:81-82`（§2.3）**逐字相同**；A157/A158 按 §2.4 语义落稿（执行已冻结路径不改设计正文 / 71 行原字节兼容 + fixture 派生不变）。
- B-020 覆盖 §2.1 全部规则（样例逐字、字段表、token/编码/重复键、写入者、node、failed 语义）与 §2.2 add/lint 时点表；B-018 行内吸收 §2.1 的写入者与 node 规则；「前置计划合法性、首行 plan_loaded 等通用规则继续成立」由 §3.5 通用 `add` 校验兜底，不单列不矛盾。
- B-021 新文两格与 `decision.1.md:92`（§2.4 逐字提案）**逐字相同**。
- B-022/B-023/B-024 落实 `decision.1.md:26`（§1.4）：删「本卡改设计 §12」分工、非目标收紧为「不修改设计正文」、变更范围移除 §12 并注明允许路径不加 design/01；旧文与 master 逐字一致（见①）。
- B-013 与 `decision.1.md:99-135`（§3）及 task_plan W1c/W1e 判据一致。
- O-001（不改 H10 行、加回链）→ B-002 落 §12 末段且含 H10 尺子句；O-002（§12 末尾）→ B-002 锚点一致；O-003/O-004（独立第 20 词 + 完整 wire format）→ B-016/B-018/B-020 与 A155/A156 落地；O-005（出口 A）→ B-014～B-024 全套。
- **例外（N-001）**：B-011 新文（`:257`）写「Issue #37（边界同步更新由编排以 Issue 评论落记）」——与 `decisions.md:34`「编排已于 2026-09-16 直接更新 Issue #37 权威正文（承接 W2 第 3 轮 #6：评论不足以改权威合同）」不符；本棒 `gh issue view 37` 实测：正文含【扩界】子条与「扩界记录」节、**评论数 = 0**。属新文与裁决记录不一致。

### ③ 20 词口径自洽与 A09 旧句

- §1.3（B-015）、§3.3（B-017）、A2（B-014）三处均写「20 个/词」；§3.4 由 B-018 新增第 20 个控制事件行、B-020 落 wire format、B-019 以批准句写「词表为 20 词」；§3.2 由 B-016 加 `resource_close` 事件专属例外。四处口径互洽。
- §3.4 A09 段旧句「**账本事件层词表不变**——仍是那 19 个词…」由 B-019 整句替换为批准文本；v3 后 §3.4 不再残留「仍是（那）19 个/词」表述（B-020 的「当前实现仍为 19 词」是「实现侧」事实陈述，与词表计数不冲突，且不匹配 R 判据的 `仍是…19` 反例模式）。
- 旁证：master §3.4 控制事件表 9 行 + agent 事件 10 个 = 19，新增 `resource_close` 后 20，计数成立。

**结论：③ 通过。**

### ④ A151～A158 连续性、OWNER 与总账

- B-003 八行恰为 `HC-RL-A151`～`A158` 连续无跳号（`:162-169`）；master 最大 `HC-RL-A150` 实测成立。
- §3.3 唯一归属清单（`:542-551`）八行：A151～A154→RLT_23、A155～A158→RLT_24，每卡恰四条；与 B-009/B-010 新文逐 ID 列出的归属一致，与 §3.2 表、§4 影响面、B-008/B-011 的口径一致。
- 总账算术：140 + 8 = 148 = AI 133（125+8）+ 人验 15；B-004（`:180`）与 §3.1（`:521-522`）一致；A2 修订保号不增条目。

**结论：④ 通过。**

### ⑤ H10 行

- 全部 B-block 无一触碰 `HC-RL-H10` 行（master `:1315`）；B-002 仅在 §12 新段回链并给「人判尺子」读法，符合 O-001=A（不改 H10 命题列）。

**结论：⑤ 通过。**

### ⑥ 「19 个 / 19 词」遗漏扫描

- design/01 master 全部词数口径出现处：`:96`（§1.3 表）、`:234`（§3.3 表）、`:295`（§3.4 A09 段）、`:1177`（A2 行）——分别由 B-015/B-017/B-019/B-014 覆盖，无遗漏。其余 `19` 命中均为 ID（A19/A119/RLT_19）或历史修订数字（119→121），非词数口径。
- **同族新发现（非词数而是事件枚举）**：`HC-RL-A85`（design `:1225`）把控制事件按写入者二分为 orchestrator 四类 + monitor 五类「与全部 agent 事件」——`resource_close` 的写入者按 object_type 分叉（pane→monitor、workspace/worktree→orchestrator），两类都装不下；O-005 只放行 A2，该行将逐字保留而过时。A68（`:1229` 节点级时序）/A89（`:1230` 阶段级时序）同族但更弱——`resource_close` 无节点/阶段级时序约束（任意位置、不限次），不列入可辩护。详见 N-002。
- DevPlan 历史行：RLT_03 卡 `:185`「19 个事件词 fail closed」、RLT_22 卡 `:415`/`:432`「19 词白名单」系已完成卡的取证/范围历史记录，不在本事件允许路径内，按「不追溯改历史」可原样保留，但宜由编排确认口径（N-003）。

**结论：⑥ design/01 词数口径无遗漏；枚举类陈旧另立 N-002、N-003。**

### 新问题

| 编号 | 级别 | 位置（文件:行/节） | 问题 | 依据 | 整改动作 |
|---|---|---|---|---|---|
| N-001 | P1 | 候选稿 `:257`（B-011 新文） | 「边界同步更新由编排以 Issue 评论落记」为事实错误：编排实际**直接更新了 Issue #37 权威正文**，且该 Issue 评论数为 0；W2r3 #6 曾明确「评论不足以改权威合同」并因此硬闸过一轮。该句若逐字晋级，会把合同边界的权威落点误记为评论。 | `decisions.md:34`；`plan-review.md` W2r3 #6 / W2r4 #6（PASS 记录正文已更新）；本棒 `gh issue view 37 --json comments` 实测 `n_comments=0`、正文含【扩界】子条 | 将 B-011 该子句改为如实口径，如「Issue #37（边界同步更新已由编排直接落进权威正文，2026-09-16）」；改动须回写候选稿并可复核 |
| N-002 | P2 | design/01 `:1225`（HC-RL-A85 行，本事件不可改） | A85 的写入者二分枚举覆盖当时全部 9 个控制事件，`resource_close` 加入后该枚举不完整且无法用现有两类表达其按 object_type 分叉的写入者；§3.5 写者一致性校验委派给 §3.4 表故操作合同不破，A155 另有写入者校验，但 §11.1 将长期保留一条过时枚举。A68/A89 时序枚举同族但可辩护（新事件无时序约束）。 | master `:1225` 原文；候选 B-018/B-020 写入者规则；`decisions.md` O-005「A2 为唯一旧行例外」 | 三选一由编排定：(a) 在 B-018 或 B-020 新文补一句「A85 写入者枚举系 19 词时代口径、未含本事件，法定写入者以 §3.4 表为准」；(b) 升为用户裁决扩例外集；(c) 登记 findings 作已知陈旧，后续规划事件修订 A85/A68/A89 |
| N-003 | P2 | DevPlan `:185`（RLT_03 机器证）、`:415`/`:432`（RLT_22 非目标/实施提示） | 三处「19 个事件词 / 19 词白名单」在 A2 改 20 词后成为历史口径；均属已完成卡的取证与范围记录，不在本事件允许路径内。 | `git show origin/master:P1-RelayLight-开发方案.md` 对应行；dispatch 允许路径不含这些卡句 | 建议按历史记录原样保留（「不追溯改历史工件」同口径），由编排确认无需注释即可；若要不误导读者，可在后续顺手补注 |

## RV 判据脚本输出

从 worktree 根原样运行 `task_plan.md` RV 节判据脚本（`A=docs/modules/relay-light/design/drafts/A11` + 内嵌 python3）：

```text
RV_RECORDS_OK ['P1-1', 'P1-2', 'P1-3', 'P1-4', 'P1-5', 'P2-1']
```

结论：**REVISE**（第一部分六项全 CLOSED；第二部分新 P1 = N-001，另 P2 = N-002、N-003）。
