<!-- dh:v1 · RLT-A-11 最小 A-adjust 候选稿 v4 · 未生效。
     本文件不属于 designInputs[]，不得作为 B 拆计划或施工输入。
     Issue #37 目标三条与 decisions.md 的用户逐项裁决是已裁决来源；
     本稿的全部具体措辞、编号与落点均为**待复核 / 待晋级提案**，不代表已生效设计。 -->
# RLT-A-11 候选稿 v4 — §12 兜底类行、职责分层口径、验收 ID 续发与 `resource_close` 第 20 词冻结

- **目标文档**：[`design/01-RelayLight-产品设计与验收.md`](../../01-RelayLight-产品设计与验收.md)、[`dev_plan/P1-RelayLight-开发方案.md`](../../../dev_plan/P1-RelayLight-开发方案.md)
- **事件 ID**：`RLT-A-11`（design 侧既有事件已使用至 `RLT-A-10`）；**Issue #37**
- **状态**：**候选稿 · 未生效**（C1c 整改后 v4：fresh-01 六项经 RV targeted-02 判 CLOSED；RV 新发现 N-001 / N-002 / N-003 于本版整改，待下一轮复审）
- **触发事实**：`workspace/RLT_11/findings.md` 的 **F-001**（§12 六类枚举非穷举）与 **F-002**（账本 `commit=` 在 squash 后悬空）经用户 2026-09-16 裁决改判为设计措辞落盘；同事件为 RLT_23 / RLT_24 续发验收 ID；同日用户裁决 **O-005 出口 A**，本事件一并冻结 `resource_close` 第 20 事件词。
- **版本 / 更改对照**：v1 为 C1 首稿，全部新增；v2 按 fresh-01 整改 P1-1 / P1-5 / P2-1；v3 按 `decisions.md`（O-001～O-005 全选 A）与 `decision.1.md` 整改 P1-2 / P1-3 / P1-4；v4 按 RV targeted-02 整改 N-001（B-011 Issue 口径更正）/ N-002（B-020 补 A85 限定句）/ N-003（findings 登记）（见文末「整改对照」）。

---

## 0. 目标与边界

### 0.1 一句话

设计 §12 的持久化产物表补一行兜底类（计划目录 / 任务工作区内未被前六类点名的产物随所在目录同口径保留），并把「账本复现接力现场、workspace + git + Issue 复现施工现场」的职责分层口径落盘、回链 `HC-RL-H10`；同时自 `HC-RL-A151` 起为 RLT_23 / RLT_24 各续发四条验收 ID；经 O-005 裁决同批在设计正文冻结 `resource_close` 第 20 事件词与其 `note` wire format（`HC-RL-A2` 为本事件唯一修订的既有验收行）。

### 0.2 已裁决的来源（Issue #37 目标三条，原文口径）

1. **F-001**：§12 表尾补兜底类行。背景产物均已按所在目录口径正确保留，属措辞缺口、非行为缺陷。
2. **F-002**：新增显式职责分层——账本复现**接力现场**（谁在何时交棒给谁、阶段/节点状态如何流转、哪一步阻塞），workspace 文档 + git + Issue 复现**施工现场**（这一步改了什么、为什么这么改）；回链 `HC-RL-H10`。推论：账本 `note` 内 `commit=<sha>` 是顺手旁注，squash 收口后失效**不构成契约破坏**。
3. **续发验收 ID**：现最大 `HC-RL-A150`，自 **A151** 起；只续号，不退役、不改号、不复用。

### 0.3 不动的边界（Issue #37 非目标 + 停止线）

- 不改任何实现代码，不动账本 schema 的**实现**（`resource_close` 落地是 RLT_24）；**2026-09-16 O-005 出口 A 例外**：允许在设计正文冻结 `resource_close` 第 20 事件词与其 `note` 子协议——`HC-RL-A2` 为唯一改动的既有验收行，Issue #37 边界由编排同步更新。
- 不追溯修改历史工件里已失效的 SHA 引用。
- 不借本次 A-adjust 夹带其他设计改动（含 RLT_22 findings 的 F-005 / F-010）。
- 不改 dev-harness、不改 skill、不改 AGENTS.md；不 commit、不 push、不建分支。
- 本候选不授权晋级、D-start、verify、验收、合并或推送。

---

## 1. 触发事实与实际清单/映射

### 1.1 实际产物清单（`git ls-files` 实测，2026-09-16，HEAD=`bd118f6`）

真计划 `rlt12-win-01` 计划目录（`docs/modules/relay-light/relay/rlt12-win-01/`）：

```text
docs/modules/relay-light/relay/rlt12-win-01/dispatch/monitor-C1.md
docs/modules/relay-light/relay/rlt12-win-01/dispatch/monitor-C2.md
docs/modules/relay-light/relay/rlt12-win-01/dispatch/monitor-F1.md
docs/modules/relay-light/relay/rlt12-win-01/dispatch/monitor-R1.md
docs/modules/relay-light/relay/rlt12-win-01/dispatch/monitor-W1.md
docs/modules/relay-light/relay/rlt12-win-01/dispatch/monitor-X1.md
docs/modules/relay-light/relay/rlt12-win-01/relay_log.jsonl
docs/modules/relay-light/relay/rlt12-win-01/relay_plan.md
```

任务工作区 `docs/modules/relay-light/workspace/RLT_21/`（29 份）：

```text
docs/modules/relay-light/workspace/RLT_21/brief.md
docs/modules/relay-light/workspace/RLT_21/check.C1.md
docs/modules/relay-light/workspace/RLT_21/check.C2.md
docs/modules/relay-light/workspace/RLT_21/done.builder.md
docs/modules/relay-light/workspace/RLT_21/done.checker.C2.md
docs/modules/relay-light/workspace/RLT_21/done.checker.md
docs/modules/relay-light/workspace/RLT_21/done.coder.C2.md
docs/modules/relay-light/workspace/RLT_21/done.coder.X1.md
docs/modules/relay-light/workspace/RLT_21/done.coder.md
docs/modules/relay-light/workspace/RLT_21/done.lesson.R1.md
docs/modules/relay-light/workspace/RLT_21/done.lesson.X1.md
docs/modules/relay-light/workspace/RLT_21/done.plan-reviewer.md
docs/modules/relay-light/workspace/RLT_21/done.requirement.R1.md
docs/modules/relay-light/workspace/RLT_21/done.requirement.X1.md
docs/modules/relay-light/workspace/RLT_21/done.scribe.C2.md
docs/modules/relay-light/workspace/RLT_21/done.scribe.F1.md
docs/modules/relay-light/workspace/RLT_21/done.scribe.R1.md
docs/modules/relay-light/workspace/RLT_21/done.scribe.md
docs/modules/relay-light/workspace/RLT_21/execution_strategy.md
docs/modules/relay-light/workspace/RLT_21/findings.md
docs/modules/relay-light/workspace/RLT_21/lesson_candidates.md
docs/modules/relay-light/workspace/RLT_21/progress.md
docs/modules/relay-light/workspace/RLT_21/review.lesson.X1.md
docs/modules/relay-light/workspace/RLT_21/review.lesson.md
docs/modules/relay-light/workspace/RLT_21/review.md
docs/modules/relay-light/workspace/RLT_21/review.plan.md
docs/modules/relay-light/workspace/RLT_21/review.requirement.X1.md
docs/modules/relay-light/workspace/RLT_21/review.requirement.md
docs/modules/relay-light/workspace/RLT_21/task_plan.md
```

任务工作区 `docs/modules/relay-light/workspace/RLT_12/`（14 份）：

```text
docs/modules/relay-light/workspace/RLT_12/brief.md
docs/modules/relay-light/workspace/RLT_12/evidence/linux-dry-run/README.md
docs/modules/relay-light/workspace/RLT_12/evidence/linux-dry-run/reviews/rlt-b07-fresh-review-sol.md
docs/modules/relay-light/workspace/RLT_12/evidence/linux-dry-run/reviews/rlt-b07-targeted-review-sol.md
docs/modules/relay-light/workspace/RLT_12/evidence/win-dry-run/README.md
docs/modules/relay-light/workspace/RLT_12/execution_strategy.md
docs/modules/relay-light/workspace/RLT_12/findings.md
docs/modules/relay-light/workspace/RLT_12/lesson_candidates.md
docs/modules/relay-light/workspace/RLT_12/progress.md
docs/modules/relay-light/workspace/RLT_12/review.code-round1.md
docs/modules/relay-light/workspace/RLT_12/review.lesson.md
docs/modules/relay-light/workspace/RLT_12/review.md
docs/modules/relay-light/workspace/RLT_12/review.requirement.md
docs/modules/relay-light/workspace/RLT_12/task_plan.md
```

### 1.2 逐类映射与「前六类无遗漏」结论

| 实际产物 | §12 现行归属 | 结论 |
|---|---|---|
| `relay/rlt12-win-01/relay_log.jsonl`、`relay_plan.md` | 第 1 类（账本与计划，随 `relay/<plan_id>/` 永久保留） | 已点名 |
| `relay/rlt12-win-01/dispatch/monitor-{W1,C1,C2,R1,X1,F1}.md` 六份监工派单 | **未点名** | → 拟增兜底行（计划目录随目录保留） |
| `workspace/RLT_21/check.C1.md`、`check.C2.md` | 第 2 类（`check.<节点>.md`，随任务工作区合入保留） | 已点名 |
| `workspace/RLT_21/review.*.md` 五份及 `review.md` 一份、`progress.md`、`findings.md`；`workspace/RLT_12/findings.md`、`progress.md`、`review.code-round1.md`、`review.lesson.md`、`review.md`、`review.requirement.md` | 第 3 类（`review.<路径>.md` / `progress.md` / `findings.md`） | 已点名 |
| `workspace/RLT_21/done.*.md` 十五份、`brief.md`、`task_plan.md`、`execution_strategy.md`、`lesson_candidates.md`；`workspace/RLT_12/brief.md`、`task_plan.md`、`execution_strategy.md`、`lesson_candidates.md` | **未点名** | → 拟增兜底行（工作区随目录保留） |
| `workspace/RLT_12/evidence/` 四份（linux-dry-run README、两份复核报告、win-dry-run README） | **未点名** | → 拟增兜底行（工作区随目录保留） |

**结论**：上述 51 个已跟踪路径中，落在第 1～3 类的均已点名正确；其余全部归入拟增的第 7 类兜底行，无第 8 类需求——「前六类无遗漏」成立的条件是补上兜底行。第 4～6 类（阶段终端空间与 pane、编排终端空间、worktree）是非文件产物，不在 `git ls-files` 范围内，维持原表述。

### 1.3 F-002：`commit=` 悬空与职责分层裁决

- 实测：`rlt12-win-01` 账本 note 内 `commit=<sha>` 共 24 处 + seq 54/66 两处裸引用，10 个 SHA 经 `git cat-file -t` 在 master 线上全部 MISSING（squash 合入只保留合并提交）。
- 用户 2026-09-16 裁决：账本复现**接力现场**，施工现场靠 workspace 文档 + git + Issue 恢复；`commit=` 本就只是顺手旁注，其失效不构成契约破坏，F-002 由「接受为常态」改判「按设计本就如此」。
- 该分层此前从未写进设计；不落盘则后续做 `HC-RL-H10` 人判的人会拿错尺子（把「复原施工改动」也压给账本）。

---

## 2. 逐字晋级文本（B-block 清单）

> 每个 `### B-…` 块对应一个可比对的连续文本片段；C2 逐字晋级，裁决改字须先回写本稿。

### B-001 — design/01 §12 表尾兜底行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§12 表格 `| worktree | 人 / 主 session | … |` 行**之后**，表尾新增一行
- 动作：新增
- 来源：Issue #37 目标 1（F-001）；§1.2 映射表

```text
| 计划目录 / 任务工作区内、以上六类未点名的其余持久化产物（如 `relay/<plan_id>/dispatch/` 派单、`done.*.md`、`brief.md` / `task_plan.md` / `execution_strategy.md` / `lesson_candidates.md`、`evidence/` 取证目录） | 不删 | 随所在目录同口径保留——计划目录内随 `docs/modules/<模块>/relay/<plan_id>/` 永久保留，任务工作区内随工作区合入保留 | N/A |
```

### B-002 — design/01 §12 职责分层段

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§12 末尾，「程序**不产生临时文件**（纯追加）……是复盘唯一证据。」段**之后**新增一段
- 动作：新增
- 来源：Issue #37 目标 2（F-002）；§1.3

```text
**两个「现场」分开复现**（RLT-A-11，承接 2026-09-16 用户裁决）：**账本**的职责是复现**接力现场**——谁在何时交棒给谁、阶段 / 节点状态如何流转、哪一步阻塞、谁救的场；**施工现场**——这一步改了什么、为什么这么改——由**任务工作区文档 + git 历史 + Issue** 复现，不由账本承担。`HC-RL-H10` 的人判尺子据此读作「只凭账本能否还原接力现场」，不要求账本独自复原施工改动；账本 `note` 内的 `commit=<sha>` 仅为顺手旁注，其指向的提交在 squash 收口后失效**不构成契约破坏**——施工现场的权威指针本来就是 git 与 Issue，不是账本 `note`。
```

### B-003 — design/01 §11.1 表尾新增八条

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§11.1 表格 `| HC-RL-A150 | … |` 行**之后**，表尾新增八行
- 动作：新增
- 来源：Issue #37 目标 3；§3.2 条目草案（措辞以 §3.2 为准，此处为晋级形态）

```text
| HC-RL-A151 | 派活通知投递确认（RLT-A-11 新增，RLT_23 承接 F-005）：skill 派活纪律段与两份 adapter 的编排/监工模板各含「向 agent 发通知后必须读 pane 末行确认实际投递；pane 出现 `queued` 排队提示时补 `send-keys enter` 并复核送达；未确认投递不得当作已通知」原文 | 结构检查三处命中该纪律原文；静态检查模板无「发出即视为送达」类软表述 |
| HC-RL-A152 | codex 启动档位按主控侧分叉（RLT-A-11 新增，RLT_23 承接 F-006）：adapter 与派活纪律写明——Claude 主控下 codex worker 以默认 sandbox 启动、不加 `--dangerously-bypass-approvals-and-sandbox`（该 flag 被本地 auto 分类器拦；worker 只在 worktree 内写文档时默认 sandbox 已够）；Codex 主控下沿用既有 bypass 结论；不得写成无条件全局口径 | 结构检查两份 adapter 各命中分叉表述；静态检查仓内 skill 无「codex 一律 bypass」类无条件措辞 |
| HC-RL-A153 | `agent_lost` 判据禁止 pane 状态单一来源（RLT-A-11 新增，RLT_23 承接 F-007）：监工模板写明 pane 的 `working → done` 不等于 agent 收工（长 `sleep` 中也会被报 `done`）；判 `agent_lost` 前必须同时确认 pane 无 `Running tools` 计时器在走、账本无该 agent 新行、Herdr `agent get` 状态非 working；仅凭 pane 报 `done` 不得判死重拉 | 结构检查监工模板命中三要素与「不得单凭 pane 状态」原文；与 A140「三者均无变化才中断」口径一致 |
| HC-RL-A154 | F 阶段收口 checklist 含显式删树确认（RLT-A-11 新增，RLT_23 承接 F-003）：skill 的 F 阶段收口清单含独立一项「确认对应 worktree 已删（`git worktree list` / `git branch` 核对），先关终端空间再删树」，为可勾选确认项而非软提醒 | 结构检查 F 模板收口清单命中该项原文，且该项为独立 checklist 行 |
| HC-RL-A155 | 独立关闭控制事件 resource_close 的基础合同（RLT_24）：第 20 事件词；note 按 §3.4 协议解析，必填 object_type/object_id/outcome，闭集、编码、重复键、未知键及写入者/节点合法性均校验；该事件不进入 agent 状态机，不改变节点或阶段派生状态。reason 条件仅由 A156 承接 | 单测分别覆盖 add 与直接植入行后的 lint：三类对象合法 ok 正例；缺基础键、空标识、非法类型/outcome、非法编码、重复键、未知键、错误 writer/node 各自退出 2，add 拒绝前后账本字节一致；合法关闭事件在终态节点后及重复尝试仍可接受，插入前后状态派生一致；本条不重复验证 reason 与 outcome 的条件关系 |
| HC-RL-A156 | 关闭事件失败原因条件（RLT_24）：在其余字段合法时，outcome=failed 必须有非空且非纯空白的 reason，outcome=ok 必须无 reason；add 落盘前与 lint 接受行前执行同一条件校验，合法 failed 可检索且 lint 通过，不新增 status --json 字段 | 对 add 与直接植入行后的 lint 各测：failed 无 reason、reason 为空、reason 解码后纯空白，以及 ok 带非空或空 reason 均退出 2；failed 带有效原因通过并按 seq/object_id 检索到原行；add 拒绝均不改账本。所有反例保持 A155 基础字段合法 |
| HC-RL-A157 | §12 两类终端空间「删失败怎么办」取证路径可执行（RLT-A-11 新增，RLT_24 承接 F-004）：RLT_24 **不修改设计正文**，只执行并验证 RLT-A-11 已冻结的 §12 取证路径——两类终端空间各制造至少一例可控关闭失败（实跑或打桩并明确标注），账本含合法 `resource_close outcome=failed` 行，以 `seq` / `object_id` 实际检索到该行，处置记录落 workspace 的 evidence / progress | 证据含命令与观察结果、合法失败行、按 `seq` / `object_id` 实际检索的输出、人工处置记录或明确的待人工处理状态（不冒称已处置）；结构存在与可执行取证均由本条核验 |
| HC-RL-A158 | 历史账本向后兼容（RLT-A-11 新增，RLT_24）：引入 `resource_close` 后，原 71 行 `rlt12-win-01` 账本字节不变、全部旧行在新实现下被接受、`lint` 退出 0、与旧实现基线的稳定 `status` 字段一致（固定输入、排除动态时间字段）；含合法 `resource_close` 行的 fixture `lint` 退出 0，插入前后 agent / node / stage 派生结果一致 | 单测：71 行原样重放断言逐条接受、`lint` 退出 0、与基线 `status` 输出一致（排除动态时间字段）；含合法关闭行的 fixture 断言派生不变；不把历史兼容写成历史补记 |
```

### B-004 — design/01 §11 总账行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§11 开头 `> **共 140 条：…` 行（现第 1165 行）
- 动作：替换（整行）
- 来源：Issue #37 验收口径「机械核验：只增不改」的正面总账；§3.1 编号核实

```text
> **共 148 条：AI 自动验收 133 条 + 人类验收 15 条**（2026-09-11 RLT-A-06 退役 A91/A108、续发 A131～A136，AI 净增 4；退役 H2、续发 H18，人验净值 0；2026-09-14 RLT-A-08 续发 A137～A143，AI 净增 7；2026-09-15 RLT-A-09 续发 A144～A150，AI 净增 7，人验净值 0，退役清单不增；2026-09-16 RLT-A-11 续发 A151～A158，并修订既有行 A2——账本事件层白名单 19→20 词，保号不改号，为本事件唯一改动的既有验收行；AI 净增 8，人验净值 0，退役清单不增）。
```

### B-005 — design/01 §15「验收 ID 稳定性」行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§15 表格 `| 验收 ID 稳定性 | … |` 行（现第 1383 行）
- 动作：替换（整行）
- 来源：Issue #37 目标 3

```text
| 验收 ID 稳定性 | 包内唯一，新条目一律续号；RLT-A-06 退役 A91/A108/H2，续发 A131～A136/H18，不复用旧号；RLT-A-08 续发 A137～A143、RLT-A-09 续发 A144～A150、RLT-A-11 续发 A151～A158，三者**均只续号，不退役、不改号、不复用**；RLT-A-11 另**修订既有行 A2**（账本事件层白名单 19→20 词，保号不改号——经 2026-09-16 用户裁决 O-005 出口 A 授权，为本事件唯一改动的既有验收行）。完整退役清单见 §11，A131～A134 保留 `split-from`，H18 保留 `supersedes`。 |
```

### B-006 — design/01 文件头 planning-event 声明行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：第 7 行 `RLT-A-07` 声明注释**之后**新增一行
- 动作：新增
- 来源：Issue #37 验收口径第 4 条；`review=` / `understanding=` 指向拟新建 B-013 文件内的可解析锚点

```text
<!-- dh:planning-event:v1 id=RLT-A-11 stage=A-adjust artifact=design/01-RelayLight-产品设计与验收.md review=evidence/11-交叉审核记录-RLT-A11-产物兜底与验收续发.md#review-rlt-a11 understanding=evidence/11-交叉审核记录-RLT-A11-产物兜底与验收续发.md#understanding-rlt-a11 -->
```

### B-007 — design/01 文件头「活动声明」句

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：第 9 行历史索引段尾句 `**活动声明为上面的 \`RLT-A-06\` 与本次最小澄清 \`RLT-A-07\`；DevPlan 活动事件仍是 \`RLT-B-06\`。**`
- 动作：替换（仅该句）
- 来源：与 B-006 配套，声明行增加后该句须同步

```text
**活动声明为上面的 `RLT-A-06`、`RLT-A-07` 与本次 `RLT-A-11`；DevPlan 活动事件仍是 `RLT-B-06`。**
```

### B-008 — design/01 文件头 RLT-A-11 修订行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：第 18 行索引段结束**之后**、第 20 行 `RLT-A-10` 修订行**之前**新增一行（新修订行置顶于既有修订行序列）
- 动作：新增
- 来源：A09/A10 修订行先例；本事件全貌

```text
> **RLT-A-11 最小 A-adjust 2026-09-16（用户已逐条裁决开放项）**——§12 表尾补兜底类行：计划目录 / 任务工作区内未被前六类点名的持久化产物随所在目录同口径保留；新增「账本复现接力现场、workspace 文档 + git + Issue 复现施工现场」职责分层口径并回链 `HC-RL-H10`，账本 `note` 内 `commit=<sha>` 定位为顺手旁注、squash 后失效不构成契约破坏。续发 HC-RL-A151～A158 八条（RLT_23 承接 A151～A154、RLT_24 承接 A155～A158）；经 O-005 裁决本事件扩界：账本事件层新增第 20 个控制事件 `resource_close` 并冻结其 `note` wire format（§1.3 / §3.2～§3.4 同步，§12 两类终端空间「删失败怎么办」单元格给出取证路径），**修订既有行 A2（19→20 词，为本事件唯一改动的既有验收行）**；其余旧行**不退役、不改号、不复用**，退役清单不增。活动总账 140→148。复核结论、用户裁决与晋级范围见 [`evidence/11-交叉审核记录-RLT-A11-产物兜底与验收续发.md`](evidence/11-交叉审核记录-RLT-A11-产物兜底与验收续发.md)。**本行只记设计晋级，不代表 D-start、不代表验收、不代表 verify。**
```

### B-009 — DevPlan `#### RLT_23`「验收口径」行

- 目标文件：`dev_plan/P1-RelayLight-开发方案.md`
- 定位锚点：`#### RLT_23` 卡内 `- **验收口径**：待 RLT-A-11 续发（\`HC-RL-A151~\`）。落点预期：…软表述）。` 行（现第 438 行）
- 动作：替换（**仍为原有的一行**，四个归属 ID 逐个列出、不用范围缩写；不生成子行——允许路径只授权「验收口径」行，fresh-01 P1-1）
- 来源：§3.3 归属清单；W2 第 2 轮 P2 约束（两卡各 ≥4 条）

```text
- **验收口径**：承接 `HC-RL-A151` / `HC-RL-A152` / `HC-RL-A153` / `HC-RL-A154` 四条（RLT-A-11 续发；来源 `workspace/RLT_11/findings.md` F-005/F-006/F-007/F-003）——通知投递确认、codex 启动档位按主控侧分叉、`agent_lost` 不凭 pane 状态单源、F 阶段删树确认；四条纪律各自可在 skill/模板文本中定位，措辞可机械核验（不用「注意某某」这类软表述）；逐条机器证见 design/01 §11.1 对应行。
```

### B-010 — DevPlan `#### RLT_24`「验收口径」行

- 目标文件：`dev_plan/P1-RelayLight-开发方案.md`
- 定位锚点：`#### RLT_24` 卡内 `- **验收口径**：待 RLT-A-11 续发（\`HC-RL-A151~\`）。预期至少覆盖：…可执行。` 行（现第 453 行）
- 动作：替换（**仍为原有的一行**，四个归属 ID 逐个列出、不用范围缩写；不生成子行——同 P1-1）
- 来源：§3.3 归属清单；W2 第 2 轮 P2 约束

```text
- **验收口径**：承接 `HC-RL-A155` / `HC-RL-A156` / `HC-RL-A157` / `HC-RL-A158` 四条（RLT-A-11 续发；来源 `workspace/RLT_11/findings.md` F-004）——`resource_close` 事件 schema 合法性、`lint` 对 `outcome=failed` 的分校验、§12 两类终端空间「删失败怎么办」列取证路径可执行、历史账本（含 `rlt12-win-01` 71 行）原样 `lint` 退出 0；逐条机器证见 design/01 §11.1 对应行。
```

### B-011 — DevPlan §0「RLT-A-11 调整」条

- 目标文件：`dev_plan/P1-RelayLight-开发方案.md`
- 定位锚点：§0 列表 `- **RLT-A-11 调整（待开工）**：…` 整条（现第 49 行）
- 动作：替换（整条；标题去掉「（待开工）」，尾部补落盘事实与证据回链）
- 来源：A09 调整条先例（第 48 行）；本事件落盘事实

```text
- **RLT-A-11 调整**：2026-09-16 RLT_11 收口后用户逐条裁决 `workspace/RLT_11/findings.md` 七条转派项。其中 **F-001**（§12 六类枚举非穷举——真计划另产出六份监工派单、RLT_21 工作区 `done.*.md`×15 及四份工件、RLT_12 `evidence/`，均已按所在目录口径正确保留，属措辞缺口）与 **F-002**（账本 note 内 `commit=<sha>` 在 squash 收口后悬空——实测 24 处 `commit=` + 2 处裸引用、10 个 SHA 全部 MISSING）经本事件落盘设计正文：§12 表尾补兜底类行；新增「账本复现接力现场、workspace 文档 + git + Issue 复现施工现场」职责分层口径并回链 `HC-RL-H10`，`commit=` 定位为顺手旁注、squash 后失效不构成契约破坏。本事件为 RLT_23/RLT_24 续发验收 ID `HC-RL-A151`～`HC-RL-A158` 八条（RLT_23：A151～A154；RLT_24：A155～A158）。同日用户裁决 O-005 出口 A，本事件一并冻结 `resource_close` 第 20 事件词与其 `note` wire format（design §1.3 / §3.2～§3.4 同步、§12 两类终端空间「删失败怎么办」单元格给取证路径），并**修订既有行 A2（19→20 词，本事件唯一改动的既有验收行）**；RLT_24 卡的目标 / 非目标 / 变更范围三句同步改写为「只实现与取证已冻结设计」，其允许路径不加 design/01。只续号、不退役、不改号（A2 保号）、不复用；活动总账 140→148。Issue #37（边界同步更新已由编排直接落进权威正文，2026-09-16）；复核与裁决证据见 [`design/evidence/11-交叉审核记录-RLT-A11-产物兜底与验收续发.md`](../design/evidence/11-交叉审核记录-RLT-A11-产物兜底与验收续发.md)。
```

### B-012 — DevPlan `dh:status`「下一步」行相关句

- 目标文件：`dev_plan/P1-RelayLight-开发方案.md`
- 定位锚点：`dh:status` 块「下一步:」行（现第 21 行）内子句 `三者构成新增第 6 批，**RLT-A-11 落盘前 RLT_23/RLT_24 不开工**。`
- 动作：替换（仅该子句）
- 来源：dispatch 允许路径「顶部现状/下一步中与 RLT-A-11 相关的句子」

```text
三者构成新增第 6 批；**RLT-A-11 候选文本已在 `plan/rlt-a11` 分支晋级**（Issue #37；§12 兜底类行、职责分层口径回链 `HC-RL-H10`、续发 `HC-RL-A151`～`HC-RL-A158`、冻结 `resource_close` 第 20 事件词与 wire format 并修订 A2；证据见 design/evidence/11），**仍待 R 复核、PR、CI 三硬门与合并收口——收口前 RLT_23/RLT_24 不开工**；「正式完成、开工闸解除」的状态回填由合并后的编排节点负责。
```

### B-013 — 新建 evidence/11 交叉审核记录（证据结构合同，非全文逐字）

- 目标文件：`design/evidence/11-交叉审核记录-RLT-A11-产物兜底与验收续发.md`（**新建**）
- 定位锚点：新文件；下列两个固定片段各须在 evidence/11 中**恰出现一次**，供 B-006 声明的 `review=` / `understanding=` 解析
- 动作：新增文件；**本 block 是证据结构合同**——仅两个固定片段逐字晋级，五节事实正文按实际发生的复核、裁决与晋级结果填写，受 task_plan C2 / C2-audit 完整性判据约束（W1c 已落），不对本 block 执行全文逐字匹配；禁止以占位句、预写 APPROVE 或未来 PROMOTE 结论充当证据
- 来源：decision.1 §3（fresh-01 P1-4，编排代执行小决策）；evidence/10 结构先例

固定片段 1（review 锚点 + `dh:planning-evidence:v1` 注释，两段相邻、恰一次）：

```text
<a id="review-rlt-a11"></a>

<!-- dh:planning-evidence:v1 event=RLT-A-11 artifact=design/01-RelayLight-产品设计与验收.md kind=review -->
```

固定片段 2（understanding 锚点，恰一次）：

```text
<a id="understanding-rlt-a11"></a>
```

五节标题完整性要求（结构存在性校验，**不是**全文字符串匹配对象）：触发事实、复核与整改、用户裁决、晋级清单、停止线。复核与整改逐项覆盖 fresh-01 全部 P1/P2 编号并回链真实存在的复审报告与 CLOSED 记录；用户裁决逐项覆盖本稿全部 O-ID，引用 `decisions.md` 的 CHOSEN 行与用户原话 / 日期；晋级清单逐项覆盖本稿全部 B-ID 并列出真实目标文件与可定位锚点 / 行号。

### B-014 — design/01 §11.1 `HC-RL-A2` 行替换（唯一旧行例外）

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§11.1 表格 `| HC-RL-A2 |` 行（现第 1177 行）
- 动作：替换（整行；**本事件唯一改动的既有验收 ID 行**，O-005 出口 A 授权；R 批须断言原 A2 与冻结基线一致、新 A2 与本块逐字一致）
- 来源：decision.1 §1.3 逐字行；decisions.md O-005

旧文逐字：

```text
| HC-RL-A2 | 账本事件层 19 词白名单 fail closed | 单测：19 个合法词全过；未知词退出码 2 且不落盘 |
```

新文逐字：

```text
| HC-RL-A2 | 账本事件层 20 词白名单 fail closed（原 19 词加 resource_close；新增词的实现由 RLT_24 承接） | 单测：20 个合法词以各自合法上下文和字段通过；未知词退出码 2 且不落盘 |
```

### B-015 — design/01 §1.3「事件词表」行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§1.3 核心决策清单表 `| 事件词表 | 19 个，…` 行（现第 96 行；dispatch 扩展所称「§0.1 词表计数」即此行）
- 动作：替换（整行）
- 来源：decision.1 §1「出口 A」第 1 条；O-005

旧文逐字：

```text
| 事件词表 | 19 个，分控制事件与 agent 事件两类，白名单 fail closed | 自由字符串 | 中 |
```

新文逐字：

```text
| 事件词表 | 20 个，分控制事件与 agent 事件两类，白名单 fail closed（RLT-A-11 新增 `resource_close` 进控制事件；新增词的实现由 RLT_24 承接） | 自由字符串 | 中 |
```

### B-016 — design/01 §3.2 `note` 字段行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§3.2 字段表 `| \`note\` | 自由文本 |` 行（现第 218 行）
- 动作：替换（整行）
- 来源：decision.1 §1「出口 A」第 1 条「§3.2 note 的事件专属例外」

旧文逐字：

```text
| `note` | 自由文本 |
```

新文逐字：

```text
| `note` | 自由文本；**例外**（RLT-A-11）：`event=resource_close` 时 `note` 按 §3.4 的关闭事件 wire format 逐键校验，非自由文本 |
```

### B-017 — design/01 §3.3「账本事件层」计数行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§3.3 三层词表 `| **账本事件层** | 19 个，见下 | …` 行（现第 234 行）
- 动作：替换（整行）
- 来源：decision.1 §1「出口 A」第 1 条「§3.3 计数」

旧文逐字：

```text
| **账本事件层** | 19 个，见下 | 编排与监工 |
```

新文逐字：

```text
| **账本事件层** | 20 个，见下（RLT-A-11 新增 `resource_close`） | 编排与监工 |
```

### B-018 — design/01 §3.4 控制事件表新增 `resource_close` 行

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§3.4 控制事件表 `| \`plan_amend\` | …` 行（现第 253 行）**之后**，控制事件表尾新增一行
- 动作：新增
- 来源：decision.1 §1「出口 A」第 1 条 + §2.1 写入者 / 节点规则

```text
| `resource_close` | pane 归监工（`monitor#<n>`）；终端空间（阶段空间与编排空间）与 worktree 归编排（`orchestrator#<n>`） | 关闭动作记账（RLT-A-11），任意位置、不限次，**不进状态机**；`node` 指向相应阶段第一个有效节点（编排终端空间 / worktree 取收口 F 阶段第一个有效节点），须引用存在且非 superseded 的节点，允许在 `node_close` / `stage_close` 之后记录，不要求 `agent_launch`，不改变节点 / 阶段关闭状态；同一对象多次关闭尝试逐次记账；`note` 按本节末 wire format 逐键校验，任一不合法退出 `2` 且账本字节不变 |
```

### B-019 — design/01 §3.4 A09 段「仍是那 19 个词」改写

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§3.4「待复核」信号段（现第 295 行）内子句 `**账本事件层词表不变**——仍是那 19 个词，信号落在 \`checkpoint\` 现有的合法迁移里`
- 动作：替换（仅该子句）
- 来源：decision.1 §1「出口 A」第 1 条「A09 段『仍是那 19 个词』改写口径」

旧文逐字：

```text
**账本事件层词表不变**——仍是那 19 个词，信号落在 `checkpoint` 现有的合法迁移里
```

新文逐字：

```text
**账本事件层词表在 RLT-A-09 当时未扩展**——信号落在 `checkpoint` 现有的合法迁移里（RLT-A-11 新增 `resource_close` 后词表为 20 词，送审信号仍使用 `checkpoint`）
```

### B-020 — design/01 §3.4 末新增 `resource_close` wire format 段

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§3.4 末（「待复核」信号段之后、`### 3.5` 之前）新增
- 动作：新增（一段连续文本，含样例、字段表、token 规则与 add/lint 时点表；外层四反引号包裹内含三反引号样例块）
- 来源：decision.1 §2.1（语法和字段）与 §2.2（add/lint 拒绝时点）；O-004 采纳文本

````text
**关闭事件 `resource_close` 的 `note` wire format**（RLT-A-11 冻结；这是 RLT_24 的目标合同——当前实现仍为 19 词、未交付该事件能力）：JSONL 顶层七字段与 `note` 的字符串类型不变；仅当 `event=resource_close` 时启用下列完整字符串协议，不解析其他事件的自由 `note`，不新增 `status --json` 字段。

```text
object_type=pane object_id=pane-7 outcome=ok
object_type=workspace object_id=stage-C1 outcome=failed reason=permission%20denied
object_type=worktree object_id=%2Ftmp%2Frlt24 outcome=ok
```

| 键 | 必填 / 可选 | 解码后合法值 |
|---|---|---|
| `object_type` | 必填一次 | `workspace` / `pane` / `worktree`，大小写敏感闭集 |
| `object_id` | 必填一次 | 非空、非纯空白的 UTF-8 文本；记录可辨识的真实资源标识，worktree 使用绝对路径；校验时不要求资源仍存在（关闭后可能已消失） |
| `outcome` | 必填一次 | `ok` / `failed`，大小写敏感闭集 |
| `reason` | 条件字段 | `failed` 必填一次且非空、非纯空白；`ok` 必须完全不存在该键（`reason=` 空值也不允许） |

- token 用**一个 ASCII 空格**分隔；不允许首尾空格、连续空格、裸换行或 Tab；每个 token 恰有一个裸 `=`；键顺序不影响接收，推荐按上表顺序写出；无其它自由尾文。
- 值先编码成 UTF-8 再作百分号编码：裸字符只允许 `[A-Za-z0-9._~-]`，其它字节写 `%HH`，接收大小写十六进制；空格用 `%20`，`+` 用 `%2B`，`=` 用 `%3D`，`%` 用 `%25`；解码只执行一次，`+` 不作为空格。非法 `%` 序列、无效 UTF-8、解码后控制字符均拒绝；标识内普通空格不被 trim 改写。
- 所有键禁止重复（即使值相同）；未知键、缺键、空值、枚举非法、非字符串 `note` 均拒绝；不做「最后值覆盖」，不静默忽略 malformed token；不设额外可选扩展键。
- 写入者：`object_type=pane` 记 `agent=monitor#<n>, by=monitor`；`workspace`（阶段空间与编排空间）与 `worktree` 记 `agent=orchestrator#<n>, by=orchestrator`，沿用既有实例名格式。实际执行人可为人，账本写入者记录其可观察结果，不把记录权限当成关闭资源授权。
- `HC-RL-A85` 的写入者二分枚举系 19 词时代口径、未含 `resource_close`；`resource_close` 的法定写入者以 §3.4 控制事件表为准。
- `note` 是事实载体，不得填凭据值；`outcome=failed` 表示关闭尝试失败，不自动等同阶段 `failed`，不自动执行删除或重试。

**`add` 与 `lint` 的拒绝时点**（同一协议、同一校验集，不允许「add 接收、等 lint 才发现」）：

| 入口 | 时点 | 必须行为 |
|---|---|---|
| `add` | CLI 输入解析后、任何账本字节追加前 | 通用行校验 → 写入者 / `node` 校验 → 完整 `note` 语法与字段校验 → `reason` 条件校验；任一不合法退出 `2`，账本字节不变；不将 `resource_close` 送进 agent 状态机 |
| `lint` | 逐行读取、通用 JSON / 七字段 / `seq` 校验后、接受该行或派生状态前 | 对 `resource_close` 执行与 `add` 相同的语义校验；任一非法退出 `2` 并指出 `seq` / 字段及原因；只读、不修复、不补默认值；合法 `failed` 是一条合法事实，不因此判 lint 失败 |
````

### B-021 — design/01 §12 两类终端空间「删失败怎么办」单元格

- 目标文件：`design/01-RelayLight-产品设计与验收.md`
- 定位锚点：§12 表格 `| 阶段的终端空间与 pane | …` 与 `| 编排的终端空间 | …` 两行（现第 1326–1327 行），两行的「删失败怎么办」单元格
- 动作：替换（两行连续片段；逐字文本按 decision.1 §2.4）
- 来源：decision.1 §2.4 逐字提案；O-005 出口 A 第 1 条「§12 两类终端空间失败取证列」

旧文逐字：

```text
| 阶段的终端空间与 pane | 监工 / 编排 | agent 干完关 pane；阶段结束编排关整个终端空间 | 报错交人，不强删 |
| 编排的终端空间 | 人 | 全计划结束后关 | 同上 |
```

新文逐字：

```text
| 阶段的终端空间与 pane | 监工 / 编排 | agent 干完关 pane；阶段结束编排关整个终端空间 | 由对应账本写入者按 §3.4 写 resource_close，note 含 object_type、object_id、outcome=failed 与非空 reason；以该行 seq 和 object_id 定位失败，再将账本路径/seq、观察证据和人工处置记录写入 workspace 的 evidence/progress；报错交人，不强删。记录失败也须保留原始观察证据并报错，不得宣称已落账。 |
| 编排的终端空间 | 人 | 全计划结束后关 | 由对应账本写入者按 §3.4 写 resource_close，note 含 object_type、object_id、outcome=failed 与非空 reason；以该行 seq 和 object_id 定位失败，再将账本路径/seq、观察证据和人工处置记录写入 workspace 的 evidence/progress；报错交人，不强删。记录失败也须保留原始观察证据并报错，不得宣称已落账。 |
```

### B-022 — DevPlan `#### RLT_24`「目标」句

- 目标文件：`dev_plan/P1-RelayLight-开发方案.md`
- 定位锚点：`#### RLT_24` 卡内 `- **目标**：…` 行（现第 450 行）
- 动作：替换（整行；删去「本卡改设计 §12」分工，改为实现与验证已冻结设计）
- 来源：decision.1 §1「出口 A」第 4 条；O-005

旧文逐字：

```text
- **目标**：给「关闭」动作定义独立事件位 `resource_close`（名称待定），带 `outcome=ok|failed`、对象标识（终端空间 / pane / worktree）与失败原因字段；相应更新账本 schema 与 `lint` 分校验；使设计 §12 两类终端空间的「删失败怎么办」一列由「永远未实测」转为**可实测**并给出取证路径；新事件正反路径有单测覆盖。
```

新文逐字：

```text
- **目标**：实现 RLT-A-11 已冻结的关闭事件 `resource_close` 与其 `note` wire format（design §3.4）：`object_type` 闭集（`workspace` / `pane` / `worktree`）、`object_id`、`outcome=ok|failed` 与 `reason` 条件；相应更新账本 schema 与 `lint` 分校验；执行并验证 RLT-A-11 已落盘的 §12 两类终端空间「删失败怎么办」取证路径（本卡不改设计正文）；新事件正反路径有单测覆盖。
```

### B-023 — DevPlan `#### RLT_24`「非目标」句

- 目标文件：`dev_plan/P1-RelayLight-开发方案.md`
- 定位锚点：`#### RLT_24` 卡内 `- **非目标**：…` 行（现第 452 行）
- 动作：替换（整行；末句由「不动 §12 以外的设计章节」收紧为「不修改设计正文」）
- 来源：decision.1 §1「出口 A」第 4 条

旧文逐字：

```text
- **非目标**：不改 herdr、不要求其提供关闭结果 API（事件由主控/监工按实际观察落账）；不追溯给历史账本补记关闭事件；不扩展到终端空间/pane/worktree 之外的资源类型；不动 §12 以外的设计章节。
```

新文逐字：

```text
- **非目标**：不改 herdr、不要求其提供关闭结果 API（事件由主控/监工按实际观察落账）；不追溯给历史账本补记关闭事件；不扩展到终端空间/pane/worktree 之外的资源类型；**不修改设计正文**——§12 取证列、A2 词表数与 §3.4 wire format 等设计变更由已批准的 RLT-A-11 承接冻结，本卡只实现与取证。
```

### B-024 — DevPlan `#### RLT_24`「变更范围」句

- 目标文件：`dev_plan/P1-RelayLight-开发方案.md`
- 定位锚点：`#### RLT_24` 卡内 `- **变更范围**：…` 行（现第 454 行）
- 动作：替换（整行；删去「设计 §12 该列表述」，允许路径不加 design/01）
- 来源：decision.1 §1「出口 A」第 4 条

旧文逐字：

```text
- **变更范围**：`tools/relay-light/relay_log.py` 与其单测、设计 §12 该列表述、本卡工作区。
```

新文逐字：

```text
- **变更范围**：`tools/relay-light/relay_log.py` 与其单测、本卡工作区（§12 取证列等设计正文已由 RLT-A-11 冻结，不在本卡变更范围；允许路径不新增 design/01）。
```

---

## 3. 续发验收条目（A151～A158）

### 3.1 编号核实

- 实测基线 `bd118f6`（= `origin/master` = HEAD）：`git show origin/master:docs/modules/relay-light/design/01-RelayLight-产品设计与验收.md | rg -o 'HC-RL-A[0-9]+' | sort -Vu | tail -1` → `HC-RL-A150`；人验最大 `HC-RL-H18`；总账 140 = AI 125 + 人验 15（design/01 第 1165 行）。
- 新号段 `A151`～`A158` 共八条，在 design/01 中均不存在，与 A150 无重叠、无跳号；晋级后总账 **148 = AI 133 + 人验 15**。退役清单**不新增**任何 ID；A1～A150 中**仅 `HC-RL-A2` 一行经 O-005 出口 A 裁决修订**（19→20 词断言，B-014，Issue #37「只增不改」边界的唯一例外由编排同步更新），其余旧行一字不改。
- 分配：RLT_23 四条纪律 → A151～A154；RLT_24 四个命题 → A155～A158。**两卡各恰四条，不低于四条、不做合并**（W2 第 2 轮 P2 约束 + 编排补充约束）。

### 3.2 新增条目草案（晋级形态即 B-003 八行；此处补设计意图）

| ID | 归属 | 承接 | 独立失败断言（为何不可合并） |
|---|---|---|---|
| `HC-RL-A151` | RLT_23 | F-005 通知投递确认 | 纪律文本是否命中「读 pane 末行 + queued 补 send-keys enter + 复核」 |
| `HC-RL-A152` | RLT_23 | F-006 codex 档位按主控侧分叉 | 两份 adapter 是否各命中分叉表述、且无全局无条件措辞 |
| `HC-RL-A153` | RLT_23 | F-007 `agent_lost` 非 pane 单源 | 模板是否含三要素与「不得单凭 pane 状态」 |
| `HC-RL-A154` | RLT_23 | F-003 F 阶段删树确认 | F checklist 是否含独立删树确认行 |
| `HC-RL-A155` | RLT_24 | F-004 `resource_close` 基础合同 | 第 20 词 + wire format 基础结构 + 状态机隔离；缺键/空标识/非法枚举/非法编码/重复键/未知键/错误 writer-node 各自退 2——reason 条件不在本条，可独立失败于 A156 |
| `HC-RL-A156` | RLT_24 | F-004 `reason` 条件 | `failed` 必填非纯空白 `reason`、`ok` 禁带；`add` 与 `lint` 同一条件校验——可独立失败于 A155（反例保持 A155 基础字段合法） |
| `HC-RL-A157` | RLT_24 | F-004 §12 取证路径 | 执行**已冻结**的 §12 两类终端空间路径并取证（可控失败 + `seq`/`object_id` 检索 + 处置记录），不改设计正文——可独立失败于 schema 条目 |
| `HC-RL-A158` | RLT_24 | F-004 历史账本兼容 | 71 行原字节重放 `lint`=0、与旧实现基线 `status` 一致；合法关闭行 fixture 派生不变——可独立失败于新增功能本身 |

八条的「验收项 / 怎么证明」逐字文本见 B-003（A155/A156 为 decision.1 §2.3 逐字、A157/A158 按 §2.4 改写）。每条均可独立判失败，故不合并；RLT_23/RLT_24 各四条满足「各 ≥4」的硬约束。

### 3.3 唯一归属清单（供 C2/R 按卡验收行核验）

```text
OWNER HC-RL-A151 RLT_23
OWNER HC-RL-A152 RLT_23
OWNER HC-RL-A153 RLT_23
OWNER HC-RL-A154 RLT_23
OWNER HC-RL-A155 RLT_24
OWNER HC-RL-A156 RLT_24
OWNER HC-RL-A157 RLT_24
OWNER HC-RL-A158 RLT_24
```

---

## 4. 影响面点名

| 位置 | 改什么 |
|---|---|
| `design/01` §12 表 | B-001 兜底行（表尾新增一行）+ B-021 两类终端空间「删失败怎么办」单元格（O-005） |
| `design/01` §12 末段 | B-002 职责分层段（新增；H10 命题列不改，O-001 已裁决 A） |
| `design/01` §11.1 表 | B-003 新增 A151～A158 八行 + **B-014 修订 `HC-RL-A2`（唯一旧行例外，O-005 授权）**；其余 A1～A150 与人验栏一行不动 |
| `design/01` §11 头部 | B-004 总账 140→148（AI 125→133；A2 修订不增减条目） |
| `design/01` §15 | B-005「验收 ID 稳定性」行补 RLT-A-11 与 A2 例外 |
| `design/01` 文件头 | B-006 声明行 + B-007 活动声明句 + B-008 修订行 |
| `design/01` §1.3 | B-015「事件词表」行 19→20（O-005） |
| `design/01` §3.2 | B-016 `note` 字段行加 `resource_close` 事件专属例外（O-005） |
| `design/01` §3.3 | B-017 账本事件层计数 19→20（O-005） |
| `design/01` §3.4 | B-018 控制事件表新增 `resource_close` 行 + B-019 A09 段「19 词」改写 + B-020 wire format 段（O-003/O-004/O-005） |
| `dev_plan` RLT_23/RLT_24 卡 | B-009 / B-010「验收口径」行（仅这两行允许动，逐 ID 列出） |
| `dev_plan` RLT_24 卡 | B-022 / B-023 / B-024「目标 / 非目标 / 变更范围」三句（O-005 扩展授权；允许路径行不动、不加 design/01） |
| `dev_plan` §0 / dh:status | B-011 调整条、B-012「下一步」相关句 |
| `design/evidence/` | B-013 新建 `11-交叉审核记录-RLT-A11-产物兜底与验收续发.md`（证据结构合同：两固定锚点片段逐字 + 五节完整性判据） |
| 其余一切 | **不动**——代码、账本 schema 的**实现**、skill、AGENTS、dev-harness、历史工件 SHA；除 A2 外其余既有验收 ID 行逐字不变 |

---

## 5. 开放项（已于 2026-09-16 逐项裁决，全部 CHOSEN A——见 `decisions.md`；下表保留候选与理由作形成史）

| O-编号 | 开放项 | 候选 A | 候选 B | 本稿倾向 | 理由 | 不同裁决会改哪些 block |
|---|---|---|---|---|---|---|
| O-001 | `HC-RL-H10` 命题列是否同步改 | 不改 H10 行，只在 §12 新段加 H10 回链 | 同步修订 H10 命题列措辞 | **A** | Issue #37 验收口径「验收 ID 行集合相对 master 只增不改」与改 H10 行**直接冲突**；选 B 须先由编排处理 Issue 验收边界，不得按现合同直接晋级 | 选 B 时新增一个 H10 行替换 block；B-002 不变 |
| O-002 | 职责分层段落位置 | §12 末尾新增段（B-002 现状） | 改放 §3.4 账本章节末 | **A** | 该口径回答「持久化产物各自证明什么」，§12 是产物与退场的权威段；做 H10 人判者沿验收行回链即落在同节 | 选 B 时 B-002 锚点改为 §3.4 末、首句措辞微调（另给替换文本） |
| O-003 | RLT_24 关闭动作的事件载体 | 账本事件层新增第 20 词 `resource_close`（控制事件类、不进状态机） | 复用 `checkpoint` + 类型化 token（沿用 RLT-A-09 套路，不动 A2 的 19 词） | **A** | 关闭对象是资源（终端空间 / pane / worktree）而非 agent 生命周期动作；独立事件位让「关失败」有一等账本位置，正对 F-004 缺口；代价是 A2 的 19 词须由本事件修订（见 O-005） | 选 B 时 B-003 的 A155/A156/A157 行、B-010、B-011、B-018～B-020 相应改写 |
| O-004 | `resource_close` 字段合同 | 完整 wire format（decision.1 §2 为准）：`object_type`∈{workspace,pane,worktree}、`object_id`、`outcome`∈{ok,failed} 必填一次，`failed` 必带非空非纯空白 `reason`、`ok` 禁带该键；单个 ASCII 空格分隔 `key=value`、值百分号编码（裸集 `[A-Za-z0-9._~-]`）、禁重复 / 未知键、不做最后值覆盖；`add` 落盘前与 `lint` 接受行前同一协议拒绝；写入者 pane 归监工、workspace / worktree 归编排 | 最小合同：只冻 `outcome` 与对象类型，标识与原因进自由 note | **A** | F-004 的病根是「失败无处可写」；字段合同不冻结则 §12「删失败怎么办」仍不可实测；自由 note 无结构可机械核验 | 选 B 时 B-003 的 A155/A156 行、B-010、B-016、B-020 相应改写 |
| O-005 | fresh-01 P1-2 出口：第 20 事件词与 A2（19 词）/ Issue #37「只增不改、不动 schema」/ RLT_24 允许路径的冲突怎么解 | 本事件扩界——一并改 A2（19→20）、§0.1/§3.2～§3.4、§12 两格取证路径与 RLT_24 卡目标/非目标/变更范围三句；Issue #37 边界同步更新（A2 为唯一旧行例外）；设计一次审清，RLT_24 只实现 | 另立规划前置——本事件只续号登记需求级验收，第 20 词合同由独立规划事件冻结后 RLT_24 方可施工 | **A** | 同一 `resource_close` 合同一次审清，不把第二次设计修订藏进 RLT_24；B 存在条件条目被误当施工输入的风险，且须另起一套规划审批 | 选 A 时新增 B-014～B-024 并同步 B-004/B-005/B-008/B-011/§3.1/§4；选 B 时 A155～A158 改条件性登记、不新增设计块 |

> 裁决结果（2026-09-16，`decisions.md`）：**O-001～O-005 全部 CHOSEN A**。O-005 出口 A 使本事件扩界——`HC-RL-A2` 成为唯一改动的既有验收行（Issue #37 边界同步更新、dispatch 允许路径已扩展至 §0.1/§3.2～§3.4/§12 两格与 RLT_24 卡三句）；O-004 采纳文本以 `decision.1.md` §2 为准；fresh-01 P1-4 由 W1c 落 task_plan 判据、本稿 B-013 已改为证据结构合同。

---

## 6. 本稿自身验收与停止线

### 6.1 可机器核对

1. §1.1 三个 fenced 清单与 `git ls-files` 三组命令输出逐行一致（6 + 15 + 4 = 25 个路径；RLT_21 四份额外工件与 RLT_12 全目录另行列出）。
2. `A151`～`A158` 在 design/01 中均不存在；OWNER 清单八行恰覆盖八条、每卡四条。
3. B-009 / B-010 的「验收口径」父行各含且仅含本卡四个 ID；B-003 八行均为 `| HC-RL-A1xx |` 表行形态。
4. B-006 声明的 `review=` / `understanding=` 与 B-013 两个固定片段内 `<a id="review-rlt-a11">` / `<a id="understanding-rlt-a11">` 对得上；B-013 仅含两个固定锚点片段、不含占位骨架全文。
5. 本批未改 design/01 与 DevPlan 任何字节（`git diff --quiet` 对两文件成立）。
6. 除 B-014（A2 行）外，本稿不存在对既有 `HC-RL-*` 行的替换 block；B-014 旧文与 `git show origin/master:docs/modules/relay-light/design/01-RelayLight-产品设计与验收.md` 的 A2 原行逐字一致。
7. B-014～B-024 各含「旧文逐字 / 新文逐字 / 锚点」（新增块 B-018/B-020 无旧文），且各对应 dispatch/README 允许路径扩展内的一个点名位置。

### 6.2 停止线 / 纪律声明

- 本文件是**候选稿**，不是既成事实；§5 五项已于 2026-09-16 经用户裁决（全 A，`decisions.md`），但在 RV 定向复审、C2 晋级、C2-audit、R 复核与 PR/CI/合并完成前，全部 B-block 仍为待落盘提案。
- 本稿未跑实现测试（纯设计文档事件）；不替用户勾选、不代签任何验收。
- 不构成晋级、D-start、verify、验收、PR、合并或清理授权；RLT_23/RLT_24 开工闸在本事件落盘前保持关闭。
- 本稿不含任何密钥或凭据值。

### 整改对照

| 原项 | 改动位置 | 改动摘要 | 是否闭合自评 |
|---|---|---|---|
| fresh-01 P1-1（DevPlan 验收口径提案扩出允许行） | B-009 / B-010 | 两卡「验收口径」各压回**原有单行**：父行逐 ID 列出归属四 ID + 判据要点，删去全部八条「机器证」子行；机器证明细由 design/01 §11.1 对应行承载；§4 影响面「仅这两行允许动」随之恢复成立 | 是——单行含且仅含本卡四 ID，无新增 DevPlan 行 |
| fresh-01 P1-5（顶部状态提案提前解除两卡依赖闸） | B-012 | 拟改子句改为「候选文本已在 `plan/rlt-a11` 分支晋级，仍待 R 复核、PR、CI 三硬门与合并收口——收口前 RLT_23/RLT_24 不开工」；「正式完成、开工闸解除」回填留给合并后的编排节点 | 是——不再宣告落盘完成或开工闸解除 |
| fresh-01 P2-1（两处数量标签不准） | §1.1 RLT_12 清单标签、§1.2 映射表 review 行 | 「13 份」→「14 份」；「review.*.md 六份」→「review.*.md 五份及 review.md 一份」；51 路径总数与兜底分类不变 | 是——与 `git ls-files` 实测一致 |
| fresh-01 P1-2（第 20 事件词与 A2/Issue #37/RLT_24 允许路径冲突） | 新增 O-005（§5）并经用户裁决选 A；新增 B-014～B-024；§0.3 / §3.1 / §4 / B-004 / B-005 / B-008 / B-011 / B-012 同步 | 本事件扩界：B-014 修订 A2（唯一旧行例外）、B-015～B-017 词表计数与 note 例外、B-018～B-020 第 20 词与 wire format 入 §3.4、B-021 §12 两格取证路径、B-022～B-024 RLT_24 卡三句改为「只实现与取证」；允许路径不加 design/01，Issue #37 边界由编排同步更新 | 是——冲突出口经用户裁决落稿；机械闸（A2 精确例外判据）列入 C2/R 判据 |
| fresh-01 P1-3（wire format 与 A155/A156 重叠未定） | B-003 的 A155/A156、B-016、B-018、B-020、O-004 选项 A | A155/A156 采用 decision.1 §2.3 逐字（reason 条件独归 A156，A155 管基础合同）；wire format 按 §2.1/§2.2 冻结进 B-020（字段表、token 规则、写入者/node、add/lint 同一协议时点表）；O-004 选项 A 描述对齐 decision.1 §2 | 是——事件载体与字段合同均为已裁决采纳文本 |
| fresh-01 P1-4（B-013 占位骨架与逐字晋级矛盾） | B-013 | 改为证据结构合同：两个固定锚点片段（`review-rlt-a11` + `dh:planning-evidence:v1` 注释、`understanding-rlt-a11`）逐字晋级且各恰一次；五节标题列完整性要求、不作全文匹配；W1c 已将对应判据落入 task_plan 共同约束 / C2 / C2-audit | 是——不再要求对未发生事实做逐字晋级 |
| O-005 新增行（非 fresh 编号，P1-2 出口的开放项登记） | §5 开放项表 | 新增 `| O-005 |` 行：问题=fresh-01 P1-2 出口；候选 A=本事件扩界，B=另立规划前置；本稿倾向 A；用户已于 decisions.md 选 A | 是——D1 判据所需首列格式与 O-001 一致 |
| RV N-001（B-011 误写「以 Issue 评论落记」） | B-011 新文 | 改为「Issue #37（边界同步更新已由编排直接落进权威正文，2026-09-16）」；全文 grep「评论」仅剩本整改行说明，B-008 修订行、B-013 固定片段、§4 影响面无同类残留 | 是——与 decisions.md 记录及 `gh` 实测（评论数 0、正文含【扩界】子条）一致 |
| RV N-002（A85 写入者二分枚举装不下 resource_close） | B-020 新文 | 取选项 (a)：wire format 段写入者条下补一句「`HC-RL-A85` 的写入者二分枚举系 19 词时代口径、未含 `resource_close`；`resource_close` 的法定写入者以 §3.4 控制事件表为准」；不新增改 A85 的 block，A2 仍是唯一旧行例外 | 是——限定句已落，A85/A68/A89 陈旧登记 findings.md C1c-F-001 |
| RV N-003（DevPlan 历史卡「19 词」口径） | findings.md | 按「不追溯改历史工件」原样保留，登记 findings.md C1c-F-002（RLT_03 :185、RLT_22 :415/:432） | 是——登记备查，不加注不改卡句 |

> fresh-01 六项 P1/P2 已经 RV targeted-02 判 CLOSED；RV 新发现 N-001～N-003 于 v4 整改完毕，待复审确认。
