<!-- dh:v1 · C1-audit fresh 独立审核；只读候选稿，不构成用户裁决或晋级授权。 -->
# RLT-A-11 候选稿 fresh-01 审核

- 对象：`design/drafts/A11/A11-候选.md` v1（416 行）；基线 `HEAD = origin/master = bd118f6`。
- 核对：Issue #37 正文、RLT_11 findings F-001/F-002 与裁决落记、设计 §0.1/§3/§11/§12/§15、DevPlan RLT_23/RLT_24、W1 合同与 C1 task_plan。`git ls-files` 实测计划目录 8、RLT_21 29、RLT_12 14，共 51 个已跟踪路径；候选清单没有漏路径。设计中尚无 A151～A158，候选八行及八条 OWNER 连续、无重复，140→148 = AI 125→133 + 人验 15 算术成立。未运行实现测试。

## 结论

REVISE；P1=5，P2=1。§12 兜底行和账本/施工现场分层方向符合 Issue #37，H10 行未被候选默认路径改动；但下列 P1 使逐字晋级、后续卡验收或开工边界无法按当前合同执行。报告仅给事实与整改动作，开放项由编排收集用户裁决。

## P1（阻断）

### P1-1：DevPlan 验收口径提案扩出了允许行

| 编号 | 级别 | 位置（文件:行/节） | 问题 | 依据 | 整改动作 |
|---|---|---|---|---|---|
| P1-1 | P1 | 候选稿 `:227-255` B-009/B-010、`:375` | 两卡各把一条「验收口径」行改成父行加四条新「机器证」子行，共多写八行；候选影响面却称“仅这两行允许动”。 | `dispatch/README.md`「允许路径」仅授权 DevPlan 两卡的「验收口径」**行**；`brief.md:24`、`task_plan.md:18,124-126` 同样限制。C2 按候选逐字晋级会越界。 | 将每卡归属的四个 ID 和必要判据压入各自**原有的一行**；具体机器证留在设计 A151～A158 或候选说明，不生成 DevPlan 子行。若确需扩行，先由编排改变允许路径合同后再晋级。 |

### P1-2：RLT_24 的事件词与设计、路径合同尚不可同时满足

| 编号 | 级别 | 位置（文件:行/节） | 问题 | 依据 | 整改动作 |
|---|---|---|---|---|---|
| P1-2 | P1 | 候选稿 `:166-169` A155～A158、`:388-391` O-003、`:378` | 默认新增第 20 个控制事件 `resource_close`，却称“全 A 不违反既有闸”：现行 A2 与 §3.3/§3.4 固定 19 词，Issue #37 又要求已有验收 ID 行相对 master 只增不改；O-003 承认 A2 要由 RLT_24 后改，但 RLT_24 明定不动 §12 外设计章节、允许路径也没有 design/01。A157 还要求 RLT_24 改 §12 列，而其允许路径同样没有 design/01。O-003 候选 B 复用 `checkpoint` 则偏离 RLT_11 F-004 裁决及 RLT_24“独立事件位”的现行目标。 | 设计 §3.3/§3.4、`HC-RL-A2`（设计 `:1177`）；DevPlan RLT_24 §目标/非目标/允许路径（`:450-459`）；RLT_11 `findings.md:12,30`；Issue #37「目标」「验收口径」。W1 `findings.md:6` 已登记 §12 路径缺口。 | 把这些现有合同冲突写成明确的待裁前置：确定独立事件词及 A2/§3.4、§12 的合法设计变更由哪一个规划事件承接，并先调整 RLT_24 的允许路径/非目标或本事件的 Issue 边界。O-003 的可选方案均须维持已裁决的独立事件位；未解前不得把“全 A”写成可直接晋级。 |

### P1-3：新事件的 note 协议未冻结，A155/A156 又重叠

| 编号 | 级别 | 位置（文件:行/节） | 问题 | 依据 | 整改动作 |
|---|---|---|---|---|---|
| P1-3 | P1 | 候选稿 `:166-168` A155～A157、`:388-389` O-003/O-004 | A155 只说 `note`“承载”对象类型、标识、outcome 和原因，未给 token 键名、取值分隔/转义、重复键、缺值规则或 `add` 与 `lint` 各在何时拒绝；因此所列正反例不能按同一字节协议执行。A155 已断言 failed 必含原因，A156 又独立断言同一件事，验收归属重叠。O-004 选项 B 将标识与原因放回自由 note，与 RLT_24 的对象标识/失败原因字段目标不一致。 | 设计 §3.2 `note` 现为自由文本、§3.4 现无关闭事件；DevPlan RLT_24 §目标要求对象标识与失败原因字段；`task_plan.md:17` 要求原子条目和正反例/结构检查命令。 | 在候选中给出可解析的最小 wire format（例如明确每个 `note` 键及空值、重复、非法值规则）和 `add`/`lint` 各自的接受/拒绝时点；把原因条件及其正反例只放在 A156，A155 专测事件词、对象与状态机隔离。将会删掉必需字段的方案标明为合同变更前不可选。 |

### P1-4：evidence/11 的逐字块与“按实际填”互斥

| 编号 | 级别 | 位置（文件:行/节） | 问题 | 依据 | 整改动作 |
|---|---|---|---|---|---|
| P1-4 | P1 | 候选稿 `:279-323` B-013 | B-013 被列为待逐字晋级的完整 fenced block，却含“C2 晋级时按候选稿 §1 填”“按实际复核轮次填”等占位句。C2 若填真实复核与裁决，最终 evidence 文件就不含该 block 的逐字原文；若保持逐字，审核证据又是空骨架。 | `task_plan.md:6,74-77,125-126` 要求每个候选 block 逐字晋级，且 `Path(...).read_text().count(<候选 block 原文>) == 1`；Issue #37 要求审核回链真实可查。 | 在晋级前按 fresh 复核和用户裁决将 B-013 更新成可逐字晋级的最终证据文本，并对变更做相应复审；或在计划中明确 B-013 只锁定固定锚点/骨架、实际证据另有完整性校验，不再对占位全文运行逐字块判据。 |

### P1-5：顶部状态提案提前解除两卡依赖闸

| 编号 | 级别 | 位置（文件:行/节） | 问题 | 依据 | 整改动作 |
|---|---|---|---|---|---|
| P1-5 | P1 | 候选稿 `:268-277` B-012 | B-012 拟由 C2 晋级时写“RLT-A-11 已…完成…落盘”“RLT_23/RLT_24 的开工闸随之解除”，但 C2 之后仍须 R 复核、PR、CI 三硬门和合并。该行会在这些门未闭合时宣告依赖解除。 | `task_plan.md:121-128,164-223` 把 C2、R、远端合入分为后续步骤；Issue #37「档位/风险」「收口」规定 PR/CI/合并后收口；DevPlan `:49,597` 写 RLT-A-11 落盘前两卡不开工。 | C2 阶段的顶部句只记录“候选文本已在分支晋级，仍待 R/PR/CI/合并，RLT_23/RLT_24 不开工”；把“正式完成、开工闸解除”的状态回填留给合并后的编排节点，并在该节点核验实际合并与相关授权。 |

## P2（不阻断）

### P2-1：清单分类的两个数量标签不准

| 编号 | 级别 | 位置（文件:行/节） | 问题 | 依据 | 整改动作 |
|---|---|---|---|---|---|
| P2-1 | P2 | 候选稿 `:88,114` | RLT_12 清单标“13 份”但逐行列了 14 个已跟踪路径；RLT_21 的 `review.*.md` 标“六份”，实测是 5 份，另有 `review.md` 1 份。51 路径总数和兜底分类本身正确。 | `git ls-files` 对三目录实测 8+29+14=51；RLT_21 `review.*.md` 5 个，RLT_12 全目录 14 个。 | 把两处标签改为“14 份”和“`review.*.md` 五份及 `review.md` 一份”，保留路径清单与映射结论。 |

### 范围外发现

- W1 `findings.md:6` 已登记 RLT_24 未来设计路径缺口；本报告的 P1-2 将它作为候选 A157 能否兑现的现行前置处理，不改 DevPlan 或其它被审文件。
