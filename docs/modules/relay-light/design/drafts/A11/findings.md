<!-- dh:v1 -->
# RLT-A-11 · 范围外发现（W1）

| ID | 级别 | 事实与来源 | 处置 |
|---|---|---|---|
| W1-F-001 | P2 | DevPlan `RLT_24` 卡第 454 行「变更范围」含设计 §12「删失败怎么办」列，但第 455～459 行 `dh:allowed-paths:v1 task=RLT_24` 只列核心、单测和工作区，未包含 design/01；第 452 行非目标又写「不动 §12 以外的设计章节」。RLT-A-11 当前允许路径虽可在晋级批改 §12，RLT_24 将来执行其卡时的路径合同仍存在字面冲突。 | 本 W1 不改 DevPlan 的「变更范围」或 allowed-paths。转编排在 RLT_24 D-start 前另行裁决/对齐，不得让 RLT_24 越权写 design。 |
| C1c-F-001 | P2 | design/01 `HC-RL-A85`（写入者二分枚举：orchestrator 四类 + monitor 五类 + 全部 agent 事件）在 `resource_close` 加入后陈旧——新事件写入者按 `object_type` 分叉，两类装不下；同族 `HC-RL-A68`/`HC-RL-A89` 时序枚举同成历史口径（新事件任意位置不限次，可辩护性较弱）。O-005 只放行 A2 旧行例外，这三行本事件不可改。 | 登记备查；候选稿 B-020 已加「A85 枚举系 19 词时代口径、法定写入者以 §3.4 表为准」限定句（RV N-002 选项 a）；修订 A85/A68/A89 留后续规划事件。 |
| C1c-F-002 | P2 | DevPlan `RLT_03` 卡「19 个事件词 fail closed」（:185）与 `RLT_22` 卡「19 词白名单」（:415/:432）在 A2 改 20 词后成为历史口径；均属已完成卡的取证/范围记录，不在本事件允许路径。 | 按「不追溯改历史工件」原样保留，不加注（RV N-003，编排已确认）。 |

W1 未发现其他需转派的范围外项；C1c 追加两行见上（RV targeted-02 N-002/N-003 登记）。
