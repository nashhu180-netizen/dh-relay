# RLT-A-05 · RLT-B-05 — RLT_03 阶段合同补充：审核、裁决与确认记录

> 本文件保存 `RLT-A-05`（A-full）与 `RLT-B-05`（B-adjust）两个事件的审核与确认证据，**不属于正式设计输入**；它只登记已经发生并获确认的合同补充，不引入新的产品裁决。
>
> **性质（如实登记）**：这是**已确认合同补充的事件 / 证据补登记**。正文改动先发生——2026-09-10 按用户已确认的 Astra 六项裁决（RLT-A-04 / RLT-B-04 之后的 A128 / A129 / A5 / A59 与 as-built 同步，见 E-070），随后 R29 规划事件闸（E-080、`reviews/closeout-r29-decision-astra.md`）判定该次改动属新的 A-full / B-adjust 实质合同事件，要求补登记 fresh 事件与当前变化集证据。**本文件不声称 marker 先于正文改动存在，也不把本次补登记称为未经授权改产品的失序施工**：内容授权在 E-070 已给，本次登记只补事件与证据。
>
> **替换范围**：`design/01` 的 `RLT-A-02/A-03/A-04` 与 DevPlan 的 `RLT-B-01/B-02/B-03/B-04` 七条原始可解析事件声明注释已按本事件转为工件内的普通历史索引（见文末表）；原 `design/evidence/01`–`05` 未被移动或改写，旧版本在 Git 历史中可还原。

<a id="review-rlt-a05"></a>

<!-- dh:planning-evidence:v1 event=RLT-A-05 artifact=design/01-RelayLight-产品设计与验收.md kind=review -->

## review（A-full）— 设计侧合同补充的独立审核

**事件**：`RLT-A-05`，stage `A-full`；工件 `design/01-RelayLight-产品设计与验收.md`。

**审核对象**（本次正式设计输入被改动的四处，均只收窄/澄清既有合同口径，未新增或改动验收 ID、未改 owner）：

| 位置 | 补充内容 |
|---|---|
| §3.1 退出码 + §11.1 `HC-RL-A5` | 命令边界：**解析级**失败（缺文件 / 缺 marker / 缺表头或表结构不合法）三命令退 `3`；计划**已解析但违反 lint 规则**时 `lint` 退 `2`、`add`/`status` 退 `3`；节点号重复移出本条、归 `HC-RL-A46` 负例；`add` 自身入参非法仍退 `2` |
| §11.1 `HC-RL-A128` | 本卡核心 = 派生活跃计划时忽略 superseded；例外清单不增不减，A46 / A72 / A75 由 RLT_03 逐项证明；`A120` 降为跨卡兼容性引用，其表尾放宽与「两正四反」集成取证归 RLT_09 |
| §3.5 映射行 + 阶段性交付注记、§4.3、§4.5.4、§11.1 `HC-RL-A129` | 产品终态保留；RLT_03 交付**基础 lint**（先忽略 superseded，被 superseded 行隔开的复现当前即通过，被其他活跃 stage 隔断的复现拒绝）；「合法同 stage 表尾追加」的放宽由 RLT_09 / A120 承接，只放宽连续性这一条 |
| §3.5 `add` 入参校验 + §11.1 `HC-RL-A59` | 豁免成员并列四名 `orchestrator#` / `monitor#` / `planner-amend#` / `strategist#`，且**仅**豁免「agent 名属于该节点 agent 表」这一条，其余校验照常 |

**独立审核证据（按发生顺序，均为本仓已落盘材料）**：

1. **合同方向裁决**：`reviews/closeout-contract-decision-astra.md`（派出 E-066 / E-067）。六项裁决 = REVISE：第 1、4、6 项 ACCEPT，第 2、3、5 项 REVISE；报告明确把 A128 的例外分域、A129 的阶段性交付边界定为**职责分域 / 阶段性行为边界**，而非纯勘误，并要求 as-built 逐句语义校正（不得只做 `RLT_04→RLT_03` 字符串替换）。
2. **需求方向原路径复验**：`reviews/closeout-requirement-direction-recheck2-opus.md`（E-071）与 `reviews/closeout-requirement-direction-recheck3-opus.md`（E-075）。E-071 以独立真 CLI 探针逐形态复核 A5（五形态 × 三命令）、A128（含/不含 superseded 的活跃结构与退出码实测相等）、A129（superseded 隔行 `lint` rc=0、活跃隔断 rc=2 `HC-RL-A129`），判四条历史 blocker（A5 / A128 / A129 / `b7f4ecc` 打包边界）全部 CLOSED；其唯一 P1 为 `review.md` 元数据单元格的误枚举，由 E-074 改正后，E-075 终审 `APPROVE`。
3. **一致性原路径复验**：`reviews/closeout-consistency-recheck2-sol.md`（E-072）。`APPROVE`（P0/P1/P2 = 0，P3 = 1 为 DevPlan 抬头阶段文案滞后，不阻塞）；逐项比对 design、DevPlan、as-built、`brief`/`review`/`progress`/`findings` 九成员闭集，确认 A5 / A128 / A129 在正文与实现行为边界上一致，RLT_05 未被提前实现。
4. **R29 规划事件方向裁决**：`reviews/closeout-r29-decision-astra.md`（E-081；报告独立复算 `scan.mjs` SHA-256 `954109826155ba30640468aa164671209535df87ef99bac080b97cc6901fcd23`，直接运行 `derivePlanningEvidence()` 得 design 为 `A-full/design-input`、`substantive=true`、`EVENT_COUNT=3`）。裁决 `VERDICT=REQUIRE_NEW_EVENT`：本次改动包含新的 A-full 实质合同决定（A128 改变本卡何时可称验收条件完整、A129 区分产品终态与阶段性交付、DevPlan 同步改机器验收边界），**不能以 fresh `planning-no-event` 免责**，须补 fresh A/B 事件与当前变化集证据，并保留旧事件历史索引。本文件即按该报告 §3.3 / §4 的字段、锚点与历史索引要求落地。

**审核结论**：设计侧四处补充已获独立复核（合同方向、需求方向、一致性、R29 方向四路）与用户确认；本次只做事件 / 证据补登记，**未改产品行为、未新增或改动验收 ID、未改 owner、未改 as-built 之外的其他路径**。

**本轮未做的事**：未重跑完整收口（`dh gate`），未改 checker、未改 Python / 测试，未动 `design/evidence/01`–`05` 与历史 reviews。

<a id="understanding-rlt-a05"></a>

<!-- dh:planning-evidence:v1 event=RLT-A-05 artifact=design/01-RelayLight-产品设计与验收.md kind=understanding -->

## understanding（A-full）— 需求理解对齐（已有事实登记，无新问答）

**用户直接原话（本节唯一一条逐字引用）**：**「按照 Astra 的决策来」**（记于 `workspace/RLT_03/progress.md` 2026-09-10 E-070 行；对应 design/DevPlan 七个精确路径的 doc-only 同步）。**本节其余条目均为转述或既有记录，不冒充用户逐字原话。**

**主控派单中的授权转述（非用户逐字原话）**：派单文本写「用户已明确授权按 Astra 的 `RLT-A-05` / `RLT-B-05` 精确治理包执行」，并限定只写五个精确路径、不跑完整收口、不改任务状态 / 依赖 / 批次 / allowed-paths。**来源与性质**：本轮主控派单指令文本，是主控的第三人称转述；用户就此的**直接**原话见上一条 E-070 记录，本文件不据此补造问答。

**历史授权链（本项目 A/B 事件一贯的确认形态）**：`RLT-A-04` 侧用户回「嗯，授权」、`RLT-B-04` 侧用户回「你来写入」（出处 `design/evidence/05-交叉审核记录-RLT03与RLT05验收边界.md`）。A04 / B04 授权的是**正式写入**，不含 commit / push；本次沿同一形态只补事件与证据登记。

**解释记录**：Astra 的两份报告已向用户逐项解释六项合同裁决与 R29 四问裁决（含「删旧 marker 改留 fresh no-event 不合理」与「应为 fresh A/B 事件 + 历史索引」的比较）；用户接受后给出上述授权。

**形式化测验的处置（如实登记）**：用户已指示**停止形式化理解测验**，本卡不再就此追加问答。因此本类别只登记**已经发生**的解释与确认，**没有的问答不编造**，也不把 A 侧问答挪作 B 侧证据。

**适用范围**：仅本次「已确认合同补充」的事件与证据补登记；不据此重新裁决产品方向，不扩大 `design/evidence/**` 或 RLT_03 的 allowed-paths，不使 `b7f4ecc` 的一次性例外覆盖本新文件。

<a id="review-rlt-b05"></a>

<!-- dh:planning-evidence:v1 event=RLT-B-05 artifact=dev_plan/P1-RelayLight-开发方案.md kind=review -->

## review（B-adjust）— DevPlan 侧承接、验收分域与依赖/批次审查

**事件**：`RLT-B-05`，stage `B-adjust`；工件 `dev_plan/P1-RelayLight-开发方案.md`。本节核对的是**任务定义面**（承接、owner、批次、依赖、路径授权），不是设计合同文本的复述，也不是 A 审核换标题。

**一、承接关系（以本次正式设计为输入）**：

| DevPlan 位置 | 承接的正式设计口径 |
|---|---|
| `RLT_03` 卡 A5 行 | 「解析级退 3 / 已解析后 `lint` 2、`add`/`status` 3 / 重复号归 A46 / `add` 自身入参非法仍退 2」，并加拒绝路径账本不增行的取证要求 |
| `RLT_03` 卡 A128 行 | 「本卡核心 = 活跃 parser/lint 忽略 superseded」+ A46/A72/A75 逐项；A120 仅作跨卡兼容引用，集成取证归 RLT_09，四项清单不增不减 |
| `RLT_03` 卡 A129 行 | 基础边界先忽略 superseded、活跃 stage 隔断按基础规则拒绝；表尾放宽与运行中追加终态承诺保留，由 RLT_09 / A120 实现取证 |
| `RLT_03` 卡 A59 行 | 四成员并列且仅豁免「agent 名属于该节点 agent 表」，其余校验照常 |
| `RLT_09` 卡目标 + A120 行 | 「A120 的两项连续性放宽由本卡交付」；补齐五条交接断言（superseded 隔行始终通过 / 合法表尾追加「拒绝→通过」/ 四项硬约束保持拒绝及有效编号 / 既有 A46·A72·A75·枚举与依赖回归保持 / 最终由 RLT_16·RLT_19 实跑证明）与「满足全部其他规则、只有表尾位置差异」的合法正例要求；既有 `C1→R1→C2` 多违规 fixture 明记不承诺原样翻绿 |

**二、验收分域审查**：owner 未变——`HC-RL-A128`、`HC-RL-A129` 在 owner 表仍各**恰一次**登记 RLT_03，`HC-RL-A120` 仍**恰一次**登记 RLT_09；`HC-RL-A46` / `A72` / `A75` 未转交 RLT_09。验收 ID 总账仍 122 条、无重复行、本次未新增 ID；新增的只是 RLT_09 的交接断言与合法 fixture 取证义务，属**取证义务细化**而非新验收单元。RLT_03 仅签 parser/lint 与账本写入状态机，RLT_05 签完整 status 生命周期与配置 Recipe，RLT_07 签五阶段模板——分域未被本次同步搅混。

**三、依赖与批次审查**：RLT_03 仍属第 1 批、依赖 `RLT_02`；RLT_09 仍属第 3 批、依赖 `RLT_03`、`RLT_05`、`RLT_07`；RLT_16 / RLT_19 的实跑路径未被删改。任务状态列未改（RLT_03 仍「进行中」、RLT_05 仍「未开始」）。RLT_03 的 `dh:allowed-paths:v1 task=RLT_03` 仍为三条（`tools/relay-light/relay_log.py`、`tools/relay-light/test_relay_log.py`、`docs/modules/relay-light/workspace/RLT_03/**`）——**未加通配、未加 `design/**` 或 `dev_plan/**`、未做目录级放行**；RLT_09 的白名单与 `design/` 禁区条文未改。批次开批条件与端到端 demo 完成点（RLT_12）未变。

**本节「与 HEAD 一致」的独立核验来源**：本轮 **Astra pane 终审**（`VERDICT=REVISE`，P0=0 / P1=0 / P2=2 / P3=0；结论经主控派单转述，报告原文截至本次修正尚未在 `workspace/RLT_03/reviews/` 落仓），**不是** E-072 的覆盖范围——批次表、owner 表与任务索引的逐项核对归属该终审，不倒填给一致性原路径复验。

**四、独立证据**：`reviews/closeout-consistency-recheck2-sol.md`（E-072，`APPROVE`）的**实际覆盖**（依其报告自身记录逐项）：节点四态与 superseded 标记分离、原 RLT_04 / RLT_06 活动 owner 16/16 逐成员语义化、`node_close` 写前校验（RLT_03）与可关闭派生（RLT_05）分域、A59 四成员且仅豁免「agent 名属于该节点 agent 表」、A5 / A128 / A129 正文与 workspace 及当前 Python 行为边界一致、RLT_05 未开始且无提前实现、`b7f4ecc` 五份 patch 独立复算与原 allowed-paths 未扩、七文件闭集与 `git diff --check`。**该报告并未对批次表、owner 表或任务索引做逐项机械比对**——那部分核验属本轮 Astra pane 终审（见上节来源声明），不得倒填给 E-072。另：`reviews/closeout-r29-decision-astra.md`（E-081）§3.1 把「DevPlan RLT_03 / RLT_09 同步改变本卡机器验收边界、给 RLT_09 增加临时限制切换与硬约束守恒的交接判据」列为 B-adjust 实质，并在 §4 规定后续执行包「记录此次补充，**不改任务状态/依赖/批次/allowed-paths**」——本次执行与该边界一致。

**五、结论**：DevPlan 侧改动属**已确认合同补充的同步**，未改变任务编排事实（任务集合、owner、批次、依赖、允许路径均不变；该「与 HEAD 一致」的核验来源见本节上方的 Astra pane 终审归属声明）；本登记为其 B 侧事件与证据。

<a id="understanding-rlt-b05"></a>

<!-- dh:planning-evidence:v1 event=RLT-B-05 artifact=dev_plan/P1-RelayLight-开发方案.md kind=understanding -->

## understanding（B-adjust）— 授权范围与理解对齐

**主控派单中的授权转述（非用户逐字原话）**：派单文本写「用户已明确授权按 Astra 的 `RLT-A-05` / `RLT-B-05` 精确治理包执行」，并同时给出五项限制：只写 `design/01`、DevPlan、本证据文件、`progress.md`、`review.md` 五个精确路径；不改任务状态 / 依赖 / 批次 / allowed-paths；不改 Python、测试、as-built、brief、task_plan、findings、lesson_candidates、历史 reviews 与 `evidence/01`–`05`、checker、R14 格式；不派活、不问用户、不 commit / verify / merge / push / 清理、不开 RLT_05；不自行跑完整收口；不在未跑完体检前宣称 E10 可用。**来源与性质**：本轮主控派单指令文本（第三人称转述）；用户直接原话见上文 A-full understanding 段的 E-070 记录，本文件**不据此补造问答**。

**内容授权来源**：E-070 已记录用户对 Astra 决定与七个精确路径的授权，**无须就同一产品内容重复询问**；本次授权覆盖的是**事件 / 证据补登记**与**一个新证据路径**的写入。`b7f4ecc` 的一次性既有 patch 例外**不**覆盖本新文件；本授权也不扩成 `design/evidence/**` 通配或新增 RLT_03 的 allowed-paths。

**时序与范围声明**：B 侧正文改动**先于**本 marker 发生；本文件不声称先登记后落盘。本次登记只把已经发生并获确认的合同补充记入事件账，不重做施工、不推翻已完成的实现与复核。

## 七条旧规划事件的历史索引（非活动声明）

> 下表为普通文字索引，**不包含可解析的事件声明注释**；七条事件的原始注释已按 `RLT-A-05` / `RLT-B-05` 转为工件内的同类索引。原证据文件未移动、未改写，旧版本可由 Git 历史还原。

| 事件 | stage | 工件 | 证据（交叉审核记录） | 原确认形成 |
|---|---|---|---|---|
| `RLT-A-02` | A-full | `design/01-RelayLight-产品设计与验收.md` | `evidence/01-交叉审核记录-RelayLight运行中改计划.md`（`#review-rlt-a02` / `#understanding-rlt-a02`） | 运行中改计划专题的 A 审核与确认 |
| `RLT-A-03` | A-full | 同上 | `evidence/03-交叉审核记录-RelayLight仓内skill单源.md`（`#review-rlt-a03` / `#understanding-rlt-a03`） | skill 仓内单源 A 增补（RLT-A-03） |
| `RLT-A-04` | A-full | 同上 | `evidence/05-交叉审核记录-RLT03与RLT05验收边界.md`（`#review-rlt-a04` / `#understanding-rlt-a04`） | 2026-09-10 用户「嗯，授权」；总账 121→122 |
| `RLT-B-01` | B-new | `dev_plan/P1-RelayLight-开发方案.md` | `../design/evidence/02-交叉审核记录-RelayLight-B拆计划.md`（`#review-rlt-b01` / `#understanding-rlt-b01`） | 首次 B 拆计划与用户整版确认 |
| `RLT-B-02` | B-adjust | 同上 | `../design/evidence/03-交叉审核记录-RelayLight仓内skill单源.md`（`#adjust-rlt-b02` / `#understanding-rlt-b02`） | 2026-09-10 用户确认仓内 skill 单源 v6 |
| `RLT-B-03` | B-adjust | 同上 | `../design/evidence/04-交叉审核记录-RelayLight-B03颗粒度优化.md`（`#review-rlt-b03` / `#understanding-rlt-b03`） | 2026-09-10 用户确认颗粒度优化 v2.1（卡数 20→17） |
| `RLT-B-04` | B-adjust | 同上 | `../design/evidence/05-交叉审核记录-RLT03与RLT05验收边界.md`（`#review-rlt-b04` / `#understanding-rlt-b04`） | 2026-09-10 用户「你来写入」 |

## 引用与来源

- 合同方向裁决：`workspace/RLT_03/reviews/closeout-contract-decision-astra.md`
- R29 规划事件方向裁决：`workspace/RLT_03/reviews/closeout-r29-decision-astra.md`（`VERDICT=REQUIRE_NEW_EVENT`）
- 需求方向原路径复验与终审：`closeout-requirement-direction-recheck2-opus.md`（E-071）、`closeout-requirement-direction-recheck3-opus.md`（E-075）
- 一致性原路径复验：`closeout-consistency-recheck2-sol.md`（E-072）
- 施工与登记事实：`workspace/RLT_03/progress.md` 的 E-070（合同同步）、E-071 / E-072 / E-074（复验与元数据修正）、E-075（需求方向终审）、E-080 / E-081（R29 探针与裁决登记）、E-083（本次事件/证据补登记）
- 用户授权：E-070 记录的「按照 Astra 的决策来」；本次派单记录的「已明确授权按 Astra 的 `RLT-A-05` / `RLT-B-05` 精确治理包执行」
