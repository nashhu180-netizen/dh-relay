<!-- dh:v1 · fresh-context B-adjust 独立审核 · 审核对象：dev_plan/drafts/RLT-B-08-RLT25-RLT26-调整候选.md v1 -->
<!-- 注：本文件不写 dh:planning-evidence 标记——事件号本身（RLT-B-08 首登 / 跳号 B-09）是候选 §0 的待裁决项，按候选自己「提议不等于登记」的口径，事件标记应在落盘时按裁决结果补写，此处不预造登记事实。 -->

# RLT-B-08（拟定）RLT_25/RLT_26 调整候选 · 交叉审核记录

## 一、审核身份与输入

- **审核者**：fresh-context 独立审核（本棒未参与候选起草；审核模型为 **Devin SWE-2 Max**——以 flourish-apricot 导出会话 JSON 的 `model_name`/`generation_model` 元数据为准，SessionStart 出现的 claude-fable 文案系过期 hook，非实际模型）。
- **审核对象**：`dev_plan/drafts/RLT-B-08-RLT25-RLT26-调整候选.md` v1（本 worktree `plan/RLT_25-RLT_26` 内唯一新增文件，`git status` 核实无其它改动）。
- **审核输入（只读核对，全部落实）**：
  - 仓根 `AGENTS.md`（宪章#5 normal=代码轮1+需求方向+教训三路；GitHub 协作流程；worker 铁律）。
  - `design/README.md`（designInputs 唯一指向 `design/01`）与 `design/01-RelayLight-产品设计与验收.md`（§3.4 `resource_close` 行 L257、§6.3 配置样例 L789-831、§11.1 验收表、§12、头部 RLT-A-11 晋级条 L21）。
  - `dev_plan/P1-RelayLight-开发方案.md`（`planning-event` 活动声明=RLT-B-06；§0 事件条；§3.1/§3.2 任务表；§4 批次表止于第 6 批；§6 对照表；§8.1/§8.3/§9；`dh:status` 块）。
  - GitHub Issue #49（RLT_25 户）、#50（RLT_26 户）、#48（RLT_13 户，两者 `Relates to` 对象）——均 OPEN，经 `gh issue view` 只读核对。
  - `wt/RLT_13` 工作树 `workspace/RLT_13/findings.md`（F-001/F-002 均 P2、「已登记·范围外」，表尾「本表不授权补卡」）；本仓 `workspace/RLT_22/findings.md` F-005（遗留→下一卡）、F-010（遗留→下一卡）；`workspace/RLT_24/findings.md` F-C1-01/F-C1-03/F-C1-04。
  - 事件号事实：`design/evidence/09` L42/L80 存在正式 `dh:planning-evidence event=RLT-B-07` 标记（review+understanding 各一），DevPlan §0 有 RLT-B-07 条而历史索引仅收 B-01~B-05；`design/drafts/A09-...候选.md` §8.5（L425）曾提议 `RLT-B-08` 承接 RLT_22 的 DevPlan 侧调整，`design/evidence/10` L79/L150 明写「卡号/批次/依赖仍待 B 侧另行决定」——**提议存在、登记未发生**，候选 §0.1 事实陈述全部成立。
  - 实现与文档落差：`tools/relay-light/relay_log.py` 已含 `resource_close`（L42/103 等），而 `tools/relay-light/skill/` 与 `docs/modules/relay-light/as-built/` 全文 grep 零命中；as-built `RLT_05-实现快照.md` L32 仍写「19 词表」、L64/L71 trigger 仅三态（无 `on:review_ready:`）、L116 与 `tools/relay-light/skill/SKILL.md` L151 仍写「两套计数」、`SKILL.md` L168 `stage_result` 行仍是「在该实例全部节点 `closed` 之后」旧口径（对照 `HC-RL-A137` 的 blocked/failed 允许未关）。
  - 权限边界：本审核只读；产出仅限本文件；未改候选、正式 DevPlan、design 正文、代码或任何其它文件；未 commit/push/PR/verify。

## 二、方案问题

> 分级口径沿用本仓 findings 惯例：P0 阻塞/数据安全，P1 阻塞目标，P2 质量/证据缺口，P3 后续不阻塞。

- **P2-1 ｜ §3 落盘清单的簿记清扫漏了三处同型失修点**（候选 §3.7 只列了 §6 缺行与脚注）。
  - ① `dev_plan` §6 对照表 `HC-RL-A2` 行 owner 仍写 `RLT_03`（L617），但 RLT-A-11 已把 A2 修订为 20 词并改派 `RLT_24` 承接（design/01 L21、§11 A2 行「实现由 RLT_24 承接」、§3.1 RLT_24 行亦写 A2）——候选 §4 称「A2/A155~A158 仍归 RLT_24」与 §6 表现状矛盾，补登时应把 A2 行一并校正或注明新旧承接关系，不能只断言行owner不变。
  - ② `dev_plan` §1（L55）「承接其 125 条 `HC-RL-A*` 与 15 条 `HC-RL-H*`，共 140 条」——RLT-A-11 后活动总账为 **148**（AI 133 + 人验 15）。
  - ③ `dev_plan` §9（L739）「前四批交付 122 条机器验收与 13 条人验；第 5 批 RLT_18 补齐 …总账 140 条」——同样停在 140，且 A151~A158 八条的归属批次是**第 6 批**，「前四批/第 5 批」的二分口径本身已失修。
  - 影响：若照现清单落盘，DevPlan 将自相矛盾（§8.1 报 148/0 漏项 vs §1/§9/§6 残留 140 口径）。修复纯机械，属候选自己立下的「簿记更正」同类，应在 v2 或落盘动作清单中补齐。
- **P2-2 ｜ RLT_25 的 A-adjust 前置被写成无条件硬前置，强于 Issue #49 的字面条件**。Issue #49 原文是「**如**目标口径需要改正式设计，先走独立 A-adjust」；候选 §1 写「A-adjust **恒为**硬前置」，理由是目标取值必然落在 design/01 §6.3/A115 或仓根宪章二者之一的修订上。候选论证在「取值本身是合同裁决、不该由施工卡反推」这一层成立、方向是更严而非更松，**不构成偷做产品决定**；但它没把另一路径摆出来：若用户裁决「维持两路、只修 AGENTS 与映射、design/01 一字不动」，按 Issue 字面并不需要独立 A 事件，裁决可折叠进本 B 事件的确认或一次轻量澄清。应作为选项交用户，而非只剩「恒为硬前置」一种形态（见 §四-2）。
- **P3-1 ｜ F-010 并入超出 Issue #50 字面范围**。Issue #50 范围只写 `resource_close`/19 词残留同步；F-010 是同一份 as-built 的 trigger 四态/三套计数滞后（RLT_22 交付物，非 RLT_24 合同）。候选处置本身正确——第六条机器证明标「并入项，待用户裁决」、§5 列开放项、「不批则整体剔除」有退路；仅提示：若批并入，落盘时应在 Issue #50 或 DevPlan 卡行补一笔范围注记，避免事后稽核出现「卡做了 Issue 没写的事」。
- **P3-2 ｜ 若 §0 采方案 B（跳号 `RLT-B-09`），候选文件名仍含 `RLT-B-08`**。形成史文件可不改名，但建议落盘清单补一句「文件名与最终事件号差异」说明，与方案 A 的三处留痕同理防歧义。

未发现 P0/P1。

## 三、用户理解风险

1. **「B-08」像一个用过的号，其实从未登记**。A09 候选 §8.5 提过它，用户可能误以为已发出。方案 A 靠三处留痕消歧；若用户在意同号双出处的追溯成本，方案 B（跳号）更干净——这正是候选把选择权留下的原因。
2. **确认本候选 ≠ 定下 normal 复核几路**。两路还是三路由前置 A 事件交用户裁决；RLT_25 在裁决落地前不能 D-start，时间上有真实等待，不是「确认完就能开工」。
3. **F-010 并入是可选项不是默认**。不批就整体剔除另立承接，不影响 RLT_26 前五条成立；批了才构成范围扩张。
4. **两卡落盘 ≠ 开工、更 ≠ 完成**。D-start、commit/push/PR/合并、verify、skill 两侧用户级副本重同步全是独立闸，逐项另取授权。
5. **「指定 Devin swe-2-max」只绑施工实例**。复核实例按冻结 Recipe 另派、施工者不自核——不是全链路都换成 Devin。

## 四、需要用户决定的问题

1. 事件号：方案 A（`RLT-B-08` 首登，附三处留痕）还是方案 B（跳号 `RLT-B-09`，B-08 永久留空备注）。
2. RLT_25 前置 A 事件：拟定号 `RLT-A-12` 与范围；**目标 Recipe 取值由该事件交用户裁决**。新增子选项——是否保留「若裁决不动 design/01 则免独立 A 事件」的条件化出口（对应 P2-2）。
3. F-010 是否并入 RLT_26；若并入，是否同步在 Issue #50 注记范围（对应 P3-1）。
4. RLT_25 是否升级为第 3 批实跑卡（RLT_14/15/16/19）的硬前置——候选建议不升、仅记口径风险。
5. §3.7 簿记补登是否随本事件落盘，**并纳入 P2-1 补出的三处**（§6 A2 行、§1 L55、§9 L739）。

## 五、逐项结论

- **忠实用户要求：成立**。RLT_13 两个 P2（F-001→Issue #49→RLT_25，F-002→Issue #50→RLT_26）一一对应；wt/RLT_13 findings 原文已核，「已登记·范围外」「本表不授权补卡」的出处标注准确，候选对未合入分支事实保留了「目前在 `wt/RLT_13` 上」标注。
- **是否偷做产品决定：未偷做**。normal 目标路数明确列为非目标、交 A 事件裁决；事件号给 A/B 双方案；F-010 并入、簿记补登、批次升级全部挂用户裁决或确认；施工模型绑定范围照用户原话（仅施工）。
- **验收回链正式设计稳定 ID：成立**。RLT_25 引 §6.3+A115/A116/A92/A117——A115 确载 normal=requirement+lesson、A116 管 R 实例集合匹配 lint、A117 管档位唯一来源、A92 管映射四类装载，均存在且语义对位；RLT_26 引 A2（20 词白名单）/A155~A158（wire format、reason 条件、§12 取证、向后兼容）/并入项 A137/A144~A150，均存在；§4 声明不新申领/不退役/不改号、活动总账保持 148 与设计一致。
- **卡粒度/依赖/批次/事件号处置：成立**。两卡各一个可独立实现-证明-签署的验收单元；RLT_25←前置 A 事件（见 P2-2 表述分歧）、RLT_26←RLT_24（已合入验收，满足）；新增第 7 批两卡并行互不依赖，与在制 RLT_13 允许路径（`workspace/RLT_13/**`、`config-fixture/**`、`relay/**`）无文件重叠；事件号处置不伪造历史、不动既有证据原文。
- **允许路径与停止边界：足够**。RLT_25 允许路径覆盖 `skill/**`（含 dh-mapping.toml）、`AGENTS.md`、两份测试与工作区，显式排除 `design/**`（A 事件领域）、dev-harness 仓、`relay_log.py`（默认排除+停止边界兜底）——与「lint 守门已实现、本卡只对齐配置与文档」的定位自洽；RLT_26 覆盖 skill 三文件+as-built 快照+测试+工作区，排除实现/设计/账本/其他时点文档/用户级副本。两卡停止边界均含「交用户裁决」出口且回链 Issue 原文。
- **Devin swe-2-max 绑定：合规**。仅施工实例固定 swe-2-max，复核按 Recipe 另派，明写「施工者不复核自己的卡」，与 Issue 及宪章#5 一致。
- **范围扩张：受控**。F-010 并入与簿记补登均为用户可否决项（P3-1 范围注记建议）；无未申报的顺手改动。

## 六、最终结论

**REVISE（无 P0/P1；P2×2 + P3×2）**。

候选主体忠实用户要求、未越权做产品决定、验收回链与依赖/批次/允许路径/停止边界均成立，可以进入主会话裁决。两处 P2 都是可机械修复的点：① §3 落盘清单补上 §6 A2 owner 行、§1 L55、§9 L739 三处失修簿记；② 把 RLT_25 前置 A-adjust 的「条件化出口」补成用户可选（而非写死恒为硬前置）。两处 P3 为落盘注记类建议。均不需要推翻卡设计，整改后主会话可按 §四逐项呈用户裁决。

**本次审核唯一改动路径**：`docs/modules/relay-light/design/evidence/12-RLT-B08-RLT25-RLT26-交叉审核.md`（新建，本文件）。

---

## 七、定向复审 v2（2026-09-20 · 同审核者 fresh-context）

**复审对象**：`dev_plan/drafts/RLT-B-08-RLT25-RLT26-调整候选.md` **v2**（头部状态行自引本文件、声明逐项整改）。逐条核验 v1 审核发现的闭合情况：

- **P2-1（§3 落盘清单簿记漏项）→ 闭合**。v2 §3.7（L95-101）扩为五处簿记补录包且明标「逐项均可被用户否决、不默认落盘」：§6 缺 A151~A158 八行、§6 `HC-RL-A2` 行 owner 仍为 `RLT_03`（L617）需校正或注新旧承接、§6 脚注 125+15→148、§1 L55 140→148、§9 L739 总账与「前四批/第 5 批」二分口径重述——v1 点名的三处漏项全部入列。§4（L110）原误述「A2/A155~A158 仍归 RLT_24」已纠正为「A2 的 §6 行现仍写 `RLT_03`……行值滞后属 §3.7 簿记校正项」，与表现状一致。§5-O5 同步收编。
- **P2-2（A-adjust 无条件硬前置）→ 闭合**。v2 §1 依赖/批次（L51）改为按 Issue #49 字面条件化：用户裁决目标 Recipe 恒为前置；裁决需改 `design/01`/验收命题 → 独立 A-adjust（拟 `RLT-A-12`）作 D-start 硬前置；裁决维持现两路 → 免独立 A 事件但裁决记录本身作 D-start 前置证据。§4（L112）、§5-O2（L117，条件化/无条件两案并列且不预判两路/三路）、§3.10（L104 §8.3 措辞）口径一致。
- **P3-1（F-010 并入超 Issue #50 字面范围）→ 闭合**。v2 §2 第六条机器证（L70）补「落盘时 DevPlan 卡来源行写『并入 RLT_22 F-010 同族残留』、Issue #50 注记/评论须当次授权后补记」；§3.9（L103）与 §5-O3（L118）呼应，退路「不批则整体剔除」保留。
- **P3-2（跳号时文件名歧义）→ 闭合**。v2 §0.2 方案 B（L21）与 §3.8（L102）写明「文件名保留 `RLT-B-08` 作形成史不改名，落盘补文件名与最终事件号差异说明」。
- **新引入 P0/P1 检查：无**。v2 新增内容均为条件化表述、注记义务与开放项选项化，未新增验收 ID、未改动卡边界与允许路径、未把任何扩项改为默认。
- **残余 P3（不阻塞）**：v2 §1 人判行（L40）仍写「由前置 A-adjust 的用户裁决承载」、§3.4 索引行依赖列（L92）仍写「前置 A-adjust（拟 `RLT-A-12`）」——两处未同步 §1/O2 的条件化口径（条件化下前置未必是 A 事件）。建议落盘时按 O2 裁决结果机械改写为「用户裁决记录或 A-adjust」，不另起复审轮次。

**复审结论：PASS**。v1 的 P2×2 + P3×2 全部闭合，无新 P0/P1；唯一残余为上述措辞未同步的 P3，属落盘期机械修正。候选可进入讲解与用户确认环节。

**本次复审唯一改动路径**：`docs/modules/relay-light/design/evidence/12-RLT-B08-RLT25-RLT26-交叉审核.md`（本文件内编辑：§一 审核者模型身份更正 + 本节追加；候选、DevPlan、design 正文、代码与其它文件均未动，无 Git/远端动作）。

## 八、主会话裁决与正式登记（2026-09-20）

<a id="review-rlt-b08"></a>
<!-- dh:planning-evidence:v1 event=RLT-B-08 artifact=dev_plan/P1-RelayLight-开发方案.md kind=review -->

- 本锚点引用上文 §七 v2 定向复审 PASS，不新增或冒充一次独立审核。主会话采纳 v1 四项整改及剩余措辞修正。
- 落盘机械核对发现候选统计沿用 19 卡，实际 §3.1 已有 21 个唯一任务 ID（含第 6 批 RLT_23/24）。新增后为 23 卡；按实际 ID 修正 §4/§8/§9，候选保留形成史。不增减任何额外卡、依赖或验收。
- §6 补 A151～A158 八个 ID（四行双列），A2 owner 校正为 RLT_24；§1/§6/§8/§9 活动总账 133 AI + 15 人验 = 148，不新增 ID。

<a id="understanding-rlt-b08"></a>
<!-- dh:planning-evidence:v1 event=RLT-B-08 artifact=dev_plan/P1-RelayLight-开发方案.md kind=understanding -->

- **讲解输入**：用户本轮引述的六项推荐方案，以及候选 v2 与独立审核 PASS。
- **用户确认原话**：「确认」。作用对象为所引六项方案，不能扩张成尚未展示的 A-adjust 整版确认。
- **O1**：RLT-B-08 首次正式登记；旧 A09 提议未登记，不补造历史。
- **O2**：normal 目标三路 = code-round1 / requirement / lesson；先走独立 A-adjust 修订正式设计，RLT_25 等其整版确认落盘后才施工。
- **O3**：RLT_26 并入 RLT_22 F-010，同步 trigger、计数与 stage_result；同次授权 Issue #50 补范围备注。
- **O4**：RLT_25 不作旧第 3 批硬前置，仅登记风险。
- **O5**：批准五处机械簿记包；卡数按实际 21→23 校正，理由见上。
- **独立 D-start 授权**：两卡正式落盘后立即以 Devin SWE-2 Max 在独立 worktree 施工 RLT_26。RLT_25 尚待 A-adjust；不把 RLT_26 的启动授权推广到 RLT_25。
- **停止线**：本次不授权 commit、push、PR、合并、verify、部署、用户级 skill 重同步。两卡共用 skill 路径，后续施工错开或合入后重核基线，禁止同时覆盖。
- **保存位置**：正式登记先保留在规划分支 plan/RLT_25-RLT_26；未提交规划文件作为授权合同快照复制到独立 wt/RLT_26，worker 只读该快照，不改 DevPlan 或规划证据。两树均保留，合入与清理另行授权。

### 后续 Git 授权（2026-09-20）

用户在正式登记准备后追加明文「做之前 代码先提交推送」。本次只对 RLT-B-08 规划相关精确三路径执行 commit/push，远端成功后才启动 RLT_26；其他任务树 WIP 不纳入。前述「不授权 commit/push」为当时边界，本条覆盖该两项，PR/合并/verify/skill 同步仍未授权。规划合同以本次提交固定，RLT_26 自 master 建树后读取这一版本；不删除规划树。

- Issue #50 范围备注已发布：https://github.com/nashhu180-netizen/dh-relay/issues/50#issuecomment-5747218816 。落盘结构检查：23 个唯一任务 ID、148 个唯一验收 ID、7 个批次、1 个活动 B 事件；git diff --check 通过。
