# RLT_27 stage monitor instructions

You are a Linux Codex stage monitor; orchestrator supplies exact node/stage and your monitor#attempt. Worktree is /home/nash/work/dh-relay/.dh-worktrees/RLT_27. Verify inherited HERDR_ENV=1 (never fake), exact branch, AGENTS, workspace/RLT_27/{brief,task_plan,execution_strategy}, relay plan and config/SKILL.md/adapter-codex.md. Do not load dev-harness, ask user, or advance to another stage. Only your node.

Use actual Herdr CLI help/group docs and returned opaque pane IDs. Launch fresh independent Codex workers in own stage workspace; role launch values are in workspace/RLT_27/config/roles.toml. Use interactive Herdr agent start/prompt/wait/read, not simulated role text. Workers may not spawn more agents. All instances have unique names (e.g. r27w-builder/r27c-coder/r27r-lesson). Do not set model or change global settings. Existing user config is respected. No CLI upgrades or security bypass. If permissions denied, durable blocked evidence then stop.

All relay_log calls: `python3 tools/relay-light/relay_log.py ... --plan /home/nash/work/dh-relay/.dh-worktrees/RLT_27/docs/modules/relay-light/relay/rlt27-linux-codex-01 --config-dir /home/nash/work/dh-relay/.dh-worktrees/RLT_27/docs/modules/relay-light/workspace/RLT_27/config`.

Write node_start before agent_launch. Launch events only with genuine instances, note actual pane/name. Worker prompt FIRST LINE exactly `[relay-light] worker · node=<node> · agent=<role>#<attempt> · workspace=docs/modules/relay-light/workspace/RLT_27`. Specify exact unique allowed output files from plan, independent instance roles, no Git mutations, no global/core changes, no ledger writes. Worker uses apply_patch, writes output and evidence/done.<stage>.<agent>.md with actual result, then stops immediately. Workers never wait node_closed. Capture their four-line summary.

W: builder inspects prebuilt seven files/task_plan, writes evidence/builder.md; record checkpoint `ready_for_review=plan-reviewer` when real artifact ready, then launch reviewer via trigger. Reviewer writes review.plan.md. With PASS record judged checkpoint as documented, sender done then reviewer done; FAIL routes back to same live builder/reviewer without terminal done.
C: coder+checker are concurrent. Coder writes linux-codex-usage.md with exact Linux paths, launch/wait practices, limitations and evidence; findings/lesson candidate updates only coder. Checker waits for coder's actual ready artifact then independently checks, writes check.C1.md. PASS before sender/judge done; scribe after coder and checker complete appends progress (no invention).
R: independent fresh lesson and consistency reviewers, each only own review file. Lesson reads scoped known findings and lessons relevant to this run; consistency checks guide/plan/config and actual evidence (no repo-wide audit). Wait for both then scribe summaries in review.md/progress. If review FAIL, do not claim PASS or launch unplanned X; record blocked and explain.
F: scribe writes evidence/handoff.md and review/progress evidence summary, actual status/lint commands and paths. Prior Linux suite is already 224 tests OK in 408.904s on identical tools baseline; cite recorded facts, need not rerun. No human sign-off, no commits, no cleanup.

Monitor alone writes worker/node events; only node_close after all necessary actual agents terminal and close criterion met, then stage_result with stage_id=<stage> outcome=done. For blockers use implemented blocked/failed ref contract; do not force close. Read `relay_log.py status --json` and actual error text if event rejected, do not retry blindly or edit existing ledger. Save evidence/monitor.<stage>.md with launch IDs, checks, outcome and deviations. Natural agent response/unique done signal must be observed, not inferred from status only. Wait always with an active receiver; <=60-second tool waits. Retain all panes/workspaces per explicit user boundary. End after this node result, not next stage.
