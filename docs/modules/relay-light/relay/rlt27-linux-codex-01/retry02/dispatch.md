# RLT_27 second isolated attempt

Coordinator instruction within existing user authorization, not a new task/scope. First attempt is truly blocked at W1 seq10 after reviewer found coordinator task_plan error. Preserve original ledger/outputs and original monitor report. Do not resume old builder or rewrite old events.

Corrected task_plan has R FAIL -> blocked/stop, no automatic X or plan amendment. This was already dispatch-monitor's rule; scope remains W/C/R/F minimal. This retry is needed due coordinator authoring error; count/document it as intervention, never claim zero-intervention.

You remain Linux Codex orchestrator in the same authorized Herdr session rlt27-linux-codex-01, but start a NEW ledger under absolute plan-dir `/home/nash/work/dh-relay/.dh-worktrees/RLT_27/docs/modules/relay-light/relay/rlt27-linux-codex-01/retry02`. Do not touch outer ledger.

Run workspace for this attempt: `docs/modules/relay-light/workspace/RLT_27/retry02` (seven documents already copied with corrected task_plan). This replaces workspace/RLT_27 in original dispatch for every worker, evidence, guide, progress, review and completion signal. Config-dir remains outer `/home/nash/work/dh-relay/.dh-worktrees/RLT_27/docs/modules/relay-light/workspace/RLT_27/config` (five files unchanged from first attempt). Use retry relay_plan output paths.

Use fresh monitors and workers with unique names prefixed r27b-, fresh stage workspaces labelled RLT27-retry02-W/C/R/F. Existing original workspaces/agents are retained, not restarted or reused as workers. Mark plan_loaded/stage_start/monitor_launch and route exactly as first dispatch, using only retry plan-dir.

Every monitor prompt must explicitly override original dispatch-monitor's plan/workspace paths with these retry paths and read corrected retry02/task_plan. Only generic role and lifecycle rules from original dispatch-monitor apply; its literal original plan/workspace paths MUST NOT be used for this attempt. Original instructions prohibiting core/global Skill/Git/cleanup still apply.

W builder observes seven prebuilt retry docs; plan-reviewer reviews corrected R FAIL boundary. C writes retry02/linux-codex-usage.md. R independent lesson/consistency. F records all results in retry02/evidence/handoff.md; actual successful run needs status/lint and real independent instances. Stop on true blockers, do not invent success. Retry uses same approved tools baseline and dedicated config; do not rerun the 408-second unchanged baseline suite.

At end preserve original failed attempt and new result, include both in final evidence and intervention count. No commit/push/merge/verify/delete. Coordinator corrected CRLF->LF on transferred docs before any worker edits; that was transfer formatting only, not production change. Default Codex launched in existing user YOLO mode; no permission mode was altered by this run, and restricted-sandbox operation is not proven.
